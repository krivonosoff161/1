# АНАЛИЗ ПРОБЛЕМЫ ЧАСТИЧНОГО ЗАКРЫТИЯ И РАСЧЕТОВ ОТ БАЛАНСА (29.12.2025)

**Дата:** 29.12.2025  
**Лог файл:** `logs/futures/futures_main_2025-12-29.log`  
**Статус:** Анализ завершен

---

## 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА

### Проблема: Частичное закрытие идет в минус

**Из логов:**
```
2025-12-29 22:33:31.401 | WARNING | ⚠️ [PARTIAL_TP_BLOCKED] BTC-USDT: Чистая прибыль $-0.09 меньше минимума $0.50 (gross_pnl=$0.16, commission=$0.25)
```

**Детали:**
- Gross PnL: **$0.16** (положительная прибыль)
- Commission: **$0.25** (комиссия больше прибыли!)
- Net PnL: **$-0.09** (убыток!)

---

## 🔍 АНАЛИЗ РАСЧЕТОВ

### 1. Данные позиции из логов:

**Из логов:**
```
98: ✂️ Частичное закрытие BTC-USDT long: текущий размер=0.28 контрактов, закрываем 50% (0.140000 контрактов)
92: 🔍 [PROFIT_DRAWDOWN] Начало для BTC-USDT | size=0.28, side=long, entry=88964.3000, current=89083.2000
```

**Параметры:**
- Символ: BTC-USDT
- Сторона: LONG
- Размер позиции: **0.28 контрактов**
- Размер закрытия: **0.14 контрактов** (50%)
- Entry price: **88964.30 USDT**
- Current price: **89083.20 USDT** (markPx)
- ctVal (BTC-USDT): **0.01** (1 контракт = 0.01 BTC)

**Расчет размера в монетах:**
- `close_size_coins = 0.14 * 0.01 = 0.0014 BTC`

---

### 2. Расчет Gross PnL (из кода):

**Код (position_manager.py:6631-6634):**
```python
if side.lower() == "long":
    partial_pnl = close_size_coins * (current_price - entry_price)
else:  # short
    partial_pnl = close_size_coins * (entry_price - current_price)
```

**Расчет:**
- `partial_pnl = 0.0014 * (89083.20 - 88964.30)`
- `partial_pnl = 0.0014 * 118.90`
- `partial_pnl = 0.16646 USDT` ≈ **$0.16** ✅ **СОВПАДАЕТ С ЛОГАМИ**

---

### 3. Расчет Commission (ПРОБЛЕМА!)

**Код (position_manager.py:6651-6654):**
```python
# Комиссия за вход + выход для частичного закрытия
partial_commission = (
    close_size_coins * current_price * commission_rate * 2
)
```

**Проблема:**
- Комиссия умножается на **2** (вход + выход)
- Но комиссия за **вход** уже была уплачена при открытии **всей позиции**!
- Для частичного закрытия нужно считать только комиссию за **выход**!

**Расчет (неправильный):**
- `commission_rate = 0.001` (0.1%)
- `partial_commission = 0.0014 * 89083.20 * 0.001 * 2`
- `partial_commission = 0.0014 * 89083.20 * 0.002`
- `partial_commission = 0.0014 * 178.1664`
- `partial_commission = 0.24943 USDT` ≈ **$0.25** ✅ **СОВПАДАЕТ С ЛОГАМИ**

**Правильный расчет должен быть:**
- `partial_commission = close_size_coins * current_price * commission_rate` (только выход!)
- `partial_commission = 0.0014 * 89083.20 * 0.001`
- `partial_commission = 0.0014 * 89.0832`
- `partial_commission = 0.12472 USDT` ≈ **$0.12**

**Net PnL (правильный):**
- `net_partial_pnl = 0.16 - 0.12 = $0.04` ✅ **ПРИБЫЛЬ!**

---

## 🔍 ПРОБЛЕМА С БАЛАНСОМ

### Проблема: `profile=unknown`

**Из логов:**
```
109: 🔍 [PARTIAL_TP_MIN_VALUE] BTC-USDT: close_value=$124.71, min_value=$2.50, profile=unknown
```

**Причина:**
- `balance_profile` не определяется правильно
- Код пытается получить баланс через `await self.client.get_balance()`
- Но `balance_profile` возвращается как `None` или без `name`

**Код (position_manager.py:6568-6573):**
```python
balance = await self.client.get_balance()
balance_profile = None
if hasattr(self, "config_manager") and self.config_manager:
    balance_profile = self.config_manager.get_balance_profile(balance)
```

**Проблема:**
- `get_balance_profile()` может вернуть словарь без ключа `name`
- Или `balance_profile` может быть `None`

**Из логов видно:**
- Баланс: **$810.63**
- Profile должен быть: **small**
- Но в логе показывается: **profile=unknown**

---

## 📊 МАТЕМАТИЧЕСКИЙ АНАЛИЗ

### Текущий расчет (неправильный):

```
Gross PnL = close_size_coins * (current_price - entry_price)
          = 0.0014 * (89083.20 - 88964.30)
          = 0.0014 * 118.90
          = $0.16 ✅

Commission = close_size_coins * current_price * commission_rate * 2
           = 0.0014 * 89083.20 * 0.001 * 2
           = 0.0014 * 178.1664
           = $0.25 ❌ (НЕПРАВИЛЬНО!)

Net PnL = $0.16 - $0.25 = $-0.09 ❌ УБЫТОК!
```

### Правильный расчет должен быть:

```
Gross PnL = close_size_coins * (current_price - entry_price)
          = 0.0014 * (89083.20 - 88964.30)
          = $0.16 ✅

Commission (только выход!) = close_size_coins * current_price * commission_rate
                            = 0.0014 * 89083.20 * 0.001
                            = $0.12 ✅

Net PnL = $0.16 - $0.12 = $0.04 ✅ ПРИБЫЛЬ!
```

---

## 🔍 ДОПОЛНИТЕЛЬНЫЕ ПРОБЛЕМЫ

### 1. Комиссия за вход уже уплачена

**При открытии позиции:**
- Комиссия за вход была уплачена за **всю позицию** (0.28 контрактов)
- При частичном закрытии комиссия за вход **НЕ должна учитываться** снова

**Правильная логика:**
- Комиссия за вход: уже уплачена при открытии (не учитываем)
- Комиссия за выход: только за закрываемую часть

### 2. Проблема с определением баланс-профиля

**Из логов:**
- Баланс: **$810.63**
- Profile должен быть: **small** (из других логов видно)
- Но в `close_partial_position`: **profile=unknown**

**Причина:**
- `get_balance_profile()` может возвращать словарь без ключа `name`
- Или `balance_profile` может быть `None`

**Код проверки:**
```python
profile={balance_profile.get('name', 'unknown') if balance_profile else 'unknown'}
```

**Проблема:**
- Если `balance_profile` - это объект, а не словарь, то `.get()` не работает
- Нужно использовать `getattr()` или проверять тип

---

## 📋 ИТОГОВАЯ ТАБЛИЦА ПРОБЛЕМ

| Проблема | Критичность | Статус | Файл/Строки |
|----------|-------------|--------|-------------|
| **Комиссия умножается на 2** | 🔴 Критично | Требует исправления | position_manager.py:6652-6654 |
| **Комиссия за вход учитывается повторно** | 🔴 Критично | Требует исправления | position_manager.py:6651-6654 |
| **Profile=unknown** | ⚠️ Средняя | Требует проверки | position_manager.py:6600 |
| **Баланс не определяется правильно** | ⚠️ Средняя | Требует проверки | position_manager.py:6568-6573 |

---

## 🎯 РЕКОМЕНДАЦИИ ПО ИСПРАВЛЕНИЮ

### Приоритет 1: Критично

1. **Исправить расчет комиссии для частичного закрытия:**
   ```python
   # БЫЛО (неправильно):
   partial_commission = (
       close_size_coins * current_price * commission_rate * 2
   )
   
   # ДОЛЖНО БЫТЬ (правильно):
   # Комиссия только за выход (комиссия за вход уже уплачена при открытии)
   partial_commission = (
       close_size_coins * current_price * commission_rate
   )
   ```

2. **Добавить комментарий:**
   ```python
   # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Комиссия только за выход
   # Комиссия за вход уже была уплачена при открытии всей позиции
   # Для частичного закрытия считаем только комиссию за выход
   partial_commission = (
       close_size_coins * current_price * commission_rate
   )
   ```

### Приоритет 2: Важно

1. **Исправить определение баланс-профиля:**
   ```python
   # БЫЛО:
   profile={balance_profile.get('name', 'unknown') if balance_profile else 'unknown'}
   
   # ДОЛЖНО БЫТЬ:
   if balance_profile:
       if isinstance(balance_profile, dict):
           profile_name = balance_profile.get('name', 'unknown')
       else:
           profile_name = getattr(balance_profile, 'name', 'unknown')
   else:
       profile_name = 'unknown'
   ```

2. **Добавить логирование для диагностики:**
   ```python
   logger.debug(
       f"🔍 [PARTIAL_TP_BALANCE] {symbol}: balance=${balance:.2f}, "
       f"profile={profile_name}, balance_profile type={type(balance_profile)}"
   )
   ```

---

## 📈 МАТЕМАТИЧЕСКИЙ ПРИМЕР (ПРАВИЛЬНЫЙ РАСЧЕТ)

### Исходные данные:
- Символ: BTC-USDT
- Размер позиции: 0.28 контрактов
- Размер закрытия: 0.14 контрактов (50%)
- Entry price: 88964.30 USDT
- Current price: 89083.20 USDT
- ctVal: 0.01 (1 контракт = 0.01 BTC)
- Commission rate: 0.1% (0.001)

### Расчет:

**1. Размер в монетах:**
```
close_size_coins = 0.14 * 0.01 = 0.0014 BTC
```

**2. Gross PnL:**
```
partial_pnl = 0.0014 * (89083.20 - 88964.30)
            = 0.0014 * 118.90
            = $0.16646 ≈ $0.17
```

**3. Commission (только выход!):**
```
partial_commission = 0.0014 * 89083.20 * 0.001
                   = 0.0014 * 89.0832
                   = $0.12472 ≈ $0.12
```

**4. Net PnL:**
```
net_partial_pnl = $0.17 - $0.12 = $0.05 ✅ ПРИБЫЛЬ!
```

**5. Проверка минимума:**
```
min_profit_after_commission = $0.50
net_partial_pnl = $0.05 < $0.50 → ❌ Блокируется (но это уже другая проблема)
```

---

## 🔍 ВЫВОДЫ

### Что работает правильно:
1. ✅ Расчет Gross PnL - правильный
2. ✅ Расчет размера закрытия - правильный
3. ✅ Проверка минимальной стоимости - работает

### Что работает неправильно:
1. ❌ **Комиссия умножается на 2** - неправильно для частичного закрытия
2. ❌ **Комиссия за вход учитывается повторно** - комиссия за вход уже уплачена
3. ⚠️ **Profile=unknown** - баланс-профиль не определяется правильно

### Критичность:
- 🔴 **Критично** - комиссия считается неправильно, что приводит к блокировке прибыльных частичных закрытий
- ⚠️ **Средняя** - проблема с определением баланс-профиля не критична, но мешает правильной работе адаптивных параметров

---

**Дата анализа:** 29.12.2025  
**Статус:** ✅ Проблемы выявлены, требуют исправления

---

## 🔍 ДОПОЛНИТЕЛЬНЫЙ АНАЛИЗ: РАСЧЕТЫ ОТ БАЛАНСА

### Проблема: Размер позиции рассчитывается неправильно от баланса?

**Из логов:**
```
106: 📊 Прогрессивная адаптация для small: баланс $810.63 → размер $131.06
```

**Параметры прогрессивной адаптации:**
- min_balance: $500.00
- threshold (max_balance): $1500.00
- size_at_min: $100.00
- size_at_max: $200.00

**Расчет:**
```
balance = $810.63
balance_range = $1500.00 - $500.00 = $1000.00
size_range = $200.00 - $100.00 = $100.00
clamped_balance = max($500.00, min($810.63, $1500.00)) = $810.63

interpolated_size = $100.00 + ($100.00 * ($810.63 - $500.00) / $1000.00)
                  = $100.00 + ($100.00 * $310.63 / $1000.00)
                  = $100.00 + ($100.00 * 0.31063)
                  = $100.00 + $31.063
                  = $131.063 ≈ $131.06 ✅ СОВПАДАЕТ С ЛОГАМИ
```

**Вывод:** ✅ Расчет размера позиции от баланса **правильный**

---

## 🔍 АНАЛИЗ: ПОЧЕМУ ЧАСТИЧНОЕ ЗАКРЫТИЕ В МИНУС

### Корневая причина:

**Комиссия считается неправильно:**
- Комиссия умножается на **2** (вход + выход)
- Но комиссия за **вход** уже была уплачена при открытии **всей позиции**
- Для частичного закрытия нужно считать только комиссию за **выход**

### Математика:

**Текущий расчет (неправильный):**
```
Commission = close_size_coins * current_price * commission_rate * 2
           = 0.0014 * 89083.20 * 0.001 * 2
           = $0.25 ❌
```

**Правильный расчет:**
```
Commission (только выход) = close_size_coins * current_price * commission_rate
                          = 0.0014 * 89083.20 * 0.001
                          = $0.12 ✅
```

**Результат:**
- Текущий Net PnL: **$-0.09** (убыток) ❌
- Правильный Net PnL: **$0.04** (прибыль) ✅

---

## 🔍 ПРОБЛЕМА С ЦЕНОЙ ДЛЯ РАСЧЕТА

### Разница в ценах:

**Из логов:**
- ExitAnalyzer использует: `current_price=87488.90` (из стакана)
- PositionManager использует: `current_price=89083.20` (markPx)

**Проблема:**
- Разница: `89083.20 - 87488.90 = 1594.30 USDT` (~1.8%)
- Это может быть из-за использования разных источников цены

**В коде (position_manager.py:6562):**
```python
current_price = float(pos_data.get("markPx", entry_price))
```

**Проблема:**
- Используется `markPx` (маркировочная цена)
- Но для расчета PnL нужна реальная цена исполнения (из стакана)

**Рекомендация:**
- Использовать цену из стакана для расчета PnL
- `markPx` использовать только как fallback

---

## 📊 ИТОГОВАЯ ТАБЛИЦА ВСЕХ ПРОБЛЕМ

| Проблема | Критичность | Статус | Файл/Строки | Влияние |
|----------|-------------|--------|-------------|---------|
| **Комиссия * 2 для частичного закрытия** | 🔴 Критично | Требует исправления | position_manager.py:6652-6654 | Блокирует прибыльные закрытия |
| **Комиссия за вход учитывается повторно** | 🔴 Критично | Требует исправления | position_manager.py:6651-6654 | Завышает комиссию в 2 раза |
| **Использование markPx вместо цены стакана** | ⚠️ Средняя | Требует проверки | position_manager.py:6562 | Может давать неточный PnL |
| **Profile=unknown** | ⚠️ Средняя | Требует проверки | position_manager.py:6600 | Не влияет на расчеты, но мешает диагностике |

---

## 🎯 ДЕТАЛЬНЫЕ РЕКОМЕНДАЦИИ

### Приоритет 1: Критично (Исправить немедленно)

1. **Исправить расчет комиссии для частичного закрытия:**
   ```python
   # БЫЛО (position_manager.py:6651-6654):
   # Комиссия за вход + выход для частичного закрытия
   partial_commission = (
       close_size_coins * current_price * commission_rate * 2
   )
   
   # ДОЛЖНО БЫТЬ:
   # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Комиссия только за выход
   # Комиссия за вход уже была уплачена при открытии всей позиции
   # Для частичного закрытия считаем только комиссию за выход
   partial_commission = (
       close_size_coins * current_price * commission_rate
   )
   ```

2. **Использовать цену из стакана для расчета PnL:**
   ```python
   # БЫЛО (position_manager.py:6562):
   current_price = float(pos_data.get("markPx", entry_price))
   
   # ДОЛЖНО БЫТЬ:
   # Получаем цену из стакана для точного расчета PnL
   if self.client and hasattr(self.client, "get_price_limits"):
       price_limits = await self.client.get_price_limits(symbol)
       if price_limits:
           current_price = price_limits.get("current_price", 0)
           if current_price > 0:
               # Используем цену из стакана
               pass
   # Fallback на markPx если стакан недоступен
   if current_price <= 0:
       current_price = float(pos_data.get("markPx", entry_price))
   ```

### Приоритет 2: Важно

1. **Исправить определение баланс-профиля:**
   ```python
   # БЫЛО (position_manager.py:6600):
   profile={balance_profile.get('name', 'unknown') if balance_profile else 'unknown'}
   
   # ДОЛЖНО БЫТЬ:
   if balance_profile:
       if isinstance(balance_profile, dict):
           profile_name = balance_profile.get('name', 'unknown')
       else:
           profile_name = getattr(balance_profile, 'name', 'unknown')
   else:
       profile_name = 'unknown'
   ```

---

## 📈 МАТЕМАТИЧЕСКИЙ ПРИМЕР (С ПРАВИЛЬНЫМИ РАСЧЕТАМИ)

### Исходные данные:
- Символ: BTC-USDT
- Размер позиции: 0.28 контрактов
- Размер закрытия: 0.14 контрактов (50%)
- Entry price: 88964.30 USDT
- Current price (markPx): 89083.20 USDT
- Current price (стакан): 87488.90 USDT (из ExitAnalyzer)
- ctVal: 0.01
- Commission rate: 0.1% (0.001)

### Расчет с markPx (текущий):

**1. Размер в монетах:**
```
close_size_coins = 0.14 * 0.01 = 0.0014 BTC
```

**2. Gross PnL:**
```
partial_pnl = 0.0014 * (89083.20 - 88964.30)
            = 0.0014 * 118.90
            = $0.16646 ≈ $0.17
```

**3. Commission (правильный расчет):**
```
partial_commission = 0.0014 * 89083.20 * 0.001  # Только выход!
                   = $0.12472 ≈ $0.12
```

**4. Net PnL:**
```
net_partial_pnl = $0.17 - $0.12 = $0.05 ✅ ПРИБЫЛЬ!
```

### Расчет с ценой стакана (более точный):

**1. Gross PnL:**
```
partial_pnl = 0.0014 * (87488.90 - 88964.30)
            = 0.0014 * (-1475.40)
            = $-2.06556 ≈ $-2.07 ❌ УБЫТОК!
```

**Проблема:** Разница в ценах очень большая! Это указывает на проблему с синхронизацией цен.

---

## 🔍 ВЫВОДЫ ПО РАСЧЕТАМ ОТ БАЛАНСА

### Что работает правильно:
1. ✅ Расчет размера позиции от баланса (прогрессивная адаптация) - правильный
2. ✅ Интерполяция размера для баланса $810.63 → $131.06 - правильная

### Что работает неправильно:
1. ❌ Комиссия для частичного закрытия умножается на 2 - неправильно
2. ⚠️ Используется markPx вместо цены стакана - может давать неточный PnL
3. ⚠️ Большая разница между markPx и ценой стакана - проблема синхронизации

---

**Дата анализа:** 29.12.2025  
**Статус:** ✅ Все проблемы выявлены и задокументированы

