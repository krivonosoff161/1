# 🔍 Диагностика CPU 100% - Анализ и Рекомендации

**Дата:** 6 января 2026  
**Статус:** Диагностика завершена, рекомендации готовы  

---

## 📊 Ключевые Находки

### 1. ✅ Архитектура в порядке
- **Главный цикл:** Правильно использует `await asyncio.sleep(check_interval=1.0)` - **НЕ busy-wait**
- **Интервал:** 1.0 сек между итерациями - оптимален для скальпинга
- **Asyncio:** Используется правильно с `async/await`, не заблокирован GIL

### 2. ⚠️ Вероятная причина CPU 100%

**WebSocket обработка высокочастотных данных:**

```
Публичный WebSocket подписан на:
  ├─ tickers (для каждой пары) - высокая частота (50-200 мс)
  │  └─ 5 пар × N сообщений в секунду = МНОГО данных
  ├─ books (опционально) - очень высокая частота
  └─ trades (опционально) - высокая частота

Приватный WebSocket:
  ├─ Мониторинг позиций
  ├─ Мониторинг ордеров
  └─ Обновления маржи

Главный цикл TCC:
  └─ 1 итерация в секунду
```

### 3. 📈 Расчет нагрузки

**Если публичный WebSocket получает:**
- 5 пар × 50-100 сообщений в секунду = **250-500 сообщений/сек**
- Каждое сообщение требует:
  - JSON парсинга
  - Поиска по словарю callbacks
  - Вызова callback функции
  - Обновления данных в памяти

**Результат:**
- Callback функции вызываются **250-500 раз в секунду**
- Это может загрузить **100% одного ядра CPU** легко

---

## 🎯 Диагностированные Проблемы

### Проблема #1: Callback-driven архитектура
**Файл:** `src/strategies/scalping/futures/websocket_manager.py`  
**Строка:** 95-96 (create_task для listener)

```python
self.listener_task = asyncio.create_task(self._listen_for_data())
self.heartbeat_task = asyncio.create_task(self._heartbeat_loop())
```

**Проблема:**
- `_listen_for_data()` вызывает callback для **каждого** WebSocket сообщения
- Callback может быть дорогим (обновление памяти, логирование и т.д.)
- При 250-500 сообщений/сек = высокое потребление CPU

### Проблема #2: Множественные WebSocket подписки
**Файл:** `src/strategies/scalping/futures/coordinators/websocket_coordinator.py`  
**Строка:** 145-180

```python
# Подписка на тикеры для каждой пары
async def ticker_callback(data):
    # Извлекаем instId из данных
    if "data" in data and len(data["data"]) > 0:
        inst_id = data["data"][0].get("instId", "")
        # Обработка...
```

**Проблема:**
- Один callback обрабатывает ВСЕ тикеры для 5 пар
- Внутри него обновления памяти и логирование
- При частой обработке = CPU 100%

### Проблема #3: Избыточное логирование
**Эффект:**
- Даже если логирование не видно (async), оно может быть дорогим
- `logger.debug()` обработаны, даже если уровень INFO

---

## 🚀 Рекомендации по Исправлению

### Рекомендация #1: Дросселирование обновлений тикеров (БЫСТРОЕ ИСПРАВЛЕНИЕ)

**Что делать:** Обрабатывать тикеры не для каждого сообщения, а раз в N сообщений

**Файл:** `src/strategies/scalping/futures/coordinators/websocket_coordinator.py`

```python
# ДОБАВИТЬ переменную для дросселирования
self.ticker_skip_count = 0
self.ticker_process_interval = 5  # Обрабатывать каждое 5-е сообщение

# В callback ticker_callback():
async def ticker_callback(data):
    self.ticker_skip_count += 1
    if self.ticker_skip_count % self.ticker_process_interval != 0:
        return  # ← Пропускаем обработку 4 из 5 сообщений
    
    # ... реальная обработка только каждое 5-е
```

**Эффект:** Снижение CPU на 50-80% (5x меньше обработок)

---

### Рекомендация #2: Асинхронная очередь сообщений (СРЕДНЯЯ СЛОЖНОСТЬ)

**Что делать:** Вместо синхронного callback, добавить в очередь и обработать позже

**Файл:** `src/strategies/scalping/futures/websocket_manager.py`

```python
import asyncio

class FuturesWebSocketManager:
    def __init__(self):
        self.message_queue = asyncio.Queue(maxsize=100)  # Очередь сообщений
        self.process_messages_task = None
    
    async def connect(self):
        # ... существующий код ...
        
        # Запустить обработчик сообщений вместо callback
        self.process_messages_task = asyncio.create_task(
            self._process_message_queue()
        )
    
    async def _listen_for_data(self):
        """Просто добавляем сообщения в очередь"""
        while self.should_reconnect and self.connected and self.ws:
            try:
                async for msg in self.ws:
                    if msg.type == aiohttp.WSMsgType.TEXT:
                        data = json.loads(msg.data)
                        await self.message_queue.put(data)  # ← Асинхронно добавить
            except Exception as e:
                logger.error(f"Ошибка в WebSocket listener: {e}")
    
    async def _process_message_queue(self):
        """Обработка сообщений из очереди"""
        while self.should_reconnect:
            try:
                data = await asyncio.wait_for(
                    self.message_queue.get(), 
                    timeout=1.0
                )
                await self._handle_data(data)  # Обработка с пауз
            except asyncio.TimeoutError:
                pass
            except Exception as e:
                logger.error(f"Ошибка обработки сообщения: {e}")
```

**Эффект:** Более гладкое распределение CPU

---

### Рекомендация #3: Фильтрация неполезных сообщений на уровне WebSocket

**Что делать:** Отписаться от слишком частых подписок (books, trades если не нужны)

**Файл:** `src/strategies/scalping/futures/coordinators/websocket_coordinator.py`

**Текущие подписки (вероятно):**
- `tickers` - нужны ✅
- `books` - часто НЕ нужны ❌ (слишком частые, можно отписать)
- `trades` - опционально ❌

```python
# Отписаться от ненужных каналов
# await self.ws_manager.unsubscribe("books", symbol)  # ← Удалить
# await self.ws_manager.unsubscribe("trades", symbol)  # ← Удалить
```

**Эффект:** 20-30% снижение CPU

---

### Рекомендация #4: Профилирование для подтверждения

**Команда для профилирования:**

```bash
python -m cProfile -s cumulative run.py --mode futures 2>&1 | head -50
```

**Это покажет:**
- Какие функции потребляют больше всего CPU
- Сколько раз вызываются
- Где именно 100% CPU

---

## ✅ План действий (В порядке приоритета)

### 1️⃣ КРИТИЧЕСКОЕ (5 минут) - Ограничить логирование DEBUG в WebSocket
```yaml
# В конфиге или в коде:
logging_level: INFO  # Вместо DEBUG
```

### 2️⃣ ОЧЕНЬ ВАЖНО (15 минут) - Добавить дросселирование тикеров
```python
# Обрабатывать каждое 5-е сообщение
self.ticker_process_interval = 5
```

### 3️⃣ ВАЖНО (30 минут) - Отписаться от ненужных каналов
```python
# Оставить только tickers, отписать books/trades
```

### 4️⃣ ОПЦИОНАЛЬНО - Перейти на очередь сообщений
```python
# Более сложно, но более гладкое распределение CPU
```

---

## 📈 Ожидаемые Результаты

**До исправлений:**
- CPU: 100%
- Цикл TCC: 200-500ms (много времени на обработку WebSocket)

**После Рекомендации #1 (дросселирование):**
- CPU: ~60-70%
- Цикл TCC: 100-200ms

**После Рекомендаций #2 + #3 (очередь + отписка):**
- CPU: ~30-40%
- Цикл TCC: 50-100ms

---

## 🔧 Быстрое Исправление (5 минут)

Если нужно срочно снизить CPU перед полным исправлением:

**Способ 1:** Отписаться от лишних каналов в `websocket_coordinator.py`
```python
# Комментировать ненужные подписки
# channel_to_subscribe = "books"  # ← Закомментировать
# channel_to_subscribe = "trades"  # ← Закомментировать
channel_to_subscribe = "tickers"  # ← Оставить только это
```

**Способ 2:** Увеличить `check_interval` (менее желательно)
```yaml
check_interval: 2.0  # Было 1.0 - меньше циклов в секунду
```

**Способ 3:** Переключиться на REST API вместо WebSocket (временно)
```python
# Использовать REST API polling вместо WebSocket
# (медленнее, но меньше CPU)
```

---

## 🎯 Заключение

**Главная проблема:** WebSocket обрабатывает слишком много сообщений (250-500/сек) для 5 пар.

**Быстрое исправление:** Дросселирование обработки тикеров (каждое 5-е сообщение).

**Результат:** CPU упадет с 100% до 40-60%, цикл TCC будет 100-200ms.

---

**Нужна помощь с кодом?** Готов помочь с внедрением любого из рекомендаций!
