# 🎯 ПЛАН АДАПТИВНЫХ ПАРАМЕТРОВ РИСКА

**Дата:** 2025-11-12  
**Цель:** Создать систему адаптивных параметров риска с учетом ARM и Balance Profiles

---

## 📊 ТЕКУЩАЯ СИТУАЦИЯ

### ✅ Что уже работает:
1. **ARM (Adaptive Regime Manager)** - адаптация под режим рынка:
   - `position_size_multiplier`: trending (1.1), ranging (1.0), choppy (0.8)
   - `tp_atr_multiplier`, `sl_atr_multiplier` - разные для каждого режима
   - `max_holding_minutes` - разные для каждого режима

2. **Balance Profiles** - адаптация под размер баланса:
   - `base_position_usd`, `min_position_usd`, `max_position_usd` - зависят от баланса
   - `max_open_positions`, `max_position_percent` - зависят от баланса
   - Прогрессивная адаптация работает правильно

### ❌ Что НЕ работает:
1. **Параметры риска захардкожены:**
   - `max_loss_per_trade = 0.02` (2%) - одинаково для всех режимов и балансов
   - `max_margin_percent = 0.80` (80%) - одинаково для всех режимов и балансов
   - `max_drawdown_percent = 0.05` (5%) - одинаково для всех режимов и балансов

2. **Параметры проверок захардкожены:**
   - `time_since_order < 30` (30 секунд) - одинаково для всех режимов
   - `balance < 20.0` ($20) - одинаково для всех балансов
   - `margin_required > balance * 0.9` (90%) - одинаково для всех балансов

3. **Параметры strength multipliers захардкожены:**
   - `strength_multiplier = 0.5, 1.5, 1.2, 1.0, 0.8` - одинаково для всех режимов

---

## 💡 РЕШЕНИЕ: АДАПТИВНЫЕ ПАРАМЕТРЫ РИСКА

### Структура конфига:

```yaml
# Риск-менеджмент для Futures - АДАПТИВНЫЙ
risk:
  # Базовые параметры (по умолчанию)
  base:
    max_loss_per_trade_percent: 2.0  # 2% макс потеря на сделку
    max_margin_percent: 80.0  # 80% макс маржа
    max_drawdown_percent: 5.0  # 5% макс просадка
    max_margin_safety_percent: 90.0  # 90% безопасности (финальная проверка)
    min_balance_usd: 20.0  # Минимум $20 баланса
    min_time_between_orders_seconds: 30  # Минимум 30 секунд между ордерами
    position_override_tolerance_percent: 50.0  # 50% разница для игнорирования override
  
  # Адаптация по режиму рынка (ARM)
  by_regime:
    trending:
      max_loss_per_trade_percent: 1.5  # Меньше риск в тренде (больше уверенности)
      max_margin_percent: 85.0  # Больше маржи в тренде (больше возможностей)
      max_drawdown_percent: 4.0  # Меньше просадка в тренде (строже контроль)
      max_margin_safety_percent: 90.0  # 90% безопасности
      min_time_between_orders_seconds: 20  # Быстрее в тренде (20 секунд)
      strength_multipliers:
        conflict: 0.6  # Конфликт: 60% (было 50%)
        very_strong: 1.6  # Сильный сигнал: 160% (было 150%)
        strong: 1.3  # Хороший сигнал: 130% (было 120%)
        medium: 1.1  # Средний сигнал: 110% (было 100%)
        weak: 0.9  # Слабый сигнал: 90% (было 80%)
      strength_thresholds:
        very_strong: 0.8  # Порог для сильного сигнала
        strong: 0.6  # Порог для хорошего сигнала
        medium: 0.4  # Порог для среднего сигнала
    
    ranging:
      max_loss_per_trade_percent: 2.0  # Стандартный риск во флэте
      max_margin_percent: 80.0  # Стандартная маржа во флэте
      max_drawdown_percent: 5.0  # Стандартная просадка во флэте
      max_margin_safety_percent: 90.0  # 90% безопасности
      min_time_between_orders_seconds: 30  # Стандартная скорость (30 секунд)
      strength_multipliers:
        conflict: 0.5  # Конфликт: 50%
        very_strong: 1.5  # Сильный сигнал: 150%
        strong: 1.2  # Хороший сигнал: 120%
        medium: 1.0  # Средний сигнал: 100%
        weak: 0.8  # Слабый сигнал: 80%
      strength_thresholds:
        very_strong: 0.8  # Порог для сильного сигнала
        strong: 0.6  # Порог для хорошего сигнала
        medium: 0.4  # Порог для среднего сигнала
    
    choppy:
      max_loss_per_trade_percent: 2.5  # Больше риск в хаосе (меньше уверенности)
      max_margin_percent: 75.0  # Меньше маржи в хаосе (больше безопасности)
      max_drawdown_percent: 6.0  # Больше просадка в хаосе (больше толерантности)
      max_margin_safety_percent: 85.0  # 85% безопасности (строже в хаосе)
      min_time_between_orders_seconds: 45  # Медленнее в хаосе (45 секунд)
      strength_multipliers:
        conflict: 0.4  # Конфликт: 40% (меньше риск в хаосе)
        very_strong: 1.4  # Сильный сигнал: 140% (меньше размер в хаосе)
        strong: 1.1  # Хороший сигнал: 110% (меньше размер в хаосе)
        medium: 0.9  # Средний сигнал: 90% (меньше размер в хаосе)
        weak: 0.7  # Слабый сигнал: 70% (меньше размер в хаосе)
      strength_thresholds:
        very_strong: 0.85  # Порог для сильного сигнала (выше в хаосе)
        strong: 0.65  # Порог для хорошего сигнала (выше в хаосе)
        medium: 0.45  # Порог для среднего сигнала (выше в хаосе)
  
  # Адаптация по балансу (Balance Profiles)
  by_balance:
    micro:  # $100 - $500
      max_loss_per_trade_percent: 2.5  # Больше риск для малого баланса (больше агрессивности)
      max_margin_percent: 85.0  # Больше маржи для малого баланса (больше использования)
      max_drawdown_percent: 6.0  # Больше просадка для малого баланса (больше толерантности)
      max_margin_safety_percent: 90.0  # 90% безопасности
      min_balance_usd: 10.0  # Минимум $10 баланса (меньше для малого баланса)
      min_time_between_orders_seconds: 25  # Быстрее для малого баланса (25 секунд)
    
    small:  # $500 - $1500
      max_loss_per_trade_percent: 2.0  # Стандартный риск для среднего баланса
      max_margin_percent: 80.0  # Стандартная маржа для среднего баланса
      max_drawdown_percent: 5.0  # Стандартная просадка для среднего баланса
      max_margin_safety_percent: 90.0  # 90% безопасности
      min_balance_usd: 20.0  # Минимум $20 баланса
      min_time_between_orders_seconds: 30  # Стандартная скорость (30 секунд)
    
    medium:  # $1500 - $3000
      max_loss_per_trade_percent: 1.8  # Меньше риск для большого баланса (больше консервативности)
      max_margin_percent: 75.0  # Меньше маржи для большого баланса (больше безопасности)
      max_drawdown_percent: 4.0  # Меньше просадка для большого баланса (строже контроль)
      max_margin_safety_percent: 85.0  # 85% безопасности (строже для большого баланса)
      min_balance_usd: 30.0  # Минимум $30 баланса (больше для большого баланса)
      min_time_between_orders_seconds: 35  # Медленнее для большого баланса (35 секунд)
    
    large:  # $3000+
      max_loss_per_trade_percent: 1.5  # Меньше риск для очень большого баланса (больше консервативности)
      max_margin_percent: 70.0  # Меньше маржи для очень большого баланса (больше безопасности)
      max_drawdown_percent: 3.0  # Меньше просадка для очень большого баланса (строже контроль)
      max_margin_safety_percent: 80.0  # 80% безопасности (строже для очень большого баланса)
      min_balance_usd: 50.0  # Минимум $50 баланса (больше для очень большого баланса)
      min_time_between_orders_seconds: 40  # Медленнее для очень большого баланса (40 секунд)

# Параметры синхронизации - АДАПТИВНЫЕ
scalping:
  positions_sync:
    base_interval_min: 15.0  # Минимум 15 секунд
    base_interval_multiplier: 3.0  # Множитель (check_interval * multiplier)
    # Адаптация по режиму рынка
    by_regime:
      trending:
        interval_multiplier: 2.5  # Быстрее в тренде (2.5x)
      ranging:
        interval_multiplier: 3.0  # Стандартная скорость (3.0x)
      choppy:
        interval_multiplier: 3.5  # Медленнее в хаосе (3.5x)
    # Адаптация по балансу
    by_balance:
      micro:
        interval_multiplier: 2.5  # Быстрее для малого баланса (2.5x)
      small:
        interval_multiplier: 3.0  # Стандартная скорость (3.0x)
      medium:
        interval_multiplier: 3.5  # Медленнее для большого баланса (3.5x)
      large:
        interval_multiplier: 4.0  # Медленнее для очень большого баланса (4.0x)

# Параметры других модулей - АДАПТИВНЫЕ
futures_modules:
  margin:
    base:
      maintenance_margin_ratio: 0.01  # 1%
      initial_margin_ratio: 0.1  # 10%
    # Адаптация по балансу (может быть разной для разных балансов)
    by_balance:
      micro:
        maintenance_margin_ratio: 0.01  # 1%
        initial_margin_ratio: 0.1  # 10%
      small:
        maintenance_margin_ratio: 0.01  # 1%
        initial_margin_ratio: 0.1  # 10%
      medium:
        maintenance_margin_ratio: 0.01  # 1%
        initial_margin_ratio: 0.1  # 10%
      large:
        maintenance_margin_ratio: 0.01  # 1%
        initial_margin_ratio: 0.1  # 10%
  
  funding_rate:
    base:
      max_funding_rate: 0.05  # 0.05%
    # Адаптация по режиму рынка (может быть разной для разных режимов)
    by_regime:
      trending:
        max_funding_rate: 0.06  # Больше толерантности в тренде (0.06%)
      ranging:
        max_funding_rate: 0.05  # Стандартная толерантность (0.05%)
      choppy:
        max_funding_rate: 0.04  # Меньше толерантности в хаосе (0.04%)
  
  adx:
    base:
      period: 9  # Период ADX
      threshold: 20.0  # Порог ADX
    # Адаптация по режиму рынка (уже есть в adaptive_regime.detection)
    # Используем значения из adaptive_regime.detection
```

---

## 🔄 ЛОГИКА АДАПТАЦИИ

### Приоритет параметров:
1. **По режиму рынка (ARM)** - первичный приоритет
2. **По балансу (Balance Profiles)** - вторичный приоритет
3. **Базовые параметры** - fallback если не указано

### Формула адаптации:
```python
# 1. Получаем параметры по режиму рынка
regime_params = risk.by_regime[regime]  # trending, ranging, choppy

# 2. Получаем параметры по балансу
balance_params = risk.by_balance[balance_profile]  # micro, small, medium, large

# 3. Комбинируем параметры (режим имеет приоритет, баланс - вторичный)
final_params = {
    "max_loss_per_trade_percent": regime_params.get("max_loss_per_trade_percent") or balance_params.get("max_loss_per_trade_percent") or risk.base.max_loss_per_trade_percent,
    "max_margin_percent": regime_params.get("max_margin_percent") or balance_params.get("max_margin_percent") or risk.base.max_margin_percent,
    "max_drawdown_percent": regime_params.get("max_drawdown_percent") or balance_params.get("max_drawdown_percent") or risk.base.max_drawdown_percent,
    # ... и т.д.
}
```

### Примеры:

#### Пример 1: Trending + Small баланс
```python
# Режим: trending
# Баланс: $1053 (small профиль)

# Параметры риска:
max_loss_per_trade_percent = 1.5%  # Из trending (приоритет)
max_margin_percent = 80.0%  # Из small (если не указано в trending)
max_drawdown_percent = 4.0%  # Из trending (приоритет)

# Параметры проверок:
min_time_between_orders_seconds = 20  # Из trending (приоритет)
min_balance_usd = 20.0  # Из small (если не указано в trending)

# Strength multipliers:
strength_multipliers = trending.strength_multipliers  # Из trending
```

#### Пример 2: Choppy + Medium баланс
```python
# Режим: choppy
# Баланс: $2000 (medium профиль)

# Параметры риска:
max_loss_per_trade_percent = 2.5%  # Из choppy (приоритет)
max_margin_percent = 75.0%  # Из choppy (приоритет, но можно комбинировать с medium)
max_drawdown_percent = 6.0%  # Из choppy (приоритет)

# Параметры проверок:
min_time_between_orders_seconds = 45  # Из choppy (приоритет)
min_balance_usd = 30.0  # Из medium (если не указано в choppy)

# Strength multipliers:
strength_multipliers = choppy.strength_multipliers  # Из choppy
```

---

## 📊 ТАБЛИЦА АДАПТИВНЫХ ПАРАМЕТРОВ

### Параметры риска по режиму рынка:

| Параметр | Trending | Ranging | Choppy |
|----------|----------|---------|--------|
| `max_loss_per_trade_percent` | 1.5% | 2.0% | 2.5% |
| `max_margin_percent` | 85.0% | 80.0% | 75.0% |
| `max_drawdown_percent` | 4.0% | 5.0% | 6.0% |
| `max_margin_safety_percent` | 90.0% | 90.0% | 85.0% |
| `min_time_between_orders_seconds` | 20 | 30 | 45 |

### Параметры риска по балансу:

| Параметр | Micro | Small | Medium | Large |
|----------|-------|-------|--------|-------|
| `max_loss_per_trade_percent` | 2.5% | 2.0% | 1.8% | 1.5% |
| `max_margin_percent` | 85.0% | 80.0% | 75.0% | 70.0% |
| `max_drawdown_percent` | 6.0% | 5.0% | 4.0% | 3.0% |
| `max_margin_safety_percent` | 90.0% | 90.0% | 85.0% | 80.0% |
| `min_balance_usd` | 10.0 | 20.0 | 30.0 | 50.0 |
| `min_time_between_orders_seconds` | 25 | 30 | 35 | 40 |

### Strength multipliers по режиму рынка:

| Уровень | Trending | Ranging | Choppy |
|---------|----------|---------|--------|
| Конфликт | 0.6 | 0.5 | 0.4 |
| Очень сильный | 1.6 | 1.5 | 1.4 |
| Хороший | 1.3 | 1.2 | 1.1 |
| Средний | 1.1 | 1.0 | 0.9 |
| Слабый | 0.9 | 0.8 | 0.7 |

---

## 🎯 ПЛАН ИСПРАВЛЕНИЙ

### Неделя 1: Добавить параметры в конфиг
1. ✅ Добавить структуру адаптивных параметров риска в конфиг
2. ✅ Добавить параметры по режиму рынка (ARM)
3. ✅ Добавить параметры по балансу (Balance Profiles)
4. ✅ Добавить параметры синхронизации
5. ✅ Добавить параметры других модулей

### Неделя 2: Исправить код
6. ✅ Создать метод для получения адаптивных параметров риска
7. ✅ Заменить захардкоженные значения на чтение из конфига
8. ✅ Реализовать логику адаптации (режим + баланс)
9. ✅ Добавить валидацию параметров из конфига

### Неделя 3: Исследование и оптимизация
10. ✅ Протестировать разные комбинации параметров
11. ✅ Выбрать оптимальные значения
12. ✅ Обновить конфиг с оптимальными значениями

---

## 📝 ВЫВОДЫ

### Главные преимущества:
1. **Адаптация под режим рынка** - разные параметры риска для trending, ranging, choppy
2. **Адаптация под баланс** - разные параметры риска для micro, small, medium, large
3. **Гибкость настройки** - все параметры в конфиге, легко изменить
4. **Приоритет параметров** - режим имеет приоритет, баланс - вторичный

### Ожидаемые результаты:
1. **Больше прибыли в тренде** - меньше риск, больше маржи, больше размер позиции
2. **Больше безопасности в хаосе** - больше риск, меньше маржи, меньше размер позиции
3. **Оптимальное использование баланса** - адаптация под размер баланса
4. **Лучшая производительность** - параметры оптимизированы для каждого режима и баланса

---

**Дата создания:** 2025-11-12  
**Статус:** 📋 ГОТОВ К ИСПРАВЛЕНИЮ

**Следующие шаги:**
1. Добавить структуру адаптивных параметров в конфиг
2. Исправить код для использования адаптивных параметров
3. Протестировать разные комбинации параметров
4. Оптимизировать значения параметров

