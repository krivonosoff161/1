# 🔍 АНАЛИЗ СИСТЕМЫ СВЕЧЕЙ ВО ВСЕХ РЕЖИМАХ

**Дата:** 26.11.2025 22:20  
**Цель:** Проверить, правильно ли грузятся и считаются свечи во всех режимах

---

## 📊 ТЕКУЩАЯ РЕАЛИЗАЦИЯ

### **1. Инициализация свечей (при старте)** ✅

**Файл:** `src/strategies/scalping/futures/orchestrator.py`  
**Метод:** `_initialize_candle_buffers()` (строка 934-1020)

**Что загружается:**
- ✅ Только таймфрейм **1m** (1 минута)
- ✅ 200 свечей для каждого символа
- ✅ Загружается через API при старте бота
- ✅ Инициализируется в `DataRegistry.CandleBuffer`

**Проблема:** ⚠️
- ❌ Загружаются только свечи **1m**
- ❌ Не загружаются другие таймфреймы (5m, 15m, 1H, 1D)

---

## 🔍 ТРЕБОВАНИЯ МОДУЛЕЙ К СВЕЧАМ

### **1. SignalGenerator** ✅

**Использует:**
- ✅ Свечи 1m из `DataRegistry`
- ✅ Минимум 20 свечей
- ✅ Fallback к API если свечей недостаточно

**Статус:** ✅ Правильно

---

### **2. Multi-Timeframe Filter** ⚠️

**Требует:**
- ⚠️ Свечи **5m** (по умолчанию, configurable)
- ⚠️ Достаточно для EMA8 и EMA21 (минимум 21 свеча)
- ⚠️ Запрашивает через API при каждом вызове (не использует DataRegistry!)

**Проблема:**
- ❌ Не использует `DataRegistry.CandleBuffer`
- ❌ Запрашивает свечи через API каждый раз
- ❌ Нет кэширования свечей 5m

**Код:**
```python
# multi_timeframe.py
async def _fetch_candles(self, symbol: str, timeframe: str, limit: int = 50):
    # Запрашивает через API каждый раз!
    url = f"https://www.okx.com/api/v5/market/candles?instId={inst_id}&bar={timeframe}&limit={limit}"
```

**Статус:** ⚠️ **Требует улучшения**

---

### **3. Volume Profile Filter** ⚠️

**Требует:**
- ⚠️ Свечи **1H** (по умолчанию)
- ⚠️ 100 свечей для расчета Volume Profile
- ⚠️ Запрашивает через API при каждом вызове

**Проблема:**
- ❌ Не использует `DataRegistry.CandleBuffer`
- ❌ Запрашивает свечи через API каждый раз
- ❌ Есть кэш (TTL=600 секунд), но все равно запрашивает через API

**Код:**
```python
# volume_profile_filter.py
async def _fetch_candles(self, symbol: str, timeframe: str, limit: int = 100):
    # Запрашивает через API с кэшированием
```

**Статус:** ⚠️ **Требует улучшения**

---

### **4. Pivot Points Filter** ⚠️

**Требует:**
- ⚠️ Свечи **1D** (дневные)
- ⚠️ 1-5 дней для расчета Pivot Points
- ⚠️ Запрашивает через API при каждом вызове

**Проблема:**
- ❌ Не использует `DataRegistry.CandleBuffer`
- ❌ Запрашивает свечи через API каждый раз
- ❌ Есть кэш (TTL=3600 секунд), но все равно запрашивает через API

**Код:**
```python
# pivot_points.py
async def _fetch_daily_candles(self, symbol: str, days: int = 1):
    # Запрашивает через API с кэшированием
```

**Статус:** ⚠️ **Требует улучшения**

---

### **5. Correlation Filter** ⚠️

**Требует:**
- ⚠️ Свечи **5m** (по умолчанию)
- ⚠️ 100 свечей для расчета корреляции
- ⚠️ Использует `CorrelationManager`, который запрашивает через API

**Проблема:**
- ❌ Не использует `DataRegistry.CandleBuffer`
- ❌ Запрашивает свечи через API

**Статус:** ⚠️ **Требует улучшения**

---

### **6. AdaptiveRegimeManager** ✅

**Требует:**
- ✅ Минимум 50 свечей для определения режима
- ✅ Использует свечи через `SignalGenerator._get_market_data()`
- ✅ Которые получаются из `DataRegistry`

**Статус:** ✅ Правильно

---

### **7. ADX Filter** ✅

**Требует:**
- ✅ Свечи для расчета ADX
- ✅ Получает через `SignalGenerator._get_market_data()`

**Статус:** ✅ Правильно

---

## 📋 ИТОГОВАЯ СИТУАЦИЯ

### **Что работает правильно:** ✅

1. ✅ **1m свечи:**
   - Загружаются при старте (200 свечей)
   - Хранятся в `DataRegistry.CandleBuffer`
   - Обновляются инкрементально через WebSocket
   - Используются в `SignalGenerator`, `AdaptiveRegimeManager`, `ADX Filter`

### **Что требует улучшения:** ⚠️

1. ⚠️ **5m свечи:**
   - ❌ Не загружаются при старте
   - ❌ Запрашиваются через API каждый раз в `Multi-Timeframe Filter`
   - ❌ Запрашиваются через API каждый раз в `Correlation Filter`
   - ❌ Не хранятся в `DataRegistry.CandleBuffer`

2. ⚠️ **1H свечи:**
   - ❌ Не загружаются при старте
   - ❌ Запрашиваются через API в `Volume Profile Filter` (с кэшем 600 сек)
   - ❌ Не хранятся в `DataRegistry.CandleBuffer`

3. ⚠️ **1D свечи:**
   - ❌ Не загружаются при старте
   - ❌ Запрашиваются через API в `Pivot Points Filter` (с кэшем 3600 сек)
   - ❌ Не хранятся в `DataRegistry.CandleBuffer`

---

## 🎯 ПРОБЛЕМЫ

### **Проблема 1: Неэффективное использование API** ⚠️

**Текущая ситуация:**
- Фильтры запрашивают свечи через API каждый раз
- Нет централизованного хранения свечей разных таймфреймов
- Дублирование запросов (несколько фильтров могут запросить одни и те же свечи)

**Последствия:**
- ⚠️ Медленная работа (запросы к API)
- ⚠️ Лимиты API (могут быть превышены)
- ⚠️ Несогласованные данные (свечи могут быть запрошены в разное время)

---

### **Проблема 2: Недостаточно свечей для всех режимов** ⚠️

**Текущая ситуация:**
- Загружаются только 1m свечи (200 штук)
- Другие таймфреймы запрашиваются по требованию
- Нет гарантии, что все нужные свечи доступны

**Последствия:**
- ⚠️ Фильтры могут не работать, если API недоступен
- ⚠️ Задержки при первом использовании фильтра
- ⚠️ Нет гарантии свежести данных

---

### **Проблема 3: Отсутствие обновления свечей других таймфреймов** ⚠️

**Текущая ситуация:**
- Только 1m свечи обновляются через WebSocket
- 5m, 1H, 1D свечи не обновляются автоматически
- Обновляются только при запросе через API

**Последствия:**
- ⚠️ Устаревшие данные для старших таймфреймов
- ⚠️ Неточные расчеты индикаторов

---

## ✅ ЧТО РАБОТАЕТ ПРАВИЛЬНО

### **1. 1m свечи** ✅

- ✅ Загружаются при старте (200 свечей)
- ✅ Хранятся в `DataRegistry.CandleBuffer`
- ✅ Обновляются инкрементально через WebSocket
- ✅ Достаточно для базовых индикаторов
- ✅ Используются в основных модулях

### **2. Использование DataRegistry** ✅

- ✅ `SignalGenerator` использует свечи из `DataRegistry`
- ✅ `AdaptiveRegimeManager` использует свечи из `DataRegistry`
- ✅ `ADX Filter` использует свечи из `DataRegistry`

---

## ⚠️ ЧТО ТРЕБУЕТ УЛУЧШЕНИЯ

### **1. Старшие таймфреймы (5m, 1H, 1D)** ⚠️

**Рекомендации:**

1. **Загружать при старте:**
   - 5m: 100-200 свечей (для Multi-Timeframe и Correlation)
   - 1H: 50-100 свечей (для Volume Profile)
   - 1D: 5-10 свечей (для Pivot Points)

2. **Хранить в DataRegistry:**
   - Создать `CandleBuffer` для каждого таймфрейма
   - Обновлять инкрементально (когда формируется новая свеча)

3. **Использовать в фильтрах:**
   - `Multi-Timeframe Filter` → использовать свечи из `DataRegistry`
   - `Volume Profile Filter` → использовать свечи из `DataRegistry`
   - `Pivot Points Filter` → использовать свечи из `DataRegistry`
   - `Correlation Filter` → использовать свечи из `DataRegistry`

---

## 📊 СВОДКА ПО МОДУЛЯМ

| Модуль | Таймфрейм | Статус | Проблема |
|--------|-----------|--------|----------|
| **SignalGenerator** | 1m | ✅ | Правильно использует DataRegistry |
| **AdaptiveRegimeManager** | 1m | ✅ | Правильно использует DataRegistry |
| **ADX Filter** | 1m | ✅ | Правильно использует DataRegistry |
| **Multi-Timeframe** | 5m | ⚠️ | Запрашивает через API, не использует DataRegistry |
| **Volume Profile** | 1H | ⚠️ | Запрашивает через API, не использует DataRegistry |
| **Pivot Points** | 1D | ⚠️ | Запрашивает через API, не использует DataRegistry |
| **Correlation** | 5m | ⚠️ | Запрашивает через API, не использует DataRegistry |

---

## 🎯 ВЫВОДЫ

### **Что работает правильно:** ✅

1. ✅ 1m свечи загружаются и обновляются правильно
2. ✅ Основные модули используют свечи из DataRegistry
3. ✅ Инкрементальное обновление 1m свечей через WebSocket работает

### **Что требует улучшения:** ⚠️

1. ⚠️ Старшие таймфреймы (5m, 1H, 1D) не загружаются при старте
2. ⚠️ Фильтры запрашивают свечи через API вместо DataRegistry
3. ⚠️ Нет автоматического обновления старших таймфреймов

### **Критичность:** 🟡 Средняя

- Основная функциональность работает (1m свечи)
- Фильтры работают, но неэффективно (запросы к API)
- Могут быть проблемы при недоступности API

---

**Рекомендация:** Улучшить систему свечей, добавив:
1. Загрузку старших таймфреймов при старте
2. Хранение в DataRegistry
3. Использование в фильтрах вместо прямых запросов к API






