# ✅ ЛОГИРОВАНИЕ СИСТЕМЫ СВЕЧЕЙ ДОБАВЛЕНО

**Дата:** 26.11.2025 23:00  
**Статус:** ✅ **РЕАЛИЗОВАНО**

---

## 🎯 ЦЕЛЬ

Добавить логирование для системы свечей, чтобы видеть все моменты работы и правильно анализировать логи.

---

## 📊 ТРИ ТИПА ЛОГОВ

### **1. Основной лог (DEBUG+)** 
**Файл:** `logs/futures/futures_main_{date}.log`  
**Уровень:** DEBUG+  
**Назначение:** Полное детальное логирование всех событий

### **2. INFO лог (INFO+)**
**Файл:** `logs/futures/info_{date}.log`  
**Уровень:** INFO+  
**Назначение:** Важные события и статусы

### **3. ERROR лог (ERROR+)**
**Файл:** `logs/futures/errors_{date}.log`  
**Уровень:** ERROR+  
**Назначение:** Только ошибки и критические проблемы

### **Дополнительно:**
- **StructuredLogger:** Структурированные данные (JSON) для анализа
  - `candles_init_{date}.json` - инициализация свечей
  - `candles_new_{date}.json` - создание новых свечей
  - `candles_usage_{date}.json` - использование свечей фильтрами
  - `candles_stats_{date}.json` - статистика буферов

---

## ✅ РЕАЛИЗОВАНО

### **1. Логирование инициализации свечей** ✅

**Где:** `src/strategies/scalping/futures/orchestrator.py`  
**Метод:** `_initialize_candle_buffers()`

**Что логируется:**

**INFO лог:**
- ✅ Начало инициализации
- ✅ Для каждого символа и таймфрейма: количество загруженных свечей
- ✅ Итоговая статистика (X буферов для Y символов)
- ✅ Ошибки для конкретных символов/таймфреймов

**StructuredLogger:**
- ✅ Для каждого успешного буфера: `log_candle_init()` → `candles_init_{date}.json`
- ✅ Для каждой ошибки: `log_candle_init()` с `status="error"` → `candles_init_{date}.json`
- ✅ Итоговая статистика: `log_candle_init()` с `symbol="ALL"` → `candles_init_{date}.json`

**Формат:**
```json
{
  "timestamp": "2025-11-26T23:00:00",
  "type": "candle_init",
  "symbol": "BTC-USDT",
  "timeframe": "5m",
  "candles_count": 200,
  "status": "success",
  "error": null
}
```

---

### **2. Логирование создания новых свечей** ✅

**Где:** `src/strategies/scalping/futures/coordinators/websocket_coordinator.py`  
**Метод:** `_update_candle_for_timeframe()`

**Что логируется:**

**INFO лог:**
- ✅ Создание новых свечей для важных таймфреймов (5m, 1H, 1D)
- ✅ Формат: `📊 Создана новая свеча {symbol} {timeframe}: timestamp={timestamp}, price={price}`

**DEBUG лог:**
- ✅ Создание новых свечей для 1m (слишком часто для INFO)
- ✅ Обновление формирующихся свечей (можно отключить, если слишком много)

**StructuredLogger:**
- ✅ Для важных таймфреймов (5m, 1H, 1D): `log_candle_new()` → `candles_new_{date}.json`
- ✅ Не логируем 1m (слишком часто, перегрузит файл)

**Формат:**
```json
{
  "timestamp": "2025-11-26T23:00:00",
  "type": "candle_new",
  "symbol": "BTC-USDT",
  "timeframe": "5m",
  "candle_timestamp": 1701024000,
  "price": 50100.50,
  "open": 50000.00,
  "high": 50200.00,
  "low": 49900.00,
  "close": 50100.50
}
```

---

### **3. Логирование использования свечей в фильтрах** ✅

**Где:** 
- `src/strategies/modules/multi_timeframe.py`
- `src/strategies/modules/volume_profile_filter.py` (TODO)
- `src/strategies/modules/pivot_points.py` (TODO)
- `src/filters/correlation_manager.py` (TODO)

**Что логируется:**

**INFO лог:**
- ✅ Fallback к API (когда DataRegistry не содержит достаточно свечей)
- ✅ Формат: `⚠️ MTF: DataRegistry содержит недостаточно свечей для {symbol}, используем fallback к API`

**DEBUG лог:**
- ✅ Получение свечей из DataRegistry
- ✅ Получение свечей через API
- ✅ Количество полученных свечей

**StructuredLogger:**
- ✅ Для каждого использования: `log_candle_usage()` → `candles_usage_{date}.json`
- ✅ Указывается источник (dataregistry или api)
- ✅ Указывается, был ли fallback к API

**Формат:**
```json
{
  "timestamp": "2025-11-26T23:00:00",
  "type": "candle_usage",
  "filter_name": "MTF",
  "symbol": "BTC-USDT",
  "timeframe": "5m",
  "source": "dataregistry",
  "candles_count": 200,
  "fallback_to_api": false
}
```

---

## 📋 РАСПРЕДЕЛЕНИЕ ПО ЛОГАМ

### **INFO лог (`info_{date}.log`):**
- ✅ Начало/конец инициализации свечей
- ✅ Итоговая статистика инициализации
- ✅ Создание новых свечей (5m, 1H, 1D)
- ✅ Fallback к API в фильтрах (важно знать!)

### **DEBUG лог (`futures_main_{date}.log`):**
- ✅ Детали инициализации для каждого символа/таймфрейма
- ✅ Создание новых свечей 1m
- ✅ Обновление формирующихся свечей
- ✅ Получение свечей фильтрами (детали)

### **ERROR лог (`errors_{date}.log`):**
- ✅ Критические ошибки инициализации
- ✅ Ошибки обновления свечей
- ✅ Ошибки получения свечей

### **StructuredLogger (JSON файлы):**
- ✅ `candles_init_{date}.json` - все события инициализации
- ✅ `candles_new_{date}.json` - все новые свечи (5m, 1H, 1D)
- ✅ `candles_usage_{date}.json` - использование свечей фильтрами
- ✅ `candles_stats_{date}.json` - статистика буферов (TODO)

---

## 🔍 КАК АНАЛИЗИРОВАТЬ

### **1. Проверка инициализации:**
```bash
# Смотрим INFO лог
grep "Инициализация буферов свечей" logs/futures/info_*.log

# Смотрим StructuredLogger
cat logs/futures/structured/candles_init_*.json | jq '.[] | select(.status=="error")'
```

### **2. Проверка обновления свечей:**
```bash
# Смотрим INFO лог (только важные таймфреймы)
grep "Создана новая свеча" logs/futures/info_*.log | grep -E "5m|1H|1D"

# Смотрим StructuredLogger
cat logs/futures/structured/candles_new_*.json | jq '.[] | select(.timeframe=="5m")'
```

### **3. Проверка использования DataRegistry:**
```bash
# Смотрим INFO лог (fallback к API)
grep "fallback к API" logs/futures/info_*.log

# Смотрим StructuredLogger
cat logs/futures/structured/candles_usage_*.json | jq '.[] | select(.fallback_to_api==true)'
```

### **4. Статистика:**
```bash
# Сколько раз использовался DataRegistry vs API
cat logs/futures/structured/candles_usage_*.json | jq '[.[] | .source] | group_by(.) | map({source: .[0], count: length})'
```

---

## ✅ ЧТО ДОБАВЛЕНО

### **В StructuredLogger:**
1. ✅ `log_candle_init()` - инициализация буферов
2. ✅ `log_candle_new()` - создание новых свечей
3. ✅ `log_candle_usage()` - использование свечей фильтрами
4. ✅ `log_candle_stats()` - статистика буферов (готово, но не используется)

### **В Orchestrator:**
1. ✅ Логирование инициализации в StructuredLogger
2. ✅ Логирование ошибок инициализации в StructuredLogger
3. ✅ Логирование итоговой статистики

### **В WebSocketCoordinator:**
1. ✅ Логирование создания новых свечей (INFO для важных, DEBUG для 1m)
2. ✅ Логирование в StructuredLogger (только для важных таймфреймов)

### **В Multi-Timeframe Filter:**
1. ✅ Логирование использования DataRegistry
2. ✅ Логирование fallback к API (INFO уровень)
3. ✅ Логирование использования API

---

## 🚧 TODO (ОПЦИОНАЛЬНО)

1. 🚧 Добавить логирование в Volume Profile Filter
2. 🚧 Добавить логирование в Pivot Points Filter
3. 🚧 Добавить логирование в Correlation Filter
4. 🚧 Добавить периодическую статистику буферов (каждые 10 минут)

---

## 📊 ПРИМЕРЫ ЛОГОВ

### **INFO лог:**
```
2025-11-26 23:00:00 | INFO     | orchestrator:_initialize_candle_buffers | 📊 Инициализация буферов свечей для всех символов и таймфреймов...
2025-11-26 23:00:01 | INFO     | orchestrator:_initialize_candle_buffers | ✅ Инициализирован буфер свечей 5m для BTC-USDT (200 свечей, Multi-Timeframe и Correlation)
2025-11-26 23:00:05 | INFO     | orchestrator:_initialize_candle_buffers | 📊 Инициализация буферов свечей завершена: 12 буферов для 3 символов
2025-11-26 23:05:00 | INFO     | websocket_coordinator:_update_candle_for_timeframe | 📊 Создана новая свеча BTC-USDT 5m: timestamp=1701024300, price=50100.50
```

### **DEBUG лог:**
```
2025-11-26 23:00:01 | DEBUG    | orchestrator:_initialize_candle_buffers | ✅ Инициализирован буфер свечей 1m для BTC-USDT (200 свечей, основные индикаторы)
2025-11-26 23:01:00 | DEBUG    | websocket_coordinator:_update_candle_for_timeframe | 📊 Создана новая свеча BTC-USDT 1m: timestamp=1701024060, price=50100.50
2025-11-26 23:05:00 | DEBUG    | multi_timeframe:_get_htf_candles | MTF: Получено 200 свечей 5m для BTC-USDT из DataRegistry
```

### **ERROR лог:**
```
2025-11-26 23:00:02 | ERROR    | orchestrator:_initialize_candle_buffers | ❌ Критическая ошибка инициализации буферов свечей: Connection timeout
```

---

**Статус:** ✅ **ОСНОВНОЕ ЛОГИРОВАНИЕ РЕАЛИЗОВАНО**

Теперь можно анализировать работу системы свечей через логи!

