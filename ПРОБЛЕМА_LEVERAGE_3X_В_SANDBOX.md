# ПРОБЛЕМА: Leverage 3x в Sandbox вместо 5x из конфига

## 🔍 ПРОБЛЕМА

Позиции открываются с leverage 3x, хотя в конфиге указано 5x.

## 🔎 ПРИЧИНЫ

1. **В sandbox mode установка leverage через API может не работать**
   - API возвращает ошибку `Parameter posSide error`
   - Или leverage не изменяется на бирже даже при успешном ответе

2. **Leverage устанавливается на бирже, а не передается при открытии позиции**
   - В OKX API leverage НЕ передается в запросе на открытие позиции
   - Используется leverage, который был установлен на бирже ранее

3. **В sandbox может быть установлен leverage 3x по умолчанию**
   - И этот leverage не меняется через API
   - Нужно установить вручную на бирже

## ✅ РЕШЕНИЕ

### Вариант 1: Установить leverage вручную на бирже (РЕКОМЕНДУЕТСЯ)

1. Зайдите на OKX sandbox
2. Откройте Futures торговлю
3. Для каждого символа (BTC-USDT, ETH-USDT, SOL-USDT, DOGE-USDT, XRP-USDT):
   - Установите leverage 5x вручную
   - Выберите isolated margin mode
   - Сохраните настройки

### Вариант 2: Использовать код с двумя попытками установки leverage

Я обновил код так, чтобы он пробовал установить leverage двумя способами:
1. Сначала без `posSide` (для isolated mode с net позициями)
2. Если не получилось, то с `posSide="long"` (может потребоваться для sandbox)

Но если это не работает, нужно установить leverage вручную на бирже.

### Вариант 3: Проверить режим позиции (Position Mode)

Возможно, на бирже установлен режим позиции "hedge mode" вместо "net mode":
- В hedge mode нужно указывать `posSide` при установке leverage
- В net mode `posSide` не требуется

## 📝 ТЕКУЩИЙ КОД

Код пытается установить leverage двумя способами:
1. Без `posSide` (для net mode)
2. С `posSide="long"` (для hedge mode или sandbox)

Если оба способа не работают, логируется предупреждение, но бот продолжает работать.

## 🎯 РЕКОМЕНДАЦИЯ

**Установите leverage 5x вручную на бирже OKX sandbox для каждого символа перед запуском бота.**

Это гарантирует, что позиции будут открываться с правильным leverage, даже если API не работает.

