# 🔍 АНАЛИЗ ПРОБЛЕМЫ ЛОЖНОГО ОБНАРУЖЕНИЯ ЗАКРЫТИЯ ПОЗИЦИЙ

**Дата:** 03 января 2026  
**Статус:** Критичность: **СРЕДНЯЯ/ВЫСОКАЯ**

---

## 📋 СУТЬ ПРОБЛЕМЫ

Бот закрывает позицию через `_close_position()`, но при следующей синхронизации `_sync_positions_with_exchange()` не распознает, что закрытие было инициировано самим ботом, и выдает ложное сообщение:

**Пример из логов:**
```
2026-01-03 12:35:21.205 | INFO | 🛑 Закрытие позиции ETH-USDT: sl_reached (side=long, size=0.6, entry=4042.7085, time=0.98 мин)
2026-01-03 12:35:21.510 | INFO | 🔄 Закрытие позиции ETH-USDT long размер=0.6 контрактов, PnL=-0.38 USDT
2026-01-03 12:35:21.843 | INFO | ✅ Позиция ETH-USDT успешно закрыта через API
...
2026-01-03 12:35:24.826 | CRITICAL | 🚨 ОБНАРУЖЕНО ЗАКРЫТИЕ НА БИРЖЕ: ETH-USDT
2026-01-03 12:35:24.827 | CRITICAL | ⚠️ Позиция закрыта на бирже, но НЕ через бота!
```

**Проблема:** Позиция была закрыта ботом (строка 12:35:21.205), но через 3 секунды появилось критическое сообщение о внешнем закрытии (строка 12:35:24.826).

---

## 🎯 КРИТИЧНОСТЬ

### ✅ **СРЕДНЯЯ/ВЫСОКАЯ** (не критично для функциональности, но критично для диагностики)

**Почему не критично для функциональности:**
- Бот работает правильно
- Позиции закрываются корректно
- Нет потери данных или денег

**Почему критично для диагностики:**
- ❌ **Ложные тревоги в логах** - засоряют логи, мешают анализу
- ❌ **Неправильный анализ работы бота** - может показать, что позиции закрываются внешне, хотя это не так
- ❌ **Скрытие реальных проблем** - если действительно будет внешнее закрытие (ликвидация, TSL на бирже), его будет сложно отличить от ложного срабатывания
- ❌ **Неправильная статистика** - может влиять на метрики и отчеты

---

## 🔍 ВСЕ МЕСТА БЕЗ ПРОВЕРКИ

### 1. ✅ **КРИТИЧНО** - `orchestrator._sync_positions_with_exchange()` (строка 2462)

**Файл:** `src/strategies/scalping/futures/orchestrator.py`  
**Строка:** 2462  
**Проблема:** Выводит сообщение о внешнем закрытии без проверки `_closing_positions_cache`

**Код (текущая проблема):**
```python
# Файл: src/strategies/scalping/futures/orchestrator.py:2388-2462
# Строка 2388-2458: Проверка stale_symbols
for symbol in list(stale_symbols):
    # ... проверки ...
    
    # Строка 2462: ВЫВОД СООБЩЕНИЯ БЕЗ ПРОВЕРКИ _closing_positions_cache
    logger.critical("=" * 80)
    logger.critical(f"🚨 ОБНАРУЖЕНО ЗАКРЫТИЕ НА БИРЖЕ: {symbol}")
    logger.critical(f"⚠️ Позиция закрыта на бирже, но НЕ через бота!")
    # ... остальной код обработки внешнего закрытия ...
```

**Проблема:** Код не проверяет `_closing_positions_cache` перед логированием, поэтому даже если позиция была закрыта ботом, появляется ложное предупреждение.

**Решение:** Добавить проверку перед строкой 2462:
```python
# Файл: src/strategies/scalping/futures/orchestrator.py:2458-2462
# ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ (03.01.2026): Проверяем, что бот сам закрыл позицию
if hasattr(self, "_closing_positions_cache") and symbol in self._closing_positions_cache:
    logger.debug(
        f"🔍 {symbol}: Позиция закрыта ботом (в _closing_positions_cache), "
        f"не логируем как внешнее закрытие"
    )
    # Удаляем из кэша, так как уже обработали
    try:
        del self._closing_positions_cache[symbol]
    except KeyError:
        pass
    continue  # Пропускаем обработку как внешнее закрытие

# Только если позиция НЕ в кэше - это действительно внешнее закрытие
logger.critical("=" * 80)
logger.critical(f"🚨 ОБНАРУЖЕНО ЗАКРЫТИЕ НА БИРЖЕ: {symbol}")
logger.critical(f"⚠️ Позиция закрыта на бирже, но НЕ через бота!")
# ... остальной код обработки внешнего закрытия ...
```

**Где добавляется символ в cache:**
```python
# Файл: src/strategies/scalping/futures/orchestrator.py:4363
# ✅ НОВОЕ (03.01.2026): Добавляем символ в cache ПЕРЕД вызовом API
if not hasattr(self, "_closing_positions_cache"):
    self._closing_positions_cache = {}
self._closing_positions_cache[symbol] = time.time()  # TTL 60 секунд

# Вызываем API для закрытия позиции
await self.client.close_position(...)
```

**Проблема с удалением из cache:**
```python
# Файл: src/strategies/scalping/futures/orchestrator.py:4747
# ❌ ПРОБЛЕМА: Символ удаляется из cache в КОНЦЕ метода _close_position()
# Но синхронизация может произойти ДО удаления, создавая race condition
try:
    del self._closing_positions_cache[symbol]
except KeyError:
    pass
```

**Рекомендация:** НЕ удалять символ из cache в конце метода `_close_position()`. Оставить символ в cache до автоматического удаления через TTL (60 сек) или до обработки в `_sync_positions_with_exchange()`.

---

### 2. ⚠️ **НЕ КРИТИЧНО** - `position_sync.sync_positions_with_exchange()` (строка 237)

**Файл:** `src/strategies/scalping/futures/core/position_sync.py`  
**Строка:** 237  
**Проблема:** Удаляет позиции из `stale_symbols`, но НЕ логирует как внешнее закрытие

**Код:**
```python
# Строка 237-240: Просто удаляет позиции
stale_symbols = set(self.active_positions.keys()) - seen_symbols
for symbol in list(stale_symbols):
    logger.warning(f"DRIFT_REMOVE {symbol} not on exchange")
    self.active_positions.pop(symbol, None)
```

**Статус:** ✅ **НЕ КРИТИЧНО** - здесь нет логирования внешнего закрытия, только удаление. Но можно добавить проверку для консистентности.

---

## 🔄 МЕСТА, ГДЕ ВЫЗЫВАЕТСЯ СИНХРОНИЗАЦИЯ ПОСЛЕ ЗАКРЫТИЯ

### 1. `_close_position()` (строка 4718)
- Вызывает синхронизацию после закрытия
- **Проблема:** Race condition - синхронизация может произойти до того, как позиция удалится из `active_positions`

### 2. `position_manager.close_position_manually()` (строка 4778)
- Вызывает синхронизацию после закрытия
- **Проблема:** Аналогичная race condition

### 3. `websocket_coordinator.handle_private_ws_positions()` (строка 683)
- Вызывает синхронизацию при обнаружении закрытия через WebSocket
- **Проблема:** Может быть закрытие через бота, но WebSocket получил обновление раньше

---

## 📊 СТАТИСТИКА ПРОБЛЕМНЫХ МЕСТ

| Место | Критичность | Проверка нужна? | Статус |
|-------|-------------|-----------------|--------|
| `orchestrator._sync_positions_with_exchange()` | 🔴 ВЫСОКАЯ | ✅ ДА | ❌ НЕТ |
| `position_sync.sync_positions_with_exchange()` | 🟡 НИЗКАЯ | ⚠️ ЖЕЛАТЕЛЬНО | ❌ НЕТ |
| `websocket_coordinator` | 🟡 НИЗКАЯ | ⚠️ ЖЕЛАТЕЛЬНО | ❌ НЕТ |

---

## ✅ РЕШЕНИЕ

### Основное исправление (КРИТИЧНО):

**Файл:** `src/strategies/scalping/futures/orchestrator.py`  
**Метод:** `_sync_positions_with_exchange()`  
**Строка:** ~2458 (перед строкой 2462)

**Полное решение с кодом:**

**Шаг 1: Исправить `_sync_positions_with_exchange()` (строка ~2458):**
```python
# Файл: src/strategies/scalping/futures/orchestrator.py:2458-2462
# ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ (03.01.2026): Проверяем, что бот сам закрыл позицию
if hasattr(self, "_closing_positions_cache") and symbol in self._closing_positions_cache:
    logger.debug(
        f"🔍 {symbol}: Позиция закрыта ботом (в _closing_positions_cache), "
        f"не логируем как внешнее закрытие"
    )
    # Удаляем из кэша, так как уже обработали
    try:
        del self._closing_positions_cache[symbol]
    except KeyError:
        pass
    continue  # Пропускаем обработку как внешнее закрытие

# Только если позиция НЕ в кэше - это действительно внешнее закрытие
logger.critical("=" * 80)
logger.critical(f"🚨 ОБНАРУЖЕНО ЗАКРЫТИЕ НА БИРЖЕ: {symbol}")
logger.critical(f"⚠️ Позиция закрыта на бирже, но НЕ через бота!")
# ... остальной код обработки внешнего закрытия ...
```

**Шаг 2: Убрать удаление из cache в конце `_close_position()` (строка 4747):**
```python
# Файл: src/strategies/scalping/futures/orchestrator.py:4747
# ❌ УДАЛИТЬ ЭТОТ КОД (или закомментировать):
# try:
#     del self._closing_positions_cache[symbol]
# except KeyError:
#     pass

# ✅ ВМЕСТО ЭТОГО: Оставить символ в cache до автоматического удаления через TTL
# или до обработки в _sync_positions_with_exchange()
```

**Шаг 3: Добавить автоматическую очистку cache (опционально):**
```python
# Файл: src/strategies/scalping/futures/orchestrator.py:2458
# Очистка устаревших записей из cache (TTL 60 секунд)
if hasattr(self, "_closing_positions_cache"):
    current_time = time.time()
    expired_symbols = [
        s for s, timestamp in self._closing_positions_cache.items()
        if current_time - timestamp > 60.0
    ]
    for expired_symbol in expired_symbols:
        try:
            del self._closing_positions_cache[expired_symbol]
        except KeyError:
            pass
```

**Ожидаемый результат после исправления:**
```
2026-01-03 12:35:21.205 | INFO | 🛑 Закрытие позиции ETH-USDT: sl_reached
2026-01-03 12:35:21.843 | INFO | ✅ Позиция ETH-USDT успешно закрыта через API
...
2026-01-03 12:35:24.826 | DEBUG | 🔍 ETH-USDT: Позиция закрыта ботом (в _closing_positions_cache), не логируем как внешнее закрытие
# ❌ НЕТ критического сообщения о внешнем закрытии
```

---

## 🎯 ИТОГО

- **Критичных мест:** 1 (`orchestrator._sync_positions_with_exchange`)
- **Желательных мест:** 2 (`position_sync`, `websocket_coordinator`)
- **Критичность:** СРЕДНЯЯ/ВЫСОКАЯ (для диагностики)
- **Влияние на функциональность:** НЕТ
- **Влияние на диагностику:** ДА (ложные тревоги)

---

**Рекомендация:** Исправить основное место (`orchestrator._sync_positions_with_exchange`) - это решит 90% проблем. Остальные места можно исправить позже для консистентности.

