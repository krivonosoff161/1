# ✅ ЭТАП 2: ИНТЕГРАЦИЯ EntryManager НАЧАТА

**Дата:** 25.11.2025  
**Статус:** В процессе реализации

---

## ✅ ВЫПОЛНЕНО

1. ✅ Импорт EntryManager добавлен в orchestrator
2. ✅ EntryManager создан в orchestrator после order_executor
3. ✅ EntryManager добавлен в SignalCoordinator (параметр и сохранение)

---

## ⏳ В ПРОЦЕССЕ

### Замена логики открытия позиций

**Текущая ситуация:**
- `execute_signal_from_price()` использует `order_executor.execute_signal()` напрямую
- После открытия позиции выполняется много логики:
  - Проверка открытия позиции
  - Обновление кэша
  - Синхронизация entry price
  - Обновление total_margin_used
  - Добавление в MaxSizeLimiter
  - Сохранение в active_positions
  - Инициализация trailing stop loss

**Проблема:**
- EntryManager.open_position() регистрирует в PositionRegistry
- Но не выполняет всю остальную логику (кэши, лимитеры, trailing stop)

**Решение:**
- Использовать EntryManager для открытия позиции
- Сохранить всю остальную логику после открытия
- Или расширить EntryManager для выполнения всей логики

---

## 📋 ПЛАН ДАЛЬНЕЙШИХ ДЕЙСТВИЙ

1. ⏳ Заменить `order_executor.execute_signal()` на `entry_manager.open_position()`
2. ⏳ Адаптировать сигнатуру (EntryManager ожидает regime, regime_params, balance_profile)
3. ⏳ Сохранить всю логику после открытия (кэши, лимитеры, trailing stop)
4. ⏳ Протестировать открытие позиций через EntryManager

---

**Продолжаю реализацию...**

