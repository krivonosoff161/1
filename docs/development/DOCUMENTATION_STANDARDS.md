# Стандарты документации

## Цель
- Документация должна быть однозначной, без дублей и с понятной точкой входа.

## Точка входа
- `docs/README.md` — навигация по разделам.
- `docs/PROJECT_ROADMAP.md` — единая дорожная карта.

## Размещение файлов
- Новые документы **не кладем** в корень `docs/`.
- Используем профильные каталоги:
  - `docs/current/` — актуальные рабочие материалы.
  - `docs/plans/active/` — активные планы.
  - `docs/reports/` — отчеты по сессиям/проверкам.
  - `docs/analysis/` — аналитика и разбор причин.
  - `docs/fixes/` — выполненные исправления.
  - `docs/archive/` — исторические материалы.

## Именование
- Формат для отчетов: `YYYY-MM-DD_<topic>.md`.
- Формат для планов: `YYYY-MM-DD_plan_<topic>.md`.
- Не использовать эмоциональные префиксы (`✅`, `🔴`, `⚠️`) в новых именах.
- Один документ = одна тема.

## Жизненный цикл документа
- `draft` -> `active` -> `archived`.
- При закрытии темы документ переносится в архив с сохранением истории.
- При обновлении темы создается новый документ, а не дублируется старый.

## Правила дедупликации
- Точные дубли удаляются автоматически.
- Почти дубли переносятся в `docs/archive/deduplicated/` с манифестом.
- Канонический документ выбирается по приоритету:
  1. `current`/`plans/active`
  2. `architecture`/`specifications`
  3. `reports`/`analysis`/`fixes`
  4. `archive`

## Контроль качества
- Раз в неделю запускать скрипт дедупликации и проверять манифест.
- Любое удаление должно быть обратимо через архив.
