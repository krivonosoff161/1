# ПРОБЛЕМА FALLBACK LIMIT OFFSET

**Дата:** 23 ноября 2025  
**Проблема:** Fallback для `limit_offset_percent` используется вместо адаптивных параметров

---

## 📊 ПРОБЛЕМА

### В логах:
```
⚠️ FALLBACK: Используется глобальный offset из конфига: 0.0% 
(per-symbol+regime и per-regime не найдены для DOGE-USDT, regime=ranging, 
by_symbol exists=False, by_regime exists=False)
```

### Что это означает:
- `by_symbol` не читается из конфига (`by_symbol exists=False`)
- `by_regime` не читается из конфига (`by_regime exists=False`)
- Используется fallback `0.0%` вместо адаптивных параметров

---

## 🔍 ПРИЧИНА

### Возможные причины:
1. ❌ `order_executor` конфиг не загружается правильно
2. ❌ `limit_order` не читается из `order_executor`
3. ❌ `by_symbol` не читается из `limit_order`
4. ❌ Структура конфига не соответствует ожидаемой

---

## ✅ РЕШЕНИЕ

### Нужно проверить:
1. ✅ Как загружается `order_executor` конфиг
2. ✅ Структура `scalping_config.order_executor`
3. ✅ Преобразование Pydantic объектов в dict
4. ✅ Логирование структуры конфига

---

## 📋 ПЛАН ИСПРАВЛЕНИЯ

### 1. Улучшить логирование:
- Логировать структуру `order_executor_config`
- Логировать структуру `limit_order_config`
- Логировать структуру `by_symbol_dict`

### 2. Проверить загрузку конфига:
- Проверить, как загружается `order_executor` из YAML
- Проверить преобразование Pydantic объектов
- Проверить вложенность структур

### 3. Исправить чтение конфига:
- Убедиться, что `by_symbol` читается правильно
- Убедиться, что `by_regime` читается правильно
- Убедиться, что преобразование в dict работает

---

**Статус:** 🔍 ИССЛЕДУЕТСЯ

