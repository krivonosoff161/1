# УНИФИКАЦИЯ LEVERAGE И МАСШТАБИРОВАНИЕ ПОЗИЦИЙ

**Дата:** 2025-12-21  
**Критическая проблема:** При добавлении к позиции с другим leverage создается НОВАЯ позиция

---

## 1. КРИТИЧЕСКАЯ ПРОБЛЕМА: LEVERAGE ПРИ ДОБАВЛЕНИИ К ПОЗИЦИИ

### Как работает OKX Futures с Isolated Margin

**Важное правило:**
- На OKX Futures с **isolated margin** каждая позиция имеет свой **leverage**
- Если leverage отличается → создается **НОВАЯ позиция** (не добавляется к существующей!)
- Пример:
  - Существующая позиция: SOL-USDT LONG, leverage=5x, размер=100 контрактов
  - Новый сигнал: leverage=3x → создастся **НОВАЯ** позиция SOL-USDT LONG, leverage=3x
  - Итого: **ДВЕ позиции** вместо одной!

**Проблемы:**
1. ❌ **Неэффективное использование маржи** - две позиции требуют больше маржи
2. ❌ **Сложность управления** - нужно управлять двумя позициями отдельно
3. ❌ **Проблемы с балансом** - может не хватить маржи для второй позиции
4. ❌ **Не работает лестничное добавление** - каждая позиция растет независимо

---

## 2. РЕШЕНИЯ ПРОБЛЕМЫ

### ✅ РЕШЕНИЕ 1: Использовать leverage существующей позиции (РЕКОМЕНДУЕТСЯ)

**Логика:**
- При добавлении к существующей позиции → используем leverage существующей позиции
- Игнорируем leverage из нового сигнала
- Гарантируем что добавление попадет в ту же позицию

**Преимущества:**
- ✅ Простое в реализации
- ✅ Гарантирует что позиция увеличится, а не создастся новая
- ✅ Не требует изменений в конфиге
- ✅ Работает с лестничным добавлением

**Недостатки:**
- ⚠️ Может использовать неоптимальный leverage (но это не критично для добавления)

**Реализация:**
```python
async def can_add_to_position(
    self,
    symbol: str,
    existing_position: Dict[str, Any],
    new_signal: Dict[str, Any],
    balance: float,
) -> Tuple[bool, str]:
    """
    Проверка возможности добавления к позиции.
    
    ✅ КРИТИЧЕСКОЕ: Используем leverage существующей позиции для добавления!
    """
    # 1. Получаем leverage существующей позиции
    existing_leverage = int(existing_position.get("lever", "0"))
    if existing_leverage <= 0:
        # Если leverage не найден, используем из конфига
        existing_leverage = getattr(self.scalping_config, "leverage", 3)
    
    # 2. Получаем leverage из нового сигнала
    new_signal_leverage = new_signal.get("leverage", existing_leverage)
    
    # 3. ✅ КРИТИЧЕСКОЕ: Если leverage отличается, ПЕРЕОПРЕДЕЛЯЕМ для добавления
    if new_signal_leverage != existing_leverage:
        logger.warning(
            f"⚠️ Leverage отличается: существующая позиция={existing_leverage}x, "
            f"новый сигнал={new_signal_leverage}x. Используем leverage={existing_leverage}x для добавления."
        )
        # Переопределяем leverage в сигнале
        new_signal["leverage"] = existing_leverage
        new_signal["original_leverage"] = new_signal_leverage  # Сохраняем для логирования
    
    # 4. Продолжаем проверки (количество добавлений, интервал, убыток, лимиты)
    # ...
```

### ⚠️ РЕШЕНИЕ 2: Блокировать добавление если leverage отличается

**Логика:**
- Если leverage нового сигнала отличается от существующей позиции → блокировать добавление
- Только если leverage совпадает → разрешить добавление

**Преимущества:**
- ✅ Гарантирует что leverage всегда одинаковый
- ✅ Простое в реализации

**Недостатки:**
- ❌ Блокирует многие добавления (если leverage часто меняется)
- ❌ Не использует возможность лестничного добавления
- ❌ Не оптимально - можно использовать leverage существующей позиции

### ❌ РЕШЕНИЕ 3: Унифицировать leverage для всех позиций по символу

**Логика:**
- Всегда использовать одинаковый leverage для одного символа
- При открытии первой позиции → устанавливать leverage
- При добавлении → всегда использовать этот leverage

**Преимущества:**
- ✅ Гарантирует единообразие
- ✅ Упрощает управление

**Недостатки:**
- ❌ Теряем адаптивность leverage (один из ключевых фич бота!)
- ❌ Не используем преимущества адаптивного leverage (разные leverage для разных условий)
- ❌ Требует сложной логики для переключения leverage

---

## 3. РЕКОМЕНДУЕМОЕ РЕШЕНИЕ: Использовать leverage существующей позиции

### Архитектура

**В PositionScalingManager:**
```python
class PositionScalingManager:
    async def calculate_next_addition_size(
        self,
        symbol: str,
        existing_position: Dict[str, Any],
        signal: Dict[str, Any],
        balance: float,
        balance_profile: str,
        regime: str,
    ) -> Optional[float]:
        """
        Рассчитать размер следующего добавления к позиции.
        
        ✅ КРИТИЧЕСКОЕ: Используем leverage существующей позиции!
        """
        # 1. Получаем leverage существующей позиции
        existing_leverage = self._get_position_leverage(existing_position)
        
        # 2. Переопределяем leverage в сигнале на leverage позиции
        original_signal_leverage = signal.get("leverage")
        signal["leverage"] = existing_leverage
        
        if original_signal_leverage != existing_leverage:
            logger.info(
                f"📊 PositionScaling {symbol}: Используем leverage существующей позиции "
                f"({existing_leverage}x вместо {original_signal_leverage}x из сигнала)"
            )
        
        # 3. Рассчитываем размер добавления с учетом leverage позиции
        # (остальная логика лестницы)
        # ...
    
    def _get_position_leverage(self, position: Dict[str, Any]) -> int:
        """
        Получить leverage существующей позиции.
        
        Returns:
            Leverage позиции или default из конфига
        """
        # Пробуем получить из позиции
        leverage_from_pos = position.get("lever", "0")
        try:
            leverage = int(leverage_from_pos)
            if leverage > 0:
                return leverage
        except (ValueError, TypeError):
            pass
        
        # Fallback: из конфига
        default_leverage = getattr(self.scalping_config, "leverage", 3)
        return default_leverage
```

**В SignalCoordinator:**
```python
# При проверке возможности добавления:
if position_in_signal_direction:
    # ✅ КРИТИЧЕСКОЕ: Получаем leverage существующей позиции
    existing_leverage = int(position_in_signal_direction.get("lever", "0"))
    if existing_leverage <= 0:
        existing_leverage = getattr(self.scalping_config, "leverage", 3)
    
    # Переопределяем leverage в сигнале
    signal_leverage = signal.get("leverage", existing_leverage)
    if signal_leverage != existing_leverage:
        logger.info(
            f"📊 Добавление к {symbol}: Используем leverage позиции "
            f"({existing_leverage}x вместо {signal_leverage}x из сигнала)"
        )
        signal["leverage"] = existing_leverage
    
    # Продолжаем проверки через PositionScalingManager
    # ...
```

### Преимущества этого подхода

1. ✅ **Работает с адаптивным leverage:**
   - Первая позиция открывается с оптимальным leverage (из AdaptiveLeverage)
   - Дальнейшие добавления используют leverage первой позиции
   - Это нормально - leverage обычно не меняется кардинально

2. ✅ **Не требует изменений в расчетах:**
   - Не нужно пересчитывать все параметры для разных leverage
   - Используем существующую логику расчета размера
   - Просто меняем leverage в сигнале перед расчетом

3. ✅ **Простое в реализации:**
   - Одна строка кода: `signal["leverage"] = existing_leverage`
   - Не требует изменений в конфиге
   - Не требует изменений в других модулях

4. ✅ **Безопасное:**
   - Гарантирует что позиция увеличится, а не создастся новая
   - Не влияет на первую позицию (она уже открыта)
   - Работает с лестничным добавлением

---

## 4. АЛЬТЕРНАТИВНОЕ РЕШЕНИЕ: Унификация leverage (НЕ РЕКОМЕНДУЕТСЯ)

### Если все же нужно унифицировать leverage

**Концепция:**
- Для каждого символа хранить "базовый leverage"
- При открытии первой позиции → устанавливать базовый leverage
- При добавлении → всегда использовать базовый leverage

**Проблемы:**
1. ❌ Теряем адаптивность leverage (ключевая фича бота!)
2. ❌ Нужно хранить "базовый leverage" где-то (PositionRegistry?)
3. ❌ Что делать если нужно изменить leverage? (закрыть все позиции?)
4. ❌ Сложная логика для переключения leverage

**Когда это может понадобиться:**
- Если leverage меняется слишком часто и это создает проблемы
- Но это редко - обычно leverage стабильный для одного символа

---

## 5. ВЛИЯНИЕ НА РАСЧЕТЫ МАРЖИ И РИСКОВ

### Текущая ситуация

**Проблема:**
- При расчете размера позиции используется leverage из сигнала
- Если leverage отличается от существующей позиции → неправильный расчет маржи

### Решение

**При добавлении к позиции:**
1. Используем leverage существующей позиции для расчета размера добавления
2. Рассчитываем маржу для добавления: `margin_needed = addition_size_usd / existing_leverage`
3. Проверяем доступную маржу: `available_margin >= margin_needed`

**Пример:**
```python
# Существующая позиция:
# - Размер: $500 USD
# - Leverage: 5x
# - Маржа: $500 / 5 = $100 USD

# Новое добавление:
# - Размер: $350 USD (70% от базового $500)
# - Leverage: 5x (используем leverage позиции!)
# - Маржа: $350 / 5 = $70 USD

# Проверка:
available_margin = balance - total_margin_used
if $70 <= available_margin * 0.8:  # Оставляем 20% резерв
    # Разрешаем добавление
else:
    # Блокируем - недостаточно маржи
```

### Не нужно пересчитывать все параметры

**Важно:**
- ✅ Не нужно пересчитывать TP/SL для разных leverage
- ✅ Не нужно пересчитывать параметры для каждого leverage
- ✅ Просто используем leverage существующей позиции при добавлении
- ✅ Все расчеты остаются теми же, просто меняем leverage в сигнале

---

## 6. ИТОГОВАЯ РЕКОМЕНДАЦИЯ

### ✅ Решение: Использовать leverage существующей позиции

**Реализация:**
1. При добавлении к позиции → получаем leverage существующей позиции
2. Переопределяем leverage в сигнале: `signal["leverage"] = existing_leverage`
3. Рассчитываем размер добавления с этим leverage
4. Размещаем ордер с этим leverage

**Преимущества:**
- ✅ Простое в реализации (1 строка кода)
- ✅ Гарантирует что позиция увеличится
- ✅ Работает с лестничным добавлением
- ✅ Не требует изменений в конфиге
- ✅ Не теряем адаптивность leverage (для первой позиции)

**Недостатки:**
- ⚠️ Может использовать неоптимальный leverage для добавления (но это не критично)

---

## 7. ПАРАМЕТРЫ УВЕЛИЧЕНИЯ (ОБНОВЛЕНО)

### Агрессивные параметры (по запросу пользователя)

```yaml
position_scaling:
  # Агрессивные параметры
  max_additions: 7  # Было 5
  min_interval_seconds: 30  # Было 60
  max_loss_for_addition: -3.0%  # Было -2.0%
  
  # Лестница (агрессивная)
  ladder:
    - 1.0   # 100%
    - 0.8   # 80% (было 0.7)
    - 0.6   # 60% (было 0.5)
    - 0.4   # 40% (было 0.3)
    - 0.3   # 30% (было 0.2)
    - 0.2   # 20% (новое)
    - 0.15  # 15% (новое)
```

### Адаптивность по балансу и режиму

```yaml
position_scaling:
  by_balance:
    small:
      max_additions: 5  # Консервативно для small
      min_interval_seconds: 45
      max_loss_for_addition: -2.0%
    
    medium:
      max_additions: 7  # Агрессивно для medium
      min_interval_seconds: 30
      max_loss_for_addition: -3.0%
    
    large:
      max_additions: 10  # Очень агрессивно для large
      min_interval_seconds: 20
      max_loss_for_addition: -4.0%
  
  by_regime:
    trending:
      ladder_multiplier: 1.15  # +15% к коэффициентам
      max_loss_for_addition: -3.5%
    
    ranging:
      ladder_multiplier: 0.85  # -15% к коэффициентам
      max_loss_for_addition: -2.5%
    
    choppy:
      ladder_multiplier: 0.7  # -30% к коэффициентам
      max_loss_for_addition: -1.5%
      max_additions_override: 4  # Максимум 4 добавления
```

---

## 8. ЧАСТИЧНОЕ ЗАКРЫТИЕ: АДАПТИВНЫЙ МИНИМУМ

### Адаптивный минимальный размер Partial TP

```python
def get_min_partial_close_value_usd(
    self, balance: float, balance_profile: str
) -> float:
    """
    Получить минимальную стоимость для Partial TP (адаптивно по балансу).
    """
    # Базовые значения
    min_values = {
        "small": 3.0,    # $3 для small баланса
        "medium": 5.0,   # $5 для medium баланса
        "large": 10.0,   # $10 для large баланса
    }
    
    # Получаем значение по профилю баланса
    base_min = min_values.get(balance_profile, 5.0)
    
    # Можно добавить дополнительную адаптацию по балансу
    # Например, если баланс очень маленький (<$50), уменьшаем минимум
    if balance < 50.0:
        base_min = max(2.0, base_min * 0.7)  # -30%, но не меньше $2
    
    return base_min
```

---

## ИТОГОВЫЙ ПЛАН

### Этап 1: Исправления + Leverage унификация

1. ✅ Исправить все найденные ошибки
2. ✅ Добавить адаптивный минимальный размер Partial TP ($3/$5/$10)
3. ✅ Реализовать использование leverage существующей позиции при добавлении
4. ✅ Интеграция в PositionScalingManager и SignalCoordinator

### Этап 2: PositionScalingManager (агрессивные параметры)

1. ✅ Создать модуль PositionScalingManager
2. ✅ Реализовать агрессивные параметры (7 добавлений, 30 сек, -3%)
3. ✅ Реализовать лестницу (7 уровней)
4. ✅ Адаптивность по балансу и режиму
5. ✅ Проверки лимитов с учетом leverage позиции

### Этап 3: Автономные решения

1. ✅ Автономная оптимизация размера позиции
2. ✅ Автономная коррекция TP/SL

---

## ВОПРОСЫ ДЛЯ ПОДТВЕРЖДЕНИЯ

1. ✅ **Leverage:** Использовать leverage существующей позиции при добавлении? (ДА - рекомендую)
2. ✅ **Параметры:** Агрессивные (7 добавлений, 30 сек, -3%)? (ДА - по запросу)
3. ✅ **Partial TP:** Адаптивный минимум ($3/$5/$10)? (ДА - по запросу)

