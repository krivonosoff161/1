# МАСТЕР-ПЛАН ИСПРАВЛЕНИЙ: КРИТИЧЕСКИЕ ПРОБЛЕМЫ БОТА

**Дата:** 2025-12-20  
**Статус:** 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ ТРЕБУЮТ НЕМЕДЛЕННОГО ВНИМАНИЯ

---

## 📋 СВОДКА ПРОБЛЕМ

### Критичность проблем:
1. 🔴 **КРИТИЧНО:** Закрытие позиций - закрывает большие убытки, маленькие прибыли
2. 🔴 **КРИТИЧНО:** Блокировка новых ордеров при открытых позициях - теряем возможности
3. 🟠 **ВЫСОКАЯ:** Низкая эффективность исполнения ордеров (11.6%)
4. 🟠 **ВЫСОКАЯ:** Ошибки 51006 - цена вне лимитов биржи (54 раза)
5. 🟡 **СРЕДНЯЯ:** Монстр-файлы (position_manager.py - 5928 строк!)
6. 🟡 **СРЕДНЯЯ:** Ошибки API вызовов - нужен аудит
7. 🟡 **СРЕДНЯЯ:** Математика и анализ - проверить все конвертации
8. 🟢 **НИЗКАЯ:** Адаптивный леверидж (можно отложить)

---

## 🔴 ПРИОРИТЕТ #1: ПРОБЛЕМА ЗАКРЫТИЯ ПОЗИЦИЙ

### Проблема:
**Бот закрывает отрицательные позиции по $1-4, а положительные минимальные ($0.02, $0.3)**

### Анализ:
- **Текущая логика:** ExitAnalyzer, TrailingSL, TP/SL проверки
- **Проблема:** Бот не видит возможности для более прибыльного закрытия
- **Влияние:** Основная причина убытков (-$39.81 за 3 дня)

### Уточняющие вопросы:

1. **ExitAnalyzer - логика закрытия:**
   - Где находится: `src/strategies/scalping/futures/positions/exit_analyzer.py` (2425 строк)
   - Вопрос: Какой `min_profit_threshold` используется для закрытия?
   - Вопрос: Проверяется ли `peak_profit` перед закрытием?
   - Вопрос: Есть ли логика "не закрывать если можно больше заработать"?

2. **TrailingStopLoss - расчет PnL:**
   - Где находится: `src/strategies/scalping/futures/indicators/trailing_stop_loss.py` (908 строк)
   - Вопрос: Правильно ли рассчитывается `get_profit_pct()` от маржи?
   - Вопрос: Используется ли `margin_used` и `unrealized_pnl` из позиции?
   - Вопрос: Какой `min_profit_to_close` установлен?

3. **PositionManager - проверки TP/SL:**
   - Где находится: `src/strategies/scalping/futures/position_manager.py` (5928 строк!)
   - Вопрос: Как работает `_check_tp_only()` - какой порог прибыли?
   - Вопрос: Как работает `_check_sl()` - какой порог убытка?
   - Вопрос: Есть ли проверка "не закрывать маленькую прибыль если тренд сильный"?

4. **Profit Harvesting:**
   - Где находится: `position_manager.py:_check_profit_harvesting()`
   - Вопрос: Какой порог используется ($0.10, $0.15, $0.20)?
   - Вопрос: Закрывает ли он позиции слишком рано?

### План исправлений:

#### Шаг 1.1: Анализ текущей логики закрытия
- [ ] Проанализировать `exit_analyzer.py` - все методы закрытия
- [ ] Проанализировать `trailing_stop_loss.py` - расчет PnL
- [ ] Проанализировать `position_manager.py` - TP/SL проверки
- [ ] Найти где происходит закрытие с маленькой прибылью

#### Шаг 1.2: Исправить логику закрытия
- [ ] Добавить проверку `peak_profit` перед закрытием
- [ ] Не закрывать если `current_profit < peak_profit * 0.7` (не терять 30% прибыли)
- [ ] Увеличить `min_profit_threshold` для закрытия (например, 0.5% вместо 0.3%)
- [ ] Добавить проверку силы тренда - не закрывать в сильном тренде

#### Шаг 1.3: Улучшить SL логику
- [ ] Проверить почему SL срабатывает на больших убытках
- [ ] Убедиться что SL проверяется с актуальной ценой из стакана
- [ ] Добавить проверку: не закрывать по SL если цена близка к развороту

---

## 🔴 ПРИОРИТЕТ #2: БЛОКИРОВКА НОВЫХ ОРДЕРОВ

### Проблема:
**`allow_concurrent=false` блокирует новые сигналы при открытых позициях - теряем возможности**

### Анализ:
- **Текущая логика:** `signal_coordinator.py:1034-1046` - блокирует если позиция открыта
- **Проблема:** Бот не может открыть новую позицию даже если она в другом направлении
- **Предложение пользователя:** Убрать блок, разрешить до 5 позиций, бот сам закроет противоположные

### Уточняющие вопросы:

1. **Текущая логика блокировки:**
   - Где находится: `src/strategies/scalping/futures/coordinators/signal_coordinator.py:1034-1046`
   - Вопрос: Что происходит если сигнал в противоположном направлении?
   - Вопрос: Есть ли логика автоматического закрытия противоположной позиции?

2. **Лимит позиций:**
   - Где находится: `config_futures.yaml` - `max_open_positions`
   - Вопрос: Какой текущий лимит?
   - Вопрос: Как работает `balance_profile.max_open_positions`?

3. **Частичное закрытие:**
   - Где находится: `position_manager.py` - логика частичного закрытия
   - Вопрос: Работает ли частичное закрытие?
   - Вопрос: Можно ли увеличить позицию новым ордером?

### План исправлений:

#### Шаг 2.1: Изменить логику блокировки
- [ ] Убрать блокировку при `allow_concurrent=false`
- [ ] Разрешить до 5 позиций одновременно
- [ ] Если сигнал в противоположном направлении → закрыть старую позицию автоматически
- [ ] Если сигнал в том же направлении → увеличить позицию (или пропустить если уже большая)

#### Шаг 2.2: Обновить конфигурацию
- [ ] Установить `allow_concurrent_positions: true`
- [ ] Установить `max_open_positions: 5`
- [ ] Добавить логику автоматического закрытия противоположных позиций

---

## 🟠 ПРИОРИТЕТ #3: НИЗКАЯ ЭФФЕКТИВНОСТЬ ОРДЕРОВ (11.6%)

### Проблема:
**Только 11.6% ордеров исполняются (204 из 1764)**

### Анализ:
- **Причины:** Ордера размещаются далеко от цены, быстро устаревают
- **Влияние:** Упущенная прибыль, много таймаутов (2003)

### Уточняющие вопросы:

1. **Расчет лимитной цены:**
   - Где находится: `src/strategies/scalping/futures/order_executor.py:_calculate_limit_price()`
   - Вопрос: Какой `limit_offset_percent` используется?
   - Вопрос: Используется ли актуальная цена из стакана?
   - Вопрос: Проверяется ли свежесть цены перед размещением?

2. **Таймауты ордеров:**
   - Где находится: `order_executor.py` - логика таймаутов
   - Вопрос: Какой `max_wait_seconds` установлен?
   - Вопрос: Отменяется ли ордер если цена близка?

3. **Валидация цены:**
   - Где находится: `order_executor.py:_place_limit_order()`
   - Вопрос: Проверяется ли цена перед размещением?
   - Вопрос: Есть ли проверка лимитов биржи?

### План исправлений:

#### Шаг 3.1: Улучшить расчет лимитной цены
- [ ] Уменьшить `limit_offset_percent` для более агрессивного размещения
- [ ] Всегда использовать актуальную цену из стакана
- [ ] Проверять свежесть цены (не старше 1 секунды)

#### Шаг 3.2: Оптимизировать таймауты
- [ ] Увеличить `max_wait_seconds` для лимитных ордеров
- [ ] Не отменять ордер если цена близка к исполнению
- [ ] Проверять актуальность цены перед отменой

---

## 🟠 ПРИОРИТЕТ #4: ОШИБКИ 51006 (ЦЕНА ВНЕ ЛИМИТОВ)

### Проблема:
**54 ошибки "Order price is not within the price limit"**

### Анализ:
- **Причина:** Не проверяются лимиты биржи (`max_buy_price`, `min_sell_price`)
- **Влияние:** Ордера не размещаются, теряем возможности

### Уточняющие вопросы:

1. **Метод get_price_limits:**
   - Где находится: `src/clients/futures_client.py:get_price_limits()`
   - Вопрос: Возвращает ли он `max_buy_price` и `min_sell_price`?
   - Вопрос: Используется ли он перед размещением ордера?

2. **Валидация перед размещением:**
   - Где находится: `order_executor.py:_place_limit_order()`
   - Вопрос: Есть ли проверка лимитов биржи?
   - Вопрос: Корректируется ли цена если она вне лимитов?

### План исправлений:

#### Шаг 4.1: Добавить проверку лимитов
- [ ] Всегда вызывать `get_price_limits()` перед размещением
- [ ] Проверять что `limit_price` в диапазоне `[min_sell_price, max_buy_price]`
- [ ] Если вне лимитов → корректировать до допустимого значения
- [ ] Логировать все случаи коррекции цены

---

## 🟡 ПРИОРИТЕТ #5: РЕФАКТОРИНГ МОНСТР-ФАЙЛОВ

### Проблема:
**position_manager.py - 5928 строк! Невозможно контролировать**

### Анализ:
- **Текущие размеры:**
  - `position_manager.py`: 5928 строк
  - `signal_generator.py`: 4376 строк
  - `orchestrator.py`: 3785 строк
  - `exit_analyzer.py`: 2425 строк

### План рефакторинга:

#### Шаг 5.1: Разбить position_manager.py
- [ ] Вынести `_check_tp_only()` → `positions/take_profit_manager.py`
- [ ] Вынести `_check_sl()` → `positions/stop_loss_manager.py`
- [ ] Вынести `_check_profit_harvesting()` → `positions/profit_harvester.py`
- [ ] Вынести `_update_peak_profit()` → `positions/peak_profit_tracker.py`
- [ ] Вынести `_check_position_safety()` → `risk/position_safety_checker.py`
- [ ] Оставить только координацию в `position_manager.py`

#### Шаг 5.2: Разбить signal_generator.py
- [ ] Вынести методы генерации сигналов → `signals/rsi_signals.py`, `signals/macd_signals.py`, etc.
- [ ] Вынести фильтры → уже есть в `signals/filter_manager.py`
- [ ] Оставить только координацию

#### Шаг 5.3: Разбить orchestrator.py
- [ ] Вынести синхронизацию → `core/position_sync.py`
- [ ] Вынести управление циклом → уже есть `core/trading_control_center.py`
- [ ] Оставить только инициализацию и координацию

---

## 🟡 ПРИОРИТЕТ #6: АУДИТ API ВЫЗОВОВ

### Проблема:
**Мелкие ошибки API вызовов - нужен аудит документации биржи**

### План:

#### Шаг 6.1: Аудит всех API вызовов
- [ ] Проверить все методы в `futures_client.py`
- [ ] Сверить с официальной документацией OKX API
- [ ] Проверить форматы параметров (clOrdId, размеры, цены)
- [ ] Исправить все несоответствия

#### Шаг 6.2: Стандартизация
- [ ] Создать единый формат для всех API вызовов
- [ ] Добавить валидацию параметров перед вызовом
- [ ] Улучшить обработку ошибок

---

## 🟡 ПРИОРИТЕТ #7: ПРОВЕРКА МАТЕМАТИКИ И АНАЛИЗА

### Проблема:
**Проверить все конвертации, логику, метаданные - нет ли моментов когда бот "фантазирует"**

### План:

#### Шаг 7.1: Аудит математики
- [ ] Проверить все расчеты PnL (от маржи vs от цены)
- [ ] Проверить конвертации размеров (ctVal, size, notional)
- [ ] Проверить расчеты комиссий
- [ ] Проверить расчеты ликвидации

#### Шаг 7.2: Аудит логики
- [ ] Проверить все условия закрытия позиций
- [ ] Проверить логику фильтров сигналов
- [ ] Проверить метаданные позиций (regime, entry_time, etc.)
- [ ] Убедиться что нет "фантазирования" параметров

---

## 🟢 ПРИОРИТЕТ #8: АДАПТИВНЫЙ ЛЕВЕРИДЖ

### Проблема:
**Предложение: адаптивный леверидж 3,5,10,20,30 в зависимости от качества сигнала**

### План:

#### Шаг 8.1: Создать модуль адаптивного левериджа
- [ ] Создать `risk/adaptive_leverage.py`
- [ ] Логика: качество сигнала → леверидж
- [ ] Интеграция с системой защиты от убытков
- [ ] Тестирование на sandbox

---

## 📋 ПОРЯДОК ВЫПОЛНЕНИЯ

### Фаза 1: КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ (1-2 дня)
1. ✅ **Приоритет #1:** Исправить логику закрытия позиций
2. ✅ **Приоритет #2:** Убрать блокировку новых ордеров
3. ✅ **Приоритет #3:** Улучшить эффективность ордеров
4. ✅ **Приоритет #4:** Исправить ошибки 51006

### Фаза 2: ОПТИМИЗАЦИЯ (3-5 дней)
5. ✅ **Приоритет #7:** Проверить математику и анализ
6. ✅ **Приоритет #6:** Аудит API вызовов

### Фаза 3: РЕФАКТОРИНГ (1-2 недели)
7. ✅ **Приоритет #5:** Разбить монстр-файлы

### Фаза 4: УЛУЧШЕНИЯ (по желанию)
8. ✅ **Приоритет #8:** Адаптивный леверидж

---

## ❓ УТОЧНЯЮЩИЕ ВОПРОСЫ

### По проблеме закрытия позиций:

1. **Какие конкретные примеры из логов?**
   - Можно ли показать примеры когда закрылась позиция с маленькой прибылью?
   - Какой был `peak_profit` в этот момент?
   - Какой был текущий PnL?

2. **Какие параметры в конфиге?**
   - `min_profit_to_close` в TrailingSL?
   - `tp_percent` для разных режимов?
   - `sl_percent` для разных режимов?

3. **Как работает ExitAnalyzer?**
   - Какой `min_profit_threshold` используется?
   - Проверяется ли `peak_profit` перед закрытием?

### По блокировке ордеров:

1. **Текущая конфигурация:**
   - `allow_concurrent_positions: false`?
   - `max_open_positions: ?`

2. **Желаемое поведение:**
   - Разрешить до 5 позиций одновременно?
   - Автоматически закрывать противоположные?
   - Увеличивать позицию если сигнал в том же направлении?

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. **Сейчас:** Ответить на уточняющие вопросы
2. **Завтра:** Начать исправление Приоритета #1 (закрытие позиций)
3. **После:** Приоритет #2 (блокировка ордеров)
4. **Параллельно:** Приоритет #3 и #4 (ордера и валидация)

---

**Готов начать исправления после уточнений!** ✅



