# РЕШЕНИЕ: Leverage 3x вместо 5x в Sandbox

## 🔍 ПРОБЛЕМА

Позиции открываются с leverage 3x, хотя в конфиге указано 5x.

## ✅ ИСПРАВЛЕНИЕ

Обновил код так, чтобы он пробовал установить leverage **двумя способами**:

1. **Без `posSide`** (для isolated mode с net позициями)
2. **С `posSide`** (для hedge mode или если первый способ не работает)

### Изменения в коде:

1. **`src/clients/futures_client.py`**:
   - Добавлен параметр `pos_side: Optional[str] = None` в метод `set_leverage`
   - Если `pos_side` указан, он добавляется в запрос

2. **`src/strategies/scalping/futures/orchestrator.py`**:
   - При инициализации: пробует установить leverage без `posSide`, затем с `posSide="long"`
   - Перед открытием позиции: пробует установить leverage без `posSide`, затем с `posSide` на основе стороны сигнала (long/short)

## 🎯 РЕЗУЛЬТАТ

Теперь код пытается установить leverage **двумя способами**, что должно работать даже в sandbox mode.

## ⚠️ ВАЖНО

Если leverage все равно не устанавливается через API в sandbox mode:

1. **Установите leverage 5x вручную на бирже OKX sandbox** для каждого символа перед запуском бота
2. Или проверьте, какой режим позиции установлен на бирже (net mode или hedge mode)

## 📝 ЛОГИ

В логах вы увидите:
- `✅ Плечо 5x установлено для BTC-USDT` - если установка успешна
- `⚠️ Попытка 1 не удалась для BTC-USDT, пробуем с posSide='long'` - если первая попытка не удалась
- `⚠️ Не удалось установить плечо 5x для BTC-USDT` - если обе попытки не удались

Если обе попытки не удались, leverage будет использоваться из конфига для расчетов, но позиции могут открываться с leverage, установленным на бирже (3x).

