# 📦 АРХИВ БАТНИКОВ ДЛЯ ЧИСТКИ ЛОГОВ

Эта папка содержит архивные/альтернативные версии батников для чистки логов.

## 🎯 ОСНОВНОЙ БАТНИК

**Используйте:** `clean_logs.bat` (в корне проекта)

Это основной и самый полный батник для архивирования логов. Он включает:
- ✅ Перемещение всех типов логов в архив
- ✅ Создание ZIP архива
- ✅ Обработка ошибок
- ✅ Подробная статистика

## 📁 АРХИВНЫЕ ВЕРСИИ

Все батники в этой папке **заглушены** и не должны использоваться напрямую.

### Структура файлов:

Для каждого батника есть 2 версии:
- `ARCHIVED_*.bat` - **ЗАГЛУШЕННАЯ** версия (не выполняется, показывает сообщение)
- `ARCHIVED_*_ORIGINAL.bat` - **ОРИГИНАЛЬНАЯ** версия (полный код, но в архиве)

### Описание версий:

1. **clean_logs_simple.bat** - Упрощенная версия (без создания ZIP, с паузами)
2. **clean_logs_debug.bat** - Отладочная версия (с подробным выводом, для тестирования)
3. **clean_logs_force_open.bat** - Версия с принудительным открытием окна (cmd /k)
4. **clean_logs_keep_open.bat** - Версия с удержанием окна открытым (без авто-закрытия)
5. **clean_logs_wrapper.bat** - Обертка для clean_logs_simple.bat
6. **test_clean_logs.bat** - Тестовая версия (проверка работы команд)

## ⚠️ ВНИМАНИЕ

- Все батники с префиксом `ARCHIVED_` **заглушены** - они не будут выполняться
- Оригинальные версии (`*_ORIGINAL.bat`) содержат полный код, но находятся в архиве
- Если нужно использовать какую-то версию, скопируйте `*_ORIGINAL.bat` в корень и переименуйте

## 🔄 ВОССТАНОВЛЕНИЕ

Если нужно восстановить какой-то батник:
1. Скопируйте `ARCHIVED_*_ORIGINAL.bat` из этой папки в корень проекта
2. Переименуйте, убрав префикс `ARCHIVED_` и суффикс `_ORIGINAL`
3. Убедитесь, что основной `clean_logs.bat` не перезаписан

