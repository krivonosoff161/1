# 🔧 СПИСОК КОНКРЕТНЫХ ИСПРАВЛЕНИЙ
**Дата:** 26 декабря 2025  
**Основано на:** Анализ рекомендаций КИМИ + Углубленный анализ всех вопросов

---

## 🎯 ПРИОРИТЕТЫ ИСПРАВЛЕНИЙ

### 🔴 КРИТИЧНО (блокирует работу):
1. TCC datetime ошибка
2. ExitAnalyzer TypeError (actual_max_holding)

### 🟡 ВАЖНО (влияет на доходность):
3. Исправить получение ATR для ATR-based системы
4. Изменить SL для ranging (2.0% → 1.5%)
5. Ослабить генерацию сигналов

### 🟢 ЖЕЛАТЕЛЬНО (улучшит результаты):
6. Снизить пороги определения режима
7. Добавить детальное логирование

---

## 1. TCC DATETIME ОШИБКА 🔴

**Файл:** `src/strategies/scalping/futures/orchestrator.py`  
**Строка:** 2482  
**Проблема:** `cannot access local variable 'datetime' where it is not associated with a value`

**Текущий код:**
```python
# Строка 18: Импорт правильный
from datetime import datetime, timedelta, timezone

# Строка 2482: НО используется datetime.now() без префикса
or existing_metadata.entry_time == datetime.now()
```

**Исправление:**
```python
# Строка 2482: Заменить
or existing_metadata.entry_time == datetime.now()

# На:
or existing_metadata.entry_time == datetime.now(timezone.utc)
```

**Проверка:** Найти все использования `datetime.now()` в orchestrator.py и заменить на `datetime.now(timezone.utc)`

---

## 2. EXITANALYZER TYPERROR 🔴

**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py`  
**Проблема:** `'>' not supported between instances of 'str' and 'int'`  
**Причина:** `actual_max_holding` может быть строкой из конфига

**Исправление:**
```python
# Уже частично исправлено, но нужно проверить все места использования actual_max_holding
# Убедиться, что везде используется float версия
actual_max_holding_float = float(actual_max_holding) if actual_max_holding is not None else 0.0
```

**Проверка:** Найти все сравнения с `actual_max_holding` и убедиться, что используется float версия

---

## 3. ИСПРАВИТЬ ПОЛУЧЕНИЕ ATR 🟡

**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py`  
**Строки:** 704-760 (метод `_get_tp_percent`)  
**Проблема:** ATR не находится, ATR-based система не работает

**Исправление:**
```python
# В методе _get_tp_percent, после строки 723, добавить:
if atr_1m is None:
    # ✅ НОВОЕ: Пробуем получить ATR из DataRegistry
    if hasattr(self, "data_registry") and self.data_registry:
        try:
            # Получаем ATR из DataRegistry (async метод)
            atr_value = await self.data_registry.get_indicator(symbol, "ATR")
            if atr_value is not None:
                # ATR может быть числом или объектом с value
                if isinstance(atr_value, (int, float)):
                    atr_1m = float(atr_value)
                elif hasattr(atr_value, "value"):
                    atr_1m = float(atr_value.value)
                elif hasattr(atr_value, "get_value"):
                    atr_1m = float(atr_value.get_value())
                else:
                    atr_1m = None
                
                if atr_1m and atr_1m > 0:
                    logger.debug(
                        f"✅ [ATR_TP] {symbol}: ATR получен из DataRegistry: {atr_1m:.6f}"
                    )
        except Exception as e:
            logger.debug(f"⚠️ [ATR_TP] {symbol}: Не удалось получить ATR из DataRegistry: {e}")

    # ✅ НОВОЕ: Пробуем получить ATR из market_data напрямую
    if atr_1m is None and market_data:
        try:
            # Пробуем разные ключи для ATR
            atr_1m = market_data.get("atr") or market_data.get("atr_14") or market_data.get("ATR")
            if atr_1m:
                atr_1m = float(atr_1m)
                logger.debug(
                    f"✅ [ATR_TP] {symbol}: ATR получен из market_data: {atr_1m:.6f}"
                )
        except Exception as e:
            logger.debug(f"⚠️ [ATR_TP] {symbol}: Не удалось получить ATR из market_data: {e}")
```

**Аналогично для SL (метод `_get_sl_percent`, строки 898-937)**

---

## 4. ИЗМЕНИТЬ SL ДЛЯ RANGING 🟡

**Файл:** `config/config_futures.yaml`  
**Строка:** 603 (ETH-USDT ranging)  
**Исправление:**
```yaml
symbol_profiles:
  ETH-USDT:
    ranging:
      tp_percent: 3.5  # ✅ УЖЕ ЕСТЬ (правильно)
      sl_percent: 1.5  # (было 2.0) - КАК РЕКОМЕНДУЕТ КИМИ!
```

**Также для других символов (если нужно):**
```yaml
  BTC-USDT:
    ranging:
      sl_percent: 1.5  # (было 2.0)
  SOL-USDT:
    ranging:
      sl_percent: 1.5  # (было 2.0)
  XRP-USDT:
    ranging:
      sl_percent: 1.5  # (было 2.0)
  DOGE-USDT:
    ranging:
      sl_percent: 1.5  # (было 2.0)
```

---

## 5. ОСЛАБИТЬ ГЕНЕРАЦИЮ СИГНАЛОВ 🟡

**Файл:** `config/config_futures.yaml`  
**Исправления:**

### 5.1. Снизить min_adx:
```yaml
# Строка 36:
min_adx: 18.0  # (было 20.0) - для ranging режима
```

### 5.2. Снизить min_ma_difference_pct для ranging:
```yaml
# В ranging режиме (строка 339):
ranging:
  indicators:
    min_ma_difference_pct: 0.005  # (было 0.01) - больше сигналов
```

**Текущее значение:** `min_ma_difference_pct: 0.01` (строка 339)  
**Изменить на:** `min_ma_difference_pct: 0.005`

### 5.3. Ослабить V-образные развороты:
**Файл:** `src/strategies/scalping/futures/signal_generator.py`  
**Метод:** `_generate_ma_signals`  
**Проблема:** V-образные развороты блокируют MA сигналы

**Исправление:**
```python
# Найти блокировку V-образных разворотов и сделать её менее строгой
# Или добавить параметр в конфиг для управления строгостью
```

---

## 6. СНИЗИТЬ ПОРОГИ ОПРЕДЕЛЕНИЯ РЕЖИМА 🟢

**Файл:** `config/config_futures.yaml`  
**Строки:** 223-224  
**Исправления:**
```yaml
adaptive_regime:
  trending_adx_threshold: 18.0  # (было 20.0) - больше TRENDING режимов
  ranging_adx_threshold: 20.0  # Оставить как есть
  trend_strength_percent: 1.5  # (было 2.0) - больше TRENDING режимов
  high_volatility_threshold: 2.5  # (было 3.0) - больше CHOPPY режимов
```

---

## 7. ДОБАВИТЬ ДЕТАЛЬНОЕ ЛОГИРОВАНИЕ 🟢

### 7.1. Логирование определения режима:
**Файл:** `src/strategies/scalping/futures/adaptivity/regime_manager.py`  
**Метод:** `detect_regime`  
**Строки:** 287-295

**Исправление:**
```python
# Убедиться, что логирование включено (уже есть, но проверить уровень)
logger.debug(  # Убедиться, что это DEBUG, а не другой уровень
    f"🧠 ARM Detect Regime:\n"
    f"   Detected: {regime.value.upper()} (confidence: {confidence:.1%})\n"
    f"   Reason: {reason}\n"
    f"   ADX: {adx_val:.1f} (+DI={di_plus:.1f}, -DI={di_minus:.1f}, direction={trend_dir})\n"
    f"   Volatility: {volatility_str}\n"
    f"   Volume Ratio: {vol_ratio:.2f}x\n"
    f"   Reversals: {indicators.get('reversals', 0)}"
)
```

### 7.2. Логирование причин отсутствия сигналов:
**Файл:** `src/strategies/scalping/futures/signal_generator.py`  
**Проблема:** Нужно логировать, почему сигналы не генерируются

**Исправление:**
```python
# В методах генерации сигналов добавить логирование причин блокировки:
# - V-образный разворот
# - ADX < min_adx
# - MA разница < min_ma_difference_pct
# - и т.д.
```

---

## 📋 ЧЕКЛИСТ ИСПРАВЛЕНИЙ

### Критично (сделать СЕЙЧАС):
- [ ] Исправить TCC datetime ошибку (orchestrator.py:2482)
- [ ] Проверить ExitAnalyzer TypeError (actual_max_holding)

### Важно (сделать СЕГОДНЯ):
- [ ] Исправить получение ATR в ExitAnalyzer
- [ ] Изменить SL для ranging (2.0% → 1.5%) в config_futures.yaml
- [ ] Снизить min_adx (20.0 → 18.0) в config_futures.yaml
- [ ] Снизить min_ma_difference_pct (0.01 → 0.005) для ranging

### Желательно (сделать на этой неделе):
- [ ] Снизить пороги определения режима (trending_adx_threshold, trend_strength_percent, high_volatility_threshold)
- [ ] Добавить детальное логирование определения режима
- [ ] Добавить логирование причин отсутствия сигналов

---

## 🎯 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

После исправлений:
- ✅ Win Rate: с 24.14% → 50-60%
- ✅ Сигналов/день: с 0 → 20-30
- ✅ PnL: с -8.41 USDT → +15-25 USDT/день
- ✅ TP достижение: с 6.9% → 30-40%
- ✅ SL закрытия: с 31% → 20-25%

---

---

## 📝 КРАТКАЯ СВОДКА ИСПРАВЛЕНИЙ

### 🔴 КРИТИЧНО (блокирует работу):
1. **orchestrator.py:2482** - `datetime.now()` → `datetime.now(timezone.utc)`
2. **exit_analyzer.py** - Проверить все использования `actual_max_holding` (уже частично исправлено)

### 🟡 ВАЖНО (влияет на доходность):
3. **exit_analyzer.py:723** - Добавить получение ATR из DataRegistry
4. **config_futures.yaml:603** - `sl_percent: 2.0` → `1.5` для ETH-USDT ranging
5. **config_futures.yaml:36** - `min_adx: 20.0` → `18.0`
6. **config_futures.yaml:339** - `min_ma_difference_pct: 0.01` → `0.005` для ranging

### 🟢 ЖЕЛАТЕЛЬНО (улучшит результаты):
7. **config_futures.yaml:223-225** - Снизить пороги определения режима
8. **regime_manager.py:287** - Убедиться, что логирование включено
9. **signal_generator.py** - Добавить логирование причин отсутствия сигналов

---

**Дата создания:** 26 декабря 2025  
**Версия:** 1.1  
**Статус:** ✅ Готов к исправлениям

