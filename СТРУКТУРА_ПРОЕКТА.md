# 📂 Структура проекта

## Дерево директорий (1-2 уровня)

```
src/
├── indicators/              # Базовые и продвинутые индикаторы
│   └── advanced/           # Продвинутые индикаторы (VolumeProfile, Pivot)
├── strategies/             # Торговые стратегии
│   ├── modules/            # Общие модули стратегий (Correlation, Pivot, VolumeProfile фильтры)
│   └── scalping/           # Скальпинг стратегии
│       ├── futures/        # Futures скальпинг
│       │   ├── filters/    # Фильтры для Futures (Funding, Liquidity, OrderFlow)
│       │   ├── indicators/ # Futures индикаторы (FastADX, FundingMonitor, MicroPivot)
│       │   ├── positions/ # Управление позициями (ExitAnalyzer, PositionMonitor)
│       │   ├── core/       # Ядро (DataRegistry, PositionRegistry, CandleBuffer)
│       │   └── coordinators/ # Координаторы (SmartExit, TrailingSL, WebSocket)
│       └── spot/           # Spot скальпинг
├── clients/                # Клиенты биржи (Futures, Spot)
├── filters/                # Общие фильтры (CorrelationManager)
├── balance/                # Управление балансом
├── risk/                   # Управление рисками
├── utils/                  # Утилиты (логирование, уведомления)
└── models.py              # Модели данных (OHLCV, Position)

config/                     # Конфигурационные файлы
├── config_futures.yaml    # Конфиг для Futures
└── config_spot.yaml       # Конфиг для Spot
```

---

## 📋 Описание директорий

### `src/indicators/`
**Назначение:** Базовые и продвинутые технические индикаторы.

**Содержимое:**
- `base.py` — базовые индикаторы
- `advanced/` — продвинутые индикаторы:
  - ✅ **`volume_profile.py`** — `VolumeProfileCalculator` (POC, VAH, VAL)
  - ✅ **`pivot_calculator.py`** — `PivotCalculator` (Pivot Points, S1-S3, R1-R3)

**Статус:** ✅ Готово к использованию

---

### `src/strategies/modules/`
**Назначение:** Общие модули для всех стратегий (фильтры, адаптеры).

**Содержимое:**
- ✅ **`correlation_filter.py`** — фильтр корреляции между парами
- ✅ **`pivot_points.py`** — модуль для работы с пивотами
- ✅ **`volume_profile_filter.py`** — фильтр Volume Profile
- `adaptive_regime_manager.py` — адаптивное управление режимами
- `multi_timeframe.py` — мульти-таймфрейм анализ

**Статус:** ✅ Есть Correlation, Pivot, VolumeProfile (но это фильтры для входа, не для закрытия)

---

### `src/strategies/scalping/futures/filters/`
**Назначение:** Фильтры для Futures скальпинга (проверка перед входом).

**Содержимое:**
- ✅ **`funding_rate_filter.py`** — фильтр funding rate (есть `FundingSnapshot`)
- ✅ **`liquidity_filter.py`** — фильтр ликвидности (есть `LiquiditySnapshot` с данными стакана)
- `order_flow_filter.py` — фильтр Order Flow
- `momentum_filter.py` — фильтр импульса
- `volatility_regime_filter.py` — фильтр волатильности

**Статус:** ✅ Есть Funding и Liquidity, но это фильтры для входа, не для получения уровней ликвидности

---

### `src/strategies/scalping/futures/indicators/`
**Назначение:** Индикаторы специально для Futures скальпинга.

**Содержимое:**
- ✅ **`funding_rate_monitor.py`** — мониторинг funding rate (есть `get_current_funding()`)
- ✅ **`micro_pivot_calculator.py`** — микро-пивоты для 15m (есть `MicroPivotCalculator`)
- ✅ **`futures_volume_profile.py`** — stub (пустой, TODO)
- `fast_adx.py` — быстрый ADX
- `order_flow_indicator.py` — индикатор Order Flow
- `trailing_stop_loss.py` — трейлинг стоп-лосс

**Статус:** ✅ Есть FundingMonitor, MicroPivot (но не полный PivotCalculator)

---

### `src/strategies/scalping/futures/positions/`
**Назначение:** Управление позициями (открытие, закрытие, мониторинг).

**Содержимое:**
- ✅ **`exit_analyzer.py`** — **ГЛАВНЫЙ ФАЙЛ** для умного закрытия (здесь вставляем код)
- `position_monitor.py` — мониторинг позиций
- `entry_manager.py` — управление входом
- `exit_decision_logger.py` — логирование решений

**Статус:** ✅ Готово к модификации

---

### `src/strategies/scalping/futures/core/`
**Назначение:** Ядро системы (реестры данных, позиций, буферы свечей).

**Содержимое:**
- ✅ **`data_registry.py`** — реестр данных (есть `get_price()`, `get_candles()`, `get_market_data()`)
- ✅ **`position_registry.py`** — реестр позиций
- ✅ **`candle_buffer.py`** — буфер свечей (`CandleBuffer`)

**Статус:** ✅ Готово к использованию

---

### `src/strategies/scalping/futures/coordinators/`
**Назначение:** Координаторы для управления компонентами.

**Содержимое:**
- ✅ **`smart_exit_coordinator.py`** — координатор умного закрытия (RSI/MACD)
- ✅ **`trailing_sl_coordinator.py`** — координатор трейлинг стоп-лосса
- `websocket_coordinator.py` — координатор WebSocket
- `signal_coordinator.py` — координатор сигналов
- `order_coordinator.py` — координатор ордеров

**Статус:** ✅ Готово к использованию

---

### `src/filters/`
**Назначение:** Общие фильтры для всех стратегий.

**Содержимое:**
- ✅ **`correlation_manager.py`** — менеджер корреляции (есть `CorrelationManager`, `get_correlation()`)
- `time_session_manager.py` — менеджер торговых сессий

**Статус:** ✅ Есть CorrelationManager

---

### `src/clients/`
**Назначение:** Клиенты для работы с биржей OKX.

**Содержимое:**
- `futures_client.py` — клиент для Futures
- `spot_client.py` — клиент для Spot

**Статус:** ✅ Готово к использованию (для получения данных с биржи)

---

## 🔍 Где что находится

### ✅ Что уже есть:

| Компонент | Расположение | Статус |
|-----------|--------------|--------|
| **VolumeProfile** | `src/indicators/advanced/volume_profile.py` | ✅ Готово (`VolumeProfileCalculator`) |
| **Pivot** | `src/indicators/advanced/pivot_calculator.py` | ✅ Готово (`PivotCalculator`) |
| **MicroPivot** | `src/strategies/scalping/futures/indicators/micro_pivot_calculator.py` | ✅ Готово (`MicroPivotCalculator`) |
| **Funding** | `src/strategies/scalping/futures/indicators/funding_rate_monitor.py` | ✅ Готово (`FundingRateMonitor`) |
| **Correlation** | `src/filters/correlation_manager.py` | ✅ Готово (`CorrelationManager`) |
| **Liquidity** | `src/strategies/scalping/futures/filters/liquidity_filter.py` | ⚠️ Есть фильтр, но нет уровней ликвидности |
| **CandlePatterns** | ❌ Не найдено | ❌ Нужно создать |
| **DataRegistry** | `src/strategies/scalping/futures/core/data_registry.py` | ✅ Готово (цена, свечи) |

---

## ⚠️ Что нужно создать/дополнить:

### 1. **CandlePatterns** (❌ Не найдено)
- **Где создать:** `src/indicators/advanced/candle_patterns.py` или `src/strategies/scalping/futures/indicators/candle_patterns.py`
- **Что нужно:** Методы `_is_hammer()`, `_is_engulfing_bearish()`, `_is_engulfing_bullish()`

### 2. **Liquidity Levels** (⚠️ Есть фильтр, но нет уровней)
- **Где создать:** Новый модуль `src/strategies/scalping/futures/indicators/liquidity_levels.py`
- **Что нужно:** Метод `get_nearest_liquidity()` возвращающий `{"below": {"volume": ..., "distance_pct": ...}, "above": {...}}`

### 3. **Volume Profile обертка** (✅ Есть, но нужно обертка)
- **Где создать:** В `exit_analyzer.py` или новый модуль
- **Что нужно:** Метод `is_high_volume_zone(price, atr_factor=0.3)` для `VolumeProfileData`

### 4. **Pivot обертка** (✅ Есть, но нужно обертка)
- **Где создать:** В `exit_analyzer.py` или новый модуль
- **Что нужно:** Метод `distance_to_level(price, level) < 0.3 * ATR` для `PivotLevels`

### 5. **Correlation обертка** (✅ Есть, но нужно обертка)
- **Где создать:** В `exit_analyzer.py` или использовать `CorrelationManager`
- **Что нужно:** Метод `get_correlation(symbol, basket, period=20)` возвращающий `float`

---

## 📌 Рекомендации по размещению кода

### ✅ Минимальная вставка в `exit_analyzer.py`:
1. Методы получения данных (обертки над существующими модулями)
2. Методы проверки индикаторов (7 методов)
3. Основной метод `_should_force_close_by_smart_analysis`

### ✅ Новые модули (создать отдельно):
1. `src/indicators/advanced/candle_patterns.py` — паттерны свечей
2. `src/strategies/scalping/futures/indicators/liquidity_levels.py` — уровни ликвидности

### ✅ Использовать существующие:
1. `VolumeProfileCalculator` из `src/indicators/advanced/volume_profile.py`
2. `PivotCalculator` из `src/indicators/advanced/pivot_calculator.py`
3. `FundingRateMonitor` из `src/strategies/scalping/futures/indicators/funding_rate_monitor.py`
4. `CorrelationManager` из `src/filters/correlation_manager.py`
5. `DataRegistry` из `src/strategies/scalping/futures/core/data_registry.py`

---

## ❓ Вопрос для уточнения

**CandlePatterns:** Нужно ли создавать отдельный модуль `candle_patterns.py`, или можно встроить методы `_is_hammer()` и `_is_engulfing_bearish()` прямо в `exit_analyzer.py`?

**Рекомендация:** Создать отдельный модуль `src/indicators/advanced/candle_patterns.py` для переиспользования в других местах.

