# ИСПРАВЛЕНИЕ: Leverage из позиции на бирже вместо конфига

## 🔍 ПРОБЛЕМА

Позиции открываются с leverage 3x, хотя в конфиге указано 5x. Проблема была в том, что код использовал leverage из позиции на бирже (который был установлен ранее - 3x), а не из конфига (5x).

## ✅ ИСПРАВЛЕНИЕ

Исправлены два файла:

### 1. `src/strategies/scalping/futures/position_manager.py`

**Было:**
```python
leverage = int(position.get("lever", "3"))
```

**Стало:**
```python
# ✅ ИСПРАВЛЕНИЕ: Используем leverage из конфига, а не из позиции на бирже
# На бирже может быть установлен старый leverage (3x), но расчеты должны использовать leverage из конфига (5x)
leverage_from_position = int(position.get("lever", "0"))
leverage = getattr(self.scalping_config, "leverage", None) or leverage_from_position or 3
if leverage_from_position != leverage:
    logger.debug(
        f"📊 Leverage: биржа={leverage_from_position}x, конфиг={leverage}x, используем {leverage}x для расчетов"
    )
```

### 2. `src/strategies/modules/liquidation_guard.py`

**Было:**
```python
leverage = int(position.get("lever", "3"))
```

**Стало:**
```python
# ✅ ИСПРАВЛЕНИЕ: Используем leverage из margin_calculator (из конфига), а не из позиции на бирже
# На бирже может быть установлен старый leverage (3x), но расчеты должны использовать leverage из конфига (5x)
leverage_from_position = int(position.get("lever", "0"))
leverage = self.margin_calculator.default_leverage or leverage_from_position or 3
if leverage_from_position != leverage:
    logger.debug(
        f"📊 Leverage: биржа={leverage_from_position}x, конфиг={leverage}x, используем {leverage}x для расчетов"
    )
```

## 🎯 РЕЗУЛЬТАТ

Теперь все расчеты (размер позиций, маржа, ликвидация, TP/SL) используют leverage из конфига (5x), а не из позиции на бирже (3x).

## ⚠️ ВАЖНО

**Позиции на бирже все еще могут открываться с leverage 3x**, если:
1. Leverage не устанавливается через API в sandbox mode
2. Leverage не установлен вручную на бирже

**Но расчеты теперь используют leverage 5x из конфига**, что означает:
- Размер позиций рассчитывается с leverage 5x
- Маржа рассчитывается с leverage 5x
- TP/SL рассчитываются с leverage 5x
- Ликвидация рассчитывается с leverage 5x

Это правильное поведение, так как мы хотим использовать leverage 5x для всех расчетов, даже если на бирже установлен leverage 3x.

