# АНАЛИЗ УЛУЧШЕНИЙ И ИСПРАВЛЕНИЙ

**Дата:** 2025-12-21  
**Цель:** Анализ увеличения позиций, автономных решений и исправление найденных проблем

---

## 1. УВЕЛИЧЕНИЕ РАЗМЕРА ПОЗИЦИИ - РАЗРЕШИТЬ ИЛИ НЕТ?

### Текущая логика

**Код блокировки:**
```220:283:src/strategies/scalping/futures/coordinators/signal_coordinator.py
if position_in_signal_direction:
    # ✅ КРИТИЧЕСКОЕ: Позиция уже есть в направлении сигнала
    # На OKX Futures новый ордер в том же направлении просто увеличит размер позиции
    # Это означает, что мы НЕ создаем новую позицию, а увеличиваем существующую
    # Поэтому блокируем, чтобы не накапливать комиссию на одной позиции
    ...
    logger.warning(
        f"⚠️ Позиция {symbol} {signal_position_side.upper()} УЖЕ ОТКРЫТА на бирже (size={pos_size}), "
        f"БЛОКИРУЕМ новый {signal_side.upper()} ордер "
        f"(на OKX Futures ордера в одном направлении объединяются, комиссия накапливается!)"
    )
    continue  # Блокируем сигнал
```

### ✅ ПРЕДЛОЖЕНИЕ: Разрешить с контролем

**Аргументы ЗА:**
1. ✅ Позиция постоянно в работе - новые сигналы усиливают позицию
2. ✅ Бот видит разворот раньше - закроет текущую позицию и откроет в противоположном направлении
3. ✅ Частичное закрытие работает - можно закрывать часть позиции при достижении TP
4. ✅ Усреднение позиции - если цена пошла против нас, новые сигналы усреднят вход

**Условия разрешения:**
1. ✅ Максимальное количество добавлений в одну позицию (например, 3-5 раз)
2. ✅ Минимальный интервал между добавлениями (например, 30-60 секунд)
3. ✅ Проверка что позиция не в большом убытке (например, не больше чем -2%)
4. ✅ Контроль общего размера позиции (не превышать max_position_size)

**Логика:**
```python
# Вместо блокировки:
if position_in_signal_direction:
    # Проверяем условия для добавления
    if self._can_add_to_position(symbol, position_in_signal_direction, signal):
        # Разрешаем добавление
        logger.info(f"✅ Добавляем к позиции {symbol}: {signal}")
    else:
        # Блокируем
        logger.warning(f"⚠️ Нельзя добавить к позиции {symbol}: условия не выполнены")
        continue
```

**Метод проверки:**
```python
def _can_add_to_position(self, symbol, existing_position, new_signal):
    """Проверка возможности добавления к позиции"""
    # 1. Проверка количества добавлений
    add_count = existing_position.get("_add_count", 0)
    if add_count >= 5:  # Максимум 5 добавлений
        return False
    
    # 2. Проверка интервала (последнее добавление было больше 30 секунд назад)
    last_add_time = existing_position.get("_last_add_time", 0)
    if time.time() - last_add_time < 30:
        return False
    
    # 3. Проверка убытка (не добавляем если убыток > 2%)
    pnl_percent = self._calculate_pnl_percent(existing_position)
    if pnl_percent < -2.0:
        return False
    
    # 4. Проверка размера (не превышаем max_position_size)
    current_size = abs(float(existing_position.get("pos", "0")))
    new_size = self._calculate_new_position_size(new_signal)
    if current_size + new_size > max_position_size:
        return False
    
    return True
```

---

## 2. АВТОНОМНЫЕ РЕШЕНИЯ - ЧТО МОЖНО ОТДАТЬ?

### ✅ Текущие "автономные" решения (уже работают)

1. **Адаптивный леверидж** - автоматически рассчитывается из сигнала, режима, волатильности
2. **Адаптивные параметры** - TP/SL/max_holding меняются по режиму и балансу
3. **Продление времени** - автоматически продлевается если позиция прибыльная и условия выполнены
4. **Частичное закрытие (Partial TP)** - автоматически закрывается часть позиции при достижении порога
5. **Умное закрытие убыточных позиций** - анализ индикаторов перед закрытием

### ✅ ПРЕДЛОЖЕНИЯ для новых автономных решений

#### 2.1. Автономное изменение размера позиции (DCA - Dollar Cost Averaging)

**Логика:**
- Если позиция в убытке > 1% и есть признаки отката → добавить к позиции
- Если позиция в прибыли > 1% и есть признаки продолжения → добавить к позиции
- Контролировать общий размер и количество добавлений

**Условия:**
- Анализ индикаторов (ADX, объем, тренд)
- Контроль рисков (не превышать max_position_size)
- Минимальный интервал между добавлениями

#### 2.2. Автономная коррекция TP/SL на основе волатильности

**Логика:**
- Если волатильность увеличилась → расширить SL, поднять TP
- Если волатильность уменьшилась → сузить SL, снизить TP
- Основано на текущей волатильности vs средней

**Условия:**
- Только для прибыльных позиций
- Максимальная коррекция ±20% от исходных TP/SL
- Не чаще чем раз в 5 минут

#### 2.3. Автономное переключение режима торговли

**Логика:**
- Если текущий режим показывает плохие результаты → временно переключить стратегию
- Например: в ranging режиме много стоп-лоссов → временно использовать более консервативные параметры

**Условия:**
- Анализ последних 10 сделок
- Минимальный интервал между переключениями (например, 1 час)
- Возврат к исходному режиму после улучшения результатов

#### 2.4. Автономная оптимизация размера позиции на основе текущего PnL

**Логика:**
- Если общий PnL дня отрицательный → уменьшить базовый размер позиции на 20%
- Если общий PnL дня положительный → можно увеличить базовый размер на 10%
- Контролировать максимальное увеличение/уменьшение

**Условия:**
- Только в конце торгового дня (или каждые 4 часа)
- Минимальный размер позиции (например, не меньше 50% от базового)
- Максимальный размер позиции (например, не больше 150% от базового)

#### 2.5. Автономное временное приостановление торговли

**Логика:**
- Если серия убытков (например, 5 подряд) → приостановить торговлю на 30 минут
- Если высокая волатильность рынка → приостановить торговлю на 15 минут
- Автоматическое возобновление после улучшения условий

**Условия:**
- Анализ последних сделок
- Анализ текущей волатильности рынка
- Уведомления в логах о приостановке

### ✅ РЕКОМЕНДАЦИЯ

**Начать с:**
1. ✅ **Увеличение позиции с контролем** (п.1) - самое простое и безопасное
2. ✅ **Автономная коррекция TP/SL на основе волатильности** - улучшит результаты
3. ✅ **Автономная оптимизация размера позиции** - защита от больших просадок

**Отложить:**
- Автономное переключение режима торговли - слишком сложно и рискованно
- Автономное временное приостановление - уже есть emergency stop

---

## 3. ИСПРАВЛЕНИЕ ОШИБОК

### 3.1. Ошибка форматирования в AdaptiveLeverage

**Проблема:**
```109:109:src/strategies/scalping/futures/risk/adaptive_leverage.py
f"volatility={volatility:.4f if volatility else 'N/A'}, "
```

**Ошибка:** В f-string нельзя использовать тернарный оператор с форматированием `.4f` внутри.

**Исправление:**
```python
# ПРАВИЛЬНО:
volatility_str = f"{volatility:.4f}" if volatility is not None else "N/A"
logger.debug(
    f"📊 Adaptive Leverage для {signal.get('symbol', 'N/A')}: "
    f"strength={signal_strength:.2f}, regime={regime}, "
    f"volatility={volatility_str}, "
    f"adjusted={adjusted_strength:.2f}, category={category}, "
    f"leverage={leverage}x"
)
```

### 3.2. Leverage = 0x - чтение с биржи не работает

**Проблема:**
```724:733:src/strategies/scalping/futures/position_manager.py
leverage_from_position = int(position.get("lever", "0"))
leverage = (
    getattr(self.scalping_config, "leverage", None)
    or leverage_from_position
    or 3
)
if leverage_from_position != leverage:
    logger.debug(
        f"📊 Leverage: биржа={leverage_from_position}x, конфиг={leverage}x, используем {leverage}x для расчетов"
    )
```

**Проблема:** Поле `lever` может быть `"0"` или отсутствовать. Нужно проверить другие поля.

**Исправление:**
```python
# Пробуем получить leverage из разных полей
leverage_from_position = None
if "lever" in position and position.get("lever"):
    try:
        leverage_from_position = int(position.get("lever", "0"))
        if leverage_from_position <= 0:
            leverage_from_position = None
    except (ValueError, TypeError):
        pass

# Если не получили, пробуем другие поля
if not leverage_from_position:
    # Некоторые биржи возвращают leverage в других полях
    if "leverage" in position:
        try:
            leverage_from_position = int(position.get("leverage", "0"))
            if leverage_from_position <= 0:
                leverage_from_position = None
        except (ValueError, TypeError):
            pass

leverage = (
    getattr(self.scalping_config, "leverage", None)
    or leverage_from_position
    or 3
)

if leverage_from_position and leverage_from_position != leverage:
    logger.debug(
        f"📊 Leverage: биржа={leverage_from_position}x, конфиг={leverage}x, используем {leverage}x для расчетов"
    )
elif not leverage_from_position:
    logger.debug(
        f"⚠️ Leverage не найден на бирже для {symbol}, используем конфиг: {leverage}x"
    )
```

### 3.3. Position_value - расчет неправильный (разница в 100 раз)

**Проблема:**
```905:927:src/strategies/scalping/futures/position_manager.py
details = await self.client.get_instrument_details(symbol)
ct_val = details.get("ctVal", 0.01)  # По умолчанию для BTC/ETH
# Реальный размер в монетах
size_in_coins = abs(size) * ct_val
# Стоимость позиции в USD
position_value = size_in_coins * current_price
```

**Проблема:** Для некоторых инструментов `ctVal` может быть другим (например, 0.1 или 1.0), и расчет может быть неверным.

**Также нужно проверить:** Может быть проблема в том, что `size` уже в монетах, а не в контрактах, или наоборот.

**Исправление:**
```python
try:
    details = await self.client.get_instrument_details(symbol)
    ct_val = float(details.get("ctVal", 0.01))
    
    # ✅ КРИТИЧЕСКОЕ: Проверяем формат размера позиции
    # Для OKX Futures: size в контрактах, нужно умножить на ctVal для монет
    # Для некоторых инструментов размер может быть уже в монетах
    size_str = str(size)
    
    # Реальный размер в монетах
    size_in_coins = abs(size) * ct_val
    
    # Стоимость позиции в USD
    position_value = size_in_coins * current_price
    
    logger.debug(
        f"📊 Расчет position_value для {symbol}: "
        f"size={size} контрактов, ctVal={ct_val}, "
        f"size_in_coins={size_in_coins:.6f}, "
        f"current_price={current_price:.2f}, "
        f"position_value={position_value:.2f} USD"
    )
    
    # ✅ ПРОВЕРКА: Если position_value кажется слишком маленьким или большим, логируем предупреждение
    # Для BTC/USDT позиция $100 должна быть примерно 100 / current_price монет
    expected_min_value = 1.0  # Минимальная позиция $1
    if position_value > 0 and position_value < expected_min_value:
        logger.warning(
            f"⚠️ Подозрительно маленький position_value для {symbol}: {position_value:.2f} USD "
            f"(size={size}, ctVal={ct_val}, price={current_price:.2f})"
        )
except Exception as e:
    logger.warning(
        f"⚠️ Ошибка получения ctVal для {symbol}, используем приблизительный расчет: {e}"
    )
    # Fallback: предполагаем что size уже в монетах (для совместимости)
    size_in_coins = abs(size)
    position_value = size_in_coins * current_price
    logger.warning(
        f"⚠️ Fallback расчет для {symbol}: size_in_coins={size_in_coins:.6f}, position_value={position_value:.2f} USD"
    )
```

### 3.4. DRIFT_ADD/REMOVE - гонки (race conditions)

**Проблема:** При синхронизации позиций могут возникать гонки:
- DRIFT_ADD регистрирует позицию
- Одновременно DRIFT_REMOVE пытается удалить позицию
- Или наоборот

**Исправление:** Использовать блокировки и проверки:

```python
# В orchestrator._sync_positions_with_exchange:
async with self._position_sync_lock:  # Блокировка для синхронизации
    # Проверяем позиции на бирже
    exchange_positions = await self.client.get_positions()
    
    # Получаем все позиции из registry
    registry_positions = await self.position_registry.get_all_positions()
    
    # DRIFT_ADD: позиция есть на бирже, но нет в registry
    for pos in exchange_positions:
        symbol = pos.get("instId", "").replace("-SWAP", "")
        size = float(pos.get("pos", "0"))
        if abs(size) >= 1e-8:  # Позиция открыта
            if symbol not in registry_positions:
                # ✅ ПРОВЕРКА: еще раз проверяем что позиции нет (race condition)
                has_position = await self.position_registry.has_position(symbol)
                if not has_position:
                    # Регистрируем
                    await self.position_registry.register_position(...)
    
    # DRIFT_REMOVE: позиция есть в registry, но нет на бирже
    for symbol in registry_positions.keys():
        # Проверяем что позиции нет на бирже
        found_on_exchange = False
        for pos in exchange_positions:
            pos_symbol = pos.get("instId", "").replace("-SWAP", "")
            if pos_symbol == symbol:
                size = float(pos.get("pos", "0"))
                if abs(size) >= 1e-8:
                    found_on_exchange = True
                    break
        
        if not found_on_exchange:
            # ✅ ПРОВЕРКА: еще раз проверяем что позиции нет (race condition)
            positions = await self.client.get_positions(symbol=symbol)
            if not positions or all(abs(float(p.get("pos", "0"))) < 1e-8 for p in positions):
                # Удаляем
                await self.position_registry.unregister_position(symbol)
```

**Также добавить блокировку в PositionRegistry:**
```python
class PositionRegistry:
    def __init__(self):
        self._positions: Dict[str, Dict[str, Any]] = {}
        self._metadata: Dict[str, PositionMetadata] = {}
        self._lock = asyncio.Lock()  # ✅ Уже есть
        self._operation_locks: Dict[str, asyncio.Lock] = {}  # ✅ НОВОЕ: блокировка для каждого символа
    
    async def register_position(self, symbol: str, ...):
        # Получаем или создаем блокировку для символа
        if symbol not in self._operation_locks:
            self._operation_locks[symbol] = asyncio.Lock()
        
        async with self._operation_locks[symbol]:
            async with self._lock:
                # Регистрация позиции
                ...
```

---

## ИТОГОВЫЙ ПЛАН ДЕЙСТВИЙ

### Приоритет 1 (Критичные исправления):
1. ✅ Исправить ошибку форматирования в AdaptiveLeverage
2. ✅ Исправить чтение leverage с биржи (проверка разных полей)
3. ✅ Исправить расчет position_value (проверка и логирование)
4. ✅ Исправить race conditions в DRIFT_ADD/REMOVE (блокировки)

### Приоритет 2 (Улучшения):
5. ✅ Разрешить увеличение позиции с контролем (максимум 5 раз, интервал 30 сек, проверка убытка)
6. ✅ Автономная коррекция TP/SL на основе волатильности
7. ✅ Автономная оптимизация размера позиции на основе дневного PnL

### Приоритет 3 (Дополнительно):
8. ⏳ Автономное изменение размера позиции (DCA)
9. ⏳ Автономное переключение режима торговли

---

## ВОПРОСЫ ДЛЯ ОБСУЖДЕНИЯ

1. **Увеличение позиции:**
   - Сколько раз разрешить добавлять? (предложение: 3-5 раз)
   - Какой минимальный интервал? (предложение: 30-60 секунд)
   - Какой максимальный убыток для добавления? (предложение: не больше -2%)

2. **Автономные решения:**
   - Какие из предложенных автономных решений вам наиболее интересны?
   - Какие условия должны быть для их активации?

3. **Исправления:**
   - Все исправления понятны? Нужны дополнительные пояснения?

