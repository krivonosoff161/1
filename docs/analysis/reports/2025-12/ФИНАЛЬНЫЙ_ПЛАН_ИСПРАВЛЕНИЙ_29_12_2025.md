# 📋 ФИНАЛЬНЫЙ ПЛАН ИСПРАВЛЕНИЙ - 29.12.2025

## 🎯 ЦЕЛЬ
Исправить все выявленные проблемы для стабильной работы бота с точными ссылками на код.

---

## 1. ✅ ПРОВЕРКА ИНИЦИАЛИЗАЦИИ

### Проблема
- В логах нет сообщения `🟢 Все модули инициализированы — торговля разрешена` (хотя оно есть в коде)
- Нужно убедиться, что все этапы инициализации логируются

### План действий

#### Шаг 1.1: Добавить детальное логирование этапов инициализации
**Файл:** `src/strategies/scalping/futures/orchestrator.py`  
**Метод:** `start()`  
**Строки:** 980-995

**Текущий код:**
```982:995:src/strategies/scalping/futures/orchestrator.py
            # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ (26.12.2025): Проверяем готовность всех модулей перед началом торговли
            await self._verify_readiness()

            # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ (28.12.2025): Устанавливаем флаг готовности после всех проверок
            # Это блокирует торговлю до полной инициализации всех модулей (candles, индикаторы, ATR, pivots, volume profile, regime)
            self.all_modules_ready = True
            self.initialization_complete.set()  # Сигнал: всё готово
            
            # Логируем статистику пропущенных сигналов (если были)
            if self.skipped_signals_due_init > 0:
                logger.info(
                    f"📊 Пропущено сигналов из-за инициализации: {self.skipped_signals_due_init}"
                )
            
            logger.info("🟢 Все модули инициализированы — торговля разрешена")
```

**Изменения:**
1. Добавить логирование ПЕРЕД `_verify_readiness()` (после строки 981):
   ```python
   logger.info("⏳ Начало проверки готовности модулей...")
   ```

2. Добавить логирование ПОСЛЕ `_verify_readiness()` (после строки 982):
   ```python
   logger.info("✅ Проверка готовности завершена успешно")
   ```

3. Добавить логирование ПЕРЕД установкой флага (после строки 984):
   ```python
   logger.info("⏳ Установка флага готовности всех модулей...")
   ```

4. Добавить логирование ПОСЛЕ установки флага (после строки 987):
   ```python
   logger.info("✅ Флаг готовности установлен (all_modules_ready=True)")
   logger.info("✅ Event initialization_complete установлен")
   ```

#### Шаг 1.2: Улучшить логирование в `_verify_readiness()`
**Файл:** `src/strategies/scalping/futures/orchestrator.py`  
**Метод:** `_verify_readiness()`  
**Строки:** 5099-5136

**Текущий код:**
```5102:5136:src/strategies/scalping/futures/orchestrator.py
        logger.info("=" * 80)
        logger.info("🔍 ПРОВЕРКА ГОТОВНОСТИ МОДУЛЕЙ ПЕРЕД НАЧАЛОМ ТОРГОВЛИ")
        logger.info("=" * 80)

        # 1. Проверяем, что свечи загружены
        await self._verify_candles_loaded()

        # 2. Проверяем, что signal_generator инициализирован
        if (
            not hasattr(self.signal_generator, "is_initialized")
            or not self.signal_generator.is_initialized
        ):
            raise ValueError("❌ КРИТИЧЕСКАЯ ОШИБКА: SignalGenerator не инициализирован")
        logger.info("✅ SignalGenerator: инициализирован и готов")

        # 3. Проверяем, что все критичные модули готовы
        if not self.data_registry:
            raise ValueError("❌ КРИТИЧЕСКАЯ ОШИБКА: DataRegistry не доступен")
        logger.info("✅ DataRegistry: готов")

        if not self.position_registry:
            raise ValueError("❌ КРИТИЧЕСКАЯ ОШИБКА: PositionRegistry не доступен")
        logger.info("✅ PositionRegistry: готов")

        if not self.signal_coordinator:
            raise ValueError("❌ КРИТИЧЕСКАЯ ОШИБКА: SignalCoordinator не доступен")
        logger.info("✅ SignalCoordinator: готов")

        if not self.position_manager:
            raise ValueError("❌ КРИТИЧЕСКАЯ ОШИБКА: PositionManager не доступен")
        logger.info("✅ PositionManager: готов")

        logger.info("=" * 80)
        logger.info("✅ ВСЕ МОДУЛИ ГОТОВЫ, ТОРГОВЛЯ МОЖЕТ НАЧАТЬСЯ")
        logger.info("=" * 80)
```

**Изменения:**
1. Обернуть `_verify_readiness()` в try-except в методе `start()` (после строки 981):
   ```python
   try:
       logger.info("⏳ Начало проверки готовности модулей...")
       await self._verify_readiness()
       logger.info("✅ Проверка готовности завершена успешно")
   except Exception as e:
       logger.error(f"❌ Ошибка проверки готовности модулей: {e}")
       raise
   ```

#### Шаг 1.3: Улучшить логирование в `trading_control_center.py`
**Файл:** `src/strategies/scalping/futures/core/trading_control_center.py`  
**Метод:** `run_main_loop()`  
**Строки:** 143-167

**Текущий код:**
```143:167:src/strategies/scalping/futures/core/trading_control_center.py
        # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ (28.12.2025): Ожидание готовности всех модулей перед началом торговли
        # Проверяем, есть ли доступ к orchestrator через signal_coordinator
        if (
            hasattr(self, "signal_coordinator")
            and self.signal_coordinator
            and hasattr(self.signal_coordinator, "orchestrator")
            and self.signal_coordinator.orchestrator
        ):
            orchestrator = self.signal_coordinator.orchestrator
            if hasattr(orchestrator, "initialization_complete"):
                logger.info("⏳ Ожидание готовности всех модулей перед началом торговли...")
                try:
                    # Ждём готовности с таймаутом 60 секунд (на случай проблем)
                    await asyncio.wait_for(orchestrator.initialization_complete.wait(), timeout=60.0)
                    logger.info("✅ Все модули готовы, торговый цикл начинается")
                except asyncio.TimeoutError:
                    logger.warning(
                        "⚠️ Таймаут ожидания готовности модулей (60 сек), продолжаем с предупреждением"
                    )
            elif hasattr(orchestrator, "all_modules_ready"):
                # Fallback: проверяем флаг напрямую
                if not orchestrator.all_modules_ready:
                    logger.warning(
                        "⚠️ Модули еще не готовы (all_modules_ready=False), продолжаем с предупреждением"
                    )
```

**Изменения:**
1. Добавить логирование ПЕРЕД проверкой (после строки 142):
   ```python
   logger.info("🔄 TCC: Проверка готовности модулей перед началом торгового цикла...")
   ```

2. Улучшить логирование при таймауте (строка 159):
   ```python
   logger.warning(
       f"⚠️ Таймаут ожидания готовности модулей (60 сек), "
       f"all_modules_ready={orchestrator.all_modules_ready if hasattr(orchestrator, 'all_modules_ready') else 'N/A'}, "
       f"продолжаем с предупреждением"
   )
   ```

---

## 2. ✅ ИСПРАВЛЕНИЕ ПРОБЛЕМЫ С ATR

### Проблема
- `ATRProvider: ATR не найден для ETH-USDT, возвращаем None`
- ATR сохраняется в DataRegistry, но ATRProvider его не находит
- В логах: `✅ DataRegistry: Сохранены индикаторы для ETH-USDT: ['rsi', 'atr', ...]` - ATR есть в списке

### Анализ кода
- **Сохранение ATR:** `signal_generator.py:1947-1951` - ATR сохраняется с ключом `"atr"` (lowercase)
- **Получение ATR:** `atr_provider.py:72` - ATRProvider ищет `indicators.get("atr")`
- **Проблема:** Возможна race condition или ATR может быть None/0 при сохранении

### План действий

#### Шаг 2.1: Проверить сохранение ATR в DataRegistry
**Файл:** `src/strategies/scalping/futures/signal_generator.py`  
**Метод:** `_generate_signals()` (или метод, который вызывает `update_indicators`)  
**Строки:** 1946-1951

**Текущий код:**
```1946:1951:src/strategies/scalping/futures/signal_generator.py
                    # Простые индикаторы (RSI, ATR)
                    for key in ["rsi", "atr", "sma_20", "ema_12", "ema_26"]:
                        if key in indicators:
                            value = indicators[key]
                            if isinstance(value, (int, float)):
                                indicators_for_registry[key] = value
```

**Изменения:**
1. Заменить цикл для ATR на отдельную проверку (после строки 1946):
   ```python
                   # Простые индикаторы (RSI, ATR)
                   for key in ["rsi", "sma_20", "ema_12", "ema_26"]:
                       if key in indicators:
                           value = indicators[key]
                           if isinstance(value, (int, float)):
                               indicators_for_registry[key] = value
                   
                   # ✅ ОТДЕЛЬНАЯ ПРОВЕРКА ДЛЯ ATR: Проверяем, что ATR не None и > 0
                   atr_value = indicators.get("atr")
                   if atr_value is not None and isinstance(atr_value, (int, float)) and atr_value > 0:
                       indicators_for_registry["atr"] = atr_value
                       logger.debug(f"📊 Сохранение ATR для {symbol}: {atr_value:.6f}")
                   else:
                       logger.warning(f"⚠️ ATR для {symbol} не найден или равен 0: {atr_value}")
   ```

2. Добавить логирование после сохранения (после вызова `update_indicators`):
   ```python
   logger.debug(f"✅ ATR сохранен в DataRegistry для {symbol}: {indicators_for_registry.get('atr', 'N/A')}")
   ```

#### Шаг 2.2: Улучшить получение ATR в ATRProvider
**Файл:** `src/strategies/scalping/futures/indicators/atr_provider.py`  
**Метод:** `get_atr()`  
**Строки:** 64-93

**Текущий код:**
```64:93:src/strategies/scalping/futures/indicators/atr_provider.py
        # 2. Пробуем получить из DataRegistry (если доступен)
        if self.data_registry:
            try:
                # DataRegistry._indicators - это словарь, доступ синхронный
                if hasattr(self.data_registry, "_indicators"):
                    indicators = self.data_registry._indicators.get(symbol, {})
                    if indicators:
                        # Пробуем разные ключи для ATR
                        atr_value = (
                            indicators.get("atr")
                            or indicators.get("ATR")
                            or indicators.get("atr_1m")
                            or indicators.get("atr_14")
                        )

                        if atr_value is not None:
                            try:
                                atr_float = float(atr_value)
                                # Обновляем кэш
                                self._atr_cache[symbol] = atr_float
                                self._cache_timestamps[symbol] = time.time()
                                logger.debug(
                                    f"✅ ATRProvider: ATR получен из DataRegistry для {symbol}: {atr_float:.6f}"
                                )
                                return atr_float
                            except (ValueError, TypeError):
                                pass
            except Exception as e:
                logger.debug(
                    f"⚠️ ATRProvider: Ошибка получения ATR из DataRegistry для {symbol}: {e}"
                )
```

**Изменения:**
1. Добавить детальное логирование всех попыток получения ATR (после строки 64):
   ```python
   logger.debug(f"🔍 ATRProvider: Попытка получить ATR для {symbol}")
   logger.debug(f"🔍 ATRProvider: data_registry доступен: {self.data_registry is not None}")
   if self.data_registry:
       logger.debug(f"🔍 ATRProvider: _indicators доступен: {hasattr(self.data_registry, '_indicators')}")
       if hasattr(self.data_registry, '_indicators'):
           logger.debug(f"🔍 ATRProvider: Символ {symbol} в _indicators: {symbol in self.data_registry._indicators}")
           if symbol in self.data_registry._indicators:
               available_keys = list(self.data_registry._indicators[symbol].keys())
               logger.debug(f"🔍 ATRProvider: Доступные ключи для {symbol}: {available_keys}")
   ```

2. Логировать содержимое `_indicators[symbol]` при отсутствии ATR (после строки 76):
   ```python
   if not atr_value:
       logger.debug(f"⚠️ ATRProvider: ATR не найден в доступных ключах для {symbol}, проверяемые ключи: ['atr', 'ATR', 'atr_1m', 'atr_14']")
       if indicators:
           logger.debug(f"⚠️ ATRProvider: Содержимое indicators для {symbol}: {indicators}")
   ```

#### Шаг 2.3: Добавить синхронизацию ATR через async метод DataRegistry
**Файл:** `src/strategies/scalping/futures/indicators/atr_provider.py`  
**Метод:** Добавить новый метод `async_get_atr()`  
**Строки:** После метода `get_atr()` (после строки 103)

**Изменения:**
1. Добавить async метод `async_get_atr()`:
   ```python
   async def async_get_atr(self, symbol: str, fallback: Optional[float] = None) -> Optional[float]:
       """
       Получить ATR через async метод DataRegistry.
       
       Args:
           symbol: Торговый символ
           fallback: Fallback значение, если ATR не найден
       
       Returns:
           ATR значение или None
       """
       # Сначала проверяем кэш
       if symbol in self._atr_cache:
           cache_age = time.time() - self._cache_timestamps.get(symbol, 0)
           if cache_age < self._cache_ttl:
               return self._atr_cache[symbol]
       
       # Пробуем получить через async метод DataRegistry
       if self.data_registry:
           try:
               atr_value = await self.data_registry.get_indicator(symbol, "atr")
               if atr_value is not None:
                   try:
                       atr_float = float(atr_value)
                       if atr_float > 0:
                           self.update_atr(symbol, atr_float)
                           logger.debug(f"✅ ATRProvider: ATR получен через async метод для {symbol}: {atr_float:.6f}")
                           return atr_float
                   except (ValueError, TypeError):
                       pass
           except Exception as e:
               logger.debug(f"⚠️ ATRProvider: Ошибка async получения ATR для {symbol}: {e}")
       
       # Fallback на синхронный метод
       return self.get_atr(symbol, fallback)
   ```

#### Шаг 2.4: Добавить периодическое обновление кэша ATR
**Файл:** `src/strategies/scalping/futures/orchestrator.py`  
**Метод:** `start()`  
**Строки:** После `_verify_readiness()` (после строки 982)

**Изменения:**
1. Добавить обновление кэша ATR после `_verify_readiness()`:
   ```python
   # ✅ Обновляем кэш ATR для всех символов после инициализации
   if self.atr_provider and self.data_registry:
       logger.info("⏳ Обновление кэша ATR для всех символов...")
       for symbol in self.scalping_config.symbols:
           try:
               atr_value = await self.data_registry.get_indicator(symbol, "atr")
               if atr_value is not None and isinstance(atr_value, (int, float)) and atr_value > 0:
                   self.atr_provider.update_atr(symbol, float(atr_value))
                   logger.info(f"✅ ATR кэш обновлен для {symbol}: {atr_value:.6f}")
               else:
                   logger.warning(f"⚠️ ATR не найден для {symbol} при обновлении кэша: {atr_value}")
           except Exception as e:
               logger.warning(f"⚠️ Ошибка обновления ATR кэша для {symbol}: {e}")
   ```

#### Шаг 2.5: Добавить автоматическое обновление ATR при сохранении в DataRegistry
**Файл:** `src/strategies/scalping/futures/core/data_registry.py`  
**Метод:** `update_indicators()`  
**Строки:** 135-150

**Текущий код:**
```135:150:src/strategies/scalping/futures/core/data_registry.py
    async def update_indicators(self, symbol: str, indicators: Dict[str, Any]) -> None:
        """
        Обновить несколько индикаторов сразу.

        Args:
            symbol: Торговый символ
            indicators: Словарь индикаторов {indicator_name -> value}
        """
        async with self._lock:
            if symbol not in self._indicators:
                self._indicators[symbol] = {}

            self._indicators[symbol].update(indicators)
            self._indicators[symbol]["updated_at"] = datetime.now()

            logger.debug(f"✅ DataRegistry: Обновлены индикаторы для {symbol}")
```

**Изменения:**
1. Добавить автоматическое обновление ATR кэша (после строки 148):
   ```python
   # ✅ Автоматическое обновление ATR кэша, если ATRProvider доступен
   if "atr" in indicators:
       atr_value = indicators.get("atr")
       if atr_value is not None and isinstance(atr_value, (int, float)) and atr_value > 0:
           # Проверяем, есть ли ссылка на ATRProvider (через orchestrator или напрямую)
           # Это будет установлено извне при необходимости
           if hasattr(self, "_atr_provider") and self._atr_provider:
               self._atr_provider.update_atr(symbol, float(atr_value))
               logger.debug(f"✅ DataRegistry: ATR кэш обновлен для {symbol}: {atr_value:.6f}")
   ```

**Примечание:** Нужно добавить метод `set_atr_provider()` в DataRegistry для установки ссылки на ATRProvider.

---

## 3. ✅ МОНИТОРИНГ ПЕРЕОТКРЫТИЯ ПОЗИЦИЙ

### Проблема
- Бот переоткрыл позицию ETH по рынку
- Нет явного логирования переоткрытия
- Нужна диагностика причин

### План действий

#### Шаг 3.1: Улучшить логирование в `_sync_positions_with_exchange`
**Файл:** `src/strategies/scalping/futures/orchestrator.py`  
**Метод:** `_sync_positions_with_exchange()`  
**Строки:** 2019-2257

**Текущий код:**
```2035:2051:src/strategies/scalping/futures/orchestrator.py
            # ✅ FIX: DRIFT_ADD log — позиция на бирже, но нет в реестре
            is_drift_add = symbol not in self.active_positions
            if is_drift_add:
                # ✅ ИСПРАВЛЕНО: Добавляем LOCK_DRIFT для предотвращения гонок
                if not hasattr(self, "_drift_locks"):
                    self._drift_locks: Dict[str, asyncio.Lock] = {}

                if symbol not in self._drift_locks:
                    self._drift_locks[symbol] = asyncio.Lock()

                async with self._drift_locks[symbol]:
                    # Повторная проверка после получения lock (double-check pattern)
                    if symbol not in self.active_positions:
                        logger.warning(
                            f"⚠️ DRIFT_ADD {symbol}: Позиция найдена на бирже, но отсутствует в реестре. "
                            f"Регистрируем позицию..."
                        )
```

**Изменения:**
1. Добавить логирование начала синхронизации (в начале метода, после строки 2011):
   ```python
   logger.info("📊 Начало синхронизации позиций с биржей...")
   ```

2. Улучшить логирование каждой найденной позиции (после строки 2019):
   ```python
   logger.info(
       f"📊 Позиция на бирже: {symbol}, size={abs_size}, "
       f"entry={entry_price:.2f}, side={position_side}, margin={margin:.2f}"
   )
   ```

3. Добавить логирование существующей позиции в реестре (после строки 2033):
   ```python
   if symbol in self.active_positions:
       existing = self.active_positions[symbol]
       logger.info(
           f"📊 Позиция в реестре: {symbol}, size={existing.get('size')}, "
           f"entry={existing.get('entry_price')}, side={existing.get('position_side')}"
       )
   ```

4. Улучшить логирование при DRIFT_ADD (строка 2048):
   ```python
   logger.warning(
       f"⚠️ DRIFT_ADD {symbol}: Позиция найдена на бирже, но отсутствует в реестре"
   )
   logger.info(
       f"📊 Параметры позиции на бирже: size={abs_size}, entry={entry_price:.2f}, side={position_side}"
   )
   logger.info(f"📊 Регистрируем позицию в PositionRegistry...")
   ```

5. Добавить логирование после регистрации (после регистрации в PositionRegistry):
   ```python
   logger.info(
       f"✅ Позиция {symbol} зарегистрирована в PositionRegistry: "
       f"entry={effective_price:.2f}, side={position_side}, size={size_in_coins}"
   )
   ```

#### Шаг 3.2: Добавить логирование в `_load_existing_positions`
**Файл:** `src/strategies/scalping/futures/orchestrator.py`  
**Метод:** `_load_existing_positions()`  
**Строки:** 1533-1750

**Текущий код:**
```1533:1536:src/strategies/scalping/futures/orchestrator.py
    async def _load_existing_positions(self):
        """✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Загружаем существующие позиции и инициализируем TrailingStopLoss"""
        try:
            logger.info("📊 Загрузка существующих позиций с биржи...")
```

**Изменения:**
1. Добавить логирование каждой найденной позиции (после строки 1551, внутри цикла):
   ```python
   logger.info(
       f"📊 Найдена позиция: {symbol}, size={abs(pos_size)}, "
       f"entry={entry_price:.2f}, side={position_side}"
   )
   ```

2. Добавить логирование инициализации TrailingStopLoss (после инициализации):
   ```python
   logger.info(f"✅ TrailingStopLoss инициализирован для {symbol}")
   ```

3. Добавить логирование завершения (в конце метода):
   ```python
   logger.info(f"✅ Загрузка позиций завершена: {len(positions_by_symbol)} позиций")
   ```

#### Шаг 3.3: Добавить метрики переоткрытия
**Файл:** `src/strategies/scalping/futures/orchestrator.py`  
**Метод:** `__init__()`  
**Строки:** 717-719

**Текущий код:**
```717:719:src/strategies/scalping/futures/orchestrator.py
        self.initialization_complete = asyncio.Event()  # Event для синхронизации готовности
        self.all_modules_ready = False  # Флаг готовности всех модулей
        self.skipped_signals_due_init = 0  # Счётчик пропущенных сигналов из-за неготовности
```

**Изменения:**
1. Добавить счетчик переоткрытий (после строки 719):
   ```python
   self.reopened_positions: Dict[str, int] = {}  # Счётчик переоткрытий по символам
   ```

2. Увеличивать счетчик при DRIFT_ADD (в `_sync_positions_with_exchange`, после строки 2048):
   ```python
   if symbol not in self.reopened_positions:
       self.reopened_positions[symbol] = 0
   self.reopened_positions[symbol] += 1
   logger.warning(
       f"📊 Переоткрытие позиции {symbol}: счетчик={self.reopened_positions[symbol]}"
   )
   ```

---

## 4. ✅ ДЕДУПЛИКАЦИЯ ТИКЕРОВ

### Проблема
- Тикеры обрабатываются несколько раз подряд с одинаковой ценой
- Много дубликатов тикеров ETH-USDT в логах

### План действий

#### Шаг 4.1: Добавить дедупликацию тикеров
**Файл:** `src/strategies/scalping/futures/coordinators/websocket_coordinator.py`  
**Метод:** `__init__()`  
**Строки:** 117-119

**Текущий код:**
```117:119:src/strategies/scalping/futures/coordinators/websocket_coordinator.py
        # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ (28.12.2025): Orchestrator для проверки готовности модулей
        self.orchestrator = orchestrator
        # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ (28.12.2025): Callback для синхронизации позиций
```

**Изменения:**
1. Добавить кэш последних тикеров (после строки 119):
   ```python
   # ✅ Дедупликация тикеров: кэш последних тикеров
   self._last_tickers: Dict[str, Dict[str, Any]] = {}  # symbol -> {price, timestamp}
   self._ticker_dedup_threshold = 0.0001  # Минимальное изменение цены для обработки (0.01%)
   ```

**Файл:** `src/strategies/scalping/futures/coordinators/websocket_coordinator.py`  
**Метод:** `handle_ticker_data()`  
**Строки:** 227-232

**Текущий код:**
```227:232:src/strategies/scalping/futures/coordinators/websocket_coordinator.py
            # Извлекаем данные из ответа WebSocket
            if "data" in data and len(data["data"]) > 0:
                ticker = data["data"][0]

                if "last" in ticker:
                    price = float(ticker["last"])
```

**Изменения:**
1. Добавить проверку дедупликации (после строки 232, перед обработкой):
   ```python
   # ✅ Дедупликация тикеров: пропускаем дубликаты
   if symbol in self._last_tickers:
       last_price = self._last_tickers[symbol].get("price")
       last_time = self._last_tickers[symbol].get("timestamp", 0)
       current_time = time.time()
       
       # Пропускаем, если цена не изменилась и прошло менее 1 секунды
       if last_price == price and (current_time - last_time) < 1.0:
           logger.debug(f"⏭️ Тикер пропущен (дубликат): {symbol} price={price:.2f}")
           return
       
       # Пропускаем, если изменение цены меньше порога
       if last_price and abs(price - last_price) / last_price < self._ticker_dedup_threshold:
           logger.debug(
               f"⏭️ Тикер пропущен (минимальное изменение): {symbol} price={price:.2f} "
               f"(изменение < {self._ticker_dedup_threshold*100:.2f}%)"
           )
           return
   
   # Обновляем кэш
   self._last_tickers[symbol] = {"price": price, "timestamp": time.time()}
   ```

**Примечание:** Нужно добавить `import time` в начало файла, если его нет.

---

## 5. ✅ ИСПРАВЛЕНИЕ ОШИБКИ ConversionMetrics

### Проблема
- `⚠️ TCC: Ошибка проверки метрик и алертов: 'ConversionMetrics' object has no attribute 'get_win_rate'`
- `ConversionMetrics` не имеет методов `get_win_rate()` и `get_emergency_close_rate()`

### Анализ кода
- **Файл:** `src/strategies/scalping/futures/metrics/conversion_metrics.py`
- **Методы:** `get_conversion_rate()`, `get_filter_reasons()`, `get_regime_stats()`, `get_signal_type_stats()`, `get_summary()`, `log_summary()`
- **НЕТ методов:** `get_win_rate()`, `get_emergency_close_rate()`

### План действий

#### Шаг 5.1: Исправить вызов метода в `_check_metrics_and_alerts`
**Файл:** `src/strategies/scalping/futures/core/trading_control_center.py`  
**Метод:** `_check_metrics_and_alerts()`  
**Строки:** 850-864

**Текущий код:**
```850:864:src/strategies/scalping/futures/core/trading_control_center.py
            conversion_rate = self.conversion_metrics.get_conversion_rate(
                period_hours=24
            )
            win_rate = self.conversion_metrics.get_win_rate(period_hours=24)
            emergency_close_rate = self.conversion_metrics.get_emergency_close_rate(
                period_hours=24
            )

            # Логируем метрики
            logger.info(
                f"📊 Метрики за 24 часа: "
                f"конверсия сигналов={conversion_rate.get('signal_to_position', 0):.1%}, "
                f"win_rate={win_rate:.1%}, "
                f"emergency_close_rate={emergency_close_rate:.1%}"
            )
```

**Изменения:**
1. Удалить вызовы несуществующих методов и использовать доступные методы:
   ```python
   if not self.conversion_metrics:
       return
   
   # Получаем метрики конверсии (используем доступные методы)
   conversion_rate = self.conversion_metrics.get_conversion_rate(period_hours=24)
   
   # ✅ ИСПРАВЛЕНО: ConversionMetrics не имеет get_win_rate() и get_emergency_close_rate()
   # Используем summary для получения полной статистики
   summary = self.conversion_metrics.get_summary(period_hours=24)
   
   # Логируем метрики
   logger.info(
       f"📊 Метрики за 24 часа: "
       f"конверсия сигналов={conversion_rate.get('executed_to_generated', 0):.1%}, "
       f"сгенерировано={conversion_rate.get('generated', 0)}, "
       f"отфильтровано={conversion_rate.get('filtered', 0)}, "
       f"исполнено={conversion_rate.get('executed', 0)}"
   )
   
   # Проверяем критические пороги (используем conversion_rate вместо win_rate)
   conversion_percent = conversion_rate.get('executed_to_generated', 0)
   if conversion_percent < 30:
       self.alert_manager.send_alert(
           f"⚠️ КРИТИЧНО: Конверсия сигналов ниже 30%: {conversion_percent:.1%}", level="warning"
       )
   ```

2. Обновить проверку критических порогов (после строки 864):
   ```python
   # ✅ ИСПРАВЛЕНО: Используем conversion_rate вместо win_rate
   conversion_percent = conversion_rate.get('executed_to_generated', 0)
   if conversion_percent < 30:
       self.alert_manager.send_alert(
           f"⚠️ КРИТИЧНО: Конверсия сигналов ниже 30%: {conversion_percent:.1%}", level="warning"
       )
   ```

---

## 📊 ПРИОРИТЕТЫ ИСПРАВЛЕНИЙ

### 🔴 КРИТИЧНО (Сделать первым)
1. **Проверка инициализации** (Шаг 1.1-1.2) - 15 мин
2. **Исправление ATR** (Шаг 2.1-2.2, 2.4) - 30 мин
3. **Исправление ConversionMetrics** (Шаг 5.1) - 10 мин

### 🟡 ВАЖНО (Сделать вторым)
4. **Улучшение получения ATR** (Шаг 2.3, 2.5) - 20 мин
5. **Логирование переоткрытия** (Шаг 3.1-3.2) - 20 мин
6. **Дедупликация тикеров** (Шаг 4.1) - 15 мин

### 🟢 ЖЕЛАТЕЛЬНО (Сделать третьим)
7. **Метрики переоткрытия** (Шаг 3.3) - 10 мин

---

## ✅ ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### 1. Инициализация
- ✅ Все этапы инициализации видны в логах
- ✅ Сообщение `🟢 Все модули инициализированы` появляется
- ✅ Можно отследить проблемы инициализации

### 2. ATR
- ✅ ATR находится в DataRegistry
- ✅ ATRProvider получает ATR корректно
- ✅ Нет `ATR не найден` в логах
- ✅ ATR кэш обновляется автоматически

### 3. Переоткрытие позиций
- ✅ Видно, когда и почему происходит переоткрытие
- ✅ Можно отследить причины
- ✅ Есть метрики для анализа

### 4. Тикеры
- ✅ Нет дубликатов тикеров в логах
- ✅ Тикеры обрабатываются только при изменении цены
- ✅ Меньше нагрузки на систему

### 5. ConversionMetrics
- ✅ Нет ошибок в логах
- ✅ Метрики работают корректно
- ✅ Используются доступные методы

---

## 📝 ЗАМЕТКИ

- Все изменения должны быть обратно совместимы
- Логирование должно быть на DEBUG уровне для детальной диагностики
- Критические моменты должны логироваться на INFO уровне
- Ошибки должны логироваться на WARNING/ERROR уровне
- Все ссылки на код проверены и актуальны



