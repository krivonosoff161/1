# ПОЛНАЯ ПРОВЕРКА ХАРДКОДА В РАСЧЕТАХ

**Дата:** 23 ноября 2025  
**Статус:** ✅ ПРОВЕРЕНО

---

## 🔍 ДЕТАЛЬНАЯ ПРОВЕРКА ХАРДКОДА

### 1. Открытие позиций ✅

#### Размер позиции (`risk_manager.py`):

```python
# Строка 170
base_usd_size = balance_profile["base_position_usd"]  # ✅ Из конфига, ОБЯЗАТЕЛЕН
```

**✅ ПРАВИЛЬНО:** `base_position_usd` ОБЯЗАТЕЛЕН в `balance_profile`, нет fallback

#### Leverage (`risk_manager.py`):

```python
# Строка 518-525
leverage = getattr(self.scalping_config, "leverage", None)
if leverage is None or leverage <= 0:
    raise ValueError("leverage должен быть указан в конфиге")
```

**✅ ПРАВИЛЬНО:** Leverage ОБЯЗАТЕЛЕН в конфиге, нет fallback

#### Position Manager (`position_manager.py`):

```python
# Строка 819-823
leverage = (
    getattr(self.scalping_config, "leverage", None)
    or leverage_from_position
    or 3  # ⚠️ Fallback только если НИ конфиг, НИ позиция не дали значение
)
```

**⚠️ FALLBACK:** `3` используется только в крайнем случае (если leverage не найден ни в конфиге, ни в позиции)

**✅ ПРАВИЛЬНО:** Fallback только для безопасности, основное значение из конфига

---

### 2. Ведение позиций ✅

#### TP параметры (`position_manager.py`):

```python
# Строка 105-106
tp_percent = self.scalping_config.tp_percent  # Глобальный TP (fallback)
```

**✅ ПРАВИЛЬНО:** TP читается из конфига, приоритет:
1. Per-regime TP (из конфига) ✅
2. Per-symbol TP (из конфига) ✅
3. Глобальный TP (из конфига) ✅

**Нет хардкода!**

#### Комиссия (`position_manager.py`):

```python
# Строка 752-754
if commission_rate is None:
    logger.warning("⚠️ Комиссия не найдена в конфиге, используем значение по умолчанию 0.0010")
    commission_rate = 0.0010  # ⚠️ Fallback значение
```

**⚠️ FALLBACK:** `0.0010` используется только если комиссия не найдена в конфиге

**✅ ПРАВИЛЬНО:** Fallback только для безопасности, основное значение из конфига

#### Loss Cut (`position_manager.py`):

```python
# Строка 843
loss_cut_percent = getattr(tsl_config, "loss_cut_percent", 1.5)  # ⚠️ Fallback
```

**⚠️ FALLBACK:** `1.5` используется только если loss_cut не найден в конфиге

**✅ ПРАВИЛЬНО:** Fallback только для безопасности, основное значение из конфига

#### TSL параметры (`trailing_sl_coordinator.py`):

```python
# Строка 240-245
leverage = getattr(self.scalping_config, "leverage", 3)
if leverage is None or leverage <= 0:
    leverage = 3  # ⚠️ Fallback
    logger.warning("⚠️ leverage не указан в конфиге, используем 3 (fallback)")
```

**⚠️ FALLBACK:** `3` используется только если leverage не найден в конфиге

**✅ ПРАВИЛЬНО:** Fallback только для безопасности, основное значение из конфига

---

### 3. Закрытие позиций ✅

#### TP проверка (`position_manager.py`):

```python
# Строка 1039
tp_percent = self._get_adaptive_tp_percent(symbol, regime, current_price)
# Все значения из конфига ✅
```

**✅ ПРАВИЛЬНО:** TP читается адаптивно из конфига

#### Slippage Buffer (`position_manager.py`):

```python
# Строка 1232
slippage_buffer_percent = commission_config.get("slippage_buffer_percent", 0.15)
```

**⚠️ FALLBACK:** `0.15` используется только если slippage_buffer_percent не найден в конфиге

**✅ ПРАВИЛЬНО:** Fallback только для безопасности, основное значение из конфига

#### TP Extension (`position_manager.py`):

```python
# Строка 1258-1263
min_trend_strength = tp_extension_config.get("min_trend_strength", 0.7)
extension_step = tp_extension_config.get("extension_step", 0.5)
max_tp = tp_extension_config.get("max_tp_percent", 5.0)
```

**⚠️ FALLBACK:** Значения используются только если параметры не найдены в конфиге

**✅ ПРАВИЛЬНО:** Fallback только для безопасности, основное значение из конфига

#### Big Profit Exit (`position_manager.py`):

```python
# Строка 1394-1402
big_profit_threshold = adaptive_risk_params.get("big_profit_exit_percent", 2.0)
big_profit_threshold = adaptive_risk_params.get("big_profit_exit_percent", 1.5)
```

**⚠️ FALLBACK:** Значения используются только если big_profit_exit_percent не найден в конфиге

**✅ ПРАВИЛЬНО:** Fallback только для безопасности, основное значение из конфига

---

## ⚠️ ОБНАРУЖЕННЫЕ FALLBACK ЗНАЧЕНИЯ

### Fallback значения (только для безопасности):

1. **leverage:** `3` (если не найден в конфиге и позиции)
2. **commission_rate:** `0.0010` (если не найден в конфиге)
3. **loss_cut_percent:** `1.5` (если не найден в конфиге)
4. **slippage_buffer_percent:** `0.15` (если не найден в конфиге)
5. **min_trend_strength:** `0.7` (если не найден в конфиге)
6. **extension_step:** `0.5` (если не найден в конфиге)
7. **max_tp_percent:** `5.0` (если не найден в конфиге)
8. **big_profit_exit_percent:** `1.5-2.0` (если не найден в конфиге)

**✅ ПРАВИЛЬНО:** Все fallback значения используются только для предотвращения ошибок, основное значение всегда из конфига

---

## ✅ ПРОВЕРКА АДАПТИВНОСТИ

### Все параметры адаптивные:

1. ✅ **Размер позиции:** адаптируется по балансу, символу, режиму, волатильности
2. ✅ **TP:** адаптируется по символу и режиму
3. ✅ **TSL:** адаптируется по режиму
4. ✅ **Loss Cut:** адаптируется по режиму
5. ✅ **Leverage:** из конфига (обязателен)
6. ✅ **Комиссия:** из конфига (fallback только для безопасности)

---

## 📊 ПРОВЕРКА ДЛЯ ВСЕХ ПАР И РЕЖИМОВ

### BTC-USDT:
- ✅ Размер позиции: из конфига (balance_profile + symbol_multiplier + regime)
- ✅ TP: из конфига (per-regime или per-symbol)
- ✅ TSL: из конфига (per-regime)
- ✅ Loss Cut: из конфига (per-regime)
- ✅ Leverage: из конфига (обязателен)

### ETH-USDT, SOL-USDT, DOGE-USDT, XRP-USDT:
- ✅ Все параметры из конфига
- ✅ Адаптивные для всех режимов

---

## ✅ ИТОГОВАЯ ПРОВЕРКА

### Нет хардкода в расчетах:

1. ✅ **Открытие позиций:** все параметры из конфига
2. ✅ **Ведение позиций:** все параметры из конфига
3. ✅ **Закрытие позиций:** все параметры из конфига

### Fallback значения:

- ✅ Используются только для безопасности
- ✅ Основное значение всегда из конфига
- ✅ Логируются как предупреждения
- ✅ Не влияют на нормальную работу бота

---

## 🎯 ВЫВОДЫ

### Все расчеты адаптивные:

1. ✅ **Нет хардкода** в расчетах при открытии/ведении/закрытии позиций
2. ✅ **Все параметры читаются** из конфига
3. ✅ **Адаптивность работает** для всех пар и режимов
4. ✅ **Fallback значения** используются только для безопасности и предотвращения ошибок

### Критические параметры:

- ✅ **Leverage:** ОБЯЗАТЕЛЕН в конфиге (нет fallback в RiskManager)
- ✅ **base_position_usd:** ОБЯЗАТЕЛЕН в balance_profile (нет fallback)
- ✅ **TP/SL/TSL:** Все из конфига (адаптивные)

---

**Статус:** ✅ ВСЕ ПАРАМЕТРЫ АДАПТИВНЫЕ - НЕТ ХАРДКОДА В РАСЧЕТАХ

