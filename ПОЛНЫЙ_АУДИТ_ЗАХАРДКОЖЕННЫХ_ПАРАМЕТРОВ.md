# 🔍 ПОЛНЫЙ АУДИТ ЗАХАРДКОЖЕННЫХ ПАРАМЕТРОВ

**Дата:** 2025-11-12  
**Цель:** Найти все захардкоженные значения и заменить их на параметры из конфига

---

## 📊 НАЙДЕННЫЕ ЗАХАРДКОЖЕННЫЕ ЗНАЧЕНИЯ

### ❌ КРИТИЧЕСКИЕ: Параметры риска (НЕ ИЗ КОНФИГА!)

**Файл:** `src/strategies/scalping/futures/orchestrator.py`

#### 1. Параметры риска (строки 65-67):
```python
self.max_loss_per_trade = 0.02  # 2% макс потеря на сделку
self.max_margin_percent = 0.80  # 80% макс маржа
self.max_drawdown_percent = 0.05  # 5% макс просадка
```

**Проблема:**
- ❌ Захардкожены значения 2%, 80%, 5%
- ✅ В конфиге есть `risk_per_trade_percent: 1.5`, но нет `max_loss_per_trade`, `max_margin_percent`, `max_drawdown_percent`
- ❌ Эти параметры НЕ используются из конфига!

**Решение:**
- Добавить в конфиг: `max_loss_per_trade_percent`, `max_margin_percent`, `max_drawdown_percent`
- Читать из конфига в коде

---

#### 2. Параметры маржи (строки 95-96):
```python
maintenance_margin_ratio=0.01  # 1%
initial_margin_ratio=0.1  # 10%
```

**Проблема:**
- ❌ Захардкожены значения 1%, 10%
- ❌ Нет в конфиге!

**Решение:**
- Добавить в конфиг: `maintenance_margin_ratio`, `initial_margin_ratio`
- Читать из конфига в коде

---

#### 3. Параметры slippage guard (строки 108-110):
```python
max_slippage_percent=slippage_config.get("max_slippage_percent", 0.1),  # 0.1% (fallback)
max_spread_percent=slippage_config.get("max_spread_percent", 0.05),  # 0.05% (fallback)
order_timeout=slippage_config.get("order_timeout", 30.0),  # 30 сек (fallback)
```

**Проблема:**
- ⚠️ Есть fallback значения 0.1%, 0.05%, 30 сек
- ✅ В конфиге есть `futures_modules.slippage_guard`, но fallback значения могут использоваться если конфиг не загружен

**Решение:**
- Убедиться, что конфиг всегда загружается правильно
- Убрать fallback значения или сделать их очень маленькими для явной ошибки

---

#### 4. Параметры FastADX (строка 136):
```python
self.fast_adx = FastADX(period=9, threshold=20.0)
```

**Проблема:**
- ❌ Захардкожены значения `period=9`, `threshold=20.0`
- ✅ В конфиге есть `adaptive_regime.detection.adx_period: 9`, `trending_adx_threshold: 20.0`
- ❌ Но они НЕ используются в коде!

**Решение:**
- Читать из конфига: `adaptive_regime.detection.adx_period`, `adaptive_regime.detection.trending_adx_threshold`
- Использовать в коде

---

#### 5. Параметры FundingRateMonitor (строка 157):
```python
self.funding_monitor = FundingRateMonitor(max_funding_rate=0.05)  # 0.05%
```

**Проблема:**
- ❌ Захардкожено значение 0.05%
- ❌ Нет в конфиге!

**Решение:**
- Добавить в конфиг: `max_funding_rate`
- Читать из конфига в коде

---

#### 6. Параметры MaxSizeLimiter (строки 161-163):
```python
self.max_size_limiter = MaxSizeLimiter(
    max_single_size_usd=1000.0,  # $1000 за позицию
    max_total_size_usd=5000.0,  # $5000 всего
    max_positions=5,  # Максимум 5 позиций
)
```

**Проблема:**
- ❌ Захардкожены значения $1000, $5000, 5 позиций
- ✅ В конфиге есть `risk.max_open_positions: 5`, но нет `max_single_size_usd`, `max_total_size_usd`
- ❌ Эти параметры НЕ используются из конфига!

**Решение:**
- Читать из конфига: `risk.max_open_positions`
- Рассчитывать `max_single_size_usd` и `max_total_size_usd` из `balance_profiles`
- Или добавить в конфиг явно

---

#### 7. Параметры синхронизации позиций (строка 204):
```python
self.positions_sync_interval = max(15.0, check_interval * 3)
```

**Проблема:**
- ❌ Захардкожено значение `15.0` секунд (минимум)
- ❌ Захардкожен множитель `3` (check_interval * 3)
- ❌ Нет в конфиге!

**Решение:**
- Добавить в конфиг: `positions_sync_interval_min`, `positions_sync_interval_multiplier`
- Читать из конфига в коде

---

#### 8. Параметры расчета размера позиции (строки 2248-2250):
```python
if min_usd_size is None or min_usd_size <= 0:
    min_usd_size = base_usd_size * 0.5  # 50% от base
if max_usd_size is None or max_usd_size <= 0:
    max_usd_size = base_usd_size * 2.0  # 200% от base
```

**Проблема:**
- ❌ Захардкожены множители 0.5 (50%) и 2.0 (200%)
- ✅ В конфиге есть `min_position_usd` и `max_position_usd` в `balance_profiles`
- ⚠️ Fallback значения используются только если не указано в конфиге

**Решение:**
- Убедиться, что в конфиге всегда указаны `min_position_usd` и `max_position_usd`
- Убрать fallback значения или сделать их очень маленькими для явной ошибки

---

#### 9. Параметры fallback для balance profiles (строки 2562, 2571):
```python
max_open_positions = 2  # fallback
max_position_percent = 8.0  # fallback
```

**Проблема:**
- ❌ Захардкожены fallback значения 2 позиции, 8%
- ✅ В конфиге есть `max_open_positions` и `max_position_percent` в `balance_profiles`
- ⚠️ Fallback значения используются только если не указано в конфиге

**Решение:**
- Убедиться, что в конфиге всегда указаны `max_open_positions` и `max_position_percent`
- Убрать fallback значения или сделать их очень маленькими для явной ошибки

---

#### 10. Параметры проверки маржи (строка 2393):
```python
if margin_required > balance * 0.9:  # 90% безопасности
    margin_required = balance * 0.9
```

**Проблема:**
- ❌ Захардкожено значение 0.9 (90%)
- ❌ Нет в конфиге!

**Решение:**
- Добавить в конфиг: `max_margin_safety_percent` (например, 0.9 = 90%)
- Читать из конфига в коде

---

#### 11. Параметры проверки последнего ордера (строка 1372):
```python
if time_since_order < 30:  # 30 секунд
```

**Проблема:**
- ❌ Захардкожено значение 30 секунд
- ❌ Нет в конфиге!

**Решение:**
- Добавить в конфиг: `min_time_between_orders_seconds`
- Читать из конфига в коде

---

#### 12. Параметры минимального баланса (строка 1518):
```python
if balance < 20.0:  # Минимум $20 баланса
```

**Проблема:**
- ❌ Захардкожено значение $20
- ✅ В конфиге есть `risk.adaptive_minimums.small.minimum_order_usd: 5.0`, но это для ордеров, не для баланса
- ❌ Нет в конфиге параметра для минимального баланса!

**Решение:**
- Добавить в конфиг: `min_balance_usd`
- Читать из конфига в коде

---

#### 13. Параметры strength multipliers (строки 2297-2322):
```python
strength_multiplier = 0.5  # Конфликт: 50%
strength_multiplier = 1.5  # Сильный сигнал: 150%
strength_multiplier = 1.2  # Хороший сигнал: 120%
strength_multiplier = 1.0  # Средний сигнал: 100%
strength_multiplier = 0.8  # Слабый сигнал: 80%
```

**Проблема:**
- ❌ Захардкожены множители 0.5, 1.5, 1.2, 1.0, 0.8
- ❌ Захардкожены пороги сигналов: 0.8, 0.6, 0.4
- ❌ Нет в конфиге!

**Решение:**
- Добавить в конфиг: `signal_strength_multipliers` (для разных уровней силы сигнала)
- Добавить в конфиг: `signal_strength_thresholds` (пороги для определения уровня силы)
- Читать из конфига в коде

---

#### 14. Параметры position overrides (строка 2224):
```python
if abs(override_size - base_usd_size) / base_usd_size > 0.5:  # 50% разница
```

**Проблема:**
- ❌ Захардкожено значение 0.5 (50%)
- ❌ Нет в конфиге!

**Решение:**
- Добавить в конфиг: `position_override_tolerance_percent` (например, 0.5 = 50%)
- Читать из конфига в коде

---

## ✅ ЧТО УЖЕ РАБОТАЕТ ПРАВИЛЬНО

### 1. Balance Profiles - ✅ РАБОТАЕТ
- ✅ Параметры берутся из конфига: `balance_profiles.small.base_position_usd: 100.0`
- ✅ Прогрессивная адаптация работает: `progressive: true`
- ✅ Параметры применяются правильно

### 2. Adaptive Regime Manager (ARM) - ✅ РАБОТАЕТ
- ✅ Параметры берутся из конфига: `adaptive_regime.trending.min_score_threshold: 1.5`
- ✅ Режимы определяются правильно: `trending`, `ranging`, `choppy`
- ✅ Параметры применяются правильно

### 3. Leverage - ✅ РАБОТАЕТ
- ✅ Параметр берется из конфига: `scalping.leverage: 5`
- ✅ Используется в коде правильно

### 4. TP/SL - ✅ РАБОТАЕТ
- ✅ Параметры берутся из конфига: `scalping.tp_percent: 1.0`, `scalping.sl_percent: 1.5`
- ✅ Используются в коде правильно

### 5. Slippage Guard - ⚠️ ЧАСТИЧНО РАБОТАЕТ
- ✅ Параметры берутся из конфига: `futures_modules.slippage_guard.max_slippage_percent: 0.1`
- ⚠️ Но есть fallback значения, которые могут использоваться

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### Проблема #1: Параметры риска НЕ из конфига
- `max_loss_per_trade = 0.02` (2%) - захардкожено
- `max_margin_percent = 0.80` (80%) - захардкожено
- `max_drawdown_percent = 0.05` (5%) - захардкожено
- **Влияние:** Параметры риска не настраиваются через конфиг!

### Проблема #2: Параметры маржи НЕ из конфига
- `maintenance_margin_ratio=0.01` (1%) - захардкожено
- `initial_margin_ratio=0.1` (10%) - захардкожено
- **Влияние:** Параметры маржи не настраиваются через конфиг!

### Проблема #3: Параметры синхронизации НЕ из конфига
- `positions_sync_interval = max(15.0, check_interval * 3)` - захардкожено
- **Влияние:** Частота синхронизации не настраивается через конфиг!

### Проблема #4: Параметры strength multipliers НЕ из конфига
- `strength_multiplier = 0.5, 1.5, 1.2, 1.0, 0.8` - захардкожены
- **Влияние:** Множители силы сигнала не настраиваются через конфиг!

### Проблема #5: Параметры проверки НЕ из конфига
- `time_since_order < 30` (30 секунд) - захардкожено
- `balance < 20.0` ($20) - захардкожено
- `margin_required > balance * 0.9` (90%) - захардкожено
- **Влияние:** Пороги проверок не настраиваются через конфиг!

---

## 💡 РЕШЕНИЯ

### Решение #1: Добавить параметры риска в конфиг
```yaml
risk:
  max_loss_per_trade_percent: 2.0  # 2% макс потеря на сделку
  max_margin_percent: 80.0  # 80% макс маржа
  max_drawdown_percent: 5.0  # 5% макс просадка
  risk_per_trade_percent: 1.5  # Риск на сделку (уже есть)
  max_margin_safety_percent: 90.0  # 90% безопасности (финальная проверка)
```

### Решение #2: Добавить параметры маржи в конфиг
```yaml
futures_modules:
  margin:
    maintenance_margin_ratio: 0.01  # 1%
    initial_margin_ratio: 0.1  # 10%
```

### Решение #3: Добавить параметры синхронизации в конфиг
```yaml
scalping:
  positions_sync_interval_min: 15.0  # Минимум 15 секунд
  positions_sync_interval_multiplier: 3.0  # Множитель (check_interval * multiplier)
```

### Решение #4: Добавить параметры strength multipliers в конфиг
```yaml
scalping:
  signal_strength:
    conflict_multiplier: 0.5  # Конфликт: 50%
    very_strong_multiplier: 1.5  # Сильный сигнал: 150%
    strong_multiplier: 1.2  # Хороший сигнал: 120%
    medium_multiplier: 1.0  # Средний сигнал: 100%
    weak_multiplier: 0.8  # Слабый сигнал: 80%
    thresholds:
      very_strong: 0.8  # Порог для сильного сигнала
      strong: 0.6  # Порог для хорошего сигнала
      medium: 0.4  # Порог для среднего сигнала
```

### Решение #5: Добавить параметры проверок в конфиг
```yaml
scalping:
  min_time_between_orders_seconds: 30  # Минимум 30 секунд между ордерами
  min_balance_usd: 20.0  # Минимум $20 баланса
  position_override_tolerance_percent: 50.0  # 50% разница для игнорирования override
```

### Решение #6: Добавить параметры других модулей в конфиг
```yaml
futures_modules:
  funding_rate:
    max_funding_rate: 0.05  # 0.05%
  adx:
    period: 9  # Период ADX
    threshold: 20.0  # Порог ADX
  max_size_limiter:
    max_single_size_usd: 1000.0  # $1000 за позицию (или рассчитывать из balance_profiles)
    max_total_size_usd: 5000.0  # $5000 всего (или рассчитывать из balance_profiles)
```

---

## 📊 ИССЛЕДОВАНИЕ ОПТИМАЛЬНЫХ ЗНАЧЕНИЙ

### Вопрос #1: Какие значения `max_loss_per_trade_percent` оптимальны?
- **Текущее:** 2.0% (захардкожено)
- **В конфиге:** `risk_per_trade_percent: 1.5%`
- **Исследование:** Нужно протестировать значения от 1.0% до 3.0%

### Вопрос #2: Какие значения `max_margin_percent` оптимальны?
- **Текущее:** 80.0% (захардкожено)
- **Исследование:** Нужно протестировать значения от 70% до 90%

### Вопрос #3: Какие значения `positions_sync_interval` оптимальны?
- **Текущее:** `max(15.0, check_interval * 3)` (захардкожено)
- **Исследование:** Нужно протестировать значения от 5 до 30 секунд

### Вопрос #4: Какие значения `strength_multipliers` оптимальны?
- **Текущее:** 0.5, 1.5, 1.2, 1.0, 0.8 (захардкожены)
- **Исследование:** Нужно протестировать разные комбинации множителей

### Вопрос #5: Какие значения `signal_strength_thresholds` оптимальны?
- **Текущее:** 0.8, 0.6, 0.4 (захардкожены)
- **Исследование:** Нужно протестировать разные пороги

---

## 🎯 ПЛАН ИСПРАВЛЕНИЙ

### Неделя 1: Добавить параметры в конфиг
1. ✅ Добавить параметры риска в конфиг
2. ✅ Добавить параметры маржи в конфиг
3. ✅ Добавить параметры синхронизации в конфиг
4. ✅ Добавить параметры strength multipliers в конфиг
5. ✅ Добавить параметры проверок в конфиг

### Неделя 2: Исправить код
6. ✅ Заменить захардкоженные значения на чтение из конфига
7. ✅ Убрать fallback значения или сделать их очень маленькими
8. ✅ Добавить валидацию параметров из конфига

### Неделя 3: Исследование и оптимизация
9. ✅ Протестировать разные значения параметров
10. ✅ Выбрать оптимальные значения
11. ✅ Обновить конфиг с оптимальными значениями

---

## 📝 ВЫВОДЫ

### Главные проблемы:
1. **Параметры риска захардкожены** - не настраиваются через конфиг
2. **Параметры маржи захардкожены** - не настраиваются через конфиг
3. **Параметры синхронизации захардкожены** - не настраиваются через конфиг
4. **Параметры strength multipliers захардкожены** - не настраиваются через конфиг
5. **Параметры проверок захардкожены** - не настраиваются через конфиг

### Что работает правильно:
1. ✅ Balance Profiles - параметры из конфига
2. ✅ Adaptive Regime Manager (ARM) - параметры из конфига
3. ✅ Leverage - параметр из конфига
4. ✅ TP/SL - параметры из конфига

### Решения:
1. **Добавить все параметры в конфиг** - чтобы можно было настраивать
2. **Заменить захардкоженные значения на чтение из конфига** - чтобы использовались параметры из конфига
3. **Провести исследование оптимальных значений** - чтобы выбрать лучшие параметры

---

**Дата создания:** 2025-11-12  
**Статус:** 📋 ГОТОВ К ИСПРАВЛЕНИЮ

**Следующие шаги:**
1. Добавить параметры в конфиг
2. Исправить код для чтения параметров из конфига
3. Провести исследование оптимальных значений
4. Обновить конфиг с оптимальными значениями

