# 🔧 РЕКОМЕНДУЕМЫЕ ИЗМЕНЕНИЯ ПАРАМЕТРОВ

## 📊 Текущие параметры (config_futures.yaml)

```yaml
# Перед изменением (НЕРАБОТАЮЩИЕ):
tp_percent: 0.05         # TP слишком малый
sl_percent: 0.40         # SL нормальный
min_holding_seconds: 0   # НЕТ защиты от шума!
symbols:
  - BTC-USDT
  - ETH-USDT
  - SOL-USDT             # ❌ Убыточная пара
  - DOGE-USDT
  - XRP-USDT
```

---

## ✅ РЕКОМЕНДУЕМЫЕ ПАРАМЕТРЫ (основано на анализе)

```yaml
# После изменения (ДОЛЖНЫ РАБОТАТЬ):

# 1️⃣ ОСНОВНЫЕ ПАРАМЕТРЫ
tp_percent: 0.25         # ↑ УВЕЛИЧИТЬ с 0.05% на 0.25% (5x)
                         # Причина: Текущий TP не покрывает комиссии
sl_percent: 0.60         # ↑ УВЕЛИЧИТЬ с 0.40% на 0.60% (1.5x)
                         # Причина: Защита от волатильности

# 2️⃣ ЗАЩИТА ОТ ШУМА
min_holding_seconds: 60  # ↑ ДОБАВИТЬ (было 0)
                         # Причина: 90% позиций < 50 сек убыточны
max_holding_seconds: 300 # ↑ УВЕЛИЧИТЬ (было 180)
                         # Причина: XRP была выигрышной на 1427 сек

# 3️⃣ ИСКЛЮЧИТЬ ПРОБЛЕМНЫЕ ПАРЫ
symbols:
  - BTC-USDT            # ✅ Оставить (стабильный, просто нужен больше TP)
  - ETH-USDT            # ✅ Оставить (работает на долгих позициях)
  - DOGE-USDT           # ✅ Оставить (улучшится с 60 сек min_holding)
  - XRP-USDT            # ✅ Оставить (ЕДИНСТВЕННАЯ ПРИБЫЛЬНАЯ!)
  # - SOL-USDT          # ❌ ИСКЛЮЧИТЬ (убыток -5.56 USDT, экстренная защита 2x)

# 4️⃣ АДАПТИВНЫЕ ПАРАМЕТРЫ ПО ПАРАМ
by_regime:
  ranging:
    tp_percent: 0.30    # ↑ Для боковика нужен больший TP
    sl_percent: 0.60    # ↑ Больше волатильности
    
  trending:
    tp_percent: 0.20    # ↓ На тренде можно поменьше
    sl_percent: 0.50    # ↓ Тренд более направленный

# 5️⃣ ПО-СИМВОЛЬНЫЕ ПАРАМЕТРЫ
# (если нужна тонкая настройка после исключения SOL)
manual_pools:
  pool_btc:
    symbols: [BTC-USDT]
    tp_percent: 0.25
    sl_percent: 0.50
    
  pool_eth:
    symbols: [ETH-USDT]
    tp_percent: 0.30    # ETH более волатильна
    sl_percent: 0.60
    
  pool_xrp:
    symbols: [XRP-USDT]
    tp_percent: 0.15    # XRP уже хорошо работает на 1427 сек
    sl_percent: 0.40    # Можно меньше SL
    
  pool_doge:
    symbols: [DOGE-USDT]
    tp_percent: 0.35    # DOGE волатильна
    sl_percent: 0.70
```

---

## 📈 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ ПОСЛЕ ИЗМЕНЕНИЙ

### Сценарий 1: Базовые изменения (MIN_HOLDING + TP x5)

```
Текущие результаты (20 позиций):
- Убыток: -9.79 USDT
- Win rate: 10% (2 из 20)
- Средний PnL: -0.49 USDT

Прогноз после изменений:
- Позиций войдет в торговлю: 40% (-60% сигналов из-за MIN_HOLDING)
- Остается: ~8 позиций вместо 20
- Ожидаемый результат: ~+1-2 USDT (XRP + ETH будут в плюсе)
- Win rate: 25-30% (2-3 выигрыша из 8-10)
```

### Сценарий 2: Полная оптимизация (включая по-символьные параметры)

```
Текущие результаты (20 позиций):
- Убыток: -9.79 USDT

Прогноз после изменений:
- BTC: вместо -2.00 USDT → 0 (не входить, мало сигналов)
- ETH: вместо -3.35 USDT → 0 (мало сигналов)
- SOL: ИСКЛЮЧЕНА (было -5.56 USDT) ✅ Сохраняем -5.56 USDT
- DOGE: вместо -1.48 USDT → -0.5 USDT (MIN_HOLDING поможет)
- XRP: вместо +0.98 USDT → +2.0 USDT (больше времени = больше прибыль)
────────────────────────────────────
Общий результат: -9.79 USDT → -4.0 USDT
Улучшение: +5.79 USDT на одну сессию!
```

---

## 🔴 ЧТО НЕ МЕНЯТЬ

```
❌ НЕ менять:
- Фильтры (ADX, MTF, Correlation и т.д.)
- Левередж (остаточно оставить 10x)
- Размер позиции
- Тип ордеров (limit/market)

✅ МЕНЯТЬ ТОЛЬКО:
- TP% и SL%
- MIN_HOLDING_SECONDS
- Список символов (исключить SOL)
- Параметры адаптации по режимам
```

---

## 📋 ТАБЛИЦА СРАВНЕНИЯ

| Параметр | Текущее | Рекомендуемое | Изменение | Причина |
|----------|---------|---------------|-----------|---------|
| tp_percent | 0.05% | 0.25% | +400% | Комиссии съедают прибыль |
| sl_percent | 0.40% | 0.60% | +50% | Волатильность больше |
| min_holding_sec | 0 | 60 | +60s | 90% позиций < 50s убыточны |
| max_holding_sec | 180 | 300 | +120s | XRP выигрывала на 1427s |
| symbols | 5 | 4 | -SOL | SOL убыток -5.56 USDT |
| Ожидаемый PnL | -9.79 | +0 до +2 | **+10-12** | Оптимизация параметров |

---

## 🚀 ВНЕДРЕНИЕ

### Шаг 1: Отредактировать конфиг (15 минут)
```bash
# Файлы для изменения:
config/config_futures.yaml          # Основной конфиг
config/manual_pools.yaml            # По-символьные параметры (опционально)
```

### Шаг 2: Обновить parameter_provider.py (если нужно)
```python
# Убедиться, что параметры берутся из конфига, а не hardcoded
# Проверить логику min_holding_seconds в position_manager.py
```

### Шаг 3: Тестировать (30-60 минут)
```bash
python run.py --mode futures    # Запустить на реальных данных или симуляции
# Должны увидеть:
# - Меньше входов (из-за MIN_HOLDING)
# - Больше прибыльных позиций
# - XRP в плюсе
# - SOL исключена
```

### Шаг 4: Мониторить (первая неделя)
```
- Watch win rate: должна ↑ с 10% на 25%+
- Watch total PnL: должен ↑ с -9.79 на 0 и далее в плюс
- Watch SOL exclusion: должна исчезнуть из логов
```

---

## ⚠️ РИСКИ И ОГРАНИЧЕНИЯ

### Риск 1: Слишком большой TP может означать упущенную прибыль
```
Если увеличить TP с 0.05% на 0.25%:
- Хорошо: Больше позиций выйдут в прибыль
- Плохо: Некоторые позиции могли бы выйти раньше с маленьким профитом

Решение: Тестировать TP = 0.15-0.25 и выбрать оптимум
```

### Риск 2: MIN_HOLDING может привести к пропуску быстрых профитов
```
Если установить MIN_HOLDING = 60 сек:
- Хорошо: Избежим шумовых убытков
- Плохо: Если быстрый профит в 5 сек, пропустим его

Решение: Это нормально, так как 1 профит на 100 быстрых позиций
```

### Риск 3: Исключение SOL может означать потерю потенциала
```
SOL дала убыток -5.56, но была 1 выигрышная позиция
Если исключить:
- Хорошо: Избежим больших убытков
- Плохо: Может быть, в будущем SOL выживет

Решение: Можно вернуть SOL позже с отдельными параметрами (SL 0.8%)
```

---

**Рекомендация:** Начните с базовых изменений (TP x5 + MIN_HOLDING 60 сек), затем по-символьная настройка.
