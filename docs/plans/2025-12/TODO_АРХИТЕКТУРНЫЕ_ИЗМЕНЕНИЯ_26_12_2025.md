# 📋 TODO: АРХИТЕКТУРНЫЕ ИЗМЕНЕНИЯ - 26.12.2025

## 🎯 ПРИНЦИПЫ РЕФАКТОРИНГА

1. ✅ **Создавать отдельные модули** - не расширять существующие монстры
2. ✅ **Разделение ответственности** - один модуль = одна задача
3. ✅ **Переносить код из больших файлов** - выносить в отдельные модули
4. ✅ **Объединять связанную логику** - группировать по функциональности
5. ✅ **Минимальные изменения** - только необходимые правки

---

## 📊 АНАЛИЗ ТЕКУЩЕЙ СТРУКТУРЫ

### Большие файлы (требуют рефакторинга):
- `orchestrator.py` - ~4848 строк (координация всего)
- `exit_analyzer.py` - ~3641 строк (анализ закрытия)
- `signal_generator.py` - ~5193 строк (генерация сигналов)
- `position_manager.py` - ~7320 строк (управление позициями)

### Модули, которые можно использовать:
- `coordinators/` - координаторы (уже есть структура)
- `positions/` - управление позициями (уже есть структура)
- `calculations/` - расчеты (уже есть структура)
- `config/` - конфигурация (уже есть структура)

---

## 🔴 ЭТАП 0: КРИТИЧЕСКИЕ ОШИБКИ (30-60 минут)

### TODO 0.1: Исправить datetime ошибку в orchestrator.py
**Файл:** `src/strategies/scalping/futures/orchestrator.py` (строка 2226)

**Изменения:**
- ❌ Убрать локальный импорт `from datetime import datetime, timezone` (строка 2226)
- ✅ Использовать глобальный импорт из строки 18: `from datetime import datetime, timedelta, timezone`
- ✅ Заменить все `datetime.now()` на `datetime.now(timezone.utc)` в функции

**Строки для изменения:**
- Строка 2226: удалить `from datetime import datetime, timezone`
- Строки 2229, 2236, 2243: использовать глобальный `datetime`

**Приоритет:** 🔴 КРИТИЧЕСКИЙ

---

### TODO 0.2: Исправить ConfigManager.get() в exit_analyzer.py
**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py` (строка 1222)

**Изменения:**
- ❌ Убрать `self.config_manager.get("exit_params", {})`
- ✅ Использовать правильные методы ConfigManager
- ✅ Проверить как ConfigManager предоставляет exit_params

**Строки для изменения:**
- Строка 1222: заменить `self.config_manager.get("exit_params", {})` на правильный метод

**Нужно проверить:**
- Как ConfigManager предоставляет доступ к exit_params?
- Есть ли метод `get_exit_params()` или атрибут `exit_params`?

**Приоритет:** 🔴 КРИТИЧЕСКИЙ

---

### TODO 0.3: Исправить получение ATR в exit_analyzer.py
**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py` (строки 730-772)

**Проблема:**
- `_get_tp_percent` - синхронный метод
- `data_registry.get_indicator` - async метод
- Невозможно получить ATR из DataRegistry

**Решение:**
- ✅ Создать новый модуль: `src/strategies/scalping/futures/indicators/atr_provider.py`
- ✅ Синхронный метод для получения ATR из кэша
- ✅ Использовать в `_get_tp_percent`

**Новый файл:**
```
src/strategies/scalping/futures/indicators/atr_provider.py
```

**Изменения:**
- В `exit_analyzer.py`: использовать `ATRProvider.get_atr(symbol)` вместо попыток получить из DataRegistry

**Приоритет:** 🔴 КРИТИЧЕСКИЙ

---

### TODO 0.4: Добавить логирование причин блокировки сигналов
**Файлы:**
- `src/strategies/scalping/futures/signal_generator.py`
- `src/strategies/scalping/futures/coordinators/signal_coordinator.py`

**Изменения:**
- ✅ Добавить логирование на каждом этапе фильтрации
- ✅ Логировать почему сигнал блокируется
- ✅ Добавить счетчик заблокированных сигналов

**Строки для изменения:**
- В `signal_generator.py`: добавить логирование при блокировке по score
- В `signal_coordinator.py`: добавить логирование при блокировке фильтрами

**Приоритет:** 🟡 ВАЖНЫЙ

---

## 🔴 ЭТАП 1: EMERGENCY LOSS PROTECTION (1-2 часа)

### TODO 1.1: Увеличить пороги emergency_loss_protection
**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py`

**Изменения:**
- Строка 1742 (TRENDING): `-2.5` → `-4.0`
- Строка 2136 (RANGING): `-1.5` → `-2.5`
- Строка ~2918 (CHOPPY): `-1.0` → `-2.0` (если есть)

**Приоритет:** 🔴 КРИТИЧЕСКИЙ

---

### TODO 1.2: Добавить минимальное время удержания перед emergency close
**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py`

**Изменения:**
- ✅ Добавить проверку времени удержания ПЕРЕД emergency_loss_protection
- ✅ RANGING: минимум 60 секунд
- ✅ TRENDING: минимум 120 секунд
- ✅ CHOPPY: минимум 30 секунд

**Строки для изменения:**
- В `_generate_exit_for_ranging`: добавить проверку времени перед строкой 2137
- В `_generate_exit_for_trending`: добавить проверку времени перед строкой 1743
- В `_generate_exit_for_choppy`: добавить проверку времени (если есть)

**Приоритет:** 🔴 КРИТИЧЕСКИЙ

---

### TODO 1.3: Учитывать спред и комиссии в emergency_loss_protection
**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py`

**Изменения:**
- ✅ Добавить `spread_buffer` к порогам
- ✅ Добавить `commission_buffer` (0.1%) к порогам
- ✅ Использовать `_get_spread_buffer()` метод

**Строки для изменения:**
- В `_generate_exit_for_ranging`: строка 2136 - добавить spread_buffer и commission_buffer
- В `_generate_exit_for_trending`: строка 1742 - добавить spread_buffer и commission_buffer

**Приоритет:** 🔴 КРИТИЧЕСКИЙ

---

## 🔴 ЭТАП 1.5: КООРДИНАЦИЯ СИСТЕМ ЗАКРЫТИЯ (2-3 часа)

### TODO 1.5.1: Создать ExitDecisionCoordinator
**Новый файл:** `src/strategies/scalping/futures/coordinators/exit_decision_coordinator.py`

**Ответственность:**
- Координация всех систем закрытия (ExitAnalyzer, SmartExitCoordinator, TrailingSLCoordinator)
- Единая матрица приоритетов
- Разрешение конфликтов

**Зависимости:**
- `ExitAnalyzer` (уже есть)
- `SmartExitCoordinator` (уже есть)
- `TrailingSLCoordinator` (уже есть)

**Интерфейс:**
```python
class ExitDecisionCoordinator:
    async def analyze_position(symbol: str) -> Optional[ExitDecision]
    def _resolve_priorities(decisions: List[ExitDecision]) -> ExitDecision
```

**Приоритет:** 🔴 КРИТИЧЕСКИЙ

---

### TODO 1.5.2: Интегрировать ExitDecisionCoordinator в orchestrator
**Файл:** `src/strategies/scalping/futures/orchestrator.py`

**Изменения:**
- ✅ Импортировать `ExitDecisionCoordinator`
- ✅ Создать экземпляр в `__init__`
- ✅ Использовать вместо прямых вызовов ExitAnalyzer, SmartExit, TSL

**Строки для изменения:**
- Добавить импорт
- В `__init__`: создать `self.exit_decision_coordinator`
- В методах управления позициями: использовать координатор

**Приоритет:** 🔴 КРИТИЧЕСКИЙ

---

## 🔴 ЭТАП 2: ОПРЕДЕЛЕНИЕ РЕЖИМА (2-3 часа)

### TODO 2.1: Улучшить систему скоринга режимов
**Файл:** `src/strategies/scalping/futures/adaptivity/regime_manager.py`

**Изменения:**
- ✅ Увеличить вес ADX в скоринге (строка 303: +0.3 → +0.4)
- ✅ Повысить порог TRENDING (строка 302: 20 → 25)
- ✅ Блокировать RANGING при ADX >= 30 (добавить проверку)

**Строки для изменения:**
- Строка 302-303: увеличить вес ADX
- Строка 318: повысить порог RANGING
- Добавить блокировку RANGING при сильном ADX

**Приоритет:** 🔴 КРИТИЧЕСКИЙ

---

### TODO 2.2: Добавить логирование определения режима
**Файл:** `src/strategies/scalping/futures/adaptivity/regime_manager.py`

**Изменения:**
- ✅ Логирование уже есть (строки 335-348)
- ✅ Улучшить формат логирования
- ✅ Добавить логирование при смене режима

**Приоритет:** 🟡 ВАЖНЫЙ

---

## 🔴 ЭТАП 2.5: СТАНДАРТИЗАЦИЯ ИНДИКАТОРОВ (2-3 часа)

### TODO 2.5.1: Создать IndicatorCalculator
**Новый файл:** `src/strategies/scalping/futures/indicators/indicator_calculator.py`

**Ответственность:**
- Единые периоды для всех индикаторов
- Кэширование результатов
- Стандартизация по парам

**Интерфейс:**
```python
class IndicatorCalculator:
    STANDARD_PERIODS = {...}
    async def calculate_all(symbol: str, candles: List[OHLCV]) -> Dict[str, Any]
    def get_cached(symbol: str, indicator_name: str) -> Optional[Any]
```

**Приоритет:** 🟡 ВАЖНЫЙ

---

### TODO 2.5.2: Интегрировать IndicatorCalculator
**Файлы:**
- `src/strategies/scalping/futures/signal_generator.py`
- `src/strategies/scalping/futures/adaptivity/regime_manager.py`

**Изменения:**
- ✅ Использовать `IndicatorCalculator` вместо прямых расчетов
- ✅ Заменить разные периоды на стандартные

**Приоритет:** 🟡 ВАЖНЫЙ

---

## 🔴 ЭТАП 3: ФИЛЬТРАЦИЯ СИГНАЛОВ (2-3 часа)

### TODO 3.1: Блокировать сигналы против ADX тренда
**Файл:** `src/strategies/scalping/futures/signal_generator.py`

**Изменения:**
- ✅ Добавить проверку ADX перед генерацией сигнала
- ✅ Если ADX >= 25 и сигнал против тренда → БЛОКИРОВАТЬ
- ✅ Не снижать strength, а полностью блокировать

**Строки для изменения:**
- В `_generate_base_signals`: добавить проверку ADX перед возвратом сигналов
- Убрать снижение strength для конфликтных сигналов (строки 2351, 2473)

**Приоритет:** 🔴 КРИТИЧЕСКИЙ

---

### TODO 3.2: Убрать снижение порога для конфликтных сигналов
**Файлы:**
- `src/strategies/scalping/futures/signal_generator.py` (строки 2351, 2473)
- `src/strategies/modules/adaptive_regime_manager.py` (строки 643-667)

**Изменения:**
- ❌ Убрать `strength *= conflict_multiplier` (строки 2351, 2473)
- ❌ Убрать `min_strength *= 0.5` для конфликтных сигналов (строка 647)
- ❌ Убрать `min_strength *= 0.5` для RANGING (строка 656)

**Приоритет:** 🔴 КРИТИЧЕСКИЙ

---

### TODO 3.3: Увеличить минимальный strength
**Файл:** `src/strategies/modules/adaptive_regime_manager.py`

**Изменения:**
- ✅ RANGING: 0.5 → 0.7
- ✅ TRENDING: 0.6 → 0.8
- ✅ CHOPPY: 0.4 → 0.6

**Строки для изменения:**
- Найти где устанавливается `min_strength` для каждого режима
- Увеличить значения

**Приоритет:** 🔴 КРИТИЧЕСКИЙ

---

## 🔴 ЭТАП 3.5: ЕДИНАЯ СИСТЕМА НАПРАВЛЕНИЯ (2-3 часа)

### TODO 3.5.1: Создать DirectionAnalyzer
**Новый файл:** `src/strategies/scalping/futures/analysis/direction_analyzer.py`

**Ответственность:**
- Взвешенная система индикаторов для определения направления
- Приоритет ADX
- Блокировка сигналов против направления

**Интерфейс:**
```python
class DirectionAnalyzer:
    INDICATOR_WEIGHTS = {"adx": 0.4, ...}
    def get_direction(symbol: str, indicators: Dict) -> Tuple[str, float]
    def should_block_signal(signal_side: str, market_direction: str) -> bool
```

**Приоритет:** 🟡 ВАЖНЫЙ

---

### TODO 3.5.2: Интегрировать DirectionAnalyzer
**Файл:** `src/strategies/scalping/futures/signal_generator.py`

**Изменения:**
- ✅ Использовать `DirectionAnalyzer` для проверки направления
- ✅ Блокировать сигналы против направления

**Приоритет:** 🟡 ВАЖНЫЙ

---

## 🔴 ЭТАП 4: СИСТЕМА ПРИОРИТЕТОВ (3-4 часа)

### TODO 4.1: Создать PriorityResolver
**Новый файл:** `src/strategies/scalping/futures/coordinators/priority_resolver.py`

**Ответственность:**
- Единая матрица приоритетов для всех решений о закрытии
- Разрешение конфликтов между решениями

**Интерфейс:**
```python
class PriorityResolver:
    PRIORITY_MATRIX = {...}
    def resolve_conflicts(decisions: List[ExitDecision]) -> ExitDecision
    def _get_priority(reason: str) -> int
```

**Приоритет:** 🟡 ВАЖНЫЙ

---

### TODO 4.2: Интегрировать PriorityResolver в ExitDecisionCoordinator
**Файл:** `src/strategies/scalping/futures/coordinators/exit_decision_coordinator.py`

**Изменения:**
- ✅ Использовать `PriorityResolver` для разрешения конфликтов

**Приоритет:** 🟡 ВАЖНЫЙ

---

## 🔴 ЭТАП 5: ПАРАМЕТРЫ (4-5 часов)

### TODO 5.1: Создать ParameterProvider
**Новый файл:** `src/strategies/scalping/futures/config/parameter_provider.py`

**Ответственность:**
- Единая точка получения параметров для пары и режима
- Приоритет: symbol+regime → symbol → regime → global

**Интерфейс:**
```python
class ParameterProvider:
    def get_parameters(symbol: str, regime: str) -> Dict[str, Any]
    def get_sl_percent(symbol: str, regime: str) -> float
    def get_tp_percent(symbol: str, regime: str) -> float
    def get_filter_params(symbol: str, regime: str) -> Dict[str, Any]
```

**Приоритет:** 🟡 ВАЖНЫЙ

---

### TODO 5.2: Интегрировать ParameterProvider
**Файлы:**
- Все модули, которые используют параметры

**Изменения:**
- ✅ Использовать `ParameterProvider` вместо прямых обращений к ConfigManager
- ✅ Унифицировать получение параметров

**Приоритет:** 🟡 ВАЖНЫЙ

---

## 🔴 ЭТАП 6: SL/TP С ATR (2-3 часа)

### TODO 6.1: Использовать ATR для расчета SL
**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py`

**Изменения:**
- ✅ В `_get_sl_percent`: использовать ATR если доступен
- ✅ SL = max(base_sl, atr_percent * multiplier)
- ✅ Ограничить минимальным (0.8%) и максимальным (3.0%)

**Строки для изменения:**
- Метод `_get_sl_percent`: добавить расчет на основе ATR

**Приоритет:** 🟡 ВАЖНЫЙ

---

### TODO 6.2: Адаптировать TP к волатильности
**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py`

**Изменения:**
- ✅ В `_get_tp_percent`: использовать ATR если доступен
- ✅ TP = SL * risk_reward_ratio
- ✅ Ограничить минимальным (1.5%) и максимальным (8.0%)

**Строки для изменения:**
- Метод `_get_tp_percent`: улучшить расчет на основе ATR (уже есть частично)

**Приоритет:** 🟡 ВАЖНЫЙ

---

## 🔴 ЭТАП 7: МЕТРИКИ И МОНИТОРИНГ (1-2 часа)

### TODO 7.1: Создать ConversionMetrics
**Новый файл:** `src/strategies/scalping/futures/metrics/conversion_metrics.py`

**Ответственность:**
- Счетчик сигналов → позиций
- Счетчик позиций → TP/SL
- Статистика конверсии

**Интерфейс:**
```python
class ConversionMetrics:
    def record_signal(symbol: str, side: str, strength: float)
    def record_position_opened(symbol: str, signal_id: str)
    def record_position_closed(symbol: str, reason: str, pnl: float)
    def get_conversion_rate() -> Dict[str, float]
```

**Приоритет:** 🟢 НИЗКИЙ

---

### TODO 7.2: Создать HoldingTimeMetrics
**Новый файл:** `src/strategies/scalping/futures/metrics/holding_time_metrics.py`

**Ответственность:**
- Среднее время до TP
- Среднее время до SL
- Распределение времени удержания

**Приоритет:** 🟢 НИЗКИЙ

---

### TODO 7.3: Создать AlertManager
**Новый файл:** `src/strategies/scalping/futures/metrics/alert_manager.py`

**Ответственность:**
- Алерты на критические события
- Мониторинг показателей

**Приоритет:** 🟢 НИЗКИЙ

---

## 📊 СВОДКА АРХИТЕКТУРНЫХ ИЗМЕНЕНИЙ

### Новые файлы (создать):
1. `src/strategies/scalping/futures/indicators/atr_provider.py` - синхронный доступ к ATR
2. `src/strategies/scalping/futures/coordinators/exit_decision_coordinator.py` - координация закрытия
3. `src/strategies/scalping/futures/coordinators/priority_resolver.py` - разрешение конфликтов
4. `src/strategies/scalping/futures/analysis/direction_analyzer.py` - определение направления
5. `src/strategies/scalping/futures/indicators/indicator_calculator.py` - стандартизация индикаторов
6. `src/strategies/scalping/futures/config/parameter_provider.py` - единая точка параметров
7. `src/strategies/scalping/futures/metrics/conversion_metrics.py` - метрики конверсии
8. `src/strategies/scalping/futures/metrics/holding_time_metrics.py` - метрики времени
9. `src/strategies/scalping/futures/metrics/alert_manager.py` - алерты

### Файлы для изменения (минимальные правки):
1. `orchestrator.py` - исправить datetime, интегрировать новые модули
2. `exit_analyzer.py` - исправить ConfigManager.get(), ATR, emergency пороги
3. `regime_manager.py` - улучшить скоринг, блокировать RANGING
4. `signal_generator.py` - блокировать сигналы против ADX, убрать снижение strength
5. `adaptive_regime_manager.py` - убрать снижение порога, увеличить min_strength

### Принципы:
- ✅ Каждый новый модуль в отдельном файле
- ✅ Минимальные изменения в существующих файлах
- ✅ Четкое разделение ответственности
- ✅ Переиспользование существующей структуры

---

**Дата создания:** 26.12.2025  
**Статус:** ⏳ ОЖИДАЕТ ПОДТВЕРЖДЕНИЯ



