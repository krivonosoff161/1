# 📋 СИСТЕМА ПРИОРИТЕТОВ ЗАКРЫТИЯ ПОЗИЦИЙ

**Дата:** 23.11.2025, 23:45  
**Статус:** Анализ текущей логики и предложение улучшений

---

## 🎯 ТЕКУЩАЯ СИСТЕМА ЗАКРЫТИЯ ПОЗИЦИЙ

### Порядок проверки (приоритет):

1. **Profit Harvesting (PH)** - быстрое закрытие при высокой прибыли за короткое время
2. **Big Profit Exit** - закрытие с трейлингом при достижении порога (например, 2.5% для ranging)
3. **Partial TP** - закрытие части позиции (например, 60%) при достижении trigger
4. **Full TP** - закрытие всей позиции при достижении TP (например, 4.0% для ranging)
5. **TSL (Trailing Stop Loss)** - динамический стоп-лосс (следует за ценой)
6. **loss_cut** - принудительное закрытие при убытке (адаптивный: 1.2-4.0%)
7. **Timeout** - закрытие по времени (для убыточных отключено в коде!)

**Текущие параметры (ranging):**
- `big_profit_exit_percent: 2.5%` - порог для big profit exit
- `tp_percent: 4.0%` - Take Profit
- `loss_cut_percent: 4.0%` - принудительное закрытие при убытке
- `min_holding_minutes: 60 мин` - защита от преждевременного закрытия
- `sl_percent: 1.2%` - **НЕ используется для закрытия!**

---

## 🔍 ПРОБЛЕМА

### 1. SL не используется для закрытия

**Текущая ситуация:**
- `sl_percent: 1.2%` существует в конфиге
- Используется **ТОЛЬКО** для расчета размера позиции в `risk_manager.py`
- **НЕ используется** для закрытия позиций

### 2. Конфликт между SL и loss_cut

**Проблема:**
- Если добавить SL=1.2% для закрытия, позиции будут закрываться слишком рано
- Сейчас loss_cut=4.0% дает шанс на разворот (позиции могут восстановиться)
- Если SL=1.2% будет закрывать позиции, не будет шанса на разворот

**Пример (ranging режим):**
- Позиция уходит в -1.5% через 5 минут
- **Сейчас:** Не закрывается (loss_cut=4.0%, min_holding=60 мин) - есть шанс на разворот ✅
- **С SL=1.2%:** Закроется сразу при -1.2% - нет шанса на разворот ❌

### 3. Big Profit Exit играет важную роль

**Текущая логика:**
- Big Profit Exit проверяется **ДО** Full TP
- Закрывает позицию с трейлингом при достижении порога (2.5% для ranging)
- Используется для фиксации прибыли раньше полного TP (4.0%)

**Пример:**
- Позиция достигает 2.5% (big_profit_exit_percent)
- Больше не растет, проседает на 30% от максимума
- Закрывается по Big Profit Exit с трейлингом ✅
- Если бы не было Big Profit Exit, позиция могла бы упасть обратно к TP или ниже

---

## ✅ РЕШЕНИЕ: МНОГОУРОВНЕВАЯ СИСТЕМА ЗАКРЫТИЯ

### Уровень 1: Быстрое закрытие прибыли (высокий приоритет)

1. **Profit Harvesting (PH)** - быстрое закрытие при высокой прибыли за короткое время
2. **Big Profit Exit** - закрытие с трейлингом при достижении порога
3. **Partial TP** - закрытие части позиции

**Логика:** Закрываем прибыль быстро и агрессивно

---

### Уровень 2: Полное закрытие прибыли (средний приоритет)

4. **Full TP** - закрытие всей позиции при достижении TP

**Логика:** Полная фиксация прибыли

---

### Уровень 3: Защита от убытков (низкий приоритет, но с гибкостью)

5. **TSL (Trailing Stop Loss)** - динамический стоп-лосс (если активен)

**Логика:** Защита прибыли и ограничение убытков

6. **Адаптивный SL** - жесткий стоп-лосс (НО с защитой min_holding!)

**НОВАЯ ЛОГИКА:**
```python
# Адаптивный SL работает ТОЛЬКО если:
# 1. Прошло min_holding_minutes (защита от преждевременного закрытия)
# 2. Убыток >= sl_percent (жесткий стоп)
# 3. TSL не активен или не сработал

if pnl_percent <= -sl_percent:
    if minutes_in_position >= min_holding_minutes:
        # Закрываем по SL
        await close_position("sl")
    else:
        # Защита min_holding активна - НЕ закрываем
        logger.debug(f"SL заблокирован: min_holding защита активна")
```

7. **loss_cut** - мягкий стоп-лосс (для ranging = 4.0%, дает шанс на разворот)

**Логика:** 
- **SL** = жесткий стоп (быстрое закрытие при убытке)
- **loss_cut** = мягкий стоп (дает время на разворот)

**Приоритет:**
- Если **SL сработал** (после min_holding) - закрываем сразу
- Если **loss_cut сработал** (но SL еще не сработал или заблокирован) - закрываем принудительно

---

## 🎯 ПРЕДЛОЖЕННАЯ СИСТЕМА

### Для ПРИБЫЛЬНЫХ позиций:

1. **Profit Harvesting** - самое быстрое закрытие
2. **Big Profit Exit** - закрытие с трейлингом
3. **Partial TP** - закрытие части
4. **Full TP** - закрытие всей позиции
5. **TSL** - защита прибыли

### Для УБЫТОЧНЫХ позиций:

**Для новых позиций (< min_holding):**
- **НЕ закрываем** по SL (защита min_holding)
- **НЕ закрываем** по loss_cut (защита min_holding)
- **Закрываем** только по TSL (если активен)

**Для старых позиций (>= min_holding):**

**Вариант A (строгий):**
1. **TSL** - если активен и сработал
2. **SL** - если убыток >= sl_percent (быстрое закрытие)
3. **loss_cut** - если убыток >= loss_cut_percent (мягкое закрытие)

**Вариант B (мягкий, с шансом на разворот):**
1. **TSL** - если активен и сработал
2. **loss_cut** - если убыток >= loss_cut_percent (мягкое закрытие, дает время)
3. **SL** - если убыток >= sl_percent И прошло дополнительное время (например, min_holding * 1.5)

---

## 📊 ПАРАМЕТРЫ ДЛЯ RANGING РЕЖИМА

### Текущие (проблемные):

| Параметр | Значение | Проблема |
|----------|----------|----------|
| `big_profit_exit_percent` | 2.5% | ✅ ОК |
| `tp_percent` | 4.0% | ✅ ОК |
| `loss_cut_percent` | 4.0% | ⚠️ Слишком высоко |
| `min_holding_minutes` | 60 мин | ⚠️ Слишком долго |
| `sl_percent` | 1.2% | ❌ Не используется |

### Предложенные (сбалансированные):

| Параметр | Значение | Логика |
|----------|----------|--------|
| `big_profit_exit_percent` | 2.5% | ✅ Оставляем |
| `tp_percent` | 4.0% | ✅ Оставляем |
| `sl_percent` | **2.0%** | ✅ Адаптивный, жесткий стоп |
| `loss_cut_percent` | **3.0%** | ✅ Мягкий стоп (вместо 4.0%) |
| `min_holding_minutes` | **20 мин** | ✅ Разумное время (вместо 60 мин) |

**Логика работы:**

1. **0-20 минут (min_holding защита):**
   - Не закрываем по SL (защита)
   - Не закрываем по loss_cut (защита)
   - Закрываем только по TSL (если активен) или по прибыли (TP, Big Profit)

2. **20-30 минут (первые 10 минут после min_holding):**
   - Закрываем по SL если убыток >= 2.0% (жесткий стоп)
   - Закрываем по loss_cut если убыток >= 3.0% (мягкий стоп)
   - Закрываем по TSL (если активен)

3. **30+ минут:**
   - Закрываем по SL если убыток >= 2.0% (быстрое закрытие)
   - Закрываем по loss_cut если убыток >= 3.0% (принудительное закрытие)
   - Закрываем по TSL (если активен)

---

## 🎯 ИТОГОВАЯ ЛОГИКА

### Приоритеты закрытия:

**Для ПРИБЫЛЬНЫХ:**
1. Profit Harvesting
2. Big Profit Exit (с трейлингом)
3. Partial TP
4. Full TP
5. TSL

**Для УБЫТОЧНЫХ:**

1. **TSL** (если активен) - самый приоритетный
2. **SL** (после min_holding) - жесткий стоп
3. **loss_cut** (после min_holding) - мягкий стоп

**Защита min_holding:**
- Блокирует SL и loss_cut до истечения min_holding
- НЕ блокирует TSL (если активен)
- НЕ блокирует закрытие по прибыли (TP, Big Profit)

---

## 📊 ОЖИДАЕМЫЙ ЭФФЕКТ

**До исправления (ranging):**
- Позиции в -1.5% не закрываются 60 минут
- Позиции закрываются только при -4.0% loss_cut
- Средний убыток: -2.0% до -4.0%
- Нет шанса закрыть раньше по SL

**После исправления (ranging):**
- Позиции в -2.0% закрываются после 20 минут (SL)
- Позиции в -3.0% закрываются после 20 минут (loss_cut)
- Средний убыток: -1.5% до -2.5%
- Есть защита min_holding (20 мин) - дает шанс на разворот
- Есть шанс на разворот в первые 20 минут

**Улучшение:**
- Снижение среднего убытка с -2.5% до -2.0%
- Снижение времени в минусе с 30-60 мин до 20-30 мин
- Сохранение шанса на разворот (первые 20 минут)
- Улучшение Risk/Reward: 2.0% SL / 4.0% TP = 1:2

---

**Дата:** 23.11.2025, 23:45

