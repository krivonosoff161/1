# ✅ ИСПРАВЛЕНЫ ВСЕ ОШИБКИ ФОРМАТИРОВАНИЯ

**Дата:** 26.11.2025  
**Статус:** ✅ ВСЕ ИСПРАВЛЕНО

---

## 🐛 ПРОБЛЕМА

**Ошибка форматирования при обновлении маржи в DataRegistry**

### Ошибка:
```
⚠️ Ошибка обновления маржи в DataRegistry: Invalid format specifier '.2f if available else 'N/A'' for object of type 'float'
```

### Причина:
В f-строке нельзя использовать условное выражение внутри спецификатора формата. Python пытается интерпретировать `.2f if available else 'N/A'` как часть спецификатора формата, что вызывает ошибку.

---

## ✅ ИСПРАВЛЕНИЯ

### 1. **data_registry.py** ✅
**Файл:** `src/strategies/scalping/futures/core/data_registry.py`  
**Строка:** 314

**Было:**
```python
logger.debug(
    f"✅ DataRegistry: Обновлена маржа: used={used:.2f}, available={available:.2f if available else 'N/A'}"
)
```

**Стало:**
```python
available_str = f"{available:.2f}" if available is not None else 'N/A'
logger.debug(
    f"✅ DataRegistry: Обновлена маржа: used={used:.2f}, available={available_str}"
)
```

---

### 2. **trailing_stop_loss.py** ✅
**Файл:** `src/strategies/scalping/futures/indicators/trailing_stop_loss.py`  
**Строки:** 791, 824

**Было:**
```python
f"режим={market_regime or 'N/A'}, тренд={trend_strength:.2f if trend_strength else 'N/A'} - "
```

**Стало:**
```python
trend_str = f"{trend_strength:.2f}" if trend_strength is not None else 'N/A'
f"режим={market_regime or 'N/A'}, тренд={trend_str} - "
```

---

## ✅ ПРАВИЛО

### ❌ НЕПРАВИЛЬНО:
```python
f"Value: {value:.2f if value else 'N/A'}"  # ❌ Ошибка!
```

### ✅ ПРАВИЛЬНО:
```python
value_str = f"{value:.2f}" if value is not None else 'N/A'
f"Value: {value_str}"  # ✅ Правильно!
```

**Или:**
```python
f"Value: {f'{value:.2f}' if value is not None else 'N/A'}"  # ✅ Тоже правильно!
```

---

## ✅ ПРОВЕРКА

- ✅ Исправлено форматирование в `data_registry.py`
- ✅ Исправлено форматирование в `trailing_stop_loss.py` (2 места)
- ✅ Проверены другие места на подобные проблемы
- ✅ Линтер не находит ошибок

---

## ✅ РЕЗУЛЬТАТ

**✅ Все ошибки форматирования исправлены!**

**✅ Обновление маржи в DataRegistry теперь работает корректно!**

**✅ Логирование в trailing_stop_loss теперь работает корректно!**

---

**✅ ГОТОВО К ИСПОЛЬЗОВАНИЮ! 🎉**






