# 🔍 ПРОБЛЕМА XRP-USDT: ПОСТОЯННЫЕ БЛОКИРОВКИ

**Дата:** 23 ноября 2025, 02:15  
**Проблема:** XRP-USDT постоянно блокируется LiquidityFilter, хотя график лучше чем у BTC/ETH

---

## 🔍 НАЙДЕННАЯ ПРОБЛЕМА

### ⚠️ LiquidityFilter блокирует все сигналы XRP-USDT

**Из логов:**
```
LiquidityFilter: XRP-USDT отклонён — объём на лучшем bid 0 < 12 (regime=ranging)
LiquidityFilter: XRP-USDT отклонён — объём на лучшем bid 0 < 3 (regime=ranging, fail-open x0.25)
```

**Проблема:**
- Объем на лучшем bid = **0** (или очень маленький)
- Порог для XRP-USDT в ranging: `min_best_bid_volume_usd: 12`
- Даже с fail-open (ослабление x0.25) все равно блокируется: `0 < 3`

---

## 📊 СРАВНЕНИЕ ПАРАМЕТРОВ

### Liquidity Filter параметры для RANGING режима:

| Символ | min_best_bid_volume_usd | min_best_ask_volume_usd | min_orderbook_depth_usd |
|--------|------------------------|-------------------------|------------------------|
| **BTC-USDT** | 112 (symbol override) | 112 (symbol override) | 1000 (symbol override) |
| **ETH-USDT** | 72 (symbol override) | 72 (symbol override) | 600 (symbol override) |
| **SOL-USDT** | 40 (ranging) | 40 (ranging) | 340 (ranging) |
| **DOGE-USDT** | 10 (ranging) | 10 (ranging) | 180 (ranging) |
| **XRP-USDT** | **12 (ranging)** | **12 (ranging)** | **192 (ranging)** |

### Проблема:
- **XRP-USDT имеет пороги выше чем DOGE-USDT** (12 vs 10)
- Но **объем на бирже может быть меньше** чем у DOGE
- Пороги для XRP-USDT **не адаптированы** под реальную ликвидность

---

## 🔧 РЕШЕНИЕ

### 1. Снизить пороги Liquidity Filter для XRP-USDT

**Текущие параметры (ranging):**
```yaml
filters:
  liquidity:
    min_best_bid_volume_usd: 12  # Слишком высоко!
    min_best_ask_volume_usd: 12  # Слишком высоко!
    min_orderbook_depth_usd: 192
```

**Рекомендуемые параметры:**
```yaml
filters:
  liquidity:
    min_best_bid_volume_usd: 5   # Снизить до 5 (как у DOGE в choppy)
    min_best_ask_volume_usd: 5   # Снизить до 5
    min_orderbook_depth_usd: 128 # Снизить до 128 (как у DOGE в choppy)
```

### 2. Проверить адаптивность параметров

**Проблема:** Возможно, параметры из `symbol_profiles[symbol][regime].filters.liquidity` не применяются правильно.

**Нужно проверить:**
- Правильно ли передаются параметры в `LiquidityFilter.evaluate()`
- Используются ли `thresholds_override` из конфига
- Применяются ли `regime_multipliers` для ranging режима

---

## 📋 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Снизить пороги Liquidity Filter для XRP-USDT в ranging режиме
2. ✅ Проверить, правильно ли применяются адаптивные параметры
3. ✅ Сравнить реальные объемы на бирже для XRP-USDT с другими символами
4. ✅ Возможно, добавить symbol_overrides для XRP-USDT в liquidity_filter

---

**Статус:** ⚠️ **ТРЕБУЕТСЯ ИСПРАВЛЕНИЕ ПАРАМЕТРОВ**


