# 🔍 Анализ системы score (ОБНОВЛЕНО)

## ✅ НАЙДЕНО: Система score существует, но работает по-другому!

### Что нашел:

1. **Строка 4840 в `signal_generator.py`:**
   ```python
   if current_time - last_signal_time < 60:  # Минимум 60 сек
       logger.debug(
           f"🔍 Сигнал для {symbol} отфильтрован по времени: "
           f"прошло {current_time - last_signal_time:.1f}с < 60с"
       )
   ```
   **Это НЕ score!** Это проверка времени между сигналами (cooldown 60 секунд).

2. **Строка 741 в `regime_manager.py`:**
   ```python
   base_min_strength = regime_params.min_score_threshold / 12.0
   ```
   **КЛЮЧЕВОЕ ОТКРЫТИЕ:** `min_score_threshold` делится на **12.0**, что означает:
   - Максимум score = **12**, а не 60!
   - `min_score_threshold` в конфиге (например, 1.4) означает минимальный score из 12
   - `1.4 / 12.0 = 0.117` → минимальная strength = 11.7%

3. **Строка 4903-4904 в `signal_generator.py`:**
   ```python
   filtered_signals = [
       s for s in signals if s.get("strength", 0) >= min_strength
   ]
   ```
   Проверяется `strength` (0-1), а не `score` (0-12).

---

## 🔍 Что означает лог "BTC-USDT 2.1 60"?

**Вероятно, это НЕ score!** Возможные варианты:

1. **Время между сигналами:**
   - `2.1` = прошло 2.1 секунды с последнего сигнала
   - `60` = минимум 60 секунд требуется
   - Сигнал блокируется из-за cooldown

2. **Другой формат лога (старая версия):**
   - Возможно, формат лога изменился
   - Или это лог из другого места

3. **TTL кэша:**
   - `2.1` = возраст данных
   - `60` = TTL (time to live)

---

## ✅ РЕАЛЬНАЯ СИСТЕМА SCORE:

### Как работает:

1. **Score считается где-то в генерации сигналов:**
   - Максимум = **12 баллов** (не 60!)
   - Бонусы от фильтров добавляются к score

2. **Score конвертируется в strength:**
   - `strength = score / 12.0` (0-1 диапазон)

3. **min_score_threshold из конфига:**
   - `min_score_threshold: 1.4` → минимальный score = 1.4 из 12
   - Конвертируется в `min_strength = 1.4 / 12.0 = 0.117` (11.7%)

4. **Проверка:**
   - `if strength >= min_strength` → сигнал проходит
   - `if strength < min_strength` → сигнал блокируется

---

## ⚠️ ПРОБЛЕМА:

**Если `min_score_threshold: 1.4` в конфиге:**
- Минимальный score = 1.4 из 12
- Минимальная strength = 0.117 (11.7%)

**Но сигналы имеют:**
- Score 2.1 → strength = 2.1/12 = 0.175 (17.5%) ✅ Проходит
- Score 9.8 → strength = 9.8/12 = 0.817 (81.7%) ✅ Проходит
- Score 29.0 → strength = 29.0/12 = 2.417 (241.7%) ❌ Невозможно!

**Вывод:** Score не может быть больше 12! Значит, лог "29.0 60" - это НЕ score.

---

## 🔧 РЕКОМЕНДАЦИИ:

✅ **ПОЛНЫЙ АНАЛИЗ ВЫПОЛНЕН!** См. файл `ПОЛНЫЙ_АНАЛИЗ_СИСТЕМЫ_SCORE.md`

### Краткие выводы:

1. **Лог "BTC-USDT 2.1 60"** - это **НЕ score**, а время между сигналами (cooldown 60 секунд)
2. **Система score** имеет максимум **12**, а не 60
3. **Score не используется напрямую** - используется `strength` (0-1)
4. **Бонусы не добавляются** к strength (код не найден)
5. **min_score_threshold** конвертируется: `min_strength = min_score_threshold / 12.0`

3. **Исправить min_score_threshold:**
   - Если нужен порог 25% от максимума → `min_score_threshold: 3.0` (3.0/12 = 25%)
   - Если нужен порог 30% → `min_score_threshold: 3.6` (3.6/12 = 30%)

---

## 📊 ТАБЛИЦА СООТВЕТСТВИЯ:

| min_score_threshold | min_strength | % от максимума |
|---------------------|--------------|----------------|
| 1.4                 | 0.117        | 11.7%          |
| 2.0                 | 0.167        | 16.7%          |
| 3.0                 | 0.250        | 25.0%          |
| 3.6                 | 0.300        | 30.0%          |
| 4.8                 | 0.400        | 40.0%          |
| 6.0                 | 0.500        | 50.0%          |

---

**Дата:** 23 декабря 2025  
**Статус:** ✅ **НАЙДЕНО - система score с максимумом 12, не 60!**

