# 📊 АНАЛИЗ ЛОГОВ - ЧИСТЫЙ ЗАПУСК 02.12.2025

## ✅ Что работает правильно:

### 1. **Расчет цены лимитного ордера** ✅
- **SOL-USDT**: offset=0.120%, разница=0.00% ✅
- **XRP-USDT**: offset=0.180%, разница=0.00% ✅
- Режим определяется правильно: `ranging` ✅
- Offset читается из конфига правильно (per-symbol+regime) ✅
- Нет ошибок о слишком большом offset ✅

### 2. **Определение режима рынка** ✅
- Режим определяется как `ranging` для всех символов ✅
- Режим передается в сигнал правильно ✅
- Используются правильные параметры по режиму ✅

### 3. **Логирование** ✅
- Детальное логирование расчета цены работает ✅
- Показывает offset, режим, разницу в % ✅

## ❌ Найденные проблемы:

### 1. **Размер ордера для BTC-USDT меньше минимума** ❌

**Проблема:**
- Размер позиции рассчитывается: `0.001800` монет (BTC)
- Для BTC-USDT: `ctVal=0.01` (1 контракт = 0.01 BTC)
- Конвертация: `0.001800 / 0.01 = 0.18` контрактов
- Минимум OKX: `0.01` контрактов
- **НО**: Проверка в `order_executor.py:907` происходит ДО конвертации!

**Ошибка в логах:**
```
❌ Размер ордера 0.001800 меньше минимального 0.01 для BTC-USDT
```

**Причина:**
- В `order_executor.py:_place_limit_order()` проверка `size < 0.01` происходит ДО конвертации в контракты
- Размер приходит в монетах (0.001800 BTC), но проверяется как контракты (0.01)
- Нужно конвертировать размер в контракты ПЕРЕД проверкой

**Исправление:**
Нужно добавить конвертацию размера из монет в контракты ПЕРЕД проверкой минимального размера в `order_executor.py:_place_limit_order()` и `_place_market_order()`.

## 📋 Статистика:

- **Всего ордеров**: 3 попытки для BTC-USDT (все отклонены)
- **Успешные ордера**: SOL-USDT, XRP-USDT (размеры правильные)
- **Ошибки**: 3 ошибки минимального размера для BTC-USDT

## 🔧 Рекомендации:

1. **Исправить проверку минимального размера** - конвертировать размер в контракты ПЕРЕД проверкой
2. **Улучшить логирование** - показывать размер и в монетах, и в контрактах
3. **Проверить расчет размера** - возможно, для BTC нужно увеличить минимальный размер позиции














