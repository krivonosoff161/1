# ✅ Проверка и исправление логики цены закрытия и Profit Drawdown (29.11.2025)

## 🔍 Проверка для всех пар и направлений

**Проверены:**
- **Пары:** BTC-USDT, ETH-USDT, SOL-USDT, DOGE-USDT, XRP-USDT
- **Направления:** LONG, SHORT
- **Механизмы:** Закрытие позиций, Profit Drawdown

---

## ❌ Обнаруженные проблемы

### 1. **Проблема с ценой закрытия (для всех пар и направлений)**

**Проблема:**
- При закрытии использовался `best_bid` (для LONG) или `best_ask` (для SHORT) **без проверки актуальности**
- `best_bid`/`best_ask` могут быть устаревшими (как при открытии)
- Это привело к закрытию SOL-USDT по цене $136.74 вместо $138.35 (разница 1.16%)

**Пример (SOL-USDT LONG):**
```
⚠️ Большая разница между актуальной ценой (136.7400) и markPx (138.3500) для SOL-USDT: 1.16%
```

**Влияние:**
- LONG позиции могут закрываться по заниженной цене (best_bid устарел)
- SHORT позиции могут закрываться по завышенной цене (best_ask устарел)
- Это приводит к убыткам вместо прибыли

---

### 2. **Проблема с Profit Drawdown (для всех пар и направлений)**

**Проблема:**
- В `_check_profit_drawdown` использовался `markPx` для расчета текущего PnL
- `markPx` может быть устаревшим, что приводит к неправильному расчету отката
- Это привело к тому, что Profit Drawdown для SOL сработал только при 98.8% откате вместо 30%

**Пример (SOL-USDT LONG):**
```
Peak profit: $0.2824 USDT (в 15:09:08)
Current profit: $0.0034 USDT (в 18:11:20)
Drawdown: 98.8% (threshold: 30.0%)
```

**Влияние:**
- Profit Drawdown срабатывает слишком поздно
- Позиции держатся дольше, чем нужно
- Упускается возможность фиксации прибыли

---

## ✅ Исправления

### 1. **Исправлена логика получения цены при закрытии**

**Файл:** `src/strategies/scalping/futures/position_manager.py` (строка 3067-3120)

**Изменения:**
- ✅ Добавлена проверка актуальности `best_bid`/`best_ask` перед использованием
- ✅ Проверяется разница с `current_price` из стакана
- ✅ Если разница > 0.5%, используется `current_price` с небольшим offset
- ✅ Для LONG: используется `current_price * 0.9995` (-0.05%) если `best_bid` устарел
- ✅ Для SHORT: используется `current_price * 1.0005` (+0.05%) если `best_ask` устарел
- ✅ Добавлено детальное логирование для диагностики

**Код:**
```python
# ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Проверяем актуальность best_bid/best_ask перед использованием
if side.lower() == "long":
    if best_bid > 0 and current_price_from_book > 0:
        spread_bid_pct = abs(best_bid - current_price_from_book) / current_price_from_book
        if spread_bid_pct < 0.005:  # Разница < 0.5% - best_bid актуален
            exit_price = best_bid
        else:
            # best_bid устарел, используем current_price с offset
            exit_price = current_price_from_book * 0.9995
            logger.warning(f"⚠️ best_bid устарел для {symbol} LONG...")
else:  # short
    if best_ask > 0 and current_price_from_book > 0:
        spread_ask_pct = abs(best_ask - current_price_from_book) / current_price_from_book
        if spread_ask_pct < 0.005:  # Разница < 0.5% - best_ask актуален
            exit_price = best_ask
        else:
            # best_ask устарел, используем current_price с offset
            exit_price = current_price_from_book * 1.0005
            logger.warning(f"⚠️ best_ask устарел для {symbol} SHORT...")
```

**Применяется к:**
- ✅ BTC-USDT LONG/SHORT
- ✅ ETH-USDT LONG/SHORT
- ✅ SOL-USDT LONG/SHORT
- ✅ DOGE-USDT LONG/SHORT
- ✅ XRP-USDT LONG/SHORT

---

### 2. **Исправлена логика Profit Drawdown**

**Файл:** `src/strategies/scalping/futures/position_manager.py` (строка 3637-3690)

**Изменения:**
- ✅ Добавлено получение актуальной цены из стакана вместо использования `markPx`
- ✅ Проверяется актуальность `markPx` перед использованием
- ✅ Если разница > 0.5%, используется `current_price` из стакана
- ✅ Добавлено детальное логирование для диагностики

**Код:**
```python
# ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Получаем актуальную цену из стакана вместо markPx
current_price = float(position.get("markPx", "0"))  # Fallback на markPx

try:
    price_limits = await self.client.get_price_limits(symbol)
    current_price_from_book = price_limits.get("current_price", 0.0)
    
    if current_price_from_book > 0:
        mark_px = float(position.get("markPx", "0"))
        if mark_px > 0:
            spread_pct = abs(mark_px - current_price_from_book) / current_price_from_book
            if spread_pct < 0.005:  # Разница < 0.5% - markPx актуален
                current_price = mark_px
            else:
                # markPx устарел, используем current_price из стакана
                current_price = current_price_from_book
                logger.warning(f"⚠️ markPx устарел для {symbol}...")
```

**Применяется к:**
- ✅ BTC-USDT LONG/SHORT
- ✅ ETH-USDT LONG/SHORT
- ✅ SOL-USDT LONG/SHORT
- ✅ DOGE-USDT LONG/SHORT
- ✅ XRP-USDT LONG/SHORT

---

## 📊 Ожидаемые улучшения

### 1. **Цена закрытия:**
- ✅ Более точная цена закрытия для всех пар и направлений
- ✅ Меньше убытков из-за устаревших цен
- ✅ Лучшее исполнение ордеров на закрытие

### 2. **Profit Drawdown:**
- ✅ Более точный расчет текущего PnL
- ✅ Более своевременное срабатывание (при 30% вместо 98.8%)
- ✅ Лучшая защита прибыли для всех пар

---

## 🔍 Детальное логирование

### При закрытии:
```
✅ Актуальная цена закрытия для {symbol} LONG: best_bid={best_bid:.4f} 
   (current_price={current_price:.4f}, spread={spread_bid_pct:.3f}%, markPx={markPx})
```

### При Profit Drawdown:
```
⚠️ markPx устарел для {symbol}, используем current_price из стакана 
   (markPx={mark_px:.4f}, current_price={current_price_from_book:.4f}, spread={spread_pct:.2f}%)
```

---

## ✅ Проверка для всех пар и направлений

### LONG позиции:
- ✅ BTC-USDT LONG: проверка актуальности `best_bid` при закрытии
- ✅ ETH-USDT LONG: проверка актуальности `best_bid` при закрытии
- ✅ SOL-USDT LONG: проверка актуальности `best_bid` при закрытии
- ✅ DOGE-USDT LONG: проверка актуальности `best_bid` при закрытии
- ✅ XRP-USDT LONG: проверка актуальности `best_bid` при закрытии

### SHORT позиции:
- ✅ BTC-USDT SHORT: проверка актуальности `best_ask` при закрытии
- ✅ ETH-USDT SHORT: проверка актуальности `best_ask` при закрытии
- ✅ SOL-USDT SHORT: проверка актуальности `best_ask` при закрытии
- ✅ DOGE-USDT SHORT: проверка актуальности `best_ask` при закрытии
- ✅ XRP-USDT SHORT: проверка актуальности `best_ask` при закрытии

### Profit Drawdown:
- ✅ Все пары (BTC, ETH, SOL, DOGE, XRP): использование актуальной цены из стакана
- ✅ Все направления (LONG, SHORT): правильный расчет PnL с актуальной ценой

---

## 📝 Примечания

1. **Порог проверки актуальности:** 0.5% (как при открытии)
2. **Offset для закрытия:** ±0.05% от `current_price` для гарантии исполнения
3. **Fallback:** Если стакан недоступен, используется `markPx` (с предупреждением)

---

## ✅ Статус

- ✅ Исправлена логика получения цены при закрытии для всех пар и направлений
- ✅ Исправлена логика Profit Drawdown для всех пар и направлений
- ✅ Добавлено детальное логирование для диагностики
- ✅ Проверка актуальности цен работает для всех механизмов

**Готово к тестированию!**





