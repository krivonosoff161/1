# ⚠️ ПРОБЛЕМЫ С СВЕЧАМИ ВО ВСЕХ РЕЖИМАХ

**Дата:** 26.11.2025 22:25  
**Статус:** Обнаружены проблемы с загрузкой свечей

---

## 📊 ТЕКУЩАЯ СИТУАЦИЯ

### **Что работает правильно:** ✅

1. ✅ **1m свечи:**
   - Загружаются при старте (200 свечей)
   - Хранятся в `DataRegistry.CandleBuffer`
   - Обновляются инкрементально через WebSocket
   - Используются в `SignalGenerator`, `AdaptiveRegimeManager`, `ADX Filter`

---

## ⚠️ ОБНАРУЖЕННЫЕ ПРОБЛЕМЫ

### **Проблема 1: Старшие таймфреймы не загружаются при старте** 🔴

**Текущая ситуация:**
- ❌ Загружаются только **1m** свечи (200 штук)
- ❌ Не загружаются **5m** свечи (нужны для Multi-Timeframe и Correlation)
- ❌ Не загружаются **1H** свечи (нужны для Volume Profile)
- ❌ Не загружаются **1D** свечи (нужны для Pivot Points)

**Где используются:**
- **5m:** `Multi-Timeframe Filter`, `Correlation Filter`
- **1H:** `Volume Profile Filter`
- **1D:** `Pivot Points Filter`

**Последствия:**
- ⚠️ Фильтры запрашивают свечи через API каждый раз
- ⚠️ Медленная работа при первом использовании фильтра
- ⚠️ Риск превышения лимитов API
- ⚠️ Несогласованные данные (свечи запрашиваются в разное время)

---

### **Проблема 2: Фильтры запрашивают свечи через API вместо DataRegistry** 🔴

**Текущая ситуация:**
- ❌ `Multi-Timeframe Filter` → запрашивает **5m** свечи через API каждый раз
- ❌ `Volume Profile Filter` → запрашивает **1H** свечи через API (с кэшем 600 сек)
- ❌ `Pivot Points Filter` → запрашивает **1D** свечи через API (с кэшем 3600 сек)
- ❌ `Correlation Filter` → запрашивает **5m** свечи через API

**Проблемы:**
- ⚠️ Неэффективно (много запросов к API)
- ⚠️ Нет централизованного хранения
- ⚠️ Дублирование запросов (несколько фильтров могут запросить одни и те же свечи)
- ⚠️ Несогласованные данные

---

### **Проблема 3: Нет автоматического обновления старших таймфреймов** 🟡

**Текущая ситуация:**
- ✅ Только **1m** свечи обновляются через WebSocket
- ❌ **5m** свечи не обновляются автоматически
- ❌ **1H** свечи не обновляются автоматически
- ❌ **1D** свечи не обновляются автоматически

**Последствия:**
- ⚠️ Устаревшие данные для старших таймфреймов
- ⚠️ Неточные расчеты индикаторов
- ⚠️ Фильтры могут использовать устаревшие данные

---

## 📋 ДЕТАЛЬНЫЙ АНАЛИЗ МОДУЛЕЙ

### **1. Multi-Timeframe Filter** ⚠️

**Требования:**
- Таймфрейм: **5m** (по умолчанию, configurable)
- Количество: минимум 21 свеча (для EMA21)

**Текущая реализация:**
- ❌ Запрашивает свечи через API при каждом вызове
- ❌ Не использует `DataRegistry.CandleBuffer`
- ❌ Есть кэш (TTL=30 секунд), но все равно запрашивает через API

**Код:**
```python
# multi_timeframe.py, строка 300-421
async def _get_htf_candles(self, symbol: str) -> List[OHLCV]:
    # Запрашивает через API каждый раз!
    if self.client and hasattr(self.client, "get_candles"):
        candles = await self.client.get_candles(...)
    else:
        candles = await self._fetch_candles_directly(...)  # Прямой запрос к API
```

**Статус:** ⚠️ **Требует улучшения**

---

### **2. Volume Profile Filter** ⚠️

**Требования:**
- Таймфрейм: **1H** (по умолчанию)
- Количество: 100 свечей

**Текущая реализация:**
- ❌ Запрашивает свечи через API при каждом вызове
- ❌ Не использует `DataRegistry.CandleBuffer`
- ❌ Есть кэш (TTL=600 секунд), но все равно запрашивает через API

**Код:**
```python
# volume_profile_filter.py, строка 232-258
async def _fetch_candles(self, symbol: str, timeframe: str, limit: int = 100):
    if self.client and hasattr(self.client, "get_candles"):
        candles = await self.client.get_candles(...)
    else:
        candles = await self._fetch_candles_directly(...)  # Прямой запрос к API
```

**Статус:** ⚠️ **Требует улучшения**

---

### **3. Pivot Points Filter** ⚠️

**Требования:**
- Таймфрейм: **1D** (дневные)
- Количество: 1-5 дней

**Текущая реализация:**
- ❌ Запрашивает свечи через API при каждом вызове
- ❌ Не использует `DataRegistry.CandleBuffer`
- ❌ Есть кэш (TTL=3600 секунд), но все равно запрашивает через API

**Код:**
```python
# pivot_points.py, строка 313-328
async def _get_pivot_levels(self, symbol: str):
    if self.client and hasattr(self.client, "get_candles"):
        daily_candles = await self.client.get_candles(...)
    else:
        daily_candles = await self._fetch_candles_directly(...)  # Прямой запрос к API
```

**Статус:** ⚠️ **Требует улучшения**

---

### **4. Correlation Filter** ⚠️

**Требования:**
- Таймфрейм: **5m** (по умолчанию)
- Количество: 100 свечей для расчета корреляции

**Текущая реализация:**
- ❌ Использует `CorrelationManager`, который запрашивает свечи через API
- ❌ Не использует `DataRegistry.CandleBuffer`

**Статус:** ⚠️ **Требует улучшения**

---

## 🎯 ВЛИЯНИЕ НА РАБОТУ БОТА

### **Режим: Trending** ⚠️

**Используются фильтры:**
- ✅ Multi-Timeframe Filter (5m) → запрашивает через API
- ✅ Volume Profile Filter (1H) → запрашивает через API
- ✅ Pivot Points Filter (1D) → запрашивает через API

**Проблема:**
- Медленная работа при генерации сигналов (много запросов к API)
- Риск превышения лимитов API

---

### **Режим: Ranging** ⚠️

**Используются фильтры:**
- ✅ Multi-Timeframe Filter (5m) → запрашивает через API
- ✅ Volume Profile Filter (1H) → запрашивает через API
- ✅ Pivot Points Filter (1D) → запрашивает через API

**Проблема:**
- Та же проблема с медленной работой

---

### **Режим: Choppy** ⚠️

**Используются фильтры:**
- ✅ Multi-Timeframe Filter (5m) → запрашивает через API
- ✅ Volume Profile Filter (1H) → запрашивает через API

**Проблема:**
- Та же проблема с медленной работой

---

## 📊 СВОДКА ПРОБЛЕМ

| Проблема | Критичность | Модули | Статус |
|----------|-------------|--------|--------|
| Старшие таймфреймы не загружаются | 🔴 Высокая | Multi-Timeframe, Volume Profile, Pivot Points, Correlation | ⚠️ |
| Фильтры запрашивают через API | 🔴 Высокая | Все фильтры | ⚠️ |
| Нет обновления старших ТФ | 🟡 Средняя | 5m, 1H, 1D | ⚠️ |

---

## ✅ ЧТО РАБОТАЕТ ПРАВИЛЬНО

1. ✅ **1m свечи** - загружаются, хранятся, обновляются
2. ✅ **Основные модули** - используют свечи из DataRegistry
3. ✅ **Базовые индикаторы** - работают правильно

---

## ⚠️ ВЫВОД

**Свечи работают правильно только для 1m таймфрейма!**

**Проблемы:**
- ⚠️ Старшие таймфреймы (5m, 1H, 1D) запрашиваются через API каждый раз
- ⚠️ Фильтры не используют централизованное хранение свечей
- ⚠️ Нет автоматического обновления старших таймфреймов

**Критичность:** 🟡 **Средняя**
- Бот работает, но неэффективно
- Может быть медленным при генерации сигналов
- Риск превышения лимитов API

---

## 🚀 РЕКОМЕНДАЦИИ

1. **Загружать старшие таймфреймы при старте:**
   - 5m: 100-200 свечей
   - 1H: 50-100 свечей
   - 1D: 5-10 свечей

2. **Хранить в DataRegistry:**
   - Создать `CandleBuffer` для каждого таймфрейма
   - Использовать в фильтрах вместо API запросов

3. **Обновлять автоматически:**
   - Определять формирование новой свечи
   - Добавлять в буфер, удалять старую






