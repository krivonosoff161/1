# 🔍 АНАЛИЗ: ПОЧЕМУ BTC МОЛЧИТ И ПРИМЕНЕНИЕ НАСТРОЕК

**Дата:** 2025-11-27  
**Вопрос:** Почему BTC не торгуется, и применяются ли настройки ко всем парам/направлениям/режимам?

---

## 🚨 ПРОБЛЕМА 1: BTC НЕ ТОРГУЕТСЯ

### Найденная причина:

**Из логов:**
```
⚠️ MA BEARISH сигнал ОТМЕНЕН для BTC-USDT: разница EMA слишком мала (0.006% < 0.02%)
```

**Проблема:**
- Разница между EMA_12 и EMA_26: **0.006-0.007%**
- Требуемый порог для ranging режима: **0.02%**
- Сигналы **ОТМЕНЯЮТСЯ** из-за слишком строгого порога!

### Текущие настройки `min_ma_difference_pct`:

| Режим | Порог | Для BTC в ranging |
|-------|-------|-------------------|
| **Trending** | 0.03% | Используется базовое значение |
| **Ranging** | **0.02%** ⚠️ | **СТРОГО!** Блокирует BTC |
| **Choppy** | 0.03% | Используется базовое значение |

**Для BTC-USDT в ranging:**
- EMA_12 = 91459.67
- EMA_26 = 91465.54
- Разница = **0.006%** (6 базисных пунктов)
- Требуется: **0.02%** (20 базисных пунктов)
- ❌ **СИГНАЛ ОТМЕНЕН!**

### Решение:

**Нужно снизить `min_ma_difference_pct` для ranging режима с 0.02% до 0.01% или меньше**, чтобы BTC мог торговать в флэте.

---

## ✅ ПРИМЕНЕНИЕ НАСТРОЕК: ПРОВЕРКА

### 1️⃣ **MIN_HOLDING - ПРИМЕНЯЕТСЯ КО ВСЕМ:**

#### По режимам:
| Режим | MIN_HOLDING | Применяется к |
|-------|-------------|---------------|
| **Trending** | 2.0 мин | ✅ ВСЕ пары, LONG и SHORT |
| **Ranging** | **35.0 мин** (изменено) | ✅ ВСЕ пары, LONG и SHORT |
| **Choppy** | 0.5 мин | ✅ ВСЕ пары, LONG и SHORT |

**✅ Применяется ко ВСЕМ парам:**
- BTC-USDT ✅
- ETH-USDT ✅
- SOL-USDT ✅
- DOGE-USDT ✅
- XRP-USDT ✅

**✅ Применяется к обоим направлениям:**
- LONG позиции ✅
- SHORT позиции ✅

**✅ Применяется ко всем режимам:**
- Trending ✅
- Ranging ✅
- Choppy ✅

**Механизм:** Настройки загружаются из `config_futures.yaml` → `trailing_stop_loss.by_regime.{regime}.min_holding_minutes` и применяются через `TrailingStopLoss.__init__()` ко всем позициям независимо от символа и направления.

---

### 2️⃣ **MIN_SCORE_THRESHOLD - ПРИМЕНЯЕТСЯ АДАПТИВНО:**

#### Базовые значения (для всех пар, если нет специфичных настроек):
| Режим | Базовый порог | Применяется к |
|-------|---------------|---------------|
| **Trending** | 1.6 | ✅ ВСЕ пары по умолчанию |
| **Ranging** | 1.6 | ✅ ВСЕ пары по умолчанию |
| **Choppy** | 1.8 | ✅ ВСЕ пары по умолчанию |

#### Специфичные значения для BTC-USDT (переопределяют базовые):
| Режим | BTC порог | Другие пары |
|-------|-----------|-------------|
| **Trending** | **2.2** | Используют 1.6 (базовый) |
| **Ranging** | **2.0** | Используют 1.6 (базовый) |
| **Choppy** | **1.6** | Используют 1.8 (базовый) |

**✅ Применяется:**
- К специфичным парам через `symbol_profiles.BTC-USDT.{regime}.arm.min_score_threshold`
- К остальным парам через базовые настройки `by_regime.{regime}.arm.min_score_threshold`
- К обоим направлениям (LONG и SHORT)
- Ко всем режимам

---

### 3️⃣ **TP/SL МУЛЬТИПЛИКАТОРЫ - ПРИМЕНЯЮТСЯ АДАПТИВНО:**

#### Базовые значения:
| Режим | TP ATR | SL ATR | Применяется к |
|-------|--------|--------|---------------|
| **Trending** | 1.0 | 0.5 | ✅ ВСЕ пары по умолчанию |
| **Ranging** | 0.8 | 0.5 | ✅ ВСЕ пары по умолчанию |
| **Choppy** | 0.9 | 0.4 | ✅ ВСЕ пары по умолчанию |

#### Специфичные значения для BTC-USDT:
| Режим | BTC TP ATR | BTC SL ATR | Другие пары |
|-------|------------|------------|-------------|
| **Trending** | **1.0** | **0.5** | Используют базовые |
| **Ranging** | **0.8** | **0.45** | Используют базовые |
| **Choppy** | **0.6** | **0.4** | Используют базовые |

**✅ Применяется:**
- К специфичным парам через `symbol_profiles.BTC-USDT.{regime}.arm.{tp|sl}_atr_multiplier`
- К остальным парам через базовые настройки
- К обоим направлениям (LONG и SHORT)
- Ко всем режимам

---

### 4️⃣ **АДАПТИВНЫЙ MIN_HOLDING ДЛЯ PARTIAL TP - ПРИМЕНЯЕТСЯ КО ВСЕМ:**

**Настройки:**
```yaml
adaptive_min_holding:
  enabled: true
  profit_threshold_1: 1.0  # Прибыль >= 1.0% → снижаем min_holding до 50%
  profit_threshold_2: 0.5  # Прибыль >= 0.5% → снижаем min_holding до 75%
```

**✅ Применяется:**
- ✅ Ко ВСЕМ парам (BTC, ETH, SOL, DOGE, XRP)
- ✅ К обоим направлениям (LONG и SHORT)
- ✅ Ко всем режимам (trending, ranging, choppy)
- ✅ Автоматически при достижении порога прибыли

**Пример для ranging (min_holding = 35 мин):**
- Прибыль >= 1.0% → MIN_HOLDING = **17.5 минут** (50% от 35)
- Прибыль >= 0.5% → MIN_HOLDING = **26.25 минут** (75% от 35)

---

## ⚠️ ПРОБЛЕМА 2: СТРОГИЙ ПОРОГ `min_ma_difference_pct` ДЛЯ BTC

### Текущие настройки:

**Ranging режим (базовый):**
```yaml
indicators:
  min_ma_difference_pct: 0.02  # ✅ ОПТИМИЗИРОВАНО: Снижено с 0.03% до 0.02%
```

**Проблема:**
- BTC в ranging имеет разницу EMA ~0.006-0.007%
- Порог 0.02% **СЛИШКОМ СТРОГИЙ** для BTC
- Сигналы **БЛОКИРУЮТСЯ**

### Решение:

**Снизить `min_ma_difference_pct` для ranging с 0.02% до 0.01%**, чтобы BTC мог торговать в флэте.

**Альтернатива:** Добавить специфичный порог для BTC в `symbol_profiles.BTC-USDT.ranging.indicators.min_ma_difference_pct`.

---

## 📊 ИТОГОВАЯ ТАБЛИЦА: ПРИМЕНЕНИЕ НАСТРОЕК

| Настройка | Все пары | Все направления | Все режимы | Специфичные переопределения |
|-----------|----------|-----------------|------------|----------------------------|
| **MIN_HOLDING** | ✅ | ✅ | ✅ | Нет (универсальные значения) |
| **MIN_SCORE_THRESHOLD** | ✅ | ✅ | ✅ | Да (BTC имеет свои пороги) |
| **TP/SL ATR** | ✅ | ✅ | ✅ | Да (BTC имеет свои значения) |
| **Адаптивный MIN_HOLDING** | ✅ | ✅ | ✅ | Нет (универсальные %) |
| **MIN_MA_DIFFERENCE_PCT** | ⚠️ | ✅ | ✅ | ⚠️ Нужно добавить для BTC |

---

## ✅ ВЫВОДЫ

### 1. Почему BTC молчит:

**Причина:** Слишком строгий порог `min_ma_difference_pct = 0.02%` для ranging режима блокирует сигналы BTC, у которого разница EMA составляет всего 0.006-0.007%.

**Решение:** Снизить `min_ma_difference_pct` для ranging с 0.02% до 0.01% или добавить специфичный порог для BTC.

### 2. Применение настроек:

**✅ MIN_HOLDING применяется:**
- Ко ВСЕМ парам
- К обоим направлениям (LONG и SHORT)
- Ко всем режимам (trending, ranging, choppy)

**✅ Остальные настройки применяются:**
- Адаптивно (есть специфичные значения для BTC, используются базовые для остальных)
- К обоим направлениям
- Ко всем режимам

**⚠️ Проблема:**
- `min_ma_difference_pct` слишком строгий для BTC в ranging режиме

---

## 🔧 РЕКОМЕНДАЦИИ

1. **Снизить `min_ma_difference_pct` для ranging с 0.02% до 0.01%**
   - Это позволит BTC торговать в флэте
   - Не повлияет негативно на другие пары (они имеют большую волатильность)

2. **Альтернатива:** Добавить специфичный порог для BTC:
   ```yaml
   symbol_profiles:
     BTC-USDT:
       ranging:
         indicators:
           min_ma_difference_pct: 0.01  # Специфичный порог для BTC
   ```

3. **Проверить другие фильтры для BTC:**
   - Проверить, не блокируют ли другие фильтры (liquidity, order_flow, funding, volatility) сигналы BTC

---

**Следующий шаг:** Исправить `min_ma_difference_pct` для ranging режима!



