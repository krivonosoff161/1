# 🔍 АНАЛИЗ ПРОБЛЕМЫ: ЛИМИТНЫЕ ОРДЕРА ВИСЯТ ДОЛГО

**Дата анализа:** 2025-11-14  
**Проблема:** Бот размещает лимитные ордера слишком далеко от текущей цены, они могут висеть час-два до исполнения, из-за чего теряется рынок

---

## 🔍 ЧАСТЬ 1: АНАЛИЗ ПРОБЛЕМЫ

### ❌ ПРОБЛЕМА: Лимитные ордера размещаются слишком далеко от цены

**Симптомы:**
- Лимитные ордера размещаются, но не исполняются сразу
- Ордера могут висеть 1-2 часа до исполнения
- За это время рынок уходит, сигнал теряет актуальность

---

### 🔍 АНАЛИЗ ТЕКУЩЕЙ ЛОГИКИ

#### 1. Как рассчитывается цена лимитного ордера

**Файл:** `src/strategies/scalping/futures/order_executor.py:209-374`

**Текущая логика:**

**Для BUY (покупка):**
```python
if best_ask > 0:
    limit_price = best_ask * 0.999  # -0.1% от best ask
```

**Проблема:**
- ❌ Ордер размещается на **0.1% НИЖЕ** лучшей цены продажи (best ask)
- ❌ Это означает, что для исполнения цена должна **УПАСТЬ** на 0.1%
- ❌ Если цена не падает, ордер может висеть часами
- ❌ Для скальпинга это неприемлемо - нужен быстрый вход

**Для SELL (продажа):**
```python
if best_ask > 0:
    limit_price = best_ask * 0.999  # -0.1% от best ask
```

**Проблема:**
- ❌ Для SELL используется та же логика, что и для BUY (best_ask * 0.999)
- ❌ Это **НЕПРАВИЛЬНО** - для SELL нужно использовать **best_bid**
- ❌ Ордер размещается ниже best ask, что не имеет смысла для продажи

---

#### 2. Отсутствие механизма отмены старых ордеров

**Файл:** `src/strategies/modules/slippage_guard.py`

**Текущее состояние:**
- ✅ SlippageGuard имеет `order_timeout: 30.0` секунд
- ❌ Но **НЕ мониторит** активные лимитные ордера
- ❌ **НЕ отменяет** ордера, которые висят слишком долго
- ❌ **НЕ заменяет** старые ордера новыми с обновленной ценой

**Проблема:**
- Ордера могут висеть часами без проверки и отмены
- Бот не знает, что ордер устарел и сигнал потерял актуальность

---

#### 3. Отсутствие механизма обновления цены лимитного ордера

**Проблема:**
- ❌ Если лимитный ордер не исполнился, бот не обновляет его цену
- ❌ Не размещает новый ордер с актуальной ценой
- ❌ Просто ждет, пока старый ордер исполнится (может быть часами)

---

## 💡 ЧАСТЬ 2: ПРИЧИНЫ ПРОБЛЕМЫ

### 1. Offset слишком консервативный

**Текущий offset:**
- BUY: `best_ask * 0.999` (-0.1% от best ask)
- SELL: `best_ask * 0.999` (-0.1% от best ask) ❌ **НЕПРАВИЛЬНО**

**Проблема:**
- Для скальпинга нужен **быстрый вход**, а не экономия на цене
- Offset 0.1% слишком маленький - цена может не дойти до ордера
- Для скальпинга лучше использовать более агрессивный offset (ближе к рыночной цене)

---

### 2. Логика для SELL неверная

**Текущая логика:**
```python
else:  # sell
    if best_ask > 0:
        limit_price = best_ask * 0.999  # -0.1% от best ask
```

**Проблема:**
- ❌ Для SELL используется `best_ask` (лучшая цена продажи)
- ❌ Должно использоваться `best_bid` (лучшая цена покупки)
- ❌ Ордер размещается ниже best ask, что не имеет смысла

**Правильная логика:**
- Для SELL: цена должна быть близко к `best_bid` (или даже ниже) для быстрого исполнения

---

### 3. Нет приоритета скорости исполнения

**Проблема:**
- Текущая логика оптимизирована для **экономии на цене** (лучшая цена исполнения)
- Но для скальпинга важнее **скорость исполнения**, а не экономия на 0.1%
- Нужно выбирать между "лучшей ценой" и "быстрым исполнением"

---

### 4. Нет механизма отмены/замены старых ордеров

**Проблема:**
- ❌ Нет проверки статуса лимитных ордеров
- ❌ Нет автоматической отмены ордеров после таймаута
- ❌ Нет замены старых ордеров новыми с актуальной ценой

---

## 🎯 ЧАСТЬ 3: РЕШЕНИЕ

### ✅ РЕШЕНИЕ 1: Исправить логику расчета цены лимитного ордера

#### Для BUY (покупка):

**Текущая логика (НЕПРАВИЛЬНО):**
```python
limit_price = best_ask * 0.999  # -0.1% от best ask
```

**Проблема:** Ордер размещается ниже best ask, может не исполниться

**Правильная логика:**
```python
# Вариант 1: Агрессивный (для быстрого исполнения)
limit_price = best_ask * 1.001  # +0.1% от best ask (выше best ask для гарантии исполнения)

# Вариант 2: Балансированный (компромисс между ценой и скоростью)
limit_price = best_ask  # По цене best ask (немедленное исполнение)

# Вариант 3: Конфигурируемый offset
limit_price = best_ask * (1 + limit_offset_percent / 100.0)  # Настраиваемый offset
```

**Рекомендация:**
- Для скальпинга использовать **Вариант 2** (limit_price = best_ask)
- Это гарантирует немедленное исполнение, но по лучшей цене

---

#### Для SELL (продажа):

**Текущая логика (НЕПРАВИЛЬНО):**
```python
limit_price = best_ask * 0.999  # -0.1% от best ask
```

**Проблема:** Используется best_ask вместо best_bid

**Правильная логика:**
```python
# Вариант 1: Агрессивный (для быстрого исполнения)
limit_price = best_bid * 0.999  # -0.1% от best bid (ниже best bid для гарантии исполнения)

# Вариант 2: Балансированный (компромисс между ценой и скоростью)
limit_price = best_bid  # По цене best bid (немедленное исполнение)

# Вариант 3: Конфигурируемый offset
limit_price = best_bid * (1 - limit_offset_percent / 100.0)  # Настраиваемый offset
```

**Рекомендация:**
- Для скальпинга использовать **Вариант 2** (limit_price = best_bid)
- Это гарантирует немедленное исполнение, но по лучшей цене

---

### ✅ РЕШЕНИЕ 2: Добавить таймаут и автоматическую отмену лимитных ордеров

**Файл:** `config/config_futures.yaml`  
**Добавить:**
```yaml
order_executor:
  limit_order:
    max_wait_seconds: 60  # Максимальное время ожидания лимитного ордера (60 сек)
    auto_cancel_enabled: true  # Автоматическая отмена после таймаута
    auto_replace_enabled: true  # Автоматическая замена ордера с новой ценой
    replace_with_market: true  # Заменять на рыночный ордер после таймаута
    limit_offset_percent: 0.01  # Offset для лимитных ордеров (0.01% = ближе к рыночной цене)
```

**Файл:** `src/strategies/scalping/futures/orchestrator.py`  
**Добавить метод мониторинга:**
```python
async def _monitor_limit_orders(self):
    """Мониторинг лимитных ордеров и их отмена/замена после таймаута"""
    try:
        limit_order_config = getattr(self.scalping_config, "order_executor", {}).get("limit_order", {})
        max_wait = limit_order_config.get("max_wait_seconds", 60)
        auto_cancel = limit_order_config.get("auto_cancel_enabled", True)
        replace_with_market = limit_order_config.get("replace_with_market", True)
        
        # Получаем активные ордера на бирже
        for symbol in self.scalping_config.symbols:
            try:
                active_orders = await self.client.get_active_orders(symbol)
                
                for order in active_orders:
                    order_id = order.get("ordId")
                    order_type = order.get("ordType", "")
                    state = order.get("state", "")
                    
                    # Проверяем только лимитные ордера, которые не исполнены
                    if order_type == "limit" and state == "live":
                        # Получаем время создания ордера
                        c_time = order.get("cTime")
                        if c_time:
                            order_time = datetime.fromtimestamp(int(c_time) / 1000)
                            wait_time = (datetime.now() - order_time).total_seconds()
                            
                            if wait_time > max_wait:
                                logger.warning(
                                    f"⚠️ Лимитный ордер {order_id} висит {wait_time:.0f} сек "
                                    f"(лимит: {max_wait} сек), отменяем..."
                                )
                                
                                # Отменяем ордер
                                if auto_cancel:
                                    await self.order_executor.cancel_order(order_id, symbol)
                                
                                # Заменяем на рыночный ордер, если включено
                                if replace_with_market:
                                    side = order.get("side", "").lower()
                                    size = float(order.get("sz", "0"))
                                    
                                    if size > 0 and side in ["buy", "sell"]:
                                        logger.info(
                                            f"📈 Размещаем рыночный ордер вместо зависшего лимитного: "
                                            f"{symbol} {side} {size:.6f}"
                                        )
                                        result = await self.order_executor._place_market_order(
                                            symbol, side, size
                                        )
                                        if result.get("success"):
                                            logger.info(
                                                f"✅ Рыночный ордер размещен вместо лимитного: {result.get('order_id')}"
                                            )
                            
            except Exception as e:
                logger.debug(f"Ошибка проверки ордеров для {symbol}: {e}")
                continue
                
    except Exception as e:
        logger.error(f"Ошибка мониторинга лимитных ордеров: {e}")
```

**Добавить вызов в основной цикл:**
```python
# В методе _main_trading_loop
async def _main_trading_loop(self):
    while self.is_running:
        try:
            # ... существующий код ...
            
            # ✅ НОВОЕ: Мониторинг лимитных ордеров
            await self._monitor_limit_orders()
            
            # ... остальной код ...
        except Exception as e:
            logger.error(f"Ошибка в торговом цикле: {e}")
```

---

### ✅ РЕШЕНИЕ 3: Улучшить логику расчета цены с учетом режима рынка

**Файл:** `config/config_futures.yaml`  
**Добавить:**
```yaml
order_executor:
  limit_order:
    # Offset для лимитных ордеров (настраиваемый)
    limit_offset_percent: 0.01  # 0.01% для быстрого исполнения (ближе к рыночной цене)
    
    # Агрессивность по режимам
    by_regime:
      trending:
        limit_offset_percent: 0.02  # +0.02% в тренде (более агрессивно для быстрого входа)
        max_wait_seconds: 30  # 30 сек в тренде (быстрее)
      ranging:
        limit_offset_percent: 0.01  # +0.01% в боковике (стандартно)
        max_wait_seconds: 60  # 60 сек в боковике
      choppy:
        limit_offset_percent: 0.0  # 0% в хаосе (по рыночной цене = немедленное исполнение)
        max_wait_seconds: 30  # 30 сек в хаосе (быстрее)
        use_market_order: true  # Использовать рыночные ордера в хаосе
```

**Файл:** `src/strategies/scalping/futures/order_executor.py`  
**Изменить метод `_calculate_limit_price`:**
```python
async def _calculate_limit_price(self, symbol: str, side: str, regime: Optional[str] = None) -> float:
    """
    ✅ УЛУЧШЕННЫЙ: Расчет цены для лимитного ордера с учетом режима рынка
    """
    try:
        # Получаем конфигурацию лимитных ордеров
        limit_order_config = getattr(self.scalping_config, "order_executor", {}).get("limit_order", {})
        
        # Получаем offset из конфига (с учетом режима)
        default_offset = limit_order_config.get("limit_offset_percent", 0.01)
        
        if regime:
            regime_config = limit_order_config.get("by_regime", {}).get(regime, {})
            offset_percent = regime_config.get("limit_offset_percent", default_offset)
        else:
            offset_percent = default_offset
        
        # Проверяем, нужно ли использовать рыночные ордера в этом режиме
        if regime:
            regime_config = limit_order_config.get("by_regime", {}).get(regime, {})
            use_market = regime_config.get("use_market_order", False)
            if use_market:
                logger.debug(f"📊 Режим {regime} требует рыночные ордера, возвращаем 0 для fallback на market")
                return 0.0  # Возвращаем 0 для fallback на рыночный ордер
        
        # Получаем лимиты цены биржи
        price_limits = await self.client.get_price_limits(symbol)
        
        if not price_limits:
            return 0.0  # Fallback на рыночный ордер
        
        best_bid = price_limits.get("best_bid", 0)
        best_ask = price_limits.get("best_ask", 0)
        current_price = price_limits.get("current_price", 0)
        
        if current_price <= 0:
            return 0.0
        
        # ✅ ИСПРАВЛЕННАЯ ЛОГИКА
        if side.lower() == "buy":
            if best_ask > 0:
                # ✅ ИСПРАВЛЕНО: Для BUY используем best_ask + offset (выше для гарантии исполнения)
                # Offset может быть положительным (выше best ask) или нулевым (по best ask)
                limit_price = best_ask * (1 + offset_percent / 100.0)
            else:
                limit_price = current_price * (1 + offset_percent / 100.0)
            
            # Проверка лимитов биржи
            max_buy_price = price_limits.get("max_buy_price", 0)
            if max_buy_price > 0 and limit_price > max_buy_price:
                # Если превышаем лимит - используем лимит или best ask (что меньше)
                limit_price = min(max_buy_price, best_ask) if best_ask > 0 else max_buy_price
                logger.warning(
                    f"⚠️ Лимитная цена BUY превышает лимит биржи, используем: {limit_price:.2f}"
                )
        else:  # sell
            if best_bid > 0:
                # ✅ ИСПРАВЛЕНО: Для SELL используем best_bid - offset (ниже для гарантии исполнения)
                # Offset может быть положительным (ниже best bid) или нулевым (по best bid)
                limit_price = best_bid * (1 - offset_percent / 100.0)
            else:
                limit_price = current_price * (1 - offset_percent / 100.0)
            
            # Проверка лимитов биржи
            min_sell_price = price_limits.get("min_sell_price", 0)
            if min_sell_price > 0 and limit_price < min_sell_price:
                # Если ниже лимита - используем лимит или best bid (что больше)
                limit_price = max(min_sell_price, best_bid) if best_bid > 0 else min_sell_price
                logger.warning(
                    f"⚠️ Лимитная цена SELL ниже лимита биржи, используем: {limit_price:.2f}"
                )
        
        logger.info(
            f"💰 Лимитная цена для {symbol} {side}: {limit_price:.2f} "
            f"(best_bid={best_bid:.2f}, best_ask={best_ask:.2f}, offset={offset_percent:.3f}%, "
            f"режим={regime or 'default'})"
        )
        
        return limit_price
        
    except Exception as e:
        logger.error(f"Ошибка расчета лимитной цены для {symbol}: {e}")
        return 0.0
```

---

### ✅ РЕШЕНИЕ 4: Добавить опцию "post-only" для гарантии maker fee

**Проблема:**
- Если лимитный ордер исполняется сразу (taker), комиссия выше (0.05% вместо 0.02%)
- Нужна опция "post-only" для гарантии, что ордер будет maker (0.02%)

**Решение:**
```yaml
order_executor:
  limit_order:
    post_only: true  # Гарантирует maker fee (0.02% вместо 0.05%)
    # Если post_only=true, ордер не исполнится сразу, но будет maker
    # Если post_only=false, ордер может исполниться сразу как taker
```

**Файл:** `src/clients/futures_client.py`  
**Добавить в метод `place_futures_order`:**
```python
# В payload добавить:
if order_type == "limit" and post_only:
    payload["postOnly"] = "true"  # Гарантирует maker fee
```

---

## 📋 ИТОГОВЫЙ ПЛАН ИСПРАВЛЕНИЙ

### Приоритет 1: Критично (немедленно)

1. ✅ **Исправить логику для SELL** - использовать `best_bid` вместо `best_ask`
2. ✅ **Исправить offset для BUY** - использовать `best_ask` или `best_ask + offset` вместо `best_ask - 0.1%`
3. ✅ **Добавить таймаут и отмену** - автоматически отменять лимитные ордера после 60 секунд
4. ✅ **Добавить замену на рыночный ордер** - после таймаута заменять лимитный ордер на рыночный

---

### Приоритет 2: Важно (ближайшее время)

1. ✅ **Настраиваемый offset** - сделать offset конфигурируемым через конфиг
2. ✅ **Адаптивный offset по режимам** - разные offset для trending/ranging/choppy
3. ✅ **Опция post-only** - для гарантии maker fee
4. ✅ **Мониторинг активных ордеров** - периодическая проверка и отмена старых ордеров

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### После исправления критичных проблем:

1. **Время ожидания лимитных ордеров:**
   - Текущий: 1-2 часа (или вообще не исполняются)
   - Ожидаемый: 30-60 секунд (автоматическая замена на рыночный)

2. **Процент исполнения лимитных ордеров:**
   - Текущий: ~50-60% (многие висят часами)
   - Ожидаемый: ~90-95% (большинство исполняются за 30-60 сек)

3. **Потеря сигналов:**
   - Текущий: ~30-40% сигналов теряются из-за зависших ордеров
   - Ожидаемый: ~5-10% (большинство сигналов исполняются)

4. **Среднее время входа:**
   - Текущий: 30-60 минут (или не входят вообще)
   - Ожидаемый: 30-60 секунд (быстрое исполнение или замена на рыночный)

---

## 🎯 ВЫВОДЫ

### ✅ Что нужно исправить:

1. ❌ **Неправильная логика для SELL** - используется best_ask вместо best_bid
2. ❌ **Слишком консервативный offset** - 0.1% слишком мало для скальпинга
3. ❌ **Нет таймаута и отмены** - ордера висят часами без проверки
4. ❌ **Нет замены на рыночный** - после таймаута нужно заменять на рыночный ордер

### ✅ Рекомендации:

1. ✅ **Исправить логику расчета цены** - использовать правильные цены для BUY/SELL
2. ✅ **Добавить таймаут и отмену** - автоматически отменять старые ордера
3. ✅ **Добавить замену на рыночный** - после таймаута заменять на рыночный ордер
4. ✅ **Сделать offset настраиваемым** - разные offset для разных режимов рынка

---

**Дата создания:** 2025-11-14  
**Версия:** 1.0  
**Автор:** AI Assistant
