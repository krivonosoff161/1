# 🔴 ПОЛНЫЙ АУДИТ ПРОБЛЕМ ПОСЛЕ РЕФАКТОРИНГА

**Дата:** 23 ноября 2025  
**Критичность:** 🔴 ВЫСОКАЯ - Бот не может нормально работать на сбор данных

---

## 📋 СПИСОК ВСЕХ ПРОБЛЕМ

### 1. ❌ order_executor не загружается из config_futures.yaml
**Файл:** `src/config.py`, `src/strategies/scalping/futures/order_executor.py`
**Проблема:** `order_executor_config` пустой `{}`, используется fallback 0.0%
**Статус:** 🔧 ИСПРАВЛЕНО (но нужно проверить после перезапуска)

### 2. ❌ margin_calculator использует fallback 'ranging' когда regime=None
**Файл:** `src/strategies/modules/margin_calculator.py`
**Проблема:** `regime=None` → fallback на 'ranging', но режим должен передаваться из позиции
**Логи:** `regime=None, используется fallback: 'ranging'`
**Статус:** ❌ НЕ ИСПРАВЛЕНО

### 3. ❌ Режим не сохраняется в active_positions при синхронизации
**Файл:** `src/strategies/scalping/futures/orchestrator.py` (метод `_sync_positions_with_exchange`)
**Проблема:** Режим сохраняется, но может быть None если не найден
**Статус:** ⚠️ ЧАСТИЧНО ИСПРАВЛЕНО (нужно проверить)

### 4. ❌ Много fallback значений в signal_generator
**Файл:** `src/strategies/scalping/futures/signal_generator.py`
**Проблема:** Много `getattr()` с fallback значениями вместо чтения из конфига
**Статус:** ❌ НЕ ПРОВЕРЕНО

### 5. ❌ OrderCoordinator использует fallback для режима
**Файл:** `src/strategies/scalping/futures/coordinators/order_coordinator.py`
**Проблема:** `current_regime = "ranging"  # Fallback` вместо получения из signal_generator
**Статус:** ❌ НЕ ИСПРАВЛЕНО

### 6. ❌ Параметры фильтров могут не загружаться правильно
**Файлы:** Все фильтры (liquidity, order_flow, funding_rate)
**Проблема:** Нужно проверить загрузку из futures_modules
**Статус:** ❌ НЕ ПРОВЕРЕНО

### 7. ❌ TP/TSL параметры могут использовать fallback
**Файлы:** `position_manager.py`, `trailing_sl_coordinator.py`
**Проблема:** Нужно проверить все места где используется fallback для TP/TSL
**Статус:** ❌ НЕ ПРОВЕРЕНО

### 8. ❌ Position sizing параметры могут использовать fallback
**Файл:** `risk_manager.py`
**Проблема:** Нужно проверить загрузку из balance_profiles
**Статус:** ❌ НЕ ПРОВЕРЕНО

---

## ✅ ПЛАН ИСПРАВЛЕНИЙ

1. ✅ Исправить загрузку order_executor (уже сделано)
2. ❌ Исправить margin_calculator - передавать regime из позиции
3. ❌ Исправить OrderCoordinator - получать regime правильно
4. ❌ Проверить все fallback значения в signal_generator
5. ❌ Проверить загрузку фильтров
6. ❌ Проверить TP/TSL параметры
7. ❌ Проверить position sizing параметры

---

**Приоритет:** 🔴 КРИТИЧЕСКИЙ - Исправить все проблемы сразу

