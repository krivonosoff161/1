# ИСПРАВЛЕНИЕ: Leverage в Sandbox Mode

## 🔍 ПРОБЛЕМА

После изменений бот пытался устанавливать leverage через API даже в sandbox mode, что вызывало ошибку:
```
Parameter posSide error
```

## ✅ РЕШЕНИЕ

Вернул проверку на sandbox mode, как было раньше:
- **В sandbox mode**: leverage НЕ устанавливается через API
- **В production mode**: leverage устанавливается через API

## 📝 ИЗМЕНЕНИЯ

### 1. `_initialize_client` (строка 281-306)
```python
# ✅ Установка leverage для торговых пар (только для production, не для sandbox)
# В sandbox mode leverage не устанавливается через API, но используется из конфига для расчетов
if not self.client.sandbox:
    # Устанавливаем leverage через API
    ...
else:
    # В sandbox просто логируем, что используем leverage из конфига
    leverage_config = getattr(self.scalping_config, "leverage", 3) or 3
    logger.info(
        f"⚠️ Sandbox mode: пропускаем установку leverage через API, "
        f"но расчеты используют leverage={leverage_config}x из конфига"
    )
```

### 2. `_execute_signal_from_price` (строка 1679-1696)
```python
# ✅ Проверка/установка leverage перед открытием позиции (только для production)
# В sandbox mode leverage не устанавливается через API, но используется из конфига для расчетов
if not self.client.sandbox:
    # Устанавливаем leverage через API
    ...
```

## 🎯 РЕЗУЛЬТАТ

1. ✅ **Ошибка `Parameter posSide error` больше не появляется** в sandbox mode
2. ✅ **Leverage из конфига используется для расчетов** (5x)
3. ✅ **Позиции открываются с правильными размерами** (используется leverage из конфига)
4. ✅ **В production mode leverage устанавливается через API** (как и должно быть)

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ

- ✅ Бот запускается без ошибок
- ✅ Leverage используется из конфига для расчетов (5x)
- ✅ Позиции открываются с правильными размерами
- ✅ В sandbox нет попыток установить leverage через API

