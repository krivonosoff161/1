# ИТОГОВОЕ ИСПРАВЛЕНИЕ: Leverage с учетом режима позиций

## 🔍 ПРОБЛЕМА

После анализа информации с биржи выявлено:
1. **Режим позиций:** `long_short_mode` (hedge mode)
2. **В hedge mode** для установки leverage **ОБЯЗАТЕЛЬНО** нужен `posSide`
3. **Установка leverage без `posSide`** вызывает ошибку `Parameter posSide error`
4. **Установка leverage с `posSide='long'`** работает успешно

## ✅ РЕШЕНИЕ

### 1. Добавлен метод `get_account_config()` в `futures_client.py`

Позволяет получить настройки аккаунта, включая режим позиций (`PosMode`).

### 2. Обновлена логика установки leverage в `orchestrator.py`

**При инициализации:**
- Проверяем режим позиций на бирже (`PosMode`)
- Если `long_short_mode` (hedge):
  - Устанавливаем leverage для **long** позиций с `posSide='long'`
  - Устанавливаем leverage для **short** позиций с `posSide='short'`
- Если `net_mode`:
  - Пробуем установить без `posSide`
  - Если не получилось, пробуем с `posSide='long'` и `posSide='short'`

**Перед открытием позиции:**
- Определяем `posSide` на основе стороны сигнала (`buy` → `long`, `sell` → `short`)
- Устанавливаем leverage с `posSide` (для hedge mode это обязательно)
- Если не получилось, пробуем без `posSide` (для net mode)

## 🎯 РЕЗУЛЬТАТ

1. ✅ **Leverage правильно устанавливается** для hedge mode с `posSide`
2. ✅ **Логика учитывает режим позиций** на бирже
3. ✅ **Код работает и для hedge, и для net mode**
4. ✅ **Расчеты используют leverage из конфига** (5x), даже если на бирже установлен другой leverage

## 📝 ВАЖНО

**Текущая ситуация на бирже:**
- Режим: `long_short_mode` (hedge)
- Позиция ETH-USDT: `cross margin`, `leverage 75x`
- Для новых позиций в **isolated mode** leverage устанавливается через API

**Рекомендация:**
- Leverage из конфига (5x) используется для всех расчетов
- Позиции на бирже могут открываться с другим leverage, если API не работает
- Для production нужно убедиться, что leverage установлен правильно на бирже

