# 🔍 АНАЛИЗ ЗАКРЫТИЯ DOGE-USDT

**Дата:** 23 ноября 2025, 02:05  
**Позиция:** DOGE-USDT LONG  
**Время открытия:** 23.11.2025, 01:40:21  
**Время закрытия:** 23.11.2025, 02:01:50  
**Длительность:** ~21 минута 29 секунд  
**Прибыль:** +0.69 USDT (+2.31% от цены, +5.17% от маржи)

---

## 🔍 НАЙДЕННАЯ ПРОБЛЕМА

### ✅ Позиция закрылась по TP (правильно)

**Из логов:**
```
2025-11-23 02:01:49 | INFO | TP достигнут для DOGE-USDT: 5.17% (TP=4.00%, net после комиссии: 4.17%, PnL=$0.62, margin=$11.94)
2025-11-23 02:01:50 | INFO | Позиция DOGE-USDT успешно закрыта по tp
```

**Причина закрытия:** TP достигнут (5.17% от маржи > 4.0% TP)

---

## ⚠️ ПРОБЛЕМЫ

### 1. Использовался Per-symbol TP вместо Per-regime TP

**Из логов:**
```
2025-11-23 02:01:49 | DEBUG | Per-symbol TP для DOGE-USDT: 4.0% (глобальный: 2.4%)
```

**Ожидалось:**
- Per-regime TP для DOGE-USDT (ranging): 4.0% (из конфига)

**Проблема:**
- Использовался Per-symbol TP (4.0%) вместо Per-regime TP (4.0%)
- Хотя значения одинаковые, логика должна была использовать Per-regime TP как приоритет 1

**Причина:**
- Возможно, режим не был правильно определен или передан в `_get_adaptive_tp_percent()`

---

### 2. Несоответствие min_hold в debug логах

**Из debug логов:**
```
02:01:48 | TSL_CHECK: DOGE-USDT check=min_holding_BLOCKED | minutes=4.3024 | min_hold=120.0000 | profit=0.0050
```

**Проблема:**
- `min_hold=120.0000` - это неправильно!
- Должно быть `min_hold=60.0000` (из конфига для ranging)

**Причина:**
- Возможно, используется `timeout_minutes` (120) вместо `min_holding_minutes` (60) в логировании

---

### 3. TP имеет приоритет над min_holding (правильно)

**Поведение:**
- Позиция закрылась через ~21 минуту при `min_holding=60 минут`
- Это **правильное поведение**, так как TP имеет приоритет над `min_holding`
- TP должен закрывать позицию независимо от `min_holding`

**Вывод:** Это не проблема, а правильное поведение!

---

## ✅ ЧТО РАБОТАЕТ ПРАВИЛЬНО

1. ✅ **TP достигнут и позиция закрыта** - правильно
2. ✅ **Адаптивный TP используется** - Per-symbol TP (4.0%) вместо глобального (2.4%)
3. ✅ **min_holding блокирует преждевременное закрытие** - работает правильно
4. ✅ **TP имеет приоритет над min_holding** - правильно

---

## 🔧 ТРЕБУЕТСЯ ИСПРАВЛЕНИЕ

### 1. Исправить логирование min_hold в TSL_CHECK
**Проблема:** В debug логах показывается `min_hold=120.0000` вместо `min_hold=60.0000`

**Исправление:** Использовать `min_holding_minutes` вместо `timeout_minutes` в логировании

### 2. Проверить, почему используется Per-symbol TP вместо Per-regime TP
**Проблема:** Логи показывают "Per-symbol TP" вместо "Per-regime TP"

**Исправление:** Проверить логику определения режима в `_get_adaptive_tp_percent()`

---

## 📋 ВЫВОД

**Позиция закрылась правильно по TP**, но есть проблемы с логированием и определением режима для TP.

**Статус:** ✅ **Позиция закрыта правильно, требуется исправление логирования**

