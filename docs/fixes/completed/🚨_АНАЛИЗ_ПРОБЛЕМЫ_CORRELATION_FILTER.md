# 🚨 АНАЛИЗ ПРОБЛЕМЫ CORRELATION FILTER

## 📊 ОБНАРУЖЕННАЯ ПРОБЛЕМА

**Проблема:** Correlation Filter блокирует ВСЕ сигналы LONG на всех 5 парах, хотя в конфиге для режима `ranging` указано `max_correlated_positions: 4`.

---

## 🔍 ДЕТАЛЬНЫЙ АНАЛИЗ

### 1. Текущая ситуация в логах:

**Найдено 49+ блокировок сигналов:**
- BTC-USDT LONG: `Too many correlated positions (3/2)` 
- ETH-USDT LONG: `Too many correlated positions (3/2)`
- SOL-USDT LONG: `Too many correlated positions (4/2)` 
- DOGE-USDT LONG: `Too many correlated positions (3/2)`
- XRP-USDT LONG: `Too many correlated positions (3/2)`

**Проблема:** Используется лимит **2**, хотя в конфиге для `ranging` указано **4**.

---

### 2. Конфигурация в `config_futures.yaml`:

**Режим `ranging` (активный режим):**
```yaml
ranging:
  modules:
    correlation_filter:
      threshold: 0.8
      max_correlated_positions: 4  # ✅ Указано 4
      block_same_direction_only: true
```

**Режим `trending`:**
```yaml
trending:
  modules:
    correlation_filter:
      threshold: 0.7
      max_correlated_positions: 3
      block_same_direction_only: true
```

**Режим `choppy`:**
```yaml
choppy:
  modules:
    correlation_filter:
      threshold: 0.85
      max_correlated_positions: 6
      block_same_direction_only: false
```

---

### 3. Проблема в коде:

**В `signal_generator.py` строка 413:**
```python
max_correlated_positions=corr_dict.get(
    "max_correlated_positions", 2  # ❌ FALLBACK = 2
),
```

**Проблема:**
- Fallback значение = **2** (жестко задано)
- Если параметр не найден в конфиге, используется **2** вместо **4**
- Возможно, параметр не правильно читается из режим-специфичного конфига

---

### 4. Масштаб проблемы:

**Открытые позиции:**
- BTC-USDT (SHORT) - открыта
- ETH-USDT (SHORT) - открыта
- DOGE-USDT (SHORT) - открыта
- XRP-USDT (LONG) - открыта

**Корреляции (из логов):**
- BTC ↔ ETH: 0.98
- BTC ↔ XRP: 0.97
- BTC ↔ DOGE: 0.99
- ETH ↔ XRP: 0.99
- ETH ↔ DOGE: 0.99
- SOL ↔ ETH: 0.98
- SOL ↔ BTC: 0.98
- SOL ↔ XRP: 0.97
- SOL ↔ DOGE: 0.98

**Все пары коррелированы между собой на 97-99%!**

---

### 5. Почему блокируются сигналы:

**Для SOLUSDT LONG:**
- Коррелирует с 4 открытыми позициями (ETH, BTC, XRP, DOGE)
- Все корреляции > 0.97 (выше порога 0.8)
- Если используется лимит **2**: `4 коррелированных >= 2` → БЛОКИРОВКА
- Если используется лимит **4**: `4 коррелированных >= 4` → БЛОКИРОВКА (тоже!)

**Для других пар (BTC, ETH, DOGE, XRP):**
- Коррелируют с 3 открытыми позициями
- Если используется лимит **2**: `3 коррелированных >= 2` → БЛОКИРОВКА

---

### 6. Критическая проблема:

**Даже если исправить чтение конфига и будет использоваться `max_correlated_positions: 4`:**

**Для SOLUSDT:**
- Уже открыто 4 коррелированные позиции
- SOLUSDT коррелирует со всеми 4 (0.97-0.98)
- `4 коррелированных >= 4` → **ВСЕ РАВНО БЛОКИРОВКА**

**Для остальных пар:**
- Уже открыто 3 коррелированные позиции  
- Новая пара коррелирует с 3 открытыми
- `3 коррелированных >= 4` → **НЕ БЛОКИРУЕТСЯ** (работает)

---

## ⚠️ ВЫВОДЫ

### Проблема #1: Используется неправильный лимит
- В логах: `(3/2)` и `(4/2)` - используется лимит **2**
- В конфиге для `ranging`: `max_correlated_positions: 4`
- **Fallback значение в коде = 2** (строка 756 в `signal_generator.py`)
- Код обновления параметров есть (строка 3324-3332), но возможно не работает для всех символов

### Проблема #2: Даже с лимитом 4 SOLUSDT все равно заблокирован
- SOLUSDT коррелирует со всеми 4 открытыми позициями
- `4 >= 4` → все равно блокировка (строгая проверка `>=`)
- **Нужно лимит >= 5**, чтобы разрешить все 5 пар одновременно

### Проблема #3: Все 5 пар высоко коррелированы
- Корреляции 97-99% между всеми парами
- Это нормально для криптовалют (двигаются вместе)
- **Фильтр работает как задумано** (блокирует избыточную корреляцию)
- Но при тренде на всех парах теряем прибыль из-за блокировки

---

## 📋 ВОЗМОЖНЫЕ РЕШЕНИЯ

### Вариант 1: Увеличить лимит до 5
- Для режима `ranging`: `max_correlated_positions: 5`
- Позволит открыть все 5 пар одновременно
- ⚠️ **Риск:** Все яйца в одной корзине (высокая корреляция)

### Вариант 2: Исправить чтение конфига
- Убедиться, что параметр правильно читается из режим-специфичного конфига
- Убрать fallback = 2, использовать значение из конфига

### Вариант 3: Изменить логику фильтра
- Разрешать открытие, если корреляция очень высокая (>0.95)
- Или учитывать не количество, а общий риск портфеля

### Вариант 4: Отключить фильтр для одинакового направления
- Если все пары LONG и коррелированы - разрешить все
- Использовать фильтр только для противоположных направлений

---

## 🔧 РЕКОМЕНДАЦИЯ

**Для режима `ranging` (флэт):**
- Увеличить `max_correlated_positions` до **5** (все 5 пар)
- **Или до 6** (как в choppy режиме, с запасом)
- ⚠️ **ВАЖНО:** Торгуем 5 парами, нужно разрешить все 5 одновременно

**Для режима `trending` (тренд):**
- Увеличить до **4-5** (в тренде тоже хочется открыть все пары)
- Или оставить **3** (меньше позиций, больше концентрация)

**Для режима `choppy` (хаос):**
- Оставить **6** (максимальная диверсификация)

**Также нужно:**
1. Проверить, почему параметры не обновляются из режим-специфичного конфига
2. Убедиться, что `update_parameters()` вызывается для всех символов
3. Возможно, изменить логику проверки с `>=` на `>` (строгий лимит)

---

**Анализ завершен:** ✅
**Проблема подтверждена:** ✅
**Решение требует изменения конфига:** ✅

