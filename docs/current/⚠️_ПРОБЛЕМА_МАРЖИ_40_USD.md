# ⚠️ ПРОБЛЕМА: Маржа ограничена $40 вместо ожидаемых значений

**Дата:** 23.11.2025, 22:30  
**Статус:** Проблема обнаружена, требуется исправление

---

## 🔍 ОБНАРУЖЕННАЯ ПРОБЛЕМА

**Симптом:** Открытые позиции имеют максимальную маржу по **$40**, вместо ожидаемых **$50-100** (для баланса $800-900).

---

## 📊 АНАЛИЗ КОНФИГУРАЦИИ

### A. Balance Profile (профиль баланса)

Для баланса **$800-900** используется профиль **small** ($500-$1500):

```yaml
small:  # $500 - $1500
  min_position_usd: 50.0
  max_position_usd: 250.0  # ✅ ОПТИМИЗИРОВАНО
  base_position_usd: 100.0
  max_position_percent: 25.0  # ✅ ОПТИМИЗИРОВАНО
```

**Ожидаемая маржа (при leverage 5x):**
- `min_margin_usd` = $50 / 5 = **$10**
- `max_margin_usd` = $250 / 5 = **$50**

### B. Symbol Profiles (параметры символов)

#### 1. DOGE-USDT в режиме choppy

**Проблема:** `max_position_usd: 40.0` (строка 724)

```yaml
DOGE-USDT:
  choppy:
    position:
      base_position_usd: 26.0
      min_position_usd: 18.0
      max_position_usd: 40.0  # ❌ ПРОБЛЕМА: Ограничивает маржу!
```

**Маржа при этом значении:**
- `max_margin_usd` = $40 / 5 = **$8** (но пользователь видит $40!)

#### 2. XRP-USDT в режиме choppy

**Исправлено:** `max_position_usd: 500.0` (строка 930)

```yaml
XRP-USDT:
  choppy:
    position:
      base_position_usd: 18.0
      min_position_usd: 12.0
      max_position_usd: 500.0  # ✅ ИСПРАВЛЕНО
```

---

## 🔍 ЛОГИКА ПРИОРИТЕТОВ

### В `risk_manager.py` (строки 254-267):

```python
if position_overrides.get("max_position_usd") is not None:
    symbol_max = float(position_overrides["max_position_usd"])
    balance_max = max_usd_size
    if symbol_max > max_usd_size:
        max_usd_size = symbol_max  # ✅ Увеличиваем если больше
        logger.debug(...)
    else:
        logger.debug(
            f"📊 Max position size из symbol_profiles (${symbol_max:.2f}) меньше или равно "
            f"balance_profile (${balance_max:.2f}), игнорируем (используем ${balance_max:.2f})"
        )  # ✅ Должны игнорировать если меньше
```

**Проблема:** Логика говорит "игнорируем", но может быть баг, где значение все равно применяется!

---

## 🔍 ВОЗМОЖНЫЕ ПРИЧИНЫ

### Причина 1: `max_position_usd: 40.0` применяется неправильно

**Проблема:** Если `max_position_usd: 40.0` из symbol_profiles меньше `max_position_usd: 250.0` из balance_profile, но код все равно использует меньшее значение.

**Маржа:**
- `max_margin_usd` = $40 / 5 = **$8** ❌ (слишком мало!)

### Причина 2: Другое ограничение

**Возможность:** Может быть, `max_position_percent` или `max_margin_percent` ограничивают до $40?

**Проверка:**
- `max_position_percent` = 25% (для small)
- `max_total_size` = $250 * 5 = $1250 (общий лимит для всех позиций)
- Но это не должно ограничивать одну позицию до $40

### Причина 3: Неправильная интерпретация

**Возможность:** Пользователь видит `max_position_usd = $40` (notional), а не маржу?

**Но:** Пользователь сказал "макс маржа была по 40 баксов", значит речь о марже.

---

## ✅ РЕШЕНИЕ

### 1. Исправить `max_position_usd: 40.0` для DOGE-USDT choppy

**Текущее значение:** `max_position_usd: 40.0` (строка 724)

**Должно быть:** `max_position_usd: 500.0` (как у других символов)

**Маржа после исправления:**
- `max_margin_usd` = $500 / 5 = **$100** ✅ (но ограничено до $50 по balance_profile)

### 2. Проверить логику в `risk_manager.py`

**Проблема:** Нужно убедиться, что `max_position_usd` из symbol_profiles **игнорируется**, если оно меньше balance_profile.

**Текущая логика:** 
- Если `symbol_max < balance_max` → игнорируем ✅
- Но возможно есть баг, где значение все равно применяется ❌

---

## 📋 РЕКОМЕНДАЦИИ

1. ✅ **Увеличить `max_position_usd` для DOGE-USDT choppy** с $40 до $500
2. ✅ **Проверить логику в `risk_manager.py`** - убедиться, что меньшее значение игнорируется
3. ✅ **Проверить другие символы** на наличие `max_position_usd < 250` (из balance_profile small)
4. ✅ **Добавить проверку в логи** - логировать, какой `max_position_usd` используется (из symbol_profiles или balance_profile)

---

## 🔢 РАСЧЕТ ДЛЯ БАЛАНСА $800-900

### Профиль: small ($500-$1500)

**Ожидаемые значения:**
- `min_position_usd` = $50.0 (из balance_profile)
- `max_position_usd` = $250.0 (из balance_profile)
- `max_position_percent` = 25.0%
- `max_margin_percent` = 85.0% (ranging) / 90.0% (trending)

**Ожидаемая маржа (при leverage 5x):**
- `min_margin_usd` = $50 / 5 = **$10**
- `max_margin_usd` = $250 / 5 = **$50**

**Если `max_position_usd: 40.0` применяется (ошибка):**
- `max_margin_usd` = $40 / 5 = **$8** ❌

**Если `max_position_usd: 500.0` применяется (правильно):**
- `max_margin_usd` = min($500 / 5, $250 / 5) = min($100, $50) = **$50** ✅

---

## ✅ ИТОГОВЫЙ ВЫВОД

**Проблема:** `max_position_usd: 40.0` для DOGE-USDT choppy ограничивает маржу до **$8**, но пользователь видит **$40**.

**Возможные объяснения:**
1. Пользователь видит `max_position_usd = $40` (notional), а не маржу
2. Логика в `risk_manager.py` неправильно применяет меньшее значение
3. Есть другое ограничение, которое ограничивает до $40

**Решение:** Исправить `max_position_usd: 40.0` → `max_position_usd: 500.0` для DOGE-USDT choppy и проверить логику приоритетов.

---

**Дата:** 23.11.2025, 22:30

