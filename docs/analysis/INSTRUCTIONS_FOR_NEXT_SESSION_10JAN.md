# 🚀 ИНСТРУКЦИЯ ДЛЯ СЛЕДУЮЩЕЙ СЕССИИ

**Дата обновления:** 2026-01-10  
**Версия:** 1.0  
**Статус:** Готово к использованию

---

## ✅ ЧТО БЫЛО ИСПРАВЛЕНО

Три критических исправления в системе Trailing Stop Loss:

1. **Добавлена иерархия fallback на entry_price** в `_get_current_price()`
2. **Добавлена защита от price=0** в `get_profit_pct()`
3. **Добавлена валидация перед should_close_position()** 

**Результат:** Loss_cut теперь работает корректно даже при недоступности WebSocket/REST API.

---

## 🎯 БЫСТРЫЙ СТАРТ

### 1. Перед запуском бота

```bash
# Убедитесь что файлы обновлены:
# - src/strategies/scalping/futures/coordinators/trailing_sl_coordinator.py ✅
# - src/strategies/scalping/futures/indicators/trailing_stop_loss.py ✅

# Запустите бота как обычно:
python run.py --mode futures
```

### 2. На что смотреть в логах

**✅ НОРМАЛЬНО (все работает):**
```
✅ TSL: WebSocket real-time price for BTC-USDT: 91955.00
⚠️ TSL: Using REST API callback for XRP-USDT: 2.0955
```

**⚠️ ВНИМАНИЕ (fallback на entry_price):**
```
🔴 TSL: КРИТИЧЕСКИЙ FALLBACK - используем entry_price=2.0887 для XRP-USDT
```
→ Это значит что WebSocket и REST API недоступны, но система работает!

**❌ КРИТИЧЕСКАЯ ОШИБКА:**
```
🔴 TSL: КРИТИЧЕСКАЯ ОШИБКА - Не удалось получить цену для BTC-USDT из всех источников!
```
→ Даже entry_price недоступен - нужна срочная диагностика!

---

## 📊 ПРОВЕРКА РАБОТОСПОСОБНОСТИ

### Сценарий 1: Нормальная работа (WebSocket доступен)

**Ожидаемое поведение:**
- Loss_cut срабатывает на убыточных позициях
- Позиции закрываются в срок
- Логи показывают WebSocket цены

**Команда для проверки:**
```bash
grep "WebSocket real-time" logs/futures/*.log
# Должны быть много совпадений
```

---

### Сценарий 2: WebSocket недоступен (REST API работает)

**Ожидаемое поведение:**
- Loss_cut по-прежнему срабатывает
- Позиции закрываются в срок (но медленнее)
- Логи показывают "Using REST API callback"

**Команда для проверки:**
```bash
grep "REST API callback" logs/futures/*.log
```

---

### Сценарий 3: Все API недоступны (есть entry_price)

**Ожидаемое поведение:**
- Loss_cut по-прежнему срабатывает (но с задержкой)
- Логи показывают "КРИТИЧЕСКИЙ FALLBACK"
- Позиции все равно закрываются

**Команда для проверки:**
```bash
grep "КРИТИЧЕСКИЙ FALLBACK" logs/futures/*.log
# Если есть много - значит проблемы с подключением!
```

---

## 🧪 ТЕСТИРОВАНИЕ LOSS_CUT

### Ручное тестирование (рекомендуется):

1. Откройте убыточную позицию (например SHORT на растущем рынке)
2. Дождитесь убытка > loss_cut порога (-0.4% от маржи)
3. Проверьте что позиция закрывается автоматически

**Ожидаемый результат:**
```
📊 Закрываем XRP-USDT по причине: loss_cut 
(price=2.0915 >= stop=2.0970, profit=-1.39%, time=5.23 мин)
```

### Проверка логов:

```bash
# Проверить loss_cut закрытия
grep "loss_cut" logs/futures/*.log

# Проверить что нет price=0.0000 в TSL_CHECK
grep "price=0.0000" logs/futures/*.log
# Результат должен быть ПУСТОЙ!
```

---

## ⚙️ ПАРАМЕТРЫ РИСКА

### Loss_cut параметры (для разных режимов):

| Режим | Loss_cut % | От маржи | Пример (leverage 10x) |
|-------|----------|---------|----------------------|
| **Trending** | 2.0% | 0.2% | -0.2% от цены |
| **Ranging** | 4.0% | 0.4% | -0.4% от цены |
| **Choppy** | 1.2% | 0.12% | -0.12% от цены |

### Critical loss_cut (2x threshold):

| Режим | Critical % | Задержка | Пример |
|-------|-----------|---------|---------|
| **Все режимы** | 2x threshold | 60 сек | -0.8% (ranging) |

**Что это значит:**
- `loss_cut` проверяется сразу (если > 90 сек в позиции)
- `critical_loss_cut` (2x) может сработать раньше (если >= 60 сек)

---

## 🔍 ДИАГНОСТИКА ПРОБЛЕМ

### Проблема: Позиции не закрываются по loss_cut

**Проверить:**
1. Логи на наличие `price=0.0000` (должно быть исправлено)
2. Логи на наличие `КРИТИЧЕСКИЙ FALLBACK` (значит проблема с данными)
3. Проверить что loss_cut параметры установлены в конфиге

**Команды:**
```bash
# Проверить price=0
grep "price=0.0000" logs/futures/*.log

# Проверить fallback
grep "FALLBACK\|КРИТИЧЕСКАЯ" logs/futures/*.log

# Проверить loss_cut настройки
grep "loss_cut" config/config_futures.yaml
```

---

### Проблема: Слишком медленное закрытие позиций

**Проверить:**
1. Доступен ли WebSocket (должен быть < 100ms)
2. Доступен ли REST API (должен быть < 2s)
3. Количество fallback на entry_price

**Команды:**
```bash
# Проверить временные метки
grep "TSL:" logs/futures/*.log | tail -20

# Проверить источник цены
grep -E "WebSocket|REST API|FALLBACK|Entry Price" logs/futures/*.log
```

---

### Проблема: Слишком много ошибок

**Проверить:**
1. Интернет соединение (VPN не требуется)
2. API rate limits (не превышены ли?)
3. Конфигурация REST API

**Команды:**
```bash
# Проверить ошибки
grep "❌\|ERROR" logs/futures/*.log

# Проверить сетевые ошибки
grep -E "connection|timeout|refused" logs/futures/*.log
```

---

## 📈 МОНИТОРИНГ МЕТРИК

### Ключевые метрики для отслеживания:

1. **Процент закрытий по loss_cut:**
   - Ожидается: ~5-10% от всех закрытий
   - Если < 2%: loss_cut может не срабатывать
   - Если > 20%: параметры риска слишком агрессивны

2. **Время до закрытия:**
   - Ожидается: < 30 сек при WebSocket
   - Ожидается: < 2 мин при REST API fallback
   - Если > 5 мин: проблема с данными

3. **Количество FALLBACK сообщений:**
   - Ожидается: 0-1 в минуту
   - Если > 10 в минуту: серьезные проблемы с подключением

---

## 📝 ФАЙЛЫ ДЛЯ ЧТЕНИЯ

После исправлений рекомендуется прочитать:

1. **[QUICK_SUMMARY_TSL_FIXES_10JAN.md](QUICK_SUMMARY_TSL_FIXES_10JAN.md)** - Краткое резюме (5 мин)
2. **[VISUAL_COMPARISON_TSL_BEFORE_AFTER.md](VISUAL_COMPARISON_TSL_BEFORE_AFTER.md)** - Визуальное сравнение (10 мин)
3. **[ИСПРАВЛЕНИЯ_TSL_PRICE_0_10-01-2026.md](ИСПРАВЛЕНИЯ_TSL_PRICE_0_10-01-2026.md)** - Подробный отчет (20 мин)
4. **[CHECKLIST_VALIDATION_TSL_FIXES_10JAN.md](CHECKLIST_VALIDATION_TSL_FIXES_10JAN.md)** - Чеклист валидации (10 мин)

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

### Немедленно (до следующей торговой сессии):
- [ ] Прочитать QUICK_SUMMARY
- [ ] Проверить синтаксис (✅ уже выполнено)
- [ ] Убедиться что файлы обновлены

### Во время торговли:
- [ ] Мониторить логи на `price=0.0000` (должны исчезнуть)
- [ ] Проверить что loss_cut срабатывает
- [ ] Отследить количество FALLBACK сообщений

### После сессии:
- [ ] Проанализировать логи
- [ ] Проверить метрики loss_cut
- [ ] Убедиться что нет критических ошибок

---

## 💡 СОВЕТ

**Если вы видите сообщение:**
```
🔴 TSL: КРИТИЧЕСКИЙ FALLBACK - используем entry_price=2.0887
```

Это **НЕ ошибка** - это нормальное поведение когда WebSocket недоступен.  
Система по-прежнему работает правильно, используя entry_price как резерв.

**Но если это происходит часто:**
- Проверьте интернет соединение
- Проверьте REST API rate limits
- Рассмотрите добавление VPN для стабильности

---

## 📞 ПОДДЕРЖКА

Если возникли проблемы:

1. Проверьте синтаксис: ✅ (выполнено)
2. Посмотрите файл ошибок: `logs/futures/staging_*/error.log`
3. Прочитайте диагностику в этом файле выше
4. Проверьте конфигурацию: `config/config_futures.yaml`

---

**Статус:** ✅ Готово к использованию  
**Дата обновления:** 2026-01-10  
**Версия:** 1.0

