# ✅ Проверка loss_cut параметров

**Дата:** 2025-11-22 13:55  
**Статус:** 🔍 ПРОВЕРКА

## 📊 Проблема

**В логах видно:**
- Режим: **RANGING** ✅
- `loss_cut=0.0120` (1.2% от маржи) ❌
- Должно быть: `loss_cut=3.0%` для RANGING ✅

---

## 🔍 Где берутся параметры loss_cut?

### **1. Инициализация TrailingStopLoss:**

**В `orchestrator.py:1246`:**
```python
loss_cut_percent=params["loss_cut_percent"],
```

**Параметры формируются в `_get_trailing_sl_params()`:**
```python
def _get_trailing_sl_params(self, regime: Optional[str] = None) -> Dict[str, Any]:
    return self.config_manager.get_trailing_sl_params(regime=regime)
```

### **2. ConfigManager.get_trailing_sl_params()**

**Метод должен:**
1. Получить общие параметры trailing_sl из конфига
2. Получить параметры для конкретного режима (ranging/trending/choppy)
3. Объединить их (режим переопределяет общие)

**Нужно проверить:**
- Правильно ли определяется regime
- Правильно ли загружаются параметры для режима
- Не используется ли fallback значение вместо режимного

---

## 🎯 Проверка

**Из debug логов видно:**
- `regime=ranging` ✅
- `loss_cut=3.0000` при инициализации TSL ✅
- НО `loss_cut=0.0120` при проверке закрытия ❌

**Возможные причины:**
1. Режим меняется с RANGING на CHOPPY во время закрытия
2. Используется другой экземпляр TSL с другими параметрами
3. Параметры перезаписываются при обновлении режима

---

**Требуется детальная проверка кода ConfigManager.get_trailing_sl_params()!**

