# 🚨 КРИТИЧЕСКОЕ ОТКРЫТИЕ: ПАРАМЕТРЫ НЕ ПРИМЕНЯЮТСЯ!

## 📍 ЧТО МЫ ОБНАРУЖИЛИ

### В конфиге (config_futures.yaml):
```yaml
tp_percent: 5.55%    # Очень высокий TP!
sl_percent: 1.0-1.5% # Нормальный SL
min_holding_minutes: 2.0-3.5 мин  # ЕСТЬ защита от шума!
```

### В реальных логах торговли:
```
BTC #1: TP = 0.05% (3.6 сек) → УБЫТОК
ETH #1: TP = 0.05% (44 сек) → УБЫТОК
SOL #1: TP = 0.05% (5 сек) → УБЫТОК
XRP #1: TP = 0.05% → но держалась 1427 сек → ПРОФИТ!
```

## 💥 ВЫВОД: КОНФИГ НЕ ПРИМЕНЯЕТСЯ!

Причины возможны:
1. **Параметры переписываются в runtime** - где-то в коде есть hardcode `tp_percent = 0.05`
2. **Неправильный путь к конфигу** - загружается старый или default конфиг
3. **Условия переопределяют параметры** - что-то меняет TP перед торговлей
4. **Разные конфиги для разных режимов** - может быть используется другой файл

---

## 🔍 ГДЕ ИСКАТЬ ПРОБЛЕМУ

### Файлы для проверки:

**1. src/strategies/scalping/futures/config/parameter_provider.py**
```python
# Ищем: tp_percent = 0.05 (hardcoded!)
# Или: return 0.05 вместо самого параметра из конфига
```

**2. src/strategies/scalping/futures/orchestrator.py**
```python
# Проверить: Как загружается конфиг?
# config = BotConfig.load_from_file(config_path)
# Правильный ли путь?
```

**3. src/strategies/scalping/futures/signal_generator.py**
```python
# Ищем: где устанавливается tp_percent
# Переписывается ли из конфига в метод?
```

**4. src/strategies/scalping/futures/positions/exit_analyzer.py**
```python
# Метод _get_tp_percent()
# Возвращает ли она значение из конфига или hardcoded?
```

---

## 📋 ПРОВЕРОЧНЫЙ ПЛАН

### Шаг 1: Найти где tp_percent = 0.05 (15 мин)
```bash
# В PowerShell:
grep -r "tp_percent.*0\.05" src/
grep -r "tp_percent" src/ | grep "="

# Должны найти ВСЕ места где это используется
```

### Шаг 2: Проверить Parameter Provider (10 мин)
```
Открыть: src/strategies/scalping/futures/config/parameter_provider.py
Найти функцию: get_exit_params() или _get_tp_percent()
Проверить: Берет ли она из self.config или возвращает hardcoded?
```

### Шаг 3: Проверить порядок загрузки (10 мин)
```
Трассировка:
run.py → main_futures.py → orchestrator.py → config loading
        ↓
    Где загружается конфиг?
    Правильный ли путь к config_futures.yaml?
    Применяется ли после загрузки?
```

### Шаг 4: Логирование параметров (5 мин)
```python
# Добавить в position_manager.py или signal_generator.py:
logger.info(f"TP параметр загруженный: {self.tp_percent}")
logger.info(f"SL параметр загруженный: {self.sl_percent}")
logger.info(f"MIN_HOLDING загруженный: {self.min_holding_seconds}")

# Если в логах видно 0.05, то это hardcode или неверная загрузка
```

---

## 🎯 ГИПОТЕЗЫ

### Гипотеза 1: Hardcoded TP в parameter_provider.py
**Вероятность:** 🔴 ВЫСОКАЯ (70%)

```python
# Вероятный код:
def get_exit_params(self, symbol, regime):
    if regime == "ranging":
        return {
            "tp_percent": 0.05,  # ← HARDCODED! Не из конфига!
            "sl_percent": 0.40,
        }
    elif regime == "trending":
        return {
            "tp_percent": 0.05,  # ← HARDCODED!
            "sl_percent": 0.40,
        }
```

### Гипотеза 2: Fallback значение в exit_analyzer.py
**Вероятность:** 🟡 СРЕДНЯЯ (40%)

```python
# Вероятный код:
def _get_tp_percent(self, symbol, regime):
    tp_percent = 0.05  # ← DEFAULT fallback!
    
    # Пытается загрузить из конфига...
    if self.config:
        try:
            # Что-то идет не так, fallback к 0.05
        except:
            pass
    
    return tp_percent  # Возвращает 0.05
```

### Гипотеза 3: Неправильный путь к конфигу
**Вероятность:** 🟡 СРЕДНЯЯ (30%)

```
Ищет:    config/config_futures.yaml
Но на самом деле берет:  конфиг по умолчанию с tp_percent=0.05
```

---

## 📊 КАК ПОНЯТЬ, ЧТО ЭТО ПРОБЛЕМА

Признаки:
1. ✅ В конфиге `tp_percent: 5.55%`
2. ✅ В логах видна торговля с `TP = 0.05%`
3. ✅ Позиции закрываются за 3-50 сек (что соответствует 0.05%)
4. ✅ Нет логирования "Loading TP from config: 5.55%"
5. ❌ Нет в логах ошибок загрузки конфига

**Вывод:** Конфиг загружается, но параметры ТП игнорируются и используется hardcoded 0.05%

---

## 🔧 РЕШЕНИЕ (ПЛАН)

### Вариант 1: Найти и заменить hardcode (быстрый)
```python
# Если найдем:
tp_percent = 0.05  # HARDCODED!

# Заменить на:
tp_percent = self.config.get("tp_percent", 0.05)  # Из конфига с fallback
```

### Вариант 2: Добавить логирование (отладка)
```python
# Добавить в signal_generator.py:
logger.info(f"🔧 [PARAMS] Загружен TP: {tp_percent}%, SL: {sl_percent}%, MIN_HOLDING: {min_holding_sec}s")

# Если в логах видно 0.05%, это подтверждает hardcode
```

### Вариант 3: Полная проверка цепи загрузки (комплексная)
```python
# Трассировка:
1. main_futures.py: logger.info(f"Loading config from: {config_path}")
2. orchestrator.py: logger.info(f"Config loaded: tp_percent={config.tp_percent}")
3. parameter_provider.py: logger.info(f"Returning TP: {tp_percent}")
4. exit_analyzer.py: logger.info(f"Using TP: {tp_percent}")

# Каждый лог покажет где ломается цепь
```

---

## 📌 НЕМЕДЛЕННЫЙ ДЕЙСТВИЯ

### 🔴 ПРИОРИТЕТ 1: Найти hardcode (5-10 мин)
```bash
grep -r "0\.05" src/strategies/ | grep -i "tp\|percent"
grep -r "0\.40" src/strategies/ | grep -i "sl\|percent"
```

**Если найдем:**
```
position_manager.py: tp_percent = 0.05
exit_analyzer.py: return 0.05
parameter_provider.py: "tp_percent": 0.05
```

→ Это то, что ищем!

### 🟡 ПРИОРИТЕТ 2: Проверить parameter_provider.py (10 мин)
```python
# Открыть и найти метод get_exit_params()
# Проверить: есть ли там hardcoded 0.05?
# Если да - заменить на:
return self.config.scalping_config.tp_percent
```

### 🟢 ПРИОРИТЕТ 3: Добавить логирование (5 мин)
```python
# В position_manager.py или exit_analyzer.py добавить:
logger.info(f"[TP_DEBUG] tp_percent={self.tp_percent}, sl_percent={self.sl_percent}")

# Перезапустить бота и проверить логи
```

---

## 🏁 ФИНАЛЬНЫЙ ВЫВОД

**ВАЖНЕЙШЕЕ ОТКРЫТИЕ:**

Конфиг имеет правильные параметры (tp_percent: 5.55%), но **бот использует hardcoded 0.05%**!

Это объясняет **ВСЕ** странные поведения:
- ✅ Почему позиции закрываются за 3-5 сек
- ✅ Почему 90% позиций убыточны
- ✅ Почему XRP была выигрышной (дело не в параметрах, а в времени!)
- ✅ Почему изменение конфига ничего не помогает

**РЕШЕНИЕ:** Найти где tp_percent = 0.05 и заменить на загрузку из конфига!

Это **КРИТИЧНЕЙШИЙ BUG** - важнее чем all остальные ошибки вместе взятые!

---

**Статус:** 🚨 **ТРЕБУЕТ НЕМЕДЛЕННОГО РЕШЕНИЯ**
