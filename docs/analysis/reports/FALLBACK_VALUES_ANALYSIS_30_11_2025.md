# Анализ Fallback Значений в Боте

**Дата:** 2025-11-30  
**Цель:** Проверка всех fallback значений и их влияния на торговлю

---

## 🔴 КРИТИЧНЫЕ FALLBACK (могут влиять на торговлю)

### 1. **Режим рынка (regime)**
**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py:171`

**Fallback:** `regime = "ranging"`

**Проблема:** 
- ✅ **ИСПРАВЛЕНО:** Теперь приоритет: signal_generator → DataRegistry → metadata → fallback
- Раньше использовался старый режим из metadata

**Статус:** ✅ Исправлено

---

### 2. **Направление позиции (position_side)**
**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py:617`

**Fallback:** `position_side = "long"`

**Проблема:** 
- Если позиция SHORT, но не определилась правильно → используется "long"
- Это приведет к **НЕПРАВИЛЬНОМУ расчету PnL%** (обратный знак!)

**Приоритет определения:**
1. `metadata.position_side`
2. `position.get("posSide")`
3. `position.get("position_side")`
4. **Fallback: "long"** ⚠️

**Рекомендация:** 
- Добавить WARNING, если используется fallback
- Проверить, что `posSide` всегда корректно передается из биржи

**Статус:** ⚠️ Требует проверки

---

### 3. **TP (Take Profit) - Глобальный fallback**
**Файл:** `src/strategies/scalping/futures/position_manager.py:208`

**Fallback:** `tp_percent = self.scalping_config.tp_percent` (глобальный TP)

**Проблема:**
- Если не найден per-regime или per-symbol TP → используется глобальный
- Может быть слишком высоким/низким для конкретной пары

**Приоритет:**
1. Per-regime TP (trending/ranging/choppy)
2. Per-symbol TP
3. **Fallback: Глобальный TP** ⚠️

**Статус:** ✅ Логируется WARNING

---

### 4. **SL (Stop Loss) - Глобальный fallback**
**Файл:** `src/strategies/scalping/futures/position_manager.py:369`

**Fallback:** `sl_percent = self.scalping_config.sl_percent` (глобальный SL)

**Проблема:**
- Аналогично TP - может быть неподходящим для пары/режима

**Приоритет:**
1. Per-regime SL
2. Per-symbol SL
3. **Fallback: Глобальный SL** ⚠️

**Статус:** ✅ Логируется WARNING

---

## 🟡 СРЕДНИЕ FALLBACK (используются при ошибках)

### 5. **Комиссия (trading_fee_rate)**
**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py:302`

**Fallback:** `trading_fee_rate = 0.0010` (0.1%)

**Статус:** ✅ Нормально - используется только если не найдено в конфиге

---

### 6. **TP для ExitAnalyzer**
**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py:328`

**Fallback:** `tp_percent = 2.4`

**Статус:** ✅ Нормально - используется только если не найдено в конфиге

---

### 7. **Big Profit Exit**
**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py:386`

**Fallback:** `default_value = 1.5` (для неизвестных символов)

**Статус:** ✅ Нормально - используется только для неизвестных символов

---

## 🟢 БЕЗОПАСНЫЕ FALLBACK (только при критических ошибках)

### 8. **Fallback цены для BTC/ETH**
**Файл:** `src/strategies/scalping/futures/order_executor.py`

**Fallback:** 
- BTC: `110000.0`
- ETH: `3900.0`
- Другие: `50000.0`

**Статус:** ✅ Безопасно - используется только если API не отвечает

---

### 9. **Fallback TP/SL при ошибке**
**Файл:** `src/strategies/scalping/futures/order_executor.py:404`

**Fallback:** `entry_price * 1.003, entry_price * 0.998` (+0.3%, -0.2%)

**Статус:** ✅ Безопасно - используется только при критической ошибке

---

### 10. **Fallback ATR**
**Файл:** `src/strategies/scalping/futures/order_executor.py:533`

**Fallback:** `price * 0.01` (1% от цены)

**Статус:** ✅ Безопасно - используется только если не удалось рассчитать ATR

---

## 📊 ИТОГОВАЯ ТАБЛИЦА

| Fallback | Критичность | Статус | Действие |
|----------|-------------|--------|----------|
| Режим (regime) | 🔴 Высокая | ✅ Исправлено | Приоритет изменен |
| Направление (position_side) | 🔴 Высокая | ⚠️ Проверить | Добавить WARNING |
| TP (глобальный) | 🟡 Средняя | ✅ Логируется | OK |
| SL (глобальный) | 🟡 Средняя | ✅ Логируется | OK |
| Комиссия | 🟢 Низкая | ✅ OK | OK |
| Fallback цены | 🟢 Низкая | ✅ OK | OK |

---

## ✅ РЕКОМЕНДАЦИИ

1. **✅ ИСПРАВЛЕНО: Добавлен WARNING для position_side fallback**
   - Теперь логируется, если используется fallback "long"

2. **✅ ИСПРАВЛЕНО: Добавлен WARNING для regime fallback**
   - Логируется в signal_coordinator при открытии позиции
   - Логируется в entry_manager при сохранении metadata

3. **Проверить логи на наличие:**
   - `⚠️ КРИТИЧНО: Режим не определен` - если есть → проблема с ARM инициализацией
   - `⚠️ FALLBACK: Используется глобальный TP/SL` - если часто → проверить конфиг
   - `⚠️ FALLBACK position_side: Используется 'long'` - если есть → проверить API posSide

4. **Мониторинг:**
   - Отслеживать частоту использования fallback значений
   - Если fallback используется часто → проблема в конфиге или данных
   - **В нормальной работе fallback НЕ должны использоваться!**

---

## 🎯 ВЫВОД

**Основные проблемы:**
1. ✅ Режим - исправлено (теперь динамический из signal_generator)
2. ✅ Направление позиции - добавлен WARNING для мониторинга
3. ✅ Режим при открытии - добавлен WARNING, если не определен

**Важно:**
- **В нормальной работе fallback НЕ должны использоваться!**
- Если fallback используются → проблема с:
  - Инициализацией ARM (regime не определяется)
  - API данными (posSide не приходит)
  - Конфигом (TP/SL не найдены)

**Остальные fallback:**
- Используются только при критических ошибках (API не отвечает)
- Логируются WARNING
- Не должны использоваться в нормальной работе

