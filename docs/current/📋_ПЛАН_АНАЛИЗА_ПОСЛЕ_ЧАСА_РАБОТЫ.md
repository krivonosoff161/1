# 📋 ПЛАН АНАЛИЗА ПОСЛЕ ЧАСА РАБОТЫ

**Дата:** 22.11.2025  
**Время работы:** ~1 час  
**Файл для анализа:** `logs/futures/debug/debug_YYYYMMDD_HHMMSS.csv`

---

## 📊 ЧТО ПРОВЕРИТЬ ПОСЛЕ ЧАСА РАБОТЫ

### 1️⃣ **ОТКРЫТЬ CSV ФАЙЛ**

```bash
# Найти последний CSV файл
dir logs\futures\debug\*.csv /O-D

# Или в Excel:
# logs/futures/debug/debug_20251122_103100.csv
```

### 2️⃣ **ПРОАНАЛИЗИРОВАТЬ СТРУКТУРУ**

**Что должно быть:**
- ✅ Заголовок: `timestamp,event_type,symbol,data`
- ✅ События `tsl_create` - создание TSL для позиций
- ✅ События `config` - загруженные параметры
- ✅ События `tsl_check` - проверки TSL (каждые 1-2 секунды)
- ✅ События `close` - закрытия позиций

---

## 🔍 КЛЮЧЕВЫЕ ВОПРОСЫ ДЛЯ АНАЛИЗА

### ВОПРОС 1: Сколько позиций открылось?

**Искать в CSV:**
```
event_type = "tsl_create"
```

**Проверить:**
- Сколько строк с `tsl_create`?
- Какие символы открыты?
- В какое время открылись?

---

### ВОПРОС 2: Работает ли min_holding защита?

**Искать в CSV:**
```
event_type = "tsl_check"
data содержит: "check=min_holding_BLOCKED"
```

**Проверить:**
- Сколько раз сработала защита?
- Как долго держалась позиция?
- Блокировала ли защита закрытие?

**Пример:**
```
10:32:43.075,tsl_check,DOGE-USDT,check=min_holding_BLOCKED | minutes=0.01 | min_hold=40.0
10:33:03.222,tsl_check,DOGE-USDT,check=min_holding_BLOCKED | minutes=0.35 | min_hold=40.0
...
11:12:42.500,tsl_check,DOGE-USDT,check=min_holding_BLOCKED | minutes=40.0 | min_hold=40.0
```

---

### ВОПРОС 3: Почему закрылись позиции?

**Искать в CSV:**
```
event_type = "close"
```

**Проверить:**
- Сколько позиций закрылось?
- Какова причина закрытия (`reason`)?
- Сколько времени держалась позиция (`time_min`)?
- Какой был PnL (`pnl_pct`)?

**Возможные причины:**
- `loss_cut` - убыток превысил порог
- `timeout` - вышло время
- `tsl` - сработал trailing stop
- `tp` - достигнут take profit

---

### ВОПРОС 4: Закрывались ли позиции слишком быстро?

**Искать в CSV:**
```
event_type = "close"
data содержит: "time_min=XX"
```

**Проверить:**
- Сколько минут держалась позиция?
- Сравнить с `min_holding` из конфига (должно быть 40+ минут)
- Закрылись ли позиции раньше 40 минут?

**Если закрылись < 40 минут:**
- ❌ **ПРОБЛЕМА!** min_holding не сработал
- Нужно найти причину в логах

**Если закрылись >= 40 минут:**
- ✅ **ОК!** min_holding работает правильно
- Но нужно проверить причину закрытия

---

### ВОПРОС 5: Был ли loss_cut слишком агрессивен?

**Искать в CSV:**
```
event_type = "close"
data содержит: "reason=loss_cut"
```

**Проверить:**
- Сколько позиций закрылось по `loss_cut`?
- Какой был PnL при закрытии (`pnl_pct`)?
- Сколько времени держалась позиция (`time_min`)?

**Примеры анализа:**

**Вариант A: Закрылись с малым убытком после 40 минут**
```
close,DOGE-USDT,reason=loss_cut|time_min=42.5|pnl_pct=-0.6%
```
✅ **ОК** - закрылись после min_holding, но loss_cut сработал рано

**Вариант B: Закрылись быстро с большим убытком**
```
close,DOGE-USDT,reason=loss_cut|time_min=5.2|pnl_pct=-2.5%
```
❌ **ПРОБЛЕМА!** - закрылись раньше min_holding (40 минут)

---

## 📊 ПРИМЕРЫ АНАЛИЗА В EXCEL

### ШАГ 1: Открыть CSV в Excel

1. Открыть файл `logs/futures/debug/debug_YYYYMMDD_HHMMSS.csv`
2. Данные разделятся по столбцам автоматически

### ШАГ 2: Добавить фильтры

1. Выделить заголовок (строка 1)
2. Данные → Фильтр
3. Теперь можно фильтровать по столбцам

### ШАГ 3: Анализировать закрытия

**Фильтр по `event_type`:**
- Выбрать только `close`

**Проверить `data`:**
- Найти `reason=` - почему закрыли?
- Найти `time_min=` - сколько минут держалась?
- Найти `pnl_pct=` - какой PnL?

**Пример результата:**
```
timestamp          event_type  symbol      data
11:15:30.123       close       DOGE-USDT   reason=loss_cut|time_min=42.5|pnl_pct=-0.6%
11:22:15.456       close       BTC-USDT    reason=timeout|time_min=90.0|pnl_pct=-1.2%
```

### ШАГ 4: Анализировать min_holding защиту

**Фильтр по `event_type`:**
- Выбрать только `tsl_check`

**Фильтр по `data`:**
- Содержит: `min_holding_BLOCKED`

**Проверить:**
- Сколько раз сработала защита?
- До какого времени блокировала закрытие?

---

## 🎯 ЧТО ИСКАТЬ В CSV

### ✅ ХОРОШИЕ ЗНАКИ:

1. **Много событий `tsl_check` с `min_holding_BLOCKED`:**
   - Защита работает
   - Блокирует закрытие до 40 минут

2. **Позиции закрываются после 40+ минут:**
   - min_holding работает правильно
   - Позиции держатся достаточно долго

3. **Небольшое количество закрытий по `loss_cut`:**
   - loss_cut не слишком агрессивен
   - Позиции успевают восстановиться

### ❌ ПЛОХИЕ ЗНАКИ:

1. **Позиции закрываются раньше 40 минут:**
   - min_holding не работает
   - Нужно найти причину

2. **Много закрытий по `loss_cut` с малым убытком:**
   - loss_cut слишком агрессивен
   - Нужно увеличить порог

3. **Нет событий `min_holding_BLOCKED`:**
   - Защита не работает
   - Нужно проверить код

---

## 📝 ШАБЛОН ОТЧЕТА

### После анализа заполнить:

**1. Общая статистика:**
- Всего открыто позиций: ____
- Всего закрыто позиций: ____
- Время работы: ____ часов

**2. Статистика закрытий:**
- Закрыто по `loss_cut`: ____
- Закрыто по `timeout`: ____
- Закрыто по `tsl`: ____
- Закрыто по `tp`: ____

**3. Время удержания:**
- Среднее время удержания: ____ минут
- Минимальное время: ____ минут
- Максимальное время: ____ минут

**4. PnL:**
- Средний PnL: ____%
- Минимальный PnL: ____%
- Максимальный PnL: ____%

**5. Проблемы:**
- Закрылись раньше 40 минут: ____ позиций
- min_holding защита сработала: Да/Нет
- loss_cut слишком агрессивен: Да/Нет

---

## 🚀 ПОСЛЕ АНАЛИЗА

### Если всё ОК:
✅ Продолжать работу бота  
✅ Мониторить логи дальше

### Если найдены проблемы:
1. **Скопировать CSV файл**
2. **Дать мне увидеть данные**
3. **Я найду ТОЧНУЮ причину**
4. **Исправим код вместе**

---

## ✅ ЧЕКЛИСТ

После часа работы:

- [ ] Открыть CSV файл
- [ ] Проверить структуру (заголовки, события)
- [ ] Посчитать открытые позиции (`tsl_create`)
- [ ] Посчитать закрытые позиции (`close`)
- [ ] Проверить причины закрытий (`reason`)
- [ ] Проверить время удержания (`time_min`)
- [ ] Проверить PnL (`pnl_pct`)
- [ ] Проверить работу min_holding защиты
- [ ] Заполнить шаблон отчета
- [ ] Если есть проблемы - скопировать CSV и дать мне

---

**Готово! После часа работы - анализируем CSV!** 🎯

