# 📋 ПЛАН ИСПРАВЛЕНИЙ - 29.12.2025

## 🎯 ЦЕЛЬ
Исправить все выявленные проблемы для стабильной работы бота:
1. ✅ Проверка инициализации (логирование готовности)
2. ✅ Исправление проблемы с ATR (ATR не найден)
3. ✅ Мониторинг переоткрытия позиций (логирование)
4. ✅ Дедупликация тикеров (частые дубликаты)
5. ✅ Исправление ошибки ConversionMetrics

---

## 1. ✅ ПРОВЕРКА ИНИЦИАЛИЗАЦИИ

### Проблема
- В логах нет сообщения `🟢 Все модули инициализированы — торговля разрешена` (хотя оно есть в коде)
- Нужно убедиться, что все этапы инициализации логируются

### План действий

#### Шаг 1.1: Добавить детальное логирование этапов инициализации
**Файл:** `src/strategies/scalping/futures/orchestrator.py`  
**Строки:** 982-995

**Действия:**
1. Добавить логирование ПЕРЕД `_verify_readiness()`:
   ```python
   logger.info("⏳ Начало проверки готовности модулей...")
   ```

2. Добавить логирование ПОСЛЕ `_verify_readiness()`:
   ```python
   logger.info("✅ Проверка готовности завершена успешно")
   ```

3. Добавить логирование ПЕРЕД установкой флага:
   ```python
   logger.info("⏳ Установка флага готовности всех модулей...")
   ```

4. Добавить логирование ПОСЛЕ установки флага:
   ```python
   logger.info("✅ Флаг готовности установлен (all_modules_ready=True)")
   logger.info("✅ Event initialization_complete установлен")
   ```

5. Убедиться, что сообщение `🟢 Все модули инициализированы — торговля разрешена` логируется (уже есть в строке 995)

#### Шаг 1.2: Улучшить логирование в `_verify_readiness()`
**Файл:** `src/strategies/scalping/futures/orchestrator.py`  
**Метод:** `_verify_readiness()`

**Действия:**
1. Добавить try-except вокруг `_verify_readiness()` с логированием ошибок
2. Логировать результат каждой проверки (candles, indicators, etc.)
3. Логировать общий результат проверки

#### Шаг 1.3: Улучшить логирование в `trading_control_center.py`
**Файл:** `src/strategies/scalping/futures/core/trading_control_center.py`  
**Строки:** 143-167

**Действия:**
1. Добавить логирование ПЕРЕД `await orchestrator.initialization_complete.wait()`
2. Добавить логирование ПОСЛЕ успешного ожидания
3. Улучшить логирование при таймауте

---

## 2. ✅ ИСПРАВЛЕНИЕ ПРОБЛЕМЫ С ATR

### Проблема
- `ATRProvider: ATR не найден для ETH-USDT, возвращаем None`
- ATR сохраняется в DataRegistry, но ATRProvider его не находит
- В логах: `✅ DataRegistry: Сохранены индикаторы для ETH-USDT: ['rsi', 'atr', ...]` - ATR есть в списке

### Анализ кода
- **Сохранение ATR:** `signal_generator.py:1947-1951` - ATR сохраняется с ключом `"atr"` (lowercase)
- **Получение ATR:** `atr_provider.py:72` - ATRProvider ищет `indicators.get("atr")`
- **Проблема:** Возможна race condition или ATR может быть None/0 при сохранении

### План действий

#### Шаг 2.1: Проверить сохранение ATR в DataRegistry
**Файл:** `src/strategies/scalping/futures/signal_generator.py`  
**Строки:** 1946-1951

**Действия:**
1. Добавить проверку, что `indicators.get("atr")` не None перед сохранением:
   ```python
   atr_value = indicators.get("atr")
   if atr_value is not None and isinstance(atr_value, (int, float)) and atr_value > 0:
       indicators_for_registry["atr"] = atr_value
       logger.debug(f"📊 Сохранение ATR для {symbol}: {atr_value:.6f}")
   else:
       logger.warning(f"⚠️ ATR для {symbol} не найден или равен 0: {atr_value}")
   ```

2. Логировать значение ATR перед сохранением
3. Логировать после сохранения: `✅ ATR сохранен в DataRegistry для {symbol}: {atr_value:.6f}`

#### Шаг 2.2: Улучшить получение ATR в ATRProvider
**Файл:** `src/strategies/scalping/futures/indicators/atr_provider.py`  
**Строки:** 64-93

**Действия:**
1. Добавить проверку наличия `_indicators` перед доступом
2. Добавить проверку наличия символа в `_indicators`
3. Добавить детальное логирование всех попыток получения ATR (DEBUG уровень):
   ```python
   logger.debug(f"🔍 ATRProvider: Попытка получить ATR для {symbol}")
   logger.debug(f"🔍 ATRProvider: _indicators доступен: {hasattr(self.data_registry, '_indicators')}")
   if hasattr(self.data_registry, '_indicators'):
       logger.debug(f"🔍 ATRProvider: Символ {symbol} в _indicators: {symbol in self.data_registry._indicators}")
       if symbol in self.data_registry._indicators:
           logger.debug(f"🔍 ATRProvider: Доступные ключи для {symbol}: {list(self.data_registry._indicators[symbol].keys())}")
   ```

4. Логировать, какие ключи проверяются
5. Логировать содержимое `_indicators[symbol]` при отсутствии ATR

#### Шаг 2.3: Добавить синхронизацию ATR через async метод DataRegistry
**Файл:** `src/strategies/scalping/futures/indicators/atr_provider.py`

**Действия:**
1. Добавить async метод `async_get_atr()` для получения через `get_indicator()`:
   ```python
   async def async_get_atr(self, symbol: str, fallback: Optional[float] = None) -> Optional[float]:
       """Получить ATR через async метод DataRegistry"""
       if self.data_registry:
           try:
               atr_value = await self.data_registry.get_indicator(symbol, "atr")
               if atr_value is not None:
                   self.update_atr(symbol, float(atr_value))
                   return float(atr_value)
           except Exception as e:
               logger.debug(f"⚠️ ATRProvider: Ошибка async получения ATR для {symbol}: {e}")
       return self.get_atr(symbol, fallback)
   ```

2. Использовать `async_get_atr()` в async контекстах (например, в ExitAnalyzer)
3. Оставить синхронный `get_atr()` для синхронных контекстов

#### Шаг 2.4: Добавить периодическое обновление кэша ATR
**Файл:** `src/strategies/scalping/futures/orchestrator.py`  
**Строки:** После `_verify_readiness()` (после строки 982)

**Действия:**
1. После `_verify_readiness()` обновить кэш ATR для всех символов:
   ```python
   # Обновляем кэш ATR для всех символов после инициализации
   if self.atr_provider and self.data_registry:
       for symbol in self.scalping_config.symbols:
           try:
               atr_value = await self.data_registry.get_indicator(symbol, "atr")
               if atr_value is not None and isinstance(atr_value, (int, float)) and atr_value > 0:
                   self.atr_provider.update_atr(symbol, float(atr_value))
                   logger.info(f"✅ ATR кэш обновлен для {symbol}: {atr_value:.6f}")
               else:
                   logger.warning(f"⚠️ ATR не найден для {symbol} при обновлении кэша")
           except Exception as e:
               logger.warning(f"⚠️ Ошибка обновления ATR кэша для {symbol}: {e}")
   ```

2. Логировать результат обновления для каждого символа

#### Шаг 2.5: Проверить расчет ATR в IndicatorManager
**Файл:** `src/strategies/scalping/futures/indicators/indicator_manager.py`

**Действия:**
1. Убедиться, что ATR рассчитывается для всех символов
2. Проверить, что результат не None
3. Добавить логирование расчета ATR (если его нет)

#### Шаг 2.6: Добавить автоматическое обновление ATR при сохранении в DataRegistry
**Файл:** `src/strategies/scalping/futures/core/data_registry.py`  
**Метод:** `update_indicators()`

**Действия:**
1. После сохранения индикаторов проверять наличие ATR
2. Если ATR есть и ATRProvider доступен, обновлять кэш:
   ```python
   # Автоматическое обновление ATR кэша
   if "atr" in indicators and hasattr(self, "_atr_provider") and self._atr_provider:
       atr_value = indicators.get("atr")
       if atr_value is not None and isinstance(atr_value, (int, float)) and atr_value > 0:
           self._atr_provider.update_atr(symbol, float(atr_value))
   ```

---

## 3. ✅ МОНИТОРИНГ ПЕРЕОТКРЫТИЯ ПОЗИЦИЙ

### Проблема
- Бот переоткрыл позицию ETH по рынку
- Нет явного логирования переоткрытия
- Нужна диагностика причин

### План действий

#### Шаг 3.1: Улучшить логирование в `_sync_positions_with_exchange`
**Файл:** `src/strategies/scalping/futures/orchestrator.py`  
**Строки:** 2019-2257

**Действия:**
1. Логировать начало синхронизации: `📊 Начало синхронизации позиций с биржей...`
2. Логировать каждую найденную позицию на бирже:
   ```python
   logger.info(f"📊 Позиция на бирже: {symbol}, size={abs_size}, entry={entry_price}, side={position_side}, margin={margin:.2f}")
   ```

3. Логировать существующую позицию в реестре (если есть):
   ```python
   if symbol in self.active_positions:
       existing = self.active_positions[symbol]
       logger.info(f"📊 Позиция в реестре: {symbol}, size={existing.get('size')}, entry={existing.get('entry_price')}, side={existing.get('position_side')}")
   ```

4. При DRIFT_ADD логировать сравнение параметров:
   ```python
   if is_drift_add:
       logger.warning(f"⚠️ DRIFT_ADD {symbol}: Позиция найдена на бирже, но отсутствует в реестре")
       logger.info(f"📊 Параметры позиции на бирже: size={abs_size}, entry={entry_price}, side={position_side}")
       logger.info(f"📊 Регистрируем позицию в PositionRegistry...")
   ```

5. Логировать регистрацию в PositionRegistry:
   ```python
   logger.info(f"✅ Позиция {symbol} зарегистрирована в PositionRegistry: entry={effective_price}, side={position_side}, size={size_in_coins}")
   ```

#### Шаг 3.2: Добавить проверку существующей позиции перед открытием новой
**Файл:** `src/strategies/scalping/futures/coordinators/signal_coordinator.py` или `entry_manager.py`

**Действия:**
1. Перед открытием позиции проверять наличие в `active_positions`
2. Логировать: `⚠️ Попытка открыть позицию {symbol}, но позиция уже существует`
3. Сравнивать параметры (entry_price, size, side)
4. Логировать решение: открыть новую или использовать существующую

#### Шаг 3.3: Добавить логирование в `_load_existing_positions`
**Файл:** `src/strategies/scalping/futures/orchestrator.py`  
**Строки:** 1533-1750

**Действия:**
1. Логировать начало: `📊 Загрузка существующих позиций с биржи...`
2. Логировать каждую найденную позицию:
   ```python
   logger.info(f"📊 Найдена позиция: {symbol}, size={size}, entry={entry_price}, side={side}")
   ```

3. Логировать инициализацию TrailingStopLoss:
   ```python
   logger.info(f"✅ TrailingStopLoss инициализирован для {symbol}")
   ```

4. Логировать завершение: `✅ Загрузка позиций завершена: {count} позиций`

#### Шаг 3.4: Добавить метрики переоткрытия
**Файл:** `src/strategies/scalping/futures/orchestrator.py`

**Действия:**
1. Добавить счетчик `self.reopened_positions = {}` в `__init__`
2. Увеличивать счетчик при DRIFT_ADD:
   ```python
   if is_drift_add:
       if symbol not in self.reopened_positions:
           self.reopened_positions[symbol] = 0
       self.reopened_positions[symbol] += 1
       logger.warning(f"📊 Переоткрытие позиции {symbol}: счетчик={self.reopened_positions[symbol]}")
   ```

3. Логировать статистику периодически (каждые 5 минут)

#### Шаг 3.5: Проверить логику синхронизации позиций
**Файл:** `src/strategies/scalping/futures/orchestrator.py`  
**Строки:** 2036-2257

**Действия:**
1. Проверить, что `is_drift_add` определяется корректно
2. Проверить, что `active_positions` обновляется до проверки
3. Проверить, что `PositionRegistry` синхронизируется корректно
4. Добавить проверку на дубликаты перед регистрацией

---

## 4. ✅ ДЕДУПЛИКАЦИЯ ТИКЕРОВ

### Проблема
- Тикеры обрабатываются несколько раз подряд с одинаковой ценой
- Много дубликатов тикеров ETH-USDT в логах

### План действий

#### Шаг 4.1: Добавить дедупликацию тикеров
**Файл:** `src/strategies/scalping/futures/coordinators/websocket_coordinator.py`  
**Метод:** `handle_ticker_data()`

**Действия:**
1. Добавить кэш последних тикеров в `__init__`:
   ```python
   self._last_tickers: Dict[str, Dict[str, Any]] = {}  # symbol -> {price, timestamp}
   self._ticker_dedup_threshold = 0.0001  # Минимальное изменение цены для обработки (0.01%)
   ```

2. В `handle_ticker_data()` проверять, изменилась ли цена:
   ```python
   # Дедупликация тикеров
   if symbol in self._last_tickers:
       last_price = self._last_tickers[symbol].get("price")
       last_time = self._last_tickers[symbol].get("timestamp", 0)
       current_time = time.time()
       
       # Пропускаем, если цена не изменилась и прошло менее 1 секунды
       if last_price == price and (current_time - last_time) < 1.0:
           logger.debug(f"⏭️ Тикер пропущен (дубликат): {symbol} price={price:.2f}")
           return
       
       # Пропускаем, если изменение цены меньше порога
       if last_price and abs(price - last_price) / last_price < self._ticker_dedup_threshold:
           logger.debug(f"⏭️ Тикер пропущен (минимальное изменение): {symbol} price={price:.2f} (изменение < {self._ticker_dedup_threshold*100:.2f}%)")
           return
   
   # Обновляем кэш
   self._last_tickers[symbol] = {"price": price, "timestamp": time.time()}
   ```

3. Логировать пропущенные дубликаты (DEBUG уровень)

---

## 5. ✅ ИСПРАВЛЕНИЕ ОШИБКИ ConversionMetrics

### Проблема
- `⚠️ TCC: Ошибка проверки метрик и алертов: 'ConversionMetrics' object has no attribute 'get_win_rate'`

### План действий

#### Шаг 5.1: Исправить вызов метода в `_check_metrics_and_alerts`
**Файл:** `src/strategies/scalping/futures/core/trading_control_center.py`  
**Метод:** `_check_metrics_and_alerts()`

**Действия:**
1. Найти вызов `get_win_rate()` в методе
2. Проверить, какой метод должен вызываться вместо него
3. Исправить вызов или добавить проверку наличия метода:
   ```python
   if hasattr(self.conversion_metrics, "get_win_rate"):
       win_rate = self.conversion_metrics.get_win_rate()
   else:
       logger.debug("⚠️ ConversionMetrics не имеет метода get_win_rate")
   ```

4. Или использовать правильный метод из ConversionMetrics

---

## 📊 ПРИОРИТЕТЫ ИСПРАВЛЕНИЙ

### 🔴 КРИТИЧНО (Сделать первым)
1. **Проверка инициализации** (Шаг 1.1-1.2) - 15 мин
2. **Исправление ATR** (Шаг 2.1-2.2, 2.4) - 30 мин
3. **Логирование переоткрытия** (Шаг 3.1) - 20 мин

### 🟡 ВАЖНО (Сделать вторым)
4. **Улучшение получения ATR** (Шаг 2.3, 2.6) - 20 мин
5. **Проверка существующей позиции** (Шаг 3.2-3.3) - 15 мин
6. **Дедупликация тикеров** (Шаг 4.1) - 15 мин

### 🟢 ЖЕЛАТЕЛЬНО (Сделать третьим)
7. **Метрики переоткрытия** (Шаг 3.4-3.5) - 15 мин
8. **Проверка расчета ATR** (Шаг 2.5) - 10 мин
9. **Исправление ConversionMetrics** (Шаг 5.1) - 10 мин

---

## ✅ ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### 1. Инициализация
- ✅ Все этапы инициализации видны в логах
- ✅ Сообщение `🟢 Все модули инициализированы` появляется
- ✅ Можно отследить проблемы инициализации

### 2. ATR
- ✅ ATR находится в DataRegistry
- ✅ ATRProvider получает ATR корректно
- ✅ Нет `ATR не найден` в логах
- ✅ ATR кэш обновляется автоматически

### 3. Переоткрытие позиций
- ✅ Видно, когда и почему происходит переоткрытие
- ✅ Можно отследить причины
- ✅ Есть метрики для анализа

### 4. Тикеры
- ✅ Нет дубликатов тикеров в логах
- ✅ Тикеры обрабатываются только при изменении цены
- ✅ Меньше нагрузки на систему

### 5. ConversionMetrics
- ✅ Нет ошибок в логах
- ✅ Метрики работают корректно

---

## 📝 ЗАМЕТКИ

- Все изменения должны быть обратно совместимы
- Логирование должно быть на DEBUG уровне для детальной диагностики
- Критические моменты должны логироваться на INFO уровне
- Ошибки должны логироваться на WARNING/ERROR уровне




