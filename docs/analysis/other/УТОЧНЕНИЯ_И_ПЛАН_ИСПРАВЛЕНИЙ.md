# 🎯 УТОЧНЕНИЯ КОНЦЕПЦИИ И ПЛАН ИСПРАВЛЕНИЙ

**Дата:** 2025-11-07

---

## 📋 УТОЧНЕНИЯ ОТ ПОЛЬЗОВАТЕЛЯ

### 1. КОНЦЕПЦИЯ TP/SL/TRAILING STOP

**Как должно работать:**
- **При открытии позиции:**
  - TP = 2-3% (неважно LONG или SHORT)
  - SL = 3-5% (неважно LONG или SHORT)
  - Бот считывает цены, ордера, видит PnL - система работает

- **Trailing Stop:**
  - Срабатывает **ТОЛЬКО** если идет тренд и позиция приближается к закрытию
  - **НО:** Если тренд есть - продлевать TP, а не закрывать по Trailing Stop!
  - Зачем терять на комиссии? Просто продлим TP для позиции где идет тренд

**Вывод:**
- Trailing Stop = защита от разворота, НЕ основной механизм закрытия
- В тренде: продлевать TP (например, 2% → 3% → 5%)
- В тренде: Trailing Stop НЕ должен закрывать позицию, только защищать от разворота

---

### 2. ИНДИКАТОРЫ

**Вопрос:** Нужно ли их улучшать/убавить/добавить?

**Текущие индикаторы:**
- RSI (14 период)
- MACD (12/26/9)
- Bollinger Bands (20 период, 2 std)
- EMA (12/26)
- SMA (20)
- ATR (14)

**Анализ:**
- Индикаторы работают, но могут давать ложные сигналы
- Нужно добавить фильтр по тренду на старших таймфреймах
- Возможно, добавить Volume индикатор для подтверждения

**Рекомендации:**
- ✅ Оставить текущие индикаторы
- ✅ Добавить фильтр по тренду (SMA на 15m/1h)
- ✅ Улучшить фильтрацию сигналов (не открывать против тренда)
- ⚠️ Возможно добавить Volume для подтверждения (но не обязательно)

---

### 3. СИСТЕМА БАЛАНСА

**Проблема:**
- При балансе $1000 позиции по $30 не дают работать остальному балансу
- 4 торговые пары (BTC, ETH, SOL, DOGE)
- Нужно пересмотреть градацию мин/мах сумм в ордерах для каждого режима

**Текущие настройки (small профиль, баланс ~$1000):**
```yaml
base_position_usd: 25.0
min_position_usd: 15.0
max_position_usd: 50.0
max_open_positions: 2
max_position_percent: 20.0
```

**Проблема:**
- 4 пары × $30 = $120 (12% баланса)
- Но с leverage 3x = $360 в риске (36% баланса)
- Остается $640 свободных, но бот не может открыть больше позиций

**Решение:**
- Уменьшить размер позиций для каждой пары
- Увеличить `max_open_positions` до 4 (по одной на пару)
- Пересчитать `base_position_usd` для работы с 4 парами

**Новые настройки:**
```yaml
base_position_usd: 15.0  # Было 25.0
min_position_usd: 10.0   # Было 15.0
max_position_usd: 25.0   # Было 50.0
max_open_positions: 4    # Было 2 (по одной на пару)
max_position_percent: 15.0  # Было 20.0 (с leverage 3x = 45% в риске)
```

**Расчет:**
- 4 пары × $15 = $60 (6% баланса)
- С leverage 3x = $180 в риске (18% баланса)
- Остается $820 свободных для других позиций

---

## 🔧 ПЛАН ИСПРАВЛЕНИЙ

### Приоритет 1: КРИТИЧНО

#### 1.1. Исправить Trailing Stop (новая концепция)

**Файл:** `src/strategies/scalping/futures/indicators/trailing_stop_loss.py`

**Изменения:**
- Trailing Stop НЕ должен закрывать позиции в тренде
- Trailing Stop работает только как защита от разворота
- Если `market_regime == "trending"` и `profit_pct > 0.5%`: НЕ закрывать по Trailing Stop
- Если `market_regime == "trending"` и позиция в прибыли: продлевать TP вместо закрытия

**Логика:**
```python
def should_close_position(...):
    # Если тренд и позиция в прибыли - НЕ закрывать по Trailing Stop
    if market_regime == "trending" and profit_pct > 0.5%:
        return False  # Продлеваем TP, не закрываем
    
    # Если позиция в убытке > 2% - закрывать строго
    if profit_pct < -2.0:
        return True
    
    # Обычная логика Trailing Stop (только для ranging/choppy)
    ...
```

---

#### 1.2. Продление TP в тренде

**Файл:** `src/strategies/scalping/futures/position_manager.py`

**Изменения:**
- В методе `_check_tp_only()`: если тренд и позиция в прибыли, продлевать TP
- Вместо закрытия на 2%, увеличивать TP до 3% → 5%

**Логика:**
```python
async def _check_tp_only(...):
    if pnl_percent >= tp_percent:
        # Если тренд - продлеваем TP вместо закрытия
        if market_regime == "trending" and pnl_percent < 5.0:
            new_tp = min(tp_percent * 1.5, 5.0)  # Увеличиваем TP
            logger.info(f"📈 Тренд: продлеваем TP {tp_percent}% → {new_tp}%")
            # Обновляем TP в позиции
            return  # Не закрываем
        
        # Обычное закрытие по TP
        await self._close_position_by_reason(position, "tp")
```

---

#### 1.3. Исправить SL для убыточных позиций

**Файл:** `src/strategies/scalping/futures/indicators/trailing_stop_loss.py`

**Изменения:**
- Для убыточных позиций: более строгий стоп
- Если `profit_pct < -1%`: `current_trail = 0.5%`
- Если `profit_pct < -2%`: закрывать немедленно

---

### Приоритет 2: ВАЖНО

#### 2.1. Добавить фильтр по тренду

**Файл:** `src/strategies/scalping/futures/signal_generator.py`

**Изменения:**
- Проверять тренд на 15m/1h перед открытием
- Не открывать LONG если цена ниже SMA(50) на старшем таймфрейме
- Не открывать SHORT если цена выше SMA(50) на старшем таймфрейме

---

#### 2.2. Пересмотреть систему баланса

**Файл:** `config/config_futures.yaml`

**Изменения:**
```yaml
balance_profiles:
  small:  # $100 - $1500
    threshold: 1500.0
    base_position_usd: 15.0   # Было 25.0
    min_position_usd: 10.0    # Было 15.0
    max_position_usd: 25.0    # Было 50.0
    max_open_positions: 4     # Было 2 (по одной на пару)
    max_position_percent: 15.0  # Было 20.0
```

---

#### 2.3. Исправить логику продления времени

**Файл:** `src/strategies/scalping/futures/orchestrator.py`

**Изменения:**
- Продлевать время ТОЛЬКО если `profit_pct > 0.5%` (не 0.1%)
- Для убыточных позиций: строго 30 минут (не продлевать)

---

### Приоритет 3: УЛУЧШЕНИЯ

#### 3.1. Улучшить фильтрацию сигналов

- Повысить `min_score_threshold` для ranging режима
- Улучшить проверку конфликтов индикаторов

---

## ✅ ЧЕКЛИСТ ПРОВЕРКИ

- [ ] Trailing Stop не закрывает позиции в тренде
- [ ] TP продлевается в тренде (2% → 3% → 5%)
- [ ] SL работает строго для убыточных позиций (-2% = закрытие)
- [ ] Фильтр по тренду на старших таймфреймах
- [ ] Система баланса пересчитана для 4 пар
- [ ] Продление времени только для прибыльных позиций (>0.5%)

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

После исправлений:
- **Сокращение убыточных сделок** с 13 до 5-7
- **Увеличение прибыли** за счет продления TP в тренде
- **Сокращение времени удержания** убыточных позиций
- **Лучшее использование баланса** для 4 пар

**Ожидаемая прибыльность:** +3-7% в неделю (вместо -3%)

---

**Автор:** AI Assistant  
**Дата:** 2025-11-07




