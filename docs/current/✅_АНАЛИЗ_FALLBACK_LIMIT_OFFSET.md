# АНАЛИЗ FALLBACK LIMIT OFFSET

**Дата:** 23 ноября 2025  
**Проблема:** `limit_order_config keys=[]` - конфиг пустой

---

## 📊 ПРОБЛЕМА

### В логах:
```
limit_order_config keys=[], by_symbol exists=False, by_regime exists=False
⚠️ FALLBACK: Используется глобальный offset из конфига: 0.0%
```

### Что это означает:
- `limit_order_config` пустой словарь (`keys=[]`)
- `by_symbol` не читается (`exists=False`)
- `by_regime` не читается (`exists=False`)
- Используется fallback `0.0%` вместо адаптивных параметров

---

## 🔍 ПРИЧИНА

### Возможные причины:
1. ❌ `order_executor` не загружается из YAML правильно
2. ❌ Структура конфига не соответствует ожидаемой
3. ❌ Pydantic не парсит вложенные структуры правильно
4. ❌ `scalping_config.order_executor` не существует или пустой

---

## ✅ РЕШЕНИЕ

### Добавлено логирование:
- ✅ Логирование структуры `order_executor_config`
- ✅ Логирование структуры `limit_order_config`
- ✅ Проверка доступных атрибутов `scalping_config`

### Следующие шаги:
1. Проверить логи после перезапуска
2. Убедиться, что `order_executor` загружается из YAML
3. Исправить чтение конфига, если нужно

---

**Статус:** 🔍 ИССЛЕДУЕТСЯ

