# АНАЛИЗ: ПРОБЛЕМА РАЗМЕЩЕНИЯ ЛИМИТНЫХ ОРДЕРОВ ДАЛЕКО ОТ РЫНКА

**Дата:** 2025-12-18  
**Статус:** 🔍 АНАЛИЗ БЕЗ КОДИНГА

---

## 🤔 ВОПРОС ПОЛЬЗОВАТЕЛЯ

**Проблема:** Бот ставит лимитные ордера далеко от рыночной цены, потом отменяет и открывает по рынку.

**Вопросы:**
1. Почему ордер ставится так далеко?
2. Как ведут себя параметры при этом?
3. Может ли быть ошибка в логике генерации сигналов?
4. Может ли быть неправильная интерпретация параметров фильтров/индикаторов?
5. Может ли быть неправильное понимание сигналов между модулями?

---

## 📊 АНАЛИЗ ЛОГИКИ РАСЧЕТА ЛИМИТНОЙ ЦЕНЫ

### 1. ЦЕПОЧКА РАСЧЕТА ЦЕНЫ

#### Шаг 1: Генерация сигнала
**Файл:** `signal_generator.py`
- Сигнал генерируется с ценой `signal["price"]`
- **ВОПРОС:** Откуда берется эта цена? Из свечи? Из индикатора? Из стакана?

#### Шаг 2: Выполнение сигнала
**Файл:** `signal_coordinator.py:execute_signal_from_price()`
- Получает `price` как параметр
- Передает в `order_executor.execute_signal(signal, position_size)`

#### Шаг 3: Расчет лимитной цены
**Файл:** `order_executor.py:_calculate_limit_price()`
- **НЕ использует** `signal["price"]` напрямую!
- Получает цены из `get_price_limits()`:
  - `best_bid` - лучшая цена покупки из стакана
  - `best_ask` - лучшая цена продажи из стакана
  - `current_price` - текущая цена из тикера
- Рассчитывает лимитную цену на основе `best_bid`/`best_ask` + offset

---

## 🔍 ПОТЕНЦИАЛЬНЫЕ ПРОБЛЕМЫ

### ПРОБЛЕМА #1: РАССИНХРОНИЗАЦИЯ МЕЖДУ ЦЕНОЙ СИГНАЛА И РЫНОЧНОЙ ЦЕНОЙ

**Сценарий:**
1. Сигнал генерируется с ценой `signal["price"] = 2840.27` (из свечи/индикатора)
2. Проходит время (0.5-2 секунды)
3. Рыночная цена изменилась: `current_price = 2809.91`
4. Лимитная цена рассчитывается от `best_ask = 2809.91`
5. **Результат:** Ордер ставится по `2809.91`, но сигнал был на `2840.27`
6. **Разница:** 30.36 USDT (1.07%) - ордер далеко от цены сигнала!

**Причина:**
- Цена сигнала берется из **прошлых данных** (свеча закрылась)
- Лимитная цена рассчитывается из **текущих данных** (стакан сейчас)
- Между ними может быть задержка и движение рынка

---

### ПРОБЛЕМА #2: НЕПРАВИЛЬНОЕ ИСПОЛЬЗОВАНИЕ BEST_BID/BEST_ASK

**Текущая логика (из кода):**

**Для BUY:**
```python
if offset_percent == 0.0:
    limit_price = best_ask  # По best_ask
else:
    limit_price = best_ask * (1 + offset_percent / 100.0)  # Выше best_ask
```

**Для SELL:**
```python
if offset_percent == 0.0:
    limit_price = best_bid  # По best_bid
else:
    limit_price = best_bid * (1 - offset_percent / 100.0)  # Ниже best_bid
```

**Проблема:**
- Если `offset_percent = 0.0` → ордер ставится **ровно** по `best_ask`/`best_bid`
- Но если цена **уже ушла**, ордер может быть **ниже** текущей цены (для BUY)
- Это означает, что ордер **не исполнится** до тех пор, пока цена не вернется

---

### ПРОБЛЕМА #3: ОТРИЦАТЕЛЬНЫЙ OFFSET ИЛИ НЕПРАВИЛЬНАЯ ЛОГИКА

**Из кода видно:**
```python
# Для BUY: если offset_percent < 0, то limit_price < best_ask
# Это ставит ордер НИЖЕ best_ask - ордер не исполнится!
```

**Пример:**
- `best_ask = 2809.91`
- `offset_percent = -0.1` (отрицательный!)
- `limit_price = 2809.91 * (1 - 0.1/100) = 2807.10`
- **Результат:** Ордер на 2.81 USDT ниже best_ask - не исполнится!

---

### ПРОБЛЕМА #4: ОГРАНИЧЕНИЕ MAX_BUY_PRICE/MIN_SELL_PRICE

**Из `futures_client.py:get_price_limits()`:**
```python
max_buy_price = best_ask * 1.001  # 0.1% выше best_ask
min_sell_price = best_bid * 0.999  # 0.1% ниже best_bid
```

**Проблема:**
- Если рассчитанная `limit_price > max_buy_price` → ограничивается до `max_buy_price`
- Но если `best_ask` **устарел** или **неправильный**, `max_buy_price` тоже будет неправильным
- Ордер может быть ограничен до неправильной цены

---

### ПРОБЛЕМА #5: FALLBACK НА CURRENT_PRICE * 0.999

**Из кода:**
```python
# Fallback если не получили price_limits
limit_price = current_price * 0.999  # -0.1% от текущей цены
```

**Проблема:**
- Для **BUY** используется `current_price * 0.999` (ниже текущей цены!)
- Это ставит ордер **ниже** рынка - ордер не исполнится!
- Для BUY нужно `current_price * 1.001` (выше текущей цены)

---

## 🎯 ГИПОТЕЗЫ О ПРИЧИНАХ

### ГИПОТЕЗА #1: ЦЕНА СИГНАЛА УСТАРЕЛА

**Сценарий:**
1. Сигнал генерируется на основе **закрытой свечи** (цена закрытия)
2. Проходит время (1-5 секунд)
3. Рынок двинулся
4. Лимитная цена рассчитывается от **текущей** цены
5. **Результат:** Ордер далеко от цены сигнала

**Проверка:** Нужно сравнить `signal["price"]` с `current_price` в момент размещения ордера

---

### ГИПОТЕЗА #2: НЕПРАВИЛЬНЫЙ OFFSET ИЗ КОНФИГА

**Сценарий:**
1. В конфиге установлен `limit_offset_percent = -0.1` (отрицательный!)
2. Для BUY это ставит ордер **ниже** best_ask
3. Ордер не исполняется
4. Через время отменяется и заменяется на market

**Проверка:** Нужно проверить конфиг на отрицательные offset

---

### ГИПОТЕЗА #3: BEST_BID/BEST_ASK УСТАРЕЛИ

**Сценарий:**
1. `get_price_limits()` получает `best_bid`/`best_ask` из стакана
2. Но стакан может быть **неактуальным** (задержка API)
3. Рассчитанная цена основана на **устаревших** данных
4. Рынок уже ушел дальше

**Проверка:** Нужно проверить актуальность `best_bid`/`best_ask` перед расчетом

---

### ГИПОТЕЗА #4: НЕПРАВИЛЬНАЯ ЛОГИКА ДЛЯ SELL

**Сценарий:**
1. Для SELL используется `best_bid * (1 - offset_percent / 100.0)`
2. Если `offset_percent = 0.0` → `limit_price = best_bid`
3. Но для SELL нужно продавать по **best_ask** (выше), а не по best_bid!
4. Ордер ставится **ниже** рынка - не исполняется

**Проверка:** Нужно проверить логику для SELL

---

### ГИПОТЕЗА #5: КОНФЛИКТ МЕЖДУ SIGNAL["PRICE"] И CURRENT_PRICE

**Сценарий:**
1. Сигнал генерируется с `signal["price"] = 2840.27`
2. Но `current_price` в момент размещения = `2809.91`
3. Лимитная цена рассчитывается от `current_price`, а не от `signal["price"]`
4. **Результат:** Ордер далеко от цены сигнала

**Проверка:** Нужно использовать `signal["price"]` как основу для расчета лимитной цены

---

## 📋 ЧТО НУЖНО ПРОВЕРИТЬ

### 1. Откуда берется `signal["price"]`?
- Из какой свечи? (открытие/закрытие/хай/лоу)
- Из какого индикатора?
- Когда генерируется сигнал?

### 2. Как используется `signal["price"]` при расчете лимитной цены?
- Используется ли вообще?
- Или всегда используется `current_price` из стакана?

### 3. Какие значения `offset_percent` в конфиге?
- Положительные или отрицательные?
- Разные для разных режимов?
- Разные для разных символов?

### 4. Актуальность `best_bid`/`best_ask`
- Когда последний раз обновлялись?
- Может ли быть задержка?

### 5. Логика для SELL
- Правильно ли используется `best_bid` vs `best_ask`?
- Правильно ли рассчитывается offset?

---

## 💡 РЕКОМЕНДАЦИИ ДЛЯ ИСПРАВЛЕНИЯ

### 1. Использовать `signal["price"]` как основу
- Если `signal["price"]` актуальна (недавно получена)
- Рассчитывать лимитную цену от `signal["price"]`, а не от `current_price`

### 2. Проверять актуальность цен
- Сравнивать `signal["price"]` с `current_price`
- Если разница > 0.5% → использовать market order вместо limit

### 3. Исправить логику для SELL
- Для SELL использовать `best_ask` (выше), а не `best_bid`
- Offset должен быть положительным для SELL

### 4. Исправить fallback
- Для BUY: `current_price * 1.001` (выше)
- Для SELL: `current_price * 0.999` (ниже)

### 5. Добавить проверку перед размещением
- Если рассчитанная `limit_price` далеко от `current_price` (> 0.2%)
- Логировать предупреждение
- Рассмотреть использование market order

---

## ⚠️ ВАЖНЫЕ ВОПРОСЫ ДЛЯ ПРОВЕРКИ

1. **Откуда берется цена сигнала?** (signal_generator.py)
2. **Используется ли signal["price"] при расчете лимитной цены?** (order_executor.py)
3. **Какие значения offset_percent в конфиге?** (config_futures.yaml)
4. **Правильно ли работает логика для SELL?** (order_executor.py:_calculate_limit_price)
5. **Есть ли проверка актуальности цен перед размещением?** (signal_coordinator.py)

---

**Следующий шаг:** Проверить код на наличие этих проблем и исправить логику расчета лимитной цены.
