# АНАЛИЗ ЛОГОВ ПОСЛЕ ПЕРЕЗАПУСКА

**Дата:** 23 ноября 2025  
**Время:** 14:17-14:20

---

## ✅ ПОЗИТИВНЫЕ МОМЕНТЫ

### 1. Позиции открыты:
- ✅ BTC-USDT SHORT открыта (14:17:55)
- ✅ DOGE-USDT SHORT открыта (14:18:49)

### 2. Лимитные ордера размещены:
- ✅ DOGE-USDT лимитный ордер размещен успешно (14:18:43)
- ✅ Нет ошибки 51006 при размещении DOGE-USDT

### 3. Расчет лимитной цены работает:
- ✅ Лимиты цены получаются из биржи
- ✅ Цена корректируется если вне лимитов

---

## ⚠️ ПРОБЛЕМЫ

### 1. Fallback все еще используется:
```
⚠️ FALLBACK: Используется глобальный offset из конфига: 0.0% 
(per-symbol+regime и per-regime не найдены для BTC-USDT, regime=ranging)

⚠️ FALLBACK: Используется глобальный offset из конфига: 0.0% 
(per-symbol+regime и per-regime не найдены для DOGE-USDT, regime=ranging)
```

**Проблема:** Настройки `by_symbol` не читаются из конфига

**Ожидалось:**
- BTC-USDT ranging: offset=0.08%
- DOGE-USDT ranging: offset=0.15%

**Получено:**
- offset=0.0% (fallback)

---

## ✅ ДОБАВЛЕНО ЛОГИРОВАНИЕ

Добавлено детальное логирование для диагностики:
1. Проверка типа и ключей `limit_order_config`
2. Проверка типа и ключей `by_symbol_dict`
3. Проверка типа и ключей `symbol_config`
4. Подробная информация о том, почему используется fallback

---

## 🔍 СЛЕДУЮЩИЕ ШАГИ

После перезапуска бота проверить логи:
1. Что показывает `limit_order_config keys`?
2. Что показывает `by_symbol_dict type` и `keys`?
3. Что показывает `symbol_config type` и `keys`?
4. Существует ли `by_symbol` в `limit_order_config`?

---

**Статус:** ⚠️ ТРЕБУЕТСЯ ДИАГНОСТИКА ПОСЛЕ ПЕРЕЗАПУСКА
