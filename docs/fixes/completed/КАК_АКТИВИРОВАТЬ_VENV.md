# 🔧 КАК АКТИВИРОВАТЬ ВИРТУАЛЬНОЕ ОКРУЖЕНИЕ

## 📋 Что это за предупреждение?

Launcher проверяет, активировано ли виртуальное окружение через переменную `VIRTUAL_ENV`.

**Ваш случай:**
- ✅ Виртуальное окружение **существует** (папка `venv/` есть)
- ❌ Виртуальное окружение **не активировано** (переменная `VIRTUAL_ENV` не установлена)

---

## 🚀 КАК АКТИВИРОВАТЬ

### Для PowerShell (рекомендуется):
```powershell
.\venv\Scripts\Activate.ps1
```

### Для Command Prompt (cmd):
```cmd
venv\Scripts\activate.bat
```

### Для Git Bash:
```bash
source venv/Scripts/activate
```

---

## ✅ ПРОВЕРКА АКТИВАЦИИ

После активации вы увидите `(venv)` в начале строки терминала:

```
(venv) PS C:\Users\krivo\simple trading bot okx>
```

Также можно проверить переменную:
```powershell
echo $env:VIRTUAL_ENV
# Должно показать путь к venv
```

---

## ⚠️ МОЖНО ЛИ ПРОДОЛЖИТЬ БЕЗ АКТИВАЦИИ?

**Да, можно**, но **не рекомендуется**:

### ❌ Проблемы без venv:
- Конфликты версий пакетов
- Установка пакетов в системный Python
- Проблемы с зависимостями

### ✅ Преимущества с venv:
- Изолированная среда
- Чистые зависимости
- Безопасность

---

## 🎯 РЕКОМЕНДАЦИЯ

**Всегда активируйте venv перед запуском бота!**

1. Откройте терминал в папке проекта
2. Активируйте venv: `.\venv\Scripts\Activate.ps1`
3. Запустите бота: `python run.py`

---

## 📝 БЫСТРАЯ АКТИВАЦИЯ

Создайте файл `activate_venv.bat` в корне проекта:

```batch
@echo off
call venv\Scripts\activate.bat
python run.py
```

Тогда можно просто запускать `activate_venv.bat` для активации и запуска.

---

**Дата:** 2025-01-XX
**Статус:** ✅ Инструкция готова








