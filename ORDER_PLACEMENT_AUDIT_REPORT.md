# 🔍 ПОЛНЫЙ АУДИТ РАЗМЕЩЕНИЯ ОРДЕРОВ

**Дата:** 04.12.2025  
**Проблема:** Бот размещает лимитные ордера слишком далеко от текущей цены, затем отменяет их и открывает по рынку, теряя комиссии

---

## 📊 НАЙДЕННЫЕ ПРОБЛЕМЫ

### 🔴 ПРОБЛЕМА #1: Неправильный расчет `max_buy_price` и `min_sell_price`

**Файл:** `src/clients/futures_client.py`, метод `get_price_limits()` (строки 463-468)

**Текущая логика:**
```python
max_buy_price = best_ask + (spread * 0.5)  # 50% от спреда выше best_ask
min_sell_price = best_bid - (spread * 0.5)  # 50% от спреда ниже best_bid
```

**Проблема:**
- Если спред большой (например, 0.5% для BTC), то `max_buy_price` будет на **0.25% выше best_ask**
- Это означает, что лимитный ордер может быть размещен на **0.25% + offset (0.05-0.12%) = 0.3-0.37%** выше текущей цены
- Для скальпинга это **слишком далеко** - ордер не исполнится и будет отменен через 30-60 секунд

**Пример:**
- BTC: best_ask = $90,000, best_bid = $89,550, spread = $450 (0.5%)
- max_buy_price = $90,000 + ($450 * 0.5) = **$90,225** (0.25% выше best_ask)
- С offset 0.12%: limit_price = $90,225 * 1.0012 = **$90,333** (0.37% выше best_ask!)
- Ордер размещается на **$333 выше текущей цены** - слишком далеко!

---

### 🔴 ПРОБЛЕМА #2: Слишком большой offset в конфиге

**Файл:** `config/config_futures.yaml` (строки 1050, 1064, 1068)

**Текущие значения:**
- Базовый: `limit_offset_percent: 0.05` (0.05%)
- Ranging: `limit_offset_percent: 0.12` (0.12%)
- Choppy: `limit_offset_percent: 0.10` (0.10%)

**Проблема:**
- Для скальпинга offset 0.12% слишком большой
- В сочетании с проблемой #1 получается: 0.25% (spread) + 0.12% (offset) = **0.37%** от текущей цены
- Ордер размещается слишком далеко и не исполняется

**Рекомендация:**
- Базовый: 0.01-0.02% (для быстрого исполнения)
- Ranging: 0.03-0.05% (для maker fee)
- Choppy: 0.02-0.03% (для быстрого исполнения)

---

### 🔴 ПРОБЛЕМА #3: Устаревшие данные из стакана

**Файл:** `src/strategies/scalping/futures/order_executor.py`, метод `_calculate_limit_price()` (строки 654-691)

**Текущая логика:**
- Для SELL проверяется актуальность `best_bid` (если разница > 0.5% от current_price, используется current_price)
- Для BUY такой проверки **НЕТ** - всегда используется `best_ask`

**Проблема:**
- Если `best_ask` устарел (например, из-за задержки API), ордер размещается по устаревшей цене
- Это может привести к размещению ордера слишком далеко от реальной текущей цены

---

### 🔴 ПРОБЛЕМА #4: Отмена ордеров через 30-60 секунд

**Файл:** `src/strategies/scalping/futures/coordinators/order_coordinator.py` (строки 93-94)

**Текущая логика:**
- `max_wait_seconds: 60` (базовый)
- `max_wait_seconds: 45` (trending/ranging)
- `max_wait_seconds: 30` (choppy)

**Проблема:**
- Если ордер размещен слишком далеко (из-за проблем #1-3), он не исполняется
- Через 30-60 секунд ордер отменяется и заменяется на рыночный
- **Потеря комиссии:** maker fee (0.02%) при отмене + taker fee (0.05%) при рыночном = **0.07%** вместо 0.02%

**Экономический эффект:**
- При 200 сделках/день и 20% отмененных ордеров = 40 отмен
- Потеря: 40 * 0.05% = **2% от оборота** (около $10-15/день при обороте $500-750)

---

### 🔴 ПРОБЛЕМА #5: Неправильная проверка лимитов биржи

**Файл:** `src/clients/futures_client.py`, метод `get_price_limits()` (строки 471-478)

**Текущая логика:**
```python
if max_buy_price > best_ask * 1.01:  # Не более 1% выше best_ask
    max_buy_price = best_ask * 1.01
```

**Проблема:**
- Лимит 1% слишком большой для скальпинга
- Ордер может быть размещен на 1% выше текущей цены - это **очень далеко**

---

## ✅ РЕШЕНИЯ

### Решение #1: Исправить расчет `max_buy_price` и `min_sell_price`

**Файл:** `src/clients/futures_client.py`, метод `get_price_limits()`

**Изменить:**
```python
# БЫЛО:
max_buy_price = best_ask + (spread * 0.5)  # 50% от спреда
min_sell_price = best_bid - (spread * 0.5)  # 50% от спреда

# СТАЛО:
# Используем минимальный offset для гарантии исполнения
max_buy_price = best_ask * 1.001  # 0.1% выше best_ask (максимум)
min_sell_price = best_bid * 0.999  # 0.1% ниже best_bid (минимум)
```

**Обоснование:**
- Для скальпинга нужны ордера близко к рынку (0.1% максимум)
- Это гарантирует быстрое исполнение и экономию комиссий

---

### Решение #2: Уменьшить offset в конфиге

**Файл:** `config/config_futures.yaml`

**Изменить:**
```yaml
order_executor:
  limit_order:
    limit_offset_percent: 0.01  # ✅ ИСПРАВЛЕНО: 0.01% (было 0.05%) для быстрого исполнения
    by_regime:
      trending:
        limit_offset_percent: 0.02  # ✅ ИСПРАВЛЕНО: 0.02% (было default)
      ranging:
        limit_offset_percent: 0.03  # ✅ ИСПРАВЛЕНО: 0.03% (было 0.12%) для maker fee
      choppy:
        limit_offset_percent: 0.02  # ✅ ИСПРАВЛЕНО: 0.02% (было 0.10%) для быстрого исполнения
```

**Обоснование:**
- Меньший offset = быстрее исполнение = меньше отмен = экономия комиссий

---

### Решение #3: Добавить проверку актуальности `best_ask` для BUY

**Файл:** `src/strategies/scalping/futures/order_executor.py`, метод `_calculate_limit_price()`

**Добавить:**
```python
# Для BUY: проверяем актуальность best_ask (аналогично SELL)
if side.lower() == "buy":
    use_best_ask = False
    if best_ask > 0 and current_price > 0:
        spread_pct = abs(best_ask - current_price) / current_price
        # Используем best_ask только если разница < 0.5% (актуальные данные)
        if spread_pct < 0.005:
            use_best_ask = True
        else:
            logger.warning(
                f"⚠️ best_ask устарел для {symbol} BUY: "
                f"best_ask={best_ask:.2f}, current={current_price:.2f}, spread={spread_pct:.3%} "
                f"(используем current_price)"
            )
    
    if use_best_ask:
        limit_price = best_ask * (1 + offset_percent / 100.0)
    elif current_price > 0:
        limit_price = current_price * (1 + offset_percent / 100.0)
```

---

### Решение #4: Улучшить проверку разницы цены

**Файл:** `src/strategies/scalping/futures/order_executor.py`, метод `_calculate_limit_price()`

**Изменить:**
```python
# БЫЛО:
if price_diff_pct > 1.0:  # Если разница > 1% - это проблема!

# СТАЛО:
if price_diff_pct > 0.2:  # ✅ ИСПРАВЛЕНО: Если разница > 0.2% - это проблема для скальпинга!
    logger.error(
        f"❌ КРИТИЧЕСКАЯ ОШИБКА: Лимитная цена для {symbol} {side} слишком далеко от текущей! "
        f"limit_price={limit_price:.2f}, current_price={current_price:.2f}, "
        f"разница={price_diff_pct:.2f}%, offset={offset_percent:.3f}%, режим={regime or 'N/A'}"
    )
    # ✅ НОВОЕ: Корректируем цену до безопасного значения
    if side.lower() == "buy":
        limit_price = current_price * 1.001  # Максимум 0.1% выше
    else:
        limit_price = current_price * 0.999  # Максимум 0.1% ниже
    logger.warning(
        f"⚠️ Цена скорректирована до безопасного значения: {limit_price:.2f}"
    )
```

---

### Решение #5: Уменьшить лимиты биржи в `get_price_limits`

**Файл:** `src/clients/futures_client.py`, метод `get_price_limits()`

**Изменить:**
```python
# БЫЛО:
if max_buy_price > best_ask * 1.01:  # Не более 1% выше best_ask
    max_buy_price = best_ask * 1.01

# СТАЛО:
if max_buy_price > best_ask * 1.001:  # ✅ ИСПРАВЛЕНО: Не более 0.1% выше best_ask
    max_buy_price = best_ask * 1.001
if min_sell_price < best_bid * 0.999:  # ✅ ИСПРАВЛЕНО: Не более 0.1% ниже best_bid
    min_sell_price = best_bid * 0.999
```

---

## 📊 ОЖИДАЕМЫЙ ЭФФЕКТ

### До исправлений:
- Ордера размещаются на **0.3-0.5%** от текущей цены
- **20-30%** ордеров отменяются через таймаут
- Потеря комиссий: **$10-15/день**

### После исправлений:
- Ордера размещаются на **0.01-0.05%** от текущей цены
- **<5%** ордеров отменяются через таймаут
- Экономия комиссий: **$10-15/день** (экономия на отменах)
- Улучшение исполнения: **+15-20%** ордеров исполняются как maker (0.02% вместо 0.05%)

---

## 🎯 ПРИОРИТЕТ ИСПРАВЛЕНИЙ

1. **ВЫСОКИЙ:** Решение #1 (исправить расчет лимитов) - основная причина проблемы
2. **ВЫСОКИЙ:** Решение #2 (уменьшить offset) - критично для скальпинга
3. **СРЕДНИЙ:** Решение #3 (проверка актуальности best_ask) - улучшит точность
4. **СРЕДНИЙ:** Решение #4 (улучшить проверку разницы) - защита от ошибок
5. **НИЗКИЙ:** Решение #5 (уменьшить лимиты биржи) - дополнительная защита

---

## 📝 ФАЙЛЫ ДЛЯ ИЗМЕНЕНИЯ

1. `src/clients/futures_client.py` - метод `get_price_limits()`
2. `config/config_futures.yaml` - секция `order_executor.limit_order`
3. `src/strategies/scalping/futures/order_executor.py` - метод `_calculate_limit_price()`

---

**Отчет готов к применению исправлений!** ✅

