# 🔍 ПОЛНЫЙ АУДИТ ЗАХАРДКОЖЕННЫХ ПАРАМЕТРОВ

## 📊 СТАТУС: НАЙДЕНО ЗАХАРДКОЖЕННЫХ ПАРАМЕТРОВ

---

## 1. 🎯 ORCHESTRATOR.PY - Критические захардкоженные значения

### 1.1 Параметры риска (Fallback значения)
**Файл:** `src/strategies/scalping/futures/orchestrator.py`
**Строки:** 67-69

```python
self.max_loss_per_trade = 0.02  # Fallback: 2% макс потеря на сделку
self.max_margin_percent = 0.80  # Fallback: 80% макс маржа
self.max_drawdown_percent = 0.05  # Fallback: 5% макс просадка
```

**Проблема:** Используются как fallback, но должны браться из конфига при инициализации
**Решение:** Читать из `risk.base` или `risk.by_regime` при старте

---

### 1.2 Leverage fallback
**Строки:** 77-79, 1017-1018, 351-355

```python
leverage = 3  # Fallback если не указан в конфиге
```

**Проблема:** Захардкожен fallback
**Решение:** Использовать значение из конфига или выдавать ошибку

---

### 1.3 MaxSizeLimiter fallback значения
**Строки:** 193-195

```python
max_single_size_usd = 150.0
max_total_size_usd = 600.0
max_positions = 5
```

**Проблема:** Захардкожены fallback значения
**Решение:** Читать из конфига или выдавать ошибку

---

### 1.4 TSL Check Interval
**Строка:** 231

```python
self._tsl_check_interval: float = 1.5  # Интервал проверки TSL в секундах
```

**Проблема:** Захардкожен интервал проверки TSL
**Решение:** Добавить в конфиг `trailing_sl.check_interval_seconds`

---

### 1.5 Emergency Stop Unlock параметры
**Строки:** ~4633-4680

```python
min_lock_time = 300  # 5 минут
unlock_threshold = max_drawdown_percent * 0.7  # 70% от лимита
```

**Проблема:** Захардкожены параметры разблокировки
**Решение:** Добавить в конфиг:
```yaml
emergency_stop:
  min_lock_minutes: 5
  unlock_threshold_percent: 70  # % от max_drawdown для разблокировки
```

---

### 1.6 TrailingSL fallback значения
**Строки:** 754-756

```python
"initial_trail": 0.005,  # 0.5%
"max_trail": 0.01,  # 1%
"min_trail": 0.003,  # 0.3%
```

**Проблема:** Fallback значения для TSL
**Решение:** Уже читаются из конфига, но fallback можно убрать

---

### 1.7 Trading Fee Rate
**Строка:** 872

```python
params["trading_fee_rate"] = 0.0010  # 0.10%
```

**Проблема:** Захардкожена комиссия
**Решение:** Читать из `commission.trading_fee_rate` (уже есть в конфиге)

---

### 1.8 Position Holding Time fallback
**Строки:** 4784-4802

```python
max_holding_minutes = 30
extend_time_if_profitable = True
min_profit_for_extension = 0.1
extension_percent = 50
```

**Проблема:** Fallback значения (уже исправлено частично)
**Решение:** ✅ Исправлено - теперь читается из конфига

---

### 1.9 ctVal fallback
**Строка:** 1198

```python
ct_val = 0.01
```

**Проблема:** Fallback для ctVal
**Решение:** Получать из API или использовать правильный fallback по символу

---

## 2. 🎯 TRAILING_STOP_LOSS.PY - Захардкоженные параметры

### 2.1 High Profit Threshold
**Строка:** 524

```python
high_profit_threshold = 0.01  # 1% прибыли
```

**Проблема:** Захардкожен порог высокой прибыли
**Решение:** Добавить в конфиг `trailing_sl.high_profit_threshold`

---

### 2.2 Regime Multiplier fallback
**Строки:** 505-511

```python
if regime_multiplier == 1.0:
    if market_regime == "trending":
        regime_multiplier = 1.5  # Fallback
    elif market_regime == "ranging":
        regime_multiplier = 1.0  # Fallback
    elif market_regime == "choppy":
        regime_multiplier = 0.8  # Fallback
```

**Проблема:** Fallback значения для regime_multiplier
**Решение:** Читать из конфига `trailing_sl.regime_multipliers.trending/ranging/choppy`

---

### 2.3 Trend Strength Boost
**Строка:** 520

```python
regime_multiplier *= 1.3  # Усиление при сильном тренде
```

**Проблема:** Захардкожен множитель для сильного тренда
**Решение:** Добавить в конфиг `trailing_sl.trend_strength_boost_multiplier`

---

### 2.4 Profit Factor для high profit
**Строка:** 530

```python
profit_factor = min(profit_pct / high_profit_threshold, 2.0)  # Максимум 2x
reduction_factor = max(0.5, 1.0 - (profit_factor - 1.0) * 0.3)  # Уменьшаем на 30%
```

**Проблема:** Захардкожены параметры расчета reduction_factor
**Решение:** Добавить в конфиг:
```yaml
trailing_sl:
  high_profit:
    max_profit_factor: 2.0
    reduction_percent_per_1pct: 30  # % уменьшения за каждый 1% сверх порога
    min_reduction_factor: 0.5
```

---

## 3. 🎯 FILTERS - Захардкоженные параметры

### 3.1 MomentumFilter
**Файл:** `src/strategies/scalping/futures/filters/momentum_filter.py`
**Строки:** 147, 169, 212

```python
max_cv = 0.5  # Максимальный коэффициент вариации
min_increasing_ratio = 0.6  # Минимум 60%
min_ratio = 0.6  # Минимум 60% свечей должны быть повышающимися
```

**Проблема:** Захардкожены пороги для momentum фильтра
**Решение:** Добавить в конфиг `momentum_filter.thresholds`

---

### 3.2 LiquidityFilter relax multipliers
**Файл:** `src/strategies/scalping/futures/filters/liquidity_filter.py`
**Строка:** 86

```python
external_relax = 1.0
```

**Проблема:** Захардкожен external_relax (но это нормально, передается извне)

---

### 3.3 OrderFlowFilter relax multipliers
**Файл:** `src/strategies/scalping/futures/filters/order_flow_filter.py`
**Строка:** 57

```python
external_relax = 1.0
```

**Проблема:** Захардкожен external_relax (но это нормально, передается извне)

---

## 4. 🎯 MODULES - Захардкоженные параметры

### 4.1 MultiTimeframe EMA multiplier
**Файл:** `src/strategies/modules/multi_timeframe.py`
**Строка:** 410

```python
multiplier = 2 / (period + 1)  # Стандартная формула EMA
```

**Проблема:** Это стандартная формула EMA, не нужно менять

---

### 4.2 LiquidationGuard thresholds
**Файл:** `src/strategies/modules/liquidation_guard.py`
**Строки:** 34-37

```python
warning_threshold: float = 1.8,
danger_threshold: float = 1.3,
critical_threshold: float = 1.1,
auto_close_threshold: float = 1.05,
```

**Проблема:** Fallback значения в конструкторе
**Решение:** ✅ Уже читаются из конфига при инициализации

---

### 4.3 AdaptiveRegimeManager fallback значения
**Файл:** `src/strategies/modules/adaptive_regime_manager.py`
**Строки:** 126-170

```python
# Fallback значения в RegimeConfig
trending_params: RegimeParameters = field(default_factory=lambda: RegimeParameters(...))
ranging_params: RegimeParameters = field(default_factory=lambda: RegimeParameters(...))
choppy_params: RegimeParameters = field(default_factory=lambda: RegimeParameters(...))
```

**Проблема:** Fallback значения используются только если конфиг не загружен
**Решение:** ✅ Это нормально - fallback для безопасности

---

## 5. 🎯 КРИТИЧЕСКИЕ ЗАХАРДКОЖЕННЫЕ ПАРАМЕТРЫ (ТРЕБУЮТ ИСПРАВЛЕНИЯ)

### 5.1 ⚠️ ВЫСОКИЙ ПРИОРИТЕТ

1. **Emergency Stop Unlock параметры** (orchestrator.py:4633)
   - `min_lock_time = 300` (5 минут)
   - `unlock_threshold = 0.7` (70%)
   - **Решение:** Добавить в `config_futures.yaml` → `emergency_stop`

2. **TSL Check Interval** (orchestrator.py:231)
   - `_tsl_check_interval = 1.5` секунды
   - **Решение:** Добавить в `config_futures.yaml` → `trailing_sl.check_interval_seconds`

3. **High Profit Threshold** (trailing_stop_loss.py:524)
   - `high_profit_threshold = 0.01` (1%)
   - **Решение:** Добавить в `config_futures.yaml` → `trailing_sl.high_profit_threshold`

4. **Regime Multipliers fallback** (trailing_stop_loss.py:505-511)
   - `trending = 1.5`, `ranging = 1.0`, `choppy = 0.8`
   - **Решение:** Добавить в `config_futures.yaml` → `trailing_sl.regime_multipliers`

5. **Trend Strength Boost** (trailing_stop_loss.py:520)
   - `regime_multiplier *= 1.3`
   - **Решение:** Добавить в `config_futures.yaml` → `trailing_sl.trend_strength_boost_multiplier`

6. **High Profit Reduction Parameters** (trailing_stop_loss.py:530)
   - `max_profit_factor = 2.0`, `reduction_percent = 30`, `min_reduction = 0.5`
   - **Решение:** Добавить в `config_futures.yaml` → `trailing_sl.high_profit.*`

---

### 5.2 ⚠️ СРЕДНИЙ ПРИОРИТЕТ

7. **MomentumFilter thresholds** (momentum_filter.py:147, 169, 212)
   - `max_cv = 0.5`, `min_increasing_ratio = 0.6`, `min_ratio = 0.6`
   - **Решение:** Добавить в `config_futures.yaml` → `momentum_filter.thresholds`

8. **Risk Parameters fallback** (orchestrator.py:67-69)
   - `max_loss_per_trade = 0.02`, `max_margin_percent = 0.80`, `max_drawdown_percent = 0.05`
   - **Решение:** Читать из конфига при инициализации (уже есть в конфиге)

9. **Leverage fallback** (orchestrator.py:77-79)
   - `leverage = 3`
   - **Решение:** Выдавать ошибку если не указан в конфиге

10. **MaxSizeLimiter fallback** (orchestrator.py:193-195)
    - `max_single_size_usd = 150.0`, `max_total_size_usd = 600.0`, `max_positions = 5`
    - **Решение:** Читать из конфига (уже есть в конфиге)

---

### 5.3 ⚠️ НИЗКИЙ ПРИОРИТЕТ (Fallback значения - нормально)

11. **ctVal fallback** (orchestrator.py:1198)
    - `ct_val = 0.01`
    - **Решение:** Fallback нормален, но можно улучшить по символам

12. **Trading Fee Rate** (orchestrator.py:872)
    - `trading_fee_rate = 0.0010`
    - **Решение:** ✅ Уже читается из конфига, но есть fallback

13. **Position Holding Time fallback** (orchestrator.py:4784-4802)
    - Fallback значения
    - **Решение:** ✅ Уже исправлено - читается из конфига

---

## 6. 📋 РЕКОМЕНДАЦИИ ПО ИСПРАВЛЕНИЮ

### Приоритет 1 (Критично):
1. Emergency Stop параметры
2. TSL Check Interval
3. High Profit Threshold и параметры
4. Regime Multipliers для TSL

### Приоритет 2 (Важно):
5. MomentumFilter thresholds
6. Risk Parameters fallback (улучшить чтение из конфига)

### Приоритет 3 (Можно оставить):
7. Fallback значения для безопасности (ctVal, leverage и т.д.)

---

## 7. ✅ УЖЕ ИСПРАВЛЕНО

1. ✅ Position Holding Time параметры - теперь читаются из конфига
2. ✅ RegimeConfig.ranging - исправлено использование get_current_parameters()
3. ✅ Trading Fee Rate - читается из конфига
4. ✅ TrailingSL параметры - читаются из конфига

---

## 8. 📊 ИТОГО

- **Критичных захардкоженных параметров:** 6
- **Средних приоритетов:** 4
- **Низких приоритетов (fallback):** 3
- **Уже исправлено:** 4

**Общий статус:** Большинство параметров уже адаптивные, осталось исправить ~10 критичных/важных параметров.









