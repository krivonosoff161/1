# ОБЪЯСНЕНИЕ ПРОБЛЕМЫ МАРЖИ $30

**Дата:** 23 ноября 2025  
**Проблема:** Маржа всегда $30, хотя прогрессивная адаптация рассчитывает $84.86

---

## 🔍 ПРОБЛЕМА

### Из логов:

```
📊 Прогрессивная адаптация для small: баланс $848.58 → размер $84.86
📊 После multiplier: base_usd_size=$150.00 (max=$150.00)
10. Финальные лимиты: min_margin=$10.00, max_margin=$30.00
11. ИТОГО: margin=$30.00
```

### Что происходит:

1. ✅ **Прогрессивная адаптация работает:** рассчитывает `$84.86` (notional)
2. ❌ **Multiplier увеличивает:** `$84.86` → `$150.00` (почти в 2 раза!)
3. ❌ **max_usd_size ограничивает:** `$150.00` (максимум из конфига)
4. ❌ **Маржа:** `$150.00 / 5 = $30.00`

---

## 🔍 ПРИЧИНА

### Multiplier по силе сигнала:

В `risk_manager.py` строка 380-411 применяется `strength_multiplier` на основе силы сигнала:

```python
# Сильный сигнал: multiplier = 1.5-2.0
# Средний сигнал: multiplier = 1.0-1.2
# Слабый сигнал: multiplier = 0.8-1.0
```

**Проблема:**
- Прогрессивный расчет: `$84.86`
- Multiplier (сильный сигнал): `$84.86 × 1.5 = $127.29` или `$84.86 × 2.0 = $169.72`
- Ограничение: `min($169.72, $150.00) = $150.00`
- Маржа: `$150.00 / 5 = $30.00`

---

## ✅ РЕШЕНИЕ

### Вариант 1: Уменьшить multiplier

Изменить `strength_multipliers` в конфиге, чтобы они не увеличивали размер так сильно:

```yaml
strength_multipliers:
  strong: 1.2  # Было 1.5-2.0
  medium: 1.0  # Было 1.0-1.2
  weak: 0.9    # Было 0.8-1.0
```

### Вариант 2: Увеличить max_position_usd

Увеличить `max_position_usd` в `balance_profiles.small`:

```yaml
small:
  max_position_usd: 200.0  # Было 150.0
```

### Вариант 3: Убрать multiplier для прогрессивных профилей

Не применять `strength_multiplier` если используется прогрессивная адаптация.

---

## 📊 РАСЧЕТ

### Текущий расчет:

1. Прогрессивный: `$84.86` ✅
2. Multiplier (1.5-2.0): `$84.86 × 1.5 = $127.29` или `× 2.0 = $169.72` ❌
3. Ограничение: `min($169.72, $150.00) = $150.00` ❌
4. Маржа: `$150.00 / 5 = $30.00` ❌

### Ожидаемый расчет (без multiplier):

1. Прогрессивный: `$84.86` ✅
2. Маржа: `$84.86 / 5 = $16.97` ✅

### Ожидаемый расчет (с меньшим multiplier):

1. Прогрессивный: `$84.86` ✅
2. Multiplier (1.2): `$84.86 × 1.2 = $101.83` ✅
3. Ограничение: `min($101.83, $150.00) = $101.83` ✅
4. Маржа: `$101.83 / 5 = $20.37` ✅

---

## 🎯 РЕКОМЕНДАЦИЯ

### Лучшее решение:

**Уменьшить `strength_multipliers`** для прогрессивных профилей, чтобы они не перезаписывали прогрессивный расчет.

Или **не применять multiplier** если используется прогрессивная адаптация (progressive=true).

---

**Статус:** 🔍 ПРОБЛЕМА НАЙДЕНА - НУЖНО ИСПРАВИТЬ MULTIPLIER

