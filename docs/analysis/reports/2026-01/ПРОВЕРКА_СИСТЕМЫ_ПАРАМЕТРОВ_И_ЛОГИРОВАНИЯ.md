# 🔍 ПРОВЕРКА СИСТЕМЫ ПАРАМЕТРОВ И ЛОГИРОВАНИЯ

**Дата:** 03 января 2026  
**Цель:** Проверить все вилки параметров и логирование для понимания работы бота

---

## 1. СТРУКТУРА ПАРАМЕТРОВ В КОНФИГЕ

### Источники параметров:

1. **`scalping.by_symbol[symbol]`** → параметры по символу (приоритет 1)
2. **`scalping.adaptive_regime[regime]`** → параметры по режиму (приоритет 2)
3. **`exit_params[regime]`** → параметры выхода по режиму
4. **`order_executor.limit_order.by_symbol[symbol]`** → параметры ордеров по символу
5. **`order_executor.limit_order.by_regime[regime]`** → параметры ордеров по режиму
6. **`risk_config.balance_profiles[profile]`** → параметры риска по балансу
7. **Default значения** → fallback (приоритет 3)

---

## 2. ПРОВЕРКА ПАРАМЕТРОВ ПО КАТЕГОРИЯМ

### 2.1. ПАРАМЕТРЫ ВХОДА (Signal Generation)

#### `min_signal_strength`
**Используется в:**
- `signal_generator.py` → проверка силы сигнала
- `signal_coordinator.py` → фильтрация сигналов

**Источники (приоритет):**
1. `scalping.by_symbol[symbol].min_signal_strength`
2. `scalping.adaptive_regime[regime].min_signal_strength`
3. `scalping.min_signal_strength` (default)

**Логирование:**
- ❌ НЕТ логирования источника параметра
- ❌ НЕТ логирования итогового значения
- ✅ Есть логирование силы сигнала в `signal_generator.py`

**Рекомендация:**
```python
logger.debug(
    f"📊 [PARAMS] {symbol} ({regime}): min_signal_strength={final_value:.2f} "
    f"(source: by_symbol={by_symbol_value or 'N/A'}, "
    f"by_regime={by_regime_value or 'N/A'}, default={default_value})"
)
```

#### `min_adx`
**Используется в:**
- `signal_generator.py` → фильтрация по ADX
- `filters/adx_filter.py` → проверка тренда

**Источники (приоритет):**
1. `scalping.by_symbol[symbol].min_adx`
2. `scalping.min_adx` (default)

**Логирование:**
- ❌ НЕТ логирования источника параметра
- ❌ НЕТ логирования итогового значения
- ✅ Есть логирование ADX значения в `adx_filter.py`

**Рекомендация:** Добавить логирование источника и итогового значения

#### `min_signal_strength_ranging`
**Используется в:**
- `signal_generator.py` → специальный порог для ranging режима

**Источники (приоритет):**
1. `scalping.by_symbol[symbol].min_signal_strength_ranging`
2. `scalping.min_signal_strength_ranging` (default)

**Логирование:**
- ❌ НЕТ логирования источника параметра
- ❌ НЕТ логирования итогового значения

**Рекомендация:** Добавить логирование

---

### 2.2. ПАРАМЕТРЫ ВЫХОДА (Exit Logic)

#### `tp_percent` / `tp_atr_multiplier`
**Используется в:**
- `exit_analyzer.py:_get_tp_percent()` → расчет Take Profit
- `position_manager.py` → проверка достижения TP

**Источники (приоритет):**
1. `scalping.by_symbol[symbol].tp_atr_multiplier` (если ATR-based)
2. `exit_params[regime].tp_percent`
3. `scalping.adaptive_regime[regime].tp_percent`
4. `scalping.tp_percent` (default)

**Логирование:**
- ⚠️ Частичное логирование в `exit_analyzer.py` (только итоговое значение)
- ❌ НЕТ логирования источника параметра
- ❌ НЕТ логирования приоритета источников

**Рекомендация:**
```python
logger.debug(
    f"📊 [PARAMS] {symbol} ({regime}): tp_percent={final_tp:.2f}% "
    f"(source: by_symbol={by_symbol_tp or 'N/A'}, "
    f"exit_params={exit_params_tp or 'N/A'}, "
    f"adaptive_regime={adaptive_regime_tp or 'N/A'}, default={default_tp})"
)
```

#### `sl_percent` / `sl_atr_multiplier`
**Используется в:**
- `exit_analyzer.py:_get_sl_percent()` → расчет Stop Loss
- `position_manager.py` → проверка достижения SL

**Источники (приоритет):**
1. `scalping.by_symbol[symbol].sl_atr_multiplier` (если ATR-based)
2. `exit_params[regime].sl_percent`
3. `scalping.adaptive_regime[regime].sl_percent`
4. `scalping.sl_percent` (default)

**Логирование:**
- ⚠️ Частичное логирование в `exit_analyzer.py` (только итоговое значение)
- ❌ НЕТ логирования источника параметра

**Рекомендация:** Добавить логирование источника и приоритета

#### `max_holding_minutes`
**Используется в:**
- `exit_analyzer.py:_get_max_holding_minutes()` → максимальное время удержания
- `exit_analyzer.py` → проверка превышения времени

**Источники (приоритет):**
1. `exit_params[regime].max_holding_minutes`
2. `scalping.adaptive_regime[regime].max_holding_minutes`
3. `scalping.by_symbol[symbol].max_holding_minutes` (если есть)
4. 120.0 (default)

**Логирование:**
- ⚠️ Частичное логирование в `exit_analyzer.py` (только итоговое значение)
- ❌ НЕТ логирования источника параметра

**Рекомендация:** Добавить логирование источника и приоритета

#### `min_holding_minutes`
**Используется в:**
- `exit_analyzer.py` → минимальное время удержания перед закрытием
- `trailing_sl_coordinator.py` → инициализация TSL

**Источники (приоритет):**
1. `scalping.by_symbol[symbol].min_holding_minutes[regime]` (per-symbol per-regime)
2. `exit_params[regime].min_holding_minutes`
3. 0.5 (default)

**Логирование:**
- ❌ НЕТ логирования источника параметра
- ❌ НЕТ логирования итогового значения

**Рекомендация:** Добавить логирование

#### `emergency_loss_threshold`
**Используется в:**
- `exit_analyzer.py` → проверка критического убытка
- `position_manager.py` → экстренное закрытие

**Источники (приоритет):**
1. `exit_params[regime].emergency_loss_threshold`
2. -2.5% для ranging, -4.0% для trending, -2.0% для choppy (hardcoded)

**Логирование:**
- ⚠️ Частичное логирование (только при срабатывании)
- ❌ НЕТ логирования источника параметра

**Рекомендация:** Добавить логирование источника

---

### 2.3. ПАРАМЕТРЫ ОРДЕРОВ (Order Execution)

#### `limit_order.offset`
**Используется в:**
- `order_executor.py:_calculate_limit_price()` → расчет цены limit ордера

**Источники (приоритет):**
1. `order_executor.limit_order.by_symbol[symbol].offset`
2. `order_executor.limit_order.by_regime[regime].offset`
3. `order_executor.limit_order.default_offset`
4. 0.1% (default)

**Логирование:**
- ⚠️ Частичное логирование в `order_executor.py` (только итоговое значение)
- ❌ НЕТ логирования источника параметра

**Рекомендация:** Добавить логирование источника и приоритета

#### `limit_order.post_only`
**Используется в:**
- `order_executor.py:_place_limit_order()` → размещение POST_ONLY ордера

**Источники (приоритет):**
1. `order_executor.limit_order.by_regime[regime].post_only`
2. `order_executor.limit_order.post_only` (default)
3. True (default)

**Логирование:**
- ✅ Есть логирование итогового значения (POST_ONLY enabled/disabled)
- ❌ НЕТ логирования источника параметра

**Рекомендация:** Добавить логирование источника

---

### 2.4. ПАРАМЕТРЫ РИСКА (Risk Management)

#### `max_margin_per_trade`
**Используется в:**
- `calculations/position_sizer.py` → расчет размера позиции
- `risk_manager.py` → проверка лимитов

**Источники (приоритет):**
1. `risk_config.balance_profiles[profile].max_margin_per_trade`
2. `risk_config.max_margin_per_trade` (default)

**Логирование:**
- ❌ НЕТ логирования источника параметра
- ❌ НЕТ логирования итогового значения

**Рекомендация:** Добавить логирование

#### `max_portfolio_margin`
**Используется в:**
- `risk_manager.py` → проверка общего лимита маржи
- `position_sizer.py` → расчет доступной маржи

**Источники (приоритет):**
1. `risk_config.balance_profiles[profile].max_portfolio_margin`
2. `risk_config.max_portfolio_margin` (default)

**Логирование:**
- ❌ НЕТ логирования источника параметра
- ❌ НЕТ логирования итогового значения

**Рекомендация:** Добавить логирование

---

### 2.5. ПАРАМЕТРЫ ИНДИКАТОРОВ

#### `indicators.rsi_overbought` / `indicators.rsi_oversold`
**Используется в:**
- `indicators/rsi_indicator.py` → расчет RSI сигналов

**Источники (приоритет):**
1. `scalping.by_symbol[symbol].indicators.rsi_overbought`
2. 70/30 (default)

**Логирование:**
- ❌ НЕТ логирования источника параметра
- ❌ НЕТ логирования итогового значения

**Рекомендация:** Добавить логирование

#### `indicators.macd_fast` / `indicators.macd_slow`
**Используется в:**
- `indicators/macd_indicator.py` → расчет MACD сигналов

**Источники (приоритет):**
1. `scalping.by_symbol[symbol].indicators.macd_fast`
2. 12/26 (default)

**Логирование:**
- ❌ НЕТ логирования источника параметра
- ❌ НЕТ логирования итогового значения

**Рекомендация:** Добавить логирование

---

### 2.6. ПАРАМЕТРЫ ФИЛЬТРОВ

#### `smart_close.reversal_score_threshold`
**Используется в:**
- `exit_analyzer.py` → проверка разворота для Smart Close

**Источники (приоритет):**
1. `scalping.by_symbol[symbol].smart_close.reversal_score_threshold[regime]`
2. `exit_params.smart_close.reversal_score_threshold[regime]`
3. 4 для ranging, 1 для trending, 4 для choppy (hardcoded)

**Логирование:**
- ❌ НЕТ логирования источника параметра
- ❌ НЕТ логирования итогового значения

**Рекомендация:** Добавить логирование

---

## 3. ПРОВЕРКА ПО ПАРАМ

### BTC-USDT
**Параметры в конфиге:**
- `min_signal_strength: 0.62`
- `smart_close.reversal_score_threshold.ranging: 4`

**Проверка:**
- ✅ Параметры определены в `scalping.by_symbol.BTC-USDT`
- ❌ НЕТ логирования использования этих параметров
- ❌ НЕТ логирования приоритета источников

### ETH-USDT
**Параметры в конфиге:**
- `min_signal_strength: 0.55`
- `sl_atr_multiplier: 3.5`
- `tp_atr_multiplier: 4.0`
- `max_holding_minutes: 30.0`
- `min_holding_minutes.ranging: 2.0`
- `smart_close.reversal_score_threshold.ranging: 5`
- `indicators.rsi_overbought: 78`
- `indicators.rsi_oversold: 22`

**Проверка:**
- ✅ Параметры определены в `scalping.by_symbol.ETH-USDT`
- ❌ НЕТ логирования использования этих параметров
- ❌ НЕТ логирования приоритета источников

### SOL-USDT
**Параметры в конфиге:**
- `min_adx: 20.0`
- `min_signal_strength: 0.58`
- `sl_atr_multiplier: 3.0`
- `smart_close.reversal_score_threshold.ranging: 5`

**Проверка:**
- ✅ Параметры определены в `scalping.by_symbol.SOL-USDT`
- ❌ НЕТ логирования использования этих параметров

### DOGE-USDT
**Параметры в конфиге:**
- ❌ НЕТ специальных параметров в `by_symbol`

**Проверка:**
- ⚠️ Используются только default параметры
- ❌ НЕТ логирования использования default параметров

### XRP-USDT
**Параметры в конфиге:**
- `sl_atr_multiplier: 3.5`
- `smart_close.reversal_score_threshold.ranging: 5`

**Проверка:**
- ✅ Параметры определены в `scalping.by_symbol.XRP-USDT`
- ❌ НЕТ логирования использования этих параметров

---

## 4. ПРОВЕРКА ПО РЕЖИМАМ

### TRENDING
**Параметры в конфиге:**
- `exit_params.trending.max_holding_minutes: 45`
- `exit_params.trending.sl_percent: 2.0`
- `exit_params.trending.tp_percent: 8.0`

**Проверка:**
- ✅ Параметры определены в `exit_params.trending`
- ❌ НЕТ логирования использования этих параметров

### RANGING
**Параметры в конфиге:**
- `exit_params.ranging.max_holding_minutes: 15`
- `exit_params.ranging.sl_percent: 0.8`
- `exit_params.ranging.tp_percent: 2.5`

**Проверка:**
- ✅ Параметры определены в `exit_params.ranging`
- ❌ НЕТ логирования использования этих параметров

### CHOPPY
**Параметры в конфиге:**
- `exit_params.choppy.max_holding_minutes: 10`
- `exit_params.choppy.sl_percent: 1.5`
- `exit_params.choppy.tp_percent: 3.0`

**Проверка:**
- ✅ Параметры определены в `exit_params.choppy`
- ❌ НЕТ логирования использования этих параметров

---

## 5. ПРОВЕРКА ПО БАЛАНСАМ

### small (< $1500)
**Параметры в конфиге:**
- `risk_config.balance_profiles.small.max_margin_per_trade: 22.0`
- `risk_config.balance_profiles.small.max_portfolio_margin: 65.0`

**Проверка:**
- ✅ Параметры определены в `risk_config.balance_profiles.small`
- ❌ НЕТ логирования использования этих параметров

### medium ($1500 - $5000)
**Параметры в конфиге:**
- `risk_config.balance_profiles.medium.max_margin_per_trade: 18.0`
- `risk_config.balance_profiles.medium.max_portfolio_margin: 60.0`

**Проверка:**
- ✅ Параметры определены в `risk_config.balance_profiles.medium`
- ❌ НЕТ логирования использования этих параметров

### large (> $5000)
**Параметры в конфиге:**
- `risk_config.balance_profiles.large.max_margin_per_trade: 15.0`
- `risk_config.balance_profiles.large.max_portfolio_margin: 50.0`

**Проверка:**
- ✅ Параметры определены в `risk_config.balance_profiles.large`
- ❌ НЕТ логирования использования этих параметров

---

## 6. ЧТО НЕ ХВАТАЕТ В ЛОГИРОВАНИИ

### 6.1. Финансовая сторона

#### ❌ Отсутствует логирование:
1. **Расчет размера позиции:**
   - Баланс
   - Профиль баланса
   - `max_margin_per_trade` (источник)
   - `max_portfolio_margin` (источник)
   - Рассчитанный размер позиции
   - Использованная маржа

2. **Расчет TP/SL:**
   - `tp_percent` / `tp_atr_multiplier` (источник)
   - `sl_percent` / `sl_atr_multiplier` (источник)
   - Рассчитанные значения TP/SL
   - ATR значение (если используется)

3. **Расчет комиссий:**
   - Maker fee rate
   - Taker fee rate
   - Рассчитанная комиссия на вход
   - Рассчитанная комиссия на выход
   - Общая комиссия

4. **Расчет PnL:**
   - Gross PnL (до комиссий)
   - Комиссия на вход
   - Комиссия на выход
   - Net PnL (после комиссий)
   - PnL в процентах

5. **Расчет маржи:**
   - Использованная маржа
   - Доступная маржа
   - Общая маржа
   - Процент использования маржи

### 6.2. Кодерская сторона

#### ❌ Отсутствует логирование:
1. **Источники параметров:**
   - Откуда взят параметр (by_symbol/by_regime/default)
   - Приоритет источников
   - Итоговое значение параметра

2. **Вилки параметров:**
   - Какие параметры проверялись
   - Какие параметры использовались
   - Почему выбран конкретный параметр

3. **Принятие решений:**
   - Почему сигнал сгенерирован/отклонен
   - Почему позиция открыта/закрыта
   - Какие параметры использовались для решения

4. **Ошибки и fallback:**
   - Когда используется fallback значение
   - Почему fallback значение использовано
   - Какое fallback значение использовано

---

## 7. РЕКОМЕНДАЦИИ

### 7.1. Немедленные действия

1. **Добавить логирование источников параметров:**
   - Для каждого параметра логировать источник (by_symbol/by_regime/default)
   - Логировать приоритет источников
   - Логировать итоговое значение

2. **Добавить логирование финансовых расчетов:**
   - Размер позиции (баланс, профиль, маржа)
   - TP/SL (источник, значение, ATR)
   - Комиссии (maker/taker, вход/выход)
   - PnL (gross/net, проценты)

3. **Добавить логирование принятия решений:**
   - Почему сигнал сгенерирован/отклонен
   - Почему позиция открыта/закрыта
   - Какие параметры использовались

### 7.2. Среднесрочные действия

1. **Создать единый метод логирования параметров:**
   - `ParameterProvider.log_parameter_usage()`
   - Автоматическое логирование источника и значения

2. **Создать единый метод логирования финансов:**
   - `FinancialLogger.log_position_calculation()`
   - Автоматическое логирование всех финансовых расчетов

3. **Добавить CSV логирование:**
   - Параметры входа (символ, режим, источник, значение)
   - Параметры выхода (символ, режим, источник, значение)
   - Финансовые расчеты (размер, TP/SL, комиссии, PnL)

---

## 8. ИТОГОВАЯ ТАБЛИЦА ПРОВЕРКИ

| Параметр | Источник | Логирование источника | Логирование значения | Статус |
|----------|----------|----------------------|---------------------|--------|
| `min_signal_strength` | by_symbol/by_regime/default | ❌ | ⚠️ | Требуется улучшение |
| `min_adx` | by_symbol/default | ❌ | ⚠️ | Требуется улучшение |
| `tp_percent` | by_symbol/exit_params/default | ❌ | ⚠️ | Требуется улучшение |
| `sl_percent` | by_symbol/exit_params/default | ❌ | ⚠️ | Требуется улучшение |
| `max_holding_minutes` | exit_params/by_regime/default | ❌ | ⚠️ | Требуется улучшение |
| `min_holding_minutes` | by_symbol/exit_params/default | ❌ | ❌ | Критично |
| `emergency_loss_threshold` | exit_params/hardcoded | ❌ | ⚠️ | Требуется улучшение |
| `limit_order.offset` | by_symbol/by_regime/default | ❌ | ⚠️ | Требуется улучшение |
| `limit_order.post_only` | by_regime/default | ❌ | ✅ | Требуется улучшение |
| `max_margin_per_trade` | balance_profiles/default | ❌ | ❌ | Критично |
| `max_portfolio_margin` | balance_profiles/default | ❌ | ❌ | Критично |
| `indicators.*` | by_symbol/default | ❌ | ❌ | Требуется улучшение |
| `smart_close.*` | by_symbol/exit_params/hardcoded | ❌ | ❌ | Критично |

**Легенда:**
- ✅ Есть логирование
- ⚠️ Частичное логирование
- ❌ Нет логирования
- 🔴 Критично (нет логирования важных параметров)

---

**Дата анализа:** 03 января 2026  
**Аналитик:** AI Assistant  
**Статус:** Детальная проверка завершена

