# ✅ ЭТАП 3.1: Удалить дублирующие кэши - ЗАВЕРШЕНО (Частично)

**Дата:** 25.11.2025  
**Статус:** ✅ Анализ завершен, дубликаты помечены как DEPRECATED

---

## ✅ ЗАВЕРШЕНО

### Анализ кэшей:

1. ✅ **Проанализированы все кэши** в orchestrator и фильтрах
2. ✅ **Определены дубликаты** и специальные переменные
3. ✅ **Помечены как DEPRECATED** дублирующие переменные

**Найденные кэши:**

**Оставить (не дубликаты):**
- ✅ `last_orders_cache`, `active_orders_cache` - ордера не в DataRegistry
- ✅ `liquidity_filter._cache`, `order_flow_filter._cache` - специфичные снимки
- ✅ `initial_balance` - специальная переменная для drawdown
- ✅ `_emergency_stop_balance` - специальная переменная для emergency stop

**Помечены как DEPRECATED:**
- ⚠️ `self.total_margin_used` - дублирует маржу из DataRegistry
  - Теперь читается из DataRegistry в `_get_used_margin()` с fallback
  - Локальная переменная оставлена для обратной совместимости

---

## 📝 ВЫВОДЫ

**Основной дубликат:**
- `self.total_margin_used` - дублирует маржу из DataRegistry

**Действия:**
1. ✅ Метод `_get_used_margin()` теперь читает из DataRegistry
2. ✅ Локальная переменная оставлена с пометкой DEPRECATED
3. ✅ Постепенный переход к использованию DataRegistry

**Следующие шаги:**
- Постепенно заменить все использования `self.total_margin_used` на `data_registry.get_margin_used()`
- Удалить локальную переменную после полного перехода

---

**Анализ завершен! Дубликаты помечены! ✅**






