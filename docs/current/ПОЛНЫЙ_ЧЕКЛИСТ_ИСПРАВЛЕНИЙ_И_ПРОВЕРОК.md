# ✅ ПОЛНЫЙ ЧЕКЛИСТ ИСПРАВЛЕНИЙ И ПРОВЕРОК

**Дата:** 2025-11-02  
**Цель:** Убедиться что ВСЕ аспекты учтены перед исправлениями

---

## 🎯 **1. ИНДИКАТОРЫ И МОДУЛИ:**

### **1.1 Добавление индикаторов:**

#### **✅ Для ARM (детекция режима):**
- [ ] **FastADX** вместо ADX Proxy
  - Использовать существующий `FastADX` класс
  - Получать `adx_value`, `di_plus`, `di_minus`, `trend_direction`
  - Проверить что `trend_direction` возвращает "bullish"/"bearish"/"neutral"

- [ ] **Volume indicators**
  - Volume MA (20) - средний объем
  - Volume Ratio = current_volume / volume_ma
  - Использовать для подтверждения TRENDING и CHOPPY

- [ ] **Williams %R** (для CHOPPY)
  - Формула: `((high_n - close) / (high_n - low_n)) * -100`
  - Быстрое изменение → choppy режим

- [ ] **ROC (Rate of Change)** (для CHOPPY)
  - Формула: `((close - close_n) / close_n) * 100`
  - Высокий ROC + частые изменения → choppy

#### **✅ Для SignalGenerator (генерация сигналов):**
- [ ] **Stochastic Oscillator** (опционально)
  - Лучшая детекция экстремумов
  - `%K = (close - low_n) / (high_n - low_n) * 100`
  - BUY: %K < 20, SELL: %K > 80

- [ ] **Volume** для подтверждения сигналов
  - Проверять что объем выше среднего для входа

---

### **1.2 Модули:**

- [ ] Проверить что **все модули инициализируются**:
  - Adaptive Regime Manager ✅
  - Correlation Filter ✅
  - Multi Timeframe Filter ✅
  - Pivot Points Filter ✅
  - Volume Profile Filter ✅

- [ ] Проверить что модули **правильно фильтруют** сигналы
- [ ] Проверить что модули **используют параметры из конфига**

---

## 🧮 **2. ПРАВИЛЬНОСТЬ МАТЕМАТИКИ:**

### **2.1 Проверка формул индикаторов:**

#### **✅ RSI:**
- [ ] Формула правильная: `100 - (100 / (1 + RS))` где `RS = avg_gain / avg_loss`
- [ ] Период расчета: 14 свечей
- [ ] Проверка границ: 0 <= RSI <= 100

#### **✅ MACD:**
- [ ] EMA быстрая: `EMA(12)` правильная формула
- [ ] EMA медленная: `EMA(26)` правильная формула
- [ ] Signal line: `EMA(9)` от MACD line
- [ ] Histogram: `MACD_line - Signal_line`

#### **✅ Bollinger Bands:**
- [ ] Middle band: `SMA(20)`
- [ ] Upper band: `SMA + (std_multiplier * std_dev)`
- [ ] Lower band: `SMA - (std_multiplier * std_dev)`
- [ ] Std multiplier: 2.0

#### **✅ EMA:**
- [ ] Формула: `EMA(t) = Price(t) * α + EMA(t-1) * (1 - α)`
- [ ] Alpha: `α = 2 / (period + 1)`
- [ ] Инициализация: `EMA[0] = Price[0]`

#### **✅ ATR:**
- [ ] True Range: `max(high-low, abs(high-prev_close), abs(low-prev_close))`
- [ ] ATR: `EMA(14) of True Range`
- [ ] Проверка на деление на ноль

#### **✅ FastADX:**
- [ ] +DM: `max(high - prev_high, 0)`
- [ ] -DM: `max(prev_low - low, 0)`
- [ ] TR: `max(high-low, abs(high-prev_close), abs(low-prev_close))`
- [ ] +DI: `100 * (+DM / TR)`
- [ ] -DI: `100 * (-DM / TR)`
- [ ] DX: `abs(+DI - -DI) / (+DI + -DI) * 100`
- [ ] ADX: сглаженный DX

---

### **2.2 Проверка порогов:**

#### **✅ RSI Пороги:**
- [ ] TRENDING: oversold=25, overbought=75 ✅
- [ ] RANGING: oversold=30, overbought=70 ✅
- [ ] CHOPPY: oversold=35, overbought=65 ✅
- [ ] Проверка: oversold < 50 < overbought (правильный порядок)

#### **✅ ADX Пороги:**
- [ ] TRENDING: ADX > 20.0 ✅
- [ ] RANGING: ADX < 15.0 ✅
- [ ] CHOPPY: не используется (высокая волатильность)

#### **✅ Volatility Пороги:**
- [ ] LOW: < 2% (0.02)
- [ ] NORMAL: 2% - 3% (0.02 - 0.03)
- [ ] HIGH: > 3% (0.03)

---

## 🔧 **3. ХАРДКОЖЕНОСТЬ ПАРАМЕТРОВ:**

### **3.1 Критичные места:**

#### **✅ SignalGenerator:**
- [ ] `RSI(period=14, overbought=70, oversold=30)` → **ЗАМЕНИТЬ** на параметры из конфига
- [ ] `EMA_12, EMA_26` → **ЗАМЕНИТЬ** на режим-специфичные (8/21, 10/25, 12/30)
- [ ] `BollingerBands(period=20, std_multiplier=2.0)` → проверить, можно ли адаптировать
- [ ] `MACD(fast=12, slow=26, signal=9)` → проверить, нужно ли адаптировать

#### **✅ ARM:**
- [ ] `SMA_20, SMA_50` → проверить, можно ли адаптировать (опционально)
- [ ] `ATR period=14` → проверить, можно ли адаптировать (опционально)
- [ ] Пороги детекции режима → проверить из конфига

#### **✅ OrderExecutor:**
- [ ] Проверить что использует режим-специфичные TP/SL
- [ ] Проверить что использует режим-специфичный размер позиции

---

## 🎨 **4. ГАРМОНИЧНОСТЬ И ОТСУТСТВИЕ КОНФЛИКТОВ:**

### **4.1 Параметры по режимам:**

#### **✅ TRENDING:**
- [ ] RSI 75/25 (агрессивно) → больше сигналов ✅
- [ ] EMA 8/21 (быстро) → быстрая реакция ✅
- [ ] Position size multiplier 1.0 → стандарт ✅
- [ ] TP/SL multipliers → проверить оптимальность

#### **✅ RANGING:**
- [ ] RSI 70/30 (стандарт) → умеренные сигналы ✅
- [ ] EMA 10/25 (средне) → баланс ✅
- [ ] Position size multiplier 0.9 → меньше риск ✅
- [ ] TP/SL multipliers → проверить оптимальность

#### **✅ CHOPPY:**
- [ ] RSI 65/35 (консервативно) → меньше сигналов ✅
- [ ] EMA 12/30 (медленно) → фильтр шума ✅
- [ ] Position size multiplier 0.5 → минимальный риск ✅
- [ ] TP/SL multipliers → проверить оптимальность

---

### **4.2 Дублирование индикаторов:**

- [ ] ARM и SignalGenerator рассчитывают SMA_20 → **УНИФИЦИРОВАТЬ**
- [ ] ARM и SignalGenerator рассчитывают ATR → **УНИФИЦИРОВАТЬ**
- [ ] Создать единый `IndicatorCalculator` для избежания дублирования

---

### **4.3 Конфликты в логике:**

- [ ] Проверить что режим переключается плавно (защита от частых переключений)
- [ ] Проверить что параметры применяются сразу после смены режима
- [ ] Проверить что нет конфликтов между модулями фильтрации

---

## 🎯 **5. ПРАВИЛЬНОСТЬ ОПРЕДЕЛЕНИЯ РЕЖИМОВ:**

### **5.1 Логика детекции:**

#### **✅ TRENDING:**
- [ ] ADX > threshold (20.0) ✅
- [ ] Trend deviation > threshold ✅
- [ ] +DI > -DI (для bullish) или -DI > +DI (для bearish) ✅
- [ ] Volume ratio > 1.2 (высокий объем) - **ДОБАВИТЬ**

#### **✅ RANGING:**
- [ ] ADX < threshold (15.0) ✅
- [ ] Range width < 3% ✅
- [ ] Trend deviation < threshold ✅
- [ ] Volume ratio < 1.2 (низкий объем) - **ДОБАВИТЬ**

#### **✅ CHOPPY:**
- [ ] Volatility > threshold (3%) ✅
- [ ] Reversals > 10 ✅
- [ ] Williams %R быстро меняется - **ДОБАВИТЬ**
- [ ] ROC высокий + частые изменения - **ДОБАВИТЬ**
- [ ] Volume ratio > 1.5 (высокий объем) - **ДОБАВИТЬ**

---

### **5.2 Проверка корректности:**

- [ ] **Тестирование:** Создать тесты для каждого режима:
  - Сильный восходящий тренд → должен определить TRENDING
  - Узкий диапазон → должен определить RANGING
  - Хаотичные движения → должен определить CHOPPY

- [ ] **Логирование:** Добавить подробное логирование детекции режима
- [ ] **Валидация:** Проверить что режим не меняется слишком часто

---

## 🚨 **6. ПРАВИЛЬНОСТЬ СИГНАЛОВ (КРИТИЧНО!):**

### **6.1 Проверка направления сигналов:**

#### **✅ LONG сигнал должен быть когда:**
- [ ] RSI < oversold (цена падает, перепроданность)
- [ ] MACD line > Signal line И histogram > 0 (восходящий тренд)
- [ ] Цена <= нижняя полоса Bollinger Bands (отскок вверх)
- [ ] EMA fast > EMA slow (быстрый тренд выше медленного)
- [ ] Цена выше EMA fast (цена в восходящем тренде)

#### **✅ SHORT сигнал должен быть когда:**
- [ ] RSI > overbought (цена растет, перекупленность)
- [ ] MACD line < Signal line И histogram < 0 (нисходящий тренд)
- [ ] Цена >= верхняя полоса Bollinger Bands (откат вниз)
- [ ] EMA fast < EMA slow (быстрый тренд ниже медленного)
- [ ] Цена ниже EMA fast (цена в нисходящем тренде)

---

### **6.2 Проверка соответствия side:**

#### **✅ Критичная проверка:**
- [ ] Если график показывает **восходящий тренд** → должен открывать **LONG**
- [ ] Если график показывает **нисходящий тренд** → должен открывать **SHORT**
- [ ] Если цена **перепродана** (RSI < 30) → должен открывать **LONG** (отскок вверх)
- [ ] Если цена **перекуплена** (RSI > 70) → должен открывать **SHORT** (откат вниз)

#### **✅ Проверка кода:**
- [ ] В `_generate_rsi_signals`: `if rsi < oversold` → `side="buy"` (LONG) ✅
- [ ] В `_generate_rsi_signals`: `if rsi > overbought` → `side="sell"` (SHORT) ✅
- [ ] В `_generate_macd_signals`: `if macd > signal` → `side="buy"` (LONG) ✅
- [ ] В `_generate_macd_signals`: `if macd < signal` → `side="sell"` (SHORT) ✅
- [ ] В `_generate_bollinger_signals`: `if price <= lower` → `side="buy"` (LONG) ✅
- [ ] В `_generate_bollinger_signals`: `if price >= upper` → `side="sell"` (SHORT) ✅
- [ ] В `_generate_ma_signals`: `if ema_fast > ema_slow and price > ema_fast` → `side="buy"` (LONG) ✅
- [ ] В `_generate_ma_signals`: `if ema_fast < ema_slow and price < ema_fast` → `side="sell"` (SHORT) ✅

---

### **6.3 Проверка маппинга side → order side:**

#### **✅ Проверка в OrderExecutor:**
- [ ] `side="buy"` → открывает **LONG** позицию ✅
- [ ] `side="sell"` → открывает **SHORT** позицию ✅
- [ ] Проверить что на бирже используется правильный `posSide` (long/short)

#### **✅ Проверка в API:**
- [ ] OKX Futures: `side="buy"` → `posSide="long"` ✅
- [ ] OKX Futures: `side="sell"` → `posSide="short"` ✅

---

### **6.4 Тестирование сигналов:**

#### **✅ Unit тесты:**
- [ ] Тест: RSI < 30 → генерирует LONG сигнал
- [ ] Тест: RSI > 70 → генерирует SHORT сигнал
- [ ] Тест: MACD пересекает сигнал снизу вверх → LONG
- [ ] Тест: MACD пересекает сигнал сверху вниз → SHORT
- [ ] Тест: Цена касается нижней полосы BB → LONG
- [ ] Тест: Цена касается верхней полосы BB → SHORT

#### **✅ Интеграционные тесты:**
- [ ] Тест на реальных данных: восходящий тренд → только LONG сигналы
- [ ] Тест на реальных данных: нисходящий тренд → только SHORT сигналы
- [ ] Тест на реальных данных: флэт → LONG и SHORT сигналы

---

## 🔍 **7. ПРОВЕРКА ВСЕЙ ЛОГИКИ:**

### **7.1 Проверка workflow:**

1. [ ] **Получение данных:**
   - WebSocket получает цены ✅
   - REST API получает исторические свечи ✅
   - Данные корректные (цены, объемы) ✅

2. [ ] **Детекция режима (ARM):**
   - Рассчитываются индикаторы ✅
   - Определяется режим (trending/ranging/choppy) ✅
   - Режим сохраняется в `current_regime` ✅

3. [ ] **Генерация сигналов:**
   - Получается режим от ARM ✅
   - Используются параметры из конфига по режиму ✅
   - Рассчитываются индикаторы с правильными параметрами ✅
   - Генерируются сигналы с правильным side (buy/sell) ✅

4. [ ] **Фильтрация сигналов:**
   - ARM фильтрует по `min_score_threshold` ✅
   - Модули фильтруют сигналы ✅
   - Проверяется лимит открытых позиций ✅

5. [ ] **Выполнение ордеров:**
   - Side мапится правильно (buy → long, sell → short) ✅
   - Используется режим-специфичный размер позиции ✅
   - Используется режим-специфичный TP/SL ✅

---

### **7.2 Проверка edge cases:**

- [ ] Что если ARM не инициализирован? → использовать ranging параметры ✅
- [ ] Что если режим не определен? → использовать ranging параметры ✅
- [ ] Что если нет исторических данных? → пропустить генерацию сигналов ✅
- [ ] Что если индикаторы не рассчитаны? → пропустить генерацию сигналов ✅
- [ ] Что если сигнал имеет strength=0? → не открывать позицию ✅
- [ ] Что если достигнут лимит открытых позиций? → не открывать новую ✅

---

## 📋 **8. ЛОГИРОВАНИЕ И ДИАГНОСТИКА:**

### **8.1 Добавить логирование:**

- [ ] **ARM:**
  - Логировать детекцию режима с индикаторами
  - Логировать смену режима
  - Логировать причины детекции

- [ ] **SignalGenerator:**
  - Логировать используемые параметры (режим, RSI пороги, EMA периоды)
  - Логировать значения индикаторов
  - Логировать сгенерированные сигналы (side, strength, reason)

- [ ] **OrderExecutor:**
  - Логировать маппинг side → posSide
  - Логировать режим-специфичные TP/SL
  - Логировать размер позиции

---

### **8.2 Добавить диагностику:**

- [ ] Создать метод `get_diagnostics()` который показывает:
  - Текущий режим
  - Используемые параметры
  - Значения индикаторов
  - Последние сигналы

---

## ✅ **9. ТЕСТИРОВАНИЕ:**

### **9.1 Unit тесты:**

- [ ] Тесты для ARM детекции режима
- [ ] Тесты для генерации сигналов
- [ ] Тесты для фильтрации сигналов
- [ ] Тесты для правильности side

### **9.2 Integration тесты:**

- [ ] Тест полного workflow: данные → режим → сигналы → ордер
- [ ] Тест на исторических данных
- [ ] Тест на разных режимах

### **9.3 Ручное тестирование:**

- [ ] Запустить бота на sandbox
- [ ] Проверить что режимы определяются правильно
- [ ] Проверить что сигналы генерируются правильно
- [ ] Проверить что side правильный (LONG на восходящем тренде, SHORT на нисходящем)

---

## 🎯 **10. ИТОГОВЫЙ ЧЕКЛИСТ:**

### **Критичные (сделать первым делом):**
1. [ ] Использовать FastADX вместо ADX Proxy
2. [ ] Использовать параметры из конфига (не хардкод)
3. [ ] Добавить метод `is_signal_valid()` в ARM
4. [ ] **ПРОВЕРИТЬ ПРАВИЛЬНОСТЬ SIDE (LONG/SHORT) - КРИТИЧНО!**

### **Важные:**
5. [ ] Добавить Volume indicators
6. [ ] Унифицировать расчет индикаторов
7. [ ] Проверить математику всех формул
8. [ ] Добавить логирование и диагностику

### **Желательно:**
9. [ ] Добавить Williams %R и ROC для CHOPPY
10. [ ] Добавить Stochastic для сигналов
11. [ ] Создать unit и integration тесты

---

**ГОТОВ К РЕАЛИЗАЦИИ! 🚀**

