# 🔍 АНАЛИЗ ЗАКРЫТИЙ ПОЗИЦИЙ

## 📅 Дата: 2025-11-21

---

## ✅ ПОДТВЕРЖДЕНО: ПОЗИЦИИ ЗАКРЫВАЮТСЯ

### Закрытые позиции (из данных биржи):

1. **DOGE-USDT** (21:38:58 - 21:39:36, 38 секунд)
   - Entry: 0.14075
   - Exit: 0.14121
   - PnL: +0.39 USDT (+1.31%)
   - **Причина:** Partial TP сработал при прибыли 1.35-1.42% (триггер 0.40%)

2. **DOGE-USDT** (21:41:14 - 21:41:51, 37 секунд)
   - Entry: 0.14170
   - Exit: 0.14201
   - PnL: +0.23 USDT (+0.77%)
   - **Причина:** Нужно проверить в логах

3. **BTC-USDT** (21:41:31 - 21:41:42, 11 секунд)
   - Entry: 84,367.4
   - Exit: 84,337.5
   - PnL: -0.04 USDT (-0.33%)
   - **Причина:** "opposite_position_in_check" - бот закрыл позицию, потому что обнаружил противоположную позицию или сигнал

4. **BTC-USDT** (частично закрыта, 21:38:57 - открыта)
   - Entry: 84,357.5
   - Exit: 84,476.5
   - PnL: +0.03 USDT (+0.18%)
   - **Причина:** Partial TP сработал

---

## ⚠️ ПРОБЛЕМА #1: CSV ФАЙЛ ПУСТОЙ

**Найдено:**
- ✅ Позиции закрываются на бирже
- ✅ Логи показывают закрытие позиций
- ❌ **CSV файл пустой** - записи не сохраняются

**Причина:**
- При закрытии через WebSocket (Partial TP, автоматическое закрытие) вызывается `_handle_position_closed_via_ws()`
- Этот метод **НЕ вызывает** `record_trade()` для записи в CSV
- Запись в CSV происходит только при закрытии через `close_position_manually()` (API)

**Решение:**
- Нужно добавить запись в CSV при закрытии через WebSocket
- Получить данные о закрытой позиции и записать в `performance_tracker`

---

## ⚠️ ПРОБЛЕМА #2: PARTIAL TP СРАБАТЫВАЕТ СЛИШКОМ РАНО

**Найдено в логах:**
```
📊 Partial TP для DOGE-USDT: режим=ranging, fraction=60.0%, trigger=0.40%
📊 Partial TP прогресс DOGE-USDT: pnl=1.35% / триггер=0.40% (337%, done=False)
📌 Partial TP DOGE-USDT: выставляем лимит sell 0.636000 контрактов @ 0.1412
```

**Проблема:**
- Partial TP срабатывает при прибыли **0.40%** (триггер)
- Это **слишком рано** для "качественной торговли"
- Мы хотели увеличить `min_profit_to_close` до **1.2%** для ranging, но Partial TP все еще срабатывает при 0.40%

**Анализ:**
- `min_profit_to_close=1.2%` - это минимальный профит для **полного закрытия** через trailing stop
- Partial TP имеет свой триггер `trigger=0.40%` - это отдельный параметр
- Нужно проверить, где настраивается триггер Partial TP

**Решение:**
- Увеличить триггер Partial TP с 0.40% до 1.0-1.2% для ranging режима
- Это позволит позициям расти до большего профита перед частичным закрытием

---

## ⚠️ ПРОБЛЕМА #3: ПОЗИЦИИ ЗАКРЫВАЮТСЯ СЛИШКОМ БЫСТРО

**Найдено:**
- DOGE-USDT: 38 секунд, 37 секунд
- BTC-USDT: 11 секунд

**Проблема:**
- Позиции закрываются **быстрее**, чем `min_holding_minutes=1.5 мин` (90 секунд)
- Это происходит из-за Partial TP, который не учитывает `min_holding_minutes`

**Анализ:**
- `min_holding_minutes` применяется только к **полному закрытию** через trailing stop
- Partial TP может сработать **до** достижения минимального времени удержания
- Это может быть проблемой, так как мы хотим держать позиции дольше

**Решение:**
- Добавить проверку `min_holding_minutes` перед размещением Partial TP ордера
- Или увеличить триггер Partial TP, чтобы он срабатывал позже

---

## 📊 СТАТИСТИКА ЗАКРЫТИЙ

### Прибыльные сделки:
- DOGE-USDT: +0.39 USDT (+1.31%) - 38 сек
- DOGE-USDT: +0.23 USDT (+0.77%) - 37 сек
- BTC-USDT: +0.03 USDT (+0.18%) - частично

### Убыточные сделки:
- BTC-USDT: -0.04 USDT (-0.33%) - 11 сек

### Win Rate:
- 3 прибыльных из 4 = **75%** ✅ (хорошо!)

### Средняя прибыль на сделку:
- Прибыльные: (0.39 + 0.23 + 0.03) / 3 = **0.22 USDT** ✅
- Это **лучше**, чем было раньше (~0.4 USDT на сделку, но с учетом комиссий)

---

## 🎯 ВЫВОДЫ

### ✅ Что работает:
1. ✅ Позиции открываются и закрываются
2. ✅ Win rate = 75% (хорошо!)
3. ✅ Прибыль на сделку увеличилась (0.22 USDT vs 0.4 USDT раньше, но с учетом комиссий это лучше)
4. ✅ Trailing stop работает (но не успевает сработать из-за Partial TP)

### ⚠️ Что нужно исправить:
1. ⚠️ **CSV файл пустой** - нужно добавить запись при закрытии через WebSocket
2. ⚠️ **Partial TP срабатывает слишком рано** (0.40%) - нужно увеличить триггер до 1.0-1.2%
3. ⚠️ **Позиции закрываются слишком быстро** - нужно добавить проверку `min_holding_minutes` для Partial TP

---

## 📝 РЕКОМЕНДАЦИИ

1. **Исправить запись в CSV:**
   - Добавить `record_trade()` в `_handle_position_closed_via_ws()`
   - Получить данные о закрытой позиции и записать в `performance_tracker`

2. **Увеличить триггер Partial TP:**
   - Для ranging: с 0.40% до 1.0-1.2%
   - Это позволит позициям расти до большего профита

3. **Добавить проверку времени для Partial TP:**
   - Не размещать Partial TP ордер, если позиция держится меньше `min_holding_minutes`

---

**Статус:** ⚠️ Позиции закрываются, но есть проблемы с записью в CSV и слишком ранним срабатыванием Partial TP.



