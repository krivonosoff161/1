# ПРОБЛЕМА: FALLBACK ВСЕ ЕЩЕ ИСПОЛЬЗУЕТСЯ

**Дата:** 23 ноября 2025  
**Проблема:** Fallback все еще используется для BTC-USDT и DOGE-USDT

---

## ⚠️ ПРОБЛЕМА

### Логи показывают:
```
⚠️ FALLBACK: Используется глобальный offset из конфига: 0.0% 
(per-symbol+regime и per-regime не найдены для BTC-USDT, regime=ranging)

⚠️ FALLBACK: Используется глобальный offset из конфига: 0.0% 
(per-symbol+regime и per-regime не найдены для DOGE-USDT, regime=ranging)
```

### Это означает:
- Настройки `by_symbol` не читаются из конфига
- Используется глобальный fallback (0.0%) вместо per-symbol+regime настроек

---

## ✅ ДОБАВЛЕНО ЛОГИРОВАНИЕ

Добавлено детальное логирование для диагностики:
1. Проверка типа и ключей `by_symbol_dict`
2. Подробная информация о том, почему используется fallback

---

## 🔍 НУЖНО ПРОВЕРИТЬ

После перезапуска бота проверить логи:
1. Что показывает `by_symbol_dict type` и `keys`?
2. Существует ли `by_symbol` в `limit_order_config`?
3. Правильно ли преобразуется в dict?

---

**Статус:** ⚠️ ТРЕБУЕТСЯ ДИАГНОСТИКА

