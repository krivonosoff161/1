# ✅ ПРИМЕНЕНЫ ИСПРАВЛЕНИЯ COPILOT (06.01.2026)

**Дата:** 2026-01-06  
**Основание:** Рекомендации Copilot после бэктестирования  
**Файл:** `docs/CURSOR_REPORT.md`

---

## 📊 РЕЗУЛЬТАТЫ БЭКТЕСТИРОВАНИЯ

**Проблемы выявлены:**
- Win Rate: **23.1%** (очень низкий)
- Profit Factor: **1.05** (едва прибыльный)
- Total PnL: **0.6499** (mock candles)
- **914 сделок** - слишком много слабых/ложных сигналов

**Диагноз Copilot:**
- Качество сигналов плохое: слишком много слабых/ложных сигналов в ranging/choppy режимах
- Фильтры в коде нужно усилить сверх текущих порогов конфига

---

## ✅ ПРИМЕНЕННЫЕ ИСПРАВЛЕНИЯ

### 1. Мульти-индикаторное подтверждение в `_detect_impulse_signals()`

**Проблема:**
- Импульсные сигналы генерируются только на основе body/ATR, volume и pivot breakout
- Нет проверки других индикаторов (MACD, RSI, BB, EMA)

**Решение:**
Добавлена система scoring с весами:
- **MACD crossover:** вес 3 (самый важный)
- **RSI overbought/oversold:** вес 2
- **Bollinger Bands breakout:** вес 1
- **EMA crossover:** вес 1

**Требование:**
- Минимум **4 балла** (хотя бы 2 подтверждения)
- Например: MACD (3) + RSI (2) = 5 баллов ✅
- Или: MACD (3) + BB (1) = 4 балла ✅

**Код:**
```python
score = 0
confirmation_details = []

# 1. MACD crossover (вес 3)
if macd_crossover:
    score += 3
    confirmation_details.append("MACD crossover")

# 2. RSI overbought/oversold (вес 2)
if (direction == "buy" and rsi_oversold) or (direction == "sell" and rsi_overbought):
    score += 2
    confirmation_details.append("RSI ...")

# 3. Bollinger Bands breakout (вес 1)
if bb_breakout:
    score += 1
    confirmation_details.append("BB breakout")

# 4. EMA crossover (вес 1)
if ema_crossover:
    score += 1
    confirmation_details.append("EMA crossover")

# Требуем минимум 4 балла
if score < 4:
    return []  # Блокируем слабый сигнал
```

**Файл:**
- `src/strategies/scalping/futures/signal_generator.py` (строки 5393-5456)

---

### 2. Улучшенный фильтр объема

**Проблема:**
- Старый фильтр проверял объем относительно среднего по lookback
- Это могло пропускать низкообъемные сигналы (шум)

**Решение:**
Добавлена проверка объема относительно **SMA20**:
- Текущий объем должен быть **>= 1.1x SMA20**
- Блокирует низкообъемные сигналы (шум)

**Код:**
```python
vol_cur = current_candle.volume
vol_sma20 = sum(c.volume for c in candles[-20:]) / 20 if len(candles) >= 20 else 0
if vol_sma20 > 0 and vol_cur < vol_sma20 * 1.1:
    logger.debug(f"🚫 Импульсный сигнал {symbol} заблокирован: низкий объем")
    return []
```

**Файл:**
- `src/strategies/scalping/futures/signal_generator.py` (строки 5359-5369)

---

### 3. ADX gate перед добавлением импульсных сигналов

**Проблема:**
- Импульсные сигналы генерировались даже при слабом ADX
- В ranging/choppy режимах это приводило к ложным сигналам

**Решение:**
Повышены требования к ADX в зависимости от режима:
- **Trending:** ADX >= 20.0
- **Ranging:** ADX >= 30.0 (строже)
- **Choppy:** ADX >= 40.0 (самое строгое)

**Код:**
```python
adx_min_required = 20.0  # По умолчанию для trending
if regime_key == "ranging":
    adx_min_required = 30.0
elif regime_key == "choppy":
    adx_min_required = 40.0

if adx_value is None or adx_value < adx_min_required:
    logger.debug(f"🚫 Импульсный сигнал {symbol} заблокирован: ADX={adx_value:.1f} < {adx_min_required:.1f}")
    return []
```

**Файл:**
- `src/strategies/scalping/futures/signal_generator.py` (строки 5378-5391)

---

## 📈 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### Win Rate
- **Было:** 23.1%
- **Ожидается:** 45-55%
- **Причина:** Мульти-индикаторное подтверждение отсеет 50-70% слабых сигналов

### Profit Factor
- **Было:** 1.05
- **Ожидается:** 1.3-1.5
- **Причина:** Только качественные сигналы с несколькими подтверждениями

### Количество сделок
- **Было:** 914 сделок
- **Ожидается:** 300-500 сделок
- **Причина:** Фильтры отсеют слабые сигналы

### Качество сигналов
- **Было:** Много ложных сигналов в ranging/choppy
- **Ожидается:** Только сильные сигналы с подтверждениями
- **Причина:** ADX gate + мульти-индикаторное подтверждение

---

## 🔍 ЛОГИРОВАНИЕ

**Добавлено логирование блокировок:**
- `🚫 Импульсный сигнал {symbol} заблокирован: низкий объем`
- `🚫 Импульсный сигнал {symbol} заблокирован: ADX={adx_value:.1f} < {adx_min_required:.1f}`
- `🚫 Импульсный сигнал {symbol} {direction} заблокирован: недостаточно подтверждений (score={score}/4)`

**Улучшено логирование успешных сигналов:**
- `🚀 Импульсный сигнал {symbol} {direction}: ..., подтверждения: {', '.join(confirmation_details)} (score={score})`

---

## ✅ СТАТУС

Все три исправления применены и готовы к тестированию.

**Следующий шаг:** Запустить бэктест и проверить результаты.

---

## 📝 ПРИМЕЧАНИЯ

1. **Мульти-индикаторное подтверждение:**
   - Система scoring гибкая - можно настроить веса и порог
   - Текущий порог 4 балла требует минимум 2 подтверждения

2. **Фильтр объема:**
   - Использует SMA20 для более стабильной оценки
   - Порог 1.1x можно настроить в конфиге

3. **ADX gate:**
   - Адаптивные пороги по режимам
   - Более строгие требования в ranging/choppy (где больше ложных сигналов)
