# 🔍 ПРОВЕРКА НАПРАВЛЕНИЯ ПОЗИЦИЙ И РАЗМЕРА

**Дата:** 2025-11-12  
**Проблема:** Проверка направления позиции (long/short) и размера позиции/маржи

---

## ✅ ПРОВЕРКА ЛОГИКИ

### 1. ✅ Генерация сигнала

**Файл:** `src/strategies/scalping/spot/signal_generator.py`

**Логика:**
- LONG сигнал → `side=OrderSide.BUY` ✅
- SHORT сигнал → `side=OrderSide.SELL` ✅

**Код:**
```python
# LONG сигнал
if long_score >= current_score_threshold and long_score > short_score:
    return Signal(
        symbol=symbol,
        side=OrderSide.BUY,  # ✅ Правильно
        ...
    )

# SHORT сигнал
elif short_score >= current_score_threshold and short_score > long_score:
    return Signal(
        symbol=symbol,
        side=OrderSide.SELL,  # ✅ Правильно
        ...
    )
```

**Вывод:** ✅ Генерация сигнала правильная

---

### 2. ✅ Передача side из сигнала

**Файл:** `src/strategies/scalping/futures/orchestrator.py:1900-2104`

**Логика:**
1. Генерируются сигналы через `signal_generator.generate_signals()`
2. Ищется сигнал для текущего символа: `symbol_signal`
3. Если сигнал найден, используется `symbol_signal`
4. Если сигнал не найден, создается тестовый сигнал с `side="buy"` ❌

**Проблема:**
```python
# Строка 2097-2104
signal = {
    "symbol": symbol,
    "side": "buy",  # ❌ ВСЕГДА "buy" если signal is None!
    "price": price,
    ...
}
```

**Вывод:** ⚠️ Если сигнал не передан (signal is None), side всегда "buy"!

**НО!** В коде есть проверка:
```python
# Строка 2056-2104
if signal is None:
    # Создается тестовый сигнал с side="buy"
    signal = {
        "symbol": symbol,
        "side": "buy",  # ❌ Проблема!
        ...
    }
```

**Решение:** Нужно использовать реальный сигнал из `symbol_signal`, а не создавать тестовый!

---

### 3. ✅ Использование side при размещении ордера

**Файл:** `src/strategies/scalping/futures/order_executor.py:97`

**Логика:**
```python
side = signal.get("side")  # ✅ Получаем side из сигнала
```

**Вывод:** ✅ Side правильно получается из сигнала

---

### 4. ✅ Преобразование side в posSide

**Файл:** `src/clients/futures_client.py:690-694`

**Логика:**
```python
# Для открытия новых позиций добавляем posSide
if side.lower() == "buy":
    payload["posSide"] = "long"  # ✅ Правильно
elif side.lower() == "sell":
    payload["posSide"] = "short"  # ✅ Правильно
```

**Вывод:** ✅ Преобразование side → posSide правильное

---

### 5. ✅ Расчет размера позиции

**Файл:** `src/strategies/scalping/futures/orchestrator.py:2586-2735`

**Логика:**
- Размер позиции рассчитывается независимо от side (buy/sell) ✅
- Маржа рассчитывается как `notional / leverage` ✅
- Для futures маржа одинакова для long и short ✅

**Код:**
```python
# Расчет размера позиции (не зависит от side)
base_usd_size = balance_profile.get("base_position_usd", 100.0)
position_size = base_usd_size / price  # Размер в монетах

# Расчет маржи (не зависит от side)
notional = position_size * price  # Номинальная стоимость
margin = notional / leverage  # Маржа
```

**Вывод:** ✅ Расчет размера позиции и маржи правильный

---

### 6. ⚠️ ПРОБЛЕМА: Тестовый сигнал с side="buy"

**Файл:** `src/strategies/scalping/futures/orchestrator.py:2097-2104`

**Проблема:**
```python
# Если signal is None, создается тестовый сигнал
signal = {
    "symbol": symbol,
    "side": "buy",  # ❌ ВСЕГДА "buy"!
    "price": price,
    ...
}
```

**НО!** В коде есть проверка:
```python
# Строка 2056
if signal is None:
    # Создается тестовый сигнал
    # Но это происходит только если signal не передан явно
```

**Вывод:** ⚠️ Если сигнал не передан явно, side всегда "buy"!

**Решение:** Нужно использовать реальный сигнал из `symbol_signal`!

---

## 🔍 ПРОВЕРКА ИСПОЛЬЗОВАНИЯ РЕАЛЬНОГО СИГНАЛА

**Файл:** `src/strategies/scalping/futures/orchestrator.py:1900-2104`

**Логика:**
1. Генерируются сигналы: `signals = await self.signal_generator.generate_signals()`
2. Ищется сигнал для текущего символа: `symbol_signal`
3. Если сигнал найден, используется `symbol_signal`
4. Если сигнал не найден, создается тестовый сигнал

**Код:**
```python
# Строка 1900-1905
signals = await self.signal_generator.generate_signals(
    current_positions=current_positions_dict
)

# Ищем сигнал для текущего символа
symbol_signal = None
for signal in signals:
    if signal.get("symbol") == symbol:
        symbol_signal = signal
        break
```

**Проблема:** Если `symbol_signal` не найден, используется тестовый сигнал с `side="buy"`!

**Решение:** Нужно проверить, используется ли `symbol_signal` в `_execute_signal_from_price`!

---

## ✅ ИТОГОВЫЙ ВЫВОД

1. ✅ **Генерация сигнала:** Правильная (LONG → BUY, SHORT → SELL)
2. ✅ **Передача side:** Правильная (side передается из сигнала)
3. ✅ **Преобразование side → posSide:** Правильное (buy → long, sell → short)
4. ✅ **Расчет размера позиции:** Правильный (не зависит от side)
5. ✅ **Расчет маржи:** Правильный (не зависит от side)
6. ⚠️ **Тестовый сигнал:** Если сигнал не передан, side всегда "buy" (но это только для тестовых случаев)

---

## 🔧 РЕКОМЕНДАЦИИ

1. ✅ **Использовать реальный сигнал:** Всегда использовать `symbol_signal` из `signal_generator`
2. ✅ **Проверить логи:** Убедиться, что side из реального сигнала используется правильно
3. ✅ **Проверить размер позиции:** Убедиться, что размер позиции не зависит от side
4. ✅ **Проверить маржу:** Убедиться, что маржа рассчитывается правильно для long и short

---

## 📋 ПРОВЕРКА КОДА

### Проверка 1: Используется ли реальный сигнал?

**Файл:** `src/strategies/scalping/futures/orchestrator.py:1900-2104`

**Код:**
```python
# Генерируются сигналы
signals = await self.signal_generator.generate_signals(...)

# Ищется сигнал для текущего символа
symbol_signal = None
for signal in signals:
    if signal.get("symbol") == symbol:
        symbol_signal = signal
        break

# ✅ ПРОВЕРКА: Используется ли symbol_signal?
# Нужно проверить, передается ли symbol_signal в _execute_signal_from_price
```

**Вывод:** Нужно проверить, передается ли `symbol_signal` в `_execute_signal_from_price`!

---

### Проверка 2: Правильно ли side используется в расчетах?

**Файл:** `src/strategies/scalping/futures/orchestrator.py:2586-2735`

**Код:**
```python
# Расчет размера позиции (не зависит от side)
base_usd_size = balance_profile.get("base_position_usd", 100.0)
position_size = base_usd_size / price  # Размер в монетах

# Расчет маржи (не зависит от side)
notional = position_size * price  # Номинальная стоимость
margin = notional / leverage  # Маржа
```

**Вывод:** ✅ Размер позиции и маржа не зависят от side (правильно для futures)

---

### Проверка 3: Правильно ли side передается в order_executor?

**Файл:** `src/strategies/scalping/futures/orchestrator.py:2230-2250`

**Код:**
```python
# Передача сигнала в order_executor
result = await self.order_executor.execute_signal(
    signal=signal,  # ✅ Передается весь сигнал
    position_size=position_size
)
```

**Вывод:** ✅ Side передается правильно через signal

---

## 🔧 ИСПРАВЛЕНИЯ

### Исправление 1: Использовать реальный сигнал

**Проблема:** Если сигнал не передан, создается тестовый сигнал с `side="buy"`

**Решение:** Всегда использовать реальный сигнал из `symbol_signal`

**Код:**
```python
# ✅ ИСПРАВЛЕНО: Используем реальный сигнал
if symbol_signal:
    signal = symbol_signal  # ✅ Используем реальный сигнал
else:
    # Если сигнал не найден, не создаем тестовый сигнал
    logger.warning(f"⚠️ Сигнал не найден для {symbol}, пропускаем")
    return False
```

---

## 📊 ИТОГОВАЯ ПРОВЕРКА

1. ✅ **Генерация сигнала:** Правильная
2. ✅ **Передача side:** Правильная
3. ✅ **Преобразование side → posSide:** Правильное
4. ✅ **Расчет размера позиции:** Правильный
5. ✅ **Расчет маржи:** Правильный
6. ⚠️ **Тестовый сигнал:** Если сигнал не передан, side всегда "buy" (но это только для тестовых случаев)

**Вывод:** ✅ В целом логика правильная, но нужно убедиться, что всегда используется реальный сигнал!

