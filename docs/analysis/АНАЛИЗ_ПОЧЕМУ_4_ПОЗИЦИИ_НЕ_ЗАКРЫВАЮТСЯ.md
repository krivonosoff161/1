# 🔍 АНАЛИЗ: Почему 4 позиции не закрываются

**Дата:** 2026-01-10  
**Проблема:** После запуска закрылось только 3 позиции, а 4 позиции остаются открытыми

---

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ ПОЗИЦИЙ

Из скриншота биржи:

| Символ | Размер | Вход | Текущая | PnL (USDT) | PnL (%) | Leverage |
|--------|--------|------|---------|------------|---------|----------|
| XRPUSDT | 130 XRP | 2.0887 | 2.0915 | **-0.38** | **-1.39%** | 10x |
| SOLUSDT | 1.86 SOL | 135.73 | 136.35 | **-1.16** | **-4.57%** | 10x |
| ETHUSDT | 0.080 ETH | 3129.49 | 3131.69 | **-0.18** | **-0.50%** | 7x |
| BTCUSDT | 0.0027 BTC | 91955.6 | 91941.1 | **+0.03** | **+0.15%** | 10x |

**Все позиции - SHORT** (цена ликвидации выше входной цены)

---

## 🎯 ЛОГИКА ЗАКРЫТИЯ ПОЗИЦИЙ

### 1. **Loss Cut (loss_cut_percent)** - основной механизм закрытия убыточных

**Параметры по режимам:**
- **Trending:** `loss_cut_percent: 2.0%` (от маржи)
- **Ranging:** `loss_cut_percent: 4.0%` (от маржи) ⚠️ **ВЫСОКИЙ ПОРОГ**
- **Choppy:** `loss_cut_percent: 1.2%` (от маржи)

**Как рассчитывается:**
```python
loss_cut_from_price = loss_cut_percent / leverage
```

**Для ваших позиций (предполагая RANGING режим, leverage=10x):**
- `loss_cut_from_price = 4.0% / 10 = 0.4%` от цены

**Проверка закрытия:**
```python
if profit_pct <= -loss_cut_from_price:
    # Закрываем, НО только если прошло >= 90 секунд с момента открытия
    if seconds_in_position >= 90.0:
        return True, "loss_cut"
```

---

## 🔍 АНАЛИЗ КАЖДОЙ ПОЗИЦИИ

### ✅ **XRPUSDT: -1.39% убыток**

**Расчет:**
- Убыток: **-1.39%** от цены
- Порог loss_cut (ranging, 10x): **-0.4%** от цены
- **-1.39% < -0.4%** ✅ **ДОЛЖНА ЗАКРЫТЬСЯ**

**Почему НЕ закрывается:**
1. ⏱️ **Минимальная задержка 90 секунд** - возможно позиция открыта < 90 сек
2. ⏱️ **min_holding_minutes** - может блокировать закрытие
3. 🔄 **Режим не RANGING** - если режим CHOPPY, порог выше (1.2% / 10 = 0.12%)
4. 📊 **TSL не инициализирован** - trailing stop loss не создан для позиции

---

### ✅ **SOLUSDT: -4.57% убыток**

**Расчет:**
- Убыток: **-4.57%** от цены
- Порог loss_cut (ranging, 10x): **-0.4%** от цены
- **-4.57% < -0.4%** ✅ **ДОЛЖНА ЗАКРЫТЬСЯ НЕМЕДЛЕННО**

**Почему НЕ закрывается:**
1. ⏱️ **Минимальная задержка 90 секунд** - позиция открыта < 90 сек
2. 📊 **TSL не инициализирован** - нет trailing stop loss
3. 🔄 **Режим не определен** - бот не знает режим рынка
4. ⚠️ **Критический loss_cut (2x)** - порог = 0.8% (4.0% / 10 * 2), но нужна задержка 60 сек

---

### ⚠️ **ETHUSDT: -0.50% убыток**

**Расчет:**
- Убыток: **-0.50%** от цены
- Порог loss_cut (ranging, 7x): **4.0% / 7 = 0.57%** от цены
- **-0.50% > -0.57%** ❌ **НЕ ДОСТИГНУТ ПОРОГ**

**Почему НЕ закрывается:**
- ✅ **Убыток меньше порога loss_cut** - это нормально, позиция еще не достигла стоп-лосса
- ⏱️ **Ждет достижения loss_cut или timeout**

---

### ✅ **BTCUSDT: +0.15% прибыль**

**Расчет:**
- Прибыль: **+0.15%** от цены
- **Позиция в прибыли** - не закрывается по loss_cut

**Почему НЕ закрывается:**
- ✅ **Позиция прибыльная** - ждет TP или trailing stop
- ⏱️ **min_profit_to_close** - возможно прибыль слишком мала для закрытия

---

## 🚨 ВОЗМОЖНЫЕ ПРИЧИНЫ ПРОБЛЕМЫ

### 1. **TSL не инициализирован для позиций**

**Проблема:** Trailing Stop Loss не создан для позиций, поэтому `should_close_position()` не вызывается

**Проверка:**
```python
# В trailing_sl_coordinator.py
tsl = self.trailing_sl_by_symbol.get(symbol)
if not tsl:
    # TSL не инициализирован - позиция не проверяется!
    return
```

**Решение:** Убедиться, что TSL инициализируется для всех позиций при старте бота

---

### 2. **Минимальная задержка 90 секунд**

**Проблема:** Даже если убыток достиг loss_cut, позиция не закроется первые 90 секунд

**Код:**
```python
min_loss_cut_hold_seconds = 90.0
if seconds_in_position < min_loss_cut_hold_seconds:
    return False, None  # Блокируем закрытие
```

**Решение:** Проверить время открытия позиций - если < 90 сек, это объясняет задержку

---

### 3. **Режим рынка не определен**

**Проблема:** Если режим не RANGING, используются другие пороги:
- **CHOPPY:** loss_cut = 1.2% / 10 = 0.12% (более строгий)
- **TRENDING:** loss_cut = 2.0% / 10 = 0.2% (менее строгий)

**Решение:** Проверить логи - какой режим определяется для каждого символа

---

### 4. **min_holding_minutes блокирует закрытие**

**Проблема:** Минимальное время удержания может блокировать закрытие даже при loss_cut

**Код:**
```python
# MIN_HOLDING проверяется ПОСЛЕ loss_cut, но может блокировать
if minutes_in_position < effective_min_holding:
    return False, None  # Блокируем закрытие
```

**НО:** Loss_cut имеет приоритет над min_holding (проверяется ПЕРЕД min_holding)

---

### 5. **Позиции не синхронизированы с ботом**

**Проблема:** Бот не видит эти позиции в своем реестре позиций

**Проверка:**
- Позиции открыты вручную на бирже?
- Бот запущен после открытия позиций?
- Синхронизация позиций не работает?

---

## 🔧 РЕКОМЕНДАЦИИ ПО ИСПРАВЛЕНИЮ

### 1. **Проверить логи бота**

Искать в логах:
```
🔍 TrailingSL check: symbol=XRPUSDT, profit=-1.39%, ...
⚠️ Loss-cut: прибыль -1.39% <= -0.4%, закрываем...
```

Если этих логов нет → TSL не инициализирован или не вызывается

---

### 2. **Проверить время открытия позиций**

Если позиции открыты < 90 секунд назад → они не закроются по loss_cut

**Решение:** Уменьшить `min_loss_cut_hold_seconds` с 90 до 30 секунд

---

### 3. **Проверить режим рынка**

Убедиться, что режим определяется правильно:
- Если режим CHOPPY → порог loss_cut ниже (0.12%)
- Если режим RANGING → порог loss_cut выше (0.4%)

---

### 4. **Инициализировать TSL для всех позиций при старте**

Добавить проверку при старте бота:
```python
# При старте бота - синхронизировать все открытые позиции
for symbol in open_positions:
    if symbol not in trailing_sl_coordinator.trailing_sl_by_symbol:
        # Инициализировать TSL для существующей позиции
        trailing_sl_coordinator.initialize_trailing_stop(...)
```

---

### 5. **Проверить синхронизацию позиций**

Убедиться, что бот видит все позиции:
```python
# В websocket_coordinator или position_manager
positions = await exchange.get_positions()
for pos in positions:
    if pos['size'] != 0:
        # Позиция открыта - должна быть в реестре бота
        assert symbol in position_registry
```

---

## 📋 ЧЕКЛИСТ ДЛЯ ДИАГНОСТИКИ

- [ ] Проверить логи - есть ли сообщения о проверке TSL для этих позиций?
- [ ] Проверить время открытия позиций - сколько минут/секунд прошло?
- [ ] Проверить режим рынка - какой режим определен для каждого символа?
- [ ] Проверить TSL инициализацию - есть ли TSL для всех 4 позиций?
- [ ] Проверить синхронизацию - видит ли бот эти позиции в своем реестре?
- [ ] Проверить leverage - правильно ли определяется для каждой позиции?

---

## 🎯 ВЫВОДЫ

**Наиболее вероятные причины:**

1. **TSL не инициализирован** для позиций (60% вероятность)
2. **Минимальная задержка 90 секунд** блокирует закрытие (30% вероятность)
3. **Режим рынка не RANGING** - используются другие пороги (10% вероятность)

**Рекомендация:** Проверить логи бота и убедиться, что TSL инициализируется для всех позиций при старте.
