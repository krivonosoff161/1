# 🔴 МАСТЕР-ПЛАН ИСПРАВЛЕНИЯ ВСЕХ ПРОБЛЕМ

**Дата:** 2025-12-20  
**Источники:** 
- Анализ логов тестовой сессии (5 часов 29 минут)
- Комплексная проверка работы бота
- Проверка неинициализированных модулей

---

## 📊 СТАТИСТИКА ПРОБЛЕМ

- **Критические (блокируют работу):** 4
- **Важные (влияют на работу):** 6
- **Низкий приоритет:** 5
- **Всего проблем:** 15

---

## 🔴 ПРИОРИТЕТ 1: КРИТИЧЕСКИЕ ОШИБКИ (БЛОКИРУЮТ РАБОТУ)

### 1. ❌ ExitAnalyzer: Ошибка сравнения типов

**Ошибка:** `'>' not supported between instances of 'str' and 'int'`

**Место:** `src/strategies/scalping/futures/positions/exit_analyzer.py:1933-1936`

**Частота:** Сотни раз (блокирует анализ позиций)

**Проблема:**
- Сравнение `minutes_in_position >= actual_max_holding`
- `minutes_in_position` возвращает строку вместо числа
- Или `actual_max_holding` возвращает строку из конфига

**Влияние:**
- ❌ Блокирует анализ позиций
- ❌ Решения о закрытии не принимаются
- ❌ Позиции не закрываются по логике бота
- ❌ Нулевая прибыль (позиции не фиксируются)

**TODO:**
- [ ] Найти место сравнения в `exit_analyzer.py:1933-1936`
- [ ] Проверить тип `minutes_in_position` (должен быть `float`)
- [ ] Проверить тип `actual_max_holding` (должен быть `float`)
- [ ] Привести оба значения к `float` перед сравнением
- [ ] Добавить проверку на `None` перед сравнением
- [ ] Протестировать исправление

**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py`

---

### 2. ❌ PeakProfitTracker: Ошибка сравнения NoneType

**Ошибка:** `'<=' not supported between instances of 'NoneType' and 'int'`

**Место:** `src/strategies/scalping/futures/positions/peak_profit_tracker.py:119`

**Частота:** Несколько раз

**Проблема:**
- Сравнение `None <= int` где-то в методе
- `peak_profit` или другой параметр равен `None`

**Влияние:**
- ❌ Не обновляется `peak_profit`
- ❌ Логика защиты прибыли не работает
- ❌ Позиции не защищаются от отката
- ❌ Теряем прибыль при откатах

**TODO:**
- [ ] Найти место сравнения в `peak_profit_tracker.py:119`
- [ ] Проверить все сравнения на `None`
- [ ] Добавить проверку `if value is not None:` перед сравнением
- [ ] Использовать безопасное сравнение или default значение
- [ ] Протестировать исправление

**Файл:** `src/strategies/scalping/futures/positions/peak_profit_tracker.py`

---

### 3. ❌ AdaptiveLeverage НЕ ИНТЕГРИРОВАН

**Проблема:** Все позиции открываются с фиксированным leverage 5x

**Место:** 
- `src/strategies/scalping/futures/orchestrator.py` (не инициализирован)
- `src/strategies/scalping/futures/coordinators/signal_coordinator.py:1495` (не используется)

**Проблема:**
- Модуль `AdaptiveLeverage` создан, но НЕ инициализирован
- В `signal_coordinator.py:1495` используется фиксированный leverage:
  ```python
  leverage_config = getattr(self.scalping_config, "leverage", None)  # Всегда 5x
  ```

**Влияние:**
- ❌ Все позиции с leverage 5x (нет оптимизации)
- ❌ Не учитывается сила сигнала
- ❌ Не учитывается режим рынка
- ❌ Не учитывается волатильность

**TODO:**
- [ ] Импортировать `AdaptiveLeverage` в `orchestrator.py`
- [ ] Инициализировать `self.adaptive_leverage = AdaptiveLeverage(config)` в `orchestrator.py`
- [ ] Передать `adaptive_leverage` в `signal_coordinator` при создании
- [ ] В `signal_coordinator.py:1495` заменить фиксированный leverage на:
  ```python
  # Получаем режим и волатильность
  regime = signal.get("regime") or "ranging"
  volatility = None  # Можно получить из DataRegistry или signal
  if self.data_registry:
      atr = await self.data_registry.get_indicator(symbol, "atr")
      if atr:
          volatility = atr / current_price  # ATR в процентах
  
  # Используем адаптивный леверидж
  leverage_config = self.adaptive_leverage.calculate_leverage(
      signal, regime, volatility
  )
  ```
- [ ] Протестировать с разными сигналами (разная сила)
- [ ] Проверить логи, что leverage меняется в зависимости от сигнала

**Файлы:**
- `src/strategies/scalping/futures/orchestrator.py`
- `src/strategies/scalping/futures/coordinators/signal_coordinator.py`

---

### 4. ❌ DRIFT_ADD - Рассинхронизация позиций

**Ошибка:** `DRIFT_ADD {SYMBOL} found on exchange but not in registry`

**Место:** `src/strategies/scalping/futures/orchestrator.py:_sync_positions_with_exchange:1743`

**Частота:** Несколько раз (ETH, DOGE, XRP, BTC, SOL)

**Проблема:**
- Позиции на бирже, но не в локальном реестре
- Рассинхронизация между биржей и локальным реестром

**Влияние:**
- ❌ Бот не видит позиции на бирже
- ❌ Не управляет ими
- ❌ Может открывать дубликаты
- ❌ Потеря контроля над позициями

**TODO:**
- [ ] Найти место обработки DRIFT_ADD в `orchestrator.py:1743`
- [ ] Вместо только логирования CRITICAL, добавить автоматическое добавление позиции в реестр
- [ ] Создать `PositionMetadata` из данных биржи
- [ ] Зарегистрировать позицию в `position_registry`
- [ ] Логировать добавление позиции
- [ ] Протестировать синхронизацию после перезапуска

**Файл:** `src/strategies/scalping/futures/orchestrator.py`

---

## 🟠 ПРИОРИТЕТ 2: ВАЖНЫЕ ОШИБКИ (ВЛИЯЮТ НА РАБОТУ)

### 5. ❌ Низкая эффективность ордеров (11.2%)

**Проблема:** Только 20 из 179 ордеров исполнено

**Статистика:**
- Размещено: 179
- Исполнено: 20
- Эффективность: 11.2% ❌
- Таймаутов: 472

**Причины:**
1. Таймауты: Ордера не исполняются в течение `max_wait_seconds` (30 сек)
2. Post-only ордера: Могут не исполняться, если цена уходит
3. Цена ордера: Возможно, размещается далеко от рынка

**TODO:**
- [ ] Проверить логику замены post_only ордеров в `order_coordinator.py`
- [ ] Убедиться, что post_only ордера заменяются на обычные лимитные после 5 секунд
- [ ] Проверить, что цена ордера близка к `best_ask/best_bid`
- [ ] Уменьшить `max_wait_seconds` или улучшить логику размещения
- [ ] Добавить больше логирования для анализа неисполняющихся ордеров
- [ ] Проанализировать, почему ордера не исполняются (цена, таймаут, post_only)

**Файлы:**
- `src/strategies/scalping/futures/coordinators/order_coordinator.py`
- `src/strategies/scalping/futures/order_executor.py`

---

### 6. ❌ Ошибки 51006 (Цена вне лимитов)

**Ошибка:** `Order price is not within the price limit (max buy price: X, min sell price: Y)`

**Место:** `src/strategies/scalping/futures/order_executor.py:_place_limit_order:1618`

**Частота:** 6 раз

**Проблема:**
- Цена ордера выходит за лимиты биржи
- Не проверяются лимиты перед размещением

**TODO:**
- [ ] В `order_executor.py:_place_limit_order` перед размещением получить лимиты:
  ```python
  price_limits = await self.client.get_price_limits(symbol)
  max_buy_price = price_limits.get("max_buy_price")
  min_sell_price = price_limits.get("min_sell_price")
  ```
- [ ] Проверить, что цена ордера в пределах лимитов:
  ```python
  if side == "buy" and limit_price > max_buy_price:
      limit_price = max_buy_price * 0.9999  # Немного ниже лимита
  elif side == "sell" and limit_price < min_sell_price:
      limit_price = min_sell_price * 1.0001  # Немного выше лимита
  ```
- [ ] Логировать корректировку цены
- [ ] Протестировать с разными символами

**Файл:** `src/strategies/scalping/futures/order_executor.py`

---

### 7. ❌ LiquidationProtector не инициализирован

**Статус:** Передается как `None` в RiskManager

**Место:** `src/strategies/scalping/futures/orchestrator.py:461`

**Проблема:**
- В `orchestrator.py:461` передается `liquidation_protector=None`
- Модуль не инициализирован

**Влияние:**
- ⚠️ Нет дополнительной защиты от ликвидации
- ⚠️ Используется только базовый LiquidationGuard

**TODO:**
- [ ] Импортировать `LiquidationProtector` из `risk.liquidation_protector`
- [ ] Инициализировать `self.liquidation_protector = LiquidationProtector(...)` в `orchestrator.py`
- [ ] Передать в `risk_manager` вместо `None`
- [ ] Протестировать защиту от ликвидации

**Файл:** `src/strategies/scalping/futures/orchestrator.py`

---

### 8. ❌ MarginMonitor не инициализирован

**Статус:** Передается как `None` в RiskManager

**Место:** `src/strategies/scalping/futures/orchestrator.py:462`

**Проблема:**
- В `orchestrator.py:462` передается `margin_monitor=None`
- Модуль не инициализирован

**Влияние:**
- ⚠️ Нет дополнительного мониторинга маржи
- ⚠️ Используется только базовый мониторинг

**TODO:**
- [ ] Импортировать `MarginMonitor` из `risk.margin_monitor`
- [ ] Инициализировать `self.margin_monitor = MarginMonitor(...)` в `orchestrator.py`
- [ ] Передать в `risk_manager` вместо `None`
- [ ] Протестировать мониторинг маржи

**Файл:** `src/strategies/scalping/futures/orchestrator.py`

---

### 9. ❌ Ошибки 502 Bad Gateway от OKX

**Ошибка:** `OKX вернул HTML вместо JSON! Status: 502`

**Место:** `src/clients/futures_client.py:_make_request:152`

**Частота:** Несколько раз

**Проблема:**
- Проблемы на стороне биржи (Cloudflare)
- Временные сбои API

**TODO:**
- [ ] Добавить retry логику для 502 ошибок
- [ ] Добавить задержку перед retry (exponential backoff)
- [ ] Логировать retry попытки
- [ ] Не блокировать работу при временных сбоях

**Файл:** `src/clients/futures_client.py`

---

### 10. ⚠️ Логика выхода полностью блокирована

**Проблемы:**
- ❌ ExitAnalyzer блокирован ошибкой сравнения
- ❌ PeakProfitTracker блокирован ошибкой NoneType
- ❌ Решения о закрытии не принимаются
- ❌ Позиции не закрываются по логике бота

**TODO:**
- [ ] Исправить ошибки в ExitAnalyzer (задача #1)
- [ ] Исправить ошибки в PeakProfitTracker (задача #2)
- [ ] После исправления протестировать логику закрытия
- [ ] Проверить, что позиции закрываются по TP/SL/Profit Harvest

**Зависит от:** Задачи #1 и #2

---

## 🟡 ПРИОРИТЕТ 3: НИЗКИЙ ПРИОРИТЕТ (ОПЦИОНАЛЬНО)

### 11. ⚠️ ExitDecisionLogger не инициализирован

**Статус:** Передается как `None` в ExitAnalyzer

**Место:** `src/strategies/scalping/futures/orchestrator.py:396`

**TODO:**
- [ ] Импортировать `ExitDecisionLogger` из `positions.exit_decision_logger`
- [ ] Инициализировать `self.exit_decision_logger = ExitDecisionLogger(...)` в `orchestrator.py`
- [ ] Передать в `exit_analyzer` вместо `None`
- [ ] Протестировать логирование решений

**Файл:** `src/strategies/scalping/futures/orchestrator.py`

---

### 12. ⚠️ PositionSizer не используется

**Статус:** Передается как `None` в EntryManager

**Место:** `src/strategies/scalping/futures/orchestrator.py:300`

**TODO:**
- [ ] Решить: использовать PositionSizer или оставить RiskManager
- [ ] Если использовать PositionSizer - интегрировать
- [ ] Если оставить RiskManager - удалить PositionSizer или пометить как deprecated

**Файл:** `src/strategies/scalping/futures/orchestrator.py`

---

### 13. ⚠️ CandleBuffer не используется

**Статус:** Не инициализирован

**Файл:** `src/strategies/scalping/futures/core/candle_buffer.py`

**TODO:**
- [ ] Проверить, используется ли CandleBuffer в других модулях
- [ ] Если используется - интегрировать в orchestrator
- [ ] Если не используется - удалить или пометить как deprecated

---

### 14. ⚠️ Логика входа не работает полностью

**Проблемы:**
- ✅ Сигналы генерируются
- ✅ Фильтры работают (ADX, MTF, Correlation)
- ❌ Ордера не исполняются (11.2% эффективность)
- ❌ Адаптивный леверидж не используется

**TODO:**
- [ ] Исправить эффективность ордеров (задача #5)
- [ ] Интегрировать адаптивный леверидж (задача #3)
- [ ] После исправления протестировать логику входа

**Зависит от:** Задачи #3 и #5

---

### 15. ⚠️ Адаптивные режимы работают частично

**Проблемы:**
- ✅ Режимы определяются (trending, ranging, choppy)
- ✅ Per-symbol режимы работают
- ❌ Адаптивный леверидж не учитывает режим
- ❌ Параметры режимов могут не применяться из-за ошибок

**TODO:**
- [ ] Интегрировать адаптивный леверидж (задача #3)
- [ ] Проверить, что параметры режимов применяются корректно
- [ ] Протестировать с разными режимами

**Зависит от:** Задачи #3

---

## 📊 СВОДНАЯ ТАБЛИЦА

| # | Проблема | Приоритет | Статус | Файлы |
|---|----------|-----------|--------|-------|
| 1 | ExitAnalyzer: ошибка сравнения | 🔴 Критично | ❌ | `exit_analyzer.py:1933-1936` |
| 2 | PeakProfitTracker: ошибка NoneType | 🔴 Критично | ❌ | `peak_profit_tracker.py:119` |
| 3 | AdaptiveLeverage не интегрирован | 🔴 Критично | ❌ | `orchestrator.py`, `signal_coordinator.py:1495` |
| 4 | DRIFT_ADD: рассинхронизация | 🔴 Критично | ❌ | `orchestrator.py:1743` |
| 5 | Низкая эффективность ордеров (11.2%) | 🟠 Важно | ❌ | `order_coordinator.py`, `order_executor.py` |
| 6 | Ошибки 51006 (цена вне лимитов) | 🟠 Важно | ❌ | `order_executor.py:1618` |
| 7 | LiquidationProtector не инициализирован | 🟠 Важно | ❌ | `orchestrator.py:461` |
| 8 | MarginMonitor не инициализирован | 🟠 Важно | ❌ | `orchestrator.py:462` |
| 9 | Ошибки 502 Bad Gateway | 🟠 Важно | ❌ | `futures_client.py:152` |
| 10 | Логика выхода блокирована | 🟠 Важно | ❌ | Зависит от #1, #2 |
| 11 | ExitDecisionLogger не инициализирован | 🟡 Низкий | ❌ | `orchestrator.py:396` |
| 12 | PositionSizer не используется | 🟡 Низкий | ❌ | `orchestrator.py:300` |
| 13 | CandleBuffer не используется | 🟡 Низкий | ❌ | `core/candle_buffer.py` |
| 14 | Логика входа не работает полностью | 🟡 Низкий | ❌ | Зависит от #3, #5 |
| 15 | Адаптивные режимы работают частично | 🟡 Низкий | ❌ | Зависит от #3 |

---

## 🎯 ПЛАН ДЕЙСТВИЙ

### Этап 1: Критические ошибки (блокируют работу)
1. Исправить ExitAnalyzer (задача #1)
2. Исправить PeakProfitTracker (задача #2)
3. Интегрировать AdaptiveLeverage (задача #3)
4. Исправить DRIFT_ADD (задача #4)

**Ожидаемый результат:** Бот сможет анализировать и закрывать позиции, использовать адаптивный леверидж

### Этап 2: Важные ошибки (влияют на работу)
5. Повысить эффективность ордеров (задача #5)
6. Исправить ошибки 51006 (задача #6)
7. Инициализировать LiquidationProtector (задача #7)
8. Инициализировать MarginMonitor (задача #8)
9. Обработать ошибки 502 (задача #9)

**Ожидаемый результат:** Бот будет эффективнее исполнять ордера, лучше защищен от рисков

### Этап 3: Опциональные улучшения
10-15. Низкоприоритетные задачи

**Ожидаемый результат:** Улучшенное логирование и мониторинг

---

## 📝 ПРИМЕЧАНИЯ

- **Задачи #1 и #2** блокируют работу ExitAnalyzer и PeakProfitTracker
- **Задача #3** критична для адаптивности левериджа
- **Задача #4** критична для синхронизации позиций
- **Задачи #5 и #6** влияют на эффективность торговли
- **Задачи #7 и #8** улучшат защиту от рисков

---

**Статус:** 🔴 4 КРИТИЧЕСКИЕ ПРОБЛЕМЫ ТРЕБУЮТ НЕМЕДЛЕННОГО ИСПРАВЛЕНИЯ


