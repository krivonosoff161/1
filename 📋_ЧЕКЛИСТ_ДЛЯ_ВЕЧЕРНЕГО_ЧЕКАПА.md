# 📋 Чеклист для вечернего чекапа логов

## ✅ Что проверено и работает

### 1. Инициализация
✅ Все модули инициализированы без ошибок  
✅ ADX Filter исправлен и работает  
✅ WebSocket подключен и работает  
✅ Торговый цикл запущен  

### 2. Открытие позиций
✅ Защита от дублирования работает (проверена на BTC-USDT)  
✅ Все фильтры работают (ADX, MTF, Pivot, Volume Profile, etc.)  
✅ Расчет размера позиции работает корректно  
✅ TrailingSL инициализируется автоматически  

### 3. ADX переключение направления
✅ **РАБОТАЕТ** - в логах видно переключения SHORT→LONG  
✅ Логируется корректно: `🔄 ADX переключил SHORT → LONG`  
✅ Используется переключенное направление для открытия  

### 4. Логирование
✅ Все события логируются  
✅ ADX переключения записываются  
✅ Проверки существующих позиций записываются с учетом переключения  

## 🔍 На что обратить внимание вечером

### 1. Ошибки в логах
```
grep -i "ERROR|CRITICAL|Exception|Traceback|❌" logs/futures/futures_main_2025-11-24.log
```
**Ожидается:** Ошибок нет (или только ожидаемые)

### 2. ADX переключения
```
grep -i "🔄 ADX переключил" logs/futures/futures_main_2025-11-24.log
```
**Что проверить:**
- Сколько раз переключилось направление
- Были ли переключения для разных символов
- Правильно ли логируется (показывает оригинальное и переключенное направление)

### 3. Открытие позиций
```
grep -i "позиция.*открыта|✅ Позиция.*открыта|OPENED" logs/futures/futures_main_2025-11-24.log
```
**Что проверить:**
- Сколько позиций открыто
- Были ли попытки открыть дубликаты (должны блокироваться)
- Все ли позиции открыты с правильным направлением

### 4. Защита от дублирования
```
grep -i "⚠️ Позиция.*УЖЕ ОТКРЫТА|БЛОКИРУЕМ|дубликат" logs/futures/futures_main_2025-11-24.log
```
**Что проверить:**
- Сколько раз сработала защита
- Правильно ли логируется (показывает переключение ADX если было)
- Нет ли ложных срабатываний

### 5. Закрытие позиций
```
grep -i "закрыт|CLOSED|TP|SL|TSL|loss_cut" logs/futures/futures_main_2025-11-24.log
```
**Что проверить:**
- По какой причине закрылись позиции (TP/SL/TSL)
- Были ли преждевременные закрытия
- Правильно ли работают адаптивные TP/SL

### 6. ADX расчеты
```
grep -i "ADX.*error|calculation error|ADX.*nan" logs/futures/futures_main_2025-11-24.log
```
**Что проверить:**
- Нет ли ошибок расчета ADX
- ADX=nan - это нормально при недостатке данных, не ошибка
- Все расчеты проходят без исключений

### 7. XRP сигналы
```
grep -i "XRP-USDT.*сигнал|XRP-USDT.*ADX|XRP-USDT.*открыт" logs/futures/futures_main_2025-11-24.log
```
**Что проверить:**
- Генерируются ли сигналы для XRP (после оптимизации)
- Какие фильтры блокируют (если блокируют)
- Были ли открыты позиции по XRP

## 📊 Статистика для сбора

### Сигналы
- Сколько сигналов сгенерировано
- Сколько переключено ADX (SHORT→LONG, LONG→SHORT)
- Сколько заблокировано ADX/MTF фильтрами
- Сколько открыто позиций

### Позиции
- Сколько открыто позиций
- Сколько закрыто по TP/SL/TSL
- Средняя прибыль/убыток
- Время удержания

### ADX переключения
- Символы с переключениями
- Направления переключений (SHORT→LONG vs LONG→SHORT)
- Результат переключений (прибыльность)

## ⚠️ Возможные проблемы (если появятся)

### 1. Дублирование позиций
**Признаки:** В логах несколько открытий одного символа в одном направлении  
**Решение:** Проверить защиту от дублирования, возможно race condition

### 2. ADX не переключает направление
**Признаки:** Много блокировок ADX, но нет переключений  
**Решение:** Проверить условия переключения в коде

### 3. Преждевременное закрытие
**Признаки:** Позиции закрываются раньше min_holding_minutes  
**Решение:** Проверить логику проверки min_holding

### 4. XRP не дает сигналов
**Признаки:** Нет сигналов для XRP-USDT  
**Решение:** Проверить параметры оптимизации, возможно еще снизить пороги

## ✅ Ожидаемое поведение

### Нормальное
- ✅ ADX переключает направление при сильном тренде
- ✅ Защита блокирует дубликаты
- ✅ Позиции открываются корректно
- ✅ Все логируется

### Проблемное (на что обратить внимание)
- ⚠️ Много ошибок в логах
- ⚠️ Дублирование позиций
- ⚠️ ADX не переключает направление
- ⚠️ Преждевременное закрытие позиций
- ⚠️ XRP не дает сигналов

## 📝 Быстрые команды для проверки

### Все ошибки
```bash
grep -i "ERROR|CRITICAL|Exception|Traceback|❌" logs/futures/futures_main_2025-11-24.log | tail -20
```

### ADX переключения
```bash
grep "🔄 ADX переключил" logs/futures/futures_main_2025-11-24.log
```

### Открытые позиции
```bash
grep "✅ Позиция.*открыта" logs/futures/futures_main_2025-11-24.log
```

### Защита от дублирования
```bash
grep "⚠️ Позиция.*УЖЕ ОТКРЫТА\|БЛОКИРУЕМ" logs/futures/futures_main_2025-11-24.log
```

### Закрытые позиции
```bash
grep "закрыт.*по\|CLOSED\|TP\|SL" logs/futures/futures_main_2025-11-24.log
```

## 🎯 Приоритет проверки

1. **Ошибки** - в первую очередь
2. **ADX переключения** - работает ли умное переключение
3. **Дублирование** - срабатывает ли защита
4. **XRP сигналы** - генерируются ли после оптимизации
5. **Закрытие позиций** - правильно ли работают TP/SL/TSL

---

**✅ Все готово к вечернему чекапу!**

Удачи с торговлей! 📈



