# Анализ отмены ордеров

## 🔴 ПРОБЛЕМА:

Бот отменяет лимитные ордера через 60 секунд, если они не исполняются.

### Причины:

1. **Отсутствие offset для лимитных ордеров:**
   - Лимитный ордер размещается по цене `best_bid` без offset (offset=0.000%)
   - Если цена не движется к `best_bid`, ордер не исполняется
   - Через 60 секунд ордер автоматически отменяется

2. **Ошибка конвертации размера при замене на рыночный ордер:**
   - Размер ордера (`sz`) возвращается в контрактах (например, 0.49 контрактов для DOGE)
   - `_place_market_order` ожидает размер в монетах (например, 490 DOGE)
   - Без конвертации размер получается слишком маленьким (0.49 < минимум 0.01)

## ✅ ИСПРАВЛЕНИЯ:

### 1. Добавлены адаптивные параметры `limit_offset_percent` в конфиг:

```yaml
order_executor:
  limit_order:
    limit_offset_percent: 0.05  # Базовый offset 0.05%
    by_regime:
      trending:
        limit_offset_percent: 0.08  # 0.08% для trending (быстрее исполнение)
        max_wait_seconds: 45  # 45 сек (быстрее отмена)
      ranging:
        limit_offset_percent: 0.05  # 0.05% для ranging (стандартное исполнение)
        max_wait_seconds: 60  # Стандартное время ожидания
      choppy:
        limit_offset_percent: 0.10  # 0.10% для choppy (быстрее исполнение)
        max_wait_seconds: 30  # 30 сек (быстрее отмена)
```

### 2. Исправлена конвертация размера при замене на рыночный ордер:

- Добавлена конвертация из контрактов в монеты через `ctVal`
- Размер теперь корректно конвертируется перед размещением рыночного ордера

### 3. Обновлен код для использования адаптивного `max_wait_seconds`:

- `max_wait_seconds` теперь читается из конфига по текущему режиму
- Адаптируется автоматически при смене режима рынка

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ:

1. **Лимитные ордера будут исполняться быстрее:**
   - С offset 0.05-0.10% ордера будут исполняться практически сразу
   - Реже будет необходимость в отмене и замене на рыночный ордер

2. **При замене на рыночный ордер размер будет корректным:**
   - Размер корректно конвертируется из контрактов в монеты
   - Рыночный ордер размещается с правильным размером

3. **Адаптивное время ожидания:**
   - В тренде: 45 сек (быстрее отмена для более быстрой торговли)
   - В боковике: 60 сек (стандартное время)
   - В хаосе: 30 сек (быстрее отмена для предотвращения зависания ордеров)

## ✅ СТАТУС ВЫПОЛНЕНИЯ:

- ✅ Добавлены адаптивные параметры `limit_offset_percent` в конфиг
- ✅ Обновлен код для использования адаптивного `max_wait_seconds` по режиму
- ✅ Исправлена конвертация размера при замене на рыночный ордер

## 🚀 ГОТОВО К ТЕСТИРОВАНИЮ!

Все исправления внесены. Лимитные ордера теперь будут:
1. Размещаться с адаптивным offset (0.05-0.10% в зависимости от режима)
2. Отменяться через адаптивное время (30-60 сек в зависимости от режима)
3. Корректно заменяться на рыночный ордер с правильным размером



