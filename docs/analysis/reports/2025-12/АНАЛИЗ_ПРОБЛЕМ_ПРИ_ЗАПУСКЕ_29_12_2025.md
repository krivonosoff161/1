# АНАЛИЗ ПРОБЛЕМ ПРИ ЗАПУСКЕ БОТА (29.12.2025)

**Дата:** 29.12.2025  
**Логи:** `logs/futures/archived/logs_2025-12-29_23-03-22`  
**Время запуска:** 22:27:36  
**Статус:** ⚠️ Обнаружены проблемы при запуске

---

## 📊 ОБЩАЯ СВОДКА ЗАПУСКА

### ✅ Успешная инициализация:
1. **Все модули инициализированы** - ConfigManager, DataRegistry, SignalGenerator, PositionManager и т.д.
2. **Свечи загружены** - 200 свечей для каждого символа (BTC, ETH, SOL, DOGE, XRP)
3. **WebSocket подключен** - Public и Private WebSocket работают
4. **Готовность подтверждена** - Сообщение "🟢 Все модули инициализированы — торговля разрешена" в 22:27:56.645
5. **Торговый цикл запущен** - TCC начал работу в 22:27:56.648

### ⚠️ Обнаруженные проблемы:

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. **Ошибка AlertManager.send_alert** 🔴 КРИТИЧНО

**Время:** 22:27:56.650  
**Лог:**
```
⚠️ TCC: Ошибка проверки метрик и алертов: 'AlertManager' object has no attribute 'send_alert'
```

**Проблема:**
- `AlertManager` не имеет метода `send_alert`
- Вызывается в `trading_control_center.py` при проверке метрик

**Влияние:**
- Алерты не отправляются
- Метрики проверяются, но алерты не работают

**Требуется:**
- `AlertManager` не имеет метода `send_alert`
- Доступные методы: `check_alerts()`, `_create_alert()`, `_process_alert()`
- **Решение:** Создать метод `send_alert(message: str, level: str = "info")` в `AlertManager`, который будет вызывать `_create_alert()` и `_process_alert()`
- Исправить вызов в `trading_control_center.py:877,887`

---

### 2. **Ошибка PivotCalculator.calculate** 🔴 КРИТИЧНО

**Время:** 22:27:57.686  
**Лог:**
```
Pivot: Ошибка получения свечей из DataRegistry для BTC-USDT: 'PivotCalculator' object has no attribute 'calculate', используем fallback к API
```

**Проблема:**
- `PivotCalculator` не имеет метода `calculate`
- Используется fallback к API

**Влияние:**
- Pivot Points рассчитываются через API вместо локального расчета
- Может замедлить работу фильтра Pivot Points

**Требуется:**
- `PivotCalculator` имеет метод `calculate_pivots()`, а не `calculate()`
- **Решение:** Исправить вызов в `pivot_points.py:329` с `self.calculator.calculate()` на `self.calculator.calculate_pivots()`

---

### 3. **Проблемы с открытием позиции BTC-USDT** ⚠️ ВАЖНО (РЕШЕНО С FALLBACK)

**Время:** 22:28:27 - 22:28:32  
**Логи:**
```
⚠️ EntryManager: Попытка открыть BTC-USDT на бирже, попытка 1/3, задержка 0.5 сек...
⚠️ EntryManager: Попытка открыть BTC-USDT на бирже, попытка 2/3, задержка 1.0 сек...
⚠️ EntryManager: Попытка открыть BTC-USDT на бирже, попытка 3/3, задержка 2.0 сек...
⚠️ EntryManager: Попытка открыть BTC-USDT на бирже завершена с ошибкой после retry, причина: нет price в order_result
✅ Fallback entry_price=87461.600000 для BTC-USDT (из order_result)
✅ Position opened: BTC-USDT LONG @ 87461.60, size=0.002800, margin=0.00 USD, leverage=10x, regime=ranging, signal_strength=0.81
```

**Проблема:**
- Позиция BTC-USDT не получила `price` из `order_result` после 3 попыток
- **НО:** Fallback сработал - позиция открыта с ценой 87461.60

**Влияние:**
- ✅ Позиция открыта успешно с fallback ценой
- ⚠️ Используется fallback цена вместо цены из order_result

**Статус:** ✅ Работает корректно - fallback сработал, позиция открыта

**Требуется:**
- Проверить, почему `order_result` не содержит `price` (может быть проблема с API ответом)
- Улучшить fallback для получения более точной цены (markPx из DataRegistry)

---

## ⚠️ НЕКРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 4. **Сетевые ошибки WinError 121** ⚠️ НЕ КРИТИЧНО

**Время:** 22:29:20-22:29:21  
**Логи:**
```
⚠️ Повторный запрос к бирже OKX (попытка 1/3): GET https://www.okx.com/api/v5/trade/orders-pending, ошибка: [WinError 121] Семантическое имя не может быть найдено
⚠️ Повторный запрос к бирже OKX (попытка 1/3): GET https://www.okx.com/api/v5/account/balance, ошибка: [WinError 121] Семантическое имя не может быть найдено
```

**Проблема:**
- Временные сетевые ошибки при запросах к OKX API
- Retry логика работает (попытка 1/3)

**Влияние:**
- Временные задержки в синхронизации позиций/баланса
- Retry логика должна справиться с этим

**Статус:** ✅ Работает корректно - retry логика обрабатывает ошибки

---

### 5. **ATR=0.0 для всех пар** ⚠️ НЕ КРИТИЧНО

**Проблема:**
- Все пары показывают ATR=0.0 (fallback вернул 0.0)
- Fallback=5.0 используется через ATRProvider

**Статус:** ✅ Работает корректно - fallback используется, ATR сохраняется в DataRegistry

---

## 📋 ДЕТАЛЬНЫЙ АНАЛИЗ

### Процесс инициализации:

**22:27:36** - Запуск бота  
**22:27:36-22:27:44** - Инициализация всех модулей (ConfigManager, SignalGenerator, PositionManager и т.д.)  
**22:27:44-22:27:55** - Загрузка свечей для всех символов (200 свечей для каждого)  
**22:27:56.645** - ✅ **"🟢 Все модули инициализированы — торговля разрешена"**  
**22:27:56.646** - ✅ **"[READY] All modules initialized - trading enabled"**  
**22:27:56.648** - Торговый цикл начался

**Вывод:** ✅ Инициализация прошла успешно, готовность подтверждена

---

### Проблемы при работе:

#### AlertManager.send_alert:
- **Файл:** `src/strategies/scalping/futures/core/trading_control_center.py`
- **Строка:** ~905
- **Проблема:** Вызывается несуществующий метод `send_alert`
- **Требуется:** Проверить, какой метод должен использоваться

#### PivotCalculator.calculate:
- **Файл:** `src/strategies/modules/pivot_points.py`
- **Строка:** ~342
- **Проблема:** Вызывается несуществующий метод `calculate`
- **Требуется:** Проверить, какой метод должен использоваться

#### Открытие позиции BTC-USDT:
- **Файл:** `src/strategies/scalping/futures/positions/entry_manager.py`
- **Строка:** ~390, ~464
- **Проблема:** `order_result` не содержит `price` после 3 попыток
- **Требуется:** Добавить fallback для получения цены

---

## ✅ ПОЛОЖИТЕЛЬНЫЕ МОМЕНТЫ

1. **Инициализация прошла успешно** - все модули готовы
2. **Свечи загружены** - 200 свечей для каждого символа
3. **WebSocket работает** - подключение и подписки успешны
4. **Готовность подтверждена** - сообщение "🟢 Все модули инициализированы" присутствует
5. **Торговый цикл запущен** - TCC начал работу
6. **Retry логика работает** - сетевые ошибки обрабатываются

---

## 🔧 РЕКОМЕНДАЦИИ ПО ИСПРАВЛЕНИЮ

### Срочно (критично):
1. **Исправить AlertManager.send_alert**
   - **Файл:** `src/strategies/scalping/futures/metrics/alert_manager.py`
   - **Проблема:** Метод `send_alert()` не существует
   - **Решение:** Добавить метод `send_alert(message: str, level: str = "info")`:
     ```python
     def send_alert(self, message: str, level: str = "info") -> None:
         """Отправить алерт напрямую"""
         alert = self._create_alert(
             "manual_alert",
             message,
             {"level": level}
         )
         self._process_alert(alert)
     ```
   - **Исправить вызовы:** `trading_control_center.py:877,887`

2. **Исправить PivotCalculator.calculate**
   - **Файл:** `src/strategies/modules/pivot_points.py`
   - **Строка:** 329
   - **Проблема:** Вызывается `self.calculator.calculate()`, но метод называется `calculate_pivots()`
   - **Решение:** Заменить `self.calculator.calculate()` на `self.calculator.calculate_pivots()`

3. **Исправить открытие позиции**
   - **Файл:** `src/strategies/scalping/futures/positions/entry_manager.py`
   - **Проблема:** `order_result` не содержит `price` после 3 попыток
   - **Решение:** Улучшить fallback для получения цены из DataRegistry или markPx (уже реализовано частично)

### Важно:
4. **Мониторинг сетевых ошибок**
   - Убедиться, что retry логика работает корректно
   - Добавить логирование для диагностики

---

## ✅ ИТОГОВАЯ ОЦЕНКА

| Компонент | Статус | Комментарий |
|-----------|--------|-------------|
| Инициализация | ✅ | Все модули инициализированы успешно |
| Готовность | ✅ | Сообщение "🟢 Все модули инициализированы" присутствует |
| Свечи | ✅ | 200 свечей загружено для каждого символа |
| WebSocket | ✅ | Подключение и подписки успешны |
| AlertManager | 🔴 | Ошибка: нет метода send_alert |
| PivotCalculator | 🔴 | Ошибка: нет метода calculate |
| Открытие позиций | ⚠️ | Проблемы с получением price из order_result |
| Сетевые ошибки | ✅ | Retry логика работает корректно |

---

**Дата создания:** 29.12.2025  
**Статус:** ⚠️ Требуется исправление критических ошибок

---

## 📋 ПЛАН ИСПРАВЛЕНИЙ

### 1. AlertManager.send_alert (КРИТИЧНО)

**Файл:** `src/strategies/scalping/futures/metrics/alert_manager.py`

**Добавить метод после строки 73:**
```python
def send_alert(self, message: str, level: str = "info") -> None:
    """
    Отправить алерт напрямую (для использования из других модулей).
    
    Args:
        message: Текст сообщения алерта
        level: Уровень серьезности ("info", "warning", "critical")
    """
    alert = self._create_alert(
        "manual_alert",
        message,
        {"level": level}
    )
    self._process_alert(alert)
```

**Исправить вызовы в `trading_control_center.py`:**
- Строка 877: Добавить проверку `if self.alert_manager:` перед вызовом
- Строка 887: Добавить проверку `if self.alert_manager:` перед вызовом

---

### 2. PivotCalculator.calculate (КРИТИЧНО)

**Файл:** `src/strategies/modules/pivot_points.py`

**Исправить строку 329:**
```python
# БЫЛО:
levels = self.calculator.calculate(
    daily_candles, self.config.use_last_n_days
)

# ДОЛЖНО БЫТЬ:
levels = self.calculator.calculate_pivots(
    daily_candles, self.config.use_last_n_days
)
```

---

### 3. Открытие позиции (ВАЖНО, но не критично)

**Статус:** ✅ Fallback уже работает, позиция открывается успешно

**Рекомендация:** Улучшить fallback для получения более точной цены из DataRegistry.markPx (уже частично реализовано в предыдущих патчах)

---

**Дата обновления:** 29.12.2025  
**Приоритет:** 🔴 Критично исправить пункты 1 и 2 перед следующим запуском

