# 🔍 ФИНАЛЬНЫЙ АУДИТ: АНАЛИЗ + РЕШЕНИЕ

**Дата:** 22.11.2025  
**Статус:** ПОЛНЫЙ АНАЛИЗ КОДА + НАЙДЕНЫ РЕШЕНИЯ

---

## ✅ ХОРОШИЕ НОВОСТИ

1. **ConfigManager читает параметры ПРАВИЛЬНО:**
   - `get_trailing_sl_params(regime)` корректно читает параметры по режиму
   - Параметры `min_holding_minutes`, `timeout_minutes` передаются в TSL правильно
   - **Код в config_manager.py (строки 356-377):**
     ```python
     if "timeout_minutes" in regime_params_dict:
         params["timeout_minutes"] = regime_params_dict["timeout_minutes"]
     if "min_holding_minutes" in regime_params_dict:
         params["min_holding_minutes"] = regime_params_dict["min_holding_minutes"]
     ```

2. **Orchestrator правильно передает параметры в TSL:**
   - Файл: `orchestrator.py` (строки 1164-1255)
   - Получает режим из signal
   - Передает в TSL объект ВСЕ параметры из конфига

---

## ❌ НАЙДЕННЫЕ ПРОБЛЕМЫ

### ПРОБЛЕМА #1: min_holding_minutes НЕ ЗАЩИЩАЕТ ОТ ЗАКРЫТИЯ ПО УБЫТКАМ!

**Файл:** `trailing_stop_loss.py` (строки 454-557)

**Сейчас работает так:**
```python
# Линия 455-457
effective_min_holding = self.min_holding_minutes  # 40 мин

# Линия 457-463 
if self.extend_time_on_profit and profit_pct > 0:
    # Если ПРИБЫЛЬ > 0 → увеличиваем 40 мин * 2.0 = 80 мин
    effective_min_holding = effective_min_holding * self.extend_time_multiplier

# Линия 466-557
if effective_min_holding is not None and minutes_in_position < effective_min_holding:
    # Если позиция в убытке (profit_pct < 0) → effective_min_holding остается 40 мин
    # НО! Дальше идут проверки loss_cut и timeout...
    # И if profit_pct < 0 → return False (выходим)
    return False  
    # ⚠️ ПРОБЛЕМА: Эта защита срабатывает ТОЛЬКО если profit_pct < min_profit_to_close

# Линия 559-576 (Loss-cut проверка)
if profit_pct <= -loss_cut_from_price:  # 0.3% (1.5% / 5x)
    # ⚠️ ПРОБЛЕМА: Даже если прошло только 1 минута,
    # если убыток > 0.3%, закрываем БЕЗ ПРОВЕРКИ min_holding_minutes!
    return True
```

**ВЫВОД:** `min_holding_minutes` защищает ТОЛЬКО от закрытия через TSL, но НЕ от закрытия через loss-cut!

---

### ПРОБЛЕМА #2: ЛОГИКА ИМЕЕТ НЕПРАВИЛЬНЫЙ ПОРЯДОК ПРОВЕРОК

**Правильный порядок должен быть:**
1. **ПЕРВЫЙ:** Проверка `min_holding_minutes` как АБСОЛЮТНЫЙ минимум
2. **ВТОРОЙ:** Только потом проверки loss-cut/timeout/TSL

**Сейчас порядок:**
1. Защита `min_holding_minutes` (строки 454-557) - НО с условиями
2. Loss-cut (строки 559-576) - **БЕЗ проверки min_holding!**
3. Timeout (строки 578-601) - **БЕЗ проверки min_holding!**
4. TSL (строки 603-710) - с проверкой min_holding

---

### ПРОБЛЕМА #3: loss_cut СЛИШКОМ МЯГКИЙ

**Конфиг:** `loss_cut_percent: 1.5` (1.5% от маржи)

**В коде:** `loss_cut_from_price = 1.5 / 5 = 0.3%` от цены!

**Сделки из ваших данных с убытками:**
- BTC: -0.59 USDT (-2.06%) за 4 мин 47 сек ← это 2.06% > 0.3% → закрыто по loss-cut!
- SOL: -0.91 USDT (-2.33%) за 4 сек ← это 2.33% > 0.3% → НЕМЕДЛЕННОЕ закрытие!
- DOGE: -0.27 USDT (-0.86%) за 8 сек ← это 0.86% > 0.3% → НЕМЕДЛЕННОЕ закрытие!

**Но почему-то закрывается быстро???**

---

### ПРОБЛЕМА #4: ЗАЩИТА ОТ ПРОСКАЛЬЗЫВАНИЯ МОЖЕТ БЫТЬ ДОПОЛНИТЕЛЬНЫМ ФАКТОРОМ

**Файл:** `trailing_stop_loss.py` (строки 475-545)

```python
# Линия 477-484
if self.min_critical_hold_seconds is not None:
    min_critical_hold_seconds = self.min_critical_hold_seconds  # Из конфига
elif effective_min_holding is not None:
    min_critical_hold_seconds = min(
        60.0, (effective_min_holding * 60.0) / 2.0
    )  # Половина min_holding_minutes = 20 мин!
else:
    min_critical_hold_seconds = 30.0  # Fallback

# Линия 496-520
if seconds_in_position >= min_critical_hold_seconds:  # Если прошло 20 мин...
    # Проверяем обычный loss-cut
    if profit_pct <= -loss_cut_from_price:  # 0.3%
        return True
else:  # Если прошло МЕНЕЕ 20 мин
    # Проверяем ТОЛЬКО 2x критический loss-cut
    if profit_pct <= -critical_loss_cut_from_price:  # 0.6% (1.5% * 2 / 5)
        return True
```

**ВЫВОД:** В течение первых 20 минут закрываем только если убыток > 0.6%. Но в ваших сделках есть убытки > 2%!

---

## 🎯 НАСТОЯЩАЯ ПРИЧИНА ПРОБЛЕМЫ

### Гипотеза: ЛОГИКА ОШИБОЧНА - min_holding НЕ БЛОКИРУЕТ loss-cut!

**Что происходит в реальности:**

1. Позиция открыта в 09:35:04 (RANGING режим)
2. Через 1 минуту цена начинает падать, убыток становится -0.5%
3. Проверка `min_holding_minutes`:
   ```python
   # profit_pct = -0.5% (УБЫТОК)
   if effective_min_holding is not None and minutes_in_position < effective_min_holding:
       # 1 минута < 40 минут → условие ИСТИННО
       logger.debug(...)
       return False  # ✅ Выходим! Не закрываем!
   ```
4. **НО** потом идет проверка loss_cut:
   ```python
   if seconds_in_position >= min_critical_hold_seconds:  # 20 мин
       # 60 сек < 1200 сек → условие ЛОЖНО
       # Пропускаем эту проверку
   else:
       # Проверяем КРИТИЧЕСКИЙ loss-cut (2x)
       if profit_pct <= -0.6%:  # -0.5% > -0.6% → не закрываем
           return True
   ```

**ВЫВОД:** При -0.5% убытке:
- `min_holding` говорит: не закрывать
- `loss_cut` (критический) говорит: не закрывать
- **Позиция должна остаться открытой!**

---

## 🚨 РЕАЛЬНАЯ ПРОБЛЕМА: МОЖЕТ БЫТЬ ПОЗИЦИЯ ЗАКРЫВАЕТСЯ ЧЕРЕЗ TP!

**Гипотеза:** Параметры TP прочитаны НЕПРАВИЛЬНО!

**Проверим конфиг:**

```yaml
symbol_profiles:
  BTC-USDT:
    ranging:
      tp_percent: 5.0  # 5% TP
```

**Но какой TP действительно используется?**

1. В `position_manager.py` может быть проверка TP?
2. В `orchestrator.py` может быть параллельная логика?
3. Может быть, TP читается из неправильного места?

---

## 📋 ЧЕКЛИСТ ДИАГНОСТИКИ (ПРИОРИТЕТ)

### ПЕРВЫЙ ПОРЯДОК ДЕЙСТВИЙ:

1. **КРИТИЧНО:** Добавить логирование в TSL при создании:
   ```python
   logger.warning(
       f"🔧 TSL создан для {symbol} с параметрами:\n"
       f"   min_holding_minutes={self.min_holding_minutes}\n"
       f"   timeout_minutes={self.timeout_minutes}\n"
       f"   loss_cut_percent={self.loss_cut_percent}\n"
       f"   regime={regime}"
   )
   ```

2. **КРИТИЧНО:** Добавить логирование в `should_close_position()`:
   ```python
   logger.warning(
       f"🔍 should_close_position check:\n"
       f"   symbol={symbol}\n"
       f"   minutes={minutes_in_position:.2f}\n"
       f"   profit={profit_pct:.2%}\n"
       f"   min_holding={self.min_holding_minutes}\n"
       f"   will_close={result}"
   )
   ```

3. **ВЫСОКИЙ ПРИОРИТЕТ:** Проверить, есть ли парал лельная логика закрытия в `position_manager.py`:
   - Может быть, там есть собственная проверка TP?
   - Может быть, there's another place closing positions?

4. **ВЫСОКИЙ ПРИОРИТЕТ:** Проверить, какие параметры TP используются:
   - Глобальные или по режиму?
   - Есть ли fallback?

---

## 💡 ПРЕДЛАГАЕМОЕ РЕШЕНИЕ

### ШАГИ СЕЙЧАС (ОБСУЖДЕНИЕ):

1. **Запустить с DEBUG логами** чтобы увидеть:
   - Какие параметры TSL создаются
   - Какие причины закрытия в реальности

2. **Проверить `position_manager.py`** на параллельную логику закрытия

3. **Проверить TP параметры** - правильно ли читаются

4. **Если найдены проблемы:** Исправить в коде

---

## 📊 РЕЗУЛЬТАТЫ АНАЛИЗА

| Компонент | Статус | Проблема | Приоритет |
|-----------|--------|---------|-----------|
| ConfigManager | ✅ OK | Читает параметры правильно | - |
| Orchestrator TSL | ✅ OK | Передает параметры правильно | - |
| TrailingSL логика | ⚠️ СЛОЖНАЯ | min_holding не блокирует loss-cut | ВЫСОКИЙ |
| Порядок проверок | ⚠️ НЕПРАВИЛЬНЫЙ | min_holding должен быть ПЕРВЫМ | ВЫСОКИЙ |
| Loss-cut | ⚠️ МЯГКИЙ | 0.3% может быть слишком мало | СРЕДНИЙ |
| TP параметры | ❓ НЕИЗВЕСТНО | Может быть читаются неправильно | КРИТИЧНЫЙ |
| Position Manager | ❓ НЕИЗВЕСТНО | Может быть парал логика | КРИТИЧНЫЙ |

---

## 🎯 СЛЕДУЮЩИЙ ШАГ

**Нужно запустить с логиров и ием, чтобы:**
1. Увидеть какие параметры TSL создаются на самом деле
2. Увидеть когда и почему закрываются позиции
3. Найти истинную причину проблемы

**После этого:** Фиксим код!

