# ПОЛНАЯ ПРОВЕРКА КОНСИСТЕНТНОСТИ ИНДИКАТОРОВ

**Дата:** 2026-01-06

**Всего проверено файлов:** 83
**Всего найдено проблем:** 8

## MACD

[OK] Проблем не найдено.

## ADX

**Найдено проблем:** 7

### direction_analyzer.py:126

**Проблема:** ADX читается напрямую без проверки наличия

**Серьезность:** LOW

```python
            # ADX (40%)
            if adx_direction == "bullish":
                bullish_score += adx_confidence * self.INDICATOR_WEIGHTS["adx"]
            elif adx_direction == "bearish":
                bearish_score += adx_confidence * self.INDICATOR_WEIGHTS["adx"]

```

### direction_analyzer.py:128

**Проблема:** ADX читается напрямую без проверки наличия

**Серьезность:** LOW

```python
                bullish_score += adx_confidence * self.INDICATOR_WEIGHTS["adx"]
            elif adx_direction == "bearish":
                bearish_score += adx_confidence * self.INDICATOR_WEIGHTS["adx"]

            # EMA (25%)
            if ema_direction == "bullish":
```

### trailing_sl_coordinator.py:837

**Проблема:** ADX читается напрямую без проверки наличия

**Серьезность:** LOW

```python
                    adx_value = self.fast_adx.get_current_adx()
                    if adx_value and adx_value > 0:
                        trend_analysis["adx"] = min(adx_value / 100.0, 1.0)
                        trend_strength = trend_analysis["adx"]

                # 2. Order Flow анализ
```

### trailing_sl_coordinator.py:838

**Проблема:** ADX читается напрямую без проверки наличия

**Серьезность:** LOW

```python
                    if adx_value and adx_value > 0:
                        trend_analysis["adx"] = min(adx_value / 100.0, 1.0)
                        trend_strength = trend_analysis["adx"]

                # 2. Order Flow анализ
                if self.order_flow:
```

### trailing_sl_coordinator.py:873

**Проблема:** ADX читается напрямую без проверки наличия

**Серьезность:** LOW

```python
                # 3. Комбинированный анализ силы тренда
                if (
                    trend_analysis["adx"] is not None
                    or trend_analysis["order_flow"] is not None
                ):
                    # Взвешенная комбинация: ADX 60%, Order Flow 40%
```

### trailing_sl_coordinator.py:881

**Проблема:** ADX читается напрямую без проверки наличия

**Серьезность:** LOW

```python

                    adx_val = (
                        trend_analysis["adx"]
                        if trend_analysis["adx"] is not None
                        else 0.5
                    )
```

### trailing_sl_coordinator.py:882

**Проблема:** ADX читается напрямую без проверки наличия

**Серьезность:** LOW

```python
                    adx_val = (
                        trend_analysis["adx"]
                        if trend_analysis["adx"] is not None
                        else 0.5
                    )
                    of_val = (
```

## ATR

[OK] Проблем не найдено.

## RSI

**Найдено проблем:** 1

### signal_generator.py:2150

**Проблема:** RSI читается как dict, но должен быть scalar

**Серьезность:** HIGH

```python

            # ✅ ИСПРАВЛЕНИЕ #31 (04.01.2026): Детальное логирование значений индикаторов при генерации сигналов
            rsi_val = indicators.get("rsi", "N/A")
            macd_val = indicators.get("macd", {})
            if isinstance(macd_val, dict):
                macd_line = macd_val.get("m
```

## BOLLINGER BANDS

[OK] Проблем не найдено.

## MOVING AVERAGES

[OK] Проблем не найдено.

