# ✅ ИСПРАВЛЕНИЯ: TSL И POST_ONLY

**Дата:** 02 января 2026  
**Статус:** ✅ ИСПРАВЛЕНО  
**Приоритет:** ВЫСОКИЙ

---

## 📋 ВЫПОЛНЕННЫЕ ИСПРАВЛЕНИЯ

### ✅ Исправление #1: Проверка существования TSL перед инициализацией

#### Места исправления:

1. **SignalCoordinator.execute_signal_from_price()** (строка 3397-3433)
   - ✅ Добавлена проверка `existing_tsl = self.trailing_sl_coordinator.get_tsl(symbol)`
   - ✅ Если TSL существует, пропускаем повторную инициализацию
   - ✅ Логирование причины пропуска

2. **Orchestrator._sync_positions_with_exchange()** (строка 2291)
   - ✅ Уже была проверка `if not self.trailing_sl_coordinator.get_tsl(symbol)`
   - ✅ Улучшена передача signal с strength

3. **TrailingSLCoordinator.initialize_trailing_stop()** (строка 363-380)
   - ✅ Добавлена проверка параметров перед переинициализацией
   - ✅ Если параметры не изменились и entry_price совпадает, возвращаем существующий TSL
   - ✅ Детальное логирование переинициализации

**Результат:** TSL не будет инициализироваться дважды, если параметры не изменились.

---

### ✅ Исправление #2: Передача signal с strength в первую инициализацию TSL

#### Места исправления:

1. **SignalCoordinator.execute_signal_from_price()** (строка 3409-3419)
   - ✅ Создается `signal_with_strength` с копией signal
   - ✅ Проверяется наличие `strength` в signal
   - ✅ Передается в `initialize_trailing_stop_callback` с strength

2. **Orchestrator._sync_positions_with_exchange()** (строка 2309-2325)
   - ✅ Получается `signal_strength` из активной позиции или метаданных
   - ✅ Добавляется в `signal_with_regime` для передачи в TSL
   - ✅ Передается в `initialize_trailing_stop` с strength

**Результат:** Агрессивный режим TSL (trail=0.40%, loss_cut=1.00%) применяется сразу при первой инициализации для сильных сигналов (strength > 0.8).

---

### ✅ Исправление #3: Проверка свежести цены перед размещением POST_ONLY ордера

#### Место исправления:

**OrderExecutor._place_limit_order()** (строка 1228-1295)

**Логика проверки:**
1. ✅ Получаем `price_limits` с timestamp и current_price
2. ✅ Проверяем возраст цены: если старше 1 секунды → отключаем POST_ONLY
3. ✅ Проверяем расхождение между лимитной ценой и текущей: если > 0.5% → отключаем POST_ONLY

**Код:**
```python
if post_only:
    price_limits = await self.client.get_price_limits(symbol)
    if price_limits:
        price_timestamp = price_limits.get("timestamp", 0)
        current_price = price_limits.get("current_price", 0)
        
        if price_timestamp > 0:
            price_age = time.time() - price_timestamp
            if price_age > 1.0:  # Цена старше 1 секунды
                logger.warning(f"⚠️ Цена устарела, отключаем POST_ONLY")
                post_only = False
            
            if current_price > 0 and price > 0:
                price_diff_pct = abs(price - current_price) / current_price * 100.0
                if price_diff_pct > 0.5:  # Расхождение > 0.5%
                    logger.warning(f"⚠️ Лимитная цена отличается на {price_diff_pct:.2f}%, отключаем POST_ONLY")
                    post_only = False
```

**Результат:** POST_ONLY ордера не размещаются, если цена устарела или сильно отличается от текущей.

---

### ✅ Исправление #4: Отключение POST_ONLY при высокой волатильности (>0.5%)

#### Место исправления:

**OrderExecutor._place_limit_order()** (строка 1258-1285)

**Логика проверки:**
1. ✅ Получаем ATR из DataRegistry
2. ✅ Рассчитываем волатильность как ATR в процентах от цены
3. ✅ Если волатильность > 0.5% → отключаем POST_ONLY
4. ✅ Альтернативный способ: получение волатильности из regime_manager

**Код:**
```python
# Проверка волатильности для отключения POST_ONLY
volatility = None
if self.data_registry:
    try:
        atr = await self.data_registry.get_indicator(symbol, "atr")
        if atr and current_price > 0:
            volatility = (atr / current_price) * 100.0
    except Exception as e:
        logger.debug(f"⚠️ Не удалось получить ATR: {e}")

# Альтернативный способ получения волатильности из regime_manager
if volatility is None and self.signal_generator:
    try:
        regime_manager = (
            self.signal_generator.regime_managers.get(symbol)
            or self.signal_generator.regime_manager
        )
        if regime_manager and hasattr(regime_manager, "last_volatility"):
            volatility = regime_manager.last_volatility
    except Exception as e:
        logger.debug(f"⚠️ Не удалось получить волатильность: {e}")

# Отключаем POST_ONLY при высокой волатильности (>0.5%)
if volatility is not None and volatility > 0.5:
    logger.warning(f"⚠️ Высокая волатильность ({volatility:.2f}% > 0.5%), отключаем POST_ONLY")
    post_only = False
```

**Результат:** POST_ONLY автоматически отключается при высокой волатильности на всех парах и режимах.

---

## 🔧 ДОПОЛНИТЕЛЬНЫЕ ИЗМЕНЕНИЯ

### ✅ Добавлены методы в OrderExecutor

1. **set_data_registry()** - для установки DataRegistry
2. **set_signal_generator()** - для установки SignalGenerator

**Место:** `src/strategies/scalping/futures/order_executor.py` (строка 88-96)

### ✅ Установка зависимостей в Orchestrator

**Место:** `src/strategies/scalping/futures/orchestrator.py` (строка 381-386)

```python
# Устанавливаем DataRegistry в OrderExecutor
if hasattr(self.order_executor, "set_data_registry"):
    self.order_executor.set_data_registry(self.data_registry)
# Устанавливаем SignalGenerator в OrderExecutor
if hasattr(self.order_executor, "set_signal_generator"):
    self.order_executor.set_signal_generator(self.signal_generator)
```

---

## 📊 ПРИМЕНЕНИЕ НА ВСЕХ ПАРАХ И РЕЖИМАХ

### ✅ Все пары (BTC, ETH, SOL, DOGE, XRP)

**Проверка TSL:**
- ✅ Работает для всех символов через `trailing_sl_by_symbol.get(symbol)`
- ✅ Проверка выполняется перед каждой инициализацией

**Проверка POST_ONLY:**
- ✅ Работает для всех символов через `get_price_limits(symbol)`
- ✅ Волатильность получается из DataRegistry для каждого символа
- ✅ Проверка выполняется перед каждым размещением POST_ONLY ордера

### ✅ Все режимы (trending, ranging, choppy)

**Проверка TSL:**
- ✅ Работает для всех режимов через `regime` в signal
- ✅ Параметры TSL адаптируются под режим через `parameter_provider.get_trailing_sl_params(symbol, regime)`

**Проверка POST_ONLY:**
- ✅ Работает для всех режимов через `regime` в `_place_limit_order()`
- ✅ Волатильность проверяется независимо от режима
- ✅ POST_ONLY может быть отключен в любом режиме при высокой волатильности

---

## 🎯 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### После исправлений:

1. ✅ **TSL инициализируется только один раз** при открытии позиции
2. ✅ **Агрессивный режим TSL применяется сразу** для сильных сигналов (strength > 0.8)
3. ✅ **POST_ONLY ордера не размещаются** при устаревшей цене или высокой волатильности
4. ✅ **Позиции открываются по актуальной цене** без расхождений

### Логирование:

- ✅ Детальное логирование всех проверок TSL
- ✅ Логирование причин отключения POST_ONLY
- ✅ Логирование волатильности для каждой пары

---

## 📋 ТЕСТИРОВАНИЕ

### Рекомендуется протестировать:

1. ✅ Открытие позиции на всех парах (BTC, ETH, SOL, DOGE, XRP)
2. ✅ Проверка, что TSL инициализируется только один раз
3. ✅ Проверка агрессивного режима TSL для сильных сигналов (strength > 0.8)
4. ✅ Проверка отключения POST_ONLY при высокой волатильности (>0.5%)
5. ✅ Проверка отключения POST_ONLY при устаревшей цене (>1 сек)
6. ✅ Проверка во всех режимах (trending, ranging, choppy)

---

**Дата исправления:** 02 января 2026  
**Файлы изменены:**
- `src/strategies/scalping/futures/coordinators/signal_coordinator.py`
- `src/strategies/scalping/futures/orchestrator.py`
- `src/strategies/scalping/futures/order_executor.py`
- `src/strategies/scalping/futures/coordinators/trailing_sl_coordinator.py`

**Статус:** ✅ ВСЕ ИСПРАВЛЕНИЯ ВНЕСЕНЫ

