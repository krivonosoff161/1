# 🔧 ПЛАН ЭКСТРЕННОГО ВОССТАНОВЛЕНИЯ

**Дата:** 2026-01-07  
**Приоритет:** КРИТИЧЕСКИЙ  
**Статус:** Утверждено после анализа убытков -6.39 USD

---

## 📌 РЕЗЮМЕ ПРОБЛЕМЫ

| Проблема | Данные | Необходимо |
|----------|--------|-----------|
| **Убыток** | -6.39 USD за 13 ч | Положительный P&L |
| **Win Rate** | 18.5% (5/27) | 40-50%+ |
| **SL Hit Rate** | 81.5% (22/27) | <30% |
| **BTC** | 0% win, 17 убытков | Полная переработка |
| **ETH** | 66% win, +1.51 USD | Дублировать модель |

---

## 🔴 СПИСОК КРИТИЧЕСКИХ ОШИБОК

### Ошибка #1: SL Рассчитывается Неправильно

**Доказательство:**
```python
# ❌ ОБНАРУЖЕННЫЕ ПРИМЕРЫ

# DOGE - Entry = Stop (НЕВОЗМОЖНО!)
Entry: 0.1526 USD
Stop:  0.1526 USD
Потеря: -0.1657 USD (как это возможно??)

# BTC - Слишком узкие стопы при волатильности ±200 пунктов
Entry: 94117.30 | SL: 94211.60 (+94p при волатильности ±200p) 
Entry: 92302.20 | SL: 92470.60 (+168p - СЛИШКОМ УЗКО)

# XRP - Микроскопические стопы (7-23 пункта)
Entry: 2.3694 | SL: 2.3677 (-0.17p) → -0.3662 USD убыток
Entry: 2.3811 | SL: 2.3788 (-0.23p) → -0.4212 USD убыток
```

**Локация в коде:** `src/strategies/scalping/{spot,futures}/order_executor.py`

**Что проверить:**
```python
# Поиск строк где вычисляется SL:
sl_price = entry_price - (atr * sl_multiplier)

# Проблемы могут быть:
1. ATR не обновляется между сделками
2. ATR вычисляется на старых данных
3. ATR = 0 или очень маленький (откуда берётся дефолт?)
4. sl_multiplier некорректный (может быть очень маленький)
5. Для DOGE: special handling даёт одинаковые entry=stop
```

**Что нужно исправить:**

```python
# ДО (неправильно):
stop_loss = entry_price - atr * multiplier
# Если ATR=0, то stop_loss = entry_price → ОШИБКА!

# ПОСЛЕ (правильно):
if atr < 0.001:  # слишком маленькое значение
    atr = get_default_atr_for_pair(symbol)
stop_loss = entry_price - atr * multiplier
# + логирование
log.info(f"ATR={atr}, Entry={entry}, SL={stop_loss}, Distance={entry-stop_loss}")
```

---

### Ошибка #2: Entry Timing Неоптимален

**Доказательство:**
```
После SL часто цена идёт в ПРАВИЛЬНУЮ сторону:

❌ 13:10:21 | BTC short @ 94117.30
   SL hit @ 94211.60 → потеря -0.49 USD
   Но цена упала ДО 93961.20! 
   Должна была быть прибыль +150 пунктов!
   
   Причина: Entry был в консолидации, не в тренде
   Когда цена выходит из консолидации, SL срабатывает
   Потом цена идёт в нужную сторону (слишком поздно)
```

**Локация в коде:** `src/strategies/scalping/{spot,futures}/signal_generator.py`

**Что проверить:**
```python
# Сигналы генерируются, но они могут быть:

1. Слишком рано (перед началом тренда)
2. В конце хода (когда все уже потеряли)
3. Без фильтра по режиму рынка (рыночная консолидация)
4. Без фильтра по волатильности (во время низкой волатильности)
```

**Что нужно исправить:**

```python
# Добавить PRE-SIGNAL проверки:

if signal_direction == SHORT:
    # Проверить что тренд вниз существует
    if current_price < sma_50:  # Выше кривой = вниз тренд нежелателен
        skip_signal("Price above SMA50, no downtrend")
        return None
    
    # Проверить что не началась консолидация
    if atr < atr_average * 0.5:
        skip_signal("Low volatility, consolidation detected")
        return None

# Логировать все пропущенные сигналы:
log.debug(f"Signal rejected: {reason}")
```

---

### Ошибка #3: Фильтры Блокируют 91% Сигналов

**Доказательство:**
```
300+ сигналов сгенерировано
27 сделок открыто
= 9% acceptance rate (91% отклонено)

Вопрос: Это правильно или фильтры слишком жёсткие?

Анализ:
- Если 91% отклонено и мы теряем деньги → фильтры неправильные
- Может быть они ОТКЛОНЯЮТ хорошие сигналы
- А пропускают ПЛОХИЕ сигналы
```

**Локация в коде:** 
- `src/strategies/modules/adaptive_regime_manager.py`
- `src/strategies/modules/multi_timeframe.py`
- `src/strategies/modules/correlation_filter.py`
- `src/risk/risk_controller.py`

**Что проверить:**

```python
# Каждый фильтр должен логировать когда отклоняет:

class MultiTimeframeFilter:
    def is_signal_valid(self, signal):
        if not self.check_higher_timeframe():
            # ❌ ЛОГИРОВАТЬ!
            logger.warning(f"MTF rejected {signal.symbol} {signal.direction}")
            return False
        return True

# Затем анализировать логи:
# Какой фильтр отклоняет самые полезные сигналы?
```

**Что нужно исправить:**

```python
# ВРЕМЕННОЕ: Отключить фильтры для тестирования
# В конфиге config_futures.yaml:

filters:
  enabled: false  # ← ОТКЛЮЧИТЬ ПОЛНОСТЬЮ
  
# Затем запустить и видеть:
# - Станет ли дороже при 300 сделок вместо 27?
# - Или станет еще хуже?

# Если станет хуже → фильтры нужны, но они неправильные
# Если станет лучше → фильтры блокируют хорошие сигналы
```

---

### Ошибка #4: Per-Symbol Оптимизация Не Сработала

**Доказательство:**
```
DOGE-USDT "оптимизированные" параметры:
- ATR(5), RSI(7), EMA(6/13), MACD(6/13), BB(10/1.5)
→ Результат: 0% прибыль (2 убытка)

XRP-USDT "оптимизированные" параметры:
- ATR(7), RSI(10), EMA(8/17), MACD(8/17), BB(15/1.8)
→ Результат: 0% прибыль (3 убытка)

ETH-USDT "оптимизированные" параметры:
- MACD(10/22) per-symbol
→ Результат: 66% прибыль (+1.51 USD) ✅
```

**Анализ:**
```
Может быть MACD(10/22) работает потому что:
1. Верные параметры для пары (совпадение)
2. ИЛИ всё остальное (SL, Entry) работает нормально для ETH
3. Но для DOGE/XRP остальное (SL, Entry) НЕ работает

Гипотеза: Проблема не в параметрах индикаторов
         а в алгоритме SL и Entry timing
```

**Локация в коде:** `src/strategies/modules/adaptive_regime_manager.py`

**Что нужно исправить:**

```python
# ВАРИАНТ 1: Удалить параметры для DOGE/XRP
# Использовать глобальные параметры вместо per-symbol

# ВАРИАНТ 2: Использовать только MACD(10/22) для всех
# И убрать все остальные параметры per-symbol

# ВАРИАНТ 3: Глубокий анализ
# Почему MACD(10/22) работает? 
# - Может быть это просто совпадение?
# - Может быть нужно тестировать другие MACD параметры?
```

---

### Ошибка #5: BTC Полностью Неработающая

**Доказательство:**
```
BTC-USDT: 0% win rate

Статистика:
- 17 сделок открыто (63% от всех)
- 0 прибыльных
- 17 убыточных (100% SL hit)
- Среднее убыток: -0.227 USD per trade
- Максимум убыток: -0.678 USD

Это не случайность. Это СИСТЕМА НЕ РАБОТАЕТ.
```

**Почему произошло:**

1. **BTC очень волатилен** (±200 пункта в минуту)
   - SL вычисляется неправильно (слишком узко)
   - Волатильность сбивает SL перед входом в направлении

2. **BTC нет per-symbol параметров** (используются глобальные)
   - Может быть глобальные параметры для микро-пар (DOGE)
   - Совсем не подходят для макро-пар (BTC)

3. **BTC не должен торговаться на скальпинг** 
   - Минимум движение заряжается комиссиями
   - Нужны позиции на 30 минут, а не 1-5 минут

**Что нужно исправить:**

```python
# ВАРИАНТ 1: Отключить BTC полностью
trading:
  symbols:
    - BTC-USDT: false  # ← ОТКЛЮЧИТЬ
    - ETH-USDT: true
    - SOL-USDT: true
    - XRP-USDT: true
    - DOGE-USDT: false  # ← ОТКЛЮЧИТЬ (тоже не работает)

# ВАРИАНТ 2: Дать BTC per-symbol параметры
# На основе анализа что работает для ETH (MACD 10/22)

# РЕКОМЕНДАЦИЯ: ВАРИАНТ 1 (отключить)
# Пока не разберёмся почему он не работает
```

---

## ✅ ПЛАН ИСПРАВЛЕНИЯ (По Приоритету)

### ШАГ 1: Отладка SL (СЕГОДНЯ - 30 минут)

**Файл:** `src/strategies/scalping/futures/order_executor.py`

```python
# Найти функцию calculate_stop_loss() или где вычисляется SL

# ДОБАВИТЬ ЛОГИРОВАНИЕ:
def place_order(self, signal, size):
    entry_price = signal.price
    
    # Получить ATR
    atr = self.indicators.get_atr(signal.symbol)
    logger.info(f"ATR for {signal.symbol}: {atr}")  # ← ДОБАВИТЬ
    
    # Рассчитать SL
    stop_loss = entry_price - atr * 2.0  # или другой multiplier
    logger.info(f"Stop Loss calculated: entry={entry_price}, atr={atr}, sl={stop_loss}")  # ← ДОБАВИТЬ
    
    # ПРОВЕРКА НА ОШИБКУ DOGE
    if abs(entry_price - stop_loss) < 0.00001:  # практически одинаковые
        logger.error(f"CRITICAL: Entry == SL! entry={entry_price}, sl={stop_loss}")  # ← ДОБАВИТЬ
        return False  # отклонить сделку
```

**После добавления логирования:**
1. Запустить бота
2. Смотреть в логи: какие ATR значения?
3. Правильно ли вычисляется SL?
4. Есть ли ошибка как с DOGE?

---

### ШАГ 2: Отключить Неработающие Пары (СЕГОДНЯ - 5 минут)

**Файл:** `config/config_futures.yaml`

```yaml
trading:
  symbols:
    BTC-USDT:     # ← Убрать эту строку
      enabled: false
    DOGE-USDT:    # ← Убрать эту строку
      enabled: false
    ETH-USDT:
      enabled: true
    SOL-USDT:
      enabled: true
    XRP-USDT:
      enabled: true
```

**Почему:**
- BTC: 0% win rate, -3.86 USD убыток
- DOGE: 0% win rate, entry=stop ошибка, всего 2 сделки

**Эффект:**
- Меньше убытков
- Сосредоточиться на парах с потенциалом

---

### ШАГ 3: Копировать ETH Параметры на Другие Пары (ЗАВТРА - 15 минут)

**Файл:** `config/config_futures.yaml` или `src/strategies/modules/adaptive_regime_manager.py`

```python
# ТОЖ Что сработало для ETH (66% win, +1.51 USD):
# MACD(10/22) + остальные параметры

# Скопировать на SOL и XRP:
indicators:
  SOL-USDT:
    macd:
      fast: 10  # ← Как у ETH
      slow: 22  # ← Как у ETH
  XRP-USDT:
    macd:
      fast: 10  # ← Как у ETH
      slow: 22  # ← Как у ETH
```

**Гипотеза:**
- MACD(10/22) эффективен не только для ETH
- Применение на SOL/XRP может улучшить результаты

---

### ШАГ 4: Добавить Entry Filter по Режиму Рынка (ЗАВТРА - 1 час)

**Файл:** `src/strategies/scalping/futures/signal_generator.py`

```python
def generate_signal(self, symbol, klines):
    # Существующий код...
    
    # НОВОЕ: проверить режим рынка
    regime = self.regime_manager.get_current_regime(symbol)
    
    if signal.direction == LONG:
        if regime == "downtrend":
            logger.info(f"Rejected {symbol} LONG in downtrend regime")
            return None  # отклонить
    
    if signal.direction == SHORT:
        if regime == "uptrend":
            logger.info(f"Rejected {symbol} SHORT in uptrend regime")
            return None  # отклонить
    
    return signal
```

**Результат:**
- Меньше входов в "неправильные" моменты
- Должно улучшить win rate

---

### ШАГ 5: Проанализировать 273 Отклонённых Сигнала (ПОСЛЕЗАВТРА - 2 часа)

**Метод:**
1. Добавить логирование всех отклонённых сигналов с причиной
2. Запустить бота ещё раз на архиве
3. Извлечь все отклонённые сигналы из логов
4. Проверить: были ли среди них потенциально прибыльные?

```python
# Добавить в signal_generator.py:

rejected_signals = []

def reject_signal(self, signal, reason):
    rejected_signals.append({
        'time': now(),
        'symbol': signal.symbol,
        'direction': signal.direction,
        'price': signal.price,
        'reason': reason
    })
    logger.debug(f"Rejected signal: {signal.symbol} {signal.direction} @ {signal.price} - {reason}")
```

**После сбора данных:**
- Определить: какие причины отклонения самые частые?
- Какие причины отклоняют ХОРОШИЕ сигналы?
- Можно ли смягчить фильтры?

---

## 🎯 ДОЛГОСРОЧНЫЕ ИЗМЕНЕНИЯ

### Архитектурные Проблемы:

1. **Per-Symbol параметры не помогают**
   - DOGE/XRP оптимизированы, но результат 0%
   - Может быть, нужен другой подход (например, ML)?
   - Или просто оптимизация неправильная?

2. **300+ сигналов → 27 сделок**
   - Это либо правильная фильтрация (отклонение плохих)
   - Либо неправильная фильтрация (отклонение хороших)
   - Нужен анализ пропущенных сигналов

3. **Фильтры не интегрированы**
   - MTF, Correlation, Volume, ARM работают отдельно
   - Может быть конфликт между ними?
   - Нужна единая система оценки сигналов

---

## 📊 МЕТРИКИ ДЛЯ ОТСЛЕЖИВАНИЯ

После исправлений отслеживать:

```
Неделя 1:
- [ ] SL hit rate упал ниже 50% (от 81.5%)
- [ ] Win rate выше 25% (от 18.5%)
- [ ] P&L положительный (от -6.39)

Неделя 2:
- [ ] SL hit rate ниже 30% (норма для скальпинга)
- [ ] Win rate выше 40% (минимум требуемый)
- [ ] P&L +10 USD минимум

Неделя 3:
- [ ] SL hit rate 20-25% (оптимально)
- [ ] Win rate 45-50% (хороший уровень)
- [ ] P&L +50 USD
```

---

## 📝 ИТОГОВОЕ РЕЗЮМЕ

**Проблемы:**
1. ❌ SL рассчитывается неправильно (слишком узко)
2. ❌ Entry timing неоптимален (входы в консолидацию)
3. ❌ BTC полностью неработающая (0% win)
4. ❌ DOGE технические ошибки (entry=stop)
5. ❌ Фильтры блокируют 91% сигналов

**Решение:**
1. ✅ Отладить расчёт SL (добавить логирование)
2. ✅ Отключить BTC и DOGE (источники убытков)
3. ✅ Скопировать ETH параметры на другие пары
4. ✅ Добавить фильтр по режиму рынка в Entry
5. ✅ Проанализировать пропущенные сигналы

**Ожидаемый результат:**
- Win rate с 18.5% → 40%+ (минимум)
- SL hit rate с 81.5% → <30% (норма)
- P&L с -6.39 USD → положительный

**Сроки:**
- Фаза 1 (Отладка): Сегодня (1 час)
- Фаза 2 (Простые исправления): Завтра (2 часа)
- Фаза 3 (Анализ): Послезавтра (2 часа)
- **Полное восстановление: 3 дня**

