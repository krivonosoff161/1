# 🚨 КРИТИЧЕСКИЕ ПРОБЛЕМЫ: TSL не работает для всех позиций!

**Дата:** 26.11.2025 21:40  
**Критичность:** 🔴 ОЧЕНЬ ВЫСОКАЯ

---

## 📊 ОБНАРУЖЕННЫЕ ПРОБЛЕМЫ

### **Проблема 1: TSL не инициализируется при загрузке существующих позиций**

**Файл:** `src/strategies/scalping/futures/orchestrator.py`

**Строка 679:**
```python
await self._load_existing_positions()
```

**Строка 1274:**
```python
tsl = self.trailing_sl_coordinator.initialize_trailing_stop(
```

**Проблема:**
- В логах **НЕТ** записей "Загружена позиция ... TrailingStopLoss инициализирован"
- Это значит, что `_load_existing_positions()` либо не вызывается, либо не инициализирует TSL

**Проверка:**
- Нет логов "📊 Загрузка существующих позиций с биржи..."
- Нет логов "✅ Загружена позиция BTC/ETH/XRP ... TrailingStopLoss инициализирован"
- Нет логов "📊 Загружено X существующих позиций с TrailingStopLoss"

---

### **Проблема 2: TSL инициализируется БЕЗ await в _sync_positions_with_exchange**

**Файл:** `src/strategies/scalping/futures/orchestrator.py`

**Строка 1579:**
```python
self.trailing_sl_coordinator.initialize_trailing_stop(  # ❌ НЕТ await!
    symbol=symbol,
    entry_price=effective_price,
    side=trailing_side,
    current_price=mark_price,
    signal=signal_with_regime,
)
```

**Проблема:**
- `initialize_trailing_stop()` - это **синхронный** метод (не async)
- Но он вызывается в async контексте
- **НЕТ** проверки результата возврата (None или TrailingStopLoss)
- Если инициализация не удалась, никто об этом не узнает

**Проверка:**
- Метод `initialize_trailing_stop()` синхронный (строка 143 в trailing_sl_coordinator.py)
- Результат не сохраняется в переменную (в отличие от строки 1274)

---

### **Проблема 3: periodic_check может не вызываться для позиций без TSL**

**Файл:** `src/strategies/scalping/futures/coordinators/trailing_sl_coordinator.py`

**Строка 1125-1129:**
```python
symbols_to_check = list(self.trailing_sl_by_symbol.keys())
if self.active_positions_ref:
    for symbol in self.active_positions_ref.keys():
        if symbol not in symbols_to_check:
            symbols_to_check.append(symbol)
```

**Проблема:**
- `periodic_check()` проверяет позиции из `trailing_sl_by_symbol` и `active_positions_ref`
- Если позиция есть в `active_positions_ref`, но TSL не инициализирован - она все равно попадает в проверку
- Но `update_trailing_stop_loss()` может не инициализировать TSL правильно

---

### **Проблема 4: Автоматическая инициализация TSL может не работать**

**Файл:** `src/strategies/scalping/futures/coordinators/trailing_sl_coordinator.py`

**Строка 413-477:**
```python
if symbol not in self.trailing_sl_by_symbol:
    logger.warning(
        f"⚠️ TrailingStopLoss не инициализирован для {symbol} "
        f"(позиция найдена в active_positions, но нет в trailing_sl_by_symbol). "
        f"Инициализируем TSL автоматически..."
    )
    # ... автоматическая инициализация ...
```

**Проблема:**
- В логах **НЕТ** записей этого предупреждения для ETH/BTC/XRP
- Это значит, что либо TSL уже есть, либо `update_trailing_stop_loss()` не вызывается для этих позиций

---

## 🔍 АНАЛИЗ ДЛЯ ВСЕХ ПАР

### **BTC-USDT:**
- В логах: PnL виден, позиция отслеживается
- ❌ НЕТ записей о TSL инициализации
- ❌ НЕТ записей об обновлении TSL

### **ETH-USDT:**
- В логах: PnL=-10.21%, позиция отслеживается
- ❌ НЕТ записей о TSL инициализации
- ❌ НЕТ записей об обновлении TSL
- ❌ Позиция не закрывается по loss_cut

### **XRP-USDT:**
- В логах: PnL виден, позиция отслеживается
- ❌ НЕТ записей о TSL инициализации
- ❌ НЕТ записей об обновлении TSL

---

## 🎯 КОРЕНЬ ПРОБЛЕМЫ

### **Гипотеза 1: `_load_existing_positions()` не вызывается или падает с ошибкой**

**Проверка:**
- Логов "📊 Загрузка существующих позиций с биржи..." нет
- Либо метод не вызывается, либо падает тихо

### **Гипотеза 2: TSL инициализируется в `_sync_positions_with_exchange()`, но результат игнорируется**

**Проблема:**
```python
self.trailing_sl_coordinator.initialize_trailing_stop(...)  # Результат не проверяется!
```

**Если `initialize_trailing_stop()` возвращает `None`:**
- Никто об этом не узнает
- TSL не создается, но код продолжает работать

### **Гипотеза 3: `periodic_check()` вызывается, но `update_trailing_stop_loss()` не работает**

**Проверка:**
- В логах нет записей об обновлении TSL
- Либо `periodic_check()` не вызывается, либо `update_trailing_stop_loss()` не логирует

---

## 🔧 ЧТО НУЖНО ПРОВЕРИТЬ

1. ✅ Вызывается ли `_load_existing_positions()`?
2. ✅ Есть ли ошибки в `_load_existing_positions()`?
3. ✅ Сохраняется ли результат `initialize_trailing_stop()` в строке 1579?
4. ✅ Вызывается ли `periodic_check()`?
5. ✅ Вызывается ли `update_trailing_stop_loss()` для всех позиций?
6. ✅ Работает ли автоматическая инициализация TSL в `update_trailing_stop_loss()`?

---

## 📝 ВЫВОДЫ

### **Критическая проблема:**

**TSL не работает для ВСЕХ позиций!**

- ❌ TSL не инициализируется при загрузке существующих позиций
- ❌ TSL не инициализируется в `_sync_positions_with_exchange()` (результат игнорируется)
- ❌ `periodic_check()` не вызывает `update_trailing_stop_loss()` для позиций без TSL
- ❌ Позиции не закрываются по loss_cut

**Это объясняет, почему:**
- ETH позиция не закрывается при -10.21% убытке
- BTC позиция не закрывается по TSL
- XRP позиция не закрывается по TSL

---

## 🚨 КРИТИЧНОСТЬ

**🔴 ОЧЕНЬ ВЫСОКАЯ:**

- Все позиции открыты без защиты TSL
- Убытки не ограничиваются loss_cut
- Риск больших потерь

**Нужно исправить СРОЧНО!**

