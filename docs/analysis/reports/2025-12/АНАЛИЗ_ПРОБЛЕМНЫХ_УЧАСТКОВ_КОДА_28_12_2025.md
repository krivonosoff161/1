# 🔍 АНАЛИЗ ПРОБЛЕМНЫХ УЧАСТКОВ КОДА

**Дата:** 28.12.2025  
**Цель:** Детальный анализ критических проблем в коде торгового бота

---

## 📋 СОДЕРЖАНИЕ

1. [risk_manager.py - Проблема с MarginMonitor](#1-risk_managerpy---проблема-с-marginmonitor)
2. [exit_analyzer.py - Ошибка сравнения типов](#2-exit_analyzerpy---ошибка-сравнения-типов)
3. [config_futures.yaml - Конфигурация режимов](#3-config_futuresyaml---конфигурация-режимов)

---

## 1. risk_manager.py - Проблема с MarginMonitor

### 📍 Расположение проблемы

**Файл:** `src/strategies/scalping/futures/risk_manager.py`  
**Строки:** 1705-1729  
**Критическая строка:** 1724

### 🔴 Проблемный код

```python
async def check_margin_safety(
    self,
    position_size_usd: float,
    current_positions: Dict[str, Any],
) -> bool:
    """
    Проверка безопасности маржи.

    Args:
        position_size_usd: Размер новой позиции
        current_positions: Текущие позиции

    Returns:
        bool: True если безопасно открывать
    """
    if not self.margin_monitor:
        return True

    try:
        return await self.margin_monitor.check_safety(  # ❌ ОШИБКА: метод не существует!
            position_size_usd, current_positions
        )
    except Exception as e:
        logger.error(f"❌ Error checking margin safety: {e}")
        return False
```

### 🔍 Анализ проблемы

**Ошибка в логах:**
```
2025-12-27 14:09:31.768 | ERROR | risk_manager:check_margin_safety:1708 | 
❌ Error checking margin safety: 'MarginMonitor' object has no attribute 'check_safety'
```

**Причина:**
1. `RiskManager` вызывает метод `check_safety()` у объекта `MarginMonitor`
2. Но в классе `MarginMonitor` этот метод **НЕ СУЩЕСТВУЕТ**
3. Доступные методы в `MarginMonitor`:
   - `check_margin_available()` ✅
   - `get_margin_ratio()` ✅
   - `check_safety()` ❌ **НЕ СУЩЕСТВУЕТ**

**Последствия:**
- Каждая проверка маржи падает с ошибкой
- Бот не может корректно проверить безопасность открытия позиции
- Все проверки маржи возвращают `False` (блокируют открытие позиций)
- Низкая конверсия сигналов в сделки (~3.1%)

### ✅ Решение

**Вариант 1:** Добавить метод `check_safety()` в `MarginMonitor`

```python
# Файл: src/strategies/scalping/futures/risk/margin_monitor.py

async def check_safety(
    self, 
    position_size_usd: float, 
    current_positions: Dict[str, Any]
) -> bool:
    """
    Проверяет безопасность открытия новой позиции.

    Args:
        position_size_usd: Размер новой позиции в USD
        current_positions: Текущие открытые позиции

    Returns:
        bool: True если безопасно открывать позицию
    """
    # Получаем текущий баланс и использованную маржу
    # (нужно получить из контекста или передать как параметры)
    current_balance = self._get_current_balance()  # Нужно реализовать
    used_margin = self._calculate_used_margin(current_positions)  # Нужно реализовать
    
    # Проверяем доступность маржи
    available_margin = current_balance - used_margin
    
    # Проверяем, достаточно ли маржи для новой позиции
    if position_size_usd > available_margin:
        return False
    
    # Проверяем коэффициент использования маржи
    margin_ratio = self.get_margin_ratio(current_balance, used_margin + position_size_usd)
    
    # Безопасно если коэффициент < 0.8 (80%)
    return margin_ratio < 0.8
```

**Вариант 2:** Изменить вызов в `RiskManager` на существующий метод

```python
# Файл: src/strategies/scalping/futures/risk_manager.py
# Строка 1724

try:
    # Используем существующий метод check_margin_available
    current_balance = await self._get_current_balance()  # Нужно реализовать
    used_margin = await self._calculate_used_margin(current_positions)  # Нужно реализовать
    
    allowed, reason = self.margin_monitor.check_margin_available(
        position_size_usd, current_balance, used_margin
    )
    
    if not allowed:
        logger.warning(f"⚠️ Margin check failed: {reason}")
    
    return allowed
except Exception as e:
    logger.error(f"❌ Error checking margin safety: {e}")
    return False
```

---

## 2. exit_analyzer.py - Ошибка сравнения типов

### 📍 Расположение проблемы

**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py`  
**Строки:** 3270-3320  
**Критическая строка:** 3291

### 🔴 Проблемный код

```python
# Строки 3274-3291
elif minutes_in_position is not None and isinstance(
    minutes_in_position, (int, float)
):
    # ✅ ИСПРАВЛЕНО: Конвертируем max_holding_minutes в float перед сравнением
    try:
        max_holding_minutes_float = (
            float(max_holding_minutes)
            if max_holding_minutes is not None
            else 0.0
        )
    except (TypeError, ValueError):
        logger.warning(
            f"⚠️ ExitAnalyzer: Не удалось преобразовать max_holding_minutes={max_holding_minutes} в float, "
            f"используем actual_max_holding_float={actual_max_holding_float}"
        )
        max_holding_minutes_float = actual_max_holding_float

    if float(minutes_in_position) >= max_holding_minutes_float:  # ❌ ОШИБКА: actual_max_holding_float может быть не определена!
        # Базовое время превышено, но есть продление - проверяем прибыль
        # ✅ ИСПРАВЛЕНО: Используем Net PnL для проверки продления (реальная прибыль после комиссий)
        if (
            extend_time_if_profitable
            and net_pnl_percent >= min_profit_for_extension
        ):
            logger.debug(
                f"⏰ ExitAnalyzer RANGING: Время {minutes_in_position:.1f} мин >= {max_holding_minutes_float:.1f} мин, "
                f"но Net прибыль {net_pnl_percent:.2f}% >= {min_profit_for_extension:.2f}% "
                f"(Gross PnL {gross_pnl_percent:.2f}%) - продлеваем до {actual_max_holding:.1f} мин"
            )
            # Продлеваем, но не закрываем пока
            return None

# Строки 3306-3320
# В ranging режиме не продлеваем TP - более консервативный подход
time_info = "N/A"
if minutes_in_position is not None:
    if actual_max_holding is not None:
        time_info = (
            f"{minutes_in_position:.1f} мин / {actual_max_holding:.1f} мин"
        )
    else:
        time_info = f"{minutes_in_position:.1f} мин"

# ✅ ИСПРАВЛЕНИЕ (21.12.2025): Используем правильное значение trigger_percent в логировании
# ✅ ИСПРАВЛЕНО: Используем Net PnL для проверки partial_tp (реальная прибыль после комиссий)
# ✅ ИСПРАВЛЕНО: Конвертируем trigger_percent и net_pnl_percent в float перед сравнением
try:
    trigger_percent_float = (
        float(trigger_percent) if trigger_percent is not None else None
    )
    net_pnl_percent_float = (
        float(net_pnl_percent) if net_pnl_percent is not None else 0.0
    )
except (TypeError, ValueError) as e:
    logger.warning(
        f"⚠️ ExitAnalyzer: Ошибка конвертации trigger_percent={trigger_percent} или net_pnl_percent={net_pnl_percent}: {e}"
    )
```

### 🔍 Анализ проблемы

**Ошибка в логах:**
```
2025-12-27 15:56:25.761 | ERROR | exit_analyzer:_generate_exit_for_ranging:3233 | 
❌ ExitAnalyzer: Ошибка анализа для BTC-USDT в режиме RANGING: 
'>' not supported between instances of 'str' and 'int'
```

**Частота:** 996+ ошибок за период анализа (каждые 2-3 секунды)

**Причина:**
1. `max_holding_minutes` читается из конфига через `_get_max_holding_minutes()`
2. В конфиге `config_futures.yaml` строка 319: `max_holding_minutes: 25` (число)
3. Но при чтении через `ParameterProvider` или `ConfigManager` может возвращаться **строка** `"25"`
4. В строке 3289 используется `actual_max_holding_float`, но эта переменная может быть **не определена** в этом блоке кода
5. Сравнение `float(minutes_in_position) >= max_holding_minutes_float` может сравнивать строку с числом

**Математика ошибки:**
- `minutes_in_position` = float (например, 0.5)
- `max_holding_minutes` = может быть строкой `"25"` или числом `25`
- `actual_max_holding_float` = может быть не определена в блоке `elif`
- Сравнение `0.5 >= "25"` → `TypeError: '>' not supported between instances of 'str' and 'int'`

### ✅ Решение

**Исправленный код:**

```python
# Строки 3274-3291
elif minutes_in_position is not None and isinstance(
    minutes_in_position, (int, float)
):
    # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Убеждаемся, что max_holding_minutes всегда float
    try:
        # Сначала пытаемся конвертировать max_holding_minutes
        max_holding_minutes_float = (
            float(max_holding_minutes)
            if max_holding_minutes is not None
            else 25.0  # Fallback значение
        )
    except (TypeError, ValueError) as e:
        logger.warning(
            f"⚠️ ExitAnalyzer: Не удалось преобразовать max_holding_minutes={max_holding_minutes} "
            f"(type={type(max_holding_minutes).__name__}) в float: {e}, используем 25.0"
        )
        max_holding_minutes_float = 25.0
    
    # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Убеждаемся, что minutes_in_position тоже float
    try:
        minutes_in_position_float = float(minutes_in_position)
    except (TypeError, ValueError) as e:
        logger.warning(
            f"⚠️ ExitAnalyzer: Не удалось преобразовать minutes_in_position={minutes_in_position} "
            f"(type={type(minutes_in_position).__name__}) в float: {e}"
        )
        return None  # Не можем сравнить - возвращаем None

    # ✅ ТЕПЕРЬ БЕЗОПАСНО: Оба значения гарантированно float
    if minutes_in_position_float >= max_holding_minutes_float:
        # Базовое время превышено, но есть продление - проверяем прибыль
        # ✅ ИСПРАВЛЕНО: Используем Net PnL для проверки продления (реальная прибыль после комиссий)
        if (
            extend_time_if_profitable
            and net_pnl_percent >= min_profit_for_extension
        ):
            logger.debug(
                f"⏰ ExitAnalyzer RANGING: Время {minutes_in_position_float:.1f} мин >= {max_holding_minutes_float:.1f} мин, "
                f"но Net прибыль {net_pnl_percent:.2f}% >= {min_profit_for_extension:.2f}% "
                f"(Gross PnL {gross_pnl_percent:.2f}%) - продлеваем до {actual_max_holding:.1f} мин"
            )
            # Продлеваем, но не закрываем пока
            return None
```

---

## 3. config_futures.yaml - Конфигурация режимов

### 📍 Расположение

**Файл:** `config/config_futures.yaml`  
**Секции:** `trending`, `ranging`, `choppy`

### 📄 Полный код конфигурации режимов

#### 🔵 TRENDING режим (строки 230-298)

```yaml
# Trending режим (тренд) - ✅ ОПТИМИЗИРОВАНО ДЛЯ КАЧЕСТВЕННЫХ СДЕЛОК
trending:
  block_counter_trend: true  # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Полная блокировка противотрендовых сигналов
  conflict_multiplier: 0.0   # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: 0.0 = полная блокировка (не ослабление)
  min_score_threshold: 2.0  # ✅ ОПТИМИЗАЦИЯ: Увеличено с 1.4 до 2.0 (+43%) - фильтрация слабых сигналов
  max_trades_per_hour: 20  # ✅ ОПТИМИЗАЦИЯ: Снижено с 35 до 20 (-43%) - меньше сделок, но качественнее
  position_size_multiplier: 1.3  # ✅ АГРЕССИВНО: Увеличено с 1.1 до 1.3 (+18%) - больше размер позиций
  tp_atr_multiplier: 1.2  # ✅ АГРЕССИВНО: Увеличено с 1.0 до 1.2 (+20%) - больше прибыль
  sl_atr_multiplier: 0.6  # ✅ АГРЕССИВНО: Увеличено с 0.5 до 0.6 (+20%) - больше места для откатов
  tp_percent: 6.0  # ✅ ОПТИМИЗАЦИЯ: Увеличено с 2.5% до 6.0% (+140%) - нормальные прибыли +4-7%
  sl_percent: 1.2  # ✅ ОПТИМИЗАЦИЯ: Увеличено с 0.8% до 1.2% - баланс с новым TP
  max_holding_minutes: 40  # ✅ АГРЕССИВНО: Увеличено с 30 до 40 минут (+33%) - больше времени для прибыли
  extend_time_if_profitable: true  # ✅ НОВОЕ: Продлевать время если позиция в прибыли
  min_profit_for_extension: 0.4  # ✅ АГРЕССИВНО: Снижено с 0.5% до 0.4% - раньше продлеваем
  extension_percent: 120  # ✅ АГРЕССИВНО: Увеличено с 100% до 120% - продлеваем еще больше
  cooldown_after_loss_minutes: 0.3  # ✅ АГРЕССИВНО: Уменьшено с 0.5 до 0.3 мин - быстрее возвращаемся
  pivot_bonus_multiplier: 1.3
  volume_profile_bonus_multiplier: 1.1
  ph_enabled: true
  ph_threshold_type: "margin_percent"  # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ (21.12.2025): Адаптивный PH от маржи
  ph_threshold_percent: 2.5  # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ (21.12.2025): 2.5% от маржи для trending
  ph_threshold: 0.12  # ✅ Fallback для обратной совместимости
  ph_time_limit: 600  # ✅ УВЕЛИЧЕНО: 10 минут (было 180 = 3 минуты)
  
  # Индикаторы для тренда
  indicators:
    rsi_overbought: 85  # ✅ ИСПРАВЛЕНО: Увеличено с 75 до 85
    rsi_oversold: 25
    volume_threshold: 1.05  # ✅ PHASE 1: Снижено с 1.15 до 1.05 (-9%)
    sma_fast: 8
    sma_slow: 21
    ema_fast: 8
    ema_slow: 21
    atr_period: 14
    min_volatility_atr: 0.0004
    min_ma_difference_pct: 0.05  # ✅ ПРИОРИТЕТ 2 (28.12.2025): Снижено с 0.1% до 0.05% для большего количества сигналов
    macd_strength_divider: 120.0  # ✅ ПРИОРИТЕТ 1 (28.12.2025): Адаптивный делитель для MACD strength (Trending: более агрессивно)
    macd_fast: 19  # ✅ ПРИОРИТЕТ 2 (28.12.2025): Режим-специфичные MACD параметры
    macd_slow: 39
    macd_signal: 9
    bb_period: 20  # ✅ ПРИОРИТЕТ 2 (28.12.2025): Режим-специфичные Bollinger Bands
    bb_std_multiplier: 2.0  # Шире полосы для тренда
  
  # Модули для тренда
  modules:
    multi_timeframe:
      block_opposite: false
      score_bonus: 1
      confirmation_timeframe: "15m"
      fail_open_enabled: true
      fail_open_blocks: 3
      fail_open_cooldown_seconds: 60
    correlation_filter:
      threshold: 0.7
      max_correlated_positions: 5  # ✅ УВЕЛИЧЕНО: С 3 до 5 для разрешения всех 5 торгуемых пар
      block_same_direction_only: true
    pivot_points:
      level_tolerance_percent: 0.2
      score_bonus_near_level: 2
      use_last_n_days: 3
    volume_profile:
      score_bonus_in_value_area: 1
      score_bonus_near_poc: 2
      poc_tolerance_percent: 0.3
      lookback_candles: 200
    adx_filter:
      adx_threshold: 18.0  # Ниже для Futures
      adx_di_difference: 1.5
```

#### 🟡 RANGING режим (строки 299-378)

```yaml
# Ranging режим (флэт) - ✅ ОПТИМИЗИРОВАНО ДЛЯ КАЧЕСТВЕННЫХ СДЕЛОК
ranging:
  block_counter_trend: false  # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Блокировка отключена (ослабление допустимо)
  conflict_multiplier: 0.3   # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Сильное ослабление (30% от силы)
  min_score_threshold: 1.8  # ✅ ОПТИМИЗАЦИЯ: Увеличено с 1.4 до 1.8 (+29%) - фильтрация слабых сигналов
  max_trades_per_hour: 15  # ✅ ОПТИМИЗАЦИЯ: Снижено с 22 до 15 (-32%) - меньше сделок, но качественнее
  position_size_multiplier: 1.2  # ✅ АГРЕССИВНО: Увеличено с 1.0 до 1.2 (+20%) - больше размер позиций
  tp_atr_multiplier: 1.0  # ✅ АГРЕССИВНО: Увеличено с 0.8 до 1.0 (+25%) - больше прибыль
  sl_atr_multiplier: 0.6  # ✅ АГРЕССИВНО: Увеличено с 0.5 до 0.6 (+20%) - больше места для откатов
  tp_percent: 5.0  # ✅ ОПТИМИЗАЦИЯ: Увеличено с 1.8% до 5.0% (+178%) - нормальные прибыли +4-7%
  tp_adaptive: true  # ✅ ГРОК: Включить адаптивный TP с ATR
  tp_atr_based: true  # ✅ ГРОК ФИКС: Включить ATR-based TP для ranging
  tp_atr_multiplier: 2.5  # ✅ ГРОК ФИКС: Множитель ATR для TP (2.5 для ranging)
  tp_min_percent: 1.5  # ✅ ГРОК ФИКС: Минимальный TP 1.5%
  tp_max_percent: 2.2  # ✅ ГРОК: Максимальный TP 2.2%
  sl_percent: 1.0  # ✅ ГРОК ФИКС: Увеличено с 0.8% до 1.0% (баланс для ranging noise)
  sl_atr_based: true  # ✅ ГРОК: Использовать ATR-based SL вместо фиксированного
  sl_atr_multiplier: 1.5  # ✅ ГРОК ФИКС: Увеличено с 1.2 до 1.5 для меньших шумовых хитов (max(0.6%, 1.5*ATR_1m))
  sl_min_percent: 0.6  # ✅ ГРОК: Минимальный SL 0.6% (защита от шума)
  sl_max_percent: 1.2  # ✅ ГРОК ФИКС: Максимальный SL 1.2% (защита от слишком широкого SL)
  max_holding_minutes: 25  # ✅ ГРОК: Уменьшено с 28 до 25 минут для более быстрой ротации
  max_holding_hard_stop: true  # ✅ ГРОК: Жесткий стоп по max_holding (не игнорировать убыточные)
  timeout_loss_percent: 1.5  # ✅ ГРОК: Закрывать по max_holding если убыток >= 1.5%
  extend_time_if_profitable: true  # ✅ НОВОЕ: Продлевать время если позиция в прибыли
  min_profit_for_extension: 0.4  # ✅ АГРЕССИВНО: Снижено с 0.5% до 0.4% - раньше продлеваем
  extension_percent: 120  # ✅ АГРЕССИВНО: Увеличено с 100% до 120% - продлеваем еще больше
  cooldown_after_loss_minutes: 0.5  # ✅ АГРЕССИВНО: Уменьшено с 1 до 0.5 мин - быстрее возвращаемся
  pivot_bonus_multiplier: 1.2
  volume_profile_bonus_multiplier: 1.0
  ph_enabled: true
  ph_threshold_type: "margin_percent"  # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ (21.12.2025): Адаптивный PH от маржи
  ph_threshold_percent: 0.9  # ✅ ГРОК ФИКС: 0.9% (баланс между 0.8-1.0% для early PH)
  ph_min_absolute_usd: 0.05  # ✅ ГРОК КОМПРОМИСС: Минимальный абсолютный порог 0.05 USD (1.5-2x комиссии)
  ph_threshold: 0.10  # ✅ Fallback для обратной совместимости
  ph_time_limit: 1200  # ✅ УВЕЛИЧЕНО: 20 минут (было 300 = 5 минут) для большего охвата
  min_holding_minutes: 0.5  # ✅ ГРОК: Уменьшено с 1 до 0.5 минуты (30 сек) для быстрого Partial TP
  
  indicators:
    rsi_overbought: 85  # ✅ ИСПРАВЛЕНО: Увеличено с 70 до 85
    rsi_oversold: 25  # ✅ ИСПРАВЛЕНО: Уменьшено с 30 до 25
    volume_threshold: 1.05  # ✅ PHASE 1: Снижено с 1.1 до 1.05 (-5%)
    sma_fast: 10
    sma_slow: 25
    ema_fast: 10
    ema_slow: 25
    atr_period: 14
    min_volatility_atr: 0.0003
    min_ma_difference_pct: 0.015  # ✅ ПРИОРИТЕТ 2 (28.12.2025): Повышено с 0.002% до 0.015% для фильтрации шума
    macd_strength_divider: 180.0  # ✅ ПРИОРИТЕТ 1 (28.12.2025): Адаптивный делитель для MACD strength (Ranging: консервативно)
    macd_fast: 12  # ✅ ПРИОРИТЕТ 2 (28.12.2025): Режим-специфичные MACD параметры (стандартные для ranging)
    macd_slow: 26
    macd_signal: 9
    bb_period: 20  # ✅ ПРИОРИТЕТ 2 (28.12.2025): Режим-специфичные Bollinger Bands
    bb_std_multiplier: 1.8  # Узкие полосы для флэта (уже оптимально)
  
  modules:
    multi_timeframe:
      block_opposite: true
      score_bonus: 1.5  # ✅ ИСПРАВЛЕНО: Снижено с 2 до 1.5 для ranging (менее агрессивно блокируем counter-trend сделки)
      confirmation_timeframe: "5m"
      fail_open_enabled: true
      fail_open_blocks: 3
      fail_open_cooldown_seconds: 60
    correlation_filter:
      threshold: 0.8
      max_correlated_positions: 5  # ✅ УВЕЛИЧЕНО: С 4 до 5 для разрешения всех 5 торгуемых пар
      block_same_direction_only: true
    pivot_points:
      level_tolerance_percent: 0.25
      score_bonus_near_level: 1
      use_last_n_days: 4
    volume_profile:
      score_bonus_in_value_area: 0
      score_bonus_near_poc: 1
      poc_tolerance_percent: 0.4
      lookback_candles: 150
    adx_filter:
      adx_threshold: 20.0  # ✅ ГРОК: Поднято с 15.0 до 20.0 для более сильных трендов
      adx_di_difference: 1.2
```

#### 🔴 CHOPPY режим (строки 379-444)

```yaml
# Choppy режим (хаос) - ✅ ОПТИМИЗИРОВАНО ДЛЯ КАЧЕСТВЕННЫХ СДЕЛОК
choppy:
  block_counter_trend: false  # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Блокировка отключена (ослабление допустимо)
  conflict_multiplier: 0.5   # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Умеренное ослабление (50% от силы)
  min_score_threshold: 2.0  # ✅ ОПТИМИЗАЦИЯ: Увеличено с 1.6 до 2.0 (+25%) - фильтрация слабых сигналов
  max_trades_per_hour: 8  # ✅ ОПТИМИЗАЦИЯ: Снижено с 10 до 8 (-20%) - меньше сделок, но качественнее
  position_size_multiplier: 1.0  # ✅ АГРЕССИВНО: Увеличено с 0.8 до 1.0 (+25%) - больше размер позиций
  tp_atr_multiplier: 1.1  # ✅ АГРЕССИВНО: Увеличено с 0.9 до 1.1 (+22%) - больше прибыль
  sl_atr_multiplier: 0.5  # ✅ АГРЕССИВНО: Увеличено с 0.4 до 0.5 (+25%) - больше места для откатов
  tp_percent: 4.5  # ✅ ОПТИМИЗАЦИЯ: Увеличено с 1.8% до 4.5% (+150%) - нормальные прибыли +4-7%
  sl_percent: 1.2  # ✅ ОПТИМИЗАЦИЯ: Увеличено с 1.0% до 1.2% - баланс с новым TP
  max_holding_minutes: 14  # ✅ АГРЕССИВНО: Увеличено с 10 до 14 минут (+40%) - больше времени для прибыли
  extend_time_if_profitable: true  # ✅ АГРЕССИВНО: Включено продление даже в хаосе
  min_profit_for_extension: 0.4  # ✅ АГРЕССИВНО: Снижено с 0.5% до 0.4% - раньше продлеваем
  extension_percent: 100  # ✅ АГРЕССИВНО: Увеличено с 50% до 100% - продлеваем больше
  cooldown_after_loss_minutes: 1.0  # ✅ АГРЕССИВНО: Уменьшено с 1.5 до 1.0 мин - быстрее возвращаемся
  pivot_bonus_multiplier: 1.0
  volume_profile_bonus_multiplier: 0.9
  ph_enabled: true
  ph_threshold_type: "margin_percent"  # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ (21.12.2025): Адаптивный PH от маржи
  ph_threshold_percent: 1.0  # ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ (21.12.2025): 1.0% от маржи для choppy
  ph_threshold: 0.1  # ✅ Fallback для обратной совместимости
  ph_time_limit: 60
  
  indicators:
    rsi_overbought: 85  # ✅ ИСПРАВЛЕНО: Увеличено с 65 до 85
    rsi_oversold: 25  # ✅ ИСПРАВЛЕНО: Уменьшено с 35 до 25
    volume_threshold: 1.1  # ✅ PHASE 1: Снижено с 1.2 до 1.1 (-8%)
    sma_fast: 12
    sma_slow: 30
    ema_fast: 12
    ema_slow: 30
    atr_period: 20
    min_volatility_atr: 0.0005
    min_ma_difference_pct: 0.03  # ✅ АДАПТИВНО: Минимальная разница EMA для CHOPPY (0.03% - еще менее строго для хаоса)
    macd_strength_divider: 150.0  # ✅ ПРИОРИТЕТ 1 (28.12.2025): Адаптивный делитель для MACD strength (Choppy: баланс)
    macd_fast: 8  # ✅ ПРИОРИТЕТ 2 (28.12.2025): Режим-специфичные MACD параметры (быстрые для хаоса)
    macd_slow: 21
    macd_signal: 5
    bb_period: 15  # ✅ ПРИОРИТЕТ 2 (28.12.2025): Режим-специфичные Bollinger Bands (быстрее период)
    bb_std_multiplier: 2.2  # Шире полосы для защиты от шума
  
  modules:
    multi_timeframe:
      block_opposite: true
      score_bonus: 3
      confirmation_timeframe: "5m"
      fail_open_enabled: true
      fail_open_blocks: 3
      fail_open_cooldown_seconds: 60
    correlation_filter:
      threshold: 0.85
      max_correlated_positions: 6  # ✅ ОСТАВЛЕНО: 6 достаточно для всех пар с запасом
      block_same_direction_only: false
    pivot_points:
      level_tolerance_percent: 0.3
      score_bonus_near_level: 0
      use_last_n_days: 5
    volume_profile:
      score_bonus_in_value_area: -1
      score_bonus_near_poc: 0
      poc_tolerance_percent: 0.5
      lookback_candles: 100
    adx_filter:
      adx_threshold: 12.0
      adx_di_difference: 1.0
```

### 🔍 Анализ конфигурации

#### Проблемы в RANGING режиме:

1. **`max_holding_minutes: 25`** (строка 319)
   - Может читаться как строка `"25"` вместо числа `25`
   - Приводит к ошибке сравнения типов в `exit_analyzer.py`

2. **`sl_percent: 1.0`** (строка 314)
   - Слишком близко к цене входа
   - Позиции закрываются очень быстро (3-48 секунд)
   - Рекомендация: увеличить до `1.5-2.0%`

3. **`min_holding_minutes: 0.5`** (строка 334)
   - Слишком мало (30 секунд)
   - Не защищает от раннего закрытия по SL
   - Рекомендация: увеличить до `1.0-2.0` минут

#### Проблемы в TRENDING режиме:

1. **`max_holding_minutes: 40`** (строка 241)
   - Может читаться как строка `"40"` вместо числа `40`
   - Та же проблема с типами

2. **`sl_percent: 1.2`** (строка 240)
   - Может быть недостаточно для тренда
   - Рекомендация: проверить на исторических данных

#### Проблемы в CHOPPY режиме:

1. **`max_holding_minutes: 14`** (строка 390)
   - Может читаться как строка `"14"` вместо числа `14`
   - Та же проблема с типами

2. **`sl_percent: 1.2`** (строка 389)
   - Может быть недостаточно для хаоса
   - Рекомендация: увеличить до `1.5-2.0%`

### ✅ Рекомендации по исправлению

1. **Убедиться, что все числовые значения читаются как числа:**
   - Проверить `ParameterProvider.get_exit_params()`
   - Проверить `ConfigManager.get_config_value()`
   - Добавить явную конвертацию в `float()` при чтении

2. **Увеличить SL для ranging режима:**
   ```yaml
   sl_percent: 1.5  # Было 1.0, увеличить до 1.5-2.0
   ```

3. **Увеличить min_holding_minutes для ranging:**
   ```yaml
   min_holding_minutes: 1.0  # Было 0.5, увеличить до 1.0-2.0
   ```

---

## 📝 ЗАКЛЮЧЕНИЕ

Все три проблемы критичны и требуют немедленного исправления:

1. **risk_manager.py** - блокирует открытие позиций из-за отсутствующего метода
2. **exit_analyzer.py** - вызывает 996+ ошибок, блокируя анализ позиций
3. **config_futures.yaml** - параметры могут читаться как строки вместо чисел

**Приоритет исправления:**
1. ✅ Исправить `exit_analyzer.py` (критично - блокирует анализ)
2. ✅ Исправить `risk_manager.py` (критично - блокирует открытие позиций)
3. ✅ Проверить чтение конфига (важно - предотвращает будущие ошибки)

---

**Дата создания:** 28.12.2025  
**Автор анализа:** AI Assistant (DeepSeek)




