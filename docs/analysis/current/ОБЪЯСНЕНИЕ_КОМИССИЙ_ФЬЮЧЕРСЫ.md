# 📊 Объяснение комиссий в фьючерсной торговле на OKX

## ❓ Вопрос пользователя:

**"Открывается ордер SHORT, он исполняется, открывается позиция с комиссией на сумму ордера. Закрывается позиция и закрывается она через ордер! И я плачу двойную комиссию. Мы вообще правильно торгуем? Или это особенности автоматической торговли? Почему не закрывается тот же ордер который открылся, а закрывается позиция новым ордером?"**

---

## ✅ ОТВЕТ: Да, это ПРАВИЛЬНО и НЕИЗБЕЖНО!

### 1️⃣ КАК РАБОТАЕТ ФЬЮЧЕРСНАЯ ТОРГОВЛЯ НА OKX:

#### 📤 **Открытие SHORT позиции:**
- Размещается ордер **SELL** (продажа контрактов)
- Комиссия: **0.05%** (taker) или **0.02%** (maker)
- Ордер исполняется → **позиция SHORT открыта**

#### 📥 **Закрытие SHORT позиции:**
- Размещается **НОВЫЙ ордер BUY** с `reduceOnly=True` (покупка контрактов обратно)
- Комиссия: **0.05%** (taker) или **0.02%** (maker)
- Ордер исполняется → **позиция SHORT закрыта**

---

### 2️⃣ ПОЧЕМУ НОВЫЙ ОРДЕР, А НЕ ЗАКРЫТИЕ ТОГО ЖЕ?

**✅ Это стандартная механика ВСЕХ бирж!**

- На фьючерсах **каждая сделка** - это **ОТДЕЛЬНЫЙ ордер**
- Нельзя "закрыть" ордер - можно только разместить **обратный ордер** с `reduceOnly=True`
- `reduceOnly=True` гарантирует, что новый ордер **только закроет позицию**, а **не откроет новую**

**Пример:**
```
1. Открытие: SELL 1.32 SOL @ $146.13 → позиция SHORT открыта
2. Закрытие: BUY 1.32 SOL @ $145.16 (reduceOnly=True) → позиция SHORT закрыта
```

---

### 3️⃣ КОМИССИЯ:

#### 📊 **На круг (Round Trip):**
- **Вход:** 0.05% (taker) или 0.02% (maker)
- **Выход:** 0.05% (taker) или 0.02% (maker)
- **ИТОГО:** 0.10% (taker) или 0.04% (maker)

#### 💡 **Почему две комиссии?**
- Каждая операция (открытие/закрытие) - это **отдельная сделка** на бирже
- Биржа взимает комиссию **за каждую сделку**
- Это **неизбежно** и работает так на всех биржах (Binance, Bybit, OKX и т.д.)

---

### 4️⃣ РЕАЛЬНЫЕ ДАННЫЕ С БИРЖИ (ETH-USDT LONG):

**Проверка по реальной сделке:**
- Entry: $3,270.88, Exit: $3,267.20, Size: 0.670 ETH
- Notional entry: 0.670 × $3,270.88 = **$2,191.49**
- Notional exit: 0.670 × $3,267.20 = **$2,189.02**
- Gross PnL (цена): $2,189.02 - $2,191.49 = **-$2.47 USDT**
- Комиссия биржи: **-$2.19 USDT**
- Реализованный PnL: -$2.47 - $2.19 = **-$4.66 USDT** ✅

**Проверка комиссии:**
- Комиссия% = $2.19 / $2,191.49 = **0.10%** (0.001)
- ✅ Совпадает с настройками OKX для taker ордеров

---

### 5️⃣ УЧЕТ В КОДЕ:

#### ✅ **Обновлено до реальных данных!**

**В конфиге (`config_futures.yaml`):**
```yaml
commission:
  trading_fee_rate: 0.0010  # ✅ ОБНОВЛЕНО: 0.10% на круг (0.05% вход + 0.05% выход для taker)
```

**В коде (`trailing_stop_loss.py`):**
```python
def get_profit_pct(self, current_price: float, include_fees: bool = True) -> float:
    # Базовая прибыль без комиссии
    gross_profit_pct = (self.entry_price - current_price) / self.entry_price  # для SHORT
    
    # Вычитаем комиссию (открытие + закрытие)
    if include_fees:
        net_profit_pct = gross_profit_pct - self.trading_fee_rate  # -0.10%
        return net_profit_pct
```

**✅ Комиссия вычитается из прибыли при расчете PnL!**

---

### 6️⃣ ПРИМЕР РАСЧЕТА (ETH-USDT LONG):

#### **Реальная сделка с биржи:**
- **Entry:** $3,270.88
- **Exit:** $3,267.20
- **Size:** 0.670 ETH
- **Leverage:** 5x

**Расчет:**
```
Notional entry: 0.670 × $3,270.88 = $2,191.49
Notional exit:  0.670 × $3,267.20 = $2,189.02

Комиссия вход:  $2,191.49 × 0.0005 = $1.096
Комиссия выход: $2,189.02 × 0.0005 = $1.094
ИТОГО комиссия: $2.19 (0.10% от $2,191.49) ✅

Gross PnL: $2,189.02 - $2,191.49 = -$2.47
Net PnL:   -$2.47 - $2.19 = -$4.66 ✅

Изменение цены: ($3,267.20 - $3,270.88) / $3,270.88 = -0.112%
Net PnL% от цены: -0.112% - 0.10% = -0.212%
Net PnL% от маржи (5x): -0.212% × 5 = -1.06% ✅ (близко к -1.07% биржи)
```

**✅ Расчеты совпадают с данными биржи!**

---

### 7️⃣ `reduceOnly=True`:

**Что делает:**
- Гарантирует, что ордер **только закроет** существующую позицию
- **Не откроет** новую позицию в противоположном направлении

**В коде (`position_manager.py`):**
```python
result = await self.client.place_futures_order(
    symbol=symbol,
    side=close_side,  # "buy" для SHORT, "sell" для LONG
    size=abs(size),
    order_type="market",
    reduce_only=True,  # ✅ Только закрытие!
)
```

**✅ Настроено правильно!**

---

### 8️⃣ ВЫВОД:

1. **✅ Да, это правильно и неизбежно**
   - Это стандартная механика фьючерсной торговли на всех биржах
   - Каждая сделка (открытие/закрытие) - отдельный ордер
   - За каждую сделку платится комиссия

2. **✅ Это особенности автоматической торговли**
   - Бот работает правильно
   - Использует `reduceOnly=True` для правильного закрытия
   - Комиссия учитывается в расчетах

3. **✅ Комиссия обновлена до реальных данных**
   - `trading_fee_rate = 0.0010` (0.10% на круг для taker)
   - Вычитается из прибыли в `get_profit_pct()`
   - Учитывается в loss_cut и других расчетах
   - Проверено по реальным сделкам с биржи ✅

4. **✅ Ничего больше менять не нужно**
   - Код работает правильно
   - Комиссия учитывается корректно
   - Это стандартная практика для всех ботов и бирж

---

## 📚 Дополнительная информация:

- **OKX Maker Fee:** 0.02% (limit orders, post-only)
- **OKX Taker Fee:** 0.05% (market orders)
- **Наш бот:** Использует `trading_fee_rate = 0.0010` (0.10%) для taker ордеров ✅
- **Реальная комиссия:** Может быть 0.10% (если оба ордера taker) или 0.04% (если оба maker)
- **Текущая настройка:** 0.10% (консервативный подход для market ордеров)

**✅ Все правильно и проверено!**
