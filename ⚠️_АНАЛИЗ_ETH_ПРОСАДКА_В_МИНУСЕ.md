# ⚠️ АНАЛИЗ: ETH позиция в минусе -10.21%, но не закрывается

**Дата:** 26.11.2025 21:22  
**Проблема:** ETH позиция в просадке -10.21%, но бот её не закрывает

---

## 📊 СИТУАЦИЯ

### **ETH-USDT позиция:**

- **PnL:** -4.46 USDT (-10.21%)
- **Entry:** 2947.40 USDT (SHORT)
- **Current:** ~2953.45 USDT
- **Margin:** 43.62 USDT
- **Margin Ratio:** 0.88 (unsafe, но не критично)
- **Status:** Позиция открыта, НЕ закрывается

### **Логи показывают:**

```
PnL=$-4.46, margin=$43.62, PnL%=-10.21%
safe=False, margin_ratio=0.88
```

**Но позиция не закрывается!**

---

## 🔍 АНАЛИЗ ЛОГИКИ

### **1. Блокировка TSL (строка 750):**

```python
if should_close_by_sl and profit_pct > 0:
    # Блокировка работает ТОЛЬКО для прибыльных позиций
    should_block_close = False
    # ... проверка индикаторов ...
```

**Вывод:**
- ✅ Блокировка TSL работает **ТОЛЬКО** для прибыльных позиций
- ❌ Для убыточных позиций (`profit_pct <= 0`) блокировка **НЕ** работает
- ✅ Убыточные позиции должны закрываться по TSL **без блокировки**

### **2. Что должно происходить с убыточной позицией:**

**Когда `profit_pct <= 0` и `should_close_by_sl = True`:**
- Позиция должна закрываться **сразу** (без блокировки)
- Нет проверки индикаторов
- Нет ожидания отскока

**Но ETH позиция не закрывается!** 🤔

---

## 🤔 ВОЗМОЖНЫЕ ПРИЧИНЫ

### **Вариант 1: TSL не срабатывает**

**Проблема:** `should_close_by_sl = False`
- TSL стоп-лосс не пробит
- Цена не достигла стоп-уровня

**Проверка:** Нужно посмотреть текущий TSL стоп-лосс для ETH

### **Вариант 2: ExitAnalyzer блокирует закрытие**

**Проблема:** ExitAnalyzer возвращает `hold` или не возвращает решение
- ExitAnalyzer может решить "держать" убыточную позицию
- Но это странно для убыточных позиций

**Проверка:** Нужно проверить, что возвращает ExitAnalyzer для ETH

### **Вариант 3: TSL не инициализирован для ETH**

**Проблема:** TSL не был создан для ETH позиции
- Позиция открыта, но TSL не работает
- `should_close_by_sl` всегда `False`

**Проверка:** Нужно проверить, есть ли TSL для ETH

---

## 🎯 ВЫВОДЫ

### **Правильно ли, что бот не закрывает убыточную позицию?**

**Это зависит от стратегии:**

**✅ Правильно, если:**
- Бот ждёт отскока (позиция SHORT, цена может упасть)
- ExitAnalyzer решил "держать" (индикаторы показывают разворот)
- TSL не пробит (цена не достигла стоп-уровня)

**❌ Неправильно, если:**
- TSL пробит, но бот не закрывает
- ExitAnalyzer не вызывается
- Бот игнорирует убыток

---

## 📝 РЕКОМЕНДАЦИИ

### **1. Проверить TSL стоп-лосс:**

- Какой текущий TSL стоп-лосс для ETH?
- Пробит ли он ценой?

### **2. Проверить ExitAnalyzer:**

- Что возвращает ExitAnalyzer для ETH?
- Решает ли он "держать" или "закрыть"?

### **3. Проверить логику закрытия убыточных позиций:**

- Должен ли бот закрывать убыточные позиции строже?
- Или правильнее ждать отскока?

---

## ⚠️ КРИТИЧЕСКИЙ ВОПРОС

**Должен ли бот закрывать убыточные позиции автоматически?**

**Если ДА:**
- Нужно добавить логику принудительного закрытия при большом убытке
- Например: закрывать при -5% или -10%

**Если НЕТ:**
- Текущее поведение правильное (ждём отскока)
- Но нужно убедиться, что TSL работает

---

## 🔧 ЧТО НУЖНО СДЕЛАТЬ

1. ✅ Проверить, какой TSL стоп-лосс для ETH
2. ✅ Проверить, что возвращает ExitAnalyzer
3. ✅ Решить: правильное ли это поведение?
4. 💡 Возможно, добавить логику принудительного закрытия больших убытков

