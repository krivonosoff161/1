# 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА: ETH TSL не работает!

**Дата:** 26.11.2025 21:27  
**Критичность:** 🔴 ВЫСОКАЯ

---

## 📊 СИТУАЦИЯ

### **ETH-USDT позиция (SHORT):**

- **Entry Price:** 2947.40 USDT
- **Current Price:** ~2953-3003 USDT
- **PnL:** -4.46 USDT (-10.21% от маржи)
- **Margin:** 43.62 USDT
- **Leverage:** 5x
- **Режим:** ranging
- **Время открытия:** 20:40:15
- **Текущее время:** 21:26+ (~46 минут в позиции)

**Позиция НЕ закрывается по TSL!** 🚨

---

## 🔍 ПОЛНЫЙ АНАЛИЗ

### **1. Расчёт loss_cut:**

```
loss_cut_percent = 4.0% от маржи (ranging режим)
loss_cut_from_price = 4.0% / 5 = 0.8% от цены
critical_loss_cut = 4.0% * 2 = 8.0% от маржи = 1.6% от цены

Текущий убыток = -10.21% от маржи = -2.04% от цены
```

**Вывод:**
- ✅ Убыток (-2.04%) **больше** loss_cut (0.8%)
- ✅ Убыток (-2.04%) **больше** critical_loss_cut (1.6%)!
- ✅ Позиция **ДОЛЖНА** закрыться по критическому loss_cut!

### **2. Защита min_holding:**

```
min_holding_minutes = 60 минут (ranging режим)
Время в позиции = 46 минут
```

**Вывод:**
- ❌ Время в позиции (46 мин) **меньше** min_holding (60 мин)
- ⚠️ Обычный loss_cut **заблокирован** защитой min_holding
- ✅ Но критический loss_cut (2x) **обходит** min_holding (только 5 сек защита)

### **3. КРИТИЧЕСКАЯ ПРОБЛЕМА:**

**В логах НЕТ записей о TSL для ETH!**

- ❌ Нет записей об инициализации TSL для ETH
- ❌ Нет записей об обновлении TSL для ETH
- ❌ Нет записей о проверке TSL для ETH
- ❌ Нет записей о loss_cut для ETH

**Это значит:**
- TSL **НЕ инициализирован** для ETH
- TSL **НЕ вызывается** для ETH
- **TSL НЕ РАБОТАЕТ** для ETH!

---

## 🎯 ПРИЧИНА

### **Возможные причины:**

1. **TSL не инициализирован при загрузке существующих позиций:**
   - Позиция открыта до перезапуска бота (20:40:15)
   - При перезапуске TSL должен инициализироваться в `_load_existing_positions()`
   - Но в логах нет записей об инициализации для ETH

2. **TSL не вызывается в periodic_check:**
   - `periodic_check()` должен вызывать `update_trailing_stop_loss()` для всех позиций
   - Но в логах нет записей об обновлении TSL для ETH

3. **TSL инициализируется, но не логируется:**
   - Возможно, логирование не работает
   - Но это маловероятно, т.к. для других позиций логи есть

---

## 🔧 ЧТО НУЖНО ПРОВЕРИТЬ

1. ✅ Вызывается ли `_load_existing_positions()`?
2. ✅ Инициализируется ли TSL для ETH в `_load_existing_positions()`?
3. ✅ Вызывается ли `periodic_check()` для всех позиций?
4. ✅ Вызывается ли `update_trailing_stop_loss()` для ETH?
5. ✅ Есть ли TSL для ETH в `trailing_sl_by_symbol`?

---

## 💡 РЕШЕНИЕ

**Нужно проверить:**
1. Почему TSL не инициализируется для ETH при загрузке существующих позиций
2. Почему TSL не вызывается для ETH в periodic_check

**Это критическая проблема, т.к. позиция в убытке -10.21% и должна закрыться!**

---

## 📝 ВЫВОДЫ

### **Текущее состояние:**

- ❌ TSL не работает для ETH
- ❌ Позиция не закрывается по loss_cut
- ❌ Убыток увеличивается

**Это проблема, которую нужно исправить СРОЧНО!**

