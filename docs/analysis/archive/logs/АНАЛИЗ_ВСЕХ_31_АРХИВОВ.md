# 📊 ДЕТАЛЬНЫЙ АНАЛИЗ ВСЕХ 31 АРХИВОВ И ТЕКУЩЕГО ЛОГА

**Дата анализа:** 2025-11-18  
**Проверено:** 30 зип-архивов + 1 текущий лог-файл

---

## 📈 ОБЩАЯ СТАТИСТИКА

| Метрика | Значение |
|---------|----------|
| **Всего архивов** | 30 |
| **Открытий позиций** | 28 |
| **Закрытий позиций** | 2,360 |
| **Записей о TP** | 3,351 |
| **Инициализаций TSL** | 398 |
| **Ошибок** | 388 |

---

## 🚨 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. **TP НЕ СРАБАТЫВАЕТ** ❌

**Проблема:**
- **3,351 записей** о проверке TP
- **Только 4 закрытия** по TP (0.12% от проверок!)
- **146 закрытий manual** (6.2% от всех закрытий)

**Примеры закрытий по TP (всего 4):**
1. `2025-11-18 14:22:09` - BTC-USDT: +2.02 USDT
2. `2025-11-18 15:41:18` - SOL-USDT: +7.60 USDT
3. `2025-11-18 16:23:33` - SOL-USDT: +1.50 USDT
4. `2025-11-18 16:26:12` - SOL-USDT: +1.27 USDT

**Почему TP не срабатывает:**
- Из логов видно: `PnL=-0.61% < TP=2.00% (с комиссией: 3.15%, нужно еще 3.76%)`
- Позиции закрываются вручную до достижения TP
- `min_holding_minutes` блокирует закрытие (см. ниже)

---

### 2. **TSL НЕ ЗАКРЫВАЕТ ПОЗИЦИИ** ❌

**Проблема:**
- **398 инициализаций** TSL
- **0 закрытий** по TSL (0%!)
- Все закрытия идут через `manual` или `tp`

**Почему TSL не работает:**
- `min_holding_minutes` блокирует закрытие (см. ниже)
- `loss_cut_percent` не срабатывает (0 срабатываний)

---

### 3. **MIN_HOLDING БЛОКИРУЕТ ВСЕ** ❌❌❌

**Проблема:**
- **48,195 блокировок** `min_holding_minutes`!
- Позиции не закрываются даже при убытках, потому что не прошло минимальное время

**Примеры блокировок:**
```
⏱️ Минимальное время удержания: позиция держится 0.32 мин < 2.00 мин, не закрываем
⏱️ Минимальное время удержания: позиция держится 0.83 мин < 2.00 мин, не закрываем (profit=-3.33%)
⏱️ Минимальное время удержания: позиция держится 1.11 мин < 2.00 мин, не закрываем
```

**Последствия:**
- Позиции с убытком -3.33% не закрываются, потому что не прошло 2 минуты
- Позиции с прибылью не закрываются по TP, потому что не прошло 2 минуты
- Все закрывается вручную после истечения `min_holding_minutes`

---

### 4. **LOSS_CUT НЕ СРАБАТЫВАЕТ** ❌

**Проблема:**
- **0 срабатываний** `loss_cut_percent`
- Позиции с убытком -16.16% не закрываются автоматически

**Примеры из логов:**
```
📊 ETH-USDT PnL=-16.16% < TP=1.95% (с комиссией: 3.10%, нужно еще 19.26%)
📊 SOL-USDT PnL=-6.45% < TP=1.90% (с комиссией: 3.05%, нужно еще 9.50%)
```

**Почему не срабатывает:**
- `min_holding_minutes` блокирует закрытие
- `min_critical_hold_seconds` (15 сек) слишком короткий, но `min_holding_minutes` (2 мин) все равно блокирует

---

### 5. **PER-REGIME TP РАБОТАЕТ, НО НЕ ПРИМЕНЯЕТСЯ** ⚠️

**Статистика:**
- **620 записей** Per-regime TP
- **2,731 записей** Per-symbol TP
- Но только 4 закрытия по TP

**Примеры:**
```
📊 Per-regime TP для DOGE-USDT (ranging): 3.25% (глобальный: 2.0%)
📊 Per-symbol TP для SOL-USDT: 1.9% (глобальный: 2.0%)
```

**Вывод:** Параметры читаются из конфига, но не применяются из-за блокировок `min_holding_minutes`.

---

## 📊 РАСПРЕДЕЛЕНИЕ ПРИЧИН ЗАКРЫТИЯ

| Причина | Количество | Процент |
|---------|------------|---------|
| **manual** | 146 | 6.2% |
| **tp** | 4 | 0.2% |
| **trailing_stop** | 0 | 0% |
| **loss_cut** | 0 | 0% |
| **Другие/неизвестно** | 2,210 | 93.6% |

**Вывод:** 93.6% закрытий происходят по неизвестным причинам или вручную.

---

## 🔍 ДЕТАЛЬНЫЙ АНАЛИЗ ПРОБЛЕМ

### Проблема #1: `min_holding_minutes` блокирует все

**Текущая логика:**
1. TSL проверяет `should_close_position()`
2. Если `time_in_position < min_holding_minutes` → **НЕ ЗАКРЫВАЕМ**
3. Даже если `loss_cut_percent` достигнут → **НЕ ЗАКРЫВАЕМ**
4. Даже если TP достигнут → **НЕ ЗАКРЫВАЕМ** (если TSL активен)

**Проблема:**
- `min_holding_minutes = 2.0` для `ranging` режима
- Позиции с убытком -3.33% не закрываются, потому что прошло только 0.83 мин
- Позиции с прибылью не закрываются по TP, потому что прошло только 0.32 мин

**Решение:**
- Убрать блокировку `min_holding_minutes` для `loss_cut_percent` (уже сделано для критических убытков)
- Убрать блокировку `min_holding_minutes` для TP (если TP достигнут, закрываем сразу)
- Оставить `min_holding_minutes` только для обычного трейлинга (не для loss_cut и TP)

---

### Проблема #2: TP не приоритетнее TSL

**Текущая логика:**
1. Если TSL активен → проверяем TSL
2. Если TSL говорит "не закрывать" (из-за `min_holding_minutes`) → **НЕ ПРОВЕРЯЕМ TP**

**Проблема:**
- TP должен иметь приоритет над TSL
- Если TP достигнут → закрываем сразу, независимо от TSL

**Решение:**
- Проверять TP **ДО** TSL
- Если TP достигнут → закрываем сразу, не проверяем TSL

---

### Проблема #3: `loss_cut_percent` не срабатывает

**Текущая логика:**
1. `loss_cut_percent = 0.8%` для `ranging` режима
2. `min_critical_hold_seconds = 15.0` сек
3. Но `min_holding_minutes = 2.0` мин все равно блокирует

**Проблема:**
- Позиция с убытком -3.33% не закрывается, потому что прошло только 0.83 мин < 2.0 мин
- `min_critical_hold_seconds` (15 сек) уже прошло, но `min_holding_minutes` (2 мин) все равно блокирует

**Решение:**
- Убрать проверку `min_holding_minutes` для `loss_cut_percent` после `min_critical_hold_seconds`
- Если `time_in_position >= min_critical_hold_seconds` И `loss_cut_percent` достигнут → закрываем сразу

---

## 💡 РЕКОМЕНДАЦИИ

### 1. **Исправить логику закрытия позиций**

**Приоритет закрытия:**
1. **TP** (если достигнут) → закрываем сразу, независимо от времени
2. **Loss Cut** (если достигнут после `min_critical_hold_seconds`) → закрываем сразу
3. **TSL** (только если прошло `min_holding_minutes`) → обычный трейлинг

**Код:**
```python
# 1. Проверяем TP ПЕРВЫМ (приоритет)
if tp_reached:
    close_position("tp")
    return

# 2. Проверяем Loss Cut (после min_critical_hold_seconds)
if time_in_position >= min_critical_hold_seconds:
    if loss_cut_reached:
        close_position("loss_cut")
        return

# 3. Проверяем TSL (только если прошло min_holding_minutes)
if time_in_position >= min_holding_minutes:
    if tsl_should_close:
        close_position("trailing_stop")
        return
```

---

### 2. **Уменьшить `min_holding_minutes` для `ranging` режима**

**Текущее значение:** `min_holding_minutes = 2.0` мин  
**Рекомендуемое:** `min_holding_minutes = 1.0` мин

**Обоснование:**
- 48,195 блокировок - это слишком много
- Позиции должны закрываться быстрее в `ranging` режиме

---

### 3. **Увеличить частоту проверки TSL**

**Текущее значение:** `check_interval_seconds = 1.0` сек  
**Рекомендуемое:** `check_interval_seconds = 0.5` сек

**Обоснование:**
- Более частая проверка позволит быстрее реагировать на изменения цены

---

### 4. **Добавить логирование причин блокировки**

**Текущее:** Логируется только "не закрываем"  
**Рекомендуемое:** Логировать причину блокировки:
- "TP достигнут, но блокируется min_holding_minutes"
- "Loss Cut достигнут, но блокируется min_holding_minutes"
- "TSL активен, но блокируется min_holding_minutes"

---

## 📋 ИТОГОВЫЕ ВЫВОДЫ

1. **TP работает, но блокируется `min_holding_minutes`** → нужно убрать блокировку для TP
2. **TSL не закрывает позиции** → нужно убрать блокировку `min_holding_minutes` для loss_cut
3. **Loss Cut не срабатывает** → нужно убрать блокировку `min_holding_minutes` для loss_cut после `min_critical_hold_seconds`
4. **Per-regime TP применяется** → параметры читаются из конфига правильно
5. **93.6% закрытий по неизвестным причинам** → нужно добавить логирование всех причин закрытия

---

## 🎯 ПРИОРИТЕТ ИСПРАВЛЕНИЙ

1. **КРИТИЧЕСКОЕ:** Убрать блокировку `min_holding_minutes` для TP
2. **КРИТИЧЕСКОЕ:** Убрать блокировку `min_holding_minutes` для `loss_cut_percent` после `min_critical_hold_seconds`
3. **ВАЖНОЕ:** Изменить приоритет: TP → Loss Cut → TSL
4. **ВАЖНОЕ:** Уменьшить `min_holding_minutes` для `ranging` режима до 1.0 мин
5. **УЛУЧШЕНИЕ:** Добавить детальное логирование причин блокировки

---

**Дата создания:** 2025-11-18  
**Статус:** Требуются критические исправления






