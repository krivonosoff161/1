# ⚠️ КРИТИЧЕСКАЯ ПРОБЛЕМА: БЫСТРЫЕ ЗАКРЫТИЯ ПОЗИЦИЙ

**Дата:** 23 ноября 2025, 01:45  
**Статус:** 🔴 **КРИТИЧЕСКАЯ ПРОБЛЕМА**

---

## 📊 АНАЛИЗ ЗАКРЫТЫХ ПОЗИЦИЙ

### Позиции пользователя:
1. **DOGE-USDT**: Закрыта через **17 секунд** с убытком **-3.00%**
2. **DOGE-USDT**: Закрыта через **4 секунды** с убытком **-0.86%**
3. **DOGE-USDT**: Частично закрыта с прибылью **+0.74%**
4. **ETH-USDT**: Закрыта через **~109 секунд** (1 мин 49 сек) с прибылью **+0.68%**

---

## 🔍 ПРОБЛЕМА

### Ожидаемые параметры для RANGING режима:
- `loss_cut_percent`: **4.0%** (из конфига, строка 1301)
- `min_holding_minutes`: **60.0 минут** (из конфига, строка 1309)
- `timeout_minutes`: **120 минут** (из конфига, строка 1303)

### Реальное поведение:
- Позиции закрываются через **4-17 секунд**
- Убыточные позиции закрываются почти сразу
- `min_holding` (60 минут) **НЕ РАБОТАЕТ**

---

## 🔎 ВОЗМОЖНЫЕ ПРИЧИНЫ

### 1. Используется неправильный режим (CHOPPY вместо RANGING)
**Параметры CHOPPY:**
- `loss_cut_percent`: **1.2%** (строка 1332)
- `min_holding_minutes`: **0.5 минуты** (30 секунд!) (строка 1339)
- `timeout_minutes`: **3 минуты** (строка 1334)

**Если используется CHOPPY:**
- `loss_cut_percent: 1.2%` объясняет закрытие через 4-17 секунд
- `min_holding_minutes: 0.5` (30 секунд) объясняет быстрое закрытие

### 2. Параметры не читаются правильно из конфига
- `get_trailing_sl_params()` может возвращать неправильные значения
- Режим может определяться неправильно при инициализации TSL

### 3. Критический 2x loss_cut обходит min_holding
- Если убыток достигает **2x loss_cut** (например, 2.4% при loss_cut=1.2%), позиция закрывается немедленно
- Это может объяснить закрытие через 4-17 секунд

---

## 🔍 ПРОВЕРКА ЛОГОВ

### Из логов видно:
- ✅ DOGE-USDT определяется как **RANGING** режим (строка 349, 371)
- ✅ Параметры индикаторов для **ranging** (RSI overbought=70, oversold=30)
- ❌ **НЕТ ЛОГОВ** инициализации TSL для DOGE-USDT с параметрами

### Отсутствующие логи:
- ❌ Нет логов `TrailingStopLoss для DOGE-USDT инициализирован: loss_cut=..., min_holding=...`
- ❌ Нет логов `TSL created` в debug CSV

---

## 🎯 ГИПОТЕЗЫ

### Гипотеза 1: Режим определяется неправильно при инициализации TSL
- При генерации сигнала режим = **RANGING**
- При инициализации TSL режим может быть **CHOPPY** или **None**
- Если режим = **None**, используются **глобальные параметры** (loss_cut=1.5%, min_holding=0.5 мин)

### Гипотеза 2: Параметры не передаются правильно
- `get_trailing_sl_params(regime="ranging")` вызывается, но возвращает неправильные значения
- Или режим не передается в `initialize_trailing_stop()`

### Гипотеза 3: Используются глобальные параметры вместо режимных
- Если `regime=None`, используются глобальные параметры:
  - `loss_cut_percent: 1.5%` (строка 1246)
  - `min_holding_minutes: 0.5` (30 секунд!) (строка 1249)

---

## ✅ РЕКОМЕНДАЦИИ

### 1. Проверить передачу режима в `initialize_trailing_stop()`
- Убедиться, что `regime` передается из сигнала
- Добавить логирование режима при инициализации TSL

### 2. Проверить `get_trailing_sl_params(regime="ranging")`
- Убедиться, что метод правильно читает параметры из конфига
- Добавить логирование возвращаемых параметров

### 3. Добавить логирование инициализации TSL
- Логировать все параметры при создании TSL
- Логировать режим, loss_cut, min_holding, timeout

### 4. Проверить критический 2x loss_cut
- Убедиться, что критический loss_cut не обходит min_holding слишком рано

---

## 📋 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Проверить логи инициализации TSL для DOGE-USDT
2. ✅ Проверить передачу режима в `initialize_trailing_stop()`
3. ✅ Проверить `get_trailing_sl_params(regime="ranging")`
4. ✅ Добавить детальное логирование параметров TSL
5. ✅ Исправить проблему с режимом или параметрами

---

**Статус:** 🔴 **ТРЕБУЕТСЯ НЕМЕДЛЕННОЕ ИСПРАВЛЕНИЕ**

