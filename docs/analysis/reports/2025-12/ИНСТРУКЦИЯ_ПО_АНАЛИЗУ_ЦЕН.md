# 📋 ИНСТРУКЦИЯ ПО ЗАПУСКУ АНАЛИЗА ДВИЖЕНИЯ ЦЕНЫ ПОСЛЕ ЗАКРЫТИЯ

## 🚀 Запуск скрипта анализа

Скрипт `scripts/analyze_price_after_close.py` собирает исторические данные о ценах после закрытия позиций и анализирует движение цены.

### Требования

1. Python 3.8+
2. Установленные зависимости: `aiohttp`, `loguru`
3. Доступ к интернету для запросов к OKX API

### Запуск

```bash
# Из корневой директории проекта
python scripts/analyze_price_after_close.py
```

### Что делает скрипт

1. **Загружает позиции** из отчета `ПОЛНЫЙ_АНАЛИЗ_ПОЗИЦИЙ_30_12_2025_ВЕЧЕР.md`
2. **Получает исторические свечи** с OKX API за 15 минут после закрытия каждой позиции
3. **Анализирует движение цены**:
   - Продолжила ли цена идти в сторону позиции (преждевременное закрытие)
   - Развернулась ли цена в правильную сторону (преждевременное закрытие)
   - Продолжила ли цена идти против позиции (правильное закрытие)
4. **Генерирует отчет** с детальным анализом каждой позиции

### Результаты

Скрипт создает два файла:

1. **`АНАЛИЗ_ДВИЖЕНИЯ_ЦЕНЫ_ПОСЛЕ_ЗАКРЫТИЯ_ДЕТАЛЬНЫЙ.md`** - Детальный отчет в формате Markdown
2. **`АНАЛИЗ_ДВИЖЕНИЯ_ЦЕНЫ_ПОСЛЕ_ЗАКРЫТИЯ_ДЕТАЛЬНЫЙ.json`** - Данные в формате JSON для дальнейшего анализа

### Формат результатов

Для каждой позиции скрипт анализирует:

- **Преждевременное закрытие**: Цена развернулась в правильную сторону после закрытия
- **Неправильное направление**: Цена продолжила идти против позиции
- **Максимальная прибыль, если бы держали**: Максимальный PnL, который был бы достигнут
- **Максимальный убыток, если бы держали**: Максимальный убыток, который был бы достигнут
- **Цена через 5/10/15 минут**: Цены в определенные моменты времени после закрытия

### Пример использования

```python
from scripts.analyze_price_after_close import PriceMovementAnalyzer
import asyncio

async def main():
    analyzer = PriceMovementAnalyzer()
    
    # Загружаем позиции из отчета
    report_file = Path("docs/analysis/reports/2025-12/ПОЛНЫЙ_АНАЛИЗ_ПОЗИЦИЙ_30_12_2025_ВЕЧЕР.md")
    positions = analyzer.load_positions_from_report(report_file)
    
    # Анализируем все позиции
    results = await analyzer.analyze_all_positions(positions, minutes_after=15)
    
    # Генерируем отчет
    output_file = analyzer.output_dir / "АНАЛИЗ_ДВИЖЕНИЯ_ЦЕНЫ_ПОСЛЕ_ЗАКРЫТИЯ_ДЕТАЛЬНЫЙ.md"
    analyzer.generate_report(results, output_file)

if __name__ == "__main__":
    asyncio.run(main())
```

### Ограничения OKX API

- Максимум 300 свечей за один запрос
- Rate limiting: рекомендуется делать задержку 0.5 секунды между запросами
- Публичный API (не требует авторизации)

### Устранение проблем

**Проблема:** Скрипт не может получить данные

**Решение:** 
- Проверьте подключение к интернету
- Проверьте, что OKX API доступен
- Убедитесь, что символы указаны правильно (например, "BTC-USDT", а не "BTCUSDT")

**Проблема:** Не все позиции обработаны

**Решение:**
- Проверьте логи - возможно, для некоторых позиций нет исторических данных
- OKX хранит исторические данные ограниченное время (обычно несколько месяцев)

---

## 📊 Анализ результатов

После запуска скрипта проверьте:

1. **Процент преждевременных закрытий** - если > 50%, значит бот закрывает позиции слишком рано
2. **Процент неправильных направлений** - если > 50%, значит бот часто входит против тренда
3. **Максимальная прибыль, если бы держали** - показывает потенциальную прибыль при правильном закрытии


