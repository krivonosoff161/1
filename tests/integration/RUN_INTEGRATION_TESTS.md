# 🧪 Интеграционные тесты для критических проблем

**Дата создания:** 2026-01-05  
**Цель:** Проверить проблемы #2, #3, #4, #5 через тесты

---

## ✅ Созданные тесты:

### 1. Проблема #2: Размер позиции = 0.000027 монет
**Файл:** `test_position_size_calculation.py`  
**Тесты:**
- ✅ `test_risk_manager_returns_coins_not_contracts` - RiskManager возвращает монеты
- ✅ `test_signal_coordinator_converts_coins_to_contracts_correctly` - Правильная конвертация монеты → контракты
- ✅ `test_position_size_notional_calculation` - Правильный расчет notional

**Проверяет:**
- RiskManager возвращает размер в МОНЕТАХ (не контрактах)
- SignalCoordinator правильно конвертирует монеты → контракты
- Notional рассчитывается правильно ($249 вместо $2.49)

---

### 2. Проблема #3: Закрытие в боковике без анализа
**Файл:** `test_exit_analyzer_timeout.py`  
**Тесты:**
- ✅ `test_timeout_checked_after_market_analysis` - Timeout проверяется ПОСЛЕ анализа
- ✅ `test_timeout_allows_extension_in_strong_trend` - Timeout может быть продлен при сильном тренде
- ✅ `test_timeout_closes_in_sideways_market_after_analysis` - В боковике timeout закрывает ПОСЛЕ анализа

**Проверяет:**
- Методы анализа вызываются ПЕРЕД проверкой timeout
- При сильном тренде позиция может остаться открытой даже после timeout
- В боковике анализ выполняется перед закрытием

---

### 3. Проблема #4: Маркерные ордера
**Файл:** `test_marker_orders_delta.py`  
**Тесты:**
- ✅ `test_delta_check_uses_market_order_when_delta_large` - При дельте >1% используется market order
- ✅ `test_delta_check_uses_limit_order_when_delta_small` - При дельте <0.5% используется limit order
- ✅ `test_limit_price_adjusted_when_delta_medium` - При дельте 0.5-1% limit price корректируется
- ✅ `test_limit_order_timeout_replaces_with_market` - После timeout лимитный ордер заменяется на market

**Проверяет:**
- Delta check правильно выбирает между limit и market ордерами
- Limit price корректируется при средней дельте
- Timeout лимитных ордеров работает правильно

---

### 4. Проблема #5: Ошибка timezone
**Файл:** `test_timezone_signal_freshness.py`  
**Тесты:**
- ✅ `test_timezone_import_available` - timezone доступен глобально
- ✅ `test_signal_freshness_check_works` - Проверка устаревания сигналов работает
- ✅ `test_timezone_utc_consistency` - timezone.utc консистентен

**Проверяет:**
- timezone доступен (не локальный import)
- Проверка устаревания сигналов работает правильно
- timezone.utc используется консистентно

---

## 🚀 Запуск тестов:

```bash
# Все интеграционные тесты
pytest tests/integration/test_position_size_calculation.py tests/integration/test_exit_analyzer_timeout.py tests/integration/test_marker_orders_delta.py tests/integration/test_timezone_signal_freshness.py -v

# Только проблема #2
pytest tests/integration/test_position_size_calculation.py -v

# Только проблема #3
pytest tests/integration/test_exit_analyzer_timeout.py -v

# Только проблема #4
pytest tests/integration/test_marker_orders_delta.py -v

# Только проблема #5
pytest tests/integration/test_timezone_signal_freshness.py -v
```

---

## 📊 Результаты:

**Всего тестов:** 13  
**Проходят:** Проверяется...  
**Покрытие:** Проблемы #2, #3, #4, #5

---

## ⚠️ Примечания:

- Тесты используют моки для изоляции проблем
- Для полной проверки можно добавить тесты с реальным подключением к sandbox бирже
- Тесты проверяют логику, а не реальные API вызовы
