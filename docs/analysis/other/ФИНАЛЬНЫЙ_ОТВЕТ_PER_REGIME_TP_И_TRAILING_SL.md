# ФИНАЛЬНЫЙ ОТВЕТ: Per-Regime TP и Trailing Stop Loss

## ✅ РЕАЛИЗОВАНО

### 1. Per-Regime TP (Take Profit) ✅

**Приоритет использования**:
1. **Per-regime TP** (если режим определен) - **ВЫСШИЙ ПРИОРИТЕТ** ✅
2. **Per-symbol TP** (fallback, если режим не определен) ✅
3. **Глобальный TP** (fallback, если ничего не найдено) ✅

**Настройки для всех пар и режимов**:

| Пара | Per-Symbol | Trending | Ranging | Choppy |
|------|------------|----------|---------|--------|
| **BTC-USDT** | 1.0% | **1.1%** | 1.0% | 0.9% |
| **ETH-USDT** | 0.95% | **1.05%** | 0.95% | 0.85% |
| **SOL-USDT** | 0.9% | **1.0%** | 0.9% | 0.8% |
| **DOGE-USDT** | 0.85% | **0.95%** | 0.85% | 0.75% |
| **XRP-USDT** | 0.85% | **0.95%** | 0.85% | 0.75% |

**Логика**:
- **Trending**: Больше TP (больше потенциал для прибыли)
- **Ranging**: Стандартный TP (средний риск)
- **Choppy**: Меньше TP (быстрый выход, меньше риск)

---

## 🔗 TRAILING STOP LOSS

### Как работает:

1. **Независимо от TP**:
   - ✅ Trailing Stop Loss использует проценты от **цены** (не от маржи)
   - ✅ TP использует проценты от **маржи**
   - ✅ Они работают **параллельно**, срабатывает тот, который достигнут первым

2. **Параметры** (одинаковые для всех режимов):
   - `initial_trail`: 0.5% от цены
   - `max_trail`: 1.0% от цены
   - `min_trail`: 0.3% от цены
   - `loss_cut_percent`: -1.5% от маржи (жесткое закрытие)
   - `timeout_loss_percent`: -0.5% от маржи (таймаут)

3. **Адаптивность**:
   - ✅ Trailing Stop Loss **адаптивен** к размеру позиции (использует проценты от цены)
   - ✅ **Не зависит** от TP или режима
   - ✅ Работает одинаково для всех пар и режимов

4. **Per-regime настройки**:
   - ✅ Trailing Stop Loss может иметь per-regime настройки через `trailing_sl` в режимах
   - ✅ Но по умолчанию использует глобальные настройки из `trailing_sl` в конфиге

---

## 📊 ПРИМЕРЫ РАБОТЫ

### Пример 1: BTC-USDT в режиме Trending

**Параметры**:
- Размер позиции: $122.16
- Маржа: $24.43 (5x leverage)
- Режим: **Trending**
- TP: **1.1%** от маржи = $0.2687
- Trailing Stop Loss: 0.5% от цены = $525 (при цене $105,000)

**Сценарий 1**: Цена выросла на 1.1% от маржи → **TP срабатывает** ($0.2687)

**Сценарий 2**: Цена выросла на 0.8%, затем развернулась на 0.5% → **Trailing SL срабатывает**

---

### Пример 2: SOL-USDT в режиме Choppy

**Параметры**:
- Размер позиции: $91.62
- Маржа: $18.32 (5x leverage)
- Режим: **Choppy**
- TP: **0.8%** от маржи = $0.1466 (быстрый выход!)
- Trailing Stop Loss: 0.5% от цены = $525 (при цене $105,000)

**Сценарий 1**: Цена выросла на 0.8% от маржи → **TP срабатывает** ($0.1466) - быстрый выход!

**Сценарий 2**: Цена выросла на 0.5%, затем развернулась на 0.5% → **Trailing SL срабатывает**

---

## 🔍 КАК ЭТО РАБОТАЕТ В КОДЕ

### 1. Сохранение режима при открытии позиции:

```python
# orchestrator.py, строки 1984-2004
regime = signal.get("regime")
if not regime and hasattr(self.signal_generator, "regime_managers"):
    manager = self.signal_generator.regime_managers.get(symbol)
    if manager:
        regime = manager.get_current_regime()

self.active_positions[symbol].update({
    "regime": regime,  # ✅ Сохраняем режим для per-regime TP
    # ... другие параметры
})
```

### 2. Получение per-regime TP при проверке:

```python
# position_manager.py, строки 431-483
tp_percent = self.scalping_config.tp_percent  # Глобальный (fallback)

# Получаем режим из позиции
regime = position.get("regime") or self.active_positions.get(symbol, {}).get("regime")

# 1. ПРИОРИТЕТ 1: Per-regime TP (если режим определен)
if regime:
    regime_lower = regime.lower()
    regime_profile = symbol_dict.get(regime_lower, {})
    regime_tp_percent = regime_profile.get("tp_percent")
    if regime_tp_percent is not None:
        tp_percent = float(regime_tp_percent)  # ✅ Используем per-regime TP

# 2. ПРИОРИТЕТ 2: Per-symbol TP (fallback)
if tp_percent == self.scalping_config.tp_percent:
    symbol_tp_percent = symbol_dict.get("tp_percent")
    if symbol_tp_percent is not None:
        tp_percent = float(symbol_tp_percent)  # ✅ Используем per-symbol TP
```

### 3. Trailing Stop Loss (независимо от TP):

```python
# trailing_stop_loss.py, строки 145-222
def update(self, current_price: float):
    # Обновляет трейлинг на основе цены (не зависит от TP!)
    stop_loss = highest_price * (1 - self.current_trail)  # Процент от цены
    return stop_loss
```

---

## ✅ ВЫВОДЫ

### 1. TP (Take Profit):
- ✅ **Per-regime TP** работает для всех пар и режимов
- ✅ **Адаптивен** к размеру позиции (процент от маржи)
- ✅ **Разные TP** для разных режимов (trending > ranging > choppy)
- ✅ **Приоритет**: Per-regime → Per-symbol → Глобальный

### 2. Trailing Stop Loss:
- ✅ **Независим** от TP (использует проценты от цены)
- ✅ **Адаптивен** к размеру позиции (процент от цены)
- ✅ **Работает одинаково** для всех пар и режимов (или через per-regime настройки)

### 3. Взаимодействие:
- ✅ Оба механизма работают **параллельно**
- ✅ Срабатывает тот, который достигнут **первым**
- ✅ Trailing Stop Loss **не зависит** от TP или режима (использует проценты от цены)

---

## 📋 ТАБЛИЦА TP ДЛЯ ВСЕХ ПАР И РЕЖИМОВ

| Пара | Режим | TP | Абсолютная прибыль (для баланса $1018) |
|------|-------|----|-----|
| BTC-USDT | Trending | 1.1% | $0.2687 |
| BTC-USDT | Ranging | 1.0% | $0.2443 |
| BTC-USDT | Choppy | 0.9% | $0.2199 |
| ETH-USDT | Trending | 1.05% | $0.2138 |
| ETH-USDT | Ranging | 0.95% | $0.1934 |
| ETH-USDT | Choppy | 0.85% | $0.1731 |
| SOL-USDT | Trending | 1.0% | $0.1832 |
| SOL-USDT | Ranging | 0.9% | $0.1649 |
| SOL-USDT | Choppy | 0.8% | $0.1466 |
| DOGE-USDT | Trending | 0.95% | $0.1548 |
| DOGE-USDT | Ranging | 0.85% | $0.1385 |
| DOGE-USDT | Choppy | 0.75% | $0.1222 |
| XRP-USDT | Trending | 0.95% | $0.1548 |
| XRP-USDT | Ranging | 0.85% | $0.1385 |
| XRP-USDT | Choppy | 0.75% | $0.1222 |

---

## 🎯 РЕЗУЛЬТАТ

1. ✅ **TP адаптивен** к режиму рынка (trending > ranging > choppy)
2. ✅ **TP адаптивен** к размеру позиции (процент от маржи)
3. ✅ **Trailing Stop Loss** работает независимо от TP (процент от цены)
4. ✅ **Оба механизма** работают параллельно и адаптивно
5. ✅ **Режим сохраняется** при открытии позиции и используется для per-regime TP

**Итог**: Система полностью адаптивна и учитывает как размер позиции, так и режим рынка! ✅

---

## 📝 ОТВЕТЫ НА ВОПРОСЫ

### Вопрос 1: "Трейлинг стоп как тогда себя поведет?"

**Ответ**: Trailing Stop Loss работает **независимо** от TP:
- Использует проценты от **цены** (не от маржи)
- Работает одинаково для всех пар и режимов
- Может иметь per-regime настройки через `trailing_sl` в режимах
- Срабатывает **параллельно** с TP (тот, который достигнут первым)

### Вопрос 2: "Как ты реализовал для всех разный TP?"

**Ответ**: Реализовано с **приоритетами**:
1. **Per-regime TP** (высший приоритет) - если режим определен
2. **Per-symbol TP** (fallback) - если режим не определен
3. **Глобальный TP** (fallback) - если ничего не найдено

### Вопрос 3: "По идее теперь под каждый режим свое TP должно быть?"

**Ответ**: ✅ **ДА!** Теперь для каждого режима есть свой TP:
- **Trending**: Больше TP (больше потенциал)
- **Ranging**: Стандартный TP (средний риск)
- **Choppy**: Меньше TP (быстрый выход)

**Реализовано для всех 5 пар и всех 3 режимов!** ✅

