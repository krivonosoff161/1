# АНАЛИЗ ЛОГИКИ БОТА И ПОТОКА ДАННЫХ

**Дата анализа:** 2025-12-21  
**Цель:** Проверка работы свечей, анализа закрытия/продления позиций, источников данных и адаптивности

---

## 1. ОБНОВЛЕНИЕ СВЕЧЕЙ - КАК РАБОТАЕТ?

### ✅ ОТВЕТ: Свечи обновляются КАЖДЫЙ ТИК через WebSocket

**Как это работает:**

1. **WebSocket тикер приходит каждый тик** (каждое изменение цены)
2. При каждом тике вызывается `_update_candle_from_ticker()` в `websocket_coordinator.py`
3. **Обновление для всех таймфреймов (1m, 5m, 1H, 1D):**
   - Если текущая минута/5 минут/час/день НЕ изменилась → **обновляется последняя свеча** (high, low, close)
   - Если время изменилось (например, прошла минута) → **создается НОВАЯ свеча**

**Код:**
```322:438:src/strategies/scalping/futures/coordinators/websocket_coordinator.py
async def _update_candle_from_ticker(
    self, symbol: str, price: float, ticker: Dict[str, Any]
) -> None:
    """
    ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Обновить свечи для всех таймфреймов (1m, 5m, 1H, 1D) на основе тикера.

    Определяет, нужно ли обновить последнюю свечу или создать новую для каждого таймфрейма:
    - 1m: Если минута не изменилась → обновляем, если изменилась → новая свеча
    - 5m: Если 5 минут не прошло → обновляем, если прошло → новая свеча
    - 1H: Если час не прошел → обновляем, если прошел → новая свеча
    - 1D: Если день не прошел → обновляем, если прошел → новая свеча
    """
    # ... КРИТИЧЕСКОЕ: Обновляем свечи для всех таймфреймов
    await self._update_candle_for_timeframe(
        symbol, "1m", price, current_timestamp, volume_ccy_24h
    )
    await self._update_candle_for_timeframe(
        symbol, "5m", price, current_timestamp, volume_ccy_24h
    )
    await self._update_candle_for_timeframe(
        symbol, "1H", price, current_timestamp, volume_ccy_24h
    )
    await self._update_candle_for_timeframe(
        symbol, "1D", price, current_timestamp, volume_ccy_24h
    )
```

**Важно:** Свечи НЕ грузятся заново каждый раз - они обновляются в реальном времени через WebSocket. Это эффективно и не создает лишних запросов к API.

---

## 2. ЧТО ПРОИСХОДИТ ПРИ ДОСТИЖЕНИИ 28 МИНУТ (max_holding)?

### ✅ ОТВЕТ: Зависит от режима и прибыли

**Для режима RANGING (ваш пример с SOL-USDT):**

Логика в `exit_analyzer.py`, метод `_generate_exit_for_ranging()`:

1. **Если позиция в УБЫТКЕ (< 0%)** при достижении max_holding:
   - ❌ **НЕ закрывается по времени**
   - Ждет либо SL, либо восстановления
   - Только при убытке >= 1.5 × SL + спред может быть "умное закрытие" после анализа

2. **Если позиция в ПРИБЫЛИ, но прибыль < min_profit_threshold (обычно 0.3%):**
   - ❌ **НЕ закрывается по времени** (после комиссий будет убыток!)

3. **Если позиция в ПРИБЫЛИ >= min_profit_threshold:**
   - ✅ **ЗАКРЫВАЕТСЯ по времени** (max_holding превышен)

4. **Если включено продление времени (`extend_time_if_profitable`):**
   - Если прибыль >= `min_profit_for_extension` (например, 0.5%)
   - Время продлевается на `extension_percent`% (например, 100% = удваивается)
   - Например: 28 мин → 56 мин (если прибыль >= 0.5%)

**Код логики:**
```1944:2030:src/strategies/scalping/futures/positions/exit_analyzer.py
if (
    minutes_in_position is not None
    and isinstance(minutes_in_position, (int, float))
    and float(minutes_in_position) >= actual_max_holding_float
):
    # ✅ ИСПРАВЛЕНО: Не закрываем убыточные позиции по max_holding
    if pnl_percent < 0:
        # Убыток - не закрываем, ждем SL или восстановления
        return {
            "action": "hold",
            "reason": "max_holding_exceeded_but_loss",
            ...
        }
    
    # Проверяем min_profit_to_close перед закрытием по времени
    if pnl_percent < min_profit_threshold:
        # Прибыль меньше min_profit_to_close - НЕ закрываем по времени
        return {
            "action": "hold",
            "reason": "max_holding_low_profit",
            ...
        }
    
    # Время превышено и позиция в прибыли >= min_profit_to_close - закрываем
    logger.info(
        f"⏰ ExitAnalyzer RANGING: Время {minutes_in_position:.1f} мин >= {actual_max_holding:.1f} мин "
        f"(базовое: {max_holding_minutes:.1f} мин), прибыль={pnl_percent:.2f}% >= {min_profit_threshold:.2%} - закрываем по времени"
    )
    return {
        "action": "close",
        "reason": "max_holding_exceeded",
        ...
    }
```

**Ваш случай (SOL-USDT, 7.0 мин / 28.0 мин, PnL=-0.56%):**
- Позиция в убытке (-0.56%)
- Время НЕ превышено (7 мин < 28 мин)
- Пока нет причин для закрытия
- **При достижении 28 минут:** Если PnL все еще < 0%, позиция НЕ закроется по времени, будет ждать SL или восстановления

---

## 3. КАК ПРОХОДИТ АНАЛИЗ ДЛЯ ЗАКРЫТИЯ/ПРОДЛЕНИЯ/УВЕЛИЧЕНИЯ ПОЗИЦИИ?

### ✅ ОТВЕТ: Структурированный анализ через ExitAnalyzer

**Процесс анализа:**

1. **Периодическая проверка позиций:**
   - Вызывается через `position_manager.manage_position()`
   - Проверяется каждую секунду через WebSocket тикер

2. **ExitAnalyzer анализирует позицию:**
   - Получает режим рынка (trending/ranging/choppy)
   - Анализирует текущий PnL%, время в позиции
   - Проверяет индикаторы (ADX, объем, тренд)
   - Принимает решение: `close` / `extend_tp` / `hold` / `None`

3. **Решения по закрытию:**
   - TP достигнут → `action: "close", reason: "tp_reached"`
   - SL достигнут → `action: "close", reason: "sl_reached"`
   - Max holding превышен + прибыль >= min_profit → `action: "close", reason: "max_holding_exceeded"`
   - Big profit exit (большая прибыль + проседание) → `action: "close", reason: "big_profit_exit"`
   - Partial TP достигнут → частичное закрытие

4. **Решения по продлению (только для TRENDING):**
   - Если сильный тренд (strength >= 0.7) + прибыль > 0.3% + время >= max_holding
   - → `action: "extend_tp", reason: "max_holding_strong_trend"`

5. **Увеличение позиции:**
   - ❌ **НЕТ автономного увеличения размера открытой позиции**
   - Новый сигнал в том же направлении → блокируется (чтобы не накапливать комиссию)
   - Увеличение возможно только через новый сигнал (но это блокируется)

**Код анализа:**
```154:240:src/strategies/scalping/futures/positions/exit_analyzer.py
async def _analyze_position_impl(
    self, symbol: str, analysis_start: float
) -> Optional[Dict[str, Any]]:
    # Получаем режим из разных источников
    regime = None
    # 1. Пробуем из signal_generator.regime_manager
    # 2. Пробуем из DataRegistry
    # 3. Пробуем из metadata
    # 4. Fallback: "ranging"
    
    # В зависимости от режима вызываем соответствующий метод
    if regime == "trending":
        return await self._generate_exit_for_trending(...)
    elif regime == "ranging":
        return await self._generate_exit_for_ranging(...)
    elif regime == "choppy":
        return await self._generate_exit_for_choppy(...)
```

---

## 4. ПРАВИЛЬНО ЛИ ДАННЫЕ СЧИТЫВАЮТСЯ ПО WEBSOCKET?

### ✅ ОТВЕТ: Да, ВСЕ данные идут через WebSocket с fallback на API

**WebSocket используется для:**

1. **Рыночные данные (тикеры):**
   - Цены в реальном времени
   - Обновление свечей (1m, 5m, 1H, 1D)
   - Объемы

2. **Позиции и ордера:**
   - Обновления позиций через Private WebSocket
   - Статусы ордеров
   - Закрытие позиций

3. **Баланс и маржа:**
   - Обновления через Private WebSocket

**REST API используется только как FALLBACK:**

- Если WebSocket не отвечает → запрос через REST API
- При инициализации (загрузка существующих позиций, начальные свечи)
- Для размещения ордеров (требуется REST API)

**Код WebSocket обработки:**
```177:320:src/strategies/scalping/futures/coordinators/websocket_coordinator.py
async def handle_ticker_data(self, symbol: str, data: dict):
    """Обработка тикера из WebSocket"""
    # Извлекаем цену
    price = float(ticker.get("last", 0))
    
    # Обновляем свечи для всех таймфреймов
    await self._update_candle_from_ticker(symbol, price, ticker)
    
    # Обновляем DataRegistry
    await self.data_registry.update_market_data(symbol, {...})
    
    # Проверяем позиции или сигналы
    if symbol in self.active_positions_ref:
        await self.position_manager.manage_position(...)
        await self.trailing_sl_coordinator.update_trailing_stop_loss(...)
    else:
        await self.check_signals_callback(symbol, price)
```

**Вывод:** ✅ Все правильно - WebSocket основной источник данных, API только для операций и fallback.

---

## 5. ЕСТЬ ЛИ У БОТА СВОБОДА ПРИНЯТИЯ РЕШЕНИЙ ИЛИ ВСЕ ИЗ АНАЛИЗА?

### ✅ ОТВЕТ: Бот НЕ додумывает данные - ВСЕ из анализа и параметров конфига

**Источники решений:**

1. **Все решения основаны на АНАЛИЗЕ:**
   - Индикаторы (ADX, RSI, MA, Bollinger Bands, MACD)
   - Режим рынка (trending/ranging/choppy) - определяется алгоритмически
   - Сила сигнала (strength) - рассчитывается из индикаторов
   - Волатильность - рассчитывается из свечей

2. **Все параметры из КОНФИГА (адаптивные вилки):**
   - TP/SL проценты - из конфига по режиму
   - Max holding - из конфига по режиму
   - Размер позиции - рассчитывается из баланса и параметров конфига
   - Leverage - адаптивный из `AdaptiveLeverage` (основан на силе сигнала, режиме, волатильности)

3. **НЕТ автономных "додумываний":**
   - Бот не создает случайные значения
   - Бот не игнорирует параметры конфига
   - Все решения логируются с причинами

**Пример генерации сигнала:**
```1395:1409:src/strategies/scalping/futures/signal_generator.py
async def _generate_symbol_signals(
    self,
    symbol: str,
    market_data: Optional[MarketData] = None,
    current_positions: Dict = None,
) -> List[Dict[str, Any]]:
    # Получение рыночных данных (если не переданы)
    if not market_data:
        market_data = await self._get_market_data(symbol)  # Из DataRegistry (WebSocket)
    
    # Генерация базовых сигналов (на основе индикаторов)
    base_signals = await self._generate_base_signals(symbol, market_data)
    
    # Применение фильтров (корреляция, риск, и т.д.)
    filtered_signals = await self._apply_filters(
        symbol, base_signals, market_data, current_positions=current_positions
    )
    
    return filtered_signals
```

**Вывод:** ✅ Все правильно - бот НЕ додумывает, все из анализа и конфига.

---

## 6. РАБОТАЕТ ЛИ АДАПТИВНОСТЬ (ВИЛКИ ПАРАМЕТРОВ) ВЕЗДЕ?

### ✅ ОТВЕТ: Да, адаптивность работает везде через систему ARM (Adaptive Regime Management)

**Адаптивные параметры применяются:**

1. **По РЕЖИМУ рынка:**
   - `trending` → другие TP/SL/max_holding чем `ranging`
   - `choppy` → другие параметры
   - Режим определяется алгоритмически из индикаторов

2. **По БАЛАНСУ (Balance Profile):**
   - `small` / `medium` / `large` профили
   - Разные размеры позиций, максимальное количество позиций

3. **По СИЛЕ СИГНАЛА:**
   - Адаптивный леверидж: сильный сигнал → больший леверидж
   - Через `AdaptiveLeverage.calculate_leverage(signal, regime, volatility)`

4. **По ВОЛАТИЛЬНОСТИ:**
   - Учитывается в расчете левериджа
   - Учитывается в расчете размера позиции

5. **Продление времени (`extend_time_if_profitable`):**
   - Если позиция прибыльная + параметры разрешают
   - Время продлевается на `extension_percent`%

**Пример адаптивного левериджа:**
```src/strategies/scalping/futures/coordinators/signal_coordinator.py
if self.adaptive_leverage:
    leverage_config = self.adaptive_leverage.calculate_leverage(
        signal, regime, volatility
    )
    logger.info(
        f"📊 Адаптивный леверидж для {symbol}: {leverage_config}x "
        f"(сила сигнала={signal.get('strength', 0.5):.2f}, режим={regime}, "
        f"волатильность={volatility*100 if volatility else 'N/A':.2f}%)"
    )
```

**Пример адаптивных параметров по режиму:**
```972:1000:src/strategies/scalping/futures/config/config_manager.py
def get_regime_params(
    self, regime_name: str, symbol: Optional[str] = None
) -> Dict[str, Any]:
    """Получает параметры текущего режима из ARM"""
    adaptive_regime = getattr(scalping_config, "adaptive_regime", {})
    regime_params = adaptive_dict.get(regime_name, {})
    
    # Если есть символьный профиль - перезаписываем
    if symbol:
        symbol_profile = self.symbol_profiles.get(symbol, {})
        regime_profile = symbol_profile.get(regime_name.lower(), {})
        arm_override = regime_profile.get("arm", {})
        if arm_override:
            regime_params = {...regime_params, ...arm_override}  # Merge
    
    return regime_params
```

**Вывод:** ✅ Адаптивность работает везде - параметры адаптируются по режиму, балансу, силе сигнала, волатильности.

---

## ИТОГОВЫЕ ВЫВОДЫ

### ✅ Что работает правильно:

1. **Свечи обновляются каждый тик через WebSocket** - эффективно, без лишних запросов
2. **Данные идут через WebSocket** с fallback на REST API
3. **Все решения на основе анализа** - нет "додумываний"
4. **Адаптивность работает везде** - параметры меняются по режиму, балансу, сигналу
5. **Логика закрытия по max_holding корректна** - учитывает прибыль/убыток, не закрывает убыточные позиции по времени

### ⚠️ Что нужно уточнить:

1. **Строка лога "7.0 мин / 28.0 мин":**
   - Это информационная строка, показывающая текущее время и максимальное время
   - При достижении 28 минут будет проверка прибыли и принятие решения

2. **Увеличение размера позиции:**
   - Сейчас НЕ реализовано автономное увеличение
   - Новые сигналы в том же направлении блокируются
   - Это сделано намеренно, чтобы не накапливать комиссию

3. **Продление времени:**
   - В режиме RANGING продление НЕ применяется (более консервативный подход)
   - В режиме TRENDING продление работает при сильном тренде

---

## РЕКОМЕНДАЦИИ

1. **Для лучшего понимания логов:**
   - Строка "7.0 мин / 28.0 мин" означает: "текущее время / максимальное время"
   - При достижении максимума будет решение на основе прибыли

2. **Мониторинг адаптивности:**
   - Проверяйте логи с "Адаптивный леверидж" - там видно какие параметры используются
   - Проверяйте логи режима - видно какой режим активен

3. **Проверка работы свечей:**
   - В логах должны быть сообщения "Создана новая свеча" при переходе на новый таймфрейм
   - Если таких сообщений нет каждую минуту для 1m - проверьте WebSocket соединение

