# 📋 ОТЧЕТ: Захардоженные параметры в Futures боте

**Дата проверки:** 2025-11-03  
**Статус:** ✅ Проверено, найдены проблемы

---

## 🚨 КРИТИЧЕСКИЕ ПРОБЛЕМЫ (требуют исправления)

### 1. ❌ `_get_regime_params` не получает параметры режима
**Файл:** `src/strategies/scalping/futures/orchestrator.py:1130`  
**Проблема:**
```python
def _get_regime_params(self, regime_name: str) -> dict:
    adaptive_regime = getattr(self.config, "adaptive_regime", {})  # ❌ Неправильный путь!
```
**Должно быть:**
```python
adaptive_regime = getattr(self.scalping_config, "adaptive_regime", {})
```
**Влияние:** Бот НЕ использует параметры режимов (trending/ranging/choppy) из конфига! Всегда возвращается `{}`!

---

### 2. ⚠️ Периоды индикаторов при инициализации - хардкод
**Файл:** `src/strategies/scalping/futures/signal_generator.py:59-75`  
**Проблема:**
```python
RSI(period=14, overbought=70, oversold=30)  # period=14 хардкод
MACD(fast_period=12, slow_period=26, signal_period=9)  # хардкод
ATR(period=14)  # хардкод
BollingerBands(period=20, std_multiplier=2.0)  # period=20 хардкод
EMA_12(period=12)  # хардкод
EMA_26(period=26)  # хардкод
```
**Влияние:** Периоды индикаторов не адаптируются под режим. В конфиге есть режим-специфичные периоды (sma_fast=8/10/12, sma_slow=21/25/30), но они не используются при инициализации индикаторов.

**Примечание:** Пороги (overbought/oversold) берутся из конфига через `_get_regime_indicators_params`, но периоды - нет.

---

## ⚠️ АДАПТИВНЫЕ МНОЖИТЕЛИ (можно вынести в конфиг, но это нормально)

### 3. Множители размера позиции по силе сигнала
**Файл:** `src/strategies/scalping/futures/orchestrator.py:956-979`  
**Хардкод:**
```python
if signal_strength > 0.8:
    strength_multiplier = 1.5  # +50%
elif signal_strength > 0.6:
    strength_multiplier = 1.2  # +20%
elif signal_strength > 0.4:
    strength_multiplier = 1.0  # Стандарт
else:
    strength_multiplier = 0.8  # -20%
```
**Статус:** ✅ Допустимо (адаптивная логика), но можно вынести в конфиг для настройки.

---

### 4. Формула адаптации TP/SL под силу сигнала
**Файл:** `src/strategies/scalping/futures/order_executor.py:424-425`  
**Хардкод:**
```python
tp_multiplier *= 0.5 + strength  # 0.5x-1.5x range
sl_multiplier *= 0.5 + strength
```
**Статус:** ✅ Допустимо (адаптивная формула), но можно вынести в конфиг.

---

### 5. TrailingStopLoss параметры при инициализации
**Файл:** `src/strategies/scalping/futures/orchestrator.py:899-902`  
**Хардкод:**
```python
tsl = TrailingStopLoss(
    initial_trail=0.05,  # ❌ Хардкод, но есть в конфиге!
    max_trail=0.2,  # ❌ Хардкод, но есть в конфиге!
    min_trail=0.02,  # ❌ Хардкод, но есть в конфиге!
    trading_fee_rate=trading_fee_rate,  # ✅ Из конфига
)
```
**Проблема:** Параметры есть в `config.futures_modules.trailing_sl`, но используются хардкод значения!  
**Решение:** Брать из конфига (там есть `initial_trail`, `max_trail`, `min_trail`).

---

### 6. TrailingStopLoss regime_multiplier
**Файл:** `src/strategies/scalping/futures/indicators/trailing_stop_loss.py:253-262`  
**Хардкод:**
```python
if market_regime == "trending":
    regime_multiplier = 1.5
elif market_regime == "ranging":
    regime_multiplier = 1.0
elif market_regime == "choppy":
    regime_multiplier = 0.8
```
**Статус:** ✅ Допустимо (адаптивная логика), но можно вынести в конфиг.

---

## ✅ FALLBACK ЗНАЧЕНИЯ (допустимо, только при ошибках)

### 7. Fallback цены для BTC/ETH
**Файл:** `src/strategies/scalping/futures/order_executor.py:395-400, 467-473`  
**Хардкод:**
```python
if "BTC" in symbol:
    entry_price = 110000.0  # Fallback при ошибке API
elif "ETH" in symbol:
    entry_price = 3900.0
else:
    entry_price = 50000.0
```
**Статус:** ✅ Допустимо - используется только если API не отвечает.

---

### 8. Fallback TP/SL при ошибке
**Файл:** `src/strategies/scalping/futures/order_executor.py:404`  
**Хардкод:**
```python
return entry_price * 1.003, entry_price * 0.998  # +0.3%, -0.2%
```
**Статус:** ✅ Допустимо - используется только при критической ошибке.

---

### 9. Fallback ATR при ошибке
**Файл:** `src/strategies/scalping/futures/order_executor.py:533`  
**Хардкод:**
```python
fallback_atr = price * 0.01  # 1% от цены
```
**Статус:** ✅ Допустимо - используется только если не удалось рассчитать ATR.

---

## 📊 КОНСТАНТЫ В АДАПТИВНЫХ МОДУЛЯХ (можно вынести в конфиг)

### 10. Нормализация min_score_threshold
**Файл:** `src/strategies/modules/adaptive_regime_manager.py:531`  
**Хардкод:**
```python
min_strength = regime_params.min_score_threshold / 12.0  # 12 - константа нормализации
```
**Статус:** ⚠️ Константа 12.0 - это максимальный возможный score (сумма всех индикаторов). Можно сделать вычисляемым, но обычно это фиксированное значение.

---

### 11. Confidence порог для CHOPPY режима
**Файл:** `src/strategies/modules/adaptive_regime_manager.py:544`  
**Хардкод:**
```python
if confidence < 0.7:  # 70% уверенности для choppy
```
**Статус:** ⚠️ Можно вынести в конфиг режима.

---

### 12. Формулы расчета confidence
**Файл:** `src/strategies/modules/adaptive_regime_manager.py:370, 398`  
**Хардкод:**
```python
# CHOPPY:
confidence = min(1.0, (vol / 0.1) * 0.4 + (reversals / 20) * 0.3 + ...)

# TRENDING:
confidence = min(1.0, (trend_dev / 5.0) * 0.3 + (adx / 50.0) * 0.3 + ...)
```
**Статус:** ⚠️ Можно вынести веса (0.4, 0.3, 0.2, 5.0, 50.0) в конфиг, но это математические константы формулы.

---

### 13. Порог confidence для CHOPPY режима (защита)
**Файл:** `src/strategies/modules/adaptive_regime_manager.py:496`  
**Хардкод:**
```python
if detection.regime != RegimeType.CHOPPY or detection.confidence < 0.8:
```
**Статус:** ⚠️ Можно вынести в конфиг.

---

## 📝 ИТОГОВАЯ СВОДКА

### 🚨 Критические (блокируют работу):
1. ❌ `_get_regime_params` не получает параметры → **ИСПРАВИТЬ!**
2. ⚠️ Периоды индикаторов не из конфига → **МОЖНО ИСПРАВИТЬ**

### ⚠️ Важные (работают, но не оптимально):
3. TrailingStopLoss параметры не из конфига → **МОЖНО ИСПРАВИТЬ**

### ✅ Допустимые (адаптивная логика или fallback):
4. Множители по силе сигнала → **Оставить как есть или вынести в конфиг**
5. Формула TP/SL адаптации → **Оставить как есть или вынести в конфиг**
6. Regime multiplier в TSL → **Оставить как есть или вынести в конфиг**
7-9. Fallback значения → **Оставить как есть**
10-13. Математические константы → **Оставить как есть или вынести в конфиг**

---

## 🔧 РЕКОМЕНДАЦИИ ПО ИСПРАВЛЕНИЮ

### Приоритет 1 (КРИТИЧНО):
1. ✅ Исправить `_get_regime_params` - использовать `self.scalping_config.adaptive_regime`
2. ✅ Использовать параметры TrailingStopLoss из конфига

### Приоритет 2 (ЖЕЛАТЕЛЬНО):
3. ✅ Использовать периоды индикаторов из конфига режима (сложно, т.к. индикаторы инициализируются один раз)

### Приоритет 3 (ОПЦИОНАЛЬНО):
4. Вынести адаптивные множители в конфиг
5. Вынести confidence пороги в конфиг

---

## ✅ ВЫВОД

**Текущее состояние:** Есть **1 критическая** проблема - параметры режимов не используются! Это означает, что бот торгует с дефолтными значениями, а не с адаптивными параметрами из конфига.

**После исправления:** Все основные параметры будут браться из конфига для каждого режима (trending/ranging/choppy).

