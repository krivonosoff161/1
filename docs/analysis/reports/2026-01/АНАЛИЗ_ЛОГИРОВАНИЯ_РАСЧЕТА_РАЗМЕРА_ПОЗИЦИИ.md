# 📊 АНАЛИЗ ЛОГИРОВАНИЯ РАСЧЕТА РАЗМЕРА ПОЗИЦИИ И АДАПТИВНОЙ СИСТЕМЫ РИСКОВ

**Дата анализа:** 03.01.2026  
**Архив логов:** `logs/futures/archived/logs_2026-01-03_13-12-04`

---

## 🔍 ЧТО ПРОВЕРЯЛОСЬ

Проверка логирования адаптивной системы рисков и расчета маржи перед открытием позиции:
1. Прогрессивная адаптация баланса (balance profile)
2. Адаптивные параметры риска (adaptive risk params)
3. Расчет размера позиции (position size calculation)
4. Расчет маржи (margin calculation)

---

## ✅ ЧТО ЛОГИРУЕТСЯ СЕЙЧАС

### 1. Прогрессивная адаптация баланса ✅ ЛОГИРУЕТСЯ (DEBUG)

**Файл:** `src/strategies/scalping/futures/config/config_manager.py` (строка 999)

**Формат:**
```
📊 Прогрессивная адаптация для small: баланс $744.38 → размер $124.44 
(min_balance=$500.00, threshold=$1500.00, size_at_min=$100.00, size_at_max=$200.00)
```

**Статус:** ✅ **РАБОТАЕТ** - логируется на уровне DEBUG

**Примеры из логов:**
```
2026-01-03 12:35:17.573 | DEBUG | 📊 Прогрессивная адаптация для small: баланс $744.38 → размер $124.44 
(min_balance=$500.00, threshold=$1500.00, size_at_min=$100.00, size_at_max=$200.00)
```

---

### 2. Адаптивные параметры риска ✅ ЛОГИРУЕТСЯ (DEBUG)

**Файл:** `src/strategies/scalping/futures/config/config_manager.py` (строка 1607)

**Формат:**
```
📊 Адаптивные параметры риска: режим=ranging, профиль=small, max_loss=1.0%, max_margin=90.0%
```

**Статус:** ✅ **РАБОТАЕТ** - логируется на уровне DEBUG

**Примеры из логов:**
```
2026-01-03 12:35:17.573 | DEBUG | 📊 Адаптивные параметры риска: режим=ranging, профиль=small, max_loss=1.0%, max_margin=90.0%
```

---

### 3. Детальный расчет размера позиции ❓ НЕ НАЙДЕНО В ЛОГАХ

**Файл:** `src/strategies/scalping/futures/risk_manager.py` (строки 1195-1235)

**Формат (код):**
```python
logger.info(f"📊 ДЕТАЛЬНЫЙ РАСЧЕТ РАЗМЕРА ПОЗИЦИИ для {symbol}:")
logger.info(f"  1. Балансовый профиль: {balance_profile['name']}, баланс=${balance:.2f}")
logger.info(f"  2. Базовый размер из конфига: base_usd_size=${base_usd_size:.2f} (notional)")
logger.info(f"  3. Лимиты из конфига: min=${min_usd_size:.2f}, max=${max_usd_size:.2f} (notional)")
logger.info(f"  4. Леверидж: {leverage}x → маржа до ограничений: ${margin_required_initial:.2f}")
logger.info(f"  5. Использованная маржа: ${used_margin:.2f}, доступная: ${balance - used_margin:.2f}")
logger.info(f"  6. Max margin percent: {max_margin_percent*100:.1f}% → лимит: ${max_margin_allowed:.2f}")
logger.info(f"  7. Доступная маржа: ${available_margin:.2f}")
```

**Статус:** ❓ **НЕ НАЙДЕНО В ЛОГАХ** - логирование есть в коде, но в логах отсутствует

**Возможные причины:**
1. Логирование вызывается только при определенных условиях (verbose режим?)
2. Логирование находится в блоке, который не выполняется
3. Логирование отключено или переопределено

---

### 4. Финальный расчет размера позиции ❓ НЕ НАЙДЕНО В ЛОГАХ

**Файл:** `src/strategies/scalping/futures/risk_manager.py` (строки 1648-1694)

**Формат (код):**
```python
logger.info("=" * 80)
logger.info(f"💰 ФИНАЛЬНЫЙ РАСЧЕТ РАЗМЕРА ПОЗИЦИИ для {symbol}:")
logger.info(f"   Баланс: ${balance:.2f} (профиль: {balance_profile['name']})")
logger.info(f"   Базовый размер (notional): ${base_usd_size:.2f}")
logger.info(f"   Леверидж: {leverage}x")
# ... и т.д.
```

**Статус:** ❓ **НЕ НАЙДЕНО В ЛОГАХ** - логирование есть в коде, но в логах отсутствует

---

### 5. Прогрессивный расчет размера ✅ ЛОГИРУЕТСЯ (INFO)

**Файл:** `src/strategies/scalping/futures/risk_manager.py` (строка 721)

**Формат:**
```
📊 Прогрессивный расчет размера для баланса ${balance:.2f}: 
${size_at_min:.2f} → ${size_at_max:.2f} (range: ${min_balance:.2f}-${max_balance:.2f}) 
→ base_size=${base_usd_size:.2f}
```

**Статус:** ✅ **ДОЛЖНО ЛОГИРОВАТЬСЯ** (INFO уровень) - но в логах не найдено для BTC/ETH

**Примечание:** Возможно, логирование срабатывает только для прогрессивных профилей и только при определенных условиях.

---

### 6. Per-symbol multiplier ✅ ЛОГИРУЕТСЯ (INFO/DEBUG)

**Файл:** `src/strategies/scalping/futures/risk_manager.py` (строки 757-770)

**Формат:**
```
📊 Per-symbol multiplier для {symbol}: {position_multiplier}x 
→ размер ${original_size:.2f} → ${base_usd_size:.2f}
```

**Статус:** ✅ **ЛОГИРУЕТСЯ** (INFO если multiplier != 1.0, DEBUG если == 1.0)

**Примечание:** Логирование зависит от наличия per-symbol multiplier в конфиге.

---

## 📋 ЧТО НЕ ЛОГИРУЕТСЯ (НО ДОЛЖНО)

### Проблемы с логированием:

1. **Детальный расчет размера позиции (строки 1195-1235):**
   - Логирование есть в коде, но не встречается в логах
   - Нужно проверить, при каких условиях оно вызывается

2. **Финальный расчет размера позиции (строки 1648-1694):**
   - Логирование есть в коде, но не встречается в логах
   - Нужно проверить, при каких условиях оно вызывается

3. **Итоговый размер позиции и маржа:**
   - Нет единого логирования итогового размера позиции перед открытием
   - Нет логирования итоговой маржи, которая будет использована

---

## 🔍 АНАЛИЗ КОДА

### Где вызывается расчет размера позиции:

1. **SignalCoordinator.execute_signal_from_price()** (строка ~2280)
   - Вызывает `risk_manager.calculate_position_size()`
   - Передает signal, price, balance

2. **RiskManager.calculate_position_size()** (строка 596)
   - Основной метод расчета размера позиции
   - Содержит детальное логирование (строки 1195-1235)
   - Содержит финальное логирование (строки 1648-1694)

### Условия логирования:

Проверка кода показывает, что детальное логирование (строки 1195-1235) находится **ПОСЛЕ** всех проверок и расчетов, но **ПЕРЕД** финальным логированием. Это означает, что логирование должно вызываться при каждом расчете размера позиции.

**Гипотеза:** Детальное логирование может быть в блоке, который не всегда выполняется (например, только при определенных условиях или в verbose режиме).

---

## 📊 ЧТО РАБОТАЕТ В ЛОГАХ

### Найдено в логах:

1. ✅ **Прогрессивная адаптация баланса** (DEBUG) - логируется часто
2. ✅ **Адаптивные параметры риска** (DEBUG) - логируется часто
3. ✅ **Расчет ликвидации** (DEBUG) - логируется для открытых позиций
4. ✅ **Информация о позициях** (INFO) - логируется при открытии/закрытии

### Не найдено в логах:

1. ❌ **Детальный расчет размера позиции** (INFO) - код есть, логов нет
2. ❌ **Финальный расчет размера позиции** (INFO) - код есть, логов нет
3. ❌ **Итоговый размер позиции перед открытием** (INFO) - нет единого логирования

---

## 💡 РЕКОМЕНДАЦИИ

### 1. Добавить логирование итогового размера позиции (по аналогии с TP/SL параметрами)

**Где:** Перед открытием позиции в `signal_coordinator.py` или `entry_manager.py`

**Формат:**
```
📊 [PARAMS] {symbol} ({regime}): РАСЧЕТ РАЗМЕРА ПОЗИЦИИ
  Баланс: ${balance:.2f} (профиль: {balance_profile})
  Базовый размер: ${base_size:.2f} USD
  Per-symbol multiplier: {multiplier}x
  Strength multiplier: {strength_mult:.2f}x
  Final size: ${final_size:.2f} USD ({size_coins:.6f} монет)
  Leverage: {leverage}x
  Margin required: ${margin:.2f} USD
  Источник: RiskManager.calculate_position_size()
```

### 2. Проверить, почему детальное логирование не вызывается

**Действие:** Найти условие, при котором логирование на строках 1195-1235 и 1648-1694 не выполняется.

### 3. Добавить логирование адаптивных параметров риска (INFO уровень)

**Где:** После получения адаптивных параметров риска

**Формат:**
```
📊 [PARAMS] {symbol} ({regime}): АДАПТИВНЫЕ ПАРАМЕТРЫ РИСКА
  Балансовый профиль: {profile}
  Max loss per trade: {max_loss}%
  Max margin: {max_margin}%
  Источник: ConfigManager.get_adaptive_risk_params()
```

---

## 🔍 ВЫВОДЫ

### ✅ Что работает:
1. **Прогрессивная адаптация баланса** - логируется (DEBUG)
2. **Адаптивные параметры риска** - логируются (DEBUG)
3. **Per-symbol multiplier** - логируется (INFO/DEBUG)

### ❓ Что требует проверки:
1. **Детальный расчет размера позиции** - код есть, но не логируется
2. **Финальный расчет размера позиции** - код есть, но не логируется

### ❌ Что отсутствует:
1. **Единое логирование итогового размера позиции** перед открытием (по аналогии с TP/SL параметрами)
2. **Логирование итоговой маржи** перед открытием позиции

### 📝 Рекомендации:
1. **Срочно:** Добавить логирование итогового размера позиции и маржи перед открытием (INFO уровень)
2. **Средний приоритет:** Проверить, почему детальное логирование не вызывается
3. **Низкий приоритет:** Поднять уровень логирования адаптивных параметров риска с DEBUG на INFO

---

**Дата создания отчета:** 03.01.2026  
**Статус анализа:** ✅ Завершен
