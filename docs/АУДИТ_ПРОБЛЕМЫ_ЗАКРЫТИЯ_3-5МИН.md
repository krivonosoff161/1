# 🚨 КРИТИЧЕСКИЙ АУДИТ: Позиции закрываются за 3-5 минут вместо 40-80 минут

**Дата:** 22.11.2025  
**Статус:** ГЛУБОКИЙ СИСТЕМНЫЙ АНАЛИЗ  
**Вывод:** Найдены МНОЖЕСТВЕННЫЕ источники проблемы

---

## 📊 СРАВНЕНИЕ: КОНФИГ vs РЕАЛЬНОСТЬ

### Ожидаемые параметры (config_futures.yaml):
```yaml
# Ranging режим (БОльшинство сделок в этом режиме)
trailing_sl.by_regime.ranging:
  timeout_minutes: 90           # 90 минут!
  min_holding_minutes: 40.0     # 40 минут!
  min_profit_to_close: 0.025    # 2.5% минимум
  initial_trail: 0.040          # 4% трейл
```

### Реальность из ваших сделок:
- Средняя длительность: **3-5 минут** ❌
- Прибыль на сделку: **0.12 - 0.43%** ❌
- Закрытие: **Постоянно TSL или быстрый выход**

---

## 🔍 НАЙДЕНЫ 5 КРИТИЧЕСКИХ ИСТОЧНИКОВ ПРОБЛЕМЫ

### 1️⃣ **ПРОБЛЕМА: min_holding_minutes НЕ ЗАПРЕЩАЕТ ЗАКРЫТИЕ, А ТОЛЬКО ОТДАЛЯЕТ**

**Файл:** `src/strategies/scalping/futures/indicators/trailing_stop_loss.py` (строки 454-557)

```python
# ✅ Это защита, но она работает НЕПРАВИЛЬНО для ranging

effective_min_holding = self.min_holding_minutes  # 40 минут

if (self.extend_time_on_profit and profit_pct > 0 
    and effective_min_holding is not None):
    # ⚠️ ПРОБЛЕМА #1: Если позиция в ПРИБЫЛИ, увеличиваем min_holding
    # на extend_time_multiplier (2.0x), то есть 80 минут
    effective_min_holding = effective_min_holding * self.extend_time_multiplier

# ⚠️ ПРОБЛЕМА #2: Но if profit_pct < 0 (убыток!), 
# min_holding остается 40 минут!
# А потом идет проверка loss_cut...
```

**Вывод:** `min_holding_minutes` работает только когда позиция в ПРИБЫЛИ!

---

### 2️⃣ **ПРОБЛЕМА: loss_cut_percent СЛИШКОМ МЯГКИЙ ДЛЯ УБЫТКОВ**

**Файл:** `config_futures.yaml` (line 1231-1233)

```yaml
trailing_sl:
  loss_cut_percent: 1.5        # 1.5% убыток от маржи
  timeout_loss_percent: 1.0    # 1.0% убыток после таймаута
  timeout_minutes: 5           # ⚠️ ЭТО ГЛОБАЛЬНЫЙ ТАЙМАУТ, НЕ РЕЖИМНЫЙ!
```

**Проблема:** 
- Глобальный `timeout_minutes: 5` используется как fallback для ВСЕХ режимов!
- В ranging должно быть 90, но если этот параметр недочитывается → используется 5!

**Код в `tsl_manager.py` (lines 60-82):**
```python
tsl = TrailingStopLoss(
    # ...
    timeout_minutes=tsl_params.get("timeout_minutes"),  # Может вернуть None!
    min_holding_minutes=tsl_params.get("min_holding_minutes"),  # Может вернуть None!
    # ...
)
```

**Если параметр None → в `trailing_stop_loss.py` (lines 84-92):**
```python
self.timeout_minutes = (
    timeout_minutes if timeout_minutes and timeout_minutes > 0 else None
)
# Становится None! Таймаут не срабатывает!
```

---

### 3️⃣ **ПРОБЛЕМА: ЛОГИКА ЗАКРЫТИЯ СЛИШКОМ СЛОЖНАЯ И ЗАПУТАННАЯ**

**Файл:** `trailing_stop_loss.py` (lines 413-710)

Порядок проверок в `should_close_position`:

```python
# Шаг 1: Защита min_holding (линии 454-557)
if minutes_in_position < effective_min_holding:
    # ✅ Защита: не закрываем
    # ⚠️ НО: Это работает только если profit_pct > 0!
    # Если profit_pct < 0 → effective_min_holding остается 40 мин
    # и эта проверка пропускается через return False
    return False  # Только если обе условия истинны

# Шаг 2: loss_cut проверка (линии 559-576)
if profit_pct <= -loss_cut_from_price:
    # ⚠️ ПРОБЛЕМА: loss_cut может быть 0.3% (1.5% / 5x)
    # А позиция уже в -0.3% → закрываем!
    return True

# Шаг 3: timeout_loss проверка (линии 578-601)
if (minutes_in_position >= timeout_minutes 
    and profit_pct <= -timeout_loss_percent / leverage):
    # ⚠️ ПРОБЛЕМА: timeout_minutes может быть None или 5!
    # Если 5 минут прошли и убыток > 0.2% → закрываем!
    return True

# Шаг 4: Базовая SL проверка (линии 603-632)
if price_hit_sl:
    # ⚠️ ПРОБЛЕМА: Даже с TSL 4% (initial_trail),
    # цена может быстро вернуться через 3-5 минут
    return True
```

**Вывод:** Логика защиты `min_holding_minutes` НЕ ПРЕГРАЖДАЕТ путь к другим проверкам!

---

### 4️⃣ **ПРОБЛЕМА: ConfigManager МОЖЕТ НЕ ЧИТАТЬ ПАРАМЕТРЫ ПРАВИЛЬНО**

**Файл:** `src/strategies/scalping/futures/config/config_manager.py`

Нужно проверить:
- Читает ли `get_trailing_sl_params()` параметры по режиму?
- Возвращает ли правильные значения для `ranging`?
- Есть ли fallback значения?

---

### 5️⃣ **ПРОБЛЕМА: ФИЛЬТРЫ ПОЗВОЛЯЮТ СЛИШКОМ МНОГО СДЕЛОК**

**Файл:** `config_futures.yaml` (line 31-33)

```yaml
max_concurrent_signals: 3    # Можно одновременно 3 сигнала
max_positions_per_symbol: 1  # Но по одной позиции на символ
```

**Вывод:** При 5 символах может быть открыто 5 позиций ОДНОВРЕМЕННО!

---

## 🔧 ПРЕДПОЛАГАЕМЫЕ硬КОД И ЗАХАРДКОЖЕННОСТЬ

### Возможные захардкоженные значения:

1. **В orchestrator.py:**
   - Fallback timeout `5` минут?
   - Fallback min_holding `0.5` минут?

2. **В trailing_stop_loss.py:**
   - Hardcoded `min_critical_hold_seconds = 30.0` (line 484)?
   - Fallback значения в линиях 655-668?

3. **В position_manager.py:**
   - Возможно, есть параллельная проверка TP?
   - Возможно, есть собственная логика закрытия?

---

## 📋 ЧЕКЛИСТ ДИАГНОСТИКИ

### Нужно проверить (по приоритету):

- [ ] **КРИТИЧНО:** Читаются ли параметры для `ranging` режима в `ConfigManager`?
  - Что возвращает `get_trailing_sl_params("ranging")`?
  - Содержит ли правильные `timeout_minutes=90`, `min_holding_minutes=40`?

- [ ] **КРИТИЧНО:** Какие параметры РЕАЛЬНО используются при создании TSL?
  - Добавить логирование: `logger.warning(f"TSL создан с: timeout={timeout_minutes}, min_holding={min_holding_minutes}")`

- [ ] **ВЫСОКИЙ ПРИОРИТЕТ:** Есть ли параллельная логика закрытия в `position_manager.py`?
  - Может быть, там есть собственная проверка TP/SL?

- [ ] **ВЫСОКИЙ ПРИОРИТЕТ:** Как вызывается `should_close_position()`?
  - Где именно в orchestrator она вызывается?
  - Пересчитываются ли параметры каждый раз?

- [ ] **СРЕДНИЙ ПРИОРИТЕТ:** Работает ли `extend_time_on_profit` правильно?
  - Если прибыль 0.1%, должна ли позиция продержаться 80 минут?

- [ ] **СРЕДНИЙ ПРИОРИТЕТ:** Есть ли другие места, где может закрыться позиция?
  - Profit Harvesting?
  - Фильтры?
  - Emergency Close?

---

## 🚨 КРИТИЧЕСКОЕ ПРЕДПОЛОЖЕНИЕ

**Гипотеза:** В `ConfigManager` или при создании TSL используются **ГЛОБАЛЬНЫЕ** параметры вместо **РЕЖИМНЫХ**:

```python
# ❌ НЕПРАВИЛЬНО (используется глобальное значение)
timeout_minutes = self.config.futures_modules.trailing_sl.timeout_minutes  # 5!

# ✅ ПРАВИЛЬНО (должно быть по режиму)
tsl_params = self.config_manager.get_trailing_sl_params(regime="ranging")
timeout_minutes = tsl_params.get("timeout_minutes")  # 90!
```

---

## 📸 СДЕЛКИ ИЗ ВАШИХ ДАННЫХ - АНАЛИЗ

```
BTC-USDT:
- Время: 09:35:04 → 09:39:51 = 4 мин 47 сек  ❌ (ожидали 40-80 мин)
- Прибыль: -0.59 USDT (-2.06%)
- Закрытие: Loss-cut? TSL?

SOL-USDT:
- Время: 09:36:36 → 09:36:40 = 4 сек ❌❌❌ (ЭКСТРЕННОЕ ЗАКРЫТИЕ!)
- Прибыль: -0.91 USDT (-2.33%)
- Закрытие: Явно критический loss-cut

DOGE-USDT:
- Время: 09:36:54 → 09:37:02 = 8 сек ❌❌❌ (ЭКСТРЕННОЕ ЗАКРЫТИЕ!)
- Прибыль: -0.27 USDT (-0.86%)
- Закрытие: ???
```

**Вывод:** Есть позиции, закрываемые за 4-8 СЕКУНД! Это явно не `min_holding_minutes=40`!

---

## ✅ СЛЕДУЮЩИЕ ДЕЙСТВИЯ

1. **Проверить логирование:** Запустить бот с DEBUG логами
2. **Добавить трассировку:** Логировать какие параметры TSL создается
3. **Проверить ConfigManager:** Убедиться, что читаются режимные параметры
4. **Найти захардкоженные значения:** Grep по цифрам (5, 0.5, и т.д.)
5. **Проверить position_manager:** Может ли там быть параллельная логика?

