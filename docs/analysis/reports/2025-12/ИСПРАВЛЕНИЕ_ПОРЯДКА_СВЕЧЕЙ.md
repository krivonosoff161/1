# ✅ ИСПРАВЛЕНИЕ ПОРЯДКА СВЕЧЕЙ OKX API

**Дата:** 31.12.2025

---

## 🔴 ПРОБЛЕМА

По логам видно, что для большинства позиций получается только **1 свеча** вместо ожидаемых 15+ свечей (15 минут после закрытия).

**Причина:**
- OKX API возвращает свечи в **обратном порядке** (от новых к старым) при использовании параметра `before`
- При использовании `after` также возвращаются свечи в обратном порядке
- Фильтрация не учитывала этот порядок, поэтому попадала только одна свеча в диапазон

**Пример из логов:**
```
Ищем свечи: 11:42:28 - 11:57:28
API возвращает: первая свеча=11:42:00, последняя свеча=11:18:00
Получено после фильтрации: 1 свеча
```

---

## ✅ РЕШЕНИЕ

1. **Использовать `before` с `end_time`** вместо `after` с `start_time`
2. **Переворачивать список свечей** после фильтрации для получения хронологического порядка
3. **Увеличить запас** при расчете `limit` (с +10 до +20)

**БЫЛО:**
```python
after_timestamp_ms = int(start_time.timestamp() * 1000)
params = {
    "after": str(after_timestamp_ms),
    "limit": str(limit),
}
# Фильтрация без учета обратного порядка
filtered_candles.sort(key=lambda x: x["timestamp"])
```

**СТАЛО:**
```python
before_timestamp_ms = int(end_time.timestamp() * 1000)
params = {
    "before": str(before_timestamp_ms),  # ✅ before с end_time
    "limit": str(limit),
}
# ✅ Переворачиваем список (OKX возвращает от новых к старым)
filtered_candles.reverse()
filtered_candles.sort(key=lambda x: x["timestamp"])
```

---

## 📋 ИЗМЕНЕНИЯ В КОДЕ

1. ✅ Изменен параметр API с `after` на `before`
2. ✅ Используется `end_time` вместо `start_time` для параметра `before`
3. ✅ Добавлено `filtered_candles.reverse()` для получения хронологического порядка
4. ✅ Увеличен запас при расчете `limit` (с +10 до +20)
5. ✅ Улучшено логирование (указано, что первая свеча - новая, последняя - старая)

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После исправления для каждой позиции должно получаться **15+ свечей** (15 минут после закрытия = 15 свечей по 1 минуте).

**Статус:** ✅ Исправлено, готово к тестированию


