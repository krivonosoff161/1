# ✅ ИТОГОВЫЙ ОТЧЕТ ИСПРАВЛЕНИЙ ЛИМИТОВ И НАПРАВЛЕНИЯ

**Дата:** 2025-11-12  
**Проблемы:** Размер позиции превышает лимит после округления, неправильное определение направления позиции

---

## ❌ НАЙДЕННЫЕ ПРОБЛЕМЫ

### 1. ❌ Размер позиции превышает лимит после округления

**Проблема:**
- BTC-USDT: Размер позиции 184.12 USD превышает лимит 180.00 USD
- Это происходит из-за округления до минимума биржи (lotSz)
- После округления размер увеличивается и превышает лимит

**Пример из логов:**
```
⚠️ Размер позиции изменен из-за округления/минимума: 0.001760 → 0.001800 монет (-2.29%), 
notional: $180.00 → $184.12, margin: $36.00 → $36.82
❌ Размер позиции 184.12 USD превышает лимит 180.00 USD
```

**Причина:**
- Размер позиции рассчитывается: `position_size = (margin_usd * leverage) / price`
- Затем округляется до `lotSz`: `rounded_size = round_to_step(size_in_contracts, lot_sz)`
- После округления размер может увеличиться и превысить лимит
- Проверка лимитов происходит ДО округления, а не ПОСЛЕ

---

### 2. ❌ Неправильное определение направления позиции

**Проблема:**
- ETH-USDT: Позиция открыта как SHORT (side=sell, posSide=short)
- Но в логах: "⚠️ Позиция ETH-USDT LONG УЖЕ ОТКРЫТА (size=0.49)"
- Это означает, что позиция определяется неправильно!

**Пример из логов:**
```
✅ Позиция ETH-USDT SHORT открыта по реальному сигналу
⚠️ Позиция ETH-USDT LONG УЖЕ ОТКРЫТА (size=0.49), БЛОКИРУЕМ новые ордера
```

**Причина:**
- В коде (строка 1815-1820) используется `abs(pos_size)` для размера
- Но затем определяется side по `pos > 0`, но `pos` уже был взят с `abs()`
- Это означает, что side всегда определяется как "long" (pos > 0 после abs())

---

## ✅ ИСПРАВЛЕНИЯ

### 1. ✅ Проверка лимитов ПОСЛЕ округления

**Изменения:**
- Проверяем лимиты ПОСЛЕ округления, а не ДО
- Если размер превышает лимит, уменьшаем его с учетом округления
- Используем floor округление, чтобы гарантировать, что размер не превысит лимит
- Если минимум превышает лимит, логируем ошибку и возвращаем 0

**Код:**
```python
# ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Проверяем лимиты ПОСЛЕ округления
if real_notional_usd > max_usd_size:
    # Уменьшаем до лимита с учетом округления
    target_notional_usd = max_usd_size
    target_position_size = target_notional_usd / price
    target_size_in_contracts = target_position_size / ct_val
    
    # ✅ Округляем ВНИЗ до ближайшего шага
    import math
    target_rounded_size_in_contracts = math.floor(target_size_in_contracts / lot_sz) * lot_sz
    
    # Проверяем минимальный размер
    if target_rounded_size_in_contracts < min_sz:
        min_notional_usd = min_sz * ct_val * price
        if min_notional_usd > max_usd_size:
            # Минимум превышает лимит - возвращаем 0
            logger.error(f"❌ КРИТИЧЕСКАЯ ОШИБКА: Минимальный размер превышает лимит!")
            return 0.0
        else:
            target_rounded_size_in_contracts = min_sz
    
    # Вычисляем реальный размер после округления
    real_position_size = target_rounded_size_in_contracts * ct_val
    real_notional_usd = real_position_size * price
    real_margin_usd = real_notional_usd / leverage
```

**Файл:** `src/strategies/scalping/futures/orchestrator.py:3050-3108`

---

### 2. ✅ Правильное определение направления позиции

**Изменения:**
- Используем `posSide` из API, если доступен
- Если `posSide` недоступен, определяем по знаку `pos` (ДО abs())
- Это правильно определяет направление позиции

**Код:**
```python
# ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Правильное определение направления позиции
pos_raw = float(symbol_positions[0].get("pos", "0"))
pos_size = abs(pos_raw)
# Используем posSide из API, если доступен, иначе определяем по знаку pos
pos_side_raw = symbol_positions[0].get("posSide", "").lower()
if pos_side_raw in ["long", "short"]:
    # Используем posSide из API (надежнее)
    pos_side = pos_side_raw
else:
    # Fallback: определяем по знаку pos (ДО abs())
    pos_side = "long" if pos_raw > 0 else "short"
```

**Файл:** `src/strategies/scalping/futures/orchestrator.py:1815-1825`

---

## 📊 ПРИМЕРЫ РАБОТЫ

### Пример 1: BTC-USDT (превышение лимита)

**Входные данные:**
- Баланс: $1063.45
- Цена: $102287.50
- Лимит: $180.00 (notional)
- Leverage: 5x

**До исправления:**
- Рассчитанный размер: $180.00 (notional) = 0.001760 монет
- После округления: 0.001800 монет = $184.12 (notional) ❌
- Результат: Размер 184.12 > лимит 180.00 → ордер отклоняется!

**После исправления:**
- Рассчитанный размер: $180.00 (notional) = 0.001760 монет
- После округления: 0.001800 монет = $184.12 (notional)
- Проверка лимитов: 184.12 > 180.00 → уменьшаем до лимита
- Уменьшенный размер: floor(180.00 / lotSz) * lotSz = 0.001700 монет = $174.00 (notional) ✅
- Результат: Размер 174.00 <= лимит 180.00 → ордер размещается!

---

### Пример 2: ETH-USDT (неправильное направление)

**Входные данные:**
- Позиция: SHORT (pos=-0.49, posSide="short")

**До исправления:**
- pos_size = abs(-0.49) = 0.49
- pos_side = "long" if pos > 0 else "short" = "long" ❌ (pos уже был взят с abs())
- Результат: Позиция определяется как LONG, хотя она SHORT!

**После исправления:**
- pos_raw = -0.49
- pos_size = abs(-0.49) = 0.49
- pos_side_raw = "short" (из API)
- pos_side = "short" ✅ (используем posSide из API)
- Результат: Позиция определяется как SHORT правильно!

---

## 🔧 ИЗМЕНЕНИЯ В КОДЕ

### 1. `_calculate_position_size()` в `orchestrator.py`

**Изменения:**
- Добавлена проверка лимитов ПОСЛЕ округления
- Если размер превышает лимит, уменьшаем его с учетом округления
- Используем floor округление для гарантии, что размер не превысит лимит
- Если минимум превышает лимит, возвращаем 0

**Файл:** `src/strategies/scalping/futures/orchestrator.py:3050-3108`

---

### 2. `_check_for_signals()` в `orchestrator.py`

**Изменения:**
- Используем `posSide` из API, если доступен
- Если `posSide` недоступен, определяем по знаку `pos` (ДО abs())
- Это правильно определяет направление позиции

**Файл:** `src/strategies/scalping/futures/orchestrator.py:1815-1825`

---

## 📋 ИТОГОВЫЙ ВЫВОД

1. ✅ **Проверка лимитов ПОСЛЕ округления:** Размер позиции не превышает лимиты после округления
2. ✅ **Правильное определение направления:** Позиция определяется правильно (LONG/SHORT)
3. ✅ **Уменьшение размера при превышении лимита:** Размер уменьшается до лимита с учетом округления
4. ✅ **Обработка ошибок:** Если минимум превышает лимит, логируется ошибка и возвращается 0

---

## 🔧 ПРИОРИТЕТ ИСПРАВЛЕНИЙ

1. ✅ **КРИТИЧЕСКОЕ:** Проверка лимитов ПОСЛЕ округления
2. ✅ **КРИТИЧЕСКОЕ:** Правильное определение направления позиции
3. ✅ **ВАЖНО:** Уменьшение размера при превышении лимита
4. ✅ **ВАЖНО:** Обработка ошибок при превышении лимита

---

## 📝 РЕКОМЕНДАЦИИ

1. **Перезапустить бота** и проверить логи — ошибок быть не должно
2. **Мониторить новые ордера** — размер позиции не должен превышать лимиты
3. **Проверить направление позиций** — позиции должны определяться правильно (LONG/SHORT)
4. **Проверить конфигурацию** — убедиться, что `min_position_usd` и `max_position_usd` настроены правильно

