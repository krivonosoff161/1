# CURSOR_AUDIT_BUNDLE_TASK.md v 1.2 — Объяснение

**Дата создания:** 2025-12-07  
**Версия:** 1.2  
**Статус:** Готов к использованию

---

## 📌 Что это такое?

**`CURSOR_AUDIT_BUNDLE_TASK.md`** — это **техническое задание для Cursor AI**, которое автоматизирует сбор и валидацию всех данных торгового бота для внешнего аудита.

### Цель:
Автоматически собрать из проекта торгового бота **единый JSON-пакет** для внешнего аудита (код, стратегия, сделки, риски, метрики).

### Кто использует:
- **Cursor AI** — выполняет сбор по этому ТЗ
- **Пользователь** — только указывает пути к файлам
- **Аналитик (Kimi/Qwen3-Max)** — получает готовый структурированный JSON

### Что получает аналитик:
Один файл `audit_bundle_YYYYMMDD_strategy.json`, в котором всё структурировано и уже провалидировано.

---

## 🔄 История создания (с Kimi/Qwen3-Max)

### Версия 1.0 (начальная):
- Базовая структура сбора данных
- Общие правила валидации
- Схема JSON-пакета

### Версия 1.1 (улучшения):
- Добавлены технические проверки (раздел 7.0):
  - `contract_multiplier`, `leverage`, `slippage_units`
  - `forward_looking` (regex-паттерны)
  - `secrets redaction`, `dst_transition`, `file_lock`
  - `avg_fill_price`, `fee_level`, `time_in_force`, `fill_id`
  - `funding_fee` валидация, `quote_currency`, `lot_multiplier`

### Версия 1.2 (микро-фиксы):
- **4.1.6:** Незакрытая свеча OKX (пропуск, не дубль)
- **4.1.7:** Zero-byte файлы (статус "missing", не ошибка)
- **7.0.16:** Кодировка файлов (UTF-8 → cp1251 → koi8-r)
- **7.0.17:** Rollover контрактов (`contract_id` колонка)
- **7.0.18:** Кросс-маржинальные позиции (`cross_margin` флаг)
- **7.0.19:** Режим позиций (hedge/netting валидация)
- **7.0.20:** Trigger price для стоп-ордеров

**Итог:** Покрытие ~99% реальных сбоев при сборе данных у трейдеров.

---

## 📋 Какие файлы собирает Cursor (строго по порядку)

### 1. Конфигурация
- `config.*` (yaml, json, py, toml) — параметры стратегии (TP/SL, фильтры, лимиты, комиссия, плечо)

### 2. Исходный код
- `strategy.py` и модули — парсинг констант, forward-looking, seed-ов

### 3. Торговые данные
- `trades.csv` — каждая сделка: цены, размер, PnL, комиссия, `contract_id`, `funding_fee`, `reason`
- `market_data.csv` — OHLCV + bid/ask + `quote_currency` (может быть 1m / 5m / 1h)
- `positions.csv` — открытые позиции: size, `leverage`, margin, `cross_margin`, `position_mode`
- `orders.csv` — каждый ордер: `fill_id`, `avg_fill_price`, `slippage_units`, `time_in_force`, `trigger_price`

### 4. Экзогенные данные
- `funding.csv`, `borrow-rates.csv`, опционы, эконом-календарь (если есть)

### 5. Оптимизация (если была)
- `burn_params.json` — диапазоны оптимизации, метрика, seed, walk-forward
- `burn_results.csv` — train/val Sharpe, maxDD, PnL по итерациям

### 6. Метрики
- `performance_report.yaml` — итоговые метрики (Sharpe, Sortino, Calmar, CAGR, MaxDD и т.д.)

---

## ✅ Основные проверки целостности (автоматом)

### Базовые проверки:
1. **Дубли ключей** — `trade_id`, `order_id+fill_id`, `timestamp+symbol`
2. **NaN / нули** — в обязательных полях
3. **Монотонность timestamp** — "прыжки" назад — ошибка
4. **Знаки size** — long > 0, short < 0
5. **Ценовая логика** — high ≥ max(open,close), bid ≤ ask

### Специфичные для OKX:
6. **Незакрытая свеча OKX** — `close==prev_close`, `volume==0` → пропускаем, не дубль
7. **Zero-byte файлы** — статус "missing", не ошибка
8. **DST-окно** — 02:00-03:00 UTC в марте/октябре → warning, не error

### Обработка файлов:
9. **Кодировки** — если UTF-8 не читается → пробовать cp1251, koi8-r, просить конвертировать
10. **Файл-блокировка** → просим сделать копию
11. **Parquet с partition-columns** → читаем через pyarrow.dataset

---

## 🔧 Технические "под-капотные" проверки (раздел 7.0)

### Инструменты и контракты:
- `instruments.csv` — `contract_value`, `tick_size`, `quote_currency` (для перевода объёма в USDT)
- `leverage` в позициях — чтобы считать маржу
- `contract_multiplier` — для фьючерсов (BTC=0.01, ETH=0.1)

### Ордера и исполнение:
- `slippage_units` = `abs` / `pct`; если `pct` — знак всегда "+"
- `avg_fill_price` — допуск ±1 тик при округлении
- `time_in_force` — GTC, IOC, FOK, GTD (для GTD требуется `expire_time`)
- `fill_id` — для partial fills (уникален `order_id+fill_id`)
- `trigger_price` — обязателен для стоп-ордеров

### Forward-looking (запрещено):
- Regex-паттерны: `df.shift(-1)`, `iloc[i+1]`, `loc[t+pd.Timedelta]`
- Слова: `future`, `lead`, `next_bar`, `tomorrow`

### Безопасность:
- **Секреты:** перед MD5 заменяем значения ключей на `***REDACTED***`
- `md5_hash_original` — хэш до редактирования

### Финансовые данные:
- **Funding fee:** для SPOT не валидируем знак; для Futures проверяем по `side` и `funding_rate`
- **VIP-уровень** → влияет на maker/taker (0.02%/0.05% у VIP0)

### Продвинутые сценарии:
- **Rollover:** не меняем `symbol`, меняем `contract_id` (BTC-USDT-250320)
- **Cross-margin:** `margin` может быть > `balance`, валидация `margin ≤ balance` отключается
- **Netting vs Hedge:** одновременный long+short запрещён в netting-mode

---

## 📦 Что получает аналитик в итоговом JSON

### Структура `audit_bundle_YYYYMMDD_strategy.json`:

```json
{
  "metadata": {
    "collection_date": "2025-12-07T13:00:00Z",
    "bot_version": "...",
    "python_version": "...",
    "os": "..."
  },
  "files": {
    "config": { "path": "...", "md5_hash": "...", "shape": {...}, "columns": {...} },
    "trades": { "path": "...", "md5_hash": "...", "shape": {...}, "columns": {...} },
    "market_data": { "path": "...", "md5_hash": "...", "shape": {...}, "columns": {...} },
    ...
  },
  "integrity_errors": {
    "status": "clean" | "warning" | "blocked",
    "duplicates": [...],
    "missing_values": [...],
    "forward_looking": [...],
    ...
  },
  "constants": {
    "tp_percent": 2.5,
    "sl_percent": 1.5,
    ...
  },
  "summary": {
    "total_trades": 150,
    "win_rate": 0.55,
    "sharpe_ratio": 1.2,
    ...
  }
}
```

**Всё в UTC, в USDT, с контрольными суммами** — можно сразу запускать аудит.

---

## 🚀 Когда применять?

### ✅ Применять СЕЙЧАС (если):
1. **Нужен внешний аудит** стратегии и кода (например, от Kimi/Qwen3-Max)
2. **Собрана статистика** за несколько дней/недель торговли
3. **Готовы к глубокому анализу** торговой логики и рисков
4. **Планируется оптимизация** параметров на основе аудита

### ⏸️ НЕ применять (если):
1. **Бот только запущен** — нет достаточной статистики
2. **Нет необходимости** во внешнем аудите
3. **Внутренний анализ** достаточен

---

## 📝 Как использовать?

### Шаг 1: Подготовка
1. Убедитесь, что бот собрал достаточно данных (минимум 1-2 недели торговли)
2. Проверьте наличие всех CSV файлов:
   - `trades_*.csv`
   - `positions_open_*.csv`
   - `orders_*.csv`
   - `signals_*.csv`
   - `market_data` (если есть)

### Шаг 2: Запуск Cursor
1. Откройте Cursor AI
2. Скопируйте содержимое `CURSOR_AUDIT_BUNDLE_TASK.md` в системный промпт
3. Дайте команду:
   ```
   Работай строго по разделу 8, шаг за шагом запрашивай файлы, проверяй, формируй JSON
   ```

### Шаг 3: Указание путей
- Cursor будет запрашивать пути к файлам по порядку
- Указывайте пути к файлам, когда Cursor попросит
- Cursor сам просит исправить ошибки, если найдет

### Шаг 4: Получение результата
- Cursor создаст файл `audit_bundle_YYYYMMDD_strategy.json` в корне проекта
- Этот файл можно отправить аналитику (Kimi/Qwen3-Max) для аудита

---

## 🎯 Рекомендуемый порядок действий

### Сейчас (после критических исправлений):
1. ✅ **Продолжить сбор логов** 4-6 часов (или до первого `trending` режима)
2. ✅ **Проверить CSV файлы** через 1-2 часа
3. ⏸️ **НЕ запускать сбор пакета** — еще мало данных

### Через 1-2 недели (после сбора статистики):
1. ✅ **Собрать пакет данных** через Cursor по `CURSOR_AUDIT_BUNDLE_TASK.md`
2. ✅ **Отправить аналитику** (Kimi/Qwen3-Max) для аудита
3. ✅ **Получить рекомендации** по оптимизации

### После аудита:
1. ✅ **Применить рекомендации** аналитика
2. ✅ **Повторить сбор** статистики
3. ✅ **Сравнить результаты** до и после оптимизации

---

## 💡 Итог

**`CURSOR_AUDIT_BUNDLE_TASK.md` v 1.2** — это **готовый инструмент** для автоматического сбора и валидации данных торгового бота для внешнего аудита.

**Когда применять:**
- ✅ После сбора достаточной статистики (1-2 недели)
- ✅ Перед внешним аудитом стратегии
- ✅ При необходимости глубокого анализа торговой логики

**Сейчас:**
- ⏸️ **НЕ применять** — еще мало данных
- ✅ **Продолжить сбор** логов и статистики
- ✅ **Использовать позже**, когда будет достаточно данных

---

**Версия документа:** 1.0  
**Последнее обновление:** 2025-12-07

