# ✅ ИСПРАВЛЕНИЕ ЛОГИРОВАНИЯ ПАРАМЕТРОВ

**Дата:** 03.01.2026  
**Проблема:** Логирование параметров не работало в fallback-блоках

---

## ПРОБЛЕМА

Логирование параметров было добавлено в код, но **НЕ РАБОТАЛО** в логах сессии 03.01.2026.

**Причина:**
- Логирование срабатывало только при использовании `ParameterProvider`
- Если использовался fallback на `config_manager`, логирование НЕ происходило
- В результате невозможно было отследить, какие параметры используются для позиций

---

## ИСПРАВЛЕНИЯ

### 1. Добавлено логирование в fallback-блоки для TP

**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py`

**Места исправления:**

1. **Fallback из `symbol_profiles`** (строка ~826)
   ```python
   # ✅ НОВОЕ (03.01.2026): Логирование источника TP параметров при использовании fallback
   logger.info(
       f"📊 [PARAMS] {symbol} ({regime}): TP параметры "
       f"tp_percent={tp_percent:.2f}%, tp_atr_multiplier={tp_atr_multiplier:.2f}, "
       f"tp_min={tp_min_percent:.2f}%, tp_max={tp_max_percent:.2f}% | "
       f"Источник: symbol_profiles.{symbol}.{regime} (fallback)"
   )
   ```

2. **Fallback из `by_regime`** (строка ~866)
   ```python
   # ✅ НОВОЕ (03.01.2026): Логирование источника TP параметров при использовании fallback
   logger.info(
       f"📊 [PARAMS] {symbol} ({regime}): TP параметры "
       f"tp_percent={tp_percent:.2f}%, tp_atr_multiplier={tp_atr_multiplier:.2f}, "
       f"tp_min={tp_min_percent:.2f}%, tp_max={tp_max_percent:.2f}% | "
       f"Источник: by_regime.{regime} (fallback)"
   )
   ```

3. **Fallback из глобального `scalping_config`** (строка ~888)
   ```python
   # ✅ НОВОЕ (03.01.2026): Логирование источника TP параметров при использовании глобального fallback
   logger.info(
       f"📊 [PARAMS] {symbol} ({regime}): TP параметры "
       f"tp_percent={tp_percent:.2f}%, tp_atr_multiplier={tp_atr_multiplier:.2f}, "
       f"tp_min={tp_min_percent:.2f}%, tp_max={tp_max_percent:.2f}% | "
       f"Источник: scalping_config.tp_percent (глобальный fallback)"
   )
   ```

### 2. Добавлено логирование в fallback-блоки для SL

**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py`

**Места исправления:**

1. **Fallback из `adaptive_regime`** (строка ~1092)
   ```python
   # ✅ НОВОЕ (03.01.2026): Логирование источника SL параметров при использовании fallback
   logger.info(
       f"📊 [PARAMS] {symbol} ({regime}): SL параметры "
       f"sl_percent={sl_percent:.2f}%, sl_atr_multiplier={sl_atr_multiplier:.2f}, "
       f"sl_min={sl_min_percent:.2f}% | "
       f"Источник: adaptive_regime.{regime} (fallback)"
   )
   ```

2. **Fallback из `by_regime`** (строка ~1129)
   ```python
   # ✅ НОВОЕ (03.01.2026): Логирование источника SL параметров при использовании fallback
   logger.info(
       f"📊 [PARAMS] {symbol} ({regime}): SL параметры "
       f"sl_percent={sl_percent:.2f}%, sl_atr_multiplier={sl_atr_multiplier:.2f}, "
       f"sl_min={sl_min_percent:.2f}% | "
       f"Источник: by_regime.{regime} (fallback)"
   )
   ```

3. **Fallback из глобального `scalping_config`** (строка ~1149)
   ```python
   # ✅ НОВОЕ (03.01.2026): Логирование источника SL параметров при использовании глобального fallback
   logger.info(
       f"📊 [PARAMS] {symbol} ({regime}): SL параметры "
       f"sl_percent={sl_percent:.2f}%, sl_atr_multiplier={sl_atr_multiplier:.2f}, "
       f"sl_min={sl_min_percent:.2f}% | "
       f"Источник: scalping_config.sl_percent (глобальный fallback)"
   )
   ```

---

## РЕЗУЛЬТАТ

**Теперь логирование будет срабатывать ВСЕГДА:**

1. ✅ При использовании `ParameterProvider` (уже было)
2. ✅ При использовании fallback из `symbol_profiles` (добавлено)
3. ✅ При использовании fallback из `by_regime` (добавлено)
4. ✅ При использовании fallback из `adaptive_regime` (добавлено для SL)
5. ✅ При использовании глобального fallback из `scalping_config` (добавлено)

**Формат логов:**
```
📊 [PARAMS] {symbol} ({regime}): TP параметры tp_percent={value}%, tp_atr_multiplier={value}, tp_min={value}%, tp_max={value}% | Источник: {источник}
📊 [PARAMS] {symbol} ({regime}): SL параметры sl_percent={value}%, sl_atr_multiplier={value}, sl_min={value}% | Источник: {источник}
```

**Источники параметров:**
- `ParameterProvider.get_exit_params()` - основной источник
- `symbol_profiles.{symbol}.{regime}` - per-symbol параметры (fallback)
- `by_regime.{regime}` - per-regime параметры (fallback)
- `adaptive_regime.{regime}` - adaptive параметры (fallback для SL)
- `scalping_config.tp_percent/sl_percent` - глобальные параметры (последний fallback)

---

## ПРОВЕРКА

**При следующем запуске бота:**
1. В логах должны появиться сообщения `📊 [PARAMS]` для каждой позиции
2. Можно будет отследить, какие параметры используются для каждой позиции
3. Можно будет увидеть источник параметров (ParameterProvider или fallback)

---

**Дата исправления:** 03.01.2026  
**Статус:** ✅ ИСПРАВЛЕНО
