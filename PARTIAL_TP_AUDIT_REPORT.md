# 🔍 АУДИТ ЧАСТИЧНОГО ЗАКРЫТИЯ ПОЗИЦИЙ (PARTIAL TP)

**Дата:** 04.12.2025  
**Статус:** Завершен

---

## 📊 ИСПОЛЬЗОВАНИЕ PARTIAL TP

**Позиций с Partial TP:** 0  
**Позиций без Partial TP:** 3684 (100%)  
**Процент использования:** 0.0%

⚠️ **КРИТИЧЕСКАЯ ПРОБЛЕМА:** Partial TP не используется ни в одной позиции!

---

## 📈 ЭФФЕКТИВНОСТЬ PARTIAL TP

| Метрика | С Partial TP | Без Partial TP |
|---------|--------------|----------------|
| Позиций | 0 | 3684 |
| Win Rate | 0.0% | 0.0% |
| Средний PnL | $0.00 | $0.00 |
| Общий PnL | $0.00 | $0.00 |

⚠️ **ПРОБЛЕМА:** Нет данных для сравнения, так как Partial TP не используется.

---

## 🔍 ОБНАРУЖЕНИЕ ЧАСТИЧНЫХ ЗАКРЫТИЙ

**Всего обнаружено частичных закрытий:** 0

⚠️ **ПРОБЛЕМА:** Нет данных о частичных закрытиях в сделках.

---

## 🔧 АНАЛИЗ РЕАЛИЗАЦИИ В КОДЕ

### 1. Реализация Partial TP

**Файл:** `src/strategies/scalping/futures/position_manager.py` (строки 2827-3100)

**Логика:**
1. ✅ Проверка `ptp_enabled` из конфига
2. ✅ Проверка `partial_tp_done` флага (однократность)
3. ✅ Проверка `pnl_percent >= ptp_trigger`
4. ✅ Проверка `min_holding` (адаптивная)
5. ✅ Проверка минимального размера позиции
6. ✅ Размещение лимитного reduce-only ордера

**Проблемы в коде:**
1. ✅ Логика реализована правильно
2. ⚠️ **ПРОБЛЕМА:** Partial TP может быть отключен в конфиге (`enabled: false`)
3. ⚠️ **ПРОБЛЕМА:** Пороги `trigger_percent` могут быть слишком высокими

---

### 2. Параметры Partial TP в конфиге

**Файл:** `config/config_futures.yaml` (строки 60-80)

**Текущие параметры:**
```yaml
partial_tp:
  enabled: true  # ✅ Включен
  fraction: 0.6  # 60% позиции
  trigger_percent: 0.4  # 0.4% прибыли
  by_regime:
    trending:
      fraction: 0.5
      trigger_percent: 0.8  # ⚠️ Высокий порог
    ranging:
      fraction: 0.6
      trigger_percent: 1.0  # ⚠️ Высокий порог
    choppy:
      fraction: 0.7
      trigger_percent: 0.6  # ⚠️ Высокий порог
```

**Проблемы:**
1. ⚠️ **ПРОБЛЕМА:** Пороги `trigger_percent` слишком высокие (0.6-1.0%)
2. ⚠️ **ПРОБЛЕМА:** Для скальпинга нужны более низкие пороги (0.2-0.4%)

---

### 3. ExitAnalyzer и Partial TP

**Файл:** `src/strategies/scalping/futures/positions/exit_analyzer.py` (строки 685-1758)

**Логика:**
1. ✅ `_get_partial_tp_params()` - получение параметров по режиму
2. ✅ `_check_adaptive_min_holding_for_partial_tp()` - проверка min_holding
3. ✅ Адаптивная `fraction` в зависимости от PnL
4. ✅ Возвращает `action: "partial_close"` с `fraction`

**Проблемы:**
1. ✅ Логика реализована правильно
2. ⚠️ **ПРОБЛЕМА:** ExitAnalyzer может не вызываться достаточно часто
3. ⚠️ **ПРОБЛЕМА:** Пороги могут быть слишком высокими для достижения

---

### 4. TrailingSLCoordinator и Partial TP

**Файл:** `src/strategies/scalping/futures/coordinators/trailing_sl_coordinator.py` (строки 788-791)

**Логика:**
```python
elif action == "partial_close":
    fraction = exit_decision.get("fraction", 0.5)
    logger.info(...)
    # Вызов close_partial_position
```

**Проблемы:**
1. ✅ Логика обработки `partial_close` есть
2. ⚠️ **ПРОБЛЕМА:** Может не вызываться, если ExitAnalyzer не возвращает решение

---

## ⚠️ НАЙДЕННЫЕ ПРОБЛЕМЫ

### 1. Partial TP не используется (КРИТИЧНО)
- **Проблема:** 0% позиций закрыто частично
- **Причина:** Пороги `trigger_percent` слишком высокие (0.6-1.0%)
- **Влияние:** Упущенная возможность фиксации прибыли
- **Приоритет:** КРИТИЧНО

### 2. Пороги trigger_percent слишком высокие (КРИТИЧНО)
- **Проблема:** 
  - Trending: 0.8%
  - Ranging: 1.0%
  - Choppy: 0.6%
- **Причина:** Пороги оптимизированы для качественной торговли, но слишком высокие для скальпинга
- **Влияние:** Partial TP не срабатывает, так как позиции закрываются раньше по другим механизмам
- **Приоритет:** КРИТИЧНО

### 3. Нет логирования Partial TP (ВЫСОКИЙ)
- **Проблема:** Нет логов о попытках Partial TP
- **Причина:** Логирование только на уровне DEBUG
- **Влияние:** Невозможно отследить, почему Partial TP не срабатывает
- **Приоритет:** ВЫСОКИЙ

### 4. Нет проверки применения параметров (СРЕДНИЙ)
- **Проблема:** Нет способа проверить, применяются ли параметры по режимам
- **Причина:** Нет логирования применения параметров
- **Влияние:** Невозможно проверить эффективность адаптивности
- **Приоритет:** СРЕДНИЙ

---

## 🎯 РЕКОМЕНДАЦИИ

### 1. Снизить пороги trigger_percent (КРИТИЧНО)
**Файл:** `config/config_futures.yaml`

**Изменить:**
```yaml
partial_tp:
  by_regime:
    trending:
      trigger_percent: 0.4  # Было 0.8, снизить до 0.4%
    ranging:
      trigger_percent: 0.3  # Было 1.0, снизить до 0.3%
    choppy:
      trigger_percent: 0.2  # Было 0.6, снизить до 0.2%
```

**Ожидаемый эффект:** Увеличение использования Partial TP с 0% до 20-30%

---

### 2. Добавить логирование Partial TP (ВЫСОКИЙ)
**Файл:** `src/strategies/scalping/futures/position_manager.py`

**Изменить:**
```python
# При попытке Partial TP логировать на уровне INFO
if ptp_enabled and not partial_done and size > 0 and pnl_percent > 0:
    logger.info(
        f"📊 Partial TP проверка {symbol}: pnl={pnl_percent:.2f}% / "
        f"триггер={ptp_trigger:.2f}% ({ptp_progress:.0f}%)"
    )
```

**Ожидаемый эффект:** Возможность отслеживания попыток Partial TP

---

### 3. Добавить проверку применения параметров (СРЕДНИЙ)
**Файл:** `src/strategies/scalping/futures/position_manager.py`

**Изменить:**
```python
# При применении параметров логировать
logger.debug(
    f"📊 Partial TP параметры для {symbol} ({regime}): "
    f"fraction={ptp_fraction:.1%}, trigger={ptp_trigger:.2f}%, "
    f"enabled={ptp_enabled}"
)
```

**Ожидаемый эффект:** Возможность проверки применения параметров

---

### 4. Проверить частоту вызова ExitAnalyzer (СРЕДНИЙ)
**Файл:** `src/strategies/scalping/futures/positions/position_monitor.py`

**Проверить:**
- Как часто вызывается `check_position()`
- Вызывается ли `ExitAnalyzer.analyze_position()`

**Ожидаемый эффект:** Убедиться, что ExitAnalyzer вызывается достаточно часто

---

## 📊 ОЦЕНКА ЭФФЕКТИВНОСТИ

### Текущее состояние:
- ⚠️ **Не используется:** 0% позиций закрыто частично
- ⚠️ **Пороги высокие:** 0.6-1.0% слишком много для скальпинга
- ✅ **Логика правильная:** Реализация корректна

### Потенциал улучшения:
- **После снижения порогов:** Использование Partial TP 20-30%
- **Улучшение win rate:** +2-3% за счет фиксации прибыли
- **Улучшение общего PnL:** +$20-30/день за счет частичного закрытия

---

## 🎯 ПРИОРИТЕТ ИСПРАВЛЕНИЙ

1. **КРИТИЧНО:** Снизить пороги trigger_percent (0.2-0.4% вместо 0.6-1.0%)
2. **ВЫСОКИЙ:** Добавить логирование Partial TP
3. **СРЕДНИЙ:** Добавить проверку применения параметров
4. **СРЕДНИЙ:** Проверить частоту вызова ExitAnalyzer

---

**Следующие шаги:**
1. Снизить пороги trigger_percent в конфиге
2. Добавить логирование Partial TP
3. Провести повторный анализ с новыми данными
4. Оптимизировать параметры на основе результатов
