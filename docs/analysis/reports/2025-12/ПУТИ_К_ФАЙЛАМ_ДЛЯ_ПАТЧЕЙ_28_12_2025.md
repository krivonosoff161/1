# ПУТИ К ФАЙЛАМ ДЛЯ ПАТЧЕЙ

**Дата:** 28.12.2025  
**Цель:** Точные пути и строки для исправлений #9, #13, #15

---

## 📁 ФАЙЛЫ ДЛЯ ПАТЧЕЙ

### 1. ✅ orchestrator.py — для #9 и #13

**Полный путь:**
```
src/strategies/scalping/futures/orchestrator.py
```

**Ключевые методы и строки:**

#### Метод `run_main_loop()` (делегирован в TradingControlCenter)
- **Файл:** `src/strategies/scalping/futures/core/trading_control_center.py`
- **Строка:** `127` — `async def run_main_loop(self) -> None:`
- **Описание:** Основной торговый цикл (бывший `run()` в orchestrator)
- **Для патча #9:** Добавить периодический вызов `_log_block_stats()` каждые 300 секунд

#### Метод `_sync_positions_with_exchange()`
- **Файл:** `src/strategies/scalping/futures/orchestrator.py`
- **Строка:** `1866` — `async def _sync_positions_with_exchange(self, force: bool = False) -> None:`
- **Диапазон строк:** `1866-2150` (приблизительно)
- **Для патча #13:** Добавить retry логику (3 попытки с задержками 0.5, 1.0, 2.0 сек)

**Как найти в коде:**
```python
# Строка 1866
async def _sync_positions_with_exchange(self, force: bool = False) -> None:
    """
    ✅ МОДЕРНИЗАЦИЯ: Синхронизирует локальные позиции и лимиты с фактическими данными биржи.
    """
    now = time.time()
    # ... код синхронизации ...
```

**Где вызывается:**
- Строка `963` — `await self._sync_positions_with_exchange(force=True)` (в `start()`)
- Строка `2591` — `await self._sync_positions_with_exchange()` (в обработке DRIFT_ADD)
- Строка `4639` — `await self._sync_positions_with_exchange(force=True)` (в другом месте)

---

### 2. ✅ signal_coordinator.py — для #9

**Полный путь:**
```
src/strategies/scalping/futures/coordinators/signal_coordinator.py
```

**Ключевые методы и строки:**

#### Метод `check_for_signals()`
- **Строка:** `1093` — `async def check_for_signals(self, symbol: str, price: float):`
- **Диапазон строк:** `1093-1857` (приблизительно, весь метод)
- **Для патча #9:** Добавить вызов `_log_block_stats()` в конце метода после обработки сигналов

**Как найти в коде:**
```python
# Строка 1093
async def check_for_signals(self, symbol: str, price: float):
    """✅ РЕАЛЬНАЯ генерация сигналов на основе индикаторов"""
    try:
        # ... код генерации и обработки сигналов ...
        # ✅ ДОБАВИТЬ В КОНЦЕ: self._log_block_stats()
```

#### Метод `_log_block_stats()`
- **Строка:** `647` — `def _log_block_stats(self):`
- **Диапазон строк:** `647-663`
- **Описание:** Логирует статистику блокировок сигналов

**Как найти в коде:**
```python
# Строка 647
def _log_block_stats(self):
    """
    ✅ НОВОЕ (28.12.2025): Периодическое логирование статистики блокировок сигналов.
    """
    total_blocked = sum(self._block_stats.values())
    if total_blocked > 0:
        logger.info(...)
```

#### Словарь `_block_stats`
- **Строка:** `137` — `self._block_stats = {`
- **Диапазон строк:** `137-144`
- **Описание:** Счетчики блокировок сигналов

**Как найти в коде:**
```python
# Строка 137 (в __init__)
self._block_stats = {
    "circuit_breaker": 0,
    "side_blocked": 0,
    "low_strength": 0,
    "existing_position": 0,
    "margin_unsafe": 0,
    "other": 0,
}
```

**Где обновляется:**
- Строка `177` — `self._block_stats["circuit_breaker"] += 1`
- Строка `194` — `self._block_stats["side_blocked"] += 1`
- Строка `203` — `self._block_stats["side_blocked"] += 1`
- Строка `278` — `self._block_stats["low_strength"] += 1`
- И другие места в `process_signals()` и `check_for_signals()`

---

### 3. ✅ talib_wrapper.py — для #15

**Полный путь:**
```
src/indicators/talib_wrapper.py
```

**Ключевые классы и строки:**

#### TALibRSI (✅ УЖЕ ИСПРАВЛЕН)
- **Строка:** `16` — `class TALibRSI(BaseIndicator):`
- **Строка:** `39-45` — Обработка ошибок с `logger.debug()` ✅
- **Статус:** Исправлен, использует `logger.debug()` вместо `logger.warning()`

#### TALibEMA (❌ НУЖНО ИСПРАВИТЬ)
- **Строка:** `66` — `class TALibEMA(BaseIndicator):`
- **Строка:** `88-94` — Обработка ошибок с `logger.warning()` ❌
- **Нужно:** Заменить `logger.warning()` на `logger.debug()`

**Как найти в коде:**
```python
# Строка 88
except Exception as e:
    # ✅ ЛОГИРОВАНИЕ: Логируем ошибку и используем fallback
    logger.warning(  # ❌ ЗАМЕНИТЬ НА logger.debug()
        f"⚠️ TALibEMA: Ошибка расчета через TA-Lib ({type(e).__name__}: {e}), используется fallback"
    )
```

#### TALibATR (❌ НУЖНО ИСПРАВИТЬ)
- **Строка:** `113` — `class TALibATR(BaseIndicator):`
- **Строка:** `143-149` — Обработка ошибок с `logger.warning()` ❌
- **Нужно:** Заменить `logger.warning()` на `logger.debug()`

**Как найти в коде:**
```python
# Строка 143
except Exception as e:
    # ✅ ЛОГИРОВАНИЕ: Логируем ошибку и используем fallback
    logger.warning(  # ❌ ЗАМЕНИТЬ НА logger.debug()
        f"⚠️ TALibATR: Ошибка расчета через TA-Lib ({type(e).__name__}: {e}), используется fallback"
    )
```

#### TALibMACD (❌ НУЖНО ИСПРАВИТЬ)
- **Строка:** `170` — `class TALibMACD(BaseIndicator):`
- **Строка:** `206-210` — Обработка ошибок с `logger.warning()` ❌
- **Нужно:** Заменить `logger.warning()` на `logger.debug()`

**Как найти в коде:**
```python
# Строка 206
except Exception as e:
    # ✅ ЛОГИРОВАНИЕ: Логируем ошибку и используем fallback
    logger.warning(  # ❌ ЗАМЕНИТЬ НА logger.debug()
        f"⚠️ TALibMACD: Ошибка расчета через TA-Lib ({type(e).__name__}: {e}), используется fallback"
    )
```

#### TALibSMA (❌ НУЖНО ИСПРАВИТЬ)
- **Строка:** `236` — `class TALibSMA(BaseIndicator):`
- **Строка:** `257-261` — Обработка ошибок с `logger.warning()` ❌
- **Нужно:** Заменить `logger.warning()` на `logger.debug()`

**Как найти в коде:**
```python
# Строка 257
except Exception as e:
    # ✅ ЛОГИРОВАНИЕ: Логируем ошибку и используем fallback
    logger.warning(  # ❌ ЗАМЕНИТЬ НА logger.debug()
        f"⚠️ TALibSMA: Ошибка расчета через TA-Lib ({type(e).__name__}: {e}), используется fallback"
    )
```

#### TALibBollingerBands (❌ НУЖНО ИСПРАВИТЬ)
- **Строка:** `282` — `class TALibBollingerBands(BaseIndicator):`
- **Строка:** `320-324` — Обработка ошибок с `logger.warning()` ❌
- **Нужно:** Заменить `logger.warning()` на `logger.debug()`

**Как найти в коде:**
```python
# Строка 320
except Exception as e:
    # ✅ ЛОГИРОВАНИЕ: Логируем ошибку и используем fallback
    logger.warning(  # ❌ ЗАМЕНИТЬ НА logger.debug()
        f"⚠️ TALibBollingerBands: Ошибка расчета через TA-Lib ({type(e).__name__}: {e}), используется fallback"
    )
```

---

## 📋 СВОДНАЯ ТАБЛИЦА ПАТЧЕЙ

| # | Файл | Метод/Класс | Строка | Что исправить |
|---|------|-------------|--------|---------------|
| **#9** | `signal_coordinator.py` | `check_for_signals()` | `1093` | Добавить `self._log_block_stats()` в конце метода |
| **#9** | `trading_control_center.py` | `run_main_loop()` | `127` | Добавить периодический вызов `_log_block_stats()` каждые 300 сек |
| **#13** | `orchestrator.py` | `_sync_positions_with_exchange()` | `1866` | Добавить retry логику (3 попытки: 0.5, 1.0, 2.0 сек) |
| **#15** | `talib_wrapper.py` | `TALibEMA` | `88-94` | Заменить `logger.warning()` на `logger.debug()` |
| **#15** | `talib_wrapper.py` | `TALibATR` | `143-149` | Заменить `logger.warning()` на `logger.debug()` |
| **#15** | `talib_wrapper.py` | `TALibMACD` | `206-210` | Заменить `logger.warning()` на `logger.debug()` |
| **#15** | `talib_wrapper.py` | `TALibSMA` | `257-261` | Заменить `logger.warning()` на `logger.debug()` |
| **#15** | `talib_wrapper.py` | `TALibBollingerBands` | `320-324` | Заменить `logger.warning()` на `logger.debug()` |

---

## 🔍 КАК НАЙТИ ФАЙЛЫ В IDE

### В VS Code / Cursor:

1. **Открыть файл по пути:**
   - Нажать `Ctrl+P` (или `Cmd+P` на Mac)
   - Ввести путь: `src/strategies/scalping/futures/orchestrator.py`
   - Нажать Enter

2. **Перейти к строке:**
   - Нажать `Ctrl+G` (или `Cmd+G` на Mac)
   - Ввести номер строки (например, `1866`)
   - Нажать Enter

3. **Поиск по методу:**
   - Нажать `Ctrl+F` (или `Cmd+F` на Mac)
   - Ввести название метода: `_sync_positions_with_exchange`
   - Найти все вхождения

### В PyCharm:

1. **Открыть файл:**
   - Нажать `Ctrl+Shift+N` (или `Cmd+Shift+O` на Mac)
   - Ввести имя файла: `orchestrator.py`
   - Выбрать нужный файл

2. **Перейти к строке:**
   - Нажать `Ctrl+G` (или `Cmd+L` на Mac)
   - Ввести номер строки

3. **Поиск по методу:**
   - Нажать `Ctrl+F` (или `Cmd+F` на Mac)
   - Ввести название метода

---

## ✅ ПРОВЕРКА ПУТЕЙ

Все файлы находятся в рабочей директории проекта:
```
C:\Users\krivo\simple trading bot okx\
```

**Относительные пути от корня проекта:**
- ✅ `src/strategies/scalping/futures/orchestrator.py`
- ✅ `src/strategies/scalping/futures/coordinators/signal_coordinator.py`
- ✅ `src/strategies/scalping/futures/core/trading_control_center.py`
- ✅ `src/indicators/talib_wrapper.py`

---

**Дата создания:** 28.12.2025  
**Версия:** 1.0  
**Статус:** ✅ Готово к использованию




