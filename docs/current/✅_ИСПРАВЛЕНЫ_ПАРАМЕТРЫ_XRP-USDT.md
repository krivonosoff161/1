# ✅ ИСПРАВЛЕНЫ ПАРАМЕТРЫ XRP-USDT

**Дата:** 23 ноября 2025, 02:20  
**Статус:** ✅ **ПАРАМЕТРЫ ИСПРАВЛЕНЫ**

---

## 🔍 НАЙДЕННАЯ ПРОБЛЕМА

### ⚠️ LiquidityFilter блокировал все сигналы XRP-USDT

**Из логов:**
```
LiquidityFilter: XRP-USDT отклонён — объём на лучшем bid 0 < 12 (regime=ranging)
LiquidityFilter: XRP-USDT отклонён — объём на лучшем bid 0 < 3 (regime=ranging, fail-open x0.25)
```

**Причина:**
- Объем на лучшем bid = **0** (или очень маленький)
- Пороги для XRP-USDT были **слишком высокими** для реальной ликвидности
- XRP-USDT имеет **меньшую ликвидность** чем BTC/ETH, но параметры были одинаковые

---

## ✅ ИСПРАВЛЕНИЯ

### 1. Снижены пороги Liquidity Filter для XRP-USDT

#### RANGING режим:
**Было:**
```yaml
filters:
  liquidity:
    min_best_bid_volume_usd: 12
    min_best_ask_volume_usd: 12
    min_orderbook_depth_usd: 192
```

**Стало:**
```yaml
filters:
  liquidity:
    min_best_bid_volume_usd: 5   # ✅ Снижено с 12 до 5
    min_best_ask_volume_usd: 5   # ✅ Снижено с 12 до 5
    min_orderbook_depth_usd: 128 # ✅ Снижено с 192 до 128
```

#### TRENDING режим:
**Было:**
```yaml
filters:
  liquidity:
    min_best_bid_volume_usd: 18
    min_best_ask_volume_usd: 18
    min_orderbook_depth_usd: 280
```

**Стало:**
```yaml
filters:
  liquidity:
    min_best_bid_volume_usd: 8   # ✅ Снижено с 18 до 8
    min_best_ask_volume_usd: 8   # ✅ Снижено с 18 до 8
    min_orderbook_depth_usd: 200 # ✅ Снижено с 280 до 200
```

#### CHOPPY режим:
**Уже было правильно:** `min_best_bid_volume_usd: 5` (не изменено)

#### Symbol Overrides (глобальные):
**Было:**
```yaml
symbol_overrides:
  XRP-USDT:
    min_best_bid_volume_usd: 12
    min_best_ask_volume_usd: 12
    min_orderbook_depth_usd: 200
```

**Стало:**
```yaml
symbol_overrides:
  XRP-USDT:
    min_best_bid_volume_usd: 5   # ✅ Снижено с 12 до 5
    min_best_ask_volume_usd: 5   # ✅ Снижено с 12 до 5
    min_orderbook_depth_usd: 128 # ✅ Снижено с 200 до 128
```

---

## 📊 СРАВНЕНИЕ ПАРАМЕТРОВ ПОСЛЕ ИСПРАВЛЕНИЯ

### Liquidity Filter параметры для RANGING режима:

| Символ | min_best_bid_volume_usd | min_best_ask_volume_usd | min_orderbook_depth_usd |
|--------|------------------------|-------------------------|------------------------|
| **BTC-USDT** | 128 (ranging) | 128 (ranging) | 1120 (ranging) |
| **ETH-USDT** | 88 (ranging) | 88 (ranging) | 800 (ranging) |
| **SOL-USDT** | 40 (ranging) | 40 (ranging) | 340 (ranging) |
| **DOGE-USDT** | 10 (ranging) | 10 (ranging) | 180 (ranging) |
| **XRP-USDT** | **5 (ranging)** ✅ | **5 (ranging)** ✅ | **128 (ranging)** ✅ |

**Вывод:** Теперь XRP-USDT имеет **самые низкие пороги** среди всех символов, что соответствует его реальной ликвидности.

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После исправления:
- ✅ XRP-USDT **не будет блокироваться** LiquidityFilter при объеме bid/ask > 5 USD
- ✅ Сигналы будут **проходить фильтры** и открывать позиции
- ✅ Параметры **адаптивные** по режиму и символу

---

## 📋 ПРОВЕРКА АДАПТИВНОСТИ

**Приоритет параметров в LiquidityFilter:**
1. **thresholds_override** (из `symbol_profiles[symbol][regime].filters.liquidity`) - **HIGHEST PRIORITY** ✅
2. **regime_multipliers** (множители для режима) ✅
3. **symbol_overrides** (per-symbol переопределения) ✅
4. **base** (глобальные параметры) ✅

**Подтверждение:**
- ✅ Параметры из `symbol_profiles.XRP-USDT.ranging.filters.liquidity` имеют **высший приоритет**
- ✅ Они применяются через `thresholds_override` в `LiquidityFilter.evaluate()`
- ✅ Адаптивность работает правильно

---

## ✅ ВЫВОД

**Параметры исправлены!**

1. ✅ Пороги Liquidity Filter для XRP-USDT снижены до реальных значений
2. ✅ Параметры адаптивные по режиму (ranging/trending/choppy)
3. ✅ XRP-USDT теперь имеет самые низкие пороги среди всех символов
4. ✅ Сигналы должны проходить фильтры и открывать позиции

**Статус:** ✅ **ПАРАМЕТРЫ ИСПРАВЛЕНЫ, ТРЕБУЕТСЯ ПЕРЕЗАПУСК БОТА**

