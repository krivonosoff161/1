# 🔍 АНАЛИЗ ПРОБЛЕМ: LEVERAGE И DRIFT

**Дата анализа:** 2025-12-21  
**Источник:** Логи бота за 2025-12-20

---

## 📋 НАЙДЕННЫЕ ПРОБЛЕМЫ

### 🔴 ПРОБЛЕМА #1: Ошибка форматирования в AdaptiveLeverage

**Файл:** `src/strategies/scalping/futures/risk/adaptive_leverage.py:109-110`

**Ошибка:**
```
ERROR | calculate_leverage:117 | ❌ Ошибка расчета адаптивного левериджа: Invalid format specifier '.4f if volatility else 'N/A'' for object of type 'float'
```

**Причина:**
В строке 109 используется неправильный синтаксис форматирования:
```python
f"volatility={volatility:.4f if volatility else 'N/A'}, "
```

Проблема: нельзя использовать тернарный оператор внутри `.4f` форматирования.

**Исправление:**
Нужно сначала проверить volatility, затем форматировать:
```python
volatility_str = f"{volatility:.4f}" if volatility else "N/A"
f"volatility={volatility_str}, "
```

---

### 🟠 ПРОБЛЕМА #2: DRIFT_ADD / DRIFT_REMOVE для XRP-USDT

**Файл:** `src/strategies/scalping/futures/orchestrator.py:_sync_positions_with_exchange`

**Логи:**
```
00:02:27.081 | WARNING | DRIFT_REMOVE XRP-USDT not on exchange
00:02:58.570 | CRITICAL | DRIFT_ADD XRP-USDT found on exchange but not in registry
00:02:58.574 | WARNING | DRIFT_ADD_SYNCED XRP-USDT force-registered in PositionRegistry
```

**Проблема:**
1. Позиция XRP-USDT появляется и исчезает
2. Сначала DRIFT_REMOVE (позиция в реестре, но не на бирже)
3. Через 31 секунду DRIFT_ADD (позиция на бирже, но не в реестре)
4. Позиция синхронизируется, но это указывает на проблему синхронизации

**Возможные причины:**
1. **Race condition** - позиция открывается/закрывается между проверками
2. **Задержка в обновлении** - биржа обновляет данные с задержкой
3. **Проблема с locks** - `_drift_locks` может не работать правильно
4. **Частичное исполнение ордера** - позиция появляется постепенно

**Что нужно проверить:**
- Работает ли `_drift_locks` правильно
- Частота синхронизации (может быть слишком часто)
- Логика обработки частично исполненных ордеров

---

### 🟡 ПРОБЛЕМА #3: Leverage читается как 0x

**Файл:** `src/strategies/scalping/futures/position_manager.py:_check_position_safety`

**В логах:**
```
Leverage: актуальный=0x, конфиг=5x, используем 5x для расчетов
```

**Код:**
```python
leverage_from_position = int(position.get("lever", "0"))
```

**Проблема:**
- `lever` из позиции возвращает 0 или не существует
- Это может означать что:
  1. Поле `lever` не приходит от биржи
  2. Поле называется по-другому
  3. Позиция еще не полностью загружена

**Что проверить:**
- Какое поле использует OKX API для leverage в позициях
- Может быть `leverage` вместо `lever`?
- Нужно проверить структуру ответа от `get_positions()`

---

### 🟢 ПРОБЛЕМА #4: Неправильный расчет position_value

**В логах:**
```
XRP-USDT: position_value=20832.23 USD (при размере 108.0 контрактов)
```

**Проблема:**
Для XRP-USDT:
- `size=108.0` контрактов
- `ctVal=100.0` (по логам)
- `size_in_coins = 108.0 * 100.0 = 10800.0` монет
- `current_price=1.93`
- `position_value = 10800.0 * 1.93 = 20844 USD` ✅ (почти правильно)

Но потом в другом месте:
- `position_value=208.40 USD` - это НЕПРАВИЛЬНО!

**Разница в 100 раз!** Это указывает на проблему расчета.

**Что проверить:**
- Как рассчитывается `position_value` в разных местах
- Может быть используется размер в контрактах вместо монет?
- Нужно проверить логику в `_check_position_safety`

---

## 📊 СТАТИСТИКА ПРОБЛЕМ

| Проблема | Частота | Критичность | Статус |
|----------|---------|-------------|--------|
| Ошибка форматирования leverage | 2 раза | Средняя | ❌ Требует исправления |
| DRIFT_ADD/REMOVE | 1 эпизод (XRP) | Высокая | ⚠️ Требует исследования |
| Leverage = 0x | Много раз | Средняя | ⚠️ Требует проверки |
| Неправильный position_value | Много раз | Высокая | ⚠️ Требует проверки |

---

## 🔍 ДЕТАЛЬНЫЙ АНАЛИЗ

### Проблема с leverage форматированием

**Текущий код (строка 109):**
```python
f"volatility={volatility:.4f if volatility else 'N/A'}, "
```

**Исправление:**
```python
volatility_str = f"{volatility:.4f}" if volatility else "N/A"
f"volatility={volatility_str}, "
```

---

### Проблема с DRIFT

**Хронология:**
1. `00:02:27` - DRIFT_REMOVE XRP-USDT (позиция исчезла из биржи)
2. `00:02:58` - DRIFT_ADD XRP-USDT (позиция появилась на бирже)
3. `00:02:58` - DRIFT_ADD_SYNCED (позиция синхронизирована)

**Возможные сценарии:**
1. Позиция была закрыта и сразу открыта заново
2. Проблема с кэшированием данных биржи
3. Race condition в логике синхронизации

**Что нужно:**
- Проверить логику в `_sync_positions_with_exchange`
- Убедиться что `_drift_locks` работает правильно
- Добавить больше логирования для понимания что происходит

---

### Проблема с position_value

**Наблюдения из логов:**
- Иногда `position_value=20832.23 USD` (правильно)
- Иногда `position_value=208.40 USD` (неправильно, в 100 раз меньше)

**Где может быть ошибка:**
- В расчете `size_in_coins` - может использоваться неправильный `ctVal`
- В умножении `size_in_coins * current_price`
- Разные места в коде используют разную логику расчета

---

## ✅ РЕКОМЕНДАЦИИ

### Срочные исправления:
1. ✅ Исправить форматирование в `adaptive_leverage.py:109`
2. ⚠️ Проверить почему leverage читается как 0
3. ⚠️ Проверить расчет position_value в разных местах

### Исследования:
1. Добавить детальное логирование для DRIFT событий
2. Проверить структуру данных от OKX API для позиций
3. Убедиться что `ctVal` правильно используется во всех местах

---

## 📝 ЗАМЕТКИ

- Бот работает, но есть проблемы синхронизации
- DRIFT события происходят, но система их обрабатывает
- Leverage используется из конфига (fallback работает), но чтение с биржи не работает
- Position_value иногда неправильно рассчитывается

---

**Статус:** ⚠️ **ТРЕБУЕТСЯ ИСПРАВЛЕНИЕ И ДОПОЛНИТЕЛЬНОЕ ИССЛЕДОВАНИЕ**

