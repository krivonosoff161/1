# АНАЛИЗ FALLBACK ЗНАЧЕНИЙ

**Дата:** 23 ноября 2025

---

## 📊 НАЙДЕННЫЕ FALLBACK ЗНАЧЕНИЯ

### 1. ⚠️ Limit Offset для BTC-USDT (ОДИН РАЗ)
**Время:** 14:29:37  
**Сообщение:**
```
⚠️ FALLBACK: Используется глобальный offset из конфига: 0.0% 
(per-symbol+regime и per-regime не найдены для BTC-USDT, regime=ranging)
```

**Причина:** Это было при открытии позиции BTC-USDT LONG.  
**Статус:** ✅ **НЕ КРИТИЧНО** - это было один раз, потом все работает нормально.

---

### 2. ✅ Margin Calculator Fallback (НОРМАЛЬНО)
**Сообщение:**
```
🔍 regime=None, используем fallback: 'ranging'
✅ Загружен safety_threshold=1.3 из конфига (regime=ranging (fallback))
```

**Причина:** Это безопасный fallback для проверки безопасности позиций, когда режим не определен.  
**Статус:** ✅ **НОРМАЛЬНО** - это правильное поведение для безопасности.

---

### 3. ✅ TP Параметры (ВСЕ АДАПТИВНЫЕ)
**Логи показывают:**
- ✅ Per-regime TP для BTC-USDT (ranging): 5.0%
- ✅ Per-regime TP для ETH-USDT (ranging): 3.5%
- ✅ Per-regime TP для SOL-USDT (ranging): 3.5%
- ✅ Per-regime TP для DOGE-USDT (ranging): 4.0%
- 📊 Per-symbol TP для BTC-USDT: 2.0% (используется когда режим не определен)

**Статус:** ✅ **ВСЕ АДАПТИВНЫЕ** - fallback к глобальному TP используется только если режим не найден.

---

## ✅ ВЫВОД

### Fallback значения:
1. **Limit Offset:** Один раз использовался fallback для BTC-USDT (не критично)
2. **Margin Calculator:** Fallback к 'ranging' используется нормально (безопасность)
3. **TP:** Все адаптивные, fallback только если режим не найден

### Нужен ли перезапуск?
**НЕТ, перезапуск НЕ НУЖЕН!**

**Причины:**
1. ✅ Все параметры уже адаптивные и работают правильно
2. ✅ Fallback значения используются только в безопасных случаях
3. ✅ Один раз fallback для limit_offset был при открытии позиции, но это не критично
4. ✅ Все остальные параметры (TP, TSL, Filters) работают адаптивно

---

## 📊 СТАТУС

**Статус:** ✅ **ВСЕ РАБОТАЕТ ПРАВИЛЬНО**

- ✅ Fallback значения используются только в безопасных случаях
- ✅ Все критичные параметры адаптивные
- ✅ Перезапуск не требуется

---

**Рекомендация:** Продолжать работу без перезапуска. Все работает правильно!

