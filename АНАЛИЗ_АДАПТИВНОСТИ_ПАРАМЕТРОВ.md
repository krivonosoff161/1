# Полный Анализ Адаптивности Параметров TSL и Рисков

## 🔍 ПРОБЛЕМА: Не все параметры адаптивны по режимам рынка!

### ❌ КРИТИЧЕСКИЕ ПРОБЛЕМЫ:

#### 1. **Trailing Stop Loss - НЕ АДАПТИВНЫЕ параметры:**

**В конфиге (`trailing_sl`):**
- ✅ `max_trail` - АДАПТИВНО (есть в `by_regime`)
- ✅ `min_profit_to_close` - АДАПТИВНО (есть в `by_regime`)
- ✅ `min_holding_minutes` - АДАПТИВНО (есть в `by_regime`)
- ✅ `extend_time_multiplier` - АДАПТИВНО (есть в `by_regime`)
- ✅ `regime_multiplier` - АДАПТИВНО (есть в `by_regime`)
- ✅ `trend_strength_boost` - АДАПТИВНО (есть в `by_regime`)
- ✅ `high_profit_threshold` - АДАПТИВНО (есть в `by_regime`)

**❌ НЕ АДАПТИВНЫЕ (одинаковые для всех режимов):**
- ❌ `initial_trail: 0.005` (0.5%) - **ОДИНАКОВЫЙ для всех режимов!**
- ❌ `loss_cut_percent: 1.5` (1.5%) - **ОДИНАКОВЫЙ для всех режимов!**
- ❌ `timeout_loss_percent: 1.0` (1.0%) - **ОДИНАКОВЫЙ для всех режимов!**
- ❌ `timeout_minutes: 5` (5 мин) - **ОДИНАКОВЫЙ для всех режимов!**
- ❌ `min_trail: 0.003` (0.3%) - **ОДИНАКОВЫЙ для всех режимов!**

**В коде (`_get_trailing_sl_params`):**
- ✅ Читает `max_trail`, `min_profit_to_close`, `min_holding_minutes`, `extend_time_multiplier`, `regime_multiplier`, `trend_strength_boost`, `high_profit_threshold` из `by_regime`
- ❌ **НЕ читает** `initial_trail`, `loss_cut_percent`, `timeout_loss_percent`, `timeout_minutes`, `min_trail` из `by_regime`!

### 📊 ТЕКУЩИЕ ПРОБЛЕМЫ ИЗ ЛОГОВ:

1. **ETH-USDT**: закрыта через 3 секунды с убытком -2.09% (превысил `loss_cut_percent: 1.5%`)
2. **SOL-USDT**: закрыта через 2 секунды с убытком -0.72% (превысил `loss_cut_percent: 1.5%`)
3. **Проблема**: `initial_trail: 0.5%` слишком маленький для скальпинга - цена сразу уходит против позиции
4. **Проблема**: `loss_cut_percent: 1.5%` слишком строгий для скальпинга - позиции закрываются слишком быстро

### ✅ РЕКОМЕНДАЦИИ ПО АДАПТАЦИИ:

#### 1. **`initial_trail` - должен быть адаптивным:**

**Текущее значение:** `0.005` (0.5%) - одинаковый для всех режимов

**Рекомендуемые значения по режимам:**
- **TRENDING**: `0.01` (1.0%) - больше места для откатов в тренде
- **RANGING**: `0.008` (0.8%) - стандартное значение для боковика
- **CHOPPY**: `0.006` (0.6%) - более строго для хаоса

**Обоснование:**
- В тренде цена может откатывать больше, нужен больший `initial_trail`
- В боковике стандартное значение
- В хаосе более строго, чтобы быстрее закрывать убыточные позиции

#### 2. **`loss_cut_percent` - должен быть адаптивным:**

**Текущее значение:** `1.5%` - одинаковый для всех режимов

**Рекомендуемые значения по режимам:**
- **TRENDING**: `2.0%` - больше терпения в тренде (может быть временный откат)
- **RANGING**: `1.8%` - стандартное значение для боковика
- **CHOPPY**: `1.2%` - более строго для хаоса (быстрее закрывать убытки)

**Обоснование:**
- В тренде можно дать больше места для временных откатов
- В боковике стандартное значение
- В хаосе нужно быстрее закрывать убытки

#### 3. **`timeout_loss_percent` - должен быть адаптивным:**

**Текущее значение:** `1.0%` - одинаковый для всех режимов

**Рекомендуемые значения по режимам:**
- **TRENDING**: `1.5%` - больше терпения в тренде
- **RANGING**: `1.2%` - стандартное значение для боковика
- **CHOPPY**: `0.8%` - более строго для хаоса

#### 4. **`timeout_minutes` - должен быть адаптивным:**

**Текущее значение:** `5` минут - одинаковый для всех режимов

**Рекомендуемые значения по режимам:**
- **TRENDING**: `7` минут - больше времени в тренде
- **RANGING**: `5` минут - стандартное значение
- **CHOPPY**: `3` минуты - быстрее закрывать в хаосе

#### 5. **`min_trail` - должен быть адаптивным:**

**Текущее значение:** `0.003` (0.3%) - одинаковый для всех режимов

**Рекомендуемые значения по режимам:**
- **TRENDING**: `0.005` (0.5%) - больше минимального трейла в тренде
- **RANGING**: `0.003` (0.3%) - стандартное значение
- **CHOPPY**: `0.002` (0.2%) - более строго для хаоса

### 🔧 ЧТО НУЖНО ИСПРАВИТЬ:

1. **В конфиге (`config_futures.yaml`):**
   - Добавить `initial_trail`, `loss_cut_percent`, `timeout_loss_percent`, `timeout_minutes`, `min_trail` в `by_regime` для каждого режима

2. **В коде (`orchestrator.py`):**
   - Обновить `_get_trailing_sl_params` чтобы читать эти параметры из `by_regime`

3. **Проверить другие параметры:**
   - Проверить все остальные параметры на адаптивность

---

## 📋 ПОЛНЫЙ АУДИТ ВСЕХ ПАРАМЕТРОВ НА АДАПТИВНОСТЬ

### ✅ АДАПТИВНЫЕ ПАРАМЕТРЫ (имеют `by_regime`):

1. **Trailing Stop Loss:**
   - ✅ `max_trail` - адаптивен
   - ✅ `min_profit_to_close` - адаптивен
   - ✅ `min_holding_minutes` - адаптивен
   - ✅ `extend_time_multiplier` - адаптивен
   - ✅ `regime_multiplier` - адаптивен
   - ✅ `trend_strength_boost` - адаптивен
   - ✅ `high_profit_threshold` - адаптивен

2. **Momentum Filter:**
   - ✅ `max_cv` - адаптивен
   - ✅ `min_increasing_ratio` - адаптивен
   - ✅ `min_ratio` - адаптивен

3. **Liquidity Filter:**
   - ✅ `regime_multipliers` - адаптивен (min_daily_volume_multiplier, min_best_bid_volume_multiplier, и т.д.)

4. **Order Flow Filter:**
   - ✅ `regime_profiles` - адаптивен (window, long_threshold, short_threshold, min_total_depth_usd)

5. **Funding Rate Filter:**
   - ✅ `by_regime.max_funding_rate` - адаптивен

6. **Emergency Stop:**
   - ✅ `by_regime.min_lock_minutes` - адаптивен
   - ✅ `by_regime.unlock_threshold_percent` - адаптивен

7. **Margin Calculator:**
   - ✅ `by_regime.safety_threshold` - адаптивен
   - ✅ `by_regime.risk_percentage` - адаптивен
   - ✅ `by_regime.margin_health_thresholds` - адаптивен

8. **Delays:**
   - ✅ `by_regime.api_request_delay_ms` - адаптивен
   - ✅ `by_regime.symbol_switch_delay_ms` - адаптивен
   - ✅ `by_regime.position_sync_delay_ms` - адаптивен

9. **Liquidation Guard:**
   - ✅ `by_regime.warning_threshold` - адаптивен
   - ✅ `by_regime.danger_threshold` - адаптивен
   - ✅ `by_regime.critical_threshold` - адаптивен
   - ✅ `by_regime.auto_close_threshold` - адаптивен

10. **Slippage Guard:**
    - ✅ `by_regime.max_slippage_percent` - адаптивен
    - ✅ `by_regime.max_spread_percent` - адаптивен
    - ✅ `by_regime.order_timeout` - адаптивен

11. **Signal Generator:**
    - ✅ `confidence` (bullish_strong, bullish_normal, bearish_strong, bearish_normal, macd_signal, rsi_signal) - адаптивен
    - ✅ `thresholds.by_regime` (correlation_threshold, pivot_tolerance, volume_profile_va_percent, volume_profile_poc_tolerance, min_signal_strength) - адаптивен

12. **FastADX:**
    - ✅ `by_regime.threshold` - адаптивен

13. **Risk Parameters:**
    - ✅ `by_regime.max_loss_per_trade_percent` - адаптивен
    - ✅ `by_regime.max_margin_percent` - адаптивен
    - ✅ `by_regime.max_drawdown_percent` - адаптивен

14. **Adaptive Regime:**
    - ✅ Все параметры режимов (trending/ranging/choppy) - адаптивны

---

### ❌ НЕ АДАПТИВНЫЕ ПАРАМЕТРЫ (НЕ имеют `by_regime`):

#### 🔴 КРИТИЧЕСКИЕ (влияют на закрытие позиций):

1. **Trailing Stop Loss:**
   - ❌ `initial_trail: 0.005` (0.5%) - **ОДИНАКОВЫЙ для всех режимов!**
   - ❌ `loss_cut_percent: 1.5` (1.5%) - **ОДИНАКОВЫЙ для всех режимов!**
   - ❌ `timeout_loss_percent: 1.0` (1.0%) - **ОДИНАКОВЫЙ для всех режимов!**
   - ❌ `timeout_minutes: 5` (5 мин) - **ОДИНАКОВЫЙ для всех режимов!**
   - ❌ `min_trail: 0.003` (0.3%) - **ОДИНАКОВЫЙ для всех режимов!**
   - ❌ `trading_fee_rate: 0.0010` (0.10%) - одинаковый (это нормально, комиссия биржи)
   - ❌ `check_interval_seconds: 1.5` - одинаковый (можно сделать адаптивным)

2. **Trailing Stop Loss - дополнительные параметры:**
   - ❌ `short_reversal_protection.min_reversal_duration_seconds: 30` - одинаковый
   - ❌ `short_reversal_protection.max_reversal_percent: 0.5` - одинаковый
   - ❌ `trail_growth.low_profit_multiplier: 1.5` - одинаковый
   - ❌ `trail_growth.medium_profit_multiplier: 2.0` - одинаковый
   - ❌ `trail_growth.high_profit_multiplier: 3.0` - одинаковый
   - ❌ `extend_time_on_profit: true` - одинаковый (можно сделать адаптивным)

#### 🟡 ВАЖНЫЕ (влияют на фильтрацию сигналов):

3. **Order Executor:**
   - ❌ `limit_order.max_wait_seconds: 60` - одинаковый
   - ❌ `limit_order.auto_cancel_enabled: true` - одинаковый
   - ❌ `limit_order.auto_replace_enabled: true` - одинаковый
   - ❌ `limit_order.replace_with_market: true` - одинаковый
   - ❌ `limit_order.post_only: false` - одинаковый

4. **Funding Rate Monitor:**
   - ❌ `max_funding_rate: 0.0075` (0.75%) - одинаковый (есть `funding_filter.by_regime`, но не `funding_monitor.by_regime`)

5. **Funding Rate Filter:**
   - ❌ `include_next_funding: true` - одинаковый
   - ❌ `refresh_interval_seconds: 300` - одинаковый

6. **Liquidity Filter:**
   - ❌ `depth_levels: 5` - одинаковый
   - ❌ `refresh_interval_seconds: 20` - одинаковый
   - ❌ `fail_open_enabled: true` - одинаковый
   - ❌ `max_consecutive_blocks: 5` - одинаковый
   - ❌ `relax_multiplier: 0.25` - одинаковый
   - ❌ `relax_duration_seconds: 45` - одинаковый

7. **Order Flow Filter:**
   - ❌ `refresh_interval_seconds: 15` - одинаковый
   - ❌ `fail_open_enabled: true` - одинаковый
   - ❌ `max_consecutive_blocks: 4` - одинаковый
   - ❌ `relax_multiplier: 0.25` - одинаковый
   - ❌ `relax_duration_seconds: 30` - одинаковый

8. **Volatility Filter:**
   - ❌ `lookback_candles: 21` - одинаковый
   - ❌ `min_range_percent: 0.1` - одинаковый
   - ❌ `max_range_percent: 5.0` - одинаковый
   - ❌ `min_atr_percent: 0.02` - одинаковый
   - ❌ `max_atr_percent: 2.5` - одинаковый
   - ❌ **НЕТ `by_regime` вообще!** - все параметры одинаковые для всех режимов!

9. **Momentum Filter:**
   - ❌ `reject_vertical_spikes: true` - одинаковый
   - ❌ `max_spike_percent: 1.0` - одинаковый
   - ❌ `spike_lookback_periods: 5` - одинаковый
   - ❌ `check_grind_into_level: true` - одинаковый
   - ❌ `grind_lookback_periods: 20` - одинаковый
   - ❌ `max_price_velocity_percent: 0.5` - одинаковый
   - ❌ `check_volume_increasing: true` - одинаковый
   - ❌ `volume_increase_periods: 10` - одинаковый
   - ❌ `check_staircase_action: true` - одинаковый
   - ❌ `min_staircase_duration_hours: 2` - одинаковый
   - ❌ `reject_decreasing_volume: true` - одинаковый
   - ❌ `reject_choppy_action: true` - одинаковый

10. **Max Size Limiter:**
    - ❌ `max_single_size_usd: 150.0` - одинаковый
    - ❌ `max_total_size_usd: 600.0` - одинаковый
    - ❌ `max_positions: 5` - одинаковый
    - ❌ `size_step_*` - одинаковые для всех символов

11. **Signal Generator:**
    - ❌ `price_change_threshold: 0.0005` - одинаковый (есть в конфиге, но не адаптивен по режимам)
    - ❌ `strength_multiplier: 2000.0` - одинаковый
    - ❌ `strength_reduction_neutral: 0.9` - одинаковый
    - ❌ `base_signal_strength: 0.001` - одинаковый

12. **Position Manager:**
    - ❌ `false_trigger_protection.margin_ratio_threshold: 1.5` - одинаковый
    - ❌ `false_trigger_protection.pnl_threshold: 10.0` - одинаковый
    - ❌ `false_trigger_protection.margin_ratio_minimum: 0.5` - одинаковый
    - ❌ `tp_extension.enabled: true` - одинаковый
    - ❌ `tp_extension.min_trend_strength: 0.7` - одинаковый
    - ❌ `tp_extension.extension_step: 0.5` - одинаковый
    - ❌ `tp_extension.max_tp_percent: 5.0` - одинаковый
    - ❌ `tp_extension.use_indicators: true` - одинаковый
    - ❌ `reversal_detection.enabled: true` - одинаковый
    - ❌ `reversal_detection.min_reversal_duration_seconds: 60` - одинаковый
    - ❌ `reversal_detection.rsi_check: true` - одинаковый
    - ❌ `reversal_detection.macd_check: true` - одинаковый
    - ❌ `reversal_detection.bollinger_check: true` - одинаковый

13. **Order Flow (базовые):**
    - ❌ `window: 20` - одинаковый (есть в `regime_profiles`, но базовый не адаптивен)
    - ❌ `long_threshold: 0.004` - одинаковый (есть в `regime_profiles`, но базовый не адаптивен)
    - ❌ `short_threshold: -0.004` - одинаковый (есть в `regime_profiles`, но базовый не адаптивен)
    - ❌ `min_total_depth_usd: 1000` - одинаковый (есть в `regime_profiles`, но базовый не адаптивен)

14. **Micro Pivot Calculator:**
    - ❌ `timeframe: "15m"` - одинаковый
    - ❌ `max_distance_pct: 0.3` - одинаковый

15. **Impulse Trading:**
    - ❌ Все параметры одинаковые (lookback_candles, min_body_atr_ratio, и т.д.)
    - ❌ **НЕТ `by_regime` вообще!**

16. **Commission & Fees:**
    - ❌ `tp_buffer_percent: 0.1` - одинаковый (это нормально, комиссия биржи)

17. **Margin Settings:**
    - ❌ `maintenance_margin_ratio: 0.01` - одинаковый (это нормально, параметр биржи)
    - ❌ `initial_margin_ratio: 0.1` - одинаковый (это нормально, параметр биржи)

---

## 🎯 ПРИОРИТЕТЫ ИСПРАВЛЕНИЯ:

### 🔴 ПРИОРИТЕТ 1 (КРИТИЧНО - влияют на закрытие позиций):
1. **Trailing Stop Loss:**
   - `initial_trail` - добавить в `by_regime`
   - `loss_cut_percent` - добавить в `by_regime`
   - `timeout_loss_percent` - добавить в `by_regime`
   - `timeout_minutes` - добавить в `by_regime`
   - `min_trail` - добавить в `by_regime`

### 🟡 ПРИОРИТЕТ 2 (ВАЖНО - влияют на фильтрацию):
2. **Volatility Filter:**
   - Добавить `by_regime` для всех параметров

3. **Momentum Filter:**
   - Добавить `by_regime` для основных параметров (max_spike_percent, max_price_velocity_percent, и т.д.)

4. **Order Executor:**
   - Добавить `by_regime` для `max_wait_seconds` (можно быстрее в тренде, медленнее в хаосе)

### 🟢 ПРИОРИТЕТ 3 (ЖЕЛАТЕЛЬНО):
5. **Position Manager:**
   - Добавить `by_regime` для `tp_extension` и `reversal_detection`

6. **Signal Generator:**
   - Добавить `by_regime` для `price_change_threshold`, `strength_multiplier`, `strength_reduction_neutral`

7. **Impulse Trading:**
   - Добавить `by_regime` для основных параметров

---

## 📊 СТАТИСТИКА:

- **Адаптивных параметров:** ~50+
- **Неадаптивных параметров:** ~80+
- **Критических неадаптивных:** 5 (в Trailing Stop Loss)
- **Важных неадаптивных:** ~30 (в фильтрах и генераторах сигналов)

### 📋 ПЛАН ДЕЙСТВИЙ:

1. ✅ Добавить адаптивные параметры в конфиг
2. ✅ Обновить код для чтения адаптивных параметров
3. ✅ Протестировать на реальных данных
4. ✅ Проверить логи после изменений

