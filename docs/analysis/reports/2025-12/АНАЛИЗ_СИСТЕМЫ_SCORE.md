# 🔍 Анализ системы score

## ❌ ПРОБЛЕМА: Не нашел где считается score с максимумом 60!

### Что нашел:

1. **В spot версии:** Есть система score с максимумом **12 баллов**
   - `long_score / 12.0` - видно в коде
   - Бонусы добавляются к score

2. **В futures версии:** 
   - ❌ **НЕ НАШЕЛ** где считается score с максимумом 60
   - ❌ **НЕ НАШЕЛ** где проверяется `min_score_threshold` против score сигнала
   - ✅ Нашел `min_score_threshold` в `RegimeParameters` (int)
   - ✅ Нашел `min_score_threshold: 1.4` в конфиге для ranging

3. **В конфиге:**
   - `min_score_threshold: 1.4` для ranging
   - `min_score_threshold: 3.0` fallback в коде

### Где должен быть score:

**Sonnet говорит:** Score 9.8/60 означает "9.8 из 60 максимум"

**Но в коде:**
- Не нашел где считается score с максимумом 60
- Не нашел где проверяется `if score >= min_score_threshold`
- Не нашел где добавляются бонусы к score

### Возможные варианты:

1. **Score не используется** - возможно система изменилась и score больше не используется
2. **Score считается в другом месте** - может быть в старом коде или в другом модуле
3. **Sonnet ошибся** - возможно он смотрел на другую систему или старую версию

---

## ✅ Что точно есть:

1. **min_score_threshold** в `RegimeParameters`:
   ```python
   min_score_threshold: int  # В коде
   min_score_threshold: 1.4  # В конфиге (float!)
   ```

2. **min_signal_strength** проверяется:
   ```python
   if strength < min_strength:
       # Блокирует сигнал
   ```

3. **Бонусы от фильтров:**
   - `volume_profile_bonus` добавляется к сигналу
   - `pivot_bonus` добавляется к сигналу
   - Но не вижу где они суммируются в общий score

---

## ❓ Вопросы:

1. **Где считается score сигнала?**
2. **Какой максимум score?** (60? 12? Другой?)
3. **Где проверяется `min_score_threshold`?**
4. **Как бонусы от фильтров влияют на score?**

---

## 🔧 Рекомендации:

1. **Проверить логи** - может там есть информация о score
2. **Проверить старый код** - может система score была удалена
3. **Спросить у пользователя** - может он знает где считается score

---

**Дата:** 23 декабря 2025  
**Статус:** ❌ **НЕ НАЙДЕНО - требуется дополнительное исследование**





