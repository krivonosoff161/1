# 🔍 АНАЛИЗ ПРЕОБРАЗОВАНИЯ НАПРАВЛЕНИЯ ПОЗИЦИИ (SIDE)

**Дата анализа:** 30.12.2025  
**Цель:** Проверить правильность преобразования `side` из сигнала (buy/sell) в позицию (long/short) и логирования

---

## 📋 ЦЕПОЧКА ПРЕОБРАЗОВАНИЯ SIDE

### 1. Сигнал (Signal Generator)

**Источник:** `signal_generator.py`

Сигналы генерируются с полем `side`:
- `side = "buy"` для LONG позиции
- `side = "sell"` для SHORT позиции

**ЦИТАТА ИЗ CSV:**
```
signals,2025-12-30T14:46:33.444558,BTC-USDT,sell,...
```

**ВЫВОД:** Сигнал правильно генерируется с `side="sell"` для SHORT позиции.

---

### 2. Преобразование в Order Executor

**Источник:** `order_executor.py` строки 111, 170

**ЦИТАТА ИЗ КОДА:**
```111:111:src/strategies/scalping/futures/order_executor.py
side = signal.get("side")
```

`side` из сигнала передается напрямую в `place_futures_order` без преобразования.

**ВЫВОД:** `side="sell"` передается в клиент без изменений.

---

### 3. Преобразование в Futures Client (на биржу)

**Источник:** `futures_client.py` строки 1194-1198

**ЦИТАТА ИЗ КОДА:**
```1194:1198:src/clients/futures_client.py
# Для открытия новых позиций добавляем posSide
if side.lower() == "buy":
    payload["posSide"] = "long"
elif side.lower() == "sell":
    payload["posSide"] = "short"
```

**АНАЛИЗ:**
- `side="buy"` → `posSide="long"` ✅ ПРАВИЛЬНО
- `side="sell"` → `posSide="short"` ✅ ПРАВИЛЬНО

**ВЫВОД:** Преобразование на биржу правильное. Если сигнал `sell`, то на биржу отправляется `posSide="short"`.

---

### 4. Получение позиции с биржи

**Источник:** `entry_manager.py` строки 408-449

**ЦИТАТА ИЗ КОДА:**
```408:449:src/strategies/scalping/futures/positions/entry_manager.py
position_side = pos_side_raw
if position_side not in ["long", "short"]:
    position_side = (
        "long" if pos_size > 0 else "short"
    )  # Fallback по знаку размера
```

**АНАЛИЗ:**
- `posSide` читается из позиции с биржи
- Если `posSide` не "long" или "short", определяется по знаку `pos_size`

**ЦИТАТА ИЗ КОДА (fallback):**
```498:503:src/strategies/scalping/futures/positions/entry_manager.py
"posSide": "long" if side == "buy" else "short",
...
"position_side": "long" if side == "buy" else "short",
```

**ВЫВОД:** Если позиция не найдена на бирже, используется fallback преобразование из сигнала:
- `side="buy"` → `position_side="long"` ✅ ПРАВИЛЬНО
- `side="sell"` → `position_side="short"` ✅ ПРАВИЛЬНО

---

### 5. Логирование в CSV (positions_open)

**Источник:** `entry_manager.py` строка 657, `performance_tracker.py` строка 358

**ЦИТАТА ИЗ КОДА:**
```655:658:src/strategies/scalping/futures/positions/entry_manager.py
self.performance_tracker.record_position_open(
    symbol=symbol,
    side=position_data.get("position_side", "long"),
    ...
)
```

**ЦИТАТА ИЗ КОДА:**
```358:358:src/strategies/scalping/spot/performance_tracker.py
"side": side,
```

**АНАЛИЗ:**
- В CSV записывается `position_side`, который уже преобразован в "long"/"short"
- Для сигнала `sell` в CSV записано `side="short"` ✅ ПРАВИЛЬНО

**ЦИТАТА ИЗ CSV:**
```
positions_open,2025-12-30T14:46:45.478329,BTC-USDT,short,88002.30000000,...
```

**ВЫВОД:** Логирование правильное. В CSV записано `short`, что соответствует сигналу `sell`.

---

### 6. Логирование в CSV (trades)

**Источник:** `position_manager.py` строка 4703, `performance_tracker.py` строка 193

**ЦИТАТА ИЗ КОДА:**
```4701:4703:src/strategies/scalping/futures/position_manager.py
trade_result = TradeResult(
    symbol=symbol,
    side=side.lower(),  # "long" или "short"
```

**ЦИТАТА ИЗ КОДА:**
```4002:4002:src/strategies/scalping/futures/position_manager.py
side = actual_position.get("posSide", "long")
```

**АНАЛИЗ:**
- `side` берется из `posSide` позиции с биржи
- Если позиция была открыта правильно как `posSide="short"`, то `side="short"` ✅ ПРАВИЛЬНО

**ЦИТАТА ИЗ CSV:**
```
trades,2025-12-30T14:50:42.309190+00:00,BTC-USDT,short,87867.4000,88688.1000,...
```

**ВЫВОД:** В CSV записано `short`, что соответствует сигналу `sell` из которого была открыта позиция.

---

## 🔴 КРИТИЧЕСКОЕ ОТКРЫТИЕ

### Несоответствие между CSV и данными биржи

**ПРОБЛЕМА:** В CSV указано `short`, но в данных биржи указано `LONG` для одной и той же позиции (BTCUSDT 17:47:01).

**ВОЗМОЖНЫЕ ПРИЧИНЫ:**

1. **Позиция была изменена на бирже вручную**
   - Если позиция была открыта как SHORT, но затем изменена на LONG вручную через интерфейс биржи
   - Это объясняет несоответствие

2. **Ошибка в данных биржи (отображение)**
   - Данные биржи показывают LONG, но CSV показывает SHORT
   - Это указывает на несоответствие между фактической позицией на бирже и записью в CSV

3. **Позиция была частично закрыта и переоткрыта**
   - Если позиция была частично закрыта и затем переоткрыта в противоположном направлении
   - Это объясняет несоответствие entry price (88002.3 vs 87867.4)

4. **Ошибка в определении направления при синхронизации**
   - Если при синхронизации позиции с биржей используется fallback по знаку `pos_size`
   - Но это не объясняет, почему в CSV записано `short`, а на бирже `LONG`

---

## 🔍 АНАЛИЗ КОДА ПРЕОБРАЗОВАНИЯ

### Проверка преобразования в Futures Client

**ЦИТАТА ИЗ КОДА:**
```1194:1198:src/clients/futures_client.py
# Для открытия новых позиций добавляем posSide
if side.lower() == "buy":
    payload["posSide"] = "long"
elif side.lower() == "sell":
    payload["posSide"] = "short"
```

**ВЫВОД:** Код преобразования правильный. Если `side="sell"`, то `posSide="short"` отправляется на биржу.

### Проверка определения направления при синхронизации

**ЦИТАТА ИЗ КОДА:**
```146:148:src/strategies/scalping/futures/core/position_sync.py
pos_side = pos.get("posSide", "").lower()
if pos_side not in ["long", "short"]:
    pos_side = "long" if pos_size > 0 else "short"
```

**АНАЛИЗ:**
- Если `posSide` отсутствует, определяется по знаку `pos_size`
- Если `pos_size > 0` → `pos_side="long"`
- Если `pos_size < 0` → `pos_side="short"`

**ПРОБЛЕМА:** Если на бирже позиция LONG (положительный размер), но `posSide` отсутствует в данных API, то fallback определит `pos_side="long"` по знаку размера. Но это не объясняет, почему в CSV записано `short`.

---

## 🔴 КРИТИЧЕСКИЙ ВЫВОД

### Наиболее вероятная причина

**ПРОБЛЕМА:** Бот правильно преобразует `side` из сигнала в `posSide` для биржи и правильно логирует в CSV. НО позиция на бирже имеет другое направление, чем записано в CSV.

**ВОЗМОЖНЫЕ СЦЕНАРИИ:**

1. **Позиция была изменена вручную**
   - Позиция была открыта как SHORT (как и записано в CSV)
   - Но затем изменена на LONG вручную через интерфейс биржи
   - Это объясняет несоответствие

2. **Ошибка в данных биржи (время открытия)**
   - Entry price в CSV (88002.3) отличается от entry price в данных биржи (87867.4)
   - Это указывает на то, что данные биржи могут относиться к другой позиции или позиция была изменена

3. **Ошибка в определении направления при синхронизации**
   - При синхронизации позиции с биржей используется `posSide` из API
   - Если `posSide` отсутствует, используется fallback по знаку размера
   - В CSV записано `side="short"` (на основе сигнала), но на бирже фактически LONG
   - Это указывает на то, что бот логировал направление на основе сигнала, а не фактической позиции на бирже

---

## ✅ ПРОВЕРКА ЛОГИКИ ПРЕОБРАЗОВАНИЯ

### Цепочка преобразования (правильная):

1. **Сигнал:** `side="sell"` ✅
2. **Order Executor:** `side="sell"` ✅ (передается без изменений)
3. **Futures Client:** `side="sell"` → `posSide="short"` ✅ (правильное преобразование)
4. **Биржа:** Получает `posSide="short"` в payload ✅
5. **Entry Manager:** Читает `posSide="short"` с биржи → `position_side="short"` ✅
6. **CSV (positions_open):** `side="short"` ✅ (правильное логирование)
7. **CSV (trades):** `side="short"` ✅ (правильное логирование)

**ВЫВОД:** Логика преобразования правильная во всех звеньях цепочки.

---

## 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА: Fallback логика в EntryManager

### Проблема в коде (строки 475-503 entry_manager.py)

**ЦИТАТА ИЗ КОДА (fallback логика):**
```475:503:src/strategies/scalping/futures/positions/entry_manager.py
# Если всё ещё не нашли — используем order_result.price (лимитная цена)
if not position_data:
    logger.warning(
        f"⚠️ EntryManager: Позиция {symbol} не найдена на бирже после retry, "
        f"используем цену из order_result"
    )
    side = signal.get("side", "").lower()
    fallback_price = order_result.get("price", signal.get("price", 0.0))
    
    position_data = {
        "symbol": symbol,
        "instId": f"{symbol}-SWAP",
        "pos": str(position_size) if side == "buy" else str(-position_size),
        "posSide": "long" if side == "buy" else "short",  # ⚠️ ПРОБЛЕМА: Используется side из сигнала, а не с биржи!
        "position_side": "long" if side == "buy" else "short",  # ⚠️ ПРОБЛЕМА: Используется side из сигнала, а не с биржи!
        ...
    }
```

**ПРОБЛЕМА:**
Если позиция не найдена на бирже после размещения ордера (например, ордер еще не исполнился, или была ошибка при получении позиций), бот использует fallback логику и устанавливает `position_side` на основе `side` из сигнала, а не на основе фактической позиции на бирже.

Это означает, что если позиция не была найдена на бирже (race condition, ошибка API, или ордер еще не исполнился), бот логирует в CSV `position_side` на основе сигнала (`side="sell"` → `position_side="short"`), независимо от фактической позиции на бирже.

### Как бот читает posSide с биржи (строки 298-326 entry_manager.py)

**ЦИТАТА ИЗ КОДА (чтение posSide с биржи):**
```298:326:src/strategies/scalping/futures/positions/entry_manager.py
# Получаем позицию с биржи
positions = await client.get_positions()
inst_id = f"{symbol}-SWAP"

position_data = None
for pos in positions:
    pos_inst_id = pos.get("instId", "")
    pos_size = abs(float(pos.get("pos", "0")))
    
    if (pos_inst_id == inst_id or pos_inst_id == symbol) and pos_size > 0.000001:
        # Определяем side позиции
        pos_side_raw = pos.get("posSide", "").lower()
        if pos_side_raw in ["long", "short"]:
            position_side = pos_side_raw
        else:
            position_side = ("long" if float(pos.get("pos", "0")) > 0 else "short")
```

**АНАЛИЗ:**
Бот перебирает все позиции и выбирает первую позицию, которая соответствует символу и имеет размер > 0.000001. Код не использует `order_id` для сопоставления открытого ордера с позицией на бирже. Если на бирже несколько позиций по одному символу (например, LONG и SHORT), бот выберет первую найденную, а не ту, которая соответствует открытому ордеру.

## 🔴 ИТОГОВЫЙ ВЫВОД

### Проблема в логике чтения posSide с биржи

**АНАЛИЗ ПОКАЗЫВАЕТ:**
1. ✅ Преобразование `side` из сигнала в `posSide` для биржи правильное (строки 1194-1198 futures_client.py)
2. ⚠️ Логирование `side` в CSV использует fallback логику, которая устанавливает `position_side` на основе `side` из сигнала, а не на основе фактической позиции на бирже (строки 475-503 entry_manager.py)
3. ⚠️ Бот читает позицию из `get_positions()` без использования `order_id` для сопоставления, выбирая первую найденную позицию по символу (строки 298-326 entry_manager.py)

### Причина несоответствия

**АНАЛИЗ КОДА И ДАННЫХ:**
1. Бот отправляет SHORT ордер на биржу (`posSide="short"`) согласно коду (строки 1194-1198 futures_client.py) ✅
2. В CSV записано `side="short"` (на основе сигнала `sell`) ✅
3. На бирже фактически LONG позиция (согласно данным биржи) ⚠️
4. Код использует fallback логику (строки 475-503 entry_manager.py), которая устанавливает `position_side` на основе `side` из сигнала, если позиция не найдена на бирже ⚠️
5. Код читает позицию из `get_positions()` без использования `order_id` для сопоставления (строки 298-326 entry_manager.py) ⚠️

**ВЫВОД:** Fallback логика и отсутствие сопоставления по `order_id` приводят к тому, что в CSV записывается направление на основе сигнала (`short`), а не фактическое направление позиции на бирже (`long`).

**РЕКОМЕНДАЦИЯ:**
1. Добавить логирование payload перед отправкой на биржу (в futures_client.py)
2. Добавить логирование всех позиций, полученных с биржи (в entry_manager.py)
3. Использовать order_id для сопоставления открытого ордера с позицией на бирже (вместо просто поиска по символу)
4. Убрать или улучшить fallback логику, чтобы она не использовала side из сигнала, если позиция не найдена на бирже

