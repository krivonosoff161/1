🔴 BUG FIX: Удален дублирующийся метод reset() в fast_adx.py

Проблема: Два метода reset() - второй (L260) переопределял первый (L58)
- Первый reset() (L58): полный сброс всех данных включая smoothed*
- Второй reset() (L260-282): неполный, не сбрасывал smoothed* полностью

Решение: Удален дублирующийся второй reset() (L260-282)
Результат: Остался только правильный полный метод reset()
