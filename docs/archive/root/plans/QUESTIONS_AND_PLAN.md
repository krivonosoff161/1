# ВОПРОСЫ И ПЛАН ИСПРАВЛЕНИЙ

**Дата:** 2025-12-20  
**Статус:** 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ ТРЕБУЮТ НЕМЕДЛЕННОГО ВНИМАНИЯ

---

## ❓ УТОЧНЯЮЩИЕ ВОПРОСЫ

### 🔴 ВОПРОС #1: Проблема закрытия позиций

**Проблема:** Бот закрывает отрицательные позиции по $1-4, а положительные минимальные ($0.02, $0.3)

**Вопросы:**

1. **Можно ли показать конкретные примеры из логов?**
   - Когда именно закрылась позиция с маленькой прибылью ($0.02, $0.3)?
   - Какой был `peak_profit` в этот момент?
   - Какой был текущий PnL?
   - Какой был режим рынка (trending/ranging/choppy)?

2. **Какие параметры должны быть?**
   - Какой `min_profit_to_close` оптимален? (сейчас 0.1-2.5% в конфиге)
   - Какой порог для "не закрывать если можно больше"? (например, 70% от peak_profit)
   - Должен ли бот проверять `peak_profit` перед закрытием?

3. **Логика закрытия:**
   - Где находится проверка `peak_profit` перед закрытием? (нужно найти)
   - Используется ли `_update_peak_profit()` результат при принятии решения о закрытии?
   - Есть ли проверка "не закрывать если current_profit < peak_profit * 0.7"?

**Файлы для анализа:**
- `src/strategies/scalping/futures/position_manager.py:_update_peak_profit()` (строка ~4200)
- `src/strategies/scalping/futures/positions/exit_analyzer.py:_generate_exit_for_*()` (строки 1600-2000)
- `src/strategies/scalping/futures/indicators/trailing_stop_loss.py:should_close_position()` (строка 509)

---

### 🔴 ВОПРОС #2: Блокировка новых ордеров

**Проблема:** `allow_concurrent=false` блокирует новые сигналы при открытых позициях

**Текущая конфигурация:**
- `allow_concurrent_positions: false` (строка 37 в config_futures.yaml)
- `max_open_positions: 5` (по баланс-профилю)

**Вопросы:**

1. **Подтверждение желаемого поведения:**
   - ✅ Разрешить до 5 позиций одновременно?
   - ✅ Автоматически закрывать противоположные позиции при новом сигнале?
   - ✅ Увеличивать позицию если сигнал в том же направлении?
   - ✅ Или пропускать сигнал если позиция уже большая?

2. **Логика закрытия противоположных:**
   - Закрывать сразу при появлении противоположного сигнала?
   - Или ждать исполнения нового ордера?

**Файлы для изменения:**
- `src/strategies/scalping/futures/coordinators/signal_coordinator.py:1034-1046`
- `config/config_futures.yaml:37`

---

### 🟠 ВОПРОС #3: Эффективность ордеров

**Проблема:** Только 11.6% ордеров исполняются (204 из 1764)

**Вопросы:**

1. **Текущие параметры:**
   - Какой `limit_offset_percent` в конфиге? (нужно найти)
   - Какой `max_wait_seconds` установлен? (нужно найти)
   - Как часто проверяется актуальность цены?

2. **Желаемые параметры:**
   - Какой `limit_offset_percent` оптимален? (0.05%? 0.1%?)
   - Какой `max_wait_seconds` оптимален? (15 сек? 30 сек?)

**Файлы для изменения:**
- `src/strategies/scalping/futures/order_executor.py:_calculate_limit_price()`
- `config/config_futures.yaml` - параметры ордеров

---

## 📋 ПЛАН ИСПРАВЛЕНИЙ (ПО ПРИОРИТЕТАМ)

### 🔴 ПРИОРИТЕТ #1: ИСПРАВИТЬ ЛОГИКУ ЗАКРЫТИЯ ПОЗИЦИЙ

**Цель:** Бот не должен закрывать позиции с маленькой прибылью, если был больший peak_profit

#### Шаг 1.1: Найти где происходит закрытие
- [ ] Найти все места где вызывается `close_position()`
- [ ] Найти где проверяется `min_profit_to_close`
- [ ] Найти где используется `peak_profit` при принятии решения

#### Шаг 1.2: Добавить проверку peak_profit
- [ ] В `exit_analyzer.py:_generate_exit_for_*()` добавить проверку:
  ```python
  if current_profit < peak_profit * 0.7:
      # Не закрывать - теряем 30% прибыли
      return {"action": "hold", "reason": "profit_too_low_vs_peak"}
  ```
- [ ] В `trailing_stop_loss.py:should_close_position()` добавить проверку peak_profit
- [ ] В `position_manager.py:_check_tp_only()` добавить проверку peak_profit

#### Шаг 1.3: Улучшить SL логику
- [ ] Проверить почему SL срабатывает на больших убытках
- [ ] Убедиться что SL использует актуальную цену из стакана
- [ ] Добавить проверку разворота перед закрытием по SL

---

### 🔴 ПРИОРИТЕТ #2: УБРАТЬ БЛОКИРОВКУ НОВЫХ ОРДЕРОВ

**Цель:** Разрешить до 5 позиций, автоматически закрывать противоположные

#### Шаг 2.1: Изменить логику в signal_coordinator.py
- [ ] Убрать блокировку в строках 1034-1046
- [ ] Добавить логику:
  ```python
  if has_opposite_position:
      # Закрыть противоположную позицию
      await close_opposite_position()
  elif has_same_direction_position:
      # Увеличить позицию или пропустить
      if position_size < max_size:
          # Увеличить
      else:
          # Пропустить
  ```

#### Шаг 2.2: Обновить конфигурацию
- [ ] Установить `allow_concurrent_positions: true`
- [ ] Убедиться что `max_open_positions: 5`

---

### 🔴 ПРИОРИТЕТ #3: УЛУЧШИТЬ ЭФФЕКТИВНОСТЬ ОРДЕРОВ

**Цель:** Увеличить эффективность с 11.6% до 60%+

#### Шаг 3.1: Улучшить расчет лимитной цены
- [ ] Уменьшить `limit_offset_percent` (например, с 0.2% до 0.05%)
- [ ] Всегда использовать актуальную цену из стакана (уже исправлено)
- [ ] Проверять свежесть цены (не старше 1 секунды)

#### Шаг 3.2: Оптимизировать таймауты
- [ ] Увеличить `max_wait_seconds` (например, с 15 до 30 сек)
- [ ] Не отменять ордер если цена близка к исполнению (< 0.1%)

---

### 🔴 ПРИОРИТЕТ #4: ИСПРАВИТЬ ОШИБКИ 51006

**Цель:** Убрать все ошибки "цена вне лимитов"

#### Шаг 4.1: Добавить проверку лимитов
- [ ] В `order_executor.py:_place_limit_order()` добавить:
  ```python
  price_limits = await self.client.get_price_limits(symbol)
  if limit_price > price_limits["max_buy_price"]:
      limit_price = price_limits["max_buy_price"] * 0.999
  if limit_price < price_limits["min_sell_price"]:
      limit_price = price_limits["min_sell_price"] * 1.001
  ```

---

### 🟠 ПРИОРИТЕТ #5: ПРОВЕРИТЬ МАТЕМАТИКУ

**Цель:** Убедиться что все расчеты правильные

#### Шаг 5.1: Аудит расчетов PnL
- [ ] Проверить `trailing_stop_loss.py:get_profit_pct()` - правильный расчет от маржи?
- [ ] Проверить `exit_analyzer.py:_calculate_pnl_percent()` - правильный расчет?
- [ ] Проверить конвертации размеров (ctVal, size, notional)

---

### 🟠 ПРИОРИТЕТ #6: АУДИТ API ВЫЗОВОВ

**Цель:** Исправить все ошибки API

#### Шаг 6.1: Проверить все вызовы
- [ ] Сверить `futures_client.py` с документацией OKX
- [ ] Проверить форматы параметров
- [ ] Исправить несоответствия

---

### 🟡 ПРИОРИТЕТ #7: РЕФАКТОРИНГ

**Цель:** Разбить монстр-файлы

#### Шаг 7.1: Разбить position_manager.py (5928 строк)
- [ ] Вынести методы в отдельные модули
- [ ] Оставить только координацию

---

## 🎯 НАЧАЛО РАБОТЫ

**Готов начать с Приоритета #1 (закрытие позиций) после ответов на вопросы!**

**Вопросы для начала:**
1. Можно ли показать примеры из логов когда закрылась позиция с маленькой прибылью?
2. Подтверждение: разрешить до 5 позиций, автоматически закрывать противоположные?

---

**Ожидаю ответов для начала работы!** ✅



