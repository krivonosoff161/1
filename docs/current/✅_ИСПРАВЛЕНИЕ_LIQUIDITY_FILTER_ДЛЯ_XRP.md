# ИСПРАВЛЕНИЕ LIQUIDITY FILTER ДЛЯ XRP-USDT

**Дата:** 23 ноября 2025  
**Проблема:** Бот не видит сигналы для XRP-USDT из-за блокировки LiquidityFilter  
**Решение:** Исправлена логика проверки объемов в зависимости от направления сигнала

---

## 📊 ПРОБЛЕМА

### На графике XRP-USDT:
- ✅ Резкое падение с ~2.04 до ~1.98 (15:15-15:30)
- ✅ RSI упал до 20.31 (сильно перепродан)
- ✅ EMA 12 пересекла EMA 24 вниз (медвежий сигнал)
- ✅ Цена пробила нижнюю полосу Боллинджера

### В логах:
```
⛔ LiquidityFilter: XRP-USDT отклонён — объём на лучшем ask 0 < 1 (regime=ranging)
```

### Причина:
- ❌ LiquidityFilter проверяет `best_ask_volume_usd` для ВСЕХ сигналов, включая LONG
- ❌ Логически неправильно: для LONG нужен bid volume, для SHORT нужен ask volume
- ❌ Бот генерирует LONG сигналы, но они блокируются из-за нулевого ask volume

---

## ✅ РЕШЕНИЕ

### Исправлена логика LiquidityFilter:
1. ✅ Добавлен параметр `signal_side` в `evaluate()`
2. ✅ Для LONG (buy): проверяем только `best_bid_volume_usd`
3. ✅ Для SHORT (sell): проверяем только `best_ask_volume_usd`
4. ✅ Для обратной совместимости: если направление не указано, проверяем оба объема

### Код:
```python
# Проверяем bid volume только для LONG сигналов
if signal_side_normalized != "sell":
    if snapshot.best_bid_volume_usd < thresholds["min_best_bid_volume_usd"]:
        # Блокируем LONG сигнал

# Проверяем ask volume только для SHORT сигналов
if signal_side_normalized != "buy":
    if snapshot.best_ask_volume_usd < thresholds["min_best_ask_volume_usd"]:
        # Блокируем SHORT сигнал
```

---

## 📋 РЕЗУЛЬТАТ

### После исправления:
- ✅ LONG сигналы для XRP-USDT не будут блокироваться из-за нулевого ask volume
- ✅ SHORT сигналы будут проверять только ask volume
- ✅ Бот сможет видеть медвежьи сигналы на падении XRP-USDT

---

**Статус:** ✅ ИСПРАВЛЕНО

