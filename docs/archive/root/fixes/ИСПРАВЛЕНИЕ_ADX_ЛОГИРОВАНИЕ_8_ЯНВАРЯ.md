# 🔧 ИСПРАВЛЕНИЕ 2 ДЕЛ ADX ПРОБЛЕМЫ - 8 ЯНВАРЯ 2026

## ✅ ИСПРАВЛЕНИЕ 1: ADX РАСЧЕТ (+DM и -DM) 

**Файл:** [src/strategies/modules/adx_filter.py](src/strategies/modules/adx_filter.py#L368-L413)

**Проблема:** 
- Методы `_calculate_plus_dm()` и `_calculate_minus_dm()` считали +DM и -DM независимо
- На одном баре могли быть ОБИЕ +DM > 0 И -DM > 0
- Это давало путаницу в +DI и -DI → неправильные направления сигналов

**Решение:**
```python
# ✅ БЫЛО (неправильно)
plus_dm[1:] = np.where(up_move > 0, up_move, 0)

# ✅ СТАЛО (правильно)
plus_dm[1:] = np.where((up_move > 0) & (up_move > down_move), up_move, 0)
```

**Результат:** Только ДОМИНИРУЮЩЕЕ движение (+DM или -DM) будет ненулевым на баре.

---

## ✅ ИСПРАВЛЕНИЕ 2: ADX НЕ СОХРАНЯЕТСЯ В DataRegistry 

**Файл:** [src/strategies/scalping/futures/signal_generator.py](src/strategies/scalping/futures/signal_generator.py#L2493-2520)

**Проблема:**
- ADX рассчитывался через `adx_filter.check_trend_strength()` (fallback)
- Сохранялся в локальный `indicators` словарь
- **НО не сохранялся в DataRegistry для следующего цикла**
- Результат: `signal_coordinator.check_for_signals()` запрашивает ADX из registry, не находит → ошибка "ADX НЕ найден"

**Решение:**
```python
# ✅ ДОБАВЛЕНО: Сохранение ADX в DataRegistry после расчета
if self.data_registry:
    try:
        await self.data_registry.update_indicators(
            symbol,
            {
                "adx": adx_value,
                "adx_plus_di": adx_plus_di,
                "adx_minus_di": adx_minus_di,
            }
        )
        logger.debug(f"✅ ADX сохранен в DataRegistry для {symbol}")
    except Exception as e:
        logger.warning(f"⚠️ Ошибка сохранения ADX в DataRegistry: {e}")
```

**Результат:** 
- ADX теперь сохраняется в registry
- Следующий цикл получит ADX из registry без ошибок
- `signal_coordinator` будет видеть ADX значения

---

## 📊 ЦЕПОЧКА ИСПРАВЛЕНИЙ

### Цикл 1: Генерация сигналов (`signal_generator.py`)
1. Рассчитываются индикаторы (RSI, MACD, ATR, Bollinger Bands)
2. **NEW:** Рассчитывается ADX через `adx_filter` (fallback)
3. **NEW:** ADX сохраняется в DataRegistry
4. Генерируются сигналы на основе индикаторов

### Цикл 2: Обработка цены (`signal_coordinator.check_for_signals()`)
1. Получает ADX из DataRegistry
2. **БЫЛО:** Ошибка "ADX НЕ найден"
3. **СТАЛО:** Находит ADX, успешно обрабатывает

---

## 🐛 ПОЧЕМУ БЫЛА ОШИБКА

```
signal_generator.py (Цикл 1):
  ├─ ADX рассчитан: 34.5 ✅
  ├─ Сохранен в indicators: ✅
  ├─ Сохранен в DataRegistry: ❌ ОШИБКА! (было пропущено)
  └─ Сигналы сгенерированы

signal_coordinator.check_for_signals() (Цикл 2):
  └─ Запрашивает ADX из DataRegistry
      └─ ❌ Не найден!
          └─ ⚠️ WARNING: "ADX НЕ найден в индикаторах"
```

---

## ✅ ПРОВЕРКА

После этого исправления:

1. ✅ ADX рассчитывается правильно (исправление 1 + 2)
2. ✅ ADX сохраняется в DataRegistry
3. ✅ `signal_coordinator` получает ADX без ошибок
4. ✅ Логи должны показать "✅ ADX сохранен в DataRegistry"
5. ✅ Больше нет "⚠️ ADX НЕ найден в индикаторах"

---

## 📋 СТАТУС

| Исправление | Статус | Файл | Строки |
|-------------|--------|------|--------|
| 1. ADX расчет +DM/-DM | ✅ | adx_filter.py | 368-413 |
| 2. ADX сохранение в registry | ✅ | signal_generator.py | 2493-2520 |

**Дата:** 8 января 2026, ~11:01 UTC  
**Критичность:** ВЫСОКАЯ (блокирует все сигналы)  
**Статус:** ИСПРАВЛЕНО ✅

