# 🔍 АНАЛИЗ ОТСУТСТВИЯ ЛОГИРОВАНИЯ ПРИ ОТКРЫТИИ ПОЗИЦИЙ

**Дата анализа:** 03.01.2026  
**Проблема:** Отсутствует логирование критических параметров при открытии позиций

---

## ❌ ЧТО ОТСУТСТВУЕТ В ЛОГАХ ПРИ ОТКРЫТИИ ПОЗИЦИЙ:

1. **Значения индикаторов (`[INDICATORS]`)** в момент генерации сигнала
2. **ADX/+DI/-DI** при прохождении ADX фильтра (детальные значения)
3. **Расчет размера позиции (`[PARAMS] РАСЧЕТ РАЗМЕРА ПОЗИЦИИ`)** перед открытием
4. **TP/SL параметры** при открытии позиции (логируются только при проверке выхода)

---

## 📊 АНАЛИЗ КОДА: ЧТО ЛОГИРУЕТСЯ И ГДЕ

### ✅ 1. Логирование индикаторов `[INDICATORS]`

**Где:** `src/strategies/scalping/futures/signal_generator.py` (строка ~2426-2452)

**Когда добавлено:** 03.01.2026

**Статус:** ✅ **ДОБАВЛЕНО**, но было исправление ошибки форматирования

**Код:**
```python
# ✅ НОВОЕ (03.01.2026): Логирование значений индикаторов перед генерацией сигналов
logger.info(
    f"📊 [INDICATORS] {symbol} ({current_regime_for_log}): Значения индикаторов | "
    f"ADX={adx_value:.1f} ({adx_trend}), RSI={rsi_str}, MACD_hist={macd_str}, ATR={atr_str} | "
    f"Источник: MarketData.indicators -> DataRegistry/IndicatorProvider"
)
```

**Проблема:**
- ✅ Логирование добавлено
- ⚠️ В логах staging была ошибка форматирования (исправлена)
- ⚠️ Но позиции открыты в 13:26-13:27, а логи начинаются с 16:40 - **логи за момент открытия отсутствуют!**

---

### ✅ 2. Логирование расчета размера позиции `[PARAMS] РАСЧЕТ РАЗМЕРА ПОЗИЦИИ`

**Где:** `src/strategies/scalping/futures/coordinators/signal_coordinator.py` (строка ~2414-2421)

**Когда добавлено:** 03.01.2026 (судя по комментариям)

**Статус:** ✅ **ДОБАВЛЕНО**

**Код:**
```python
# ✅ НОВОЕ (03.01.2026): Детальное логирование размера позиции и маржи перед открытием
logger.info(
    f"📊 [PARAMS] {symbol} ({current_regime}): РАСЧЕТ РАЗМЕРА ПОЗИЦИИ | "
    f"Баланс: ${balance:.2f} (профиль: {balance_profile_name}), "
    f"Размер: {position_size:.6f} контрактов ({size_in_coins:.6f} монет), "
    f"Notional: ${notional_usd:.2f} USD, Леверидж: {leverage_config}x, "
    f"Маржа: ${margin_usd:.2f} USD | "
    f"Источник: RiskManager.calculate_position_size()"
)
```

**Проблема:**
- ✅ Логирование добавлено
- ⚠️ Но позиции открыты в 13:26-13:27, а логи начинаются с 16:40 - **логи за момент открытия отсутствуют!**

---

### ❌ 3. Логирование TP/SL параметров ПРИ ОТКРЫТИИ

**Где логируется TP/SL:**
- `src/strategies/scalping/futures/positions/exit_analyzer.py` - при проверке выхода
- НЕТ логирования при открытии позиции!

**Проблема:** ❌ **ОТСУТСТВУЕТ логирование TP/SL параметров при открытии позиции!**

**Где должно логироваться:**
- При инициализации TrailingStopLoss (когда позиция открывается)
- Или в `execute_signal` после открытия позиции
- Или в `entry_manager.open_position`

**Текущая ситуация:**
- TP/SL параметры логируются только в `exit_analyzer` при проверке выхода
- Но при открытии позиции эти параметры НЕ логируются
- В JSON файлах `exit_decisions` есть TP/SL, но они создаются при закрытии, а не при открытии

---

### ✅ 4. Логирование ADX фильтра `[FILTER]`

**Где:** `src/strategies/scalping/futures/signals/filter_manager.py`

**Когда добавлено:** 03.01.2026

**Статус:** ✅ **ДОБАВЛЕНО**

**Код:**
```python
# ✅ НОВОЕ (03.01.2026): Детальное логирование блокировки/прохождения фильтров
logger.info(
    f"📊 [FILTER] {symbol} ({signal_type_str} {signal_side_str}): ADX Filter - PASSED/BLOCKED | "
    f"ADX={adx_value}, +DI={di_plus}, -DI={di_minus} | "
    f"Источник: ADXFilter.check_trend_strength()"
)
```

**Проблема:**
- ✅ Логирование добавлено
- ⚠️ Но позиции открыты в 13:26-13:27, а логи начинаются с 16:40 - **логи за момент открытия отсутствуют!**

---

## 🔍 ПРИЧИНЫ ОТСУТСТВИЯ ЛОГИРОВАНИЯ В ДАННЫХ ЛОГАХ:

### Причина #1: Логи за момент открытия отсутствуют

**Факты:**
- Позиции открыты: 13:26:33 (ETH-USDT), 13:27:09 (BTC-USDT)
- Staging архив создан: 16:54:12
- Логи в staging начинаются: 16:40:58
- **Разница:** ~3 часа 14 минут

**Вывод:** Логи за момент открытия (13:26-13:27) **не попали в staging архив**, так как они были созданы ДО того, как staging был собран.

---

### Причина #2: Логирование было добавлено ПОСЛЕ открытия позиций

**Временная последовательность:**
1. Позиции открыты: 13:26-13:27
2. Логирование добавлено: 03.01.2026 (утром/днем)
3. Staging архив создан: 16:54:12

**Вывод:** Логирование было добавлено ПОСЛЕ того, как позиции были открыты, поэтому в логах за 13:26-13:27 его нет.

---

### Причина #3: TP/SL параметры НЕ логируются при открытии

**Текущая реализация:**
- TP/SL параметры логируются только в `exit_analyzer.py` при проверке выхода
- При открытии позиции TP/SL параметры НЕ логируются

**Где должно быть:**
- В `TrailingStopLoss.initialize()` - при инициализации TSL
- Или в `signal_coordinator.execute_signal()` - после расчета размера позиции
- Или в `entry_manager.open_position()` - после открытия позиции

**Вывод:** ❌ **Логирование TP/SL при открытии отсутствует в коде!**

---

## ✅ ЧТО УЖЕ ЛОГИРУЕТСЯ (добавлено 03.01.2026):

1. ✅ `[INDICATORS]` - значения индикаторов (RSI, MACD, ATR, ADX) - **РАБОТАЕТ**
2. ✅ `[PARAMS] РАСЧЕТ РАЗМЕРА ПОЗИЦИИ` - размер, маржа, баланс - **РАБОТАЕТ**
3. ✅ `[FILTER]` - детальное логирование фильтров с параметрами - **РАБОТАЕТ**
4. ✅ `[PARAMS] АДАПТИВНЫЕ ПАРАМЕТРЫ РИСКА` - параметры риска - **РАБОТАЕТ**
5. ❌ `[PARAMS] TP/SL параметры при открытии` - **ОТСУТСТВУЕТ!**

---

## 🔧 ЧТО НУЖНО ДОБАВИТЬ:

### 1. Логирование TP/SL параметров при открытии позиции

**Где добавить:**
- В `TrailingStopLoss.initialize()` - когда TSL инициализируется для новой позиции
- Или в `signal_coordinator.execute_signal()` - после расчета размера позиции, перед открытием

**Что логировать:**
```
📊 [PARAMS] {symbol} ({regime}): TP/SL ПАРАМЕТРЫ ПРИ ОТКРЫТИИ |
tp_percent={tp}%, tp_atr_multiplier={tp_atr}, tp_min={tp_min}%, tp_max={tp_max}% |
sl_percent={sl}%, sl_atr_multiplier={sl_atr}, sl_min={sl_min}% |
Источник: ParameterProvider.get_exit_params()
```

**Приоритет:** 🔴 **ВЫСОКИЙ** - критично для анализа решений трейдера

---

## 📊 ИТОГОВЫЙ ВЫВОД:

### Почему логирование отсутствует в staging логах:

1. **Логи за момент открытия (13:26-13:27) не попали в staging** - staging создан позже (16:54)
2. **Логирование было добавлено ПОСЛЕ открытия позиций** - позиции открыты в 13:26, логирование добавлено днем 03.01.2026
3. **TP/SL параметры НЕ логируются при открытии** - логируются только при проверке выхода

### Что нужно сделать:

1. ✅ **Найти логи за 13:26-13:27** в других архивах (если они есть)
2. ✅ **Добавить логирование TP/SL при открытии** - в `trailing_sl_coordinator.initialize_trailing_stop()` после получения exit_params
3. ✅ **Проверить новую сессию** - когда бот откроет новые позиции с новым логированием

---

## 🔧 РЕКОМЕНДАЦИЯ: ДОБАВИТЬ ЛОГИРОВАНИЕ TP/SL ПРИ ОТКРЫТИИ

### Где добавить:

В `src/strategies/scalping/futures/coordinators/trailing_sl_coordinator.py`, метод `initialize_trailing_stop()`, после получения `exit_params` через `ParameterProvider.get_exit_params()`.

### Что логировать:

```python
# ✅ НОВОЕ (03.01.2026): Логирование TP/SL параметров при открытии позиции
if self.parameter_provider:
    exit_params = self.parameter_provider.get_exit_params(symbol=symbol, regime=regime)
    if exit_params:
        tp_atr_mult = exit_params.get("tp_atr_multiplier", "N/A")
        sl_atr_mult = exit_params.get("sl_atr_multiplier", "N/A")
        logger.info(
            f"📊 [PARAMS] {symbol} ({regime}): TP/SL ПАРАМЕТРЫ ПРИ ОТКРЫТИИ | "
            f"tp_atr_multiplier={tp_atr_mult}, sl_atr_multiplier={sl_atr_mult}, "
            f"max_holding={exit_params.get('max_holding_minutes', 'N/A')}мин | "
            f"Источник: ParameterProvider.get_exit_params()"
        )
```

**Приоритет:** 🔴 **ВЫСОКИЙ** - критично для анализа решений трейдера

---

**Дата создания отчета:** 03.01.2026  
**Статус:** ⚠️ **Требуется добавление логирования TP/SL при открытии**
