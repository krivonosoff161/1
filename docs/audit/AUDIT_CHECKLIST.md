# 📋 УНИВЕРСАЛЬНОЕ ТЗ «Найди всё, что я не знаю»

> **Использование:** После каждого запуска передай этот файл Cursor для полного аудита.
> 
> **Цель:** 0 🔴 и 0 ⚠️ — тогда «go-live».

---

## 🔍 12 РАЗДЕЛОВ АУДИТА

После каждого запуска сделай полный аудит по 12 разделам:

### 1. Хард-код чисел
```
Паттерны: = 0.05, = 5, > 25, == "trending"
Где искать: src/strategies/scalping/futures/
```

### 2. Хард-код символов
```
Паттерны: if symbol == "APT", if symbol == "BTC"
Где искать: src/strategies/scalping/futures/
```

### 3. Отсутствие логов
```
Обязательные логи:
- SIGNAL_SKIP (при пропуске сигнала)
- EXIT_HIT (при закрытии позиции)
- DRIFT_ADD / DRIFT_REMOVE (при drift реестра)
- TRAIL_UPDATE / TRAIL_RELOAD (при обновлении трейлинга)
- FILL latency (при исполнении ордера)
```

### 4. Отсутствие WARNING-порогов
```
Обязательные WARNING:
- slippage > 0.2% → WARNING
- exit_slippage > 0.3% → WARNING  
- trail_distance < 0.05% → WARNING
- latency > 300 мс → WARNING
```

### 5. Race-conditions
```
Проверить:
- KeyError при доступе к позициям
- Double fill (двойное исполнение)
- Double posId (дублирование WebSocket сообщений)
- asyncio.Lock на критических секциях
```

### 6. Утечки ресурсов
```
Проверить:
- Task leak (create_task без cancel)
- TCP-handles leak (WebSocket reconnect)
- RSS > +5% за 2 часа
- Незакрытые соединения
```

### 7. Отсутствие фильтров
```
Обязательные фильтры перед return signal:
- EV filter (expected_move > cost)
- Gap filter (gap < max_gap_pct)
- Circuit-breaker (is_symbol_blocked)
- Correlation filter (max correlated positions)
```

### 8. Нет adaptive-перегрузки при смене regime
```
При смене режима должны обновляться:
- Trailing SL параметры
- TP/SL multipliers
- Position size multipliers
```

### 9. Drift реестра ↔ биржа
```
Проверить:
- DRIFT_ADD: позиция на бирже, нет в боте
- DRIFT_REMOVE: позиция в боте, нет на бирже
- sync_positions_with_exchange() каждые 60 сек
```

### 10. Неправильные cast-ы
```
Проверить:
- str > int (TypeError)
- float("") (ValueError)
- position.get("size") без float()
```

### 11. Нет graceful-shutdown при memory/limit
```
Проверить:
- psutil.rss > 600 MB → shutdown
- max_open_positions limit
- max_daily_loss_percent
```

### 12. Нет exponential backoff при reconnect
```
Проверить:
- max_reconnect_attempts
- delay = min(5 * (2 ** attempts), 300)
- Сброс счётчика после успешного connect
```

---

## 📊 ФОРМАТ ОТЧЁТА

Для каждого пункта указать:

| # | Раздел | Файл:строка | Статус | Комментарий |
|---|--------|-------------|--------|-------------|
| 1 | Хард-код чисел | risk_manager.py:75 | 🔴 | `= 5` → вынести в конфиг |
| 2 | Хард-код символов | — | ✅ | Не найдено |
| 3 | Логи | signal_coordinator.py | ✅ | SIGNAL_SKIP есть |
| ... | ... | ... | ... | ... |

---

## 🎯 КРИТЕРИИ GO-LIVE

| Статус | Значение | Действие |
|--------|----------|----------|
| ✅ | Всё ОК | Можно в прод |
| ⚠️ | Частично | Исправить перед go-live |
| 🔴 | Критично | Блокирует go-live |

**Цель: 0 🔴 и 0 ⚠️ — тогда «go-live».**

---

## 📝 КОМАНДА ДЛЯ CURSOR

```
«Сделай полный аудит по AUDIT_CHECKLIST.md:
- Проверь все 12 разделов
- Для каждого: файл:строка + статус + комментарий
- Верни таблицу с итогом
- Цель: 0 🔴 и 0 ⚠️»
```

---

## 📅 ИСТОРИЯ АУДИТОВ

| Дата | Результат | Комментарий |
|------|-----------|-------------|
| 2025-12-01 | ⚠️ 3 / 🔴 0 | Первый полный аудит |
| ... | ... | ... |

