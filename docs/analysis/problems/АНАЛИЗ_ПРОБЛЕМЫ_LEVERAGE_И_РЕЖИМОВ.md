# АНАЛИЗ ПРОБЛЕМЫ: Leverage и режимы позиций на бирже OKX

## 🔍 ОБНАРУЖЕННЫЕ ПРОБЛЕМЫ

### 1. Режим позиций: `long_short_mode` (hedge mode)

**На бирже установлен режим:** `PosMode: long_short_mode`

Это означает:
- **Hedge mode** (режим с раздельными long/short позициями)
- В этом режиме **ОБЯЗАТЕЛЬНО** нужно указывать `posSide` при установке leverage
- Можно одновременно иметь long и short позиции по одному символу

### 2. Текущая позиция ETH-USDT в cross mode

**Позиция ETH-USDT:**
- `mgnMode: cross` (cross margin mode)
- `leverage: 75x`
- `posSide: long`

**Проблема:** Мы пытаемся установить leverage для **isolated mode**, но позиция открыта в **cross mode**!

### 3. Установка leverage работает только с posSide

**Тест установки leverage:**
- ❌ Без `posSide`: ошибка `Parameter posSide error`
- ✅ С `posSide='long'`: успешно установлено leverage 5x для isolated mode

## ✅ РЕШЕНИЕ

### Вариант 1: Установить leverage для обоих направлений (long и short)

В hedge mode нужно устанавливать leverage отдельно для long и short позиций:

```python
# Устанавливаем leverage для long позиций
await client.set_leverage(symbol, 5, pos_side="long")

# Устанавливаем leverage для short позиций
await client.set_leverage(symbol, 5, pos_side="short")
```

### Вариант 2: Переключиться в net mode

Если не нужен hedge mode, можно переключиться в net mode:
- В net mode не нужно указывать `posSide` при установке leverage
- Но это требует закрытия всех позиций

### Вариант 3: Установить leverage для текущего режима (cross/isolated)

Нужно проверять текущий режим маржи и устанавливать leverage соответственно:
- Если позиция в **cross mode** - устанавливать leverage для cross mode
- Если позиция в **isolated mode** - устанавливать leverage для isolated mode

## 🎯 РЕКОМЕНДАЦИЯ

**Для текущей ситуации (hedge mode + cross margin):**

1. Устанавливать leverage для **long** позиций с `posSide='long'`
2. Устанавливать leverage для **short** позиций с `posSide='short'`
3. Учитывать режим маржи (cross/isolated) при установке leverage

## 📝 ИЗМЕНЕНИЯ В КОДЕ

Нужно обновить код установки leverage:

1. Проверять режим позиций (`PosMode`)
2. Если `long_short_mode` (hedge) - устанавливать leverage для обоих направлений
3. Если `net_mode` - устанавливать leverage без `posSide`

