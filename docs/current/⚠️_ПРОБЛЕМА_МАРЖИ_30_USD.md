# ПРОБЛЕМА МАРЖИ $30 USD

**Дата:** 23 ноября 2025  
**Проблема:** Маржа всегда $30, хотя прогрессивная адаптация рассчитывает $84.86

---

## 🔍 ОБНАРУЖЕННАЯ ПРОБЛЕМА

### Из логов:

```
📊 Прогрессивная адаптация для small: баланс $848.58 → размер $84.86
⚠️ Размер позиции изменен из-за округления/минимума: 0.053443 → 0.053000 монет (+0.83%), 
notional: $150.00 → $148.76, margin: $30.00 → $29.75
```

### Проблема:

1. ✅ **Прогрессивная адаптация работает:** рассчитывает `$84.86` (notional)
2. ❌ **Но используется:** `$150.00` (notional) → `$30.00` (margin)
3. ❌ **Разница:** `$84.86` vs `$150.00` (почти в 2 раза больше!)

---

## 🔍 ПРИЧИНЫ ПРОБЛЕМЫ

### Возможные причины:

1. **Переопределение в symbol_profiles:**
   - В `symbol_profiles` может быть `base_position_usd: 150.0`
   - Это переопределяет прогрессивный расчет

2. **Неправильное применение progressive:**
   - `progressive` возвращает `base_position_usd`, но он может перезаписываться

3. **Лимиты max_usd_size:**
   - `max_usd_size` может ограничивать размер до `$150`

---

## 📊 ПРОВЕРКА КОНФИГА

### balance_profiles.small:

```yaml
small:
  threshold: 1500.0
  min_balance: 500.0
  size_at_min: 50.0
  size_at_max: 150.0
  base_position_usd: 100.0  # Для обратной совместимости
  max_position_usd: 150.0
  progressive: true
```

### Прогрессивный расчет:

- Баланс: `$848.58`
- Диапазон: `$500 - $1500`
- Размер: `$50 - $150`
- Расчет: `$50 + ($848.58 - $500) / ($1500 - $500) * ($150 - $50) = $84.86` ✅

### Но используется:

- Notional: `$150.00` ❌
- Margin: `$30.00` ❌

---

## ✅ РЕШЕНИЕ

### Нужно проверить:

1. ✅ **Применяется ли progressive расчет:**
   - Проверить, что `base_position_usd` из progressive используется

2. ✅ **Нет ли переопределения:**
   - Проверить `symbol_profiles` на наличие `base_position_usd: 150.0`

3. ✅ **Лимиты не ограничивают:**
   - Проверить, что `max_usd_size` не ограничивает до `$150`

---

**Статус:** 🔍 ПРОБЛЕМА ОБНАРУЖЕНА - НУЖНО ИСПРАВИТЬ

