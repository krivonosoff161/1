# 🔍 Анализ фильтров и захардкоженных параметров

## ❌ Главный виновник: V-образный разворот

### Расположение: `signal_generator.py:2868-3040`

**Захардкоженный fallback:**
```python
reversal_threshold = 0.0015  # Fallback: 0.15% для обнаружения разворота
```

**Как работает:**
1. Проверяет последние 7 свечей
2. Ищет максимум/минимум не в последних 2 свечах
3. Если падение/рост > 0.15% → блокирует сигнал

**Проблема:**
- **0.15% слишком чувствительно** для криптовалют (BTC/ETH часто имеют колебания 0.2-0.5%)
- Блокирует BEARISH при развороте ВВЕРХ
- Блокирует BULLISH при развороте ВНИЗ
- **1797 отмененных сигналов** из-за этого!

**Где используется:**
- `signal_generator.py:3332-3336` - блокирует BULLISH при V-развороте вниз
- `signal_generator.py:3396-3400` - блокирует BEARISH при V-развороте вверх

**Рекомендация:**
- Увеличить fallback до 0.3-0.5% (в 2-3 раза)
- Или сделать настраиваемым через конфиг (уже есть поддержка, но fallback слишком низкий)

---

## 📋 Все захардкоженные параметры

### 1. **V-образный разворот** (signal_generator.py)

| Параметр | Значение | Строка | Проблема |
|----------|----------|--------|-----------|
| `reversal_threshold` (fallback) | **0.0015** (0.15%) | 2868 | **Слишком чувствительно** |
| Окно свечей | **7** | 2852 | Фиксировано |
| Максимум не в последних | **2 свечи** | 2996, 3027 | Фиксировано |

---

### 2. **ADX Filter** (множество мест)

| Параметр | Значение | Строка | Файл |
|----------|----------|--------|------|
| `adx_threshold` (fallback) | **25.0** | 1721, 1977, 2310, 2539, 2779 | signal_generator.py |
| `adx_threshold` (fallback) | **25.0** | 71, 115 | regime_manager.py |
| `adx_threshold` (fallback) | **18.0** | 509, 754 | signal_generator.py |
| `adx_di_difference` (fallback) | **1.5** | 510, 755 | signal_generator.py |
| `ranging_adx_threshold` | **20.0** | 116 | regime_manager.py |

**Проблема:** 
- **25.0 слишком высокий** (было 20.0, поднято до 25.0)
- Блокирует сигналы при ADX 20-25 (слабый тренд)

---

### 3. **Correlation Filter**

| Параметр | Значение | Строка | Файл |
|----------|----------|--------|------|
| `correlation_threshold` (fallback) | **0.7** | 480, 834 | signal_generator.py |

**Проблема:** 
- 0.7 (70%) - высокий порог корреляции
- Блокирует сигналы если корреляция < 70%

---

### 4. **Pivot Points Filter**

| Параметр | Значение | Строка | Файл |
|----------|----------|--------|------|
| `level_tolerance_percent` (fallback) | **0.25%** | 493 | signal_generator.py |

**Проблема:** 
- 0.25% - может быть слишком строго для некоторых символов

---

### 5. **Volume Profile Filter**

| Параметр | Значение | Строка | Файл |
|----------|----------|--------|------|
| `poc_tolerance_percent` (fallback) | **0.25%** | 506 | signal_generator.py |

**Проблема:** 
- 0.25% - может быть слишком строго

---

### 6. **Profit Hunter (PH)**

| Параметр | Значение | Строка | Файл |
|----------|----------|--------|------|
| `ph_threshold` (fallback) | **0.20** (20%) | 540 | signal_generator.py |
| `ph_time_limit` (fallback) | **300** (5 мин) | 541 | signal_generator.py |

---

### 7. **Strength Multipliers**

| Параметр | Значение | Строка | Файл |
|----------|----------|--------|------|
| `strength_multiplier` (fallback) | **2000.0** | 2815 | signal_generator.py |
| `strength_reduction_neutral` (fallback) | **0.9** | 2816 | signal_generator.py |
| `conflict_multiplier` (fallback) | **0.5** | 2123, 947 | signal_generator.py, risk_manager.py |
| `weak_multiplier` (fallback) | **0.8** | 968 | risk_manager.py |

---

### 8. **Min Strength (разные места)**

| Параметр | Значение | Строка | Файл |
|----------|----------|--------|------|
| `min_signal_strength` (fallback) | **0.3** | 4792, 4882 | signal_generator.py |
| `signal_strength` (fallback) | **0.5** | 70, 72, 1909 | adaptive_leverage.py, order_executor.py |
| `trend_strength` (fallback) | **0.7** | 1207, 1629, 1773 | exit_analyzer.py |
| `trend_strength` (fallback) | **0.8** | 1745 | exit_analyzer.py |
| `trend_strength` (fallback) | **0.3** | 1209 | exit_analyzer.py |
| `min_trend_strength` (fallback) | **0.7** | 2873 | position_manager.py |

---

### 9. **MACD/RSI Strength Calculation**

| Параметр | Значение | Строка | Файл |
|----------|----------|--------|------|
| `base_strength` divisor | **200.0** | 2382, 2458, 146, 216 | signal_generator.py, macd_signal_generator.py |

**Как работает:**
```python
base_strength = min(abs(histogram) / 200.0, 1.0)
```
- MACD histogram делится на 200.0 для расчета strength
- Если histogram = 200 → strength = 1.0 (100%)

---

### 10. **Regime Manager**

| Параметр | Значение | Строка | Файл |
|----------|----------|--------|------|
| `adx_threshold` (default) | **25.0** | 71 | regime_manager.py |
| `trending_adx_threshold` | **25.0** | 115 | regime_manager.py |
| `ranging_adx_threshold` | **20.0** | 116 | regime_manager.py |
| `min_strength` reduction (conflict) | **0.5** (50%) | 789 | regime_manager.py |
| `min_strength` reduction (ranging) | **0.5** (50%) | 798 | regime_manager.py |

---

### 11. **Fast ADX**

| Параметр | Значение | Строка | Файл |
|----------|----------|--------|------|
| `fast_adx_threshold` (fallback) | **20.0** | 333, 336 | orchestrator.py |

---

## 🎯 Топ-5 самых проблемных захардкоженных параметров

### 1. **V-образный разворот: 0.15%** ⚠️ КРИТИЧНО
- **1797 отмененных сигналов**
- Слишком чувствительно
- **Решение:** Увеличить до 0.3-0.5%

### 2. **ADX threshold: 25.0** ⚠️ ВЫСОКИЙ ПРИОРИТЕТ
- Блокирует сигналы при ADX 20-25
- **Решение:** Снизить до 20.0

### 3. **Correlation threshold: 0.7** ⚠️ СРЕДНИЙ ПРИОРИТЕТ
- Высокий порог корреляции
- **Решение:** Снизить до 0.6

### 4. **Min signal strength: 0.7** (в конфиге, но есть fallback 0.3)
- Fallback 0.3 слишком низкий
- **Решение:** Использовать конфиг (0.7), но снизить до 0.6

### 5. **MACD strength divisor: 200.0**
- Может быть слишком строго
- **Решение:** Проверить, возможно увеличить до 250-300

---

## 📊 Итоговая таблица захардкоженных параметров

| Фильтр/Параметр | Fallback | Конфигурируемый? | Проблема |
|-----------------|----------|------------------|----------|
| **V-образный разворот** | 0.15% | ✅ Да (но fallback низкий) | **Слишком чувствительно** |
| **ADX threshold** | 25.0 | ✅ Да | Слишком высокий |
| **ADX DI difference** | 1.5 | ✅ Да | OK |
| **Correlation threshold** | 0.7 | ✅ Да | Высокий |
| **Pivot tolerance** | 0.25% | ✅ Да | OK |
| **Volume Profile tolerance** | 0.25% | ✅ Да | OK |
| **PH threshold** | 0.20 | ✅ Да | OK |
| **PH time limit** | 300 сек | ✅ Да | OK |
| **Strength multiplier** | 2000.0 | ❌ Нет | Захардкожен |
| **MACD divisor** | 200.0 | ❌ Нет | Захардкожен |
| **Conflict multiplier** | 0.5 | ✅ Да | OK |
| **Min strength** | 0.3 | ✅ Да (конфиг 0.7) | Fallback низкий |

---

## ✅ Рекомендации

### Критичные исправления:

1. **V-образный разворот:**
   - Увеличить fallback с 0.15% до 0.3-0.5%
   - Или добавить в конфиг с дефолтом 0.3%

2. **ADX threshold:**
   - Снизить fallback с 25.0 до 20.0
   - Использовать значение из конфига (min_adx: 25.0 → 20.0)

3. **Correlation threshold:**
   - Снизить fallback с 0.7 до 0.6

### Менее критичные:

4. **MACD strength divisor:**
   - Проверить, возможно увеличить до 250-300

5. **Strength multiplier:**
   - Вынести в конфиг

---

**Дата анализа:** 2025-12-23  
**Вывод:** V-образный разворот - главный виновник блокировки сигналов (1797 отмен). ADX threshold (25.0) - второй по важности.





