# ✅ ИТОГОВОЕ ЛОГИРОВАНИЕ СИСТЕМЫ СВЕЧЕЙ

**Дата:** 26.11.2025 23:10  
**Статус:** ✅ **ПОЛНОСТЬЮ РЕАЛИЗОВАНО**

---

## 📊 ТРИ ТИПА ЛОГОВ

### **1. Основной лог (DEBUG+)** 
**Файл:** `logs/futures/futures_main_{date}.log`  
**Уровень:** DEBUG+  
**Назначение:** Полное детальное логирование всех событий

### **2. INFO лог (INFO+)**
**Файл:** `logs/futures/info_{date}.log`  
**Уровень:** INFO+  
**Назначение:** Важные события и статусы

### **3. ERROR лог (ERROR+)**
**Файл:** `logs/futures/errors_{date}.log`  
**Уровень:** ERROR+  
**Назначение:** Только ошибки и критические проблемы

### **StructuredLogger (JSON):**
**Файлы:**
- `logs/futures/structured/candles_init_{date}.json` - инициализация свечей
- `logs/futures/structured/candles_new_{date}.json` - создание новых свечей (5m, 1H, 1D)
- `logs/futures/structured/candles_usage_{date}.json` - использование свечей фильтрами

---

## ✅ ЧТО ЛОГИРУЕТСЯ И КУДА

### **1. Инициализация свечей** 📊

**Где:** `src/strategies/scalping/futures/orchestrator.py` → `_initialize_candle_buffers()`

**INFO лог:**
- ✅ Начало инициализации: `📊 Инициализация буферов свечей для всех символов и таймфреймов...`
- ✅ Для каждого символа и таймфрейма: `✅ Инициализирован буфер свечей {timeframe} для {symbol} ({count} свечей, {description})`
- ✅ Итоговая статистика: `📊 Инициализация буферов свечей завершена: {total} буферов для {symbols} символов`

**ERROR лог:**
- ✅ Критические ошибки: `❌ Критическая ошибка инициализации буферов свечей: {error}`
- ✅ Ошибки для конкретных символов: `⚠️ Не удалось получить свечи {timeframe} для {symbol}: {error}`

**StructuredLogger:**
- ✅ Каждый успешный буфер → `candles_init_{date}.json`
- ✅ Каждая ошибка → `candles_init_{date}.json` с `status="error"`
- ✅ Итоговая статистика → `candles_init_{date}.json` с `symbol="ALL"`

---

### **2. Создание новых свечей** 🔄

**Где:** `src/strategies/scalping/futures/coordinators/websocket_coordinator.py` → `_update_candle_for_timeframe()`

**INFO лог (только для 5m, 1H, 1D):**
- ✅ Создание новой свечи: `📊 Создана новая свеча {symbol} {timeframe}: timestamp={timestamp}, price={price}`

**DEBUG лог (для 1m):**
- ✅ Создание новой свечи: `📊 Создана новая свеча {symbol} 1m: timestamp={timestamp}, price={price}`
- ✅ Обновление формирующейся свечи: `📊 Обновлена формирующаяся свеча {symbol} {timeframe}: price={price}`

**StructuredLogger (только для 5m, 1H, 1D):**
- ✅ Каждая новая свеча → `candles_new_{date}.json`

---

### **3. Использование свечей в фильтрах** 🔍

**Где:** 
- `src/strategies/modules/multi_timeframe.py` → `_get_htf_candles()` ✅
- `src/strategies/modules/volume_profile_filter.py` (TODO)
- `src/strategies/modules/pivot_points.py` (TODO)
- `src/filters/correlation_manager.py` (TODO)

**INFO лог:**
- ✅ Fallback к API: `⚠️ MTF: DataRegistry содержит недостаточно свечей для {symbol}, используем fallback к API`

**DEBUG лог:**
- ✅ Получение из DataRegistry: `MTF: Получено {count} свечей {timeframe} для {symbol} из DataRegistry`
- ✅ Получение через API: `MTF: Получено {count} свечей {timeframe} для {symbol} через API`

**StructuredLogger:**
- ✅ Каждое использование → `candles_usage_{date}.json`
- ✅ Указывается источник (dataregistry или api)
- ✅ Указывается fallback_to_api (true/false)

---

## 📋 РАСПРЕДЕЛЕНИЕ ПО ЛОГАМ

| Событие | INFO лог | DEBUG лог | ERROR лог | StructuredLogger |
|---------|----------|-----------|-----------|------------------|
| **Инициализация (начало/конец)** | ✅ | - | - | ✅ |
| **Инициализация (детали)** | ✅ | ✅ | - | ✅ |
| **Инициализация (ошибки)** | ✅ | - | ✅ | ✅ |
| **Новая свеча (5m, 1H, 1D)** | ✅ | - | - | ✅ |
| **Новая свеча (1m)** | - | ✅ | - | - |
| **Обновление формирующейся свечи** | - | ✅ | - | - |
| **Fallback к API** | ✅ | - | - | ✅ |
| **Использование DataRegistry** | - | ✅ | - | ✅ |
| **Ошибки обновления** | - | - | ✅ | - |

---

## 🔍 КАК АНАЛИЗИРОВАТЬ ЛОГИ

### **1. Проверка инициализации:**
```bash
# INFO лог - начало и конец
grep "Инициализация буферов свечей" logs/futures/info_*.log

# INFO лог - детали для каждого символа
grep "Инициализирован буфер свечей" logs/futures/info_*.log

# StructuredLogger - ошибки
cat logs/futures/structured/candles_init_*.json | jq '.[] | select(.status=="error")'

# StructuredLogger - статистика
cat logs/futures/structured/candles_init_*.json | jq '.[] | select(.symbol=="ALL")'
```

### **2. Проверка обновления свечей:**
```bash
# INFO лог - только важные таймфреймы (5m, 1H, 1D)
grep "Создана новая свеча" logs/futures/info_*.log | grep -E "5m|1H|1D"

# DEBUG лог - все таймфреймы
grep "Создана новая свеча" logs/futures/futures_main_*.log

# StructuredLogger - новые свечи 5m
cat logs/futures/structured/candles_new_*.json | jq '.[] | select(.timeframe=="5m")'
```

### **3. Проверка использования DataRegistry:**
```bash
# INFO лог - fallback к API (важно!)
grep "fallback к API" logs/futures/info_*.log

# DEBUG лог - использование DataRegistry
grep "из DataRegistry" logs/futures/futures_main_*.log

# StructuredLogger - статистика использования
cat logs/futures/structured/candles_usage_*.json | jq '[.[] | .source] | group_by(.) | map({source: .[0], count: length})'

# StructuredLogger - fallback к API
cat logs/futures/structured/candles_usage_*.json | jq '.[] | select(.fallback_to_api==true)'
```

### **4. Проверка ошибок:**
```bash
# ERROR лог - все ошибки
cat logs/futures/errors_*.log | grep -i "свеч"

# StructuredLogger - ошибки инициализации
cat logs/futures/structured/candles_init_*.json | jq '.[] | select(.status=="error")'
```

---

## 📊 ПРИМЕРЫ ЛОГОВ

### **INFO лог:**
```
2025-11-26 23:00:00 | INFO | orchestrator:_initialize_candle_buffers | 📊 Инициализация буферов свечей для всех символов и таймфреймов...
2025-11-26 23:00:01 | INFO | orchestrator:_initialize_candle_buffers | ✅ Инициализирован буфер свечей 5m для BTC-USDT (200 свечей, Multi-Timeframe и Correlation)
2025-11-26 23:00:05 | INFO | orchestrator:_initialize_candle_buffers | 📊 Инициализация буферов свечей завершена: 12 буферов для 3 символов
2025-11-26 23:05:00 | INFO | websocket_coordinator:_update_candle_for_timeframe | 📊 Создана новая свеча BTC-USDT 5m: timestamp=1701024300, price=50100.50
2025-11-26 23:10:00 | INFO | multi_timeframe:_get_htf_candles | ⚠️ MTF: DataRegistry содержит недостаточно свечей для BTC-USDT, используем fallback к API
```

### **DEBUG лог:**
```
2025-11-26 23:00:01 | DEBUG | orchestrator:_initialize_candle_buffers | ✅ Инициализирован буфер свечей 1m для BTC-USDT (200 свечей, основные индикаторы)
2025-11-26 23:01:00 | DEBUG | websocket_coordinator:_update_candle_for_timeframe | 📊 Создана новая свеча BTC-USDT 1m: timestamp=1701024060, price=50100.50
2025-11-26 23:05:00 | DEBUG | multi_timeframe:_get_htf_candles | MTF: Получено 200 свечей 5m для BTC-USDT из DataRegistry
```

### **ERROR лог:**
```
2025-11-26 23:00:02 | ERROR | orchestrator:_initialize_candle_buffers | ❌ Критическая ошибка инициализации буферов свечей: Connection timeout
```

### **StructuredLogger (JSON):**
```json
{
  "timestamp": "2025-11-26T23:00:01",
  "type": "candle_init",
  "symbol": "BTC-USDT",
  "timeframe": "5m",
  "candles_count": 200,
  "status": "success",
  "error": null
}
```

---

## ✅ РЕАЛИЗОВАНО

1. ✅ Методы в StructuredLogger:
   - `log_candle_init()` - инициализация буферов
   - `log_candle_new()` - создание новых свечей
   - `log_candle_usage()` - использование свечей фильтрами
   - `log_candle_stats()` - статистика буферов (готово, но не используется)

2. ✅ Логирование инициализации в orchestrator:
   - INFO лог для важных событий
   - StructuredLogger для всех событий
   - ERROR лог для ошибок

3. ✅ Логирование создания новых свечей в websocket_coordinator:
   - INFO лог для важных таймфреймов (5m, 1H, 1D)
   - DEBUG лог для 1m
   - StructuredLogger для важных таймфреймов

4. ✅ Логирование использования в MTF фильтре:
   - INFO лог для fallback к API
   - DEBUG лог для использования DataRegistry
   - StructuredLogger для всех использований

5. ✅ Передача structured_logger:
   - В WebSocketCoordinator
   - В SignalGenerator (через `set_structured_logger()`)
   - В MTF Filter

---

## 🚧 TODO (ОПЦИОНАЛЬНО)

1. 🚧 Добавить логирование в Volume Profile Filter
2. 🚧 Добавить логирование в Pivot Points Filter
3. 🚧 Добавить логирование в Correlation Filter
4. 🚧 Добавить периодическую статистику буферов (каждые 10 минут)

---

## 📊 ИТОГОВАЯ СВОДКА

**Что логируется:**
- ✅ Инициализация всех буферов свечей
- ✅ Создание новых свечей (5m, 1H, 1D в INFO, 1m в DEBUG)
- ✅ Использование свечей в фильтрах (MTF реализовано)
- ✅ Fallback к API (важно знать!)

**Куда логируется:**
- ✅ INFO лог - важные события
- ✅ DEBUG лог - детали
- ✅ ERROR лог - ошибки
- ✅ StructuredLogger - структурированные данные для анализа

**Как анализировать:**
- ✅ Поиск по ключевым словам в логах
- ✅ Анализ JSON файлов через `jq`
- ✅ Статистика использования DataRegistry vs API

---

**Статус:** ✅ **ГОТОВО К ИСПОЛЬЗОВАНИЮ**

Теперь можно анализировать работу системы свечей через логи!






