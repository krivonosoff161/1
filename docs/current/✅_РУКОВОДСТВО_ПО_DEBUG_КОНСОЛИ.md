# ✅ РУКОВОДСТВО ПО DEBUG КОНСОЛИ

**Дата:** 23.11.2025  
**Версия:** 1.0

---

## 🔬 ИНТЕРАКТИВНАЯ DEBUG КОНСОЛЬ

### 🚀 Быстрый старт

```bash
# Способ 1: Через batch файл
debug_console.bat

# Способ 2: Напрямую через Python
python scripts/debug_console.py
```

---

## 📋 ДОСТУПНЫЕ КОМАНДЫ

### 1. `config` - Загрузить и показать конфигурацию
```bash
🔬 debug> config
```
**Что делает:**
- Загружает конфигурацию из `config_futures.yaml`
- Показывает основные параметры (символы, leverage, TP/SL)

**Пример вывода:**
```
✅ Конфигурация загружена успешно

📋 КОНФИГУРАЦИЯ:
--------------------------------------------------------------------------------
✅ Scalping enabled: True
✅ Symbols: ['BTC-USDT', 'ETH-USDT', 'SOL-USDT', 'DOGE-USDT', 'XRP-USDT']
✅ Leverage: 5
✅ TP: 2.4%
✅ SL: 1.2%
--------------------------------------------------------------------------------
```

---

### 2. `check` - Проверить конфигурацию
```bash
🔬 debug> check
```
**Что делает:**
- Проверяет конфигурацию на ошибки
- Валидирует основные параметры
- Показывает предупреждения

**Пример вывода:**
```
🔍 ПРОВЕРКА КОНФИГУРАЦИИ:
--------------------------------------------------------------------------------
✅ Символы: BTC-USDT, ETH-USDT, SOL-USDT, DOGE-USDT, XRP-USDT
✅ TP: 2.4%
✅ SL: 1.2%
✅ OKX API: Sandbox

✅ Конфигурация в порядке!
--------------------------------------------------------------------------------
```

---

### 3. `positions` - Показать открытые позиции
```bash
🔬 debug> positions
```
**Что делает:**
- Показывает открытые позиции (требует запущенный бот)
- В будущем: интеграция с WebSocket для реального времени

---

### 4. `signals` - Проверить сигналы
```bash
🔬 debug> signals
```
**Что делает:**
- Проверяет сигналы (требует запущенный бот)
- В будущем: интеграция с SignalGenerator

---

### 5. `params` - Показать параметры для символа
```bash
🔬 debug> params
Введите символ (например, BTC-USDT): BTC-USDT
```
**Что делает:**
- Показывает адаптивные параметры для конкретного символа
- Включает TP/SL/TSL для разных режимов

---

### 6. `logs` - Показать последние логи
```bash
🔬 debug> logs
```
**Что делает:**
- Показывает последние 50 строк из последнего лог-файла
- Помогает быстро проверить текущее состояние бота

**Пример вывода:**
```
📝 ПОСЛЕДНИЕ 50 СТРОК ЛОГОВ:
--------------------------------------------------------------------------------
2025-11-23 14:25:14 | INFO | ✅ Позиция BTC-USDT закрыта: TP достигнут
2025-11-23 14:25:15 | INFO | 📊 Сигнал для ETH-USDT: LONG
...
--------------------------------------------------------------------------------
```

---

### 7. `balance` - Проверить баланс
```bash
🔬 debug> balance
```
**Что делает:**
- Проверяет баланс на бирже (требует запущенный бот)
- Альтернатива: `python tests/debug/check_balance.py`

---

### 8. `help` - Показать справку
```bash
🔬 debug> help
```
**Что делает:**
- Показывает полную справку по всем командам
- Список дополнительных инструментов

---

### 9. `exit` / `quit` / `q` - Выход
```bash
🔬 debug> exit
```
**Что делает:**
- Выходит из debug консоли

---

## 🛠️ ДОПОЛНИТЕЛЬНЫЕ ИНСТРУМЕНТЫ

### 📊 Полная симуляция бота
```bash
python tests/debug/bot_full_simulator.py
```
**Что делает:**
- Полная симуляция работы бота
- Проверка всех модулей
- Валидация параметров

---

### 💰 Проверка баланса
```bash
python tests/debug/check_balance.py
```
**Что делает:**
- Проверяет баланс на OKX
- Показывает доступные средства
- Проверяет маржу

---

### ✅ Валидация конфигов
```bash
python scripts/validate_configs.py
```
**Что делает:**
- Проверяет синтаксис YAML
- Валидирует структуру
- Проверяет соответствие Pydantic моделям

---

### 🔍 Глубокий анализ конфигов
```bash
python scripts/deep_config_analysis.py
```
**Что делает:**
- Глубокий анализ структуры конфигов
- Проверка дублирования ключей
- Проверка порядка ключей

---

## 📝 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Быстрая проверка конфигурации
```bash
🔬 debug> config
🔬 debug> check
```

### Пример 2: Просмотр логов
```bash
🔬 debug> logs
```

### Пример 3: Проверка параметров символа
```bash
🔬 debug> params
Введите символ (например, BTC-USDT): BTC-USDT
```

---

## 🎯 РЕКОМЕНДУЕМЫЙ WORKFLOW

### Перед запуском бота:
1. **Проверка конфигурации:**
   ```bash
   debug_console.bat
   # В консоли:
   config
   check
   ```

2. **Валидация конфигов:**
   ```bash
   python scripts/validate_configs.py
   ```

3. **Полная симуляция:**
   ```bash
   python tests/debug/bot_full_simulator.py
   ```

### Во время работы бота:
1. **Просмотр логов:**
   ```bash
   debug_console.bat
   # В консоли:
   logs
   ```

2. **Проверка позиций:**
   ```bash
   debug_console.bat
   # В консоли:
   positions
   ```

---

## 🔧 РАСШИРЕНИЕ ФУНКЦИОНАЛЬНОСТИ

Debug консоль легко расширяется. Для добавления новых команд:

1. Добавьте метод в класс `DebugConsole`
2. Добавьте обработку команды в метод `run()`
3. Обновите справку в `show_help()`

**Пример добавления команды:**
```python
def show_new_feature(self):
    """Новая функция"""
    print("Новая функция работает!")

# В методе run():
elif command == "new":
    self.show_new_feature()
```

---

## 📚 СВЯЗАННЫЕ ДОКУМЕНТЫ

- `tests/debug/README.md` - Полное руководство по debug инструментам
- `docs/current/✅_ПОЛНЫЙ_АНАЛИЗ_КОНФИГУРАЦИЙ.md` - Анализ конфигураций
- `src/strategies/modules/debug_logger.py` - DebugLogger модуль

---

## ⚠️ ПРИМЕЧАНИЯ

1. **Требования:**
   - Python 3.8+
   - Установленные зависимости из `requirements.txt`

2. **Ограничения:**
   - Некоторые команды требуют запущенный бот
   - Для проверки баланса нужны API ключи

3. **Безопасность:**
   - Debug консоль не выполняет торговые операции
   - Все операции только для чтения

---

**Статус:** ✅ **ГОТОВО К ИСПОЛЬЗОВАНИЮ**

