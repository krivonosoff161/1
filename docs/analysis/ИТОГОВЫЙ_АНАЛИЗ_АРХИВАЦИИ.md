# 🔍 ИТОГОВЫЙ АНАЛИЗ АРХИВАЦИИ И ЧТЕНИЯ ЛОГОВ

**Дата:** 2025-11-16  
**Статус:** ✅ Исправлено

---

## 📋 ТЕКУЩАЯ СИТУАЦИЯ

### **Два подхода к архивации:**

1. **`clean_logs.bat`** - перемещает логи и CSV в папку `archived/<дата_время>/`
   - Простое перемещение файлов (не ZIP)
   - Перемещает логи + CSV сделки в одну папку
   - Создает папку с датой/временем для каждого запуска

2. **`archive_logs.py`** - создает ZIP архивы с логами + JSON/CSV
   - Создает ZIP архивы
   - Включает логи + сделки (JSON/CSV) в один архив
   - Автоматическая архивация по возрасту (7 дней)

---

## ✅ ИСПРАВЛЕНИЯ

### **1. Анализатор читает из подпапок `archived`**

**Файл:** `logs/analyze_logs.py:149-167`

```python
# ✅ ИСПРАВЛЕНО: Ищем .zip архивы и .log файлы в папке archived (включая подпапки)
archived_dir = futures_dir / "archived"
if archived_dir.exists():
    # Ищем .zip архивы в корне archived
    for zip_file in archived_dir.glob("*.zip"):
        log_files.append(zip_file)
    
    # Ищем .zip архивы и .log файлы в подпапках archived (рекурсивно)
    for subdir in archived_dir.iterdir():
        if subdir.is_dir():
            # Логи в подпапках (из clean_logs.bat)
            for log_file in subdir.glob("*.log"):
                if log_file.is_file():
                    log_files.append(log_file)
            
            # ZIP архивы в подпапках
            for zip_file in subdir.glob("*.zip"):
                if zip_file.is_file():
                    log_files.append(zip_file)
```

**Результат:** Анализатор находит и читает логи из папок, созданных `clean_logs.bat`.

---

### **2. Анализатор правильно читает ZIP с несколькими файлами**

**Файл:** `logs/analyze_logs.py:184-220`

**Проблема:** Если в ZIP архиве несколько файлов (лог + JSON/CSV сделки), читался первый попавшийся.

**Исправление:**
```python
# ✅ ИСПРАВЛЕНО: Ищем .log файл в архиве (игнорируем JSON/CSV)
log_files_in_zip = [f for f in file_list if f.endswith(".log") and not f.endswith(".csv") and not f.endswith(".json")]

if log_files_in_zip:
    # Читаем .log файл (не JSON/CSV сделки)
    if len(log_files_in_zip) == 1:
        log_to_read = log_files_in_zip[0]
    else:
        # Если несколько .log файлов, выбираем тот, который похож на имя архива
        archive_name = log_file.stem
        matching_logs = [f for f in log_files_in_zip if archive_name in f or f.startswith("futures_main")]
        log_to_read = matching_logs[0] if matching_logs else log_files_in_zip[0]
```

**Результат:** Анализатор читает только `.log` файлы из ZIP, игнорируя JSON/CSV сделки.

---

### **3. Архивирование логов + сделок вместе**

**Файл:** `logs/archive_logs.py:84-115`

**Исправление:** При архивации логов также архивируются соответствующие сделки (JSON/CSV) в тот же архив:

```python
# ✅ ИСПРАВЛЕНО: Архивируем лог + соответствующие сделки (JSON/CSV)
with zipfile.ZipFile(zip_path, "w", zipfile.ZIP_DEFLATED) as zipf:
    # Добавляем сам лог файл
    zipf.write(log_file, log_file.name)
    
    # Ищем соответствующие файлы сделок (по дате из имени файла)
    # Формат имени: futures_main_YYYY-MM-DD.log
    date_match = None
    for part in log_file.stem.split("_"):
        if len(part) == 10 and part.count("-") == 2:  # YYYY-MM-DD
            date_match = part
            break
    
    if date_match:
        # Ищем JSON и CSV файлы сделок с этой датой
        trades_json = trades_dir / f"trades_{date_match}.json"
        trades_csv = trades_dir / f"trades_{date_match}.csv"
        
        if trades_json.exists():
            zipf.write(trades_json, trades_json.name)
            print(f"   📄 Добавлен в архив: {trades_json.name}")
        
        if trades_csv.exists():
            zipf.write(trades_csv, trades_csv.name)
            print(f"   📄 Добавлен в архив: {trades_csv.name}")
```

**Результат:** Логи и сделки архивируются вместе в один ZIP, чтобы не было путаницы.

---

## 🔄 СОВМЕСТИМОСТЬ

### **Анализатор читает:**

1. ✅ `.log` файлы в корне `logs/futures/`
2. ✅ `.log` файлы в подпапках `logs/futures/<subdir>/`
3. ✅ `.zip` архивы в корне `logs/futures/`
4. ✅ `.zip` архивы в папке `logs/futures/archived/`
5. ✅ `.log` файлы в подпапках `logs/futures/archived/<дата_время>/` (из clean_logs.bat)
6. ✅ `.zip` архивы в подпапках `logs/futures/archived/<дата_время>/`

### **При чтении ZIP архивов:**

- ✅ Читает только `.log` файлы (игнорирует JSON/CSV сделки)
- ✅ Если несколько `.log` файлов, выбирает подходящий по имени
- ✅ Если нет `.log` файлов, игнорирует архив

---

## 📊 РЕКОМЕНДАЦИИ

### **Вариант 1: Использовать `archive_logs.py` (рекомендуется)**

**Преимущества:**
- ZIP архивы компактнее (экономия места)
- Логи + сделки в одном архиве (удобно)
- Автоматическая архивация по возрасту
- Анализатор полностью поддерживает

**Использование:**
```batch
# Через батник (если создать)
archive_logs.bat

# Или напрямую
python logs\archive_logs.py
```

---

### **Вариант 2: Обновить `clean_logs.bat` для создания ZIP**

**Идея:** Изменить `clean_logs.bat`, чтобы он создавал ZIP архивы (как `archive_logs.py`), а не просто перемещал файлы.

**Преимущества:**
- Единый подход к архивации
- Компактное хранение
- Логи + сделки вместе

---

### **Вариант 3: Оставить как есть (оба подхода)**

**Текущая ситуация:**
- `clean_logs.bat` - ручная архивация в папки
- `archive_logs.py` - автоматическая архивация в ZIP

**Условия:**
- ✅ Анализатор поддерживает оба подхода
- ✅ Читает логи из папок и ZIP архивов
- ⚠️ Может быть путаница, если используются оба подхода

---

## ✅ ИТОГО

**Статус:** ✅ Все исправлено

**Что работает:**
1. ✅ Анализатор читает логи из ZIP архивов
2. ✅ Анализатор читает логи из подпапок `archived` (из clean_logs.bat)
3. ✅ При архивации логи + сделки сохраняются вместе (в archive_logs.py)
4. ✅ При чтении ZIP читается только .log файл (игнорируются JSON/CSV)

**Рекомендация:**
- Использовать `archive_logs.py` для автоматической архивации
- Или обновить `clean_logs.bat`, чтобы он создавал ZIP архивы (как `archive_logs.py`)

---

**Дата:** 2025-11-16  
**Статус:** Готово к использованию

