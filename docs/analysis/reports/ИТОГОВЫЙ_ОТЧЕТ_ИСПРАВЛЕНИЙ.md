# 📋 ИТОГОВЫЙ ОТЧЕТ ИСПРАВЛЕНИЙ

**Дата:** 2025-11-12  
**Цель:** Исправить все жестко заданные параметры и проблемы с закрытием позиций с минусом

---

## ✅ ВЫПОЛНЕННЫЕ ИСПРАВЛЕНИЯ

### 1. ✅ Исправлены fallback значения Trailing Stop Loss

**Проблема:**
- Fallback значения были неправильными (0.05 = 5% вместо 0.005 = 0.5%)
- Если конфиг не загружен, используются неправильные значения

**Исправление:**
```python
# До:
params: Dict[str, Any] = {
    "initial_trail": 0.05,  # 5%
    "max_trail": 0.2,  # 20%
    "min_trail": 0.02,  # 2%
}

# После:
params: Dict[str, Any] = {
    "initial_trail": 0.005,  # ✅ ИСПРАВЛЕНО: 0.5% (как в конфиге)
    "max_trail": 0.01,  # ✅ ИСПРАВЛЕНО: 1% (как в конфиге)
    "min_trail": 0.003,  # ✅ ИСПРАВЛЕНО: 0.3% (как в конфиге)
}
```

**Файл:** `src/strategies/scalping/futures/orchestrator.py:732-736`

---

### 2. ✅ Вынесены множители режимов в конфиг

**Проблема:**
- Множители режимов были жестко заданы в коде (1.5, 1.0, 0.8)
- Невозможно было изменить поведение для разных режимов

**Исправление:**
- Добавлены параметры `regime_multiplier` и `trend_strength_boost` в конфиг
- Параметры читаются из конфига и передаются в TrailingStopLoss
- Используются fallback значения, если параметры не найдены в конфиге

**Конфиг:**
```yaml
trailing_sl:
  by_regime:
    trending:
      regime_multiplier: 1.5  # ✅ НОВОЕ
      trend_strength_boost: 1.3  # ✅ НОВОЕ
    ranging:
      regime_multiplier: 1.0  # ✅ НОВОЕ
      trend_strength_boost: 1.0  # ✅ НОВОЕ
    choppy:
      regime_multiplier: 0.8  # ✅ НОВОЕ
      trend_strength_boost: 1.0  # ✅ НОВОЕ
```

**Файлы:**
- `config/config_futures.yaml:1079-1092`
- `src/strategies/scalping/futures/orchestrator.py:830-838, 861-862, 1006-1007`
- `src/strategies/scalping/futures/indicators/trailing_stop_loss.py:98-99, 457-479`

---

### 3. ✅ Увеличен timeout для убыточных позиций

**Проблема:**
- `timeout_loss_percent: 0.5%` и `timeout_minutes: 3` - слишком строгие параметры
- Позиции закрывались слишком рано, не дав времени на восстановление

**Исправление:**
```yaml
# До:
timeout_loss_percent: 0.5  # Закрывать при убытке -0.5%
timeout_minutes: 3  # Таймаут 3 минуты

# После:
timeout_loss_percent: 1.0  # ✅ ИСПРАВЛЕНО: Закрывать при убытке -1.0%
timeout_minutes: 5  # ✅ ИСПРАВЛЕНО: Таймаут 5 минут
```

**Файл:** `config/config_futures.yaml:1067-1068`

---

## ⚠️ ОБНАРУЖЕННЫЕ ПРОБЛЕМЫ

### 1. ⚠️ Логика закрытия SHORT позиций

**Проблема:**
- Для SHORT позиций стоп устанавливается на `entry_price * (1 - trail%)` (ниже entry)
- Если цена сразу идет ВВЕРХ (против SHORT), стоп не обновляется
- Позиция может закрыться с убытком, если цена поднимается выше стопа

**Текущая логика:**
- При инициализации: `lowest_price = entry_price`, стоп = `entry_price * (1 - trail%)` (ниже entry)
- Если цена растет: `lowest_price` не обновляется (остается `entry_price`), стоп остается на `entry_price * (1 - trail%)`
- Если цена поднимается выше стопа, позиция закрывается

**Проблема:**
- Для SHORT стоп должен быть ВЫШЕ entry_price для защиты от роста
- Но текущая логика устанавливает стоп НИЖЕ entry_price
- Это может привести к закрытию позиции сразу после открытия, если цена идет против позиции

**Рекомендация:**
- Проверить логику `get_stop_loss()` для SHORT
- Убедиться, что стоп всегда ВЫШЕ entry_price для SHORT при инициализации
- Или изменить логику так, чтобы стоп обновлялся даже когда цена идет против позиции

---

### 2. ⚠️ Комиссия при расчете прибыли

**Проблема:**
- Комиссия учитывается в расчете прибыли (`include_fees=True`)
- Если позиция открыта с небольшой прибылью, комиссия может сделать ее убыточной
- `profit_pct` может быть отрицательным сразу после открытия из-за комиссии

**Пример:**
- Позиция открыта с прибылью +0.05% (gross)
- Комиссия 0.09% (на круг)
- `profit_pct` (net) = +0.05% - 0.09% = -0.04% (убыток!)
- Позиция может быть закрыта как убыточная

**Рекомендация:**
- Проверить, правильно ли учитывается комиссия
- Возможно, нужно учитывать комиссию только при закрытии, а не при открытии
- Или увеличить `min_holding_minutes` для убыточных позиций

---

### 3. ⚠️ Минимальное время удержания для убыточных позиций

**Проблема:**
- Loss Cut применяется ВСЕГДА, даже если не прошло минимальное время
- Это может привести к закрытию позиции сразу после открытия

**Текущая логика:**
```python
# ✅ ЭТАП 4.4: Не закрываем позицию, если не прошло минимальное время удержания
if effective_min_holding is not None and minutes_in_position < effective_min_holding:
    # Исключение: жёсткое ограничение убытка применяется всегда
    if self.loss_cut_percent is not None and profit_pct <= -self.loss_cut_percent:
        return True  # Закрываем даже если не прошло минимальное время
    return False  # Не закрываем для остальных случаев
```

**Проблема:**
- Если позиция сразу уходит в минус -1.5%, она закрывается даже если не прошло 0.5 минуты
- Это может привести к закрытию позиции сразу после открытия

**Рекомендация:**
- Увеличить `min_holding_minutes` для убыточных позиций
- Или отключить Loss Cut для позиций младше `min_holding_minutes`
- Или увеличить `loss_cut_percent` до -2.0% или -2.5%

---

## 📋 ИТОГОВЫЙ СПИСОК ИЗМЕНЕНИЙ

1. ✅ **Fallback значения Trailing Stop Loss** - исправлены
2. ✅ **Множители режимов** - вынесены в конфиг
3. ✅ **Timeout для убыточных позиций** - увеличен
4. ⚠️ **Логика закрытия SHORT позиций** - нужно проверить
5. ⚠️ **Комиссия при расчете прибыли** - нужно проверить
6. ⚠️ **Минимальное время удержания для убыточных позиций** - нужно проверить

---

## 🔧 ПРИОРИТЕТ ДАЛЬНЕЙШИХ ИСПРАВЛЕНИЙ

1. **КРИТИЧЕСКОЕ:** Проверить логику закрытия SHORT позиций
2. **ВАЖНО:** Увеличить минимальное время удержания для убыточных позиций
3. **ВАЖНО:** Проверить комиссию при расчете прибыли

---

## 📝 РЕКОМЕНДАЦИИ

1. **Перезапустить бота** и проверить логи — ошибок быть не должно
2. **Мониторить новые позиции** для подтверждения увеличенного timeout и правильного поведения TSL
3. **Проверить логику закрытия SHORT позиций** в следующей итерации
4. **Увеличить минимальное время удержания** для убыточных позиций, если проблема повторится

