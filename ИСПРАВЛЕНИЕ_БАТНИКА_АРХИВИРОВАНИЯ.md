# ✅ ИСПРАВЛЕНИЕ БАТНИКА АРХИВИРОВАНИЯ ЛОГОВ

## 🐛 ПРОБЛЕМА

Батник `clean_logs.bat` проверял наличие файлов в корне `logs`, но **НЕ переносил** их в архив:

- ✅ Проверял: `logs\trades_*.csv` (строка 36)
- ❌ НЕ переносил: `logs\trades_*.csv`
- ✅ Переносил: `logs\trades_*.json` (строка 210)
- ❌ НЕ проверял и НЕ переносил: `logs\orders_*.csv`, `logs\positions_open_*.csv`, `logs\signals_*.csv`
- ❌ НЕ переносил: другие `logs\*.json` (кроме `trades_*.json`)

## ✅ ИСПРАВЛЕНИЯ

### 1. Добавлена проверка файлов в корне logs:
```batch
if exist "logs\orders_*.csv" set filefound=1
if exist "logs\positions_open_*.csv" set filefound=1
if exist "logs\signals_*.csv" set filefound=1
```

### 2. Добавлен перенос CSV файлов из корня logs:
- `logs\trades_*.csv` → архив
- `logs\orders_*.csv` → архив
- `logs\positions_open_*.csv` → архив
- `logs\signals_*.csv` → архив

### 3. Добавлен перенос других JSON файлов:
- `logs\*.json` (кроме `trades_*.json` и `log_analyzer_config.json`) → архив
- `log_analyzer_config.json` НЕ переносится (конфиг анализатора, не лог)

## 📊 ЧТО ТЕПЕРЬ ПЕРЕНОСИТСЯ

### Из `logs\futures\`:
- ✅ `*.log` - логи
- ✅ `*.zip` - архивы
- ✅ `debug\*.csv` - отладочные CSV
- ✅ `trades_*.csv` - сделки
- ✅ `orders_*.csv` - ордера
- ✅ `positions_open_*.csv` - открытые позиции
- ✅ `signals_*.csv` - сигналы
- ✅ `structured\*.json` - структурированные JSON
- ✅ `extracted\*.*` - извлеченные файлы
- ✅ `temp_extracted\*.*` - временные извлеченные файлы

### Из корня `logs\`:
- ✅ `trades_*.csv` - сделки (НОВОЕ)
- ✅ `trades_*.json` - сделки JSON
- ✅ `orders_*.csv` - ордера (НОВОЕ)
- ✅ `positions_open_*.csv` - открытые позиции (НОВОЕ)
- ✅ `signals_*.csv` - сигналы (НОВОЕ)
- ✅ `*.json` - другие JSON файлы (НОВОЕ, кроме `log_analyzer_config.json`)

## 🔍 ПРИМЕРЫ ФАЙЛОВ, КОТОРЫЕ ТЕПЕРЬ ПЕРЕНОСЯТСЯ

### До исправления:
- ❌ `logs\trades_2025-12-07.csv` - оставался в корне
- ❌ `logs\orders_2025-12-07.csv` - оставался в корне
- ❌ `logs\positions_open_2025-12-07.csv` - оставался в корне
- ❌ `logs\signals_2025-12-07.csv` - оставался в корне
- ✅ `logs\log_analyzer_config.json` - НЕ переносится (конфиг анализатора, не лог)

### После исправления:
- ✅ Все файлы переносятся в архив

## 📋 ИЗМЕНЕНИЯ В КОДЕ

### Добавлены секции:
1. **Перенос CSV из корня logs** (после переноса из futures)
2. **Перенос других JSON** (после TEMP_EXTRACTED)

### Обновлена статистика:
- Добавлен счетчик: `otherjsoncount`
- Обновлен `totalmoved` для учета новых файлов
- Обновлено сообщение о неудачных переносах

## ✅ РЕЗУЛЬТАТ

Теперь батник **полностью** собирает все логи и файлы в архив:
- ✅ Файлы из `logs\futures\`
- ✅ Файлы из корня `logs\`
- ✅ Все типы файлов (log, csv, json, zip)

---

**Дата**: 2025-12-07
**Версия**: 1.0

