# ✅ Исправление ошибок адаптивных фильтров

## 🔧 Что исправлено:

### 1. ✅ Исправлен `get_atr()` → `get_indicators()`

**Было:**
```python
atr = self.data_registry.get_atr(symbol, period=14)  # ❌ Метода нет!
```

**Стало:**
```python
indicators = await self.data_registry.get_indicators(symbol)
atr = indicators.get("atr", 0.0) if indicators else 0.0
```

---

### 2. ✅ Исправлен `get_current_price()` → `get_price()`

**Было:**
```python
current_price = self.data_registry.get_current_price(symbol)  # ❌ Метода нет!
```

**Стало:**
```python
current_price = await self.data_registry.get_price(symbol)
```

---

### 3. ✅ Исправлен `get_symbol_stats()` → `get_win_rate()`

**Было:**
```python
stats = self.trading_statistics.get_symbol_stats(symbol, lookback_hours=24)  # ❌ Метода нет!
win_rate = stats.get("win_rate", 0.5)
```

**Стало:**
```python
win_rate = self.trading_statistics.get_win_rate(regime=None, symbol=symbol)
```

---

### 4. ✅ Методы сделаны async

**Изменено:**
- `get_reversal_threshold()` → `async def get_reversal_threshold()`
- `get_adx_threshold()` → `async def get_adx_threshold()`
- `get_macd_divisor()` → `async def get_macd_divisor()`
- `get_strength_multiplier()` → `async def get_strength_multiplier()`
- `_get_volatility_factor()` → `async def _get_volatility_factor()`

**Обновлено:**
- Все вызовы этих методов теперь используют `await`

---

## ✅ Проверка:

- ✅ Код компилируется без ошибок
- ✅ Все методы используют правильные API
- ✅ Типы данных проверяются

---

**Дата:** 23 декабря 2025  
**Статус:** ✅ Исправлено

