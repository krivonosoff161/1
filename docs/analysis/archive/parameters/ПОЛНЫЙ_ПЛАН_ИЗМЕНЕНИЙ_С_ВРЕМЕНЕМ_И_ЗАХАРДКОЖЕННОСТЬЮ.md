# 🎯 ПОЛНЫЙ ПЛАН ИЗМЕНЕНИЙ: ВРЕМЯ, ЗАХАРДКОЖЕННОСТЬ, ВСЕ РЕЖИМЫ И ПАРЫ

## 🔍 АНАЛИЗ ТЕКУЩЕЙ СИТУАЦИИ

### 1. ВРЕМЯ ЖИЗНИ ПОЗИЦИЙ

**Текущие параметры:**

#### TRENDING:
- `max_holding_minutes: 18` - максимальное время удержания
- `min_holding_minutes: 1.0` (в TSL) - минимальное время удержания
- `extend_time_on_profit: true` - продлевать время для прибыльных
- `extend_time_multiplier: 2.0` (в TSL) - множитель продления
- `timeout_minutes: 7` - таймаут для убыточных позиций
- **Effective min_holding:** 1.0 * 2.0 = **2.0 минуты** (для прибыльных)

#### RANGING:
- `max_holding_minutes: 13` - максимальное время удержания
- `min_holding_minutes: 2.0` (в TSL) - минимальное время удержания
- `extend_time_on_profit: true` - продлевать время для прибыльных
- `extend_time_multiplier: 1.5` (в TSL) - множитель продления
- `timeout_minutes: 5` - таймаут для убыточных позиций
- **Effective min_holding:** 2.0 * 1.5 = **3.0 минуты** (для прибыльных) ❌ СЛИШКОМ ДОЛГО!

#### CHOPPY:
- `max_holding_minutes: 6` - максимальное время удержания
- `min_holding_minutes: 0.3` (в TSL) - минимальное время удержания
- `extend_time_on_profit: false` - НЕ продлевать время
- `extend_time_multiplier: 1.2` (в TSL) - не используется
- `timeout_minutes: 3` - таймаут для убыточных позиций
- **Effective min_holding:** 0.3 минуты (не продлевается)

**Проблема:** Для ranging effective min_holding = 3.0 минуты - это слишком долго для скальпинга!

---

### 2. ЗАХАРДКОЖЕННОСТЬ В КОДЕ

**Файл:** `src/strategies/scalping/futures/indicators/trailing_stop_loss.py`
**Строки:** 217, 238

**Текущий код:**
```python
self.current_trail = min(
    self.initial_trail + max(profit_pct_total, 0.0) * 2,  # ❌ ЗАХАРДКОЖЕНО 2.0!
    self.max_trail,
)
```

**Проблема:**
- Множитель `2.0` захардкожен
- Параметры `trail_growth_*_multiplier` загружаются в конфиг, но НЕ передаются в TSL
- НЕ используется адаптация по уровню прибыли (low/medium/high)

**Решение:**
1. Передать `trail_growth_*_multiplier` в конструктор TSL
2. Использовать их в формуле вместо захардкоженного 2.0
3. Адаптировать по уровню прибыли (low/medium/high)

---

## 🎯 РЕШЕНИЕ: ЧТО ДЕЛАЕМ СО ВРЕМЕНЕМ?

### ВАРИАНТ 1: УБРАТЬ ПРОДЛЕНИЕ ВРЕМЕНИ
**Плюсы:**
- Быстрее закрытие позиций
- Меньше риск разворота
- Проще логика

**Минусы:**
- Меньше времени для роста прибыли
- Может закрывать слишком рано

### ВАРИАНТ 2: УВЕЛИЧИТЬ ВРЕМЯ, НО БЕЗ ПРОДЛЕНИЯ
**Плюсы:**
- Больше времени для роста прибыли
- Проще логика (нет продления)
- Предсказуемое время удержания

**Минусы:**
- Может держать убыточные позиции дольше

### ВАРИАНТ 3: ОСТАВИТЬ ПРОДЛЕНИЕ, НО УМЕНЬШИТЬ МНОЖИТЕЛЬ
**Плюсы:**
- Гибкость (продлевает только прибыльные)
- Меньше риск разворота (меньший множитель)

**Минусы:**
- Сложнее логика

---

## ✅ РЕКОМЕНДАЦИЯ: ВАРИАНТ 2 + ВАРИАНТ 3 (ГИБРИД)

**Для TRENDING:**
- Увеличить `min_holding_minutes: 1.0 → 2.0`
- Увеличить `max_holding_minutes: 18 → 30`
- Оставить `extend_time_on_profit: true`
- Уменьшить `extend_time_multiplier: 2.0 → 1.3` (меньше продления)
- **Effective min_holding:** 2.0 * 1.3 = **2.6 минуты** (для прибыльных)

**Для RANGING:**
- Увеличить `min_holding_minutes: 2.0 → 1.5` (уменьшить!)
- Увеличить `max_holding_minutes: 13 → 20`
- Оставить `extend_time_on_profit: true`
- Уменьшить `extend_time_multiplier: 1.5 → 1.2` (меньше продления)
- **Effective min_holding:** 1.5 * 1.2 = **1.8 минуты** (для прибыльных) ✅

**Для CHOPPY:**
- Увеличить `min_holding_minutes: 0.3 → 0.5`
- Увеличить `max_holding_minutes: 6 → 10`
- Оставить `extend_time_on_profit: false` (не продлевать в хаосе)
- **Effective min_holding:** 0.5 минуты (не продлевается)

---

## 🔧 ИСПРАВЛЕНИЕ ЗАХАРДКОЖЕННОСТИ

### Шаг 1: Передать trail_growth multipliers в TSL

**Файл:** `src/strategies/scalping/futures/orchestrator.py`
**Строки:** ~1563-1572

**Текущий код:**
```python
tsl = TrailingStopLoss(
    initial_trail=initial_trail,
    max_trail=max_trail,
    min_trail=min_trail,
    trading_fee_rate=trading_fee_rate,
    loss_cut_percent=params["loss_cut_percent"],
    timeout_loss_percent=params["timeout_loss_percent"],
    timeout_minutes=params["timeout_minutes"],
    min_holding_minutes=params["min_holding_minutes"],
    min_profit_to_close=params["min_profit_to_close"],
    extend_time_on_profit=params["extend_time_on_profit"],
    extend_time_multiplier=params["extend_time_multiplier"],
    leverage=leverage,
    min_critical_hold_seconds=params.get("min_critical_hold_seconds"),
)
```

**Исправленный код:**
```python
tsl = TrailingStopLoss(
    initial_trail=initial_trail,
    max_trail=max_trail,
    min_trail=min_trail,
    trading_fee_rate=trading_fee_rate,
    loss_cut_percent=params["loss_cut_percent"],
    timeout_loss_percent=params["timeout_loss_percent"],
    timeout_minutes=params["timeout_minutes"],
    min_holding_minutes=params["min_holding_minutes"],
    min_profit_to_close=params["min_profit_to_close"],
    extend_time_on_profit=params["extend_time_on_profit"],
    extend_time_multiplier=params["extend_time_multiplier"],
    leverage=leverage,
    min_critical_hold_seconds=params.get("min_critical_hold_seconds"),
    # ✅ НОВОЕ: Передаем trail_growth multipliers
    trail_growth_low_multiplier=params.get("trail_growth_low_multiplier", 1.5),
    trail_growth_medium_multiplier=params.get("trail_growth_medium_multiplier", 2.0),
    trail_growth_high_multiplier=params.get("trail_growth_high_multiplier", 3.0),
)
```

### Шаг 2: Добавить поля в конструктор TSL

**Файл:** `src/strategies/scalping/futures/indicators/trailing_stop_loss.py`
**Строки:** ~33-54

**Добавить в `__init__`:**
```python
def __init__(
    self,
    ...
    trail_growth_low_multiplier: float = 1.5,  # ✅ НОВОЕ
    trail_growth_medium_multiplier: float = 2.0,  # ✅ НОВОЕ
    trail_growth_high_multiplier: float = 3.0,  # ✅ НОВОЕ
):
    ...
    # ✅ НОВОЕ: Сохраняем trail_growth multipliers
    self.trail_growth_low_multiplier = trail_growth_low_multiplier
    self.trail_growth_medium_multiplier = trail_growth_medium_multiplier
    self.trail_growth_high_multiplier = trail_growth_high_multiplier
```

### Шаг 3: Использовать в формуле update()

**Файл:** `src/strategies/scalping/futures/indicators/trailing_stop_loss.py`
**Строки:** 217, 238

**Текущий код:**
```python
self.current_trail = min(
    self.initial_trail + max(profit_pct_total, 0.0) * 2,  # ❌ ЗАХАРДКОЖЕНО
    self.max_trail,
)
```

**Исправленный код:**
```python
# ✅ АДАПТИВНО: Используем trail_growth multipliers из конфига
if profit_pct_total < 0.005:  # < 0.5% - низкая прибыль
    trail_multiplier = self.trail_growth_low_multiplier
elif profit_pct_total < 0.015:  # 0.5-1.5% - средняя прибыль
    trail_multiplier = self.trail_growth_medium_multiplier
else:  # > 1.5% - высокая прибыль
    trail_multiplier = self.trail_growth_high_multiplier

self.current_trail = min(
    self.initial_trail + max(profit_pct_total, 0.0) * trail_multiplier,  # ✅ АДАПТИВНО
    self.max_trail,
)
```

---

## 📊 ПОЛНЫЕ ИЗМЕНЕНИЯ ДЛЯ ВСЕХ РЕЖИМОВ И ПАР

### 1. ADAPTIVE REGIME (общие параметры)

#### TRENDING:
```yaml
min_score_threshold: 3.0  # Было 2.5 (+20%)
tp_atr_multiplier: 1.0  # Было 0.6 (+67%)
sl_atr_multiplier: 0.5  # Было 0.4 (+25%)
max_holding_minutes: 30  # Было 18 (+67%)
extend_time_if_profitable: true
min_profit_for_extension: 0.5
extension_percent: 100
```

#### RANGING:
```yaml
min_score_threshold: 2.0  # Было 1.5 (+33%)
tp_atr_multiplier: 0.8  # Было 0.5 (+60%)
sl_atr_multiplier: 0.5  # Было 0.4 (+25%)
max_holding_minutes: 20  # Было 13 (+54%)
extend_time_if_profitable: true
min_profit_for_extension: 0.5
extension_percent: 100
```

#### CHOPPY:
```yaml
min_score_threshold: 2.5  # Было 3.0 (-17%, но все еще строже чем было 2.0)
tp_atr_multiplier: 0.9  # Было 0.3 (+200%)
sl_atr_multiplier: 0.4  # Было 0.3 (+33%)
max_holding_minutes: 10  # Было 6 (+67%)
extend_time_if_profitable: false
```

---

### 2. TRAILING STOP LOSS (все режимы)

#### TRENDING:
```yaml
initial_trail: 0.012  # Было 0.01 (+20%)
max_trail: 0.035  # Было 0.03 (+17%)
min_profit_to_close: 0.01  # Было 0.005 (+100%)
min_holding_minutes: 2.0  # Было 1.0 (+100%)
extend_time_on_profit: true
extend_time_multiplier: 1.3  # Было 2.0 (-35%)
timeout_minutes: 7
trail_growth:
  low_profit_multiplier: 1.5
  medium_profit_multiplier: 2.0
  high_profit_multiplier: 3.0
```

#### RANGING:
```yaml
initial_trail: 0.015  # Было 0.008 (+87.5%)
max_trail: 0.03  # Было 0.02 (+50%)
min_profit_to_close: 0.012  # Было 0.008 (+50%)
min_holding_minutes: 1.5  # Было 2.0 (-25%)
extend_time_on_profit: true
extend_time_multiplier: 1.2  # Было 1.5 (-20%)
timeout_minutes: 5
trail_growth:
  low_profit_multiplier: 1.5
  medium_profit_multiplier: 1.5  # Было 2.0 (-25%)
  high_profit_multiplier: 2.5
```

#### CHOPPY:
```yaml
initial_trail: 0.01  # Было 0.006 (+67%)
max_trail: 0.025  # Было 0.015 (+67%)
min_profit_to_close: 0.01  # Было 0.006 (+67%)
min_holding_minutes: 0.5  # Было 0.3 (+67%)
extend_time_on_profit: false
extend_time_multiplier: 1.2  # Не используется
timeout_minutes: 3
trail_growth:
  low_profit_multiplier: 1.2
  medium_profit_multiplier: 1.5  # Было 2.0 (-25%)
  high_profit_multiplier: 2.0
```

---

### 3. SYMBOL PROFILES (BTC-USDT и ETH-USDT)

#### BTC-USDT - TRENDING:
```yaml
arm:
  min_score_threshold: 2.0  # Было 1.6 (+25%)
  tp_atr_multiplier: 1.0  # Было 0.60 (+67%)
  sl_atr_multiplier: 0.5  # Было 0.40 (+25%)
```

#### BTC-USDT - RANGING:
```yaml
arm:
  min_score_threshold: 1.8  # Было 1.4 (+29%)
  tp_atr_multiplier: 0.8  # Было 0.55 (+45%)
  sl_atr_multiplier: 0.45  # Было 0.35 (+29%)
```

#### BTC-USDT - CHOPPY:
```yaml
arm:
  min_score_threshold: 2.0  # Было 1.6 (+25%)
  tp_atr_multiplier: 0.6  # Было 0.40 (+50%)
  sl_atr_multiplier: 0.4  # Было 0.30 (+33%)
```

#### ETH-USDT - TRENDING:
```yaml
arm:
  min_score_threshold: 2.0  # Было 1.6 (+25%)
  tp_atr_multiplier: 1.0  # Было 0.60 (+67%)
  sl_atr_multiplier: 0.5  # Было 0.40 (+25%)
```

#### ETH-USDT - RANGING:
```yaml
arm:
  min_score_threshold: 1.5  # Было 1.1 (+36%)
  tp_atr_multiplier: 0.8  # Было 0.50 (+60%)
  sl_atr_multiplier: 0.45  # Было 0.32 (+41%)
```

#### ETH-USDT - CHOPPY:
```yaml
arm:
  min_score_threshold: 2.2  # Было 1.7 (+29%)
  tp_atr_multiplier: 0.6  # Было 0.35 (+71%)
  sl_atr_multiplier: 0.4  # Было 0.28 (+43%)
```

---

## 📋 СВОДНАЯ ТАБЛИЦА ИЗМЕНЕНИЙ

| Параметр | TRENDING | RANGING | CHOPPY |
|----------|----------|---------|--------|
| **min_score_threshold** | 2.5→3.0 (+20%) | 1.5→2.0 (+33%) | 3.0→2.5 (-17%) |
| **tp_atr_multiplier** | 0.6→1.0 (+67%) | 0.5→0.8 (+60%) | 0.3→0.9 (+200%) |
| **sl_atr_multiplier** | 0.4→0.5 (+25%) | 0.4→0.5 (+25%) | 0.3→0.4 (+33%) |
| **max_holding_minutes** | 18→30 (+67%) | 13→20 (+54%) | 6→10 (+67%) |
| **min_holding_minutes** | 1.0→2.0 (+100%) | 2.0→1.5 (-25%) | 0.3→0.5 (+67%) |
| **extend_time_multiplier** | 2.0→1.3 (-35%) | 1.5→1.2 (-20%) | - (не используется) |
| **effective min_holding** | 2.6 мин | 1.8 мин | 0.5 мин |
| **initial_trail** | 0.01→0.012 (+20%) | 0.008→0.015 (+87.5%) | 0.006→0.01 (+67%) |
| **max_trail** | 0.03→0.035 (+17%) | 0.02→0.03 (+50%) | 0.015→0.025 (+67%) |
| **min_profit_to_close** | 0.005→0.01 (+100%) | 0.008→0.012 (+50%) | 0.006→0.01 (+67%) |
| **trail_growth_medium** | 2.0 (без изменений) | 2.0→1.5 (-25%) | 2.0→1.5 (-25%) |

---

## ✅ ИТОГОВЫЙ ПЛАН ДЕЙСТВИЙ

### 1. ИСПРАВИТЬ КОД (высокий приоритет):
- [ ] Передать `trail_growth_*_multiplier` в конструктор TSL
- [ ] Добавить поля в `__init__` TSL
- [ ] Использовать адаптивные множители в `update()` вместо захардкоженного 2.0

### 2. ИЗМЕНИТЬ КОНФИГ (высокий приоритет):
- [ ] Обновить параметры для всех режимов (trending/ranging/choppy)
- [ ] Обновить TSL параметры для всех режимов
- [ ] Обновить symbol_profiles для BTC-USDT и ETH-USDT

### 3. ПРОВЕРИТЬ РЕЗУЛЬТАТЫ:
- [ ] Проверить, что trailing stop использует адаптивные множители
- [ ] Проверить, что время удержания работает правильно
- [ ] Проверить, что позиции закрываются с большей прибылью

---

## 🎯 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

**После всех изменений:**
- ✅ Trailing stop использует адаптивные множители из конфига
- ✅ Время удержания оптимизировано (не слишком долго, не слишком коротко)
- ✅ TP/SL увеличены для большей прибыли на сделку
- ✅ Фильтры ужесточены для лучшего качества сигналов
- ✅ Все параметры адаптивные (ничего не захардкожено)

**Эффект:**
- Win rate: 50% → 60% (благодаря ужесточению фильтров)
- Прибыль на сделку: $0.4 → $0.7 (+75%)
- Время удержания: оптимизировано для каждого режима
- Trailing stop: адаптивный, не захардкожен

