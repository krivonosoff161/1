# ПРОБЛЕМА FALLBACK И ЛИМИТНОЙ ЦЕНЫ

**Дата:** 23 ноября 2025  
**Проблемы:** 
1. Fallback используется для BTC-USDT (настройки не найдены)
2. Лимитная цена вне допустимого диапазона биржи

---

## ⚠️ ПРОБЛЕМА #1: FALLBACK ДЛЯ BTC-USDT

### Логи:
```
⚠️ FALLBACK: Используется глобальный offset из конфига: 0.0% 
(per-symbol+regime и per-regime не найдены для BTC-USDT, regime=ranging)
```

### Причина:
- Настройки `by_symbol` для BTC-USDT не найдены в конфиге
- Или структура конфига неправильная

### Проверка:
- Нужно проверить структуру `by_symbol` в конфиге
- Нужно проверить логику чтения `by_symbol` в коде

---

## ⚠️ ПРОБЛЕМА #2: ЛИМИТНАЯ ЦЕНА ВНЕ ДИАПАЗОНА

### Логи:
```
💰 Лимитная цена для BTC-USDT sell: 85866.80 
(лимиты: max_buy=85909.83, min_sell=85823.87)

Ошибка биржи: 
Order price is not within the price limit 
(max buy price: 87,623.9, min sell price: 85,905.7)
```

### Проблема:
- Рассчитанная лимитная цена: `85866.80`
- Лимиты из `get_price_limits`: `min_sell=85823.87`
- Реальные лимиты биржи: `min_sell=85,905.7`

**Разница:** `85,905.7 - 85,866.80 = 38.9` (цена ниже лимита!)

### Причина:
- `get_price_limits` возвращает неправильные лимиты
- Или лимиты биржи изменились между запросами
- Или расчет `min_sell_price` неправильный

---

## ✅ НУЖНО ИСПРАВИТЬ

1. ✅ Проверить структуру `by_symbol` в конфиге
2. ✅ Исправить расчет `min_sell_price` в `get_price_limits`
3. ✅ Добавить проверку лимитов биржи перед размещением ордера

---

**Статус:** ⚠️ ТРЕБУЕТСЯ ИСПРАВЛЕНИЕ

