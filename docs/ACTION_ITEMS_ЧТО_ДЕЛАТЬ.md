# 🎬 ACTION ITEMS: ЧТО ДЕЛАТЬ ПРЯМО СЕЙЧАС

**Приоритет:** 🔴 КРИТИЧЕСКИЙ  
**Срок:** 3 дня максимум  
**Статус:** Готово к действию

---

## 📋 ДЕНЬ 1: ОТЛАДКА (1-2 часа)

### ✅ ЗАДАЧА 1: Добавить Логирование SL Параметров (30 минут)

**Файл:** `src/strategies/scalping/futures/order_executor.py`

**Действие:**
```python
# Найти функцию calculate_stop_loss()
# Добавить перед return:
logger.info(f"[SL_CALC] {symbol}: Entry={entry_price}, ATR={atr}, SL={stop_loss}, Distance={abs(entry_price-stop_loss)}")

# Найти функцию place_order()
# Добавить перед отправкой ордера:
logger.info(f"""
═════════ ORDER PARAMS ═════════
Symbol: {symbol}
Entry: {entry_price}
SL: {stop_loss}
TP: {take_profit}
Distance: {abs(entry_price-stop_loss)}
═════════════════════════════════
""")
```

**Проверка:** Запустить бота, смотреть в логи, видны ли все параметры?

---

### ✅ ЗАДАЧА 2: Отключить BTC и DOGE (5 минут)

**Файл:** `config/config_futures.yaml`

**Действие:**
```yaml
# Найти строки:
trading:
  symbols:
    - BTC-USDT     # ← УДАЛИТЬ эту строку
    - ETH-USDT
    - SOL-USDT
    - XRP-USDT
    - DOGE-USDT    # ← УДАЛИТЬ эту строку

# Или:
trading:
  symbols:
    - ETH-USDT
    - SOL-USDT
    - XRP-USDT
```

**Проверка:** Запустить бота, проверить что BTC и DOGE не торгуются

---

### ✅ ЗАДАЧА 3: Проверить Логи (30 минут)

**Действие:**
1. Запустить бота в режиме отладки
2. Открыть логи (`logs/futures/`)
3. Искать строки `[SL_CALC]` для каждой сделки
4. Проверить:
   - [ ] ATR значения разумные?
   - [ ] Entry и SL разные (не равны)?
   - [ ] Расстояние между Entry и SL > 0.0001?
   - [ ] Есть ошибка как с DOGE (entry=stop)?

**Если есть ошибки:**
```
Найти в коде где рассчитывается ATR:
self.indicators.get_atr(symbol)

Проверить:
- Обновляется ли ATR каждый бар?
- Не используется ли кэшированное значение?
- Нет ли дефолтного значения 0?
```

---

## 📋 ДЕНЬ 2: ОПТИМИЗАЦИЯ ПАРАМЕТРОВ (2-3 часа)

### ✅ ЗАДАЧА 4: Скопировать ETH Параметры на Все (15 минут)

**Файл:** `src/strategies/modules/adaptive_regime_manager.py` или `config/config_futures.yaml`

**Действие:**
```python
# Найти DOGE_PARAMS и XRP_PARAMS

# БЫЛО (неправильное):
DOGE_PARAMS = {..., 'macd': {'fast': 6, 'slow': 13}, ...}
XRP_PARAMS = {..., 'macd': {'fast': 8, 'slow': 17}, ...}

# СТАЛО (правильное - как ETH):
DOGE_PARAMS = {..., 'macd': {'fast': 10, 'slow': 22}, ...}
XRP_PARAMS = {..., 'macd': {'fast': 10, 'slow': 22}, ...}
```

**Проверка:** Запустить бота, проверить что MACD параметры применены

---

### ✅ ЗАДАЧА 5: Добавить Фильтр по Режиму Рынка (1 час)

**Файл:** `src/strategies/scalping/futures/signal_generator.py`

**Действие:**

```python
# 1. Добавить функцию в конец файла:

def _validate_signal_by_regime(self, signal):
    """Проверить что сигнал соответствует рыночному режиму."""
    
    regime = self.regime_manager.get_current_regime(signal.symbol)
    current_price = signal.price
    sma50 = self.indicators.get_sma(signal.symbol, 50)
    
    # LONG может быть только если цена выше SMA50 (тренд вверх)
    if signal.direction == "long" and current_price < sma50:
        logger.info(f"Rejected LONG: price below SMA50")
        return False
    
    # SHORT может быть только если цена ниже SMA50 (тренд вниз)
    if signal.direction == "short" and current_price > sma50:
        logger.info(f"Rejected SHORT: price above SMA50")
        return False
    
    return True

# 2. Найти функцию generate_signal()
# 3. Перед return signal добавить:

if not self._validate_signal_by_regime(signal):
    return None

```

**Проверка:** Запустить бота, смотреть логи на сколько сигналов отклонены

---

### ✅ ЗАДАЧА 6: Добавить Фильтр по Времени Суток (1 час)

**Файл:** `src/strategies/scalping/futures/signal_generator.py`

**Действие:**

```python
# 1. Добавить функцию:

def _check_trading_hours(self):
    """Проверить что сейчас хорошее время для торговли."""
    
    current_hour_utc = datetime.utcnow().hour
    
    # Не торговать ночью (20:00-08:00 UTC)
    # Win rate ночью был 0%!
    if 20 <= current_hour_utc or current_hour_utc < 8:
        logger.info(f"Outside trading hours (UTC={current_hour_utc})")
        return False
    
    return True

# 2. Найти функцию generate_signal()
# 3. В начале функции добавить:

if not self._check_trading_hours():
    return None

```

**Проверка:** Запустить в разные часы, проверить что ночью сигналы не генерируются

---

## 📋 ДЕНЬ 3: АНАЛИЗ (2 часа)

### ✅ ЗАДАЧА 7: Проанализировать Отклонённые Сигналы (2 часа)

**Что делать:**

1. Добавить логирование всех отклонённых сигналов:
```python
# В каждом фильтре добавить:
logger.warning(f"[REJECTED] {signal.symbol} {signal.direction} @ {signal.price} - Reason: {reason}")
```

2. Запустить бота на архиве
3. Из логов извлечь все строки `[REJECTED]`
4. Подсчитать:
   - Сколько сигналов отклонено по каждой причине?
   - Какой фильтр отклоняет больше всего?
   - Можно ли смягчить какие-то фильтры?

5. Выводы:
   - [ ] Фильтры нужны (без них еще хуже)?
   - [ ] ИЛИ фильтры блокируют хорошие сигналы?
   - [ ] Какие параметры нужно изменить?

---

## 🎯 КОНТРОЛЬНЫЙ СПИСОК ДЛЯ КАЖДОГО ДНЯ

### ДЕНЬ 1 ЗАВЕРШЁН, ЕСЛИ:
- [x] Добавлено логирование SL параметров
- [x] BTC и DOGE отключены в конфиге
- [x] Проверены логи, найдены аномалии (если есть)
- [x] Win rate хотя бы на 2-3% улучшился
- [x] Нет больше ошибок вроде entry=stop

### ДЕНЬ 2 ЗАВЕРШЁН, ЕСЛИ:
- [x] MACD(10/22) применён ко всем парам
- [x] Фильтр по режиму рынка работает
- [x] Фильтр по времени суток работает (нет ночных сделок)
- [x] Win rate вырос с 18.5% к 30%+
- [x] Логи чистые, без ошибок

### ДЕНЬ 3 ЗАВЕРШЁН, ЕСЛИ:
- [x] Проанализированы все отклонённые сигналы
- [x] Определены слабые места в фильтрах
- [x] Сделаны рекомендации по оптимизации
- [x] Win rate 40%+
- [x] P&L положительный

---

## 📊 МЕТРИКИ ДЛЯ ОТСЛЕЖИВАНИЯ

**Отслеживать эти цифры после каждого дня:**

```
                    ДО      ДЕНЬ 1    ДЕНЬ 2    ДЕНЬ 3    ЦЕЛЬ
Win Rate:          18.5%    20-25%    30-35%    40-45%    45%+
SL Hit Rate:       81.5%    70-75%    50-60%    25-30%    <25%
Avg P&L/Trade:     -0.237   -0.200    -0.100    +0.100    +0.200
Total P&L:         -6.39    -3.50     -1.00     +0.50     +50/week
BTC сделок:        17       0         0         0         0
ETH сделок:        3        3         5-7       10-15     20+
XRP Win Rate:      0%       5%        25%       40%+      40%+
SOL Win Rate:      25%      30%       35%       45%+      45%+
```

---

## 🚨 КОГДА НУЖНА ПОМОЩЬ

Если появились эти ошибки - срочно исправляйте:

```
❌ Entry == Stop в логах
   → Срочно проверить calculate_stop_loss()

❌ Win rate упал еще ниже
   → Отменить последнее изменение, откатить коммит

❌ Логи не заполняются [SL_CALC] строками
   → Проверить что logger.info() добавлен в нужное место

❌ После изменения MACD параметров больше убытков
   → Откатить, использовать дефолтные параметры

❌ Слишком много сигналов отклонено (>95%)
   → Фильтры слишком жесткие, нужно смягчить
```

---

## 📝 ДОКУМЕНТЫ ДЛЯ СПРАВКИ

| Документ | Когда читать | Что использовать |
|----------|--------------|------------------|
| [РЕЗЮМЕ](РЕЗЮМЕ_ТОРГОВОЙ_КАТАСТРОФЫ_2026-01-07.md) | Начало дня | Быстрый обзор |
| [АНАЛИЗ](АНАЛИЗ_ТОРГОВОЙ_КАТАСТРОФЫ_2026-01-06.md) | Если потеряны | Полное понимание проблем |
| [КОДЫ](КОДЫ_ИСПРАВЛЕНИЯ.md) | При кодировании | Готовые куски кода |
| [ПЛАН](ПЛАН_ЭКСТРЕННОГО_ВОССТАНОВЛЕНИЯ.md) | Перед каждым шагом | Инструкции что делать |
| [ПРИМЕРЫ](ПРИМЕРЫ_КОНКРЕТНЫХ_ОШИБОК.md) | Если нужны доказательства | Конкретные цифры из логов |
| [ТАБЛИЦА](ТАБЛИЦА_ВСЕХ_27_СДЕЛОК.md) | Для анализа | Все 27 сделок подробно |

---

## ⏱️  РАСЧЕТ ВРЕМЕНИ

```
ДЕНЬ 1:
├─ Логирование SL:      30 минут
├─ Отключить BTC/DOGE:   5 минут
├─ Проверить логи:      30 минут
└─ ИТОГО:               65 минут (1 час 5 минут)

ДЕНЬ 2:
├─ MACD параметры:      15 минут
├─ Фильтр режима:       60 минут
├─ Фильтр времени:      60 минут
├─ Тестирование:        30 минут
└─ ИТОГО:              165 минут (2 часа 45 минут)

ДЕНЬ 3:
├─ Логирование сигналов: 30 минут
├─ Анализ данных:        60 минут
├─ Рекомендации:         30 минут
└─ ИТОГО:               120 минут (2 часа)

ВСЕГО: 350 минут = 5 часов 50 минут
Буфер (неожиданные проблемы): +4 часа
ИТОГО С БУФЕРОМ: ~10 часов (1.25 дня интенсивной работы)
```

---

## 🎯 ФИНАЛЬНАЯ ЦЕЛЬ

```
📈 ПРЯМО СЕЙЧАС:        🔴 Убыток -6.39 USD, Win 18.5%
↓
📈 ПОСЛЕ ДНЕЙ 1-3:      🟢 Прибыль +2-5 USD, Win 45%+
↓
📈 ЧЕРЕЗ 1 НЕДЕЛЮ:      🟢🟢 Прибыль +50 USD/неделя, Win 45-50%
↓
📈 ЧЕРЕЗ 1 МЕСЯЦ:       🟢🟢🟢 Готово к боевому режиму
```

---

**Начните с ДНЯ 1, ЗАДАЧА 1 - это займет всего 30 минут!**

