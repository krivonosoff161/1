# 🔍 ОТЧЕТ: Проблемы параметров индикаторов и фильтров

## ❌ КРИТИЧЕСКИЕ ПРОБЛЕМЫ:

### 1. EMA Периоды НЕ адаптируются под режим

**Проблема:**
- Индикаторы EMA создаются ОДИН РАЗ при инициализации с фиксированными периодами
- В коде: `EMA_12` и `EMA_26` с периодами из базового конфига (обычно 12/26)
- В конфиге для режимов указаны ДРУГИЕ периоды:
  - **trending**: ema_fast: 8, ema_slow: 21
  - **ranging**: ema_fast: 10, ema_slow: 25  
  - **choppy**: ema_fast: 12, ema_slow: 30

**Влияние:**
- Сигналы генерируются с НЕПРАВИЛЬНЫМИ периодами EMA
- Для trending должен быть 8/21, но используется 12/26 (слишком медленно)
- Для ranging должен быть 10/25, но используется 12/26 (не оптимально)
- Для choppy должен быть 12/30, но используется 12/26 (не оптимально)

**Решение:**
- Нужно создавать EMA индикаторы адаптивно для каждого режима
- Или пересчитывать EMA с правильными периодами при генерации сигналов

---

### 2. RSI Пороги - частично работают

**Статус:** ✅ Пороги (overbought/oversold) берутся из режима через `_get_regime_indicators_params`
- trending: rsi_overbought: 85, rsi_oversold: 25
- ranging: rsi_overbought: 85, rsi_oversold: 25
- choppy: rsi_overbought: 85, rsi_oversold: 25

**НО:** Период RSI всегда 14 (захардкожен в `__init__`)

---

### 3. MACD Параметры захардкожены

**Проблема:**
- MACD всегда использует 12/26/9 (захардкожено)
- Нет адаптации под режим

**Влияние:**
- MACD сигналы не оптимизированы для разных режимов рынка

---

### 4. min_ma_difference_pct - разные значения для режимов

**Статус:** ✅ Используются правильные значения из конфига:
- trending: 0.1% (0.001)
- ranging: 0.002% (0.00002) 
- choppy: 0.03% (0.0003)

**НО:** Проблема в том, что EMA рассчитываются с неправильными периодами, поэтому разница тоже неправильная!

---

### 5. ADX Threshold - используются правильно

**Статус:** ✅ Используются режим-специфичные пороги:
- trending: adx_threshold: 18.0
- ranging: adx_threshold: 20.0
- choppy: adx_threshold: 12.0

---

### 6. Bollinger Bands - захардкожены

**Проблема:**
- Period: 20, Std: 2.0 (захардкожено)
- Нет адаптации под режим

---

### 7. ATR Period - захардкожен

**Проблема:**
- ATR всегда использует period: 14
- В конфиге для choppy указано atr_period: 20, но НЕ используется

---

## 📊 ОПТИМИЗАЦИЯ ПАРАМЕТРОВ:

### Параметры, которые РАБОТАЮТ правильно:
1. ✅ RSI пороги (overbought/oversold) - адаптируются под режим
2. ✅ min_ma_difference_pct - адаптируется под режим
3. ✅ ADX threshold - адаптируется под режим
4. ✅ volume_threshold - адаптируется под режим
5. ✅ min_volatility_atr - адаптируется под режим

### Параметры, которые НЕ работают правильно:
1. ❌ EMA периоды - НЕ адаптируются (критично!)
2. ❌ RSI period - всегда 14 (не критично, но не оптимально)
3. ❌ MACD параметры - всегда 12/26/9 (не критично)
4. ❌ Bollinger Bands - всегда 20/2.0 (не критично)
5. ❌ ATR period - всегда 14 (в конфиге для choppy указано 20, но не используется)

---

## 🎯 РЕКОМЕНДАЦИИ:

### КРИТИЧНО исправить:
1. **EMA периоды** - сделать адаптивными для каждого режима
   - Это влияет на качество всех MA сигналов
   - Нужно либо создавать индикаторы адаптивно, либо пересчитывать значения

### Желательно исправить:
2. **ATR period для choppy** - использовать 20 вместо 14
3. **RSI period** - сделать адаптивным (но не критично, 14 - стандарт)

### Не критично:
4. MACD параметры - можно оставить стандартные
5. Bollinger Bands - можно оставить стандартные

---

## 📝 ДЕТАЛИ ПРОБЛЕМЫ EMA:

**Текущая реализация:**
```python
# В __init__ (строки 196-197):
self.indicator_manager.add_indicator("EMA_12", TALibEMA(period=ema_fast))  # ema_fast = 12 (базовое)
self.indicator_manager.add_indicator("EMA_26", TALibEMA(period=ema_slow))  # ema_slow = 26 (базовое)
```

**Проблема:**
- Индикаторы создаются ОДИН РАЗ с фиксированными периодами
- При генерации сигналов используются эти индикаторы, но они рассчитаны с НЕПРАВИЛЬНЫМИ периодами

**Что нужно:**
- Либо создавать EMA индикаторы адаптивно для каждого режима при генерации сигналов
- Либо пересчитывать EMA значения с правильными периодами для текущего режима

