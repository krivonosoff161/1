# Анализ проблемы незакрытия позиций - 30.11.2025

## 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА

**За весь день работы бота закрылась только 1 позиция из 5 открытых.**

## 📊 Что произошло

### Позиции на бирже (на момент остановки):
- **SOL-USDT**: PnL = -0.25 USDT (-0.59%)
- **ETH-USDT**: PnL = -4.67 USDT (-10.25%)
- **DOGE-USDT**: PnL = +0.02 USDT (+0.06%)
- **BTC-USDT**: (закрыта вручную)
- **XRP-USDT**: (закрыта вручную)

### Логи показывают:
- ExitAnalyzer вызывается регулярно (каждые 5 секунд)
- **ВСЕ решения ExitAnalyzer = None** (решение не принято - удерживаем позицию)
- Все позиции в режиме **"ranging"**
- Нет ни одного сообщения "TP достигнут" или "Big profit exit достигнут"

## 🔍 Анализ причин

### 1. **ExitAnalyzer не получает entry_price**

**Проблема:** ExitAnalyzer использует `_get_entry_price_and_side()` для получения entry_price, но:
- Может не найти entry_price в metadata
- Может не найти entry_price в position.avgPx
- Может не найти entry_price в PositionRegistry

**Симптомы:**
- В логах нет предупреждений "Не удалось получить entry_price" (значит entry_price получается)
- Но PnL% может рассчитываться неправильно

### 2. **PnL% рассчитывается неправильно**

**Проблема:** ExitAnalyzer рассчитывает PnL% как:
```python
pnl_percent = (current_price - entry_price) / entry_price - trading_fee_rate
```

**Но:**
- Если entry_price = 0 или None → PnL% = 0.0
- Если entry_price неправильный → PnL% неправильный
- Комиссия вычитается из PnL%, что может сделать его отрицательным даже при прибыли

**Пример:**
- ETH-USDT: PnL = -4.67 USDT (-10.25%) на бирже
- Но ExitAnalyzer может рассчитывать PnL% = 0.0% или отрицательный

### 3. **TP слишком высокий для текущего PnL**

**Проблема:** В конфиге для режима "ranging":
- `tp_percent = 2.4%` (глобальный)
- `partial_tp.trigger_percent = 0.6%` (для ranging)
- `big_profit_exit_percent_majors = 1.5%` (для BTC/ETH)
- `big_profit_exit_percent_alts = 2.0%` (для SOL/DOGE/XRP)

**Но:**
- Если PnL% = 0.0% (из-за неправильного entry_price) → TP не достигнут
- Если PnL% отрицательный → TP не достигнут
- Если PnL% положительный, но < 0.6% → partial_tp не срабатывает

### 4. **ExitAnalyzer не логирует детали**

**Проблема:** До добавления логирования не было видно:
- Какой entry_price используется
- Какой PnL% рассчитывается
- Почему TP не достигнут

## ✅ Исправления

### 1. **Добавлено детальное логирование**

Добавлено логирование в `_generate_exit_for_ranging()`:
- `🔍 ExitAnalyzer RANGING {symbol}: entry_price={entry_price:.2f}, current_price={current_price:.2f}, side={position_side}, PnL%={pnl_percent:.2f}%`
- `🔍 ExitAnalyzer RANGING {symbol}: TP={tp_percent:.2f}%, PnL%={pnl_percent:.2f}%, достигнут={pnl_percent >= tp_percent}`
- `🔍 ExitAnalyzer RANGING {symbol}: big_profit_exit={big_profit_exit_percent:.2f}%, PnL%={pnl_percent:.2f}%, достигнут={pnl_percent >= big_profit_exit_percent}`
- `🔍 ExitAnalyzer RANGING {symbol}: partial_tp enabled={enabled}, trigger_percent={trigger_percent:.2f}%`
- `🔍 ExitAnalyzer RANGING {symbol}: Нет причин для закрытия - TP={tp_percent:.2f}% (не достигнут), ...`

### 2. **Что нужно проверить в следующий раз**

1. **Проверить логи с новым логированием:**
   - Какой entry_price используется?
   - Какой PnL% рассчитывается?
   - Почему TP не достигнут?

2. **Проверить entry_price:**
   - Правильно ли сохраняется entry_price в metadata при открытии позиции?
   - Правильно ли получается entry_price из position.avgPx?

3. **Проверить PnL%:**
   - Совпадает ли PnL% из ExitAnalyzer с PnL% на бирже?
   - Правильно ли вычитается комиссия?

4. **Проверить TP:**
   - Не слишком ли высокий TP для текущего PnL?
   - Не слишком ли высокий partial_tp trigger?

## 🚀 Рекомендации

### Немедленные действия:

1. **Запустить бота с новым логированием** и проверить:
   - Какой entry_price используется
   - Какой PnL% рассчитывается
   - Почему TP не достигнут

2. **Проверить сохранение entry_price:**
   - При открытии позиции entry_price должен сохраняться в metadata
   - При загрузке существующих позиций entry_price должен загружаться из position.avgPx

3. **Проверить расчет PnL%:**
   - Сравнить PnL% из ExitAnalyzer с PnL% на бирже
   - Убедиться, что комиссия вычитается правильно

### Долгосрочные улучшения:

1. **Добавить fallback для entry_price:**
   - Если entry_price не найден, использовать текущую цену как fallback
   - Логировать предупреждение при использовании fallback

2. **Улучшить расчет PnL%:**
   - Использовать реальный PnL с биржи, если доступен
   - Вычитать комиссию только если позиция открыта > 10 секунд

3. **Снизить пороги TP:**
   - Для ranging режима: `tp_percent = 1.5%` (вместо 2.4%)
   - Для partial_tp: `trigger_percent = 0.4%` (вместо 0.6%)

## 📋 Чек-лист для следующего запуска

- [ ] Проверить логи с новым логированием
- [ ] Проверить entry_price для всех позиций
- [ ] Проверить PnL% для всех позиций
- [ ] Проверить, почему TP не достигнут
- [ ] Исправить проблему с entry_price (если найдена)
- [ ] Исправить проблему с PnL% (если найдена)
- [ ] Снизить пороги TP (если нужно)

---

**Дата анализа:** 30.11.2025  
**Статус:** 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА - требует немедленного исправления


