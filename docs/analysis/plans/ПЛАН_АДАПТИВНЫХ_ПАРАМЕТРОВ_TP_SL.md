# 📋 ПЛАН РЕАЛИЗАЦИИ АДАПТИВНЫХ ПАРАМЕТРОВ TP/SL

**Дата:** 05 января 2026  
**Статус:** Предложение для обсуждения

---

## ✅ ЧТО УЖЕ ЕСТЬ

### Текущая реализация адаптивности:

1. **`ParameterProvider.get_regime_params(symbol, regime, balance)`**
   - ✅ Уже принимает `balance` и применяет адаптивные параметры
   - ✅ Использует `ConfigManager.get_adaptive_risk_params(balance, regime)`

2. **`ParameterProvider.get_risk_params(symbol, balance, regime)`**
   - ✅ Полностью адаптивный по балансу
   - ✅ Возвращает `max_position_size`, `max_open_positions`, `max_loss_percent` в зависимости от баланса

3. **`ConfigManager.get_adaptive_risk_params(balance, regime)`**
   - ✅ Уже реализована логика адаптации по балансу (small/medium/large)

---

## ❌ ЧТО НУЖНО УЛУЧШИТЬ

### Проблема:

**`ParameterProvider.get_exit_params(symbol, regime)`** - **СТАТИЧЕСКИЙ** метод:
- ❌ Не принимает контекст (PnL, drawdown, position_size, margin_used)
- ❌ TP/SL множители не адаптируются по текущему состоянию позиции
- ❌ Не учитывает просадку при расчете SL
- ❌ Не расширяет TP при сильном P&L

---

## 🎯 ПРЕДЛАГАЕМЫЙ ПЛАН

### Вариант 1: Минимальные изменения (РЕКОМЕНДУЕТСЯ)

**Расширить существующий метод `get_exit_params()` опциональными параметрами:**

```python
def get_exit_params(
    self,
    symbol: str,
    regime: Optional[str] = None,
    # ✅ НОВОЕ: Опциональный контекст для адаптации
    balance: Optional[float] = None,
    current_pnl: Optional[float] = None,  # Текущий P&L позиции в %
    drawdown: Optional[float] = None,  # Текущая просадка в %
    position_size: Optional[float] = None,  # Размер позиции
    margin_used: Optional[float] = None,  # Используемая маржа
) -> Dict[str, Any]:
    """
    Получить параметры выхода (TP/SL) для режима.
    
    ✅ НОВОЕ: Поддерживает адаптивные параметры на основе контекста.
    
    Args:
        symbol: Торговый символ
        regime: Режим рынка. Если None, определяется автоматически
        balance: Текущий баланс (для адаптации по балансу)
        current_pnl: Текущий P&L позиции в % (для расширения TP)
        drawdown: Текущая просадка в % (для ужесточения SL)
        position_size: Размер позиции (для корректировки риска)
        margin_used: Используемая маржа (для проверки безопасности)
    
    Returns:
        Словарь с параметрами выхода (адаптивными если контекст передан)
    """
    # 1. Получаем базовые параметры (как сейчас)
    exit_params = self._get_base_exit_params(symbol, regime)
    
    # 2. Применяем адаптацию по балансу (если передан)
    if balance is not None:
        adaptive_params = self._adapt_by_balance(balance, exit_params)
        exit_params.update(adaptive_params)
    
    # 3. Применяем адаптацию по P&L (если передан)
    if current_pnl is not None:
        adaptive_tp = self._adapt_tp_by_pnl(current_pnl, exit_params)
        exit_params.update(adaptive_tp)
    
    # 4. Применяем адаптацию по просадке (если передан)
    if drawdown is not None:
        adaptive_sl = self._adapt_sl_by_drawdown(drawdown, exit_params)
        exit_params.update(adaptive_sl)
    
    return exit_params
```

**Преимущества:**
- ✅ Обратная совместимость (все параметры опциональные)
- ✅ Минимальные изменения в существующем коде
- ✅ Постепенное внедрение (можно добавлять контекст постепенно)

---

### Вариант 2: Новый метод (если нужна полная изоляция)

**Создать новый метод `get_adaptive_exit_params()`:**

```python
def get_adaptive_exit_params(
    self,
    symbol: str,
    context: Dict[str, Any],  # Полный контекст
    regime: Optional[str] = None,
) -> Dict[str, Any]:
    """
    Получить адаптивные параметры выхода на основе контекста.
    
    Args:
        symbol: Торговый символ
        context: Контекст позиции:
            {
                'balance': float,
                'current_pnl': float,  # %
                'drawdown': float,  # %
                'position_size': float,
                'margin_used': float,
                'regime': str,
            }
        regime: Режим рынка (если не в context)
    """
    # Реализация адаптивной логики
    ...
```

**Недостатки:**
- ❌ Дублирование кода
- ❌ Нужно обновлять все места вызова

---

## 🔧 ЛОГИКА АДАПТАЦИИ

### 1. Адаптация по балансу (уже есть, но можно улучшить)

```python
def _adapt_by_balance(self, balance: float, exit_params: Dict) -> Dict:
    """Адаптация TP/SL по балансу"""
    if balance < 1500:  # small
        return {
            'tp_atr_multiplier': exit_params.get('tp_atr_multiplier', 2.0) * 0.9,  # Консервативный
            'sl_atr_multiplier': exit_params.get('sl_atr_multiplier', 1.5) * 0.9,  # Ужесточенный
        }
    elif balance < 3500:  # medium
        return exit_params  # Без изменений
    else:  # large
        return {
            'tp_atr_multiplier': exit_params.get('tp_atr_multiplier', 2.0) * 1.1,  # Агрессивный
            'sl_atr_multiplier': exit_params.get('sl_atr_multiplier', 1.5) * 1.0,  # Стандартный
        }
```

### 2. Адаптация TP по P&L (НОВОЕ)

```python
def _adapt_tp_by_pnl(self, current_pnl: float, exit_params: Dict) -> Dict:
    """Расширение TP при сильном P&L"""
    base_tp = exit_params.get('tp_atr_multiplier', 2.0)
    
    # Если P&L уже превысил базовый TP, расширяем его
    if current_pnl > base_tp * 0.8:  # 80% от базового TP
        extension = min((current_pnl - base_tp * 0.8) * 0.3, 0.5)  # Макс +0.5x
        return {
            'tp_atr_multiplier': base_tp + extension,
        }
    
    return {}
```

### 3. Адаптация SL по просадке (НОВОЕ)

```python
def _adapt_sl_by_drawdown(self, drawdown: float, exit_params: Dict) -> Dict:
    """Ужесточение SL при просадке"""
    base_sl = exit_params.get('sl_atr_multiplier', 1.5)
    
    # Если просадка > 5%, ужесточаем SL
    if drawdown > 5.0:
        tightening = min((drawdown - 5.0) * 0.1, 0.3)  # Макс +0.3x
        return {
            'sl_atr_multiplier': base_sl + tightening,
        }
    
    return {}
```

---

## 📝 ИНТЕГРАЦИЯ В СУЩЕСТВУЮЩИЙ КОД

### 1. Обновить `ExitAnalyzer`:

```python
# Было:
exit_params = self.parameter_provider.get_exit_params(symbol, regime)

# Станет:
exit_params = self.parameter_provider.get_exit_params(
    symbol,
    regime,
    balance=current_balance,
    current_pnl=position_pnl,
    drawdown=position_drawdown,
    position_size=position_size,
    margin_used=margin_used,
)
```

### 2. Обновить `EntryManager` (при открытии позиции):

```python
# При открытии позиции контекст минимальный, но баланс есть
exit_params = self.parameter_provider.get_exit_params(
    symbol,
    regime,
    balance=current_balance,  # Есть при открытии
    # current_pnl, drawdown - нет при открытии (позиция только открывается)
)
```

---

## ⚙️ КОНФИГУРАЦИЯ

### Добавить в `config_futures.yaml`:

```yaml
adaptive_exit_params:
  enabled: true  # Включить/выключить адаптацию
  
  # Адаптация по балансу
  balance_adaptation:
    small:  # balance < $1,500
      tp_multiplier: 0.9
      sl_multiplier: 0.9
    medium:  # $1,500 <= balance < $3,500
      tp_multiplier: 1.0
      sl_multiplier: 1.0
    large:  # balance >= $3,500
      tp_multiplier: 1.1
      sl_multiplier: 1.0
  
  # Адаптация по P&L
  pnl_adaptation:
    enabled: true
    extension_threshold: 0.8  # Расширять TP если P&L > 80% от базового TP
    max_extension: 0.5  # Максимальное расширение TP (в ATR множителях)
    extension_factor: 0.3  # Коэффициент расширения
  
  # Адаптация по просадке
  drawdown_adaptation:
    enabled: true
    tightening_threshold: 5.0  # Ужесточать SL если просадка > 5%
    max_tightening: 0.3  # Максимальное ужесточение SL (в ATR множителях)
    tightening_factor: 0.1  # Коэффициент ужесточения
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Сценарии для тестирования:

1. **Низкий баланс (< $1,500):**
   - Проверить, что TP/SL становятся консервативнее

2. **Высокий баланс (> $3,500):**
   - Проверить, что TP становится агрессивнее

3. **Сильный P&L (> 80% от базового TP):**
   - Проверить расширение TP

4. **Большая просадка (> 5%):**
   - Проверить ужесточение SL

5. **Комбинация факторов:**
   - Низкий баланс + большая просадка
   - Высокий баланс + сильный P&L

---

## 📊 ПРИОРИТЕТЫ РЕАЛИЗАЦИИ

### Фаза 1 (Минимум):
1. ✅ Расширить `get_exit_params()` опциональными параметрами
2. ✅ Добавить адаптацию по балансу (улучшить существующую)
3. ✅ Интегрировать в `ExitAnalyzer`

### Фаза 2 (Рекомендуется):
4. ✅ Добавить адаптацию по P&L (расширение TP)
5. ✅ Добавить адаптацию по просадке (ужесточение SL)
6. ✅ Добавить конфигурацию в YAML

### Фаза 3 (Опционально):
7. ✅ Добавить адаптацию по размеру позиции
8. ✅ Добавить адаптацию по используемой марже
9. ✅ Расширенное тестирование

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Обратная совместимость:**
   - Все новые параметры должны быть опциональными
   - Если контекст не передан, метод работает как сейчас (статически)

2. **Производительность:**
   - Адаптивные расчеты должны быть быстрыми
   - Использовать кэширование где возможно

3. **Логирование:**
   - Логировать все адаптации для отладки
   - Формат: `[ADAPTIVE] {symbol}: TP={old}→{new} (reason: {reason})`

4. **Безопасность:**
   - Не допускать экстремальных значений (min/max ограничения)
   - Валидация всех адаптированных параметров

---

**Рекомендация:** Начать с **Варианта 1 (минимальные изменения)** и постепенно добавлять адаптации.
