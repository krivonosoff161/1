# 📊 АНАЛИЗ ПРОБЛЕМ: Лимитные ордера и TSL

**Дата:** 2025-11-15  
**Символ:** SOL-USDT  
**Проблемы:** Отмена лимитных ордеров, мгновенное закрытие позиций

---

## 🔴 ПРОБЛЕМА 1: Лимитный ордер был отменен

### 📝 Описание проблемы:

**Хронология:**
- **16:13:30** - Реальный сигнал LONG @ $142.29 (сила=0.90)
- **16:13:33** - Лимитный ордер размещен @ 141.30 (не исполнился)
- **16:14:44** - Через ~71 сек ордер отменен и заменен рыночным @ 142.07
- **16:14:46** - Позиция открыта @ 142.08 (avgPx с биржи)

**Причина:**
- `limit_offset_percent` = 0.05% для ranging режима был слишком мал
- Цена быстро ушла вверх, лимитный ордер остался в стакане
- `max_wait_seconds` = 60 сек оказалось недостаточно для ranging режима
- После таймаута ордер был заменен рыночным, что привело к проскальзыванию

### ✅ РЕШЕНИЕ (ИСПРАВЛЕНО):

**Изменения в `config/config_futures.yaml` (строки 1076-1077):**

```yaml
ranging:
  limit_offset_percent: 0.08  # ✅ ИСПРАВЛЕНО: Увеличено с 0.05% до 0.08% (быстрее исполнение)
  max_wait_seconds: 90  # ✅ ИСПРАВЛЕНО: Увеличено с 60 до 90 сек (больше времени для исполнения)
```

**Ожидаемый результат:**
- Лимитные ордера будут быстрее исполняться (больший offset)
- Больше времени для исполнения в ranging режиме
- Меньше замен на рыночные ордера (меньше проскальзывания)

---

## 🔴 ПРОБЛЕМА 2: Позиция закрыта сразу после открытия

### 📝 Описание проблемы:

**Хронология:**
- **16:14:46** - Позиция открыта @ 142.08 (avgPx)
- **16:14:46** - TSL инициализирован: entry=142.08, trail=0.80%
- **16:14:46** - Цена упала до 141.24 (проскальзывание или кратковременная флуктуация)
- **16:14:46** - Убыток: -0.69% от цены (3.46% от маржи)
- **16:14:46** - TSL закрыл позицию через ~1 сек (`loss_cut_percent` = 1.8% от маржи = 0.36% от цены)
- **16:14:47** - Позиция закрыта @ 142.00

**Причина:**
- `loss_cut_percent` = 1.8% от маржи (0.36% от цены при leverage 5x)
- Убыток -0.69% > порога 0.36%
- Позиция держалась всего ~1 сек (менее `min_holding_minutes: 0.5` = 30 сек)
- TSL закрыл позицию через `loss_cut_override`, игнорируя минимальное время удержания

**Проблема:**
- Проскальзывание при размещении рыночного ордера (-0.55% от цены сигнала)
- Или кратковременная флуктуация цены сразу после открытия
- TSL слишком агрессивен - закрывает позиции сразу после открытия

### ✅ РЕШЕНИЕ (ИСПРАВЛЕНО):

**Изменения в `src/strategies/scalping/futures/indicators/trailing_stop_loss.py` (строки 406-445):**

```python
# ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Защита от слишком быстрого закрытия
# Не закрываем позицию сразу после открытия из-за проскальзывания или кратковременных флуктуаций
# Критический убыток должен быть в 2 раза больше обычного loss_cut_percent
# И позиция должна держаться хотя бы 15 секунд

seconds_in_position = minutes_in_position * 60.0
min_critical_hold_seconds = 15.0  # Минимум 15 секунд для критического закрытия

if self.loss_cut_percent is not None:
    loss_cut_from_price = self.loss_cut_percent / self.leverage
    # ✅ КРИТИЧЕСКОЕ: Для слишком быстрого закрытия (менее 15 сек) требуем 2x убыток
    critical_loss_cut_from_price = (self.loss_cut_percent * 2.0) / self.leverage
    
    # ✅ КРИТИЧЕСКОЕ: Закрываем только если убыток критический (2x) И прошло минимум 15 сек
    if seconds_in_position >= min_critical_hold_seconds:
        if profit_pct <= -critical_loss_cut_from_price:
            # Закрываем по критическому убытку (2x)
            return True
    else:
        # Позиция держится менее 15 секунд - НЕ закрываем даже при большом убытке
        # (это может быть проскальзывание или кратковременная флуктуация)
        if profit_pct <= -loss_cut_from_price:
            logger.debug(
                f"⏱️ Loss-cut ИГНОРИРУЕТСЯ (слишком рано): ..."
            )
```

**Логика защиты:**
1. Если позиция держится **< 15 секунд** → **НЕ закрываем** даже при убытке >= `loss_cut_percent`
   - Это может быть проскальзывание или кратковременная флуктуация
   - Ждем минимум 15 секунд для стабилизации цены

2. Если позиция держится **>= 15 секунд** → закрываем только при **критическом убытке (2x)**
   - Критический убыток = 2 × `loss_cut_percent` (для ranging = 3.6% от маржи = 0.72% от цены)
   - Обычный убыток будет игнорироваться до `min_holding_minutes: 0.5` (30 сек)

**Ожидаемый результат:**
- Позиции не будут закрываться сразу после открытия из-за проскальзывания
- Защита от ложных срабатываний TSL в первые 15 секунд
- Критические убытки все еще будут закрываться (но с задержкой 15+ секунд)

---

## 📊 СРАВНЕНИЕ: До и После

### ❌ ДО исправлений:

1. **Лимитные ордера:**
   - `limit_offset_percent` = 0.05% (слишком мало)
   - `max_wait_seconds` = 60 сек
   - Ордера часто не исполнялись и отменялись
   - Замена на рыночные ордера → проскальзывание

2. **TSL закрытие:**
   - Закрывал позиции сразу после открытия (через 1-2 сек)
   - Игнорировал проскальзывание и кратковременные флуктуации
   - Потери из-за преждевременного закрытия

### ✅ ПОСЛЕ исправлений:

1. **Лимитные ордера:**
   - `limit_offset_percent` = 0.08% (+60% для ranging)
   - `max_wait_seconds` = 90 сек (+50% для ranging)
   - Быстрее исполнение, меньше зависших ордеров
   - Меньше проскальзывания при замене на рыночные ордера

2. **TSL закрытие:**
   - Защита от закрытия в первые 15 секунд
   - Критический убыток увеличен до 2x для быстрого закрытия
   - Меньше ложных срабатываний из-за проскальзывания
   - Больше времени для позиций для восстановления

---

## 🎯 РЕКОМЕНДАЦИИ

### 1. Мониторинг:
- Отслеживать частоту отмены лимитных ордеров
- Отслеживать проскальзывание при размещении рыночных ордеров
- Отслеживать закрытия позиций в первые 15 секунд (должно быть 0)

### 2. Дополнительные улучшения:
- Рассмотреть увеличение `limit_offset_percent` для trending режима (сейчас 0.08%)
- Рассмотреть использование `post_only: true` для лимитных ордеров в ranging режиме (maker fee 0.02%)
- Рассмотреть адаптацию `min_critical_hold_seconds` по режимам (15 сек для ranging, возможно больше для trending)

### 3. Тестирование:
- Протестировать с реальными сигналами в ranging режиме
- Проверить, что лимитные ордера исполняются быстрее
- Проверить, что позиции не закрываются сразу после открытия

---

## ✅ СТАТУС

- ✅ **ПРОБЛЕМА 1 (Лимитные ордера):** ИСПРАВЛЕНО
- ✅ **ПРОБЛЕМА 2 (Мгновенное закрытие):** ИСПРАВЛЕНО
- ⏳ **ТЕСТИРОВАНИЕ:** Ожидается проверка на следующем запуске бота

---

**Исправлено:** 2025-11-15  
**Файлы изменены:**
- `config/config_futures.yaml` (строки 1076-1077)
- `src/strategies/scalping/futures/indicators/trailing_stop_loss.py` (строки 406-445)



