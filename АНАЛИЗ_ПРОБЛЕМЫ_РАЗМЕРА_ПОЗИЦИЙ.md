# 🔍 АНАЛИЗ ПРОБЛЕМЫ РАЗМЕРА ПОЗИЦИЙ

## ❌ ПРОБЛЕМА

В логах видно:
```
📊 Max position size из symbol_profiles ($58.00) применен (было $500.00 из balance_profile, base=$197.19)
💰 ФИНАЛЬНЫЙ РАСЧЕТ: margin=$10.94 (лимит: $10.00-$11.60 маржи)
```

**Проблема:**
- `max_position_usd` из `symbol_profiles` ($58.00) **ограничивает** размер
- Это дает только **$11.60 маржи** ($58 / 5 = $11.60)
- Но должно быть **$100 маржи** ($500 / 5 = $100)

## 🤔 ВОПРОС

**Что должно быть приоритетом?**

1. **Вариант A:** `balance_profile.max_position_usd` - глобальный лимит для всех символов
2. **Вариант B:** `symbol_profiles.{symbol}.{regime}.position.max_position_usd` - специфичный лимит для символа/режима

**Текущая логика:**
- Если указано в `symbol_profiles` → используется оно (ограничивает)
- Если не указано → используется `balance_profile`

**Проблема:**
- `symbol_profiles` имеет значения $30-58, которые **слишком маленькие**
- Они ограничивают размер позиций

## ✅ РЕШЕНИЕ

**Нужно уточнить логику:**

1. **Если `max_position_usd` в `symbol_profiles` НЕ указан** → использовать `balance_profile`
2. **Если `max_position_usd` в `symbol_profiles` указан** → использовать **МИНИМУМ** между ним и `balance_profile` (более строгое ограничение)

**ИЛИ:**

1. **Убрать `max_position_usd` из `symbol_profiles`** для всех пар/режимов
2. Использовать только `balance_profile.max_position_usd` ($500)

**ИЛИ:**

1. **Изменить приоритет:** `balance_profile` имеет приоритет, `symbol_profiles` используется только если он **БОЛЬШЕ**

## 📋 ТЕКУЩЕЕ СОСТОЯНИЕ

- `balance_profile.small.max_position_usd = $500.0`
- `symbol_profiles.BTC-USDT.ranging.position.max_position_usd = $58.0`
- **Результат:** Используется $58.0 (ограничивает размер)

## 🎯 РЕКОМЕНДАЦИЯ

**Убрать `max_position_usd` из `symbol_profiles`** для всех пар/режимов, чтобы использовался только `balance_profile`.

Или изменить логику: использовать **МАКСИМУМ** между `symbol_profiles` и `balance_profile` (более либеральное ограничение).





