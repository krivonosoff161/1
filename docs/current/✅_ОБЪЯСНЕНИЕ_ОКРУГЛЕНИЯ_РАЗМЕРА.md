# ОБЪЯСНЕНИЕ ОКРУГЛЕНИЯ РАЗМЕРА ПОЗИЦИИ

**Дата:** 23 ноября 2025  
**Вопрос:** Почему размер позиции изменяется из-за округления?

---

## ⚠️ ПРЕДУПРЕЖДЕНИЕ ИЗ ЛОГОВ

```
⚠️ Размер позиции изменен из-за округления/минимума: 0.053443 → 0.053000 монет (+0.83%), 
notional: $150.00 → $148.76, margin: $30.00 → $29.75
```

---

## 🔍 ПРИЧИНЫ ОКРУГЛЕНИЯ

### 1. Минимальный размер позиции (Lot Size) ✅

**Что это:**
- Биржа OKX устанавливает минимальный размер позиции для каждого инструмента
- Для ETH-USDT-SWAP минимальный размер обычно `0.001 ETH` или `0.01 ETH`

**Пример:**
- Рассчитанный размер: `0.053443 ETH`
- Минимальный размер: `0.001 ETH`
- Округление до: `0.053000 ETH` (ближайшее значение, кратное шагу)

**✅ ПРАВИЛЬНО:** Биржа не принимает позиции меньше минимального размера

---

### 2. Шаг размера позиции (Tick Size) ✅

**Что это:**
- Биржа устанавливает шаг, на который можно изменить размер позиции
- Для ETH-USDT-SWAP шаг обычно `0.001 ETH` или `0.01 ETH`

**Как работает:**
```python
# Из risk_manager.py (строка 690):
size_in_contracts = position_size / ct_val
rounded_size_in_contracts = round_to_step(size_in_contracts, lot_sz)
```

**Пример:**
- Рассчитанный размер: `0.053443 ETH`
- Шаг размера: `0.001 ETH`
- Округление до: `0.053000 ETH` (ближайшее значение, кратное шагу)

**✅ ПРАВИЛЬНО:** Размер позиции должен быть кратным шагу

---

### 3. Ограничения биржи ✅

**Что это:**
- Биржа может устанавливать дополнительные ограничения на размер позиции
- Например, максимальный размер одной позиции или общий размер всех позиций

**Как работает:**
```python
# Из risk_manager.py (строка 731):
if real_notional_usd > max_usd_size:
    # Уменьшаем до лимита с учетом округления
    target_rounded_size_in_contracts = math.floor(target_size_in_contracts / lot_sz) * lot_sz
```

**Пример:**
- Рассчитанный размер: `0.053443 ETH` (notional = $150.00)
- Максимальный размер одной позиции: `$150.00`
- Округление до: `0.053000 ETH` (notional = $148.76)

**✅ ПРАВИЛЬНО:** Размер позиции не превышает максимальный лимит

---

## 📊 РАСЧЕТ В ПРИМЕРЕ

### Исходные данные:
- Рассчитанный размер: `0.053443 ETH`
- Цена входа: `$2809.15`
- Notional: `0.053443 × $2809.15 = $150.00`
- Margin (5x leverage): `$150.00 / 5 = $30.00`

### После округления:
- Округленный размер: `0.053000 ETH`
- Цена входа: `$2809.15`
- Notional: `0.053000 × $2809.15 = $148.76`
- Margin (5x leverage): `$148.76 / 5 = $29.75`

### Изменение:
- Размер: `-0.000443 ETH` (-0.83%)
- Notional: `-$1.24` (-0.83%)
- Margin: `-$0.25` (-0.83%)

---

## ✅ ЭТО НОРМАЛЬНОЕ ПОВЕДЕНИЕ

### Почему это происходит:

1. ✅ **Биржа требует** минимальный размер позиции
2. ✅ **Биржа требует** размер, кратный шагу
3. ✅ **Биржа ограничивает** максимальный размер позиции

### Что делает бот:

1. ✅ Рассчитывает оптимальный размер позиции
2. ✅ Округляет до минимального размера/шага
3. ✅ Проверяет максимальные лимиты
4. ✅ Логирует изменение для прозрачности

---

## 🎯 ВЫВОДЫ

### Округление размера позиции:

1. ✅ **Нормальное поведение** - биржа требует округления
2. ✅ **Небольшое изменение** - обычно менее 1%
3. ✅ **Прозрачность** - бот логирует все изменения
4. ✅ **Безопасность** - бот соблюдает ограничения биржи

### Рекомендации:

- ✅ Это не ошибка, а нормальное поведение
- ✅ Изменение обычно незначительное (<1%)
- ✅ Бот правильно обрабатывает округление
- ✅ Логи показывают все изменения для прозрачности

---

**Статус:** ✅ ОКРУГЛЕНИЕ РАЗМЕРА ПОЗИЦИИ - НОРМАЛЬНОЕ ПОВЕДЕНИЕ БИРЖИ

