# 🤖 ПОЛНОЕ ОПИСАНИЕ ТОРГОВОГО БОТА OKX FUTURES

> **⚠️ ВАЖНО: НЕ УДАЛЯТЬ ЭТОТ ФАЙЛ!**  
> Этот файл содержит полное техническое описание торгового бота для передачи в нейросеть Kimi.

**Дата создания:** 30 ноября 2025  
**Тип торговли:** Futures (фьючерсы) на бирже OKX  
**Статус:** Активно работает

---

## 📋 СОДЕРЖАНИЕ

1. [Общая архитектура](#общая-архитектура)
2. [Структура проекта и файлы](#структура-проекта-и-файлы)
3. [Жизненный цикл бота](#жизненный-цикл-бота)
4. [Генерация сигналов](#генерация-сигналов)
5. [Формирование ордеров](#формирование-ордеров)
6. [Управление позициями](#управление-позициями)
7. [Закрытие позиций](#закрытие-позиций)
8. [Системы фильтрации](#системы-фильтрации)
9. [Системы защиты](#системы-защиты)
10. [Расчеты и математика](#расчеты-и-математика)
11. [Работа с биржей OKX](#работа-с-биржей-okx)
12. [Адаптивные системы](#адаптивные-системы)

---

## 🏗️ ОБЩАЯ АРХИТЕКТУРА

### Основные компоненты

Бот построен по модульной архитектуре с четким разделением ответственности:

```
┌─────────────────────────────────────────────────────────┐
│              FuturesScalpingOrchestrator                 │
│              (Главный координатор)                       │
└─────────────────────────────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ Signal       │  │ Order        │  │ Position     │
│ Generator    │  │ Executor     │  │ Manager      │
└──────────────┘  └──────────────┘  └──────────────┘
        │                 │                 │
        └─────────────────┼─────────────────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ Filters      │ │ Risk         │ │ Indicators   │
│ (10+ типов)  │ │ Manager      │ │ (RSI, MACD...)│
└──────────────┘ └──────────────┘ └──────────────┘
```

### Технологический стек

- **Язык:** Python 3.8+
- **Асинхронность:** asyncio
- **API биржи:** OKX REST API + WebSocket
- **Конфигурация:** YAML (Pydantic)
- **Логирование:** loguru
- **Тип торговли:** Futures (USDT-Margined Perpetual Swaps)
- **Режим маржи:** Isolated Margin
- **Плечо:** 5x (настраивается)

---

## 📁 СТРУКТУРА ПРОЕКТА И ФАЙЛЫ

### Корневая структура

```
simple trading bot okx/
├── run.py                          # Точка входа (выбор Spot/Futures)
├── src/
│   ├── main_futures.py             # Запуск Futures бота
│   ├── clients/
│   │   └── futures_client.py       # Клиент OKX Futures API
│   ├── strategies/
│   │   └── scalping/
│   │       └── futures/
│   │           ├── orchestrator.py        # Главный оркестратор
│   │           ├── signal_generator.py     # Генератор сигналов
│   │           ├── order_executor.py      # Исполнитель ордеров
│   │           ├── position_manager.py     # Менеджер позиций
│   │           ├── risk_manager.py         # Менеджер рисков
│   │           ├── filters/               # Фильтры сигналов
│   │           ├── indicators/            # Технические индикаторы
│   │           ├── coordinators/          # Координаторы процессов
│   │           └── calculations/         # Расчетные модули
│   ├── strategies/modules/         # Общие модули
│   └── risk/                       # Системы управления рисками
├── config/
│   └── config_futures.yaml        # Конфигурация Futures
└── logs/
    └── futures/                    # Логи Futures торговли
```

### Ключевые файлы и их функции

#### 1. `run.py` - Точка входа
- Выбор режима торговли (Spot/Futures)
- Проверка конфигурации
- Запуск соответствующего бота

#### 2. `src/main_futures.py` - Запуск Futures бота
- Загрузка конфигурации `config_futures.yaml`
- Инициализация `FuturesScalpingOrchestrator`
- Обработка ошибок и graceful shutdown

#### 3. `src/strategies/scalping/futures/orchestrator.py` - Главный оркестратор
**Ответственность:**
- Координация всех модулей
- Главный торговый цикл
- Управление жизненным циклом позиций
- Мониторинг безопасности

**Основные методы:**
- `start()` - Запуск бота
- `run()` - Главный цикл торговли
- `_process_symbol()` - Обработка каждого символа
- `_update_trailing_stop_loss()` - Обновление трейлинг-стопа
- `_close_position()` - Закрытие позиции

#### 4. `src/strategies/scalping/futures/signal_generator.py` - Генератор сигналов
**Ответственность:**
- Расчет технических индикаторов
- Генерация торговых сигналов (BUY/SELL)
- Применение фильтров
- Адаптация под режим рынка (ARM)

**Используемые индикаторы:**
- RSI (Relative Strength Index)
- MACD (Moving Average Convergence Divergence)
- Bollinger Bands
- EMA (Exponential Moving Average)
- SMA (Simple Moving Average)
- ATR (Average True Range)

#### 5. `src/strategies/scalping/futures/order_executor.py` - Исполнитель ордеров
**Ответственность:**
- Размещение ордеров на бирже
- Выбор типа ордера (MARKET/LIMIT)
- Расчет цены для лимитных ордеров
- Обработка ошибок и повторные попытки

#### 6. `src/strategies/scalping/futures/position_manager.py` - Менеджер позиций
**Ответственность:**
- Отслеживание открытых позиций
- Расчет PnL (прибыль/убыток)
- Проверка условий закрытия (TP/SL)
- Управление Trailing Stop Loss

#### 7. `src/strategies/scalping/futures/risk_manager.py` - Менеджер рисков
**Ответственность:**
- Расчет размера позиции
- Проверка лимитов маржи
- Защита от ликвидации
- Контроль максимальных потерь

#### 8. `src/clients/futures_client.py` - Клиент OKX API
**Ответственность:**
- REST API запросы к бирже
- Получение рыночных данных
- Размещение ордеров
- Получение баланса и позиций

---

## 🔄 ЖИЗНЕННЫЙ ЦИКЛ БОТА

### 1. Инициализация (`orchestrator.start()`)

```python
1. Загрузка конфигурации из config_futures.yaml
2. Инициализация OKXFuturesClient (API ключи, sandbox режим)
3. Инициализация всех модулей:
   - SignalGenerator (генератор сигналов)
   - OrderExecutor (исполнитель ордеров)
   - PositionManager (менеджер позиций)
   - RiskManager (менеджер рисков)
   - Фильтры (10+ типов)
   - Индикаторы (RSI, MACD, BB, EMA, SMA, ATR)
4. Загрузка существующих позиций с биржи
5. Инициализация Trailing Stop Loss для существующих позиций
6. Запуск WebSocket для получения рыночных данных
7. Получение начального баланса
```

### 2. Главный торговый цикл (`orchestrator.run()`)

**Цикл выполняется каждые ~6 секунд:**

```python
while active:
    for symbol in symbols:  # BTC-USDT, ETH-USDT, SOL-USDT...
        # ШАГ 1: Проверка rate limit
        await _check_rate_limit()
        
        # ШАГ 2: Обновление рыночных данных
        await _update_market_data(symbol)
        # - Получение свечей 5m через REST API
        # - Получение текущей цены через WebSocket
        
        # ШАГ 3: Расчет индикаторов
        indicators = indicator_manager.calculate_all(candles)
        # - RSI, MACD, Bollinger Bands, EMA, SMA, ATR
        
        # ШАГ 4: Определение режима рынка (ARM)
        regime = regime_manager.detect_regime(candles, current_price)
        # - TRENDING (трендовый)
        # - RANGING (боковой)
        # - CHOPPY (хаотичный)
        
        # ШАГ 5: Мониторинг существующих позиций
        if symbol in active_positions:
            position = active_positions[symbol]
            
            # Обновление Trailing Stop Loss
            await _update_trailing_stop_loss(symbol, current_price)
            
            # Проверка условий закрытия
            await position_manager.manage_position(position)
            # - Проверка TP (Take Profit)
            # - Проверка SL (Stop Loss)
            # - Проверка Trailing SL
            # - Проверка времени удержания
            
        # ШАГ 6: Генерация новых сигналов (если can_trade)
        if can_trade(symbol):
            signal = await signal_generator.generate_signal(
                symbol=symbol,
                indicators=indicators,
                tick=current_price,
                current_positions=active_positions
            )
            
            # ШАГ 7: Применение фильтров
            if signal:
                if await _apply_filters(signal):
                    # ШАГ 8: Исполнение сигнала
                    await _execute_signal(signal)
        
        # ШАГ 9: Статистика (каждые 60 сек)
        if time.time() - last_stats_time > 60:
            _log_statistics()
```

### 3. Остановка (`orchestrator.stop()`)

```python
1. Установка флага active = False
2. Закрытие всех открытых позиций (опционально)
3. Закрытие WebSocket соединений
4. Сохранение статистики
5. Запись финальных логов
```

---

## 📊 ГЕНЕРАЦИЯ СИГНАЛОВ

### Процесс генерации сигнала

#### 1. Расчет индикаторов

**RSI (Relative Strength Index):**
```python
# Период: 14 (настраивается)
# Перекупленность: RSI > 70
# Перепроданность: RSI < 30

if RSI < 30:
    long_score += 2  # Сигнал на покупку
elif RSI > 70:
    short_score += 2  # Сигнал на продажу
```

**MACD (Moving Average Convergence Divergence):**
```python
# Параметры: fast=12, slow=26, signal=9
# Сигнал BUY: macd_line > signal_line AND histogram > 0
# Сигнал SELL: macd_line < signal_line AND histogram < 0

if macd_line > signal_line and histogram > 0:
    long_score += 2
elif macd_line < signal_line and histogram < 0:
    short_score += 2
```

**Bollinger Bands:**
```python
# Период: 20, стандартное отклонение: 2.0
# Сигнал BUY: цена <= нижняя полоса
# Сигнал SELL: цена >= верхняя полоса

if price <= bb_lower:
    long_score += 2
elif price >= bb_upper:
    short_score += 2
```

**EMA (Exponential Moving Average):**
```python
# EMA_12 и EMA_26
# Сигнал BUY: EMA_12 > EMA_26 AND цена > EMA_12
# Сигнал SELL: EMA_12 < EMA_26 AND цена < EMA_12

if ema_12 > ema_26 and price > ema_12:
    long_score += 2
elif ema_12 < ema_26 and price < ema_12:
    short_score += 2
```

**ATR (Average True Range):**
```python
# Период: 14
# Используется для расчета волатильности
# Минимальная волатильность для входа: 0.0004 (0.04%)
```

#### 2. Система оценки (Scoring System)

**Базовые баллы (0-12):**
- SMA Trend: +1 балл
- EMA Trend: +2 балла
- RSI: +2 балла
- Bollinger Bands: +2 балла
- Volume: +2 балла
- MACD: +2 балла

**Бонусы от фильтров:**
- Multi-Timeframe: +1-3 балла
- Pivot Points: +1-3 балла
- Volume Profile: +1-3 балла

**Минимальный порог (адаптивный по режиму):**
- TRENDING: 6/12 баллов (50%)
- RANGING: 4/12 баллов (33%)
- CHOPPY: 7/12 баллов (58%)

#### 3. Определение направления сигнала

```python
if long_score >= threshold and long_score > short_score:
    signal_direction = "LONG"
    signal_strength = long_score / 12.0
elif short_score >= threshold and short_score > long_score:
    signal_direction = "SHORT"
    signal_strength = short_score / 12.0
else:
    return None  # Сигнал не генерируется
```

#### 4. Применение фильтров

После генерации сигнал проходит через систему фильтров (см. раздел [Системы фильтрации](#системы-фильтрации)).

---

## 📝 ФОРМИРОВАНИЕ ОРДЕРОВ

### Процесс размещения ордера

#### 1. Расчет размера позиции (`risk_manager.calculate_position_size()`)

**Адаптивные профили баланса:**

**SMALL (< $1,500):**
```python
base_position = $50
min_position = $20
max_position = $120
max_open = 2 позиции
max_percent = 8% от баланса
```

**MEDIUM ($1,500 - $3,500):**
```python
base_position = $100
min_position = $40
max_position = $200
max_open = 3 позиции
max_percent = 6% от баланса
```

**LARGE (> $3,500):**
```python
base_position = $200
min_position = $80
max_position = $400
max_open = 3 позиции
max_percent = 5% от баланса
```

**Расчет с учетом режима:**
```python
# Получаем профиль для режима
regime_profile = get_regime_profile(symbol, regime)

# Базовый размер из профиля
base_size = regime_profile.get("base_position", $100)

# Корректировка на волатильность
atr_percent = calculate_atr_percent(candles, current_price)
volatility_multiplier = calculate_volatility_multiplier(atr_percent, regime)

# Финальный размер
position_size = base_size * volatility_multiplier

# Ограничения
position_size = min(position_size, max_position)
position_size = max(position_size, min_position)
position_size = min(position_size, balance * max_percent)
```

#### 2. Выбор типа ордера

**LIMIT ордер (по умолчанию):**
```python
# Преимущества:
# - Комиссия maker: 0.02% (вместо 0.05% taker)
# - Контроль цены входа
# - Экономия на комиссиях

# Расчет цены для LIMIT:
if side == "LONG":
    # Покупаем ниже текущей цены
    limit_price = current_price * (1 - offset_percent)
elif side == "SHORT":
    # Продаем выше текущей цены
    limit_price = current_price * (1 + offset_percent)

# Offset зависит от режима:
# - TRENDING: 0.05% (0.0005)
# - RANGING: 0.03% (0.0003)
# - CHOPPY: 0.08% (0.0008)
```

**MARKET ордер (fallback):**
```python
# Используется если:
# - Не удалось рассчитать цену для LIMIT
# - Срочный вход (высокая волатильность)
# - Ошибка размещения LIMIT ордера

# Размещается по текущей рыночной цене
```

#### 3. Размещение ордера на бирже (`order_executor.execute_signal()`)

**REST API запрос:**
```python
POST /api/v5/trade/order

{
    "instId": "BTC-USDT-SWAP",
    "tdMode": "isolated",  # Isolated margin
    "side": "buy",  # или "sell"
    "ordType": "limit",  # или "market"
    "sz": "0.01",  # Размер позиции
    "px": "50000.0",  # Цена (для LIMIT)
    "posSide": "long",  # или "short"
    "lever": "5"  # Плечо
}
```

**Обработка ответа:**
```python
if order_status == "filled":
    # Ордер исполнен
    position = create_position(order)
    active_positions[symbol] = position
    
    # Инициализация Trailing Stop Loss
    trailing_sl.initialize(
        entry_price=order.fill_price,
        side=signal.side,
        symbol=symbol
    )
    
elif order_status == "partially_filled":
    # Частичное исполнение
    # Ждем полного исполнения или отменяем
    
elif order_status == "canceled":
    # Ордер отменен
    # Логируем причину
```

---

## 📈 УПРАВЛЕНИЕ ПОЗИЦИЯМИ

### Отслеживание позиций

**Структура позиции:**
```python
position = {
    "symbol": "BTC-USDT",
    "side": "long",  # или "short"
    "entry_price": 50000.0,
    "size": 0.01,
    "margin": 100.0,  # Маржа
    "leverage": 5,
    "entry_time": datetime.now(),
    "regime": "trending",  # Режим при входе
    "trailing_sl": TrailingStopLoss(),
    "tp_percent": 2.4,  # Take Profit %
    "sl_percent": 1.2,  # Stop Loss %
    "pnl": 0.0,  # Текущий PnL
    "pnl_percent": 0.0  # PnL в процентах
}
```

### Расчет PnL (Profit and Loss)

**Для LONG позиции:**
```python
pnl = (current_price - entry_price) * size
pnl_percent = (current_price - entry_price) / entry_price * 100

# С учетом комиссий:
pnl_net = pnl - (entry_price * size * fee_rate) - (current_price * size * fee_rate)
pnl_percent_net = pnl_net / (entry_price * size) * 100
```

**Для SHORT позиции:**
```python
pnl = (entry_price - current_price) * size
pnl_percent = (entry_price - current_price) / entry_price * 100

# С учетом комиссий:
pnl_net = pnl - (entry_price * size * fee_rate) - (current_price * size * fee_rate)
pnl_percent_net = pnl_net / (entry_price * size) * 100
```

### Обновление Trailing Stop Loss

**Логика для LONG:**
```python
# Отслеживаем максимальную цену
if current_price > highest_price:
    highest_price = current_price
    
    # Рассчитываем прибыль
    profit_pct = (highest_price - entry_price) / entry_price
    
    # Адаптивный трейлинг в зависимости от прибыли
    if profit_pct < 0.5%:
        trail_multiplier = 1.5x
    elif profit_pct < 1.5%:
        trail_multiplier = 2.0x
    else:
        trail_multiplier = 3.0x
    
    # Новый стоп-лосс
    trailing_sl = highest_price * (1 - initial_trail * trail_multiplier)
    
    # Ограничения
    trailing_sl = max(trailing_sl, entry_price * (1 - max_trail))
    trailing_sl = min(trailing_sl, entry_price * (1 - min_trail))
```

**Логика для SHORT:**
```python
# Отслеживаем минимальную цену
if current_price < lowest_price:
    lowest_price = current_price
    
    # Рассчитываем прибыль
    profit_pct = (entry_price - lowest_price) / entry_price
    
    # Адаптивный трейлинг
    # (аналогично LONG, но в обратную сторону)
    
    # Новый стоп-лосс
    trailing_sl = lowest_price * (1 + initial_trail * trail_multiplier)
```

---

## 🚪 ЗАКРЫТИЕ ПОЗИЦИЙ

### Условия закрытия

#### 1. Take Profit (TP)

**Фиксированный TP:**
```python
# TP устанавливается при открытии позиции
tp_percent = 2.4%  # Адаптивно по режиму

# Проверка:
if pnl_percent >= tp_percent:
    await close_position(symbol, reason="tp")
```

**Адаптивный TP по режиму:**
- TRENDING: 2.4% (1.2x ATR)
- RANGING: 1.8% (0.6x ATR)
- CHOPPY: 2.0% (0.8x ATR)

#### 2. Stop Loss (SL)

**Фиксированный SL:**
```python
# SL устанавливается при открытии позиции
sl_percent = 1.2%  # Адаптивно по режиму

# Проверка:
if pnl_percent <= -sl_percent:
    await close_position(symbol, reason="sl")
```

**Адаптивный SL по режиму:**
- TRENDING: 1.2% (0.8x ATR)
- RANGING: 0.8% (0.4x ATR)
- CHOPPY: 1.5% (1.2x ATR)

#### 3. Trailing Stop Loss

**Логика закрытия:**
```python
# Для LONG:
if current_price <= trailing_sl:
    await close_position(symbol, reason="trailing_sl")

# Для SHORT:
if current_price >= trailing_sl:
    await close_position(symbol, reason="trailing_sl")
```

#### 4. Partial Take Profit

**Частичное закрытие:**
```python
# Триггер: прибыль достигла trigger_percent
if pnl_percent >= trigger_percent:
    # Закрываем часть позиции
    close_size = position_size * fraction
    
    # Адаптивно по режиму:
    # - TRENDING: 50% при 0.5%
    # - RANGING: 60% при 0.6%
    # - CHOPPY: 70% при 0.3%
    
    await close_position_partial(symbol, close_size, reason="partial_tp")
```

#### 5. Time Stop

**Максимальное время удержания:**
```python
# Адаптивно по режиму:
# - TRENDING: 45 минут
# - RANGING: 12 минут
# - CHOPPY: 30 минут

holding_time = current_time - entry_time
if holding_time > max_holding_time:
    await close_position(symbol, reason="time_stop")
```

#### 6. Break-even Stop Loss

**Защита прибыли:**
```python
# После достижения 1.0 ATR прибыли
if pnl_percent >= 1.0 * atr_percent:
    # Устанавливаем SL на уровень входа (break-even)
    sl_price = entry_price
    # Защищаем от убытка
```

### Процесс закрытия

```python
async def close_position(symbol: str, reason: str):
    # ШАГ 1: Получение позиции
    position = active_positions.get(symbol)
    if not position:
        return
    
    # ШАГ 2: Размещение MARKET ордера на закрытие
    if position.side == "long":
        close_side = "sell"
    else:
        close_side = "buy"
    
    order = await client.place_order(
        symbol=symbol,
        side=close_side,
        order_type="market",
        quantity=position.size
    )
    
    # ШАГ 3: Ожидание исполнения
    await wait_for_order_fill(order)
    
    # ШАГ 4: Расчет финального PnL
    final_pnl = calculate_pnl(position, order.fill_price)
    
    # ШАГ 5: Запись статистики
    trading_statistics.record_trade(
        symbol=symbol,
        side=position.side,
        pnl=final_pnl,
        reason=reason
    )
    
    # ШАГ 6: Удаление из активных позиций
    del active_positions[symbol]
    
    # ШАГ 7: Логирование
    logger.info(f"✅ Позиция закрыта: {symbol} | Причина: {reason} | PnL: {final_pnl:.2f}%")
```

---

## 🔍 СИСТЕМЫ ФИЛЬТРАЦИИ

### Типы фильтров

#### 1. Correlation Filter (Фильтр корреляции)

**Назначение:** Предотвращает открытие позиций в сильно коррелированных парах.

**Логика:**
```python
# Проверка корреляции между парами
correlation = calculate_correlation(symbol1, symbol2)

# Если корреляция > 0.7 и уже есть позиция в одной паре
if correlation > 0.7 and has_position(symbol1):
    if same_direction(symbol1, symbol2):
        block_signal(symbol2, reason="high_correlation")
```

**Параметры:**
- `correlation_threshold: 0.7` - Порог высокой корреляции
- `max_correlated_positions: 1` - Максимум позиций в коррелированных парах
- `block_same_direction_only: true` - Блокировать только одинаковые направления

#### 2. Multi-Timeframe Filter (Мультитаймфрейм фильтр)

**Назначение:** Подтверждает сигнал на нескольких таймфреймах.

**Логика:**
```python
# Проверка на разных таймфреймах
timeframes = ["5m", "15m", "1h"]

for tf in timeframes:
    candles_tf = get_candles(symbol, tf)
    indicators_tf = calculate_indicators(candles_tf)
    
    if confirm_signal(indicators_tf, direction):
        confirmation_score += 1

# Минимум 2 подтверждения из 3
if confirmation_score >= 2:
    allow_signal()
else:
    block_signal(reason="insufficient_timeframe_confirmation")
```

**Бонусы:**
- 1 подтверждение: +1 балл
- 2 подтверждения: +2 балла
- 3 подтверждения: +3 балла

#### 3. ADX Filter (Фильтр силы тренда)

**Назначение:** Фильтрует слабые тренды.

**Логика:**
```python
# Расчет ADX (Average Directional Index)
adx = calculate_adx(candles, period=14)

# Адаптивный порог по режиму:
# - TRENDING: ADX > 25
# - RANGING: ADX > 20
# - CHOPPY: ADX > 25

if adx < threshold:
    block_signal(reason="weak_trend_strength")

# Проверка направления (+DI vs -DI)
if direction == "LONG" and di_plus < di_minus + di_difference:
    block_signal(reason="wrong_trend_direction")
```

**Параметры:**
- `adx_threshold: 25.0` - Минимальная сила тренда
- `di_difference: 1.5` - Минимальная разница между +DI и -DI
- `adx_period: 14` - Период расчета ADX

#### 4. Pivot Points Filter (Фильтр точек разворота)

**Назначение:** Использует уровни поддержки/сопротивления.

**Логика:**
```python
# Расчет Pivot Points
pivot = (high + low + close) / 3
resistance = 2 * pivot - low
support = 2 * pivot - high

# Проверка близости к уровням
if direction == "LONG":
    if price <= support * 1.002:  # В пределах 0.2% от поддержки
        allow_signal()
        bonus_score += 2
elif direction == "SHORT":
    if price >= resistance * 0.998:  # В пределах 0.2% от сопротивления
        allow_signal()
        bonus_score += 2
```

**Бонусы:**
- TRENDING: +1-2 балла × 1.5
- RANGING: +2 балла × 1.5
- CHOPPY: +3 балла × 2.0

#### 5. Volume Profile Filter (Фильтр профиля объема)

**Назначение:** Анализирует распределение объема по ценам.

**Логика:**
```python
# Расчет Volume Profile
volume_profile = calculate_volume_profile(candles)

# Проверка зон высокой ликвидности
high_volume_zones = get_high_volume_zones(volume_profile)

if price in high_volume_zones:
    allow_signal()
    bonus_score += 2
```

**Бонусы:**
- TRENDING: +1-2 балла × 1.0
- RANGING: +2 балла × 1.5
- CHOPPY: +3 балла × 2.0

#### 6. Funding Rate Filter (Фильтр funding rate)

**Назначение:** Учитывает funding rate для Futures.

**Логика:**
```python
# Получение funding rate
funding_rate = await client.get_funding_rate(symbol)

# Для LONG: избегаем высокий положительный funding
if direction == "LONG" and funding_rate > 0.01:  # > 1%
    block_signal(reason="high_funding_rate_long")

# Для SHORT: избегаем высокий отрицательный funding
if direction == "SHORT" and funding_rate < -0.01:  # < -1%
    block_signal(reason="high_funding_rate_short")
```

**Параметры:**
- `max_funding_rate: 0.01` - Максимальный funding rate (1%)

#### 7. Liquidity Filter (Фильтр ликвидности)

**Назначение:** Проверяет достаточную ликвидность для исполнения.

**Логика:**
```python
# Получение order book
order_book = await client.get_order_book(symbol)

# Расчет ликвидности в пределах 0.5% от текущей цены
liquidity = calculate_liquidity(order_book, price_range=0.005)

# Минимальная ликвидность: $10,000
if liquidity < 10000:
    block_signal(reason="insufficient_liquidity")
```

**Параметры:**
- `min_liquidity: 10000` - Минимальная ликвидность (USD)

#### 8. Order Flow Filter (Фильтр потока ордеров)

**Назначение:** Анализирует поток покупок/продаж.

**Логика:**
```python
# Получение данных о сделках
trades = await client.get_recent_trades(symbol)

# Расчет потока
buy_volume = sum(t.volume for t in trades if t.side == "buy")
sell_volume = sum(t.volume for t in trades if t.side == "sell")

# Для LONG: нужен преобладающий поток покупок
if direction == "LONG":
    if buy_volume < sell_volume * 1.2:  # Покупки должны быть на 20% больше
        block_signal(reason="weak_buy_pressure")
```

#### 9. Volatility Regime Filter (Фильтр режима волатильности)

**Назначение:** Адаптирует параметры под волатильность.

**Логика:**
```python
# Расчет волатильности (ATR %)
atr_percent = calculate_atr_percent(candles, current_price)

# Определение режима волатильности
if atr_percent < 0.01:  # < 1%
    volatility_regime = "low"
elif atr_percent > 0.03:  # > 3%
    volatility_regime = "high"
else:
    volatility_regime = "normal"

# Адаптация параметров
if volatility_regime == "low":
    # Уменьшаем размер позиции
    position_size *= 0.5
elif volatility_regime == "high":
    # Увеличиваем SL
    sl_percent *= 1.5
```

#### 10. Momentum Filter (Фильтр импульса)

**Назначение:** Проверяет силу импульса движения.

**Логика:**
```python
# Расчет импульса (изменение цены за период)
momentum = (current_price - price_n_periods_ago) / price_n_periods_ago

# Минимальный импульс: 0.2%
if abs(momentum) < 0.002:
    block_signal(reason="weak_momentum")

# Проверка направления
if direction == "LONG" and momentum < 0:
    block_signal(reason="negative_momentum")
```

#### 11. Time Filter (Фильтр времени)

**Назначение:** Торговля в активные сессии (опционально).

**Логика:**
```python
# Определение торговой сессии
current_hour = datetime.utcnow().hour

# Азиатская сессия: 00:00-09:00 UTC
# Европейская сессия: 07:00-16:00 UTC
# Американская сессия: 13:00-22:00 UTC

if not is_trading_session(current_hour):
    block_signal(reason="outside_trading_hours")
```

**Примечание:** По умолчанию отключен (крипта торгуется 24/7).

#### 12. Balance Checker (Проверка баланса)

**Назначение:** Проверяет достаточность баланса.

**Логика:**
```python
# Получение баланса
balance = await client.get_balance()

# Минимальный баланс: $50
if balance < 50:
    block_signal(reason="insufficient_balance")

# Резерв: 10% от баланса
reserve = balance * 0.1
available = balance - reserve

# Проверка достаточности для позиции
if position_size > available:
    block_signal(reason="insufficient_available_balance")
```

---

## 🛡️ СИСТЕМЫ ЗАЩИТЫ

### Защиты от рисков

#### 1. Consecutive Losses Protection (Защита от последовательных убытков)

**Логика:**
```python
# Отслеживание последовательных убытков
if trade.pnl < 0:
    consecutive_losses += 1
else:
    consecutive_losses = 0

# Максимум 3 подряд убытка → STOP
if consecutive_losses >= 3:
    emergency_stop()
    block_all_trading()
```

**Параметры:**
- `max_consecutive_losses: 3` - Максимум последовательных убытков

#### 2. Daily Loss Limit (Дневной лимит убытков)

**Логика:**
```python
# Расчет дневного PnL
daily_pnl = sum(trade.pnl for trade in today_trades)

# Лимит: 10% от начального баланса
if daily_pnl <= -initial_balance * 0.10:
    emergency_stop()
    block_all_trading()
```

**Параметры:**
- `daily_loss_limit: 0.10` - 10% от баланса

#### 3. Daily Profit Lock (Блокировка дневной прибыли)

**Логика:**
```python
# Расчет дневного PnL
daily_pnl = sum(trade.pnl for trade in today_trades)

# Блокировка при +5% прибыли
if daily_pnl >= initial_balance * 0.05:
    stop_trading()
    logger.info("✅ Дневная прибыль зафиксирована: +5%")
```

**Параметры:**
- `daily_profit_lock: 0.05` - 5% от баланса

#### 4. Max Open Positions (Максимум открытых позиций)

**Логика:**
```python
# Подсчет открытых позиций
open_positions_count = len(active_positions)

# Максимум: 3 позиции
if open_positions_count >= 3:
    block_new_signals(reason="max_positions_reached")
```

**Параметры:**
- `max_open_positions: 3` - Максимум открытых позиций

#### 5. Max Position Size (Максимальный размер позиции)

**Логика:**
```python
# Расчет размера позиции
position_size = calculate_position_size(...)

# Максимум: 5% от баланса
max_size = balance * 0.05

if position_size > max_size:
    position_size = max_size
    logger.warning("⚠️ Размер позиции ограничен до 5%")
```

**Параметры:**
- `max_position_size_percent: 0.05` - 5% от баланса

#### 6. Max Loss per Trade (Максимальная потеря на сделку)

**Логика:**
```python
# Максимальная потеря: 2% от баланса
max_loss = balance * 0.02

# Расчет максимального размера позиции
# Если SL = 1.2%, то максимальный размер:
max_position = max_loss / 0.012

if position_size > max_position:
    position_size = max_position
    logger.warning("⚠️ Размер ограничен max_loss_per_trade")
```

**Параметры:**
- `max_loss_per_trade: 0.02` - 2% от баланса

#### 7. Max Margin Used (Максимальное использование маржи)

**Логика:**
```python
# Расчет использованной маржи
used_margin = sum(pos.margin for pos in active_positions.values())

# Максимум: 80% от баланса
max_margin = balance * 0.80

if used_margin + new_position_margin > max_margin:
    block_signal(reason="max_margin_reached")
```

**Параметры:**
- `max_margin_percent: 0.80` - 80% от баланса

#### 8. Drawdown Protection (Защита от просадки)

**Логика:**
```python
# Расчет просадки
drawdown = (initial_balance - current_balance) / initial_balance

# Максимум: 5% просадки
if drawdown > 0.05:
    emergency_stop()
    close_all_positions()
```

**Параметры:**
- `max_drawdown: 0.05` - 5% просадки

### Защиты позиций

#### 9. Break-even Stop Loss

**Логика:**
```python
# После достижения 1.0 ATR прибыли
if pnl_percent >= 1.0 * atr_percent:
    # Устанавливаем SL на уровень входа
    sl_price = entry_price
    # Защищаем от убытка
```

#### 10. Trailing Stop Loss

**Описание:** Динамически подстраивает стоп-лосс под движение цены (см. раздел [Управление позициями](#управление-позициями)).

#### 11. Partial Take Profit

**Описание:** Частичное закрытие позиции для фиксации прибыли (см. раздел [Закрытие позиций](#закрытие-позиций)).

#### 12. Time Stop

**Описание:** Максимальное время удержания позиции (см. раздел [Закрытие позиций](#закрытие-позиций)).

### Технические защиты

#### 13. Spread Filter (Фильтр спреда)

**Логика:**
```python
# Расчет спреда
spread = (ask_price - bid_price) / bid_price

# Максимум: 0.1%
if spread > 0.001:
    block_signal(reason="spread_too_wide")
```

**Параметры:**
- `max_spread: 0.001` - 0.1% спреда

#### 14. Min Order Value (Минимальная стоимость ордера)

**Логика:**
```python
# Расчет стоимости ордера
order_value = position_size * current_price

# Минимум: $30
if order_value < 30:
    block_signal(reason="order_value_too_small")
```

**Параметры:**
- `min_order_value: 30` - $30 USD

#### 15. Rate Limiting (Ограничение частоты запросов)

**Логика:**
```python
# Отслеживание количества запросов
requests_count = count_requests_in_last_minute()

# Максимум: 60 запросов/минуту
if requests_count >= 60:
    wait_until_rate_limit_reset()
```

**Параметры:**
- `max_requests_per_minute: 60` - 60 запросов/минуту

#### 16. Slippage Guard (Защита от проскальзывания)

**Логика:**
```python
# Проверка проскальзывания при исполнении
expected_price = signal.price
actual_price = order.fill_price

slippage = abs(actual_price - expected_price) / expected_price

# Максимум: 0.2% проскальзывания
if slippage > 0.002:
    logger.warning(f"⚠️ Высокое проскальзывание: {slippage:.2%}")
    # Логируем для анализа
```

**Параметры:**
- `max_slippage: 0.002` - 0.2% проскальзывания

#### 17. Liquidation Protector (Защита от ликвидации)

**Логика:**
```python
# Расчет ликвидационной цены
liquidation_price = calculate_liquidation_price(position)

# Безопасное расстояние: 5% от текущей цены
safe_distance = current_price * 0.05

if abs(current_price - liquidation_price) < safe_distance:
    logger.warning("⚠️ Близко к ликвидации!")
    # Рассматриваем закрытие позиции
```

**Параметры:**
- `liquidation_safety_distance: 0.05` - 5% от цены

---

## 🧮 РАСЧЕТЫ И МАТЕМАТИКА

### Расчет размера позиции

**Базовая формула:**
```python
# 1. Базовый размер из профиля баланса
base_size = get_base_size_from_profile(balance)

# 2. Корректировка на волатильность
atr_percent = calculate_atr_percent(candles, price)
volatility_multiplier = calculate_volatility_multiplier(atr_percent, regime)

# 3. Финальный размер
position_size = base_size * volatility_multiplier

# 4. Применение ограничений
position_size = min(position_size, max_position)
position_size = max(position_size, min_position)
position_size = min(position_size, balance * max_percent)
position_size = min(position_size, max_loss / sl_percent)
```

**Корректировка на волатильность:**
```python
# Базовый ATR % для режима
base_atr = regime_profile.base_atr_percent

# Текущий ATR %
current_atr = calculate_atr_percent(candles, price)

# Множитель
if current_atr < base_atr * 0.5:
    multiplier = min_multiplier  # 0.5x
elif current_atr > base_atr * 1.5:
    multiplier = max_multiplier  # 1.5x
else:
    multiplier = 1.0 + (current_atr / base_atr - 1.0) * 0.5
```

### Расчет маржи

**Для Futures с изолированной маржой:**
```python
# Размер позиции в USD
position_value = position_size * entry_price

# Маржа с учетом плеча
margin_required = position_value / leverage

# Maintenance margin (для защиты от ликвидации)
maintenance_margin = position_value * maintenance_margin_ratio

# Initial margin (требуемая маржа)
initial_margin = position_value * initial_margin_ratio

# Используем максимальное значение
margin = max(margin_required, initial_margin)
```

**Параметры:**
- `leverage: 5` - Плечо
- `maintenance_margin_ratio: 0.01` - 1% maintenance margin
- `initial_margin_ratio: 0.20` - 20% initial margin (для 5x плеча)

### Расчет PnL

**Для LONG позиции:**
```python
# Валовая прибыль
gross_pnl = (exit_price - entry_price) * size

# Комиссии
entry_fee = entry_price * size * fee_rate
exit_fee = exit_price * size * fee_rate
total_fees = entry_fee + exit_fee

# Чистая прибыль
net_pnl = gross_pnl - total_fees

# PnL в процентах
pnl_percent = net_pnl / (entry_price * size) * 100
```

**Для SHORT позиции:**
```python
# Валовая прибыль
gross_pnl = (entry_price - exit_price) * size

# Комиссии (аналогично)
total_fees = (entry_price + exit_price) * size * fee_rate

# Чистая прибыль
net_pnl = gross_pnl - total_fees

# PnL в процентах
pnl_percent = net_pnl / (entry_price * size) * 100
```

### Расчет Trailing Stop Loss

**Для LONG:**
```python
# Максимальная цена
highest_price = max(highest_price, current_price)

# Прибыль
profit_pct = (highest_price - entry_price) / entry_price

# Адаптивный множитель трейлинга
if profit_pct < 0.005:  # < 0.5%
    multiplier = 1.5
elif profit_pct < 0.015:  # 0.5-1.5%
    multiplier = 2.0
else:  # > 1.5%
    multiplier = 3.0

# Текущий трейлинг
current_trail = initial_trail + profit_pct * multiplier
current_trail = min(current_trail, max_trail)
current_trail = max(current_trail, min_trail)

# Цена стоп-лосса
trailing_sl_price = highest_price * (1 - current_trail)
```

**Для SHORT:**
```python
# Минимальная цена
lowest_price = min(lowest_price, current_price)

# Прибыль
profit_pct = (entry_price - lowest_price) / entry_price

# Адаптивный множитель (аналогично)

# Цена стоп-лосса
trailing_sl_price = lowest_price * (1 + current_trail)
```

### Расчет ATR (Average True Range)

**Формула:**
```python
# True Range для каждой свечи
true_range = max(
    high - low,  # Разница High и Low
    abs(high - prev_close),  # Разница High и предыдущего Close
    abs(low - prev_close)  # Разница Low и предыдущего Close
)

# ATR = среднее значение True Range за период
atr = sum(true_ranges[-period:]) / period

# ATR в процентах от цены
atr_percent = atr / current_price
```

**Параметры:**
- `atr_period: 14` - Период расчета ATR

### Расчет RSI (Relative Strength Index)

**Формула:**
```python
# Изменения цены
deltas = [price[i] - price[i-1] for i in range(1, len(price))]

# Прибыли и убытки
gains = [delta if delta > 0 else 0 for delta in deltas]
losses = [-delta if delta < 0 else 0 for delta in deltas]

# Средние значения
avg_gain = sum(gains[-period:]) / period
avg_loss = sum(losses[-period:]) / period

# Relative Strength
rs = avg_gain / avg_loss if avg_loss > 0 else float('inf')

# RSI
rsi = 100 - (100 / (1 + rs))
```

**Параметры:**
- `rsi_period: 14` - Период расчета RSI
- `overbought: 70` - Уровень перекупленности
- `oversold: 30` - Уровень перепроданности

---

## 🌐 РАБОТА С БИРЖЕЙ OKX

### Тип торговли

**Futures (USDT-Margined Perpetual Swaps):**
- Тип контракта: Perpetual Swap
- Валюта маржи: USDT
- Режим маржи: Isolated Margin (изолированная маржа)
- Плечо: 5x (настраивается)

### API Endpoints

#### 1. Получение рыночных данных

**Свечи:**
```python
GET /api/v5/market/candles
{
    "instId": "BTC-USDT-SWAP",
    "bar": "5m",  # 5-минутные свечи
    "limit": 50  # 50 последних свечей
}
```

**Текущая цена:**
```python
GET /api/v5/market/ticker
{
    "instId": "BTC-USDT-SWAP"
}
```

**Order Book:**
```python
GET /api/v5/market/books
{
    "instId": "BTC-USDT-SWAP",
    "sz": 20  # Глубина стакана
}
```

#### 2. Размещение ордеров

**MARKET ордер:**
```python
POST /api/v5/trade/order
{
    "instId": "BTC-USDT-SWAP",
    "tdMode": "isolated",  # Isolated margin
    "side": "buy",  # или "sell"
    "ordType": "market",
    "sz": "0.01",  # Размер позиции
    "posSide": "long",  # или "short"
    "lever": "5"  # Плечо
}
```

**LIMIT ордер:**
```python
POST /api/v5/trade/order
{
    "instId": "BTC-USDT-SWAP",
    "tdMode": "isolated",
    "side": "buy",
    "ordType": "limit",
    "sz": "0.01",
    "px": "50000.0",  # Цена
    "posSide": "long",
    "lever": "5"
}
```

#### 3. Получение позиций

```python
GET /api/v5/account/positions
{
    "instId": "BTC-USDT-SWAP"
}
```

**Ответ:**
```json
{
    "data": [{
        "instId": "BTC-USDT-SWAP",
        "pos": "0.01",  # Размер позиции
        "posSide": "long",  # Направление
        "avgPx": "50000.0",  # Средняя цена входа
        "markPx": "50100.0",  # Текущая маркировочная цена
        "upl": "1.0",  # Нереализованный PnL
        "margin": "100.0",  # Использованная маржа
        "liqPx": "45000.0"  # Ликвидационная цена
    }]
}
```

#### 4. Получение баланса

```python
GET /api/v5/account/balance
{
    "ccy": "USDT"
}
```

**Ответ:**
```json
{
    "data": [{
        "details": [{
            "ccy": "USDT",
            "eq": "1000.0",  # Equity (баланс)
            "availEq": "900.0"  # Доступный баланс
        }]
    }]
}
```

#### 5. Закрытие позиции

```python
POST /api/v5/trade/order
{
    "instId": "BTC-USDT-SWAP",
    "tdMode": "isolated",
    "side": "sell",  # Противоположная сторона
    "ordType": "market",
    "sz": "0.01",  # Размер позиции для закрытия
    "posSide": "long",
    "ccy": "USDT",
    "reduceOnly": true  # Только закрытие
}
```

### WebSocket подключение

**Подписка на тикеры:**
```python
# Подключение
ws = await websocket_connect("wss://ws.okx.com:8443/ws/v5/public")

# Подписка
subscribe = {
    "op": "subscribe",
    "args": [{
        "channel": "tickers",
        "instId": "BTC-USDT-SWAP"
    }]
}
await ws.send_json(subscribe)

# Получение данных
while True:
    message = await ws.receive_json()
    if message.get("arg", {}).get("channel") == "tickers":
        ticker_data = message["data"][0]
        current_price = float(ticker_data["last"])
```

### Комиссии

**Maker комиссия (LIMIT ордера):**
- 0.02% от объема сделки

**Taker комиссия (MARKET ордера):**
- 0.05% от объема сделки

**Расчет комиссий:**
```python
# Для LIMIT ордера (maker)
entry_fee = entry_price * size * 0.0002  # 0.02%
exit_fee = exit_price * size * 0.0002   # 0.02%
total_fees = entry_fee + exit_fee

# Для MARKET ордера (taker)
entry_fee = entry_price * size * 0.0005  # 0.05%
exit_fee = exit_price * size * 0.0005   # 0.05%
total_fees = entry_fee + exit_fee
```

---

## 🎯 АДАПТИВНЫЕ СИСТЕМЫ

### ARM (Adaptive Regime Manager)

**Назначение:** Автоматически определяет режим рынка и адаптирует все параметры торговли.

#### Определение режима

**TRENDING (Трендовый):**
```python
# Условия:
# - ADX > 25
# - Четкое направление (+DI значительно больше -DI)
# - Низкая волатильность разворотов

if adx > 25 and di_plus > di_minus + 1.5 and reversals < 3:
    regime = "TRENDING"
```

**RANGING (Боковой):**
```python
# Условия:
# - ADX < 20
# - Боковое движение (цена в диапазоне)
# - Низкая волатильность

if adx < 20 and range_width < price * 0.02 and volatility < 0.01:
    regime = "RANGING"
```

**CHOPPY (Хаотичный):**
```python
# Условия:
# - ADX > 25
# - Высокая волатильность
# - Много разворотов

if adx > 25 and volatility > 0.03 and reversals > 5:
    regime = "CHOPPY"
```

#### Адаптивные параметры по режиму

**TRENDING:**
```python
tp_percent = 2.4%  # 1.2x ATR
sl_percent = 1.2%  # 0.8x ATR
score_threshold = 6/12  # 50%
max_holding_time = 45 минут
partial_tp_fraction = 0.5  # 50%
partial_tp_trigger = 0.5%
```

**RANGING:**
```python
tp_percent = 1.8%  # 0.6x ATR
sl_percent = 0.8%  # 0.4x ATR
score_threshold = 4/12  # 33%
max_holding_time = 12 минут
partial_tp_fraction = 0.6  # 60%
partial_tp_trigger = 0.6%
```

**CHOPPY:**
```python
tp_percent = 2.0%  # 0.8x ATR
sl_percent = 1.5%  # 1.2x ATR
score_threshold = 7/12  # 58%
max_holding_time = 30 минут
partial_tp_fraction = 0.7  # 70%
partial_tp_trigger = 0.3%
```

### Adaptive Balance Manager

**Назначение:** Адаптирует размер позиций под баланс счета.

**Профили баланса:**
- SMALL (< $1,500): Базовый размер $50
- MEDIUM ($1,500 - $3,500): Базовый размер $100
- LARGE (> $3,500): Базовый размер $200

**Корректировка на волатильность:**
- Низкая волатильность: ×0.5
- Нормальная волатильность: ×1.0
- Высокая волатильность: ×1.5

---

## 📝 ЗАКЛЮЧЕНИЕ

Этот торговый бот представляет собой сложную систему автоматической торговли на фьючерсах OKX с множеством адаптивных механизмов, фильтров и защит. Бот работает в режиме **Futures (USDT-Margined Perpetual Swaps)** с изолированной маржой и плечом 5x.

**Ключевые особенности:**
- ✅ Адаптивное управление параметрами (ARM)
- ✅ 10+ типов фильтров сигналов
- ✅ 17+ систем защиты от рисков
- ✅ Trailing Stop Loss с адаптивным трейлингом
- ✅ Partial Take Profit для фиксации прибыли
- ✅ Интеграция с OKX REST API и WebSocket
- ✅ Подробное логирование всех операций

**Важно:** Бот находится в активной разработке и постоянно улучшается на основе анализа результатов торговли.

---

**Дата последнего обновления:** 30 ноября 2025  
**Версия документа:** 1.0





